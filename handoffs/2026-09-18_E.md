# Lane E — build day 6 — 2026-09-18

**The third fresh-attack lens returned `PASS` on `7eb617f` (0 blockers, 6 non-blocking, 3
sentence-precision items) and its regression twin `PASS` (0 blockers, 5 non-blocking), after
a round-1 `FAIL` / `FAIL` whose seven blockers (four distinct defects, three of them found by
both lenses) each have a test measured failing on the pre-fix tree and a re-execution of the
finding's own construction by the next lens, and a round-2 `PASS` / `PASS` that still found
the prevalence rule's "exactly when" false on a constructed shape. Thirty-six items are carried — nineteen
behaviours with no observer in the suite, five imprecise sentences or records, and twelve
design, policy and reachability items — each with a one-line repro below; two of them are
decisions for you (needs-from-Josh 1 and 2), and one (carried item 25, an `inf` reachable in
the O:E cell's `resample_sd`) must close before E7 wires calibration into `cmd_run`.**

Three commits on `main` on top of `4eb44f3`, not pushed (`ahead 3` of `origin/main` at
`7eb617f`; the commit carrying this note and the two lens-3 notes makes it 4): `b93e050`
(build), `a5a5ed8` (repair round 1), `7eb617f` (repair round 2). Suite at `7eb617f`,
measured now in both shells: **429 passed / 1 skipped / 1 xfailed**; `-m day6` **65 passed**;
`-m day5` **54 passed** (unchanged); `-m day1`..`day4` 59+1 / 40 / 29+1 / 182 (the day-5
note's counts). The `4eb44f3` baseline was 364 / 1 / 1 (lens-1 regression note, worktree),
so the difference is exactly the 65 day-6 tests (52 collected in `tests/test_calibration.py`,
9 in `tests/test_descriptive.py`, 4 in `tests/test_subgroups_carried.py`; the marker
count includes parametrisations). Lint, format, doctor clean in both shells. `git diff
--name-status 4eb44f3 HEAD -- tests/` is four `A` (those three files and the test-only
`tests/assembler.py`), no `M`, no `D`; the added test lines contain no `skip`, `xfail` or
`.only` (grep count 0, measured now). `git diff 4eb44f3 HEAD -- src/proofpack/cli.py
src/proofpack/io/ src/proofpack/stats/bootstrap.py src/proofpack/stats/subgroups.py
pyproject.toml` is 0 bytes, so lane A's files and every day-4/5 constant are as they were
(`MIN_UNITS_PER_STRATUM 2`, `MAX_FROZEN_VARIANCE_SHARE 0.2`, `MIN_USABLE_FRACTION 0.9`,
`DEFAULT_B 2000`, `FEW_EVENTS 5`, `LOW/VERY_LOW_PRECISION_UNITS 10/30`, read now). `grep -c
calibration src/proofpack/cli.py` is 0: nothing built today is reachable through the CLI.

---

## What landed

Every item of `spec/briefs/day6_E.md` is built, plus the day-5 note's carried items 16–19
and the day-5 inheritance list (1)–(7). The build commit is `b93e050`; the repair commits
are named where they changed an item. Oracle figures marked "now" were measured in this
session in the main tree (`proofpack.__file__` printed as the main tree's first); the rest
are quoted from the lens note named.

| # | Item | Module / function | Commit | Test ids (`tests/test_calibration.py` unless stated) | Oracle and measured figure |
|---|---|---|---|---|---|
| 1 | O:E ratio with a log-scale delta interval (`log_delta`); refused under clustering with the DEC-09 reason, single-stratum case bootstrap in its place | `stats/calibration.py`: `oe_log_delta_bounds`, `_oe_cell`, `_clustered_cell` | `b93e050`; `a5a5ed8` (the resampler switched from within-class to `clustered_flat`; class-units guard before any draw) | `test_f4_oe_ipa_and_both_eces_match_the_formulae_computed_here_to_1e_9`, `test_a_clustered_plan_refuses_every_analytic_interval_and_routes_to_the_cluster_bootstrap[declared/detected]`, `test_the_clustered_oe_and_intercept_intervals_are_the_percentile_of_a_one_stratum_bootstrap`, `test_one_case_holding_180_of_200_rows_is_insufficient_clusters_on_the_oe_and_the_fits`, `test_the_flat_resampler_holds_o_at_100_on_100_two_row_cases_of_one_event_each` | Now, F4: engine 1.1439466158 (0.7998, 1.6361) against the hand formula `O/E`, `exp(ln(O/E) ∓ z sqrt((1 − O/N)/O))` written in this session: deviation 0 / 0 / 2.2e-16. Lens-3 fresh-attack: own case bootstrap from the cell's generator on 80 shuffled cases of 1–5 rows reproduces the clustered bounds `(0.8913016702459742, 1.1394371138915673)` bit for bit |
| 2 | Calibration-in-the-large: own IRLS, offset model with the slope fixed at 1, Wald from the observed information | `irls_logistic`, `_fit_offset`, `_irls_cells`, `IrlsFit` | `b93e050`; `a5a5ed8` (clustered route); `7eb617f` (the four `irls_not_converged` exits named) | `test_f4_intercept_in_the_large_matches_statsmodels_glm_with_offset_to_1e_6`, `test_irls_not_converged_at_iteration_one_names_the_step_halving_exit_not_the_budget` | Now, F4: engine 0.4562304655 (−0.6290, 1.5414), SE 0.5536866259, 4 iterations; `statsmodels GLM(Binomial, offset=logit p).fit(tol=1e-10)` deviation 2.2e-16 on the estimate, 2.19e-9 on the SE |
| 3 | Slope and intercept: own IRLS, two parameters, Wald from the inverse observed information; typed outcomes `complete_separation`, `irls_not_converged`, `constant_score`, `single_class` | `irls_logistic`, `_fit_joint`, `_irls_cells` | `b93e050`; `a5a5ed8`; `7eb617f` | `test_f4_slope_and_intercept_match_statsmodels_glm_to_1e_6`, `test_separation_and_non_convergence_are_typed_reasons_never_inf_nan_or_a_traceback`, `test_a_constant_score_is_constant_score_for_the_slope_and_the_offset_intercept_still_fits`, `test_the_clustered_slope_interval_is_the_percentile_of_refits_on_resampled_cases` | Now, F4: slope 1.1945124796 (0.0935, 2.2955), intercept 0.4997716342 (−0.6665, 1.6661), SEs 0.5617458147 (slope) / 0.5950556263 (intercept), 7 iterations; `GLM(Binomial).fit(tol=1e-10)` deviation 0 / 1.7e-16 on the estimates, 7.21e-10 / 3.3e-10 on the SEs; statsmodels' **default** `.fit()` SEs are 1.99e-5 / 9.13e-6 away (it stops one IRLS iteration earlier); `fit(method="newton", tol=1e-12)` 1.1e-16 / 1.1e-16. Lens-1 fresh-attack: over 150 random cohorts the largest engine-vs-`tol=1e-10` SE gap was 1.03e-5 (n = 30), where `newton` equals the engine bit for bit — so the `tol=1e-10` oracle is an oracle at 1e-6, not an identity (recorded in `fixtures/f4_expected.json`) |
| 4 | Scores at exactly 0 or 1: clipped to `[1e-12, 1 − 1e-12]` for the two logit-scale models only; `n_clipped`, `clip_eps` in the block; flag `scores_clipped_for_logit` on the three IRLS Numbers | `CLIP_EPS`, `calibration_block` | `b93e050` | `test_scores_at_exactly_0_and_1_are_clipped_for_the_logit_models_only_and_counted` | The test pins the fitted values against statsmodels on the clipped vector and asserts that a 1e-6 clip moves the slope by more than 1e-4 (a value, not just the flag); O:E, Brier, ECE and the curve use the raw score |
| 5 | Decile curve: ten equal-mass bins, stable-sort tie rule, each bin's observed rate a `proportion_ci` cell (Wilson i.i.d.; cluster bootstrap with Wilson refused under a plan) with the day-5 tiers; N = 1..9 render ten bins with the empty ones `zero_denominator` | `equal_mass_bins`, `_decile_curve` | `b93e050`; `a5a5ed8` (N < 10 traceback fixed; the curve flag placed with `dataclasses.replace`, no in-place append) | `test_the_decile_tie_rule_is_stable_sort_order`, `test_dec10_every_decile_bin_number_is_wilson_or_the_cluster_bootstrap`, `test_an_n25_cohort_still_shapes_every_number_with_its_tier_annotation`, `test_a_cohort_of_one_to_nine_rows_renders_ten_bins_with_the_empty_ones_zero_denominator[none/declared × 1,2,5,9]`, `test_each_decile_bins_cluster_bootstrap_groups_the_bins_own_rows_by_case` | Now: N = 1 / 2 / 5 / 9 give ten bins with 9 / 8 / 5 / 1 `zero_denominator`, JSON without NaN. Lens-3 fresh-attack: hand Wilson per bin 2.2e-16 on 150 cohorts; every bin's `n`, `events`, `mean_pred` and observed rate exactly 0 from its own stable-sort deciles. Lens-2 fresh-attack: own case bootstrap of all ten bins on 200 two-row cases whose rows differ in score reproduces every rendered bound to 1.1e-16 |
| 6 | Brier, reference Brier, IPA; the Brier equals `stats.subgroups._brier_cell` on the whole cohort bit for bit; reference Brier typed `fixed_by_outcome_stratification` when `_prevalence_invariant` says the draws hold the prevalence | `_brier_cells`, `_prevalence_invariant` | `b93e050`; `a5a5ed8` (rule v1); `7eb617f` (the exact rule `R·pos_u − P·rows_u` constant per stratum) | `test_f4_brier_matches_sklearn_to_1e_9`, `test_the_overall_brier_equals_the_subgroup_module_brier_cell_on_the_whole_cohort`, `test_brier_ref_is_fixed_under_outcome_stratification_and_varies_when_case_sizes_differ`, `test_brier_ref_is_fixed_when_every_unit_in_a_stratum_carries_the_same_class_counts`, `test_the_prevalence_rule_agrees_with_the_resamplers_own_draws_on_eleven_shapes` | Now, F4: Brier 0.158755 (0.0916, 0.2416), `sklearn.metrics.brier_score_loss` deviation 0; reference 0.24 `fixed_by_outcome_stratification`; IPA 0.3385208333 (−0.0290, 0.6148) at B = 200, hand IPA deviation 0. Lens-3 fresh-attack: the prevalence rule agrees with an exhaustive enumeration of every draw on 2,567 random shapes (717 invariant), 0 mismatches; lens-3 regression: 6,917 shapes, 0 mismatches |
| 7 | ECE under two stated schemes (right-closed equal-width bins on `arange(11)/10`; the decile bins), `supplementary`, **no interval** with the reason string beside it | `equal_width_bin_index`, `ece`, `_ece_entry`, `_ECE_CI_WHY` | `b93e050`; `a5a5ed8` (the reason string rewritten: it no longer asserts a bootstrap fact) | `test_the_two_ece_schemes_differ_where_the_partitions_differ_and_each_states_its_scheme`, `test_the_equal_width_bins_are_right_closed_with_the_first_bin_closed_at_zero`, `test_the_equal_width_edges_are_arange_over_ten_and_not_linspace` | Now, F4: 0.1955 / 0.1955, hand equal-width ECE deviation 0. R2's 0.1255 (5 bins) / 0.1955 (10) reproduce only with right-closed bins (left-closed gives 0.1645 / 0.2405 — lens-1 regression). F4 does **not** separate the two schemes; `make_cohort(300)` does (the test) |
| 8 | The 200/200 annotation: `below_200_events_or_nonevents` on every bin's rendered Number and `curve_flag` with the counts; strict on both sides; never a suppression | `CURVE_MIN_EVENTS`, `curve_flag` | `b93e050`; `a5a5ed8` (test renamed to what it inspects; the flag on `number`, not the companion); `7eb617f` (i.i.d. companion pinned) | `test_the_200_200_annotation_is_strict_on_both_sides_and_the_flagged_fits_keep_their_intervals[199-300 / 200-300 / 300-199 / 300-200 / 199-199]` | Construction: 199 flagged, 200 not, either class; on the flagged side the O:E is `log_delta` with bounds and the three fits `irls_wald` with bounds (the lens-1 regression's mutant that suppressed the O:E below 200 now dies here: `3 failed, 2 passed`) |
| 9 | Suppression: `logit` / `other` → block `None` with `score_not_probability`; `lower_is_positive` → `score_not_positive_class_probability`, never `1 − p` | `suppression_reason`, `CalibrationResult.as_document` | `b93e050` | `test_score_type_logit_and_other_give_a_null_block_with_the_typed_reason[logit/other]`, `test_a_lower_is_positive_probability_is_refused_with_its_own_reason_not_transformed` | Block `None`; reason in `NOT_ESTIMABLE_REASONS`, in the schema enum and in the sibling key `calibration_suppressed_reason` (decision 1 below) |
| 10 | Census and invariants: every proportion Wilson or the cluster bootstrap; every analytic companion refused under a plan; every Number re-validates with a CI or a typed reason; no verdict word | census tests above; `test_every_number_in_the_day6_output_has_a_ci_or_a_typed_reason`, `test_no_verdict_word_appears_in_any_key_or_engine_string_of_the_day6_output`, `test_a_clustered_plan_refuses_every_analytic_interval_...` | `b93e050`; `a5a5ed8` (`units_per_stratum` asserted per resampler) | as named | 36 Numbers per computed block (7 × 2 + 2 ECE + 10 × 2; the test's own count); lens-1 fresh-attack: 17 companions refused under a plan, no `wilson` / `log_delta` / `irls_wald` / `bootstrap_percentile` anywhere under a plan; all three fresh-attack lenses walked every constructed block for `Number(**d)`, methods in `METHODS`, flags in `FLAGS`, no verdict token |
| 11 | `table1`, `flow`, `missingness` | `stats/descriptive.py`: `table1_block`, `table1_levels`, `flow_block`, `missingness_block` | `b93e050`; `a5a5ed8` (docstring says what leaves: counts and shares keyed by column names and level labels); `7eb617f` (numeric site labels pinned) | `tests/test_descriptive.py` (9 collected, incl. `test_table1_and_missingness_keys_are_column_names_and_level_labels_and_nothing_per_row`) | Construction: 3 missing labels / 2 missing scores / 4 indeterminates over 60 rows → exactly those counts, 51 analysed; every attribute's shares sum to 1 within 1e-12 with the Unknown level; Table 1 level `n` equals the subgroup rows' `n`; thirty `site` values `"1000"`..`"1029"` become thirty keys with `n: 1` |
| 12 | Schema and enums: `calibrationBlock`, `calibrationSuppressedReason`, `eceEntry`, `decileBin`, `curveFlag`, `table1Block`, `table1Levels`, `missingnessBlock`, `flow` extended; six reasons and four flags added | `schema/output_schema_v1.json`, `stats/number.py` | `b93e050` | `test_the_enums_added_today_agree_between_code_and_schema`, `test_the_schema_rejects_a_block_that_drops_the_scheme_or_smuggles_a_reason_into_a_present_block`, `test_the_three_blocks_validate_against_their_schema_fragments_and_carry_no_number` | Now: `METHODS` 18 / `FLAGS` 15 / `NOT_ESTIMABLE_REASONS` 21 equal the schema's three enums as sets; the lens-1 regression read the schema diff as a pure tightening (bare object/null → `oneOf` fragments with `additionalProperties: false`) |
| 13 | Fixture F4 (R2 §9 rows verbatim) and its expected values with the commands that produced them; `r_rms_val_prob: [pending]` for the day-9 F13b capture | `fixtures/f4_calibration.csv`, `fixtures/f4_expected.json` | `b93e050`; `a5a5ed8` (the offset note corrected — default and `tol=1e-10` fits both stop at 4 iterations there; the joint object carries its own note) | `test_f4_fixture_rows_and_recorded_values_match_r2_section_9`, `test_the_sentences_the_day6_lenses_falsified_are_gone_from_the_shipped_text` | All three regression lenses regenerated the CSV body from the header's recipe byte-identical and re-ran every recorded statsmodels / sklearn / hand value to 0.0 (the largest R2 4-dp gap 4.7e-5, under the test's 5e-5) |
| 14 | No CLI wiring: the whole day-6 document exists only in the test-only assembler | `tests/assembler.py` (`assemble(cols, crit, policy)`) | `b93e050` | `test_the_assembled_document_validates_against_the_output_schema` (the brief's acceptance test: i.i.d., declared-clustered, detected-clustered and logit-suppressed documents) | `1 passed` now in both shells; 0 schema errors on the four documents; the detected document carries W13 and `flow.clustering_route: detected` |
| 15 | Mutation sweep, day-6 list (DEC-12 ii) | `scripts/mutation_sweep.py` (`day=6`, `--marker day6`; now also copies `scripts/` so the sentences test can read `coverage_calibration.py`) | `b93e050` (20); `a5a5ed8` (+10); `7eb617f` (+4, two re-targeted) | none (a script, run and recorded) | **34 planted, 34 killed, 0 survived** now in both shells (Re-run these); 20 / 20 at `b93e050` (lens 1), 30 / 30 at `a5a5ed8` (lens 2) |
| 16 | Calibration coverage script (DEC-08, DEC-18 c) and the T7 calibration section | `scripts/coverage_calibration.py` v1; `design/conventions_T7.md` "Calibration (build day 6)" | `a5a5ed8` (script); `7eb617f` (T7 section, annotation table, coverage table) | none (a script and prose; every figure re-measured now, see the coverage paragraph under round 1) | 24 clustered and 12 i.i.d. cells equal T7 in both shells now |
| 17 | Day-5 carried items 16–19 | `tests/test_subgroups_carried.py` (marker `day6`, so `-m day5` stays at 54) | `b93e050`; `a5a5ed8` (item 17b) | `test_item16_the_rendered_two_sided_interval_is_the_percentile_of_per_resample_differences`, `test_item17_a_bootstrap_at_level_0_90_is_strictly_inside_its_0_95_interval_and_says_so`, `test_item17b_the_difference_bootstrap_takes_its_bounds_and_its_frozen_rule_at_the_level_asked`, `test_item19_frozen_sides_reads_the_intervals_own_level_not_the_interquartile_range` | Item 16: an independent two-sided bootstrap written in the test reproduces the rendered bounds bit for bit and both one-sided mutants die (lens-1 regression re-planted: `values_a − values_b.mean()` and `values_a.mean() − values_b` killed); item 18 rides on it (`n = min(n1, n2)`, `resample_sd == diffs.std(ddof=1)`); item 17: the two `subgroups.py` sites lens 1 planted (`_frozen_sides(..., DEFAULT_LEVEL)`, `percentile_bounds(usable, DEFAULT_LEVEL)`) die in 17b (lens-2 regression: `assert () == ('a',)`, `assert (0.7089…) == (0.7164…)`); item 19: an 80/20 side is not frozen at 0.95 and is at 0.5. Unreachable through the CLI today (no declarable level) — stated as the mutant kills |

**Acceptance line** (the brief: "1e-6/1e-9; both ECE schemes; suppression reason rendered").
The F4 tests assert the statsmodels estimates and SEs at 1e-6 (`tests/test_calibration.py`
lines 174–260 per the lens-3 regression) and the sklearn / hand values at 1e-9; both ECE
schemes render with `scheme`, `bins` and `edges` and differ on `make_cohort(300)`; the
suppression reason renders as `calibration_suppressed_reason` beside a `None` block and is
asserted by name for `logit`, `other` and `lower_is_positive`. The O:E, the three IRLS fits,
the Brier, reference Brier, IPA and both ECEs each have an oracle outside the repo in the
day-6 tests (statsmodels GLM with and without offset, sklearn, or a hand formula written in
the test); the decile bins' Wilson values ride on day 2's `proportion_ci` oracle and were
re-derived by hand by all three fresh-attack lenses (largest gap 2.2e-16). The measured gaps
are in the table.

## What was cut and why

Nothing from the brief, and the orchestrator recorded no cut. Two things adjacent to it are
not built, by decision:

- **DEC-18 (a), the design-effect-adjusted Wilson for clustered proportions**, is not built:
  DEC-18 (15 Sept) says it is "NOT built today"; it is a new method string with its own
  coverage run — a build-day item on its own. Until it lands, the decile bins under a plan
  take the day-5 route and the day-5 tier annotations (DEC-18 c), and the day-5 coverage
  table's shortfall at few cases and high proportions applies to them; nobody has measured
  the clustered decile-bin intervals against the bar (all six lens notes say so under "could
  not check").
- **Calibration by group** (day-5 inheritance item 5) is not built; the fairness block's
  `calibration_by_group` stays `not_computed_this_run` and `fairnessBlock` is unchanged.

## What did not land, and why — the 36 carried items

Each reproduces on `7eb617f` unless marked otherwise. "Survives" means the mutant passes
`-m day6` (or the full suite where stated) on this tree, as measured by the lens named; on
each, at least one lens checked the shipped behaviour by construction and found it right.
The lens that owns the repro is named so its note can be opened for the full construction.

**A. Behaviours with no observer in the suite (a regression would ship green).**

| # | Item | Why carried | Repro (one line) | Owner |
|---|---|---|---|---|
| 1 | `_decile_curve` passing `plan=None`: a bin whose ids do not repeat renders Wilson, route `none`, under a declared plan | Each of 1–18 needs its own observer test; the repair rounds fixed blockers and false sentences | plant `plan=None` in `_decile_curve`; `-m day6` passes | lens 1 M3 |
| 2 | `_Ctx.tier` dropping the few-events rule on i.i.d. rows: n ≥ 30 with < 5 events loses `very_low_precision` | Same | drop the events argument in `_Ctx.tier`; full suite passes | lens 1 M6 |
| 3 | The i.i.d. IPA resample using the full-cohort Brier: the interval collapses to `boundary_estimate` and nothing asserts the i.i.d. IPA has one | Same (the F4 IPA bounds `(−0.0290, 0.6148)` and its flags are asserted by no test — lens-3 regression N1) | plant the full-cohort Brier in the IPA draw; full suite passes | lens 1 M7 |
| 4 | `IRLS_TOL = 1e-3`: F4 moves 1.2e-7 / 4.7e-8 (inside the 1e-6 gate); worst move over 300 random cohorts 5.5e-6 | Needs an oracle on random cohorts at a tolerance the F4 gate cannot see | plant `IRLS_TOL = 1e-3`; full suite passes | lens 1 M8 |
| 5 | `_clustered_cell` dropping the tier flags: a clustered cohort under 30 cases loses `very_low_precision` on seven Numbers | Same | drop `precision_flags` in `_clustered_cell`; full suite passes | lens 1 M9 |
| 6 | 200/200 counted on cases instead of rows under clustering | The code's choice (rows decide; `event_cases` reported beside) is not pinned — open question 1 | count on cases; full suite passes | lens 1 M11 |
| 7 | `flow.n_sites` counting `Unknown/missing` as a site: one blank site gives 3 → 4 | Same | plant the count without the exclusion; full suite passes | lens 1 M13 / lens 2 L2-7 |
| 8 | `table1.dev` tabulated over `~mask` instead of the `dataset = dev` rows: differs when excluded non-dev rows exist beside dev rows | Same | plant `~mask`; full suite passes | lens 1 M14 |
| 9 | The DEC-09 companion on the O:E and the fits carrying `est=None` (0.625 → null) | Same | plant `est=None` in `_unavailable` for the companion; full suite passes | lens 2 L2-4 |
| 10 | A refused clustered draw reporting `analytic_status: refused_clustered` instead of `unavailable` | Same | 40 one-row cases, 2 events: O:E `degenerate_resamples` with the wrong status; full suite passes | lens 2 L2-5 |
| 11 | `scores_clipped_for_logit` dropped from the IRLS Numbers under a clustered plan (100 two-row cases, 20 rows at 0.0 / 1.0) | Same | drop the flag on the clustered route; full suite passes | lens 2 L2-6 |
| 12 | `calibration_from_table` passing `table.case_id` unsliced by the mask: 40 rows, one missing label → `ValueError: cluster_ids must align row-for-row` instead of a block of 39 | Same | plant the unsliced column; full suite passes | lens 2 L2-8 |
| 13 | `curve_flag.event_cases` counted over the non-event rows: 60 events / 140 non-events → 52 becomes 92 | Same | plant the wrong mask; full suite passes | lens 2 L2-9 |
| 14 | `_Ctx.tier` applying the few-events rule under clustering (the clustered O:E on 60 one-row cases with 3 events would gain `very_low_precision`) | The observer does not exist; whether it *should* apply is needs-from-Josh 2 (item 31) | plant `events=` under a plan; full suite passes | lens 2 L2-10 / lens-3 regression N5 |
| 15 | `_prevalence_invariant` slicing `per_unit[start : start + n_units − 1]` (each stratum's last unit never inspected): 4 positive cases with the two-row case last beside 10 one-row negatives → `fixed_by_outcome_stratification` where the draws take 5 distinct prevalences | The eleven shapes all place their differing units earlier in the stratum; one shape whose differing unit is last closes it | plant the slice; full suite passes | lens 3 L3-1 |
| 16 | `_irls_cells` using `z_for(DEFAULT_LEVEL)` instead of `z_for(ctx.level)`: F4 at `level=0.90` renders `(−0.6290, 1.5414)` under the label `ci_level: 0.9` where the hand value is `0.45623 ± 1.644854 × 0.553687 = (−0.4545, 1.3670)` | The day-5 item-17 defect class (a level-blind interval) on the three Wald cells; unreachable through the CLI today; one assertion (F4 at 0.90, three bounds) closes it | plant `DEFAULT_LEVEL`; full suite passes | lens 3 L3-2 |
| 17 | `mean_pred` as the bin's median: F4's bins are evenly spaced so mean = median there | One random cohort's bin means asserted closes it (lens 3's own re-derivation found 0 deviation on 150 cohorts) | plant the median; full suite passes | lens 3 L3-3 |
| 18 | `SEPARATION_TOL = 1e-4`: `p ∈ {1e-5, 1 − 1e-5} × 30`, `y = p > 0.5` converges at iteration 1 to `−2.3e-12 (−80.02, 80.02)` `irls_wald`; the mutant types it `complete_separation` | Both typed; a constant with no observer at its own value | plant `1e-4`; full suite passes | lens 3 L3-4 |
| 19 | The docstring's "a resample on which the IRLS does not converge is `nan` to the resampler and counted against `MIN_USABLE_FRACTION`" has no test; the lens-1 regression held it on 40 two-row cases (slope `usable 172` → `degenerate_resamples`) and the lens-2 regression on 40 one-row cases with 2 events (slope `usable 175`, joint intercept `176`) | One assertion (`usable < 200` on either construction) | build the lens-1 regression N6 cohort; read `usable_resamples` | lens-1 regression N6 |

**B. Sentences and records that are imprecise, not false.**

| # | Item | Why carried | Repro | Owner |
|---|---|---|---|---|
| 20 | `test_the_prevalence_rule_agrees_with_the_resamplers_own_draws_on_eleven_shapes` docstring says "the five shapes marked `True`"; `PREVALENCE_SHAPES` marks seven `True` and four `False` (the assertions are per shape and right) | A count in a sentence | `python -c "import sys; sys.path.insert(0,'tests'); from test_calibration import PREVALENCE_SHAPES as S; print(sum(e for *_, e in S))"` → `7` | lens 3 N1 |
| 21 | T7's "Which annotations each calibration cell carries" names 60 one-row cases with 3 events at `p = 0.08`, on which the slope and joint intercept are `constant_score` with `flags: []`, not the `['very_low_precision']` the row's measurement column reads (the O:E and intercept-in-the-large do read it); the Brier row's `[]` is a tier list on a refusal (`boundary_estimate`); no test feeds that cohort | The rule the table states is what the code does; the measurement column is not what that input produces, and Josh's needs-2 decision rests on it | `probe/t7_check.py` in the lens-3 fresh-attack scratchpad; `grep -n "0.08" tests/test_calibration.py` is empty | lens 3 N2 / lens-3 regression N1 |
| 22 | T7 "Typed IRLS outcomes" says two tests "feed the separated, one-crossing and all-1.0 inputs and serialise each block without NaN"; the one-crossing input goes to `irls_logistic` directly and no block is built from it | Say "the two blocks" | `tests/test_calibration.py` lines 983–988 per the lens | lens 3 N5 |
| 23 | The module docstring's "the `MAX_STEP_HALVINGS` halvings of one Newton step all raise the deviance ... 1 on 50 rows all at `p = 1.0` with 12 events": on that input the Newton step is `−7.6e11`, 30 of the 31 candidates have an **infinite** deviance and the last a finite 16322.67 above `dev0 = 2099.95`; the exit fires on `isfinite and <= dev`, so the mechanism is non-finiteness, which the sentence does not name, and "the 30 halvings" omits the full step | Wording, not behaviour; the test asserts `iterations == 1`, the reason and the phrase, not the candidate deviances | re-run `_fit_offset` on that input and print the candidate deviances | lens-3 regression N2 |
| 24 | `_oe_cell`, `_decile_curve`, `_ece_entry`, `_expit`, `_deviance`, `_fit_offset`, `_fit_joint`, `_cell_key`, `_unavailable`, `_cell_dict`, `_equal_width_rows`, `_attribute_labels`, `_missing_mask` have no docstring | Documentation; every public function and both module docstrings name their oracle or formula | — | lens-1 regression N5 |

**C. Design, policy and reachability items.**

| # | Item | Why carried | Repro | Owner |
|---|---|---|---|---|
| 25 | **`oe.bootstrap.resample_sd` is `inf` when a draw's O:E exceeds about 1e154** (numpy squares the draws in `usable.std(ddof=1)`); the percentile bounds stay finite; `json.dumps(block, allow_nan=False)` raises and `cli.py`'s default `json.dumps` would write the non-JSON token `Infinity`; the schema does not catch a Python `inf`. **Reproduced now at `7eb617f`:** 57 rows at `1e-160` beside 3 at `0.5`, 20 events, 30 two-row declared cases → `est 13.33`, `resample_sd inf`, `json.dumps(..., allow_nan=False)` raises `Out of range float values are not JSON compliant: inf` | Day 4's `bootstrap_percentile` is the site (`sd = None` or the typed `boundary_estimate` route when the sd is not finite); not reachable through `cmd_run` today, and a probability column below 1e-154 is not one a real model produces — but it **must close before E7 wires the block**, with the 57-plus-3 input as the regression test. First item of tomorrow's list | `probe/inf_thresh.py` in the lens-3 fresh-attack scratchpad, or the construction above | lens 3 N3 |
| 26 | A blank `case_id` in one row (`""`, `None`, `"NA"`) passes `schema.validate` (object column, `None`) and `gates.ingest` (exit 2, W10) and then raises `TypeError: '<' not supported between instances of 'str' and 'NoneType'` from `numpy.unique` in `flow_block`, `calibration_from_table` **and** day 5's `subgroup_analysis` | Pre-existing (the day-5 path raises identically); `cmd_run` stops at ingest today; E7 needs an S-code halt or a typed treatment at ingest — `io/` and `gates` were out of this day's scope | `make_cohort(30, with_case_id=True)`, set `case_id[0] = ""`, validate, call `flow_block` | lens 2 N4; re-verified lens-3 regression |
| 27 | `flow` does not reconcile when the table carries `dataset = dev` rows: 60 rows with 10 dev (one with a missing label) → `rows_read 60`, `excluded_missing_label 1`, `analysed 49`, which sums to 50; the dev rows appear only as `table1.n_dev` and the dev row's missing label is counted nowhere | D1 §4.2's flow keys have no dev entry (a spec gap); one key (`dev_rows`) and a D1 amendment close it — a build item, not a repair | `calibration_from_table` on the descriptive test's cohort with ten `dataset = dev` rows; sum the flow | lens 1 N5; re-verified lens 2 |
| 28 | `calibration_from_table` raises `ValueError: calibration needs a score column` on a `y_pred`-only table (`score.type: probability` declared, no score column — allowed by S01) | Not reachable until E7 wires the call; there it becomes a typed suppression or a gate, not a raise | a `y_pred`-only cohort through `calibration_from_table` | lens 1 N6; re-verified lens 2 |
| 29 | `ece_equal_mass_10.edges` under ties is bin minima plus the last maximum, not a partition: 95 rows over nine tied values give `[0.1, …, 0.5, 0.5, 0.6, …, 0.9, 0.9]`; 100 identical scores give eleven `0.5`s and an equal-mass ECE of 0.08 beside an equal-width ECE of 0 | `decile_tie_rule` and T7 state the scheme; renaming the key is a schema change | 95 rows over nine tied values | lens 1 N8; re-verified lens 2, 3 |
| 30 | `calibration` and `calibration_suppressed_reason` (and `missingness`) are not top-level `required` in the schema: a document omitting both validates | Pre-existing for `calibration`; the assembled-document test asserts presence; E7's assembly is where the document's required set is decided | drop both keys from an assembled document; validate | lens-1 regression N7 |
| 31 | **Annotation policy on the calibration cells.** (a) The few-events tier (`very_low_precision` below 5 events) rides on the O:E and the three fits on i.i.d. rows only; the Brier / reference Brier / IPA and every clustered cell carry the units tiers alone, so within one block the O:E says `very_low_precision` and the Brier beside it does not (lens-2 fresh-attack, 60 one-row cases, 3 events). Day 5's `_brier_cell` made the same choice and the cross-check test forces the calibration Brier to match it. (b) `imprecise` (R2 §3.3: Wilson half-width > 0.10, written for a proportion) rides on the Brier and on the IPA — a quantity on `(−∞, 1]` — through `Number.with_precision_flags`: F4's IPA carries `['very_low_precision', 'imprecise']` (measured now) | An annotation-rule change on a gate (and on day 5's `_brier_cell`); needs-from-Josh 2. T7 now states which cells carry which rule and what `imprecise` means off the proportion scale | the T7 table's input (item 21); F4's IPA flags | lens 2 N3, N7; lens-3 regression N1 |
| 32 | The committed coverage process puts the case effect **inside** the score, so given the scores the rows are independent Bernoulli draws and the i.i.d. analytic interval is valid on it (its column reads 0.913–0.967): the table measures the single-stratum bootstrap's small-sample behaviour, not its handling of within-case correlation the score does not carry. Lens 2 measured a second process (`b_case ~ N(0, 2.0)` not in the score, 30 × 10 and 50 × 8 cases): i.i.d. analytic 0.645–0.755 on the O:E and both intercepts, the clustered route 0.930–0.970 | T7 carries the lens's figures marked as the lens's; a second DGP in `coverage_calibration.py` is a build item with its own run (about 5 minutes per seed at R = 200 per the lens) | `probe/cov_resid_harsh.py` in the lens-2 fresh-attack scratchpad | lens 2 N7 |
| 33 | **Which IRLS exit fired is invisible in the output.** A customer reading `iterations: 1` beside `irls_not_converged` has to read the docstring to learn the step-halving exit fired; under quasi-complete separation (four rows at 0.5 with both labels) the joint fit runs the full 100 iterations to `irls_not_converged` with a finite deviance, and the docstring names only `complete_separation` for a non-existent MLE | `detail.exit` is an output-shape change (the schema's `detail` is open, but the renderer and D1 have not seen the key); quasi-separation as its own reason is a method choice. Open question 5 | the all-1.0 cohort (`iterations 1`); four rows at 0.5 with both labels (100 iterations) | repair 2 carried; lens 2 N7; lens-3 regression N4 |
| 34 | `brier_ref` reads `fixed_by_outcome_stratification` on a resampler the class-units guard refuses: one case holding 180 of 200 rows → `brier` and `ipa` `insufficient_clusters`, `brier_ref` `fixed_by_outcome_stratification` with `est 0.2499` (identical at `a5a5ed8`; at `b93e050` it was `insufficient_clusters`) | Both reasons are true of the cell and the estimate is the plain `π(1 − π)`; the 180/200 test's key list omits `brier_ref`, so the precedence is a builder's choice no note recorded — record it in T7 | the 180-of-200 construction; read `brier_ref` | lens 3 N6 |
| 35 | **One coverage cell of twenty-four below the bar:** the intercept-in-the-large on 60 cases × 3 rows at seed 2026 reads **0.897** (0.930 at seed 7; Monte-Carlo SE about 0.017; the analytic Wald on the same cohorts 0.913) — re-measured now in both shells | Rendered with its tier annotation and recorded in the module docstring and T7 rather than refused on a threshold set from one cell; your decision (needs-from-Josh 1); DEC-18 (c) applies meanwhile | `python scripts/coverage_calibration.py --full`, the 60 × 3 `intercept_large` row | repair 1; lens-2 regression N4 |
| 36 | `stats.subgroups._brier_cell` (day 5) keeps `not_computed_this_run` with `score_type` in detail for a non-probability score (its day-5 test asserts it), so the document has a second spelling beside `score_not_probability` | Not changed today to keep the day-5 files untouched; one line in `subgroups.py` and one assertion in `tests/test_subgroups.py`. Open question 2 | a `logit` score through `subgroup_analysis` and through `calibration_block` | build decision 16 |

Not carried: lens-1 M12 (`oe_log_delta_bounds` ignoring `level`) and M16 (`SEPARATION_TOL =
1e-2`) — equivalent today per the lens (no level but 0.95 is declarable; a crossing pair
forbids the tolerance from mattering); lens-3 L3-5 (`IRLS_TOL × 1e4`) — estimates move by at
most 9.8e-13 over 50 cohorts, equivalent at the output; the observer tests that pass on the
pre-fix tree by design (lens-1 regression N2; lens-2 regression N6; lens-3 regression's
"four more") — each mutant- or sentence-anchored in the repair notes and re-planted by the
next lens; lens-2 regression N2 (the i.i.d. bin companion losing the 200/200 flag at
`a5a5ed8`) — pinned in `7eb617f` and recorded in T7; lens-2 regression N3 (no T7 section) —
written in `7eb617f`; lens-3 regression N3 and N4 (a note sentence generalising past three
shapes; "four sites" is five) — scratchpad notes, not shipped text; the build note's decisions
7, 11 and 12 — superseded by the repair notes.

## What the lenses found

Every lens ran in a detached worktree with `PYTHONPATH=<worktree>/src` forced and
`proofpack.__file__` printed from the worktree before any count was trusted (the lens-3
fresh-attack also printed the unforced form, which imported the main tree — the trap the
brief names). Blocker titles are quoted as the lenses wrote them.

### Round 1, at `b93e050` — fresh-attack `FAIL` (4 blockers, 8 non-blocking, 4 sentence violations beside B4); regression `FAIL` (3 blockers, 7 non-blocking)

> **B1.** "the clustered O:E, intercept-in-the-large and joint-intercept intervals cover the
> truth 61–79 % of the time" — the cells resampled cases *within outcome class*, which holds
> `O = sum(y)` at the same integer in every draw with one or two rows per case (`distinct O =
> [91]` on 200 rows as 200 one-row and as 100 two-row cases); on the lens's process (case effect
> N(0, 0.8), row noise N(0, 0.9), truth O:E = 1) at 300 replicates, B = 200: O:E **0.630 /
> 0.670 / 0.787** on 200 × 1, 200 × 2, 60 × 3 cases, intercept-in-the-large **0.613 / 0.673 /
> 0.793**, joint intercept **0.725** (200 × 2), against a bar of 0.90; the slope 0.907–0.947.
> One cohort at B = 2000: engine `(1.091, 1.271)` where the analytic gives `(1.010, 1.367)`
> and an unstratified case bootstrap `(1.009, 1.330)`. Mutant M2 (the resample's `O` replaced
> by the cohort's) survived the full suite: every test case was built by `np.repeat`.
>
> **B2 / RG-B1.** "`calibration_block` raises for 1 <= N <= 9" — `mass_edges` took `.max()` of
> an empty last bin (`ValueError: zero-size array to reduction operation maximum which has no
> identity`) where the docstring said the empty bin is `zero_denominator`; a 400-cohort fuzz
> hit it 95 times (regression lens).
>
> **B3 / RG-B3.** "two-row mixed cases: reference Brier refused as `boundary_estimate`, docstring
> false" — `_prevalence_invariant` read any mixed stratum as varying, so the cell was
> bootstrapped, every draw held the prevalence, and the rendered reason was the one decision 11
> said it avoids (48 and 44 mixed cases of 100 at two seeds: **1** distinct prevalence in 100
> draws).
>
> **B4 / RG-B2.** "an engine string in the output JSON asserts a false statistical fact" — the
> string `ci_not_computed_because` said the bootstrap ECE "overstates it in every resample";
> **145 / 1000** resamples sat below the point on the lens's first cohort and **371 / 1000** on
> `make_cohort(300)` (regression lens); the bootstrap *mean* was above the point on every input.

Fixed in `a5a5ed8`: B1 by resampling the O:E and the three IRLS quantities in a single
stratum (`clustered_flat`), a single-class draw `nan` and counted against
`MIN_USABLE_FRACTION`, the day-4 class-units guard applied before any draw (so one case
holding 180 of 200 rows stays `insufficient_clusters`), and the Brier cells kept within class
to stay bit for bit with `_brier_cell`; B2 by reading the edges from the bins that hold rows;
B3 with a rule reading the resampler's per-unit class counts; B4 by rewriting the string to
say what was inspected (no coverage run against the 0.90 bar). Regression proof: the three
final test files copied into a `b93e050` worktree → `16 failed, 45 passed`, each on the
assertion the repair note quotes (`assert (1.0885…) == (1.0510…)`; `assert 200 == 171`;
eight `ValueError`s; `assert 'boundary_estimate' == 'fixed_by_out…tratification'`;
`('calibration.py', 'overstates it in every resample')`) — reproduced by the lens-2
regression (`16 failed, 45 passed in 7.93s`). Coverage re-measured on the lens's own process
at two seeds, 24 clustered cells: 23 at or above 0.90, one at 0.897 (item 35).

What round 1 could not break (lens-1 fresh-attack, own code from the published formulae):
F4 slope 0, intercept 5.6e-17, SEs 0 / 0, intercept-in-the-large 0 (SE 1.1e-16), O:E and
bounds 0, Brier / reference / IPA 0, both ECEs 0, bin means 0, bin Wilson 1.1e-16; 150
cohorts at n = 30 / 200 / 2000: slope ≤ 4.4e-16, Wald bounds ≤ 8.9e-16, everything else
exactly 0 or ≤ 2.2e-16; statsmodels `fit(tol=1e-10)` estimates 7.0e-11, `method="newton"` SEs
bit for bit, sklearn Brier 5.6e-17; i.i.d. coverage 0.913–0.967 on the same process;
constant scores, exact 0 / 1 / 1e-300 / `5e-324` scores, `y == (p > 0.5)` (`complete_separation`),
logits at ±700 / ±800 (no overflow), single class both ways, N = 0 with and without a plan,
NaN / inf / 1.2 / −0.2 (`ValueError`, and H03 / S02 halt them before any table reaches here),
`logit` / `other` / `lower_is_positive` refused, the 200/200 boundary, a 95-row tie cohort
(`[10 × 5, 9 × 5]`), 100,000 i.i.d. rows in 2.5 s, 50,000 two-row cases at B = 200 in 26.2 s.
The regression lens re-ran every recorded oracle value to 0.0, regenerated F4 byte-identical,
ran the sweep in both shells (20 / 20) and the scipy-hidden import.

### Round 2, at `a5a5ed8` — fresh-attack `PASS` (0 blockers, 7 non-blocking, 3 sentence violations); regression `PASS` (0 blockers, 6 non-blocking)

Both lenses found the same sentence: the module docstring's "the resampled prevalence is the
same in every draw **exactly when** every unit of a stratum carries the same class counts" is
an "if", not an "iff" — 30 mixed cases of (1, 1) beside 30 of (2, 2) hold the prevalence in
all 500 draws and were read as varying, rendering `brier_ref` `boundary_estimate` (the
fresh-attack, three such shapes; the regression lens on 12 mixed (1, 2) beside 12 (2, 4) with
30 pure one-row cases, **one** prevalence in 1,000 draws). Also found: `irls_not_converged`
emitted at `iterations: 1` where the docstring named only the budget (50 rows all at `p = 1.0`
with 12 events; the step-halving exit); the FA-B3 phrase in the sentences test line-wrapped
at `b93e050` so that assertion observed nothing; the i.i.d. bin companion losing the 200/200
flag at `a5a5ed8` (unrecorded); no T7 section; four smaller sentences with run
counter-examples ("`O` varies from draw to draw" — 500 flat draws on 100 two-row (1, 1)
cases give `distinct O = {100}`); nine of the fresh-attack's ten planted mutants surviving,
one of them L2-2 (a prefix of the case column instead of the bin's own ids), which changes
every bin's clustered interval on any cohort whose case rows differ in score and which no
test could see because `clustered_arrays` built every case by `np.repeat`.

Fixed in `7eb617f`: the exact prevalence rule (`R·pos_u − P·rows_u` constant within each
stratum; the derivation in the function docstring), with a test comparing the rule against
the module's own 500 draws in both directions on eleven named shapes; the four
`irls_not_converged` exits named; the sentences test collapsing whitespace, its FA-B3 phrase
corrected and ten lens-2 phrases added (every phrase grepped at its named sha: 8 / 8 at
`b93e050`, 10 / 10 at `a5a5ed8`, 0 / 18 at HEAD — re-counted by the lens-3 regression); the
L2-2 observer (200 two-row cases with differing scores; an independent case bootstrap of all
ten bins written in the test) and the L2-2 mutant in the sweep; the i.i.d. companion pinned;
the T7 calibration section. Regression proof: `4 failed, 57 passed` at `a5a5ed8` on the
quoted assertions (lens-3 regression: `4 failed, 57 passed in 6.32s`; the flat-O and
iteration-one tests fail there on their docstring assertions only, their behaviour
assertions pass — the shipped code was right, the sentence wrong).

What round 2 could not break (lens-2 fresh-attack): F4 every quantity 0, bin Wilson 1.1e-16;
150 cohorts ≤ 4.4e-16 (slope), ≤ 2.2e-16 (the rest), `newton` / `Logit` / sklearn to
4.4e-16; the clustered O:E and slope on 80 cases of 1–5 rows with the rows shuffled: own
single-stratum bootstrap from the cell keys reproduces the O:E bounds to 1.1e-16 and the
slope (statsmodels refits) to 2.7e-13; every decile bin's clustered bounds to 1.1e-16; the
recorded seed-2026 coverage table in all 24 cells; **a process with within-case correlation
the score does not carry** (`b_case ~ N(0, 2.0)`, 30 × 10 and 50 × 8): i.i.d. analytic
0.655 / 0.740 on the O:E and 0.645 / 0.710 on the intercept-in-the-large, the repaired route
**0.970 / 0.930** and **0.955 / 0.935** — the property a cluster bootstrap exists for; N = 5
declared two-row cases (`insufficient_clusters` everywhere, bins 6–10 `zero_denominator`,
schema valid); 100,000 rows as 50,000 two-row cases (`fixed_by_outcome_stratification`,
29.8 s); a level of 0.90 carried on every Number; one crossing pair (slope 44.49 in 18
iterations, finite); the verdict walk on an assembled document with dev rows, a missing label
and a blank site. The regression lens re-measured every repair-note figure in both shells,
re-planted the six mutant-measured claims on the named test alone, ran the sweep in both
shells (30 / 30) and both coverage seeds (identical bar the elapsed line).

### Round 3, at `7eb617f` — fresh-attack `PASS` (0 blockers, 6 non-blocking, 3 sentence-precision items); regression `PASS` (0 blockers, 5 non-blocking)

Lens 2's sentence closed by execution: the eleven-shape test's shapes A, B, D and RG-N1 render
`fixed_by_outcome_stratification` and the control `cluster_bootstrap_percentile
(0.24994, 0.24996)` (lens-3 regression, `probe/recheck.py`); and by enumeration — every
possible draw on 2,567 random case shapes of at most five units per stratum (717 invariant)
and 500 draws on 2,000 larger shapes, **0 mismatches** with `_prevalence_invariant` in either
direction (fresh-attack, `probe/prev_fuzz.py`); 6,917 shapes over two seeds, 2,030 invariant,
0 mismatches (regression, `probe/prev_exhaustive.py`). What round 3 found is carried items
15–18, 20–23, 25 and 34 above; the `inf` (item 25) is the one with a customer-facing
consequence once E7 wires the block.

What round 3 could not break (lens-3 fresh-attack, seed 2026-09-18): F4 to 3.6e-15 over every
quantity; 150 cohorts: joint intercept 1.2e-14, slope 8.0e-15, Wald bound 4.0e-15,
intercept-in-the-large 2.2e-16, O:E 0, its bounds 2.2e-16, Brier / reference / IPA / both
ECEs / every bin's `n`, `events`, `mean_pred`, observed rate exactly 0, bin Wilson 2.2e-16;
statsmodels `newton` 2.2e-16 / 4.4e-16, sklearn 0; the clustered O:E and Brier bit for bit
and the intercept-in-the-large and slope to 1.1e-16 from its own case bootstrap off each
cell's generator (`units_per_stratum {'positive': 14, 'negative': 17, 'mixed': 49}` for the
Brier); both coverage seeds reproduce T7's 36 cells; `E = 0` (all scores 0.0 with 14 events:
O:E `zero_denominator`, intercept-in-the-large `irls_not_converged`, slope `constant_score`);
ties on 95 / 100 / 37 / 1,000 rows over 9 / 1 / 3 / 7 distinct values (ten bins in each of the
four, none empty); 199 / 199 flagged on the rendered bin Numbers and not on the companions; levels 0.90
and 0.99 carried on every Number including the ECE; 100,000 i.i.d. rows at B = 2,000 in 2.4 s,
50,000 two-row declared cases at B = 200 in 27.4 s; scores at `expit(±700)`. The regression
lens re-measured every repair-2 figure in both shells, re-planted the six named sweep mutants
on their named tests (`E assert 20 == 38`; `('mixed (1,1) x 30 + (2,2) x 30', {0.5})`;
`prevalence_invariant_always_true` dies on the control shape — both directions observed),
counted the sentence census at three shas, ran the sweep in both shells (34 / 34, 170 s /
172 s) and both coverage seeds (identical bar the elapsed line and `Out-File`'s BOM).

Could not check, all three rounds: coverage of the clustered decile-bin intervals and of the
Brier / reference Brier / IPA under a clustered plan (DEC-18 a not built; the script measures
neither); the R `rms::val.prob` capture (`r_rms_val_prob: [pending]`, day 9); the Debray 2017
attribution for the O:E variance (`[unverified - cited from memory]` in the docstring; the
tests recompute the formula, not the citation); lens 2's second-process coverage figures
(carried in T7 as the lens's own, re-run by nobody); the seed-7 coverage run in PowerShell
(now run, below).

## The decisions taken today

Each is stated in full in the `stats.calibration` module docstring and summarised in
`design/conventions_T7.md` "Calibration (build day 6)"; the test that feeds the literal input
is named.

| Decision | Rule as shipped | Where recorded | Test that feeds the input |
|---|---|---|---|
| Suppression shape | Block `None`; the typed reason goes to a sibling top-level key `calibration_suppressed_reason: {reason, score_type, orientation}` (null when the block is present); inside a present block `suppressed_reason` is `null` by schema. A deviation from D1 §4.2's letter (which puts the reason inside a block that cannot hold it when null), not its intent — needs-from-Josh 3 | `CalibrationResult.as_document`; schema `calibrationSuppressedReason` | `test_score_type_logit_and_other_give_a_null_block_with_the_typed_reason`, `test_the_schema_rejects_a_block_that_drops_the_scheme_or_smuggles_a_reason_into_a_present_block` |
| `lower_is_positive` | **Refused, never transformed**: `1 − p` would assume the score is the probability of the negative class, which the customer did not declare. Its own reason `score_not_positive_class_probability` (the remedy — re-orient — differs from `logit` / `other`'s `score_not_probability` — declare a probability) | docstring "Suppression, never a silent transform"; T7 "Scope" | `test_a_lower_is_positive_probability_is_refused_with_its_own_reason_not_transformed` |
| Boundary rule (scores at exactly 0 or 1) | Clipped to `[1e-12, 1 − 1e-12]` for the two logit-scale models **only**; `n_clipped` and `clip_eps` in the block; flag `scores_clipped_for_logit` on the three IRLS Numbers when the count is positive; O:E, Brier, ECE and the curve use the raw score | `CLIP_EPS`; docstring "Scores at exactly 0 or 1" | `test_scores_at_exactly_0_and_1_are_clipped_for_the_logit_models_only_and_counted` (pins the fitted value against statsmodels on the clipped vector; a 1e-6 clip moves the slope by > 1e-4) |
| Tie rule (decile bins) | Stable sort (`numpy.argsort(kind="stable")`): tied rows keep row order; `numpy.array_split`, so the first `N mod 10` bins hold one extra row (on N = 25 the first five bins hold three rows, the last five two); stated in `decile_tie_rule` in the block | docstring "Decile curve"; T7 | `test_the_decile_tie_rule_is_stable_sort_order` (ties across 20 rows placed in row order), `test_an_n25_cohort_still_shapes_every_number_with_its_tier_annotation` |
| Empty bins | N < 10: the bins that receive no rows are `zero_denominator`, never dropped; edges read from the bins that hold rows (`a5a5ed8`) | docstring | `test_a_cohort_of_one_to_nine_rows_renders_ten_bins_with_the_empty_ones_zero_denominator` |
| 200/200 inequality | `events < 200 or nonevents < 200` → flagged; exactly 200 of each is not. Counted in analysed **rows**; under a plan `event_cases` / `nonevent_cases` reported beside them in `curve_flag` (open question 1). The flag rides on `curve_flag`, the block's `flags` and every bin's rendered Number — on i.i.d. rows not on the Wilson companion. An annotation, never a suppression: O:E, slope, intercept and every bin stay reported. `convention: "ProofPack convention after Van Calster 2019"` — ProofPack's, not a regulator's | `CURVE_MIN_EVENTS`, `CALIBRATION_CONVENTION`; docstring; T7 | `test_the_200_200_annotation_is_strict_on_both_sides_and_the_flagged_fits_keep_their_intervals[199-300 / 200-300 / 300-199 / 300-200 / 199-199]` |
| ECE edges | Equal-width bins right-closed `(lo, hi]`, first bin closed at 0, edges `arange(11)/10` (not `linspace`: the third edge differs, `0.3` vs `0.30000000000000004`, and places `0.1·3`, `0.1·6`, `0.1·7` in bins 3 / 6 / 7 here and 2 / 5 / 6 under `linspace`); the rule that reproduces R2's F4 figures. Equal-mass ECE uses the decile bins. Both carry `{bins, scheme, edges}` and a `supplementary` note | docstring "ECE"; T7 | `test_the_equal_width_bins_are_right_closed_with_the_first_bin_closed_at_zero`, `test_the_equal_width_edges_are_arange_over_ten_and_not_linspace` |
| ECE has no interval | `not_computed_this_run` on the Number and `ci_not_computed_because` beside it: "no coverage run of a bootstrap interval for a binned absolute deviation has been made against the DEC-08 bar of 0.90, and the ECE is reported as a descriptive figure (R2 section 1.4)". The `b93e050` string asserted a bootstrap fact that two lenses falsified; the shipped string names what was inspected. An ECE interval is a build item with its own coverage run (open question 3) | `_ECE_CI_WHY` | `test_the_sentences_the_day6_lenses_falsified_are_gone_from_the_shipped_text` (reads the string from a rendered block) |
| O:E variance | `Var(ln O) = (1 − O/N)/O`, `E` taken as fixed; derivation in the docstring; attributed to Debray et al. 2017 **[unverified - cited from memory]**; `E = 0` is `zero_denominator`, `O = 0` and `O = N` are `single_class` before any interval | docstring "O:E ratio" | `test_f4_oe_ipa_and_both_eces_match_the_formulae_computed_here_to_1e_9` (recomputes from the formula, not the citation) |
| IRLS rules | Newton with step-halving; converged when the largest parameter change is at most `1e-10 × (1 + |parameter|)`; at most 100 iterations; `complete_separation` when every fitted probability is within 1e-8 of its label; `irls_not_converged` on any of **four exits** (budget spent; the 30 halvings of one step all raise the deviance — `detail.iterations` is that step's index; singular information at a step or at convergence; non-finite parameter or SE); `single_class` before any fit; `constant_score` when `logit(p)` takes one value | `IRLS_TOL`, `IRLS_MAX_ITER`, `SEPARATION_TOL`, `MAX_STEP_HALVINGS`; docstring "IRLS rules"; T7 | `test_separation_and_non_convergence_are_typed_reasons_never_inf_nan_or_a_traceback`, `test_irls_not_converged_at_iteration_one_names_the_step_halving_exit_not_the_budget`, `test_a_constant_score_is_constant_score_for_the_slope_and_the_offset_intercept_still_fits` |
| Wald SE oracle | `statsmodels GLM(Binomial).fit(tol=1e-10)` is the test oracle **at 1e-6, not an identity**: on F4 it is 3.3e-10 / 7.21e-10 from the engine (the default `.fit()` 9.13e-6 / 1.99e-5, one IRLS iteration earlier), `fit(method="newton", tol=1e-12)` 1.1e-16; on the lens-1 n = 30 cohort the tight IRLS SE was 1.03e-5 off. Both fits and their notes are in the fixture | `fixtures/f4_expected.json` (offset and joint objects, each with its note) | `test_f4_slope_and_intercept_match_statsmodels_glm_to_1e_6`, `test_f4_intercept_in_the_large_matches_statsmodels_glm_with_offset_to_1e_6` |
| Clustered route: two resamplers | O:E and the three IRLS quantities resample cases in a **single stratum** (`clustered_flat`; a draw's `O` and `E` are the sums over the drawn cases); the Brier cells resample cases **within outcome class** (`clustered_by_case`) to stay bit for bit with `_brier_cell`. A drawn set with one outcome class, or a non-converged refit, is `nan` to the resampler and counted against `MIN_USABLE_FRACTION`. The day-4 class-units rule over the cases within class is applied **before any draw** and refuses `insufficient_clusters`; both resamplers' quantities are carried under `bootstrap.resampling` (the guard's under `class_units_guard`). Every analytic companion is refused `clustered_data_analytic_ci_invalid` (DEC-09); flags `log_delta_refused_clustered` / `irls_wald_refused_clustered` on the rendered Number | docstring "Under a clustered plan"; T7 | `test_the_clustered_oe_and_intercept_intervals_are_the_percentile_of_a_one_stratum_bootstrap`, `test_a_clustered_oe_resample_with_one_outcome_class_is_nan_and_counted_as_degenerate` (40 one-row cases, 2 events: `usable_resamples` 171 of 200), `test_one_case_holding_180_of_200_rows_is_insufficient_clusters_on_the_oe_and_the_fits`, the census test |
| Prevalence rule | `_prevalence_invariant` returns `True` when, within every stratum, `R·pos_u − P·rows_u` takes one value over the units (`P`, `R` the cohort's positive and total row counts; integers, exact); then `brier_ref` carries `fixed_by_outcome_stratification` with no interval rather than the `boundary_estimate` a zero-width draw would produce; otherwise bootstrapped. The derivation (necessity by moving a stratum's whole draw between two units; sufficiency because any draw sums to the identity draw's `RP − PR = 0`) is in the function docstring; the sentence says "on the eleven shapes the test feeds", not "exactly when" | `_prevalence_invariant`; `number.py` comment; T7 | `test_the_prevalence_rule_agrees_with_the_resamplers_own_draws_on_eleven_shapes` (distinct-prevalence counts 16 / 11 / 13 / 92 on the four varying shapes), `test_brier_ref_is_fixed_when_every_unit_in_a_stratum_carries_the_same_class_counts` (48 mixed two-row cases fixed; 72 mixed three-row cases bootstrapped) |
| The 0.897 cell | Rendered with its tier annotation and recorded (docstring, T7), not refused on a threshold set from one cell — the same class of decision as the day-5 needs 1; DEC-18 (c) applies | docstring; T7 coverage table | none (a script and prose; re-measured now) |
| Reason and flag names | Reasons `score_not_probability`, `score_not_positive_class_probability`, `irls_not_converged`, `complete_separation`, `constant_score`, `fixed_by_outcome_stratification` (`NOT_ESTIMABLE_REASONS` 15 → 21); flags `below_200_events_or_nonevents`, `scores_clipped_for_logit`, `log_delta_refused_clustered`, `irls_wald_refused_clustered` (`FLAGS` 11 → 15); **no new method string** (`METHODS` stays 18); `single_class` and `not_computed_this_run` reused | `stats/number.py`; schema `$defs` | `test_the_enums_added_today_agree_between_code_and_schema` |
| `table1.dev`, `flow`, `missingness` | `table1.dev` tabulated from `dataset = dev` rows when present (never analysed), `None` with the reason otherwise; `similarity` / `overlap` `None` with `not_computed: v1.1`; `flow.n_cases` is the analysed row count without a case column, `n_sites` counts distinct `site` levels excluding Unknown (`None` without a site column), `clustering_route` beside `clustered`; missingness shares are over every row read, an attribute's `Unknown/missing` counts as missing, numeric NaN and the indeterminate `-1` are missing. What leaves the module: counts and shares keyed by column names and level labels (a level spelt as a number is a key like any other) | `descriptive.py` docstring | `tests/test_descriptive.py` |

## Re-run these

Every command below was run now, at `7eb617f`, in the main tree, in **both** Git Bash and
PowerShell 5.1; `python -c "import proofpack;print(proofpack.__file__)"` printed
`C:\Users\joshs\GPS\ProofPack\proofpack\src\proofpack\__init__.py` in each first. Outputs
are pasted as printed; the shells differ only in seconds. The sweep and the two coverage runs
ran concurrently with the suites in both shells, so their wall times are inflated against
the notes' (sweep 160–172 s; coverage 151–159 s); the counts and cells are deterministic.

```bash
cd C:/Users/joshs/GPS/ProofPack/proofpack
```

| Command | Output, Git Bash | Output, PowerShell 5.1 |
|---|---|---|
| `python -m pytest -q -p no:cacheprovider` | `429 passed, 1 skipped, 1 xfailed in 71.08s (0:01:11)` (under the concurrent sweep) | `429 passed, 1 skipped, 1 xfailed in 53.21s` |
| `python -m pytest -q -p no:cacheprovider -m day6` | `65 passed, 366 deselected in 11.42s` | `65 passed, 366 deselected in 8.48s` |
| `python -m pytest -q -p no:cacheprovider -m day5` | `54 passed, 377 deselected in 26.78s` | `54 passed, 377 deselected in 19.58s` |
| `python -m pytest -q -p no:cacheprovider -m day1` | `59 passed, 1 skipped, 371 deselected in 4.79s` | `59 passed, 1 skipped, 371 deselected in 4.22s` |
| `python -m pytest -q -p no:cacheprovider -m day2` | `40 passed, 391 deselected in 1.86s` | `40 passed, 391 deselected in 1.82s` |
| `python -m pytest -q -p no:cacheprovider -m day3` | `29 passed, 401 deselected, 1 xfailed in 2.40s` | `29 passed, 401 deselected, 1 xfailed in 2.26s` |
| `python -m pytest -q -p no:cacheprovider -m day4` | `182 passed, 249 deselected in 25.42s` | `182 passed, 249 deselected in 18.04s` |
| `python -m pytest -q -p no:cacheprovider "tests/test_calibration.py::test_the_assembled_document_validates_against_the_output_schema"` | `1 passed in 2.22s` | `1 passed in 2.06s` |
| `python -m ruff check .` | `All checks passed!` | `All checks passed!` |
| `python -m ruff format --check .` | `68 files already formatted` (ruff counts the Markdown files whose fenced blocks it formats: 66 when the repair-2 note ran, 68 with the two lens-3 notes untracked in the tree, 69 with this note in the tree — measured after writing it) | `68 files already formatted` |
| `python -m proofpack.cli doctor --offline` | `All essential checks passed.` (17 `[ok  ]` lines; `docx extra not installed - HTML/JSON only`; `reference platform windows-amd64-cp314`) | `All essential checks passed.` |
| `python scripts/mutation_sweep.py --marker day6` | `34 planted, 34 killed, 0 survived; 203 s` (73-line output; every mutant's line reads `killed` with a `1 failed, N passed, 366 deselected` under it) | `34 planted, 34 killed, 0 survived; 199 s` (the two outputs are identical once the elapsed line, the temp-dir name and the per-test seconds are stripped: 0 diff lines) |
| `python scripts/coverage_calibration.py --full` | header `coverage_calibration.py v1 (build day 6 repair round 1, 2026-09-18); seed 2026; R=300, B=200; nominal 0.95; bar 0.90`; clustered O:E `0.920 / 0.943 / 0.903`, intercept-in-the-large `0.920 / 0.943 / 0.897`, slope `0.940 / 0.943 / 0.933`, joint intercept `0.903 / 0.927 / 0.917` on 200 × 1 / 200 × 2 / 60 × 3; i.i.d. `0.960 / 0.967 / 0.940`, `0.930 / 0.953 / 0.913`, `0.950 / 0.953 / 0.960`, `0.927 / 0.943 / 0.923`; `elapsed 213 s` | identical bar `elapsed 157 s` and `Out-File`'s BOM (0 diff lines with both stripped) |
| `python scripts/coverage_calibration.py --full --seed 7` | clustered O:E `0.937 / 0.960 / 0.937`, intercept-in-the-large `0.927 / 0.957 / 0.930`, slope `0.937 / 0.940 / 0.920`, joint intercept `0.923 / 0.953 / 0.940`; i.i.d. `0.980 / 0.990 / 0.967`, `0.947 / 0.973 / 0.953`, `0.967 / 0.950 / 0.940`, `0.937 / 0.960 / 0.947`; `elapsed 157 s` | identical bar `elapsed 151 s` (0 diff lines) |
| repair-r7's 27 (`python -m pytest tests/test_bootstrap.py -q -p no:cacheprovider -k "one_id_per_row or not_a_case_column or composite_case_key or one_dimensional_case_column or contradict_each_other or w13_beside_the_interval or cell_is_degenerate or four_faults or shape_it_checks or surfaces_no_clustering"`) | `27 passed, 155 deselected in 0.96s` | `27 passed, 155 deselected in 0.70s` |
| day-5 round-1's seven (`python -m pytest tests/test_subgroups.py -q -p no:cacheprovider -k "refused_on_clustered_rows or either_arm_has_zero_variance or matches_numpy_on_the_raw_columns or exactly_at_the_constant or naming_the_analysed_rows or every_phrase_the_brief_names or side_a_then_side_b"`) | `7 passed, 47 deselected in 1.95s` | `7 passed, 47 deselected in 1.67s` |
| day-5 round-2's seven (`python -m pytest tests/test_subgroups.py -q -p no:cacheprovider -k "one_side_is_frozen or applies_the_single_cell or every_route or no_default_clustering_route or falsified_are_gone or side_a_then_side_b or every_phrase_the_brief"`) | `7 passed, 47 deselected in 3.39s` | `7 passed, 47 deselected in 3.01s` |

Also run now in Git Bash, main tree: the lens-1 B1 one-liner prints `1.0096 1.3674 1.0275
1.328 cluster_bootstrap_percentile` (was `1.0911 1.2709` at `b93e050` per the lens; companion
`clustered_data_analytic_ci_invalid`, `units_per_stratum {'all': 100}`); B2: N = 1 / 2 / 5 /
9 each return ten bins with 9 / 8 / 5 / 1 `zero_denominator` and serialise without NaN; B3:
`brier_ref` on the 100 × 2 cohort `fixed_by_outcome_stratification`, est 0.248; B4: the ECE
reason string carries neither "every resample" nor "overstates"; lens 2's shape A (30 mixed
(1, 1) beside 30 (2, 2)) `fixed_by_outcome_stratification`; the scipy-hidden import
(`scipy`, `scipy.stats`, `statsmodels`, `sklearn` set to `None` in `sys.modules`) imports
`proofpack`, `proofpack.stats`, `calibration` and `descriptive` with no scipy module loaded
afterwards; `METHODS` 18 / `FLAGS` 15 / `NOT_ESTIMABLE_REASONS` 21 equal the schema enums.
Day-5 `--marker day5` sweep and `coverage_bar.py` not re-run: `bootstrap.py` and
`subgroups.py` are unchanged since `4eb44f3` (0 bytes). Do not paste the bare `proofpack
doctor --offline`: the console script is not on `PATH` in either shell; `python -m
proofpack.cli doctor --offline` is the form that runs.

The regression proofs for the two repair rounds are in the repair notes (scratchpad) and were
re-run by the regression lenses: the three round-1 test files copied into a worktree at
`b93e050` → `16 failed, 45 passed`; the two round-2 files into a worktree at `a5a5ed8` → `4
failed, 57 passed`. To repeat one, add a worktree with `git worktree add --detach <path>
<sha>`, run with `PYTHONPATH=<worktree>/src` forced and `proofpack.__file__` printed first —
the editable `.pth` on this machine otherwise imports the main tree and gives a false green
(the lens-3 fresh-attack printed the unforced form and got the main tree). `git worktree
list` shows only the main tree now (the two `lens-S6-r3-*` worktrees another workflow had
registered on this repo at the start of this session are gone).

## Tomorrow needs

Day 7 is **E7 — run gates and manifest** (`spec/briefs/day7_E.md`): the criteria engine
(`met` / `not_met` / `not_assessable`; bounds and margins from `criteria.yaml` only),
`stats.attainability`, `io.ledger`, manifest hashing (inputs + `criteria.yaml` +
`mapping.json` + code version + platform), `licence.*` verify (Ed25519 against the embedded
and published key; `licence install`; trial / quarterly / annual; grace watermark); tests F8
(n = 30 criterion → `not_met`, `attainable_at_n: false`), ledger count, manifest stable
across runs, F20 (tampered / expired / wrong-key refused), a grep finding no "pass / fail /
verdict". In order:

1. **The merge of lane A's `a-p1-mapper` branch is the orchestrator's**, before E7 starts.
   Today's tree touched neither `src/proofpack/cli.py` nor `src/proofpack/io/mapping.py` (0
   bytes against `4eb44f3`), so the merge has no lane-E conflict on lane A's files; the new
   files on this side are the four test files, the two `stats` modules, the two scripts, the
   two fixtures, the schema and `number.py` additions and T7. Re-run `-m day6` and the sweep
   after the merge and paste the counts.
2. **Close carried item 25 before `cmd_run` calls `calibration_block`:** `resample_sd = None`
   (or the typed `boundary_estimate` route) when `usable.std(ddof=1)` is not finite, in day-4
   `bootstrap_percentile`, with the 57-plus-3 input as the regression test measured failing
   first, and `json.dumps(..., allow_nan=False)` on the block in that test. A `bootstrap.py`
   change touches a statistical gate: DEC-12(i) lens.
3. **Wiring, inside `cmd_run` only** (lane A owns `map`): when calibration and the descriptive
   blocks enter the run output, (a) a blank `case_id` needs an S-code halt or a typed
   treatment at ingest (item 26 — the day-5 `subgroup_analysis` raises the same `TypeError`);
   (b) a `y_pred`-only table gives a typed suppression, not a raise (item 28); (c) the
   document's required set (`calibration`, `calibration_suppressed_reason`, `missingness`,
   `table1`, `flow`) is decided in the schema (item 30); (d) `flow.dev_rows` (item 27) with
   the D1 line. `tests/assembler.py` is the shape the run output takes today; keep its four
   documents as the acceptance fixtures.
4. **The verdict grep and E7's status vocabulary.** `VERDICT_WORDS` (`tests/test_subgroups.py`)
   holds `met`, `not_met`, `meets`, `pass`, `fail`, `verdict` and their inflections, and the
   day-5 and day-6 grep tests walk every key and engine string of the assembled documents
   except `declarations`. E7's criteria block is the **one** place `met` / `not_met` /
   `not_assessable` may appear: scope both walks to exclude that block by key, and add the
   E7 twin that asserts those three tokens appear only under it and that `pass`, `fail` and
   `verdict` appear nowhere in any key or string — the brief's grep. No status anywhere else;
   no margin chosen by the engine (`criteria.yaml`'s author, date and justification only).
5. **F8 and attainability read the day-5/6 Numbers as they are:** a criterion on a Number with
   `method: none` and a typed reason is `not_assessable`, never `not_met`; the cell shape
   `{number, analytic, …}` means the criterion reads `number` (the renderer's field). Say in
   the note which field each criterion reads.
6. If the build finishes with time left: items 16 and 15 (one assertion each — F4 at
   `level=0.90` with the three Wald bounds asserted; one prevalence shape whose differing unit
   is last in its stratum), then items 9–13 (one assertion each; lens 2 named the inputs).
7. **DEC-18 (a)** is its own E slot, not E7's; carried item 32 (a second DGP in the coverage
   script) goes with it.

Nothing else in the engine changed today: `Number` is untouched (frozen), every day-4/5
constant reads as the day-5 note lists them, and the day-1 to day-5 test files are unchanged
(`git diff --name-only 4eb44f3 HEAD -- tests/` names only the four new files).

## Needs from Josh

1. **The 0.897 cell (carried item 35).** The intercept-in-the-large on 60 cases × 3 rows at
   seed 2026 reads 0.897 (0.930 at seed 7; Monte-Carlo SE about 0.017; the analytic Wald on
   the same cohorts 0.913); the other 23 clustered cells are 0.903–0.960. Render it with the
   tier annotation as now (DEC-18 c), or add a case-count floor for the calibration route?
   The same class of decision as the day-5 needs 1; T7 records the cell either way.
2. **The few-events tier and `imprecise` on the calibration cells (item 31).** Apply the
   `very_low_precision`-below-5-events rule to the Brier / reference Brier / IPA and to the
   clustered O:E and fits (an annotation-rule change on day 5's `_brier_cell` as well), or
   leave as measured; and should the IPA carry `imprecise` (a proportion tier applied on the
   IPA's own scale), or should the renderer read the row tier instead? One line each.
3. **Suppression shape (decision 1).** Keep the sibling key `calibration_suppressed_reason`
   beside the `None` block, or amend D1 §4.2 to match? The day-8/9 renderer reads whichever
   you pick.
4. **DEC-18 (a) needs an E slot.** The design-effect Wilson is not in E7's brief; until it
   lands the clustered decile bins carry the day-5 coverage table's shortfall with only a tier
   annotation, and nobody has measured them.

## Open questions (at most five)

1. **200/200 under clustering: rows or cases?** Today the analysed rows decide the flag and
   `event_cases` / `nonevent_cases` are reported beside them; Van Calster's sentence says
   "patients". One line either way, and a test pins it (carried item 6).
2. **One spelling for "not a probability":** switch day-5 `_brier_cell` from
   `not_computed_this_run` (with `score_type` in detail) to `score_not_probability` so the
   document has one reason for the same cause? One line in `subgroups.py`, one assertion in
   `tests/test_subgroups.py` (item 36).
3. **ECE interval:** a bootstrap interval once a coverage run exists, or descriptive with no
   interval for v1 (R2: "treat ECE as descriptive")? Three lenses measured the bootstrap mean
   above the point on every input tried and 145–371 of 1,000 resamples below it.
4. **`table1.dev`** is built from `dataset = dev` rows when present; D1's `--dev-summary`
   path (a separate dev table) is not — v1.1 with similarity / overlap?
5. **Naming the IRLS exit in the output (item 33):** add `detail.exit` (a schema-open key the
   renderer has not seen) so `iterations: 1` beside `irls_not_converged` is readable without
   the docstring, and should quasi-complete separation get its own reason?

## Sentences refused

Aggregated from the build note, the two repair notes and the six lens notes; each is a
sentence that was not written because its counter-example ran, or because no counter-example
was constructed.

- "The calibration is good / well calibrated / acceptable" or any target comparison — no
  target exists in the module; the verdict grep walks every assembled document and the day-6
  grep asserts `well calibrated` and `miscalibrated` are flagged and `calibration` is not.
- "ECE with a 95 % interval" — no interval is emitted (decision above).
- "The ECE bootstrap is biased upward" / "overstates it in every resample" — the `b93e050`
  string said the latter; 145 / 1000 and 371 / 1000 resamples sat below the point on the
  lenses' inputs; the shipped string now claims nothing about the bootstrap's behaviour.
- "F4 distinguishes the two ECE schemes" — it does not; both give 0.1955 on those rows.
- "Matches statsmodels to 1e-6" without the tolerance qualifier, and "statsmodels at
  `tol=1e-10` equals the engine" — the default fit is 1.99e-5 / 9.13e-6 away on F4 and the
  tight fit 1.03e-5 away on the lens-1 n = 30 cohort; the sentence written is "matches
  `GLM(Binomial).fit(tol=1e-10)` to 1e-6, and the default fit to 2e-5".
- "The Debray 2017 attribution is verified" — not fetched; `[unverified]` stays in the
  docstring and the tests recompute the formula.
- "The 200/200 convention is a regulator's" — it is ProofPack's, after Van Calster 2019, and
  the block says so in `convention`.
- "The output JSON is wired" / "`cmd_run` never reaches calibration" as a guarantee — the
  assembler is test-only; `cmd_run` does not call `calibration_block` at `7eb617f` and E7
  changes that.
- "The clustered decile bins meet the DEC-08 bar" — not measured by anyone; they inherit the
  day-5 table's shortfall (DEC-18 c).
- "The clustered calibration intervals meet DEC-08" / "now have correct coverage" / "cover at
  0.95" as a class — 23 of 24 measured cells at or above 0.90 and one at 0.897, on one process
  at three shapes and two seeds; lens 2's second process 0.914–0.970 on two shapes; stated as
  those figures.
- "The single-stratum case bootstrap fixes B1" / "the repair fixed the clustered intervals" —
  two cohorts reproduced bit for bit from the cell generators and coverage measured on the
  shapes above; nothing claimed beyond them.
- "The clustered route is right for the slope" (lens 1, pre-fix) — measured 0.907–0.947 on
  three shapes, stated as those figures.
- "The i.i.d. numbers are correct" as a class — stated as the deviation figures on F4 and 150
  cohorts in each lens note.
- "The class-units guard preserves every lens construction" — it preserves the one the lens
  listed (180 of 200) and the test feeds that input.
- "`_prevalence_invariant` is exact for every resampler" / "is correct" / "the engine never
  renders `boundary_estimate` on a fixed-prevalence resampler" — it agrees with the module's
  own 500 draws on eleven named shapes in both directions and with exhaustive enumeration on
  2,567 and 6,917 random shapes of at most five units per stratum; stated as those counts.
- "The flat resampler makes `O` vary" / "so the event count `O` varies from draw to draw" — 500
  flat draws on 100 two-row (1, 1) cases give `distinct O = {100}`; the O:E interval there
  comes from `E` alone.
- "Holds `O` at the same integer in every draw whenever every case is pure" (the `b93e050`
  text) — pure cases of sizes 1 and 2 give 13 distinct values of `O` in 500 by-class draws.
- "The four `irls_not_converged` exits are the only ways the fit fails" — they are the four
  exits in `irls_logistic` (five return sites) that return that reason; `complete_separation`,
  `constant_score` and `single_class` are the others.
- "Item 17 is closed" — the two `subgroups.py` sites now have observers and die; unreachable
  through the CLI today (no declarable level), stated as the mutant kills.
- "The mutation sweep is adequate" / "shows the tests have teeth" — 34 of 34 declared mutants
  die; 8 of lens 1's 18, 7 of lens 2's 10 and 9 of lens 3's 18 planted mutants survive at
  `7eb617f` (items 1–18).
- "The day-6 tests cover N < 10" (at `b93e050`) — they did not; closed in round 1 by the
  N = 1..9 test.
- "Every new test pins the repair" — four of round 1's seventeen new tests, and four of round
  2's new or extended tests, pass on the pre-fix tree by design (observers of a sentence the engine already satisfied,
  or mutant-anchored); the repair notes and the regression lenses say which.
- "The i.i.d. route is unchanged" (round 1 / round 2) — at `a5a5ed8` the i.i.d. bin companion
  lost the 200/200 flag (pinned in `7eb617f`); at `7eb617f` `_prevalence_invariant` is
  reached on the row resampler and returns `True` there as before, and every F4 figure equals
  the fixture; stated as those measurements.
- "No inf can reach the output" / "no customer can obtain a traceback" — item 25 is an `inf`
  on a score column below 1e-154 and item 26 a `TypeError` on a blank `case_id`, both on a
  path E7 has not wired.
- "T7 now carries every calibration convention" / "T7 is complete" — it carries the ones
  listed there, each a summary of a docstring paragraph; items 21 and 22 are in it.
- "Nothing else changed" after the repair-2 checkout incident — the diff against `a5a5ed8`
  was re-read file by file and the suite, sweep and coverage re-run; the lens-3 regression
  checked the diff and its measurements, not the lost edits.
- "No row value, header or id leaves this module" (`descriptive.py` at `b93e050`) — attribute
  level labels are row values and column names are headers, and both leave, as a Table 1
  requires; the sentence now says what is aggregated.
- The sklearn binning clause — the engine's `arange` edges and sklearn's `linspace` edges
  disagree on `0.1·3`, `0.1·6`, `0.1·7`; the docstring names the edge arrays instead.

In this note: "the 36 items are harmless" was not written — item 25 writes a non-JSON token
into a customer document the day calibration is wired, item 26 is a traceback on the same
day, and items 1–18 are behaviours a regression would ship green; "the lenses found nothing"
was not written for round 3 — they found six and five items and they are carried above;
"the clustered route now handles within-case correlation" was not written — the committed
process does not exercise it (item 32) and the one that does is a lens's measurement.
