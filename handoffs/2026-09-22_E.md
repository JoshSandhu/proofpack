# Lane E — build day 8 — 2026-09-22 (closed 23 September)

**The final lens round (3, at `7fa690b`) returned `FAIL` from both lenses: five distinct blockers are
open — the checker accepts a claim relabelled to a template that states another metric or the opposite
status (a `met` row as `CRITERION_NOT_MET_RECORD`, a sensitivity as `AUROC_ESTIMATE`), nine homoglyph
spellings of listed words pass `free_text`, an operating point declared with id `threshold_free` loses
its block and its criterion prints `not assessable`, repair 2 re-opened 180 `well-calibrated` spellings
that `657ef11` rejected, and the withdrawn sweep mutant `checker_format_characters_kept` is not
equivalent although the committed comment says so.** Forty items are carried (the table below, the
five blockers among them); lens 3 found no wrong T8 cell from blockers 1, 2 and 4 on the pages it
rendered (T8 prints no claim sentence; the 70 engine claims on the synthetic document carry
`free_text: null`), while blocker 3 put a `not assessable` row on the T8 page of lens 3's measured run.

Eight commits on `main` on top of `7b2ca2a`, none pushed: `86b5fdd`, `a2246c1`, `84f1b11`, `ee295cc`,
`afe59b9` (the builder, items 1–9; it was stopped by the account's weekly usage limit and wrote no
note), `29fc04e` (item 10, the orchestrator), `ad7cc57` + `657ef11` (repair 1), `7fa690b` (repair 2,
finished and committed by the orchestrator after the session holding it ended at 02:40). Suite now,
main tree, both shells: **1012 passed, 1 skipped, 1 xfailed** (788 at `7b2ca2a`, E7 note); `-m day8`
224. `git diff 7b2ca2a HEAD -- src/proofpack/stats/` is 0 lines (measured now); `tests/` 139 `A`, 5
`M`, 0 `D`.

## What landed

| # | Item | Commit(s) | Test ids (marker `day8`) | Oracle or literal input → measured figure |
|---|---|---|---|---|
| 1 | `overall` on a clustered plan and on a `y_pred`-only table (E7 cut 1, carried 42) | `86b5fdd`; `7fa690b` (blank `y_pred` without a score column excluded, counted under `flow.excluded_missing_score`; a `y_pred` at a declared indeterminate value is an indeterminate row) | `tests/test_overall_carried.py` (8); `tests/test_e8_repair2.py::test_a_blank_y_pred_without_a_score_column_is_excluded_and_counted`, `::test_a_y_pred_carrying_the_declared_indeterminate_value_is_an_indeterminate_row` | Lens 3 regression: 120 rows, no score, 60 blank `y_pred` → `{tp 21, fn 3, fp 7, tn 29}` equal to numpy on the non-blank rows; sensitivity 21/24 Wilson against statsmodels `0.00e+00` both ends (at `657ef11` the same table gave `{tp 21, fn 21, fp 7, tn 71}`). Lens 3 fresh attack: 300 rows in 100 cases, six pooled proportions at `0.00e+00`, `cluster_bootstrap_percentile`; youden / balanced accuracy / LR+ / LR− / DOR / F1 / MCC `clustered_data_analytic_ci_invalid`, `ci (None, None)` |
| 2–3 | `jinja2>=3.1` imported inside `render/html.py` only; `design/tokens.json` from D5 §3.1, force-included, `render/theme.py` | `a2246c1` | `tests/test_render_theme.py` (13) | Lens 3 regression: 19 names and hex values equal to D5, WCAG recomputed equal to every `contrast_computed`; the wheel installed into a fresh venv with 0 `.pth` files, `proofpack.__file__` and `tokens.json` inside it; the force-include line removed → `FileNotFoundError`, `assert 1 == 0` |
| 4–6 | `narrate/claims.py`, 56-template library (D4 §8's 55 + `SUBGROUP_ESTIMATE`), `narrate/checker.py`, the corpus | `84f1b11`; `ad7cc57`; `7fa690b` | `tests/test_claims.py`, `tests/test_e8_repair1.py`, `tests/test_e8_repair2.py`; corpus `tests/fixtures/claims_corpus/` | Now: 130 corpus files, 43 reason codes, 70 engine claims on the synthetic document, 70 accepted. Lens 3 regression: 42,849 value-ref swaps to every Number path, 0 accepted; 8,988 single-field mutations, 39 accepted (blocker 1) |
| 7–8 | `render/format.py` (D4 §1.2 once; `declared()` for the customer's numbers), `render/anchors.py`, `render/html.py`, `templates/{base,pages,T8}.html`; `run --format`, `--templates`, `T8.html` | `ee295cc`; `ad7cc57` (declared digits, `prior_version`, `declaration_index`); `7fa690b` (fairness-only criteria rows) | `tests/test_render_t8.py`, `tests/test_render_format.py`, `tests/test_e8_repair2.py::test_a_fairness_bound_without_a_criteria_list_prints_its_row_on_t8` | Now, both shells, the E7 e2e fixture (400 rows) through `cli.main(..., registry=ephemeral_registry())`: exit 0, `T8.html` 40,145 bytes, no CRLF, no BOM. Lens 2 fresh attack: 22 criteria rows on its own `criteria.yaml`, every observed / Value / Compared / `n` cell equal to its own D4 §1.2 formatting character by character. Lens 3: draft label beside all 14 FDA AI-DSF mentions and in 3 `guidance_refs`; forbidden-word walk 0 hits |
| 9 | T8 golden, footer on every page, anchor label, section 7 counts, verdict grep over HTML | `afe59b9` | `tests/test_render_t8.py` | Lens 3 regression: golden 39,800 bytes LF, equal; footer macro emptied → 4 failed; qualifier removed → `AnchorError` |
| 10 | Day-8 mutation list (DEC-12 ii) | `29fc04e`; 13 added in `ad7cc57`; retargets and one swap in `7fa690b` | `tests/test_sweep_day8.py` (2) | Now, worktree at `7fa690b`: **41 planted, 41 killed, 0 survived; 298 s**. Blocker 5: one mutant was withdrawn on an untested sentence |

## What was cut and why

1. **Item 11 (E7 carried 24, 20, 21–23: `id` uniqueness, `scope` required, `date` range, `value`
   finiteness, the absent age band)** — not started. The builder hit the weekly usage limit at 15:30;
   this is the limit, not a decision.
2. **T1 and T7** answer `--templates T1` / `T7` with a typed "not built in E8" line (E9's).
3. **No builder note exists.** The build note was assembled by the orchestrator from the commit
   messages; every builder figure in it is labelled as quoted, and the lenses re-measured them.

## What did not land and why — the 40 carried items

Each reproduces at `7fa690b` as the named lens measured it; rows 1–5 were re-run now in both shells.

| # | Item | Why carried | Repro (one line) | Owner |
|---|---|---|---|---|
| 1 | **Blocker.** Template relabel inside the bound set is bound by nothing: `CL-0062` (`met`) as `CRITERION_NOT_MET_RECORD` accepted (18 criterion relabels incl. `ATTAINABILITY_NOTE`); `CL-0001` sensitivity as `AUROC_ESTIMATE` accepted (13 overall claims); `FAIRNESS_GAP` / `CALIB_HIERARCHY` pointers permuted; `CALIB_NA` beside a calibration block; count templates on other counts; `FLOW_COUNTS` carrying a subgroup. Rule-4 docstring "so `AUROC_ESTIMATE` with `op1` is refused" is false | The limit ended the session before repair 3 | `repros.py` below: `CL-0001 sensitivity as AUROC_ESTIMATE -> True \| CL-0062 met as CRITERION_NOT_MET_RECORD -> True` | lens-3 FA-B1 = RG-B2 |
| 2 | **Blocker.** Nine homoglyphs pass `free_text`: `pɑss`, `unbiɑsed`, `faiI`, `ƒail`, `faiǀ`, `gօօd`, `ϲonsistent`, `Ꮲass`, `rneets`; `_tokens` docstring false | Scope decision first (needs 2) | `f('p\u0251ss'), f('faiI'), f('\u0192ail')` → `[None, None, None]` | lens-3 FA-B2 |
| 3 | **Blocker.** Operating-point id `threshold_free`: `out["threshold_free"]` overwrites the block; `C1 sensitivity` prints `not_assessable` / `metric_not_computed`, exit 0, no H08 | Repair is an H08 at declaration; touches `overall_block` → DEC-12(i) lens | `sorted(d['overall']['threshold_free'])` → `['auprc', 'auroc', 'auroc_wald', 'prevalence', 'roc']` | lens-3 FA-B3 |
| 4 | **Blocker (regression by repair 2).** `well-calibrated` matched as a whole token: 180 spellings `657ef11` rejected are accepted (`well-calibratedness`, `xwell-calibrated`) | Restore the substring test beside the token test, with a test; verdict-word gate → lens again | `f('well-calibratedness'), f('well-calibratedly')` → `None None` | lens-3 RG-B1 |
| 5 | **Blocker.** `checker_format_characters_kept` withdrawn as "equivalent"; planted, `f('com\u00adpass')` is `free_text_verdict_word` (real code `None`) and `-m day8` gives 224 passed. The comment in `scripts/mutation_sweep.py` is false | Restore the mutant, add the `com\u00adpass` test, rewrite the comment | plant `if False:` at `checker.py:523` in a scratch worktree | lens-3 RG-B3 |
| 6 | A `y_pred` at a declared indeterminate value **beside a score column** now leaves the analysis (120 rows, 12 such: AUROC `0.8632` → `0.8817`); mutant restricting it to no-score tables survives the full suite; the S4 list omitted it | Josh's decision (needs 3), then a pin | 120-row table, `y_pred = indeterminate` on 12, `report_both_ways` | lens-3 FA-N1 = RG-N1 |
| 7 | `checker_smallcap_s_unmapped` (U+A731 dropped from `CONFUSABLES`) survives `-m day8` | One corpus case | plant; `-m day8` | lens-3 FA-N2 |
| 8 | `anchors_month_year_fixed` survives: every draft map row is dated 2025-01-07 | A planted map row in a test (E9 anchor work) | plant; `-m day8` | lens-1 FA-N2 |
| 9 | `_declaration_for` wrapping an out-of-range `declaration_index` survives (not re-run after repair 2) | Unreachable from the engine; no test feeds one | plant the wrap; `-m day8` | lens-2 FA N4 |
| 10 | `_JOIN_RUN = 13` survives; the test comment reads as if the 14-letter literal pins 14 | `calibrated` is itself listed; comment only | plant 13; `-m day8` → 224 passed | lens-3 RG-N3 |
| 11 | `gate_h02` docstring "a blank cell … `analysis_mask` excludes it and counts it": beside a score column it is neither; schema / README "counted under `excluded_missing_score`": a blank on a blank-label row counts under `excluded_missing_label` | Wording | 12 blank `y_pred`, 6 on blank labels → `missing_label 6, missing_score 6` | lens-3 FA-N3 |
| 12 | `FLOW_COUNTS` skeleton "lacked a score" on a `y_pred`-only table | Not rendered today; E9 wording | read the skeleton | lens-3 FA-N4 |
| 13 | Corpus `129`'s `rule` "every run of consecutive tokens is joined" (the code joins 2–14) | Wording | read the file | lens-3 RG-N3 |
| 14 | Docstrings: `_tokens` / `_words` cite lens ids, no D1 §4.4 line; two repair-2 tests and `_number_or_none`, `_authored`, `_justification` have none | Documentation | read them | lens-3 RG-N4; lens-2 RG-N5 |
| 15 | `check` docstring "reads no environment": it reads two packaged files | Wording | read `checker.py` | lens-2 FA N10 |
| 16 | Repair-2 pre-fix artefact `prefix_tb.txt` ends `25 failed, 137 passed`, the note says 26 / 136 (lens 3 measured 27 / 137 with the orchestrator's two tests) | A record | the three files at `657ef11` | lens-3 RG-N2 |
| 17 | Golden mask is `manifest.numpy` only; the brief says run_id / started / duration_s | Recorded; not a defect | unmasked render differs in one line | lens-1 RG-N10; lens-3 RG-N7 |
| 18 | `python -m proofpack doctor` fails (`No module named proofpack.__main__`); `python -m proofpack.cli` runs | A `__main__.py` or the docs name the `.cli` form | run the first form | lens-3 RG-N8 |
| 19 | The token join refuses ordinary prose: "Specificity was reported in significant detail." → `free_text_verdict_word` (5,338 fuzz texts newly refused) | Errs to refusal; E9's narrative meets it; needs 2 | `f('Specificity was reported in significant detail.')` | lens-3 RG-N5 |
| 20 | Number words and inflections accepted: `ninety`, `IV`, `guidances`, `unsafe`, `safely`, `biases`, `C.F.R.`, `FDAcleared` | D1 §4.4's list is the binding text; needs 2 | `f('ninety')` | lens-1 FA-N3; lens-2 RG-N7 |
| 21 | Operating-point ids `a/b` or `op~1`: 58 of 60 engine claims refused `operating_point_mismatch` (the facet regex reads the escaped pointer) | False refusals, counted in section 7 | declare `op~1` | lens-3 FA-N5 |
| 22 | A subgroup level whose every row is excluded vanishes from `subgroups` (day-4 behaviour now reachable through `y_pred`) | DEC-08 question for E9 | sex `F`, 50 rows, every `y_pred` blank | lens-3 FA-N6 |
| 23 | Section 7 prints the three status words on a document with no criteria (D4 §14: none anywhere) | T8 template change | render with `criteria: []` | lens-2 FA N2 |
| 24 | `resolve` keeps two accepted claims on one slot (`['CL-0001', 'CL-9999']`) | Rule decision (open q 3) | two accepted claims, different ids | lens-2 FA N3 |
| 25 | `declared(1e22)` prints positional digits where `run.json` has `1e+22`; the YAML echo `1.0e+22` | Implausible threshold; recorded | `fmt.declared(1e22)` | lens-2 FA N1 |
| 26 | CLI shape: `--format HTML` folds to `html`; `--format ''` → json only, `--templates ''` → T8; `--templates T1` hints `--format json,html` | S4 takes the wording with the pin | the three calls | lens-1 FA-N9; lens-2 FA N11 |
| 27 | No-licence watermark still `LICENCE EXPIRED - not for submission` | DEC-48 decided `NO LICENCE - not for submission`; an E9 build | the plain-CLI run below | E7 carried 25 |
| 28 | `guidance_ref` may be any map row (70 of 70 swaps accepted) | "in the map" as specified; a D4 §8 decision | swap to `FDA_STAT2007_CI` | lens-1 FA-N4; lens-2 RG-N6 |
| 29 | `calibration: null` beside `calibration_suppressed_reason` and DEC-35's IPA annotation print nowhere on T8 (T8 has no calibration section) | T1 (E9) | the `y_pred`-only fixture | lens-1 FA-N5 |
| 30 | Draft label spelling differs from D4 §1.1 ("per draft guidance (Jan 2025), …") | The binding text names the built spelling; needs 4 | read the page | lens-1 FA-N7 |
| 31 | Mixed scales on the fairness row (Value `0.100` beside `+7.0` pp); `font-weight` literals outside tokens; D4 §11 items 4 and 10 not built | Design day / E9 | read the page | lens-1 FA-N8, N11, N12 |
| 32 | No `jinja2` at run time → exit 5 after `run.json`, no summary line | S4's Pyodide bundle decides (see S4 list) | hide `jinja2`; `run` | lens-1 RG-N5 |
| 33 | `guidance_refs` in `run.json` carries 3 anchors, the page 7 | Schema-description decision (open q 5) | count both | lens-1 RG-N8 |
| 34 | The declarations echo is `run.json`'s block as sorted YAML, not the customer's file (D4 §11 item 3 says verbatim) | Needs 5 | read section 3 | lens-1 RG-N9 |
| 35 | `integrity.halts` computed, never printed | D4 §11 item 7 does not list it | read `T8.html` | lens-1 RG-N11 |
| 36 | The wheel test has a `pytest.skip` path | One line to `pytest.fail`; gate test | read `_build_wheel` | lens-1 RG-N13 |
| 37 | `declaration_index: true` reads entry 0 | Unreachable (schema `integer`) | hand-built row | lens-2 FA N7 |
| 38 | `y_true` of `NA` declared indeterminate is counted `excluded_missing_label` (day-1 loader) | Outside E8 | `NA` as indeterminate value | lens-2 FA N9 |
| 39 | A bare YAML `date: 2026-01-01` is a `date` object → H08; the customer must quote it | Outside E8 | unquoted date | lens-1 FA N13 |
| 40 | Never checked in a browser: `unicode-bidi: isolate` on `.customer-text`; `var()` inside `@page` | No browser driven by any lens | print T8 from a browser | lenses 1–3 "could not check" |

Closed without a repair: the NUL byte reaching the page raw (lens-1 FA N6) — absent from the bytes at
`657ef11` and `7fa690b` (lens-2 and lens-3 fresh attacks).

**E7 items still open, under their old numbers** (repros in `handoffs/2026-09-21_E.md`; not re-run
today unless named):

| E7 # | State at `7fa690b` |
|---|---|
| 1–10 | Open (one observer each; no E8 work touched them) |
| 11–15 | Open (sentences and records) |
| 16 | Open (ledger race) |
| 17 | Open: a fairness-only `criteria.yaml` is not counted; DEC-47 counts runs "with a `criteria` list" |
| 18 | Decided (DEC-47: count after the write); not built |
| 19–23 | Open; 20–23 were item 11 (cut) |
| 24 | Half closed: the renderer keys rows by position and `declaration_index` (lens 2 D12); the H08 uniqueness rule was item 11 (cut) |
| 25 | Decided (DEC-48); not built (row 27 above) |
| 26, 28, 29, 33–37, 39–41, 43 | Open |
| 27 | Closed by DEC-50 (refuse, as built) |
| 30, 32 | Lane A (A-P2) |
| 31 | Decided (DEC-49: `<input>.mapping.json`); not built |
| 38 | Open (the CLI summary prints status counts; unchanged) |
| 42 | **Closed** by item 1 |

E6 items still open (old numbers, E6 note): 1–24, 29, 32, 33, 34, 36.

## What the lenses found

Each lens ran in detached worktrees with `PYTHONPATH=<worktree>/src` forced and `proofpack.__file__`
printed first; nothing was pushed.

**Round 1 at `29fc04e` — fresh attack `FAIL` (5 blockers); regression `PASS` (13 non-blocking).**
B1: "the checker does not bind the pointer to `metric_id`, `operating_point` or (outside `/subgroups`)
`subgroup`; 339 wrong-number bindings accepted". B2: "the free_text token rule is evaded by nine
spellings of listed words" (`p\u0430ss`, ZWJ, `un-biased`, `pass-`). B3: "a y_pred-only table whose
labels are not the declared classes passes every gate and prints sensitivity `0/42 (0.0%) [0.0, 8.4]`
and `criterion not met`". B4: "declared thresholds, criterion values, prevalence and the fairness bound
print rounded to three decimals, under a caption that says 'as the manufacturer wrote the value'". B5:
"a criteria.yaml without the optional `model.prior_version` makes `proofpack run` exit 5 after writing
run.json". Could not break: the clustered block (six pooled ratios `0.00e+00`), 76 digit runs traced
on the page, `--offline` 0 socket calls, the wheel in a fresh venv. Re-derivation deviations:
sensitivity 96/128 Wilson against statsmodels `1.11e-16`; AUROC against sklearn `0`.

**Round 2 at `657ef11` — fresh attack `FAIL` (4 blockers); regression `FAIL` (2 blockers).** FA-B1 =
RG-B1: "a blank or indeterminate-valued `y_pred` cell in a `y_pred`-only table is scored as 'predicted
negative'" (60 blanks → `21/42 (50.0%) [35.5, 64.5]ᶜ`). FA-B2: "rule 4 reads eight pointer shapes; 225
of the synthetic document's 418 Numbers sit at other paths" (13,962 of 42,849 swaps accepted). FA-B3:
"a `criteria.yaml` with a fairness bound and no `criteria` list yields a `not_met` row … and a page
that says 'No acceptance criteria were declared'". FA-B4 / RG-B2: six spaced or hyphenated spellings,
then `pa\x01ss` and `ᴘᴀss`. Could not break: the five round-1 blockers on their literal inputs; 13
injection payloads in every customer slot; 22 criteria rows re-derived. Deviations: six pooled ratios
`0.00e+00`; Wilson `1.11e-16`.

**Round 3 at `7fa690b` — fresh attack `FAIL` (3 blockers); regression `FAIL` (3 blockers).** Carried
rows 1–5 above, quoted there. Round-2 blockers all closed on their literal inputs (42,849 swaps, 0
accepted; `18/22 (81.8%) [61.5, 92.7]ᵇᶜ` on the 20-blanked-positives table; the fairness-only row
printed). Could not break: `stats/` untouched, seven constants as E7; 60 i.i.d. Wilson cells worst
deviation `2.22e-16`; the repaired mask against numpy and statsmodels `0.00e+00` to `1.11e-16`; the
wheel; imports with 13 modules hidden; watermarks `TRIAL` / `LICENCE EXPIRED` / `NO LICENCE`.

## The decisions taken today

| Decision | Rule as shipped | Where | Test that feeds the input |
|---|---|---|---|
| `--format` default | `json,html`; HTML only on licence `ok` / `grace`; otherwise JSON, exit 4 (DEC-45) | `cli.py`, README | `tests/test_render_t8.py` (T8 only on ok / grace) |
| Where T8 goes | `<out>/T8.html` beside `run.json` | `run.py` | same |
| Rounding | D4 §1.2 once in `render/format.py`; templates do no arithmetic; JSON keeps full precision | `render/format.py` | `test_twenty_printed_cells_equal_fmt_number_of_the_document_number` |
| Customer numbers | `format.declared`: every digit `run.json` carries (`0.80` in YAML prints `0.8`) | `render/format.py` | `test_declared_numbers_print_every_digit_on_the_page` (0.4275, 0.8525, 0.0125, 0.1005) |
| Criteria rows | by position (`criterion_index` on claims); author and justification from `declaration_index` | `criteria.py`, `render/html.py` | `test_two_criteria_sharing_an_id_print_their_own_author_and_justification` |
| Customer text | verbatim, escaped, bidi controls as character references; a verdict word in it is flagged, not refused | `render/html.py` | `test_html_injection_in_a_justification_and_a_level_label_is_escaped` |
| H02 on `y_pred` | a value outside classes ∪ indeterminate values halts, with or without a score column | `gates.py` | `test_y_pred_outside_the_declared_classes_is_h02_at_the_gate_and_through_the_cli`, `test_h02_on_y_pred_fires_beside_a_score_column` |
| Blank / indeterminate `y_pred` | no score column: blank excluded under `excluded_missing_score`; declared indeterminate value → indeterminate row (with or without a score column — needs 3) | `io/schema.py` | the two repair-2 tests in row 1 |
| Pointers off the eight shapes | refused, `value_ref_unbound` (43 codes) | `narrate/checker.py` | `test_every_value_ref_swap_to_every_number_path_is_rejected` |
| `free_text` normalisation | NFKC, casefold, NFD, drop `Mn` / `Cf`, 27 confusables + small capitals, U+2010–2015 → `-`, runs of letters and hyphens, up to 14 tokens joined | `narrate/checker.py` | `test_free_text_separated_control_and_small_capital_spellings_are_rejected` |
| `guidance_refs` item | `{id, label, draft, url}`; `draft` is structured data | `run.py`, output schema | `test_every_fda_aidsf_anchor_carries_the_draft_label_in_data_and_on_the_page` |
| tokens.json contrast | D5's stated figure kept beside the computed one until the design day (DEC-51) | `design/tokens.json` | `tests/test_render_theme.py` |

## CLI and output changes S4 must know (DEC-43)

The site is pinned at `a0c9abc`; nothing below breaks it until S4 moves `ENGINE_REF`.

- **`run --format`**: new; default (none given) = `json,html`; values `json`, `html` (case folded:
  `HTML` → `html`); `docx`, `pdf,json` → exit 5, typed line, no directory.
- **`run --templates`**: new; default `T8`; `T1` / `T7` → exit 0, no document, a typed "not built in
  E8" line; `T9` → exit 5.
- **`<out>/T8.html`**: new file beside `run.json` on licence `ok` / `grace`. Otherwise the line
  `HTML not written: licence refused (no_file); run.json only (D1 section 7: after grace, JSON only)`
  and exit 4 (measured now).
- **Next-step lines**: `Next step: open T8.html beside run.json; T1 and T7 land on E9 (docs: /docs/run)`
  and `Next step: proofpack licence install FILE, then run again for T8.html (docs: /docs/run)` (both
  measured now), plus a `--format json` variant; E7's `then the T1/T7/T8 renderers` line is gone.
- **`run.json` keys**: `claims` (list, required), `claim_rejections` (list, required; `reason_code`
  enum of 43), `guidance_refs` filled (3 items on the e2e run, each `{id, label, draft, url}`);
  `criteria_results[].declaration_index` (int or null, required); `overall` non-null on clustered and
  `y_pred`-only tables; `overall.threshold_free.suppressed_reason` on a `y_pred`-only table. On the
  e2e fixture `run.json` is 253,943 bytes (216,303 in the E7 note), 20 top-level keys.
- **`flow`**: `excluded_missing_score` also counts blank `y_pred` cells on a table without a score
  column; `indeterminate` also counts a `y_pred` equal to a declared indeterminate value, **beside a
  score column too** (repair 2's note omitted this; lens-3 RG-N1).
- **H02** fires on `y_pred` (`y_pred contains value(s) outside declared classes and indeterminate
  values`, `detail.column`); `HALT_CODES["H02"]` and the `schema_v1.json` `y_pred` description changed.
  The demo's HALT set, if it lists H02 shapes, needs the case.
- **`--json-log` `run` payload**: six new keys `formats`, `templates`, `documents`, `document_notes`,
  `claims`, `claim_rejections` (lens-1 RG-N6).
- **Dependencies**: `jinja2>=3.1` and MarkupSafe at run time; `tokens.json` and three templates in the
  wheel. `proofpack-site/scripts/demo_sample.py` line 62 calls `run` with no `--format` (read now), so
  the Pyodide bundle must carry jinja2 + MarkupSafe or the call must pass `--format json`.

## Re-run these

Run now (23 September, before 12:15 by the machine clock) at `7fa690b` in the main tree; `python -c
"import proofpack;print(proofpack.__file__)"` printed
`C:\Users\joshs\GPS\ProofPack\proofpack\src\proofpack\__init__.py` in each shell first. `S` =
`C:/Users/joshs/AppData/Local/Temp/claude/C--Users-joshs-GPS-ProofPack/a743e7e5-3be8-4d70-9ed0-681f5784a106/scratchpad`,
`H` = `$S/handoff-E8` (holds `repros.py`, `html_run.py`, `sweep_day8.txt`), `E` = `$S/e2e_shell`.

| Command | Git Bash | PowerShell 5.1 |
|---|---|---|
| `python -m pytest -q -p no:cacheprovider` | `1012 passed, 1 skipped, 1 xfailed in 73.18s` | `1012 passed, 1 skipped, 1 xfailed in 72.41s` |
| `-m day8` / `day7` / `day6` / `day5` | `224 passed, 790 deselected` / `139` / `285` / `54` | the same four counts |
| `-m day4` / `day3` / `day2` / `day1` | `182` / `29 passed, 1 xfailed` / `40` / `59 passed, 1 skipped` | the same |
| `python -m ruff check .` / `ruff format --check .` | `All checks passed!` / `134 files already formatted` | the same |
| `python -m proofpack.cli doctor --offline` | `All essential checks passed.` / `Next step: proofpack declare --out criteria.yaml, then proofpack map --input test.csv --criteria criteria.yaml`, exit 0 | the same, `exit=0` |
| `python scripts/mutation_sweep.py --list` | 212 lines: 34 day5, 112 day6, 25 day7, 41 day8 | 212: the same split |
| `python scripts/mutation_sweep.py --marker day8` (worktree at `7fa690b`, `PYTHONPATH` forced; it plants into source, so not in the main tree) | `41 planted, 41 killed, 0 survived; 298 s`, worktree clean after | not run |
| `PYTHONIOENCODING=utf-8 python $H/repros.py` (the five blockers, from the engine root) | `engine claims: 70 accepted: 70` / `B-relabel: CL-0001 sensitivity as AUROC_ESTIMATE -> True \| CL-0062 met as CRITERION_NOT_MET_RECORD -> True` / `B-homoglyph: [None, None, None]` / `B-well-calibrated: None None` / `B-threshold_free: ['auprc', 'auroc', 'auroc_wald', 'prevalence', 'roc']` | identical |
| blocker 5: `checker.py:523` → `if False:` in a scratch worktree, then `f('com\u00adpass')` and `-m day8` | `free_text_verdict_word` / `224 passed, 790 deselected` (restored; tree clean) | not run |
| `PROOFPACK_HOME=<empty> python -m proofpack.cli run --input $E/test.csv --criteria $E/criteria.yaml --out $E/pack_handoff8_bash --offline` (no licence) | `analysed 400 of 400 rows; criteria rows: 2 met, 2 not met, 5 not assessable` / `licence refused (no_file); watermark: LICENCE EXPIRED - not for submission` / `HTML not written: …` / `Next step: proofpack licence install FILE, then run again for T8.html (docs: /docs/run)`, `exit=4`; `ingest_report.json`, `run.json` 253,943 bytes, 20 keys, 70 claims, 0 rejections, 3 guidance refs, `declaration_index [0, 1, 2, 2, 2, 3, 4, 5, None]` | the same lines, `exit=4`, `run.json` 253,943 bytes |
| `PYTHONIOENCODING=utf-8 python $H/html_run.py $E $H/html_bash` (valid test-only licence via `cli.main(..., registry=ephemeral_registry())`) | `licence ok (valid)`, `exit 0 ['T8.html', 'ingest_report.json', 'run.json']`, `T8.html bytes 40145 CRLF False BOM False` | identical |

`python -m proofpack doctor` does not run (carried 18); use the `.cli` form. To repeat a pre-fix
figure, add a worktree at the base sha and force `PYTHONPATH` — the editable `.pth` otherwise imports
the main tree.

## Tomorrow needs

Day 9 is **E9 — template library, T1, T7** (`spec/briefs/day9_E.md`): ~40 sentence templates keyed to
claims, T1's 15 sections, T7 from the run, `/sample-pack`, model card v1.1, golden snapshots, every
FDA-draft anchor labelled, forbidden grep 0, render under 60 s on 100k rows, D5 furniture and figures
F2–F5. What E9 inherits, in order:

1. **Repair 3 before the sentence renderer.** E9 fills skeletons from claims, so carried 1 becomes a
   wrong sentence the day a relabelled claim reaches it: bind each template to its metric family and
   `CRITERION_NOT_MET_RECORD` to `not_met` rows, bind slot order for `FAIRNESS_GAP` /
   `CALIB_HIERARCHY` (or fill by facet and record it), give count templates their paths. Carried 4
   and 5 (restore the substring test and the mutant, with the `com\u00adpass` test). Carried 3 as an
   H08 on `threshold_free` as an id — `overall_block`, so DEC-12(i) applies. Carried 2 waits on
   needs 2.
2. **DEC-48**: `NO LICENCE - not for submission` for a run with no licence file; the renderer
   already prints the manifest's string.
3. **T1 prints** `calibration: null` beside `calibration_suppressed_reason` (DEC-36), the IPA
   annotation from the row tier (DEC-35), the 0.897 cell with its tier (DEC-34); section 7 prints no
   status word when no criteria exist (carried 23).
4. **DEC-55**: `gate_h05` under `clustering: case_id` — an E9 item with its own DEC-12(i) lens.
5. Item 11 (cut) and the decided-not-built E rules: DEC-47 (ledger after the write), DEC-49 (one
   mapping default, with lane A).
6. Carried 8 (a planted anchor map row) with E9's anchor work; carried 19 before any narrative
   uses `free_text`.

## Needs from Josh

E7 needs 1–7 were all decided on 23 September (DEC-44 to DEC-50) and E8 need 1 by DEC-51; none carries.

1. **D1 §5.6 H02 row and §2 `y_pred` row** should read "y_true or y_pred" and say what a blank `y_pred`
   does (as built: excluded under `excluded_missing_score` when there is no score column). Spec is
   read-only for lane E.
2. **What `free_text` defends against** (carried 2, 19, 20): accidental verdict words, or a person
   evading the list. As built it maps a closed list of 27 confusables plus small capitals; nine
   homoglyphs pass and ordinary prose ("in significant") is refused. Recommendation: accidental words
   only, the limit written into the checker docstring and D1 §4.4, since no LLM is in the pipeline at
   launch and every engine claim carries `free_text: null`.
3. **A `y_pred` at a declared indeterminate value beside a valid score** (carried 6): leave the
   analysis (as built at `7fa690b`: AUROC `0.8817` on lens 3's table) or be scored by its score (as at
   `657ef11`: `0.8632`)?
4. **The draft label spelling** (carried 30): D4 §1.1 reads "per draft guidance (Jan 2025), not for
   implementation"; the page and the binding text read "draft guidance (January 2025), not for
   implementation". Amend D4 to the built spelling?
5. **The declarations echo** (carried 34): `run.json`'s block as sorted YAML (as built, so the page is
   a function of `run.json`) or the customer's `criteria.yaml` verbatim (D4 §11 item 3)?

## Open questions

1. Is `json,html` the right `--format` default before T1 / T7 exist, given the site's demo call passes
   no `--format`?
2. Should E9's sentence renderer fill slots by pointer facet rather than position (carried 1's
   permutation cases then print right), with the checker still binding the template?
3. `resolve`: should two accepted claims on one slot both stand (carried 24), and should the second
   rejected claim on a filled slot read `substituted: true`?
4. Should the corpus pin a minimum count (130 today) beside the 100 % rejection test?
5. Should T8's own four anchors join `guidance_refs` so data and page list the same seven (carried 33)?

## Sentences refused

Aggregated from the build note (none recorded: no builder note), the two repair notes and the six lens
notes.

- "The checker binds every claim to its Number." — refused in all six lens notes and repair 1;
  written instead: the swap and mutation counts (42,849 / 0; 8,988 / 39) and carried 1.
- "free_text cannot spell / carry a verdict word (past the rule)." — refused by repair 1, lens 1,
  lens 2 FA and RG, lens 3 FA; written instead: the literal spellings rejected and accepted.
- "Repair 2 closes the free_text evasions." — carried 4 is a regression it introduced.
- "The sweep kills every non-equivalent mutant." — carried 5.
- "H02 catches every mislabelled y_pred" / "a y_pred the engine cannot score." — the blank and the
  `NA` passed at `657ef11`; written instead: the tables fed.
- "The blank-y_pred repair is correct." — written instead: one 120-row and one 200-row clustered
  table re-derived, deviations 0 to 1.11e-16.
- "Repair 2 changes nothing beside a score column." — carried 6.
- "The S4 list is complete." — carried 6 was missing from it.
- "The declared value prints as the manufacturer wrote it." — deleted from the caption; `0.80` in
  YAML is `0.8` on the page.
- "The renderer prints the declared value as declared." — written instead: `declared()` equal to
  `json.dumps` on eleven literals.
- "The checker never raises (on malformed input)." — deleted; the nine claims and two documents fed
  are named.
- "A right-to-left override cannot reorder the page." — deleted; the two byte assertions named and
  the stylesheet rule stated as unchecked in a browser.
- "The renderer escapes every injection" / "the page is safe against injection." — written instead:
  the twelve, then thirteen, literal payloads and the slots fed.
- "No number on the page is untraceable to run.json" / "every number traces to run.json." — written
  instead: 76 digit runs on one page, 22 rows re-derived.
- "The clustered overall block is correct" / "correct on every declaration." — written instead: six
  pooled ratios at `0.00e+00` on named fixtures, and carried 3.
- "The wheel test proves the packaging." — written instead: the files in one wheel and the paths in
  one venv.
- "The new tests would have caught the lens findings." — nine of ten repair-1 tests failed at
  `29fc04e`; the tenth and two repair-2 tests are pins, named.
- "The checker rejects every semantically false claim." — 130 literal cases are rejected; nothing is
  known about the 131st.

In this note: "the checker is ready for E9's sentences" was not written (carried 1); "T8 is
submission-ready" was not written (carried 3 reaches the page, and 29–31, 34 are open page items);
"repair 2 fixed the round-2 blockers" was written only as "closed on their literal inputs", as lens 3
measured it.
