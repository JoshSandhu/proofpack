# Lane E - build day 8, repair 3 and lens round 4 - 2026-09-23

**The final lens round (4, at `fe35332`) is `FAIL`: the fresh attack found four blockers (the regression lens passed with none), and they are open - the T8 criteria table prints another operating point's Number, or a dash, when an operating-point id contains `.` or `[n]` (B1); a `y_pred`-only table with two operating points prints the same `y_pred` numbers under both thresholds and sets both criterion statuses from them (B2); the engine's own `CALIB_NA` claim states a reason the document contradicts and the checker accepts it (B3); and 43 `free_text` spellings of listed words are accepted (B4).** Forty-four items are carried (the table below, the four blockers among them); B1 and B2 put a wrong Number and a wrong status on the T8 page, so the engine stays unpushed and E8 is not done.

This note supplements `handoffs/2026-09-22_E.md` (items 1-10, repairs 1-2, lens rounds 1-3), which is not rewritten. Since then: `fe35332` (repair 3, on top of `0f553c9`, the orchestrator's merge of lane A's `a-p2-egress`). Nothing is pushed. Main tree now, both shells: **1122 passed, 1 skipped, 1 xfailed**; `-m day8` 334; `git diff 7b2ca2a HEAD -- src/proofpack/stats/` is 0 lines; `tests/` since `7b2ca2a`: 153 `A`, 7 `M`, 0 `D` (all measured now).

## What landed

Items 1-10 and repairs 1-2: see `2026-09-22_E.md`, "What landed". Today's work is repair 3 (`fe35332`, note `scratchpad/notes/E8_repair3.md`), on lens round 3's five blockers.

| Lens-3 id | Change | Test ids (`day8`) | Literal input -> measured figure |
|---|---|---|---|
| RG-B1 (repair 2 had re-opened 180 `well-calibrated` spellings) | `free_text_reason` reads the text five ways (`_readings`: the `657ef11` map, the `7fa690b` map, the full map, capital `I` as `l`, `rn` as `m`) and pools the words; a reading containing `wellcalibrated` once hyphens are removed is `free_text_verdict_word` | `test_e8_repair3.py::test_every_spelling_rejected_at_657ef11_or_7fa690b_is_still_rejected`; corpus `139` | 23,020 generated texts, 0 lost against frozen copies of both earlier rules (repair 3). Lens 4 regression: 107,776 texts, rejected at `0f553c9` and accepted at `fe35332`: 0. Lens 4 fresh attack: 650,760 texts, 0 lost |
| RG-B2 / FA-B1 (template relabels) | Rule 8b, new code `template_mismatch` (44 codes): `CRITERION_NOT_MET_RECORD` needs a `not_met` row, `ATTAINABILITY_NOTE` a boolean `attainable_at_n`, `AUROC_ESTIMATE` `metric_id auroc`, estimate pointers match `TEMPLATE_SHAPES`, family pointers in `SLOT_ORDER`, count templates in `SCALAR_SLOTS` | `::test_the_round_3_template_relabels_are_rejected_with_the_named_code`, `::test_every_engine_claim_relabelled_to_every_other_template`; corpus `131`-`138` | 70 claims x 55 templates = 3,850 relabels, 4 accepted and named in the test. Re-run now: `CL-0001 sensitivity as AUROC_ESTIMATE -> False`, `CL-0062 met as CRITERION_NOT_MET_RECORD -> False` |
| RG-B3 (a mutant withdrawn on an untested sentence) | `checker_format_characters_kept` restored to the day-8 sweep; the "became equivalent" comment replaced | `::test_a_format_character_inside_an_unlisted_word_is_dropped_not_a_boundary` | `com` + U+00AD + `pass` -> `None` (re-run now); the sweep kills the mutant (lens 4 regression) |
| FA-B2 (nine homoglyphs) | `ɑ ƒ ǀ օ Ꮲ` hand-mapped; `ϲ`/`Ϲ` mapped before NFKC; the capital-`I` and `rn` readings | `::test_the_nine_homoglyph_literals_and_their_capitals_are_rejected`; corpus `140` | Re-run now: `pɑss`, `faiI`, `ƒail` -> `free_text_verdict_word` x3. The TR39 data was not in the engine at `fe35332` (see DEC-60 below) |
| FA-B3 (`threshold_free` as an op id; a statistical-gate input, DEC-12(i)) | `io/declare.py`: an operating-point id in {`threshold_free`, `auroc`, `brier`} is H08, `detail {field, reserved}` | `::test_an_operating_point_id_the_document_reserves_halts_h08[...]` (3), `::test_the_ids_the_lens_ran_clean_still_run` | Re-run now: `HaltError: H08: operating point id is a key the run document reserves: threshold_free`. Lens 4 regression fed 27 ids: the three halt, 24 run; `Threshold_Free` re-derived with numpy and statsmodels Wilson, worst deviation `1.11e-16` |
| RG-N3, FA-N3, FA-N4 | Sentence corrections (corpus `129` "two to fourteen"; blank-`y_pred`-on-blank-label counted under `excluded_missing_label`; `FLOW_COUNTS` skeleton names the `y_pred`) | `::test_the_refused_sentences_are_gone_from_the_shipped_text`, `::test_a_blank_y_pred_on_a_blank_label_row_counts_under_missing_label` | 12 blank `y_pred`, 6 on blank labels -> label 6, score 6, analysed 108 (repair 3). Lens 4 found the new sentence false on a `dev` row (carried 46) |
| DEC-12(ii) | 22 new day-8 mutants (63 day-8, 16 `ap2` in the day-8 run) | `tests/test_sweep_day8.py` | Re-run now (Git Bash): **79 planted, 79 killed, 0 survived; 875 s**, exit 0 (rows 7, 47, 48 are mutants outside this list) |

Pre-fix (repair 3's new and changed test files copied into a `0f553c9` worktree, `PYTHONPATH` forced): `22 failed, 154 passed`, every first `E` line an assertion (repair 3; lens 4 regression re-measured the same figure).

## What was cut and why

1. **Item 11 (E7 carried 20-24: `id` uniqueness, `scope` required, `date` range, `value` finiteness, the absent age band)** - not started. The builder hit the weekly usage limit on 22 September; this is the limit, not a decision.
2. **Repair 4** - not run. This run was one lens round and a handoff, as the orchestrator set it; lens 4's four blockers go to tomorrow.
3. **The TR39 data in the engine** - repair 3 (committed 13:30 BST) did not fetch `confusables.txt`: a download needs Josh's approval in chat, and a relayed decision is not that. Josh approved it and the orchestrator fetched it at 13:45 (spec `0e62ff9`); it is not vendored in the engine at `fe35332`.
4. **T1, T7** - `--templates T1` / `T7` print a typed "not built in E8" line (E9's).

## What did not land and why - the 44 carried items

Numbers 6-40 are the 22 September note's numbers (its repros hold unless a row says otherwise); 41-53 are new. Rows 41-44 were re-run now in both shells and row 7 in Git Bash; the others are as the named lens measured them.

**Closed by repair 3 since the 22 September note** (lens 4 regression, and rows 1-5 re-run now with `handoff-E8/repros.py`): 1 (relabels, on the literal inputs; the residue is 51 and 52), 2 (the nine homoglyphs; the residue is 44), 3 (`threshold_free`), 4 (`well-calibrated` regression), 5 (the mutant), 10, 11 (the residue is 46), 12, 13.

| # | Item | Why carried | Repro (one line) |
|---|---|---|---|
| **41** | **Blocker (lens-4 FA-B1).** `criteria_rows` resolves `metric_ref` as a dotted path: ops `0` (0.2) and `[0]` (0.8) -> row `C_br` prints `99/101 (98.0%) [93.1, 99.5]` (op `0`'s) beside `0.287` and `criterion not met`; op `t0.5` prints `—` beside `criterion met`; op `a[0` -> exit 5. Same at `0f553c9` (E8's renderer) | Found in round 4; round cap | `repros4.py` line `B1:` (re-run now: the three rows above) |
| **42** | **Blocker (lens-4 FA-B2; statistical gate).** No score column: every operating point reads `y_pred`; ops 0.1 and 0.9 both `{tp 85, fn 16, fp 39, tn 160}`, sensitivity 0.8416, both criteria `met`, `warnings []`. On the score column at 0.9 the lens measured 12/101. Since E8 item 1 (`86b5fdd`) | Needs a rule (needs 6); DEC-12(i) lens | `repros4.py` line `B2:` |
| **43** | **Blocker (lens-4 FA-B3).** `CALIB_NA` emitted whenever `calibration` is not an object; on `no_score_column` its sentence reads "...declared as probability, not a probability." and `check` accepts it | Rule 8b inspects the null, not the reason | `repros4.py` line `B3:` -> `no_score_column True Calibration statistics are not computed because the score was declared as probability, not a probability.` |
| **44** | **Blocker (lens-4 FA-B4, with RG-N2).** 43 accepted texts: 33 look-alikes (`fai\|`, `faiΙ`, `ꓑass`, `gⲟⲟd`, `veгdict`, ...) plus `faiＩ`, `fai𝐈`; `well calibratedness` and four more separator-plus-suffix spellings; `&sect; eight`, `ninety`, `Roman IV` | DEC-60 data not vendored; separator rule and number words need a rule (needs 2) | `repros4.py` line `B4:` -> six `None`. Against the fetched TR39 18.0.0 file (SHA-256 `6ed3ee96...`, measured now): all 34 distinct look-alike characters in lens 4's 38 literals are listed (`handoff-E8-r4/tr39_note.py`); two map to `l` plus a combining mark. What the engine does with the file once vendored is not measured |
| 45 | Manufacturer words outside `.customer-text`: model `Safe triage`, version `2 unbiased`, op id `pass` -> 4 forbidden-word hits (3 page headers, 1 criteria cell) and the `<title>`. Same at `0f553c9` | D4 §1.2 read literally vs §1.5's header (needs 7) | lens-4 FA non-blocking 1 |
| 46 | Repair-3 sentences measured false: blank `y_pred` "counted under `excluded_missing_score` when `y_true` is present" (README, two schema descriptions, `analysis_mask` docstring) - a `dev` row counts under `dev_rows` (120 rows, 20 `dev`: score 5 not 10); `templates.py` "one skeleton differs" (three do); `_BEFORE_NFKC` (NFKC(U+03F9) is U+03A3); `_HOMOGLYPHS_R3` calls U+ABB2 a capital; `_SMALL_CAPITALS` range (11 unmapped); `_readings` "every capital `I`" (ASCII only) | Round cap | lens-4 FA non-blocking 2; lens-4 RG N1 |
| 47 | Reserved-id gate mutants survive `-m day8`: `reserved_first_op_only` (no test feeds a reserved id in second position), `reserved_casefold` / `reserved_gate_case_sensitive_to_lower` (no test feeds a case variant that must run) | Pins for a statistical gate; DEC-12(i) | lens-4 FA non-blocking 3; lens-4 RG N5 |
| 48 | Checker mutants survive `-m day8`, non-equivalent: `second_earlier_reading_dropped` (`ᴘassɑ`, `ᴄonsistentɑ` accepted, 334 passed), `rn_first_only` (`turn rneets`), `capital_i_reads_text_not_raw`, `wellcal_substring_full_only`, `shapes_subgroup_estimate_op_only`, `flow_scalar_slots_site_count_any` | One literal per mutant | lens-4 RG N5; lens-4 FA non-blocking 3 |
| 49 | D4 §1.2's Clopper-Pearson beside Wilson at k = 0 or k = n: `proportion_exact_alternative` has no caller in `src/`; T8 prints one interval. Predates E8 | Data gap; T1 (E9) | lens-4 FA non-blocking 5 |
| 50 | `claim_id` `CL-０００１` (fullwidth digits) accepted: `\d` matches Unicode digits | One-line `[0-9]` | lens-4 FA non-blocking 6 |
| 51 | 8,988 single-field mutations, 11 accepted: 7 `metric_id` swaps inside `CALIB_HIERARCHY` (4) / `FAIRNESS_GAP` (3); 4 criterion relabels onto rows whose status the template states | The family skeleton prints every slot; recorded | lens 2's `b_checker.py` (lens 4 both) |
| 52 | Rule 8b accepts a subset of a family's pointers (`CL-0060` with `oe` dropped; `FLOW_COUNTS` over `/flow/n_sites` alone) | E9 must fill slots by pointer facet | lens-4 RG N3 |
| 53 | The capital-`I` reading refuses prose where `I` plus `l`-joined letters form a listed word (none in the four guard sentences) | Errs to refusal, like 19 | repair-3 carried 4 |
| 6 | A `y_pred` at a declared indeterminate value beside a score column leaves the analysis (AUROC `0.8632` -> `0.8817` on lens 3's table) | Needs 3 | 120 rows, 12 such, `report_both_ways` |
| 7 | `checker_smallcap_s_unmapped` survives: re-run now in a `fe35332` worktree, mutant planted -> `f('paꜱꜱ')` `None`, `-m day8` `334 passed`; unmutated -> `free_text_verdict_word` | One literal | plant; `-m day8` |
| 8 | `anchors_month_year_fixed` survives (every draft map row dated 2025-01-07) | E9 anchor work | plant; `-m day8` |
| 9 | `_declaration_for` wrap on an out-of-range index | Unreachable | plant |
| 14 | Docstrings cite lens ids, no D1 §4.4 line (`_tokens`, `_words`, `_readings`); `errors.py` H08 text does not name reserved ids | Documentation | read them |
| 15 | `check` "reads no environment" (reads two packaged files) | Wording | read `checker.py` |
| 16 | Repair-2 pre-fix artefact 25/137 vs note 26/136 | Record | - |
| 17 | Golden mask is `manifest.numpy` only | Record | unmasked render, 1 line |
| 18 | `python -m proofpack doctor` fails (no `__main__`); use `python -m proofpack.cli` | Docs or `__main__.py` | run it |
| 19 | Token join refuses prose: "Specificity was reported in significant detail." | Needs 2 | `f(...)` |
| 20 | Number words, inflections and derived words accepted: `ninety`, `IV`, `guidances`, `unsafe`, `safely`, `biases`, `unbiasedness`, `consistently`, `passable`, `goodness`, `clearances`, `approvals` | Needs 2 | `f('unbiasedness')` |
| 21 | Op ids `a/b`, `op~1`: 58 of 60 engine claims refused `operating_point_mismatch` | False refusals | declare `op~1` |
| 22 | A subgroup level with every row excluded vanishes from `subgroups` | DEC-08 question, E9 | sex `F`, all `y_pred` blank |
| 23 | Section 7 prints the three status words with no criteria | T8 template | `criteria: []` |
| 24 | `resolve` keeps two accepted claims on one slot | Open question | two ids, one slot |
| 25 | `declared(1e22)` positional | Record | `fmt.declared(1e22)` |
| 26 | `--format HTML` folds; `''` asymmetry; `--templates T1` hint | S4 wording | the three calls |
| 27 | No-licence watermark `LICENCE EXPIRED - not for submission` (re-run now) | DEC-48 is an E9 build | e2e run below |
| 28 | `guidance_ref` may be any map row | As specified | swap to `FDA_STAT2007_CI` |
| 29 | `calibration: null` + reason, DEC-35 IPA: not on T8 | T1 (E9) | - |
| 30 | Draft label spelling vs D4 §1.1 | Needs 4 | read the page |
| 31 | Fairness row mixed scales; `font-weight` literals; D4 §11 items 4, 10 | Design day / E9 | read the page |
| 32 | No `jinja2` -> exit 5 after `run.json` | S4's Pyodide bundle | hide `jinja2` |
| 33 | `guidance_refs` 3 vs page 7 | Open question | count both |
| 34 | Declarations echo is sorted YAML, not the file | Needs 5 | section 3 |
| 35 | `integrity.halts` never printed | Not in D4 §11 | `T8.html` |
| 36 | Wheel test `pytest.skip` path | One line | `_build_wheel` |
| 37 | `declaration_index: true` reads entry 0 | Unreachable | hand-built row |
| 38 | `y_true` `NA` declared indeterminate -> `excluded_missing_label` | Day-1 loader | `NA` |
| 39 | Bare YAML `date:` -> H08 | Customer must quote | unquoted date |
| 40 | `unicode-bidi: isolate` and `@page var()` never checked in a browser | No browser driven | print T8 |

**E7 items still open, under their old numbers** (repros in `2026-09-21_E.md`; unchanged since the 22 September note; not re-run today):

| E7 # | State at `fe35332` |
|---|---|
| 1-16 | Open (16 is the ledger race) |
| 17 | Open: a fairness-only `criteria.yaml` is not counted (DEC-47 counts runs "with a `criteria` list") |
| 18 | Decided (DEC-47: count after the write); not built |
| 19-23 | Open; 20-23 were item 11 (cut) |
| 24 | Half closed (rows by position and `declaration_index`); the H08 uniqueness rule was item 11 |
| 25 | Decided (DEC-48); not built (carried 27) |
| 26, 28, 29, 33-37, 39-41, 43 | Open |
| 27 | Closed (DEC-50) |
| 30, 32 | Lane A (A-P2, merged at `0f553c9`; see `2026-09-22_A.md`) |
| 31 | Decided (DEC-49); not built |
| 38 | Open |
| 42 | Closed (E8 item 1) |

## What the lenses found

Every lens ran in detached worktrees with `PYTHONPATH=<worktree>/src` forced and `proofpack.__file__` printed first. Rounds 1-3 are summarised in `2026-09-22_E.md`; in one line each:

- **Round 1 at `29fc04e`:** fresh attack FAIL (5: pointer not bound to metric / op / subgroup, 339 swaps accepted; nine `free_text` spellings; a `yes`/`no` `y_pred` table printing `0/42`; declared values rounded to three places; a missing `prior_version` exits 5). Regression PASS. Deviations: Wilson against statsmodels `1.11e-16`, AUROC against sklearn `0`.
- **Round 2 at `657ef11`:** fresh attack FAIL (4), regression FAIL (2): blank `y_pred` scored as negative; 225 of 418 Numbers off the eight pointer shapes; fairness-only criteria; spaced and control-character spellings. Deviations: six pooled ratios `0.00e+00`.
- **Round 3 at `7fa690b`:** fresh attack FAIL (3), regression FAIL (3): the five repair-3 rows above. Deviations: 60 Wilson cells, worst `2.22e-16`.

**Round 4 at `fe35332` - fresh attack `FAIL` (4 blockers, rows 41-44); regression `PASS` (no blockers, seven non-blocking, rows 46-48, 52 and records).** The blockers, as the fresh-attack note words them:

- B1: "the T8 criteria table resolves `metric_ref` as a dotted path, so an operating-point id containing `.` or `[n]` prints another operating point's Number, or no Number".
- B2: "a `y_pred`-only table with two operating points prints the same `y_pred` numbers under both thresholds and sets both criterion statuses from them".
- B3: "the engine's `CALIB_NA` claim states 'the score was declared as probability, not a probability' on every `y_pred`-only run, and the checker accepts it".
- B4: "`free_text` accepts spellings of listed words: 35 homoglyph literals, five `well calibrated` spellings with a suffix, and three of the orchestrator's named corpus cases".

The regression lens graded the `well calibrated` separator-plus-suffix class (its N2) non-blocking, because every commit since `657ef11` accepted it; the fresh attack graded it a blocker under the rule it was given. This note carries it as a blocker (row 44).

What round 4 could not break (quoted figures from the two notes):

- The overall block on 300 rows in 100 cases: two-by-two `{tp 69, fn 18, fp 30, tn 183}` equal to the lens's numpy count; six proportions deviate `0.00e+00`; `proportion_ci` equal on five cells; seven metrics carry `clustered_data_analytic_ci_invalid`. `git diff 0f553c9 fe35332 -- src/proofpack/stats/ src/proofpack/run.py src/proofpack/criteria.py` is empty.
- Twenty-two T8 cells re-derived from `run.json` with the lens's own `Decimal` half-even formatter on a 360-row clustered run: 0 mismatches. Lens 3's script: another 22 rows, 0 mismatches; 60 Wilson cells, worst `2.22e-16`.
- The reserved-id gate: 27 ids fed; `Threshold_Free` two-by-two `{tp 57, fn 12, fp 35, tn 96}` equal; sensitivity 57/69, specificity 96/131, PPV 57/92, NPV 96/108 against statsmodels Wilson, worst `1.11e-16`.
- The checker: 42,849 value-ref swaps, 0 accepted; the orchestrator's structural cases each get the expected code.
- The renderer: thirteen injection payloads (U+202E written as `&#x202e;` 111 times; `{{ 7*7 }}` kept literal); the draft label beside all 14 AI-DSF mentions and on 3 `guidance_refs`; golden 39,800 bytes equal.
- `--offline` with eight network entry points raising: 0 calls. The wheel in a fresh venv with 0 `.pth` files; `tokens.json` resolves inside it.

## The decisions taken today

| Decision | Rule as shipped (or recorded) | Where | Test that feeds the input |
|---|---|---|---|
| DEC-60 (Josh, in chat) | `free_text` maps TR39 confusables to Latin letters, data vendored with URL, version and SHA-256; the class stays open beyond it. Fetched 13:45 (18.0.0, 763,128 bytes, SHA-256 `6ed3ee96...`, re-measured now); **not in the engine at `fe35332`** | spec `0e62ff9`; `checker.py` comment marks the coverage `[unverified]` | none yet |
| Reserved op ids | `threshold_free`, `auroc`, `brier` -> H08 at declaration; case variants run as distinct keys (a choice, not a test: row 47) | `io/declare.py` | `test_an_operating_point_id_the_document_reserves_halts_h08` |
| Template binding | Rule 8b, `template_mismatch`; slot filling left to E9 | `narrate/checker.py` | `test_every_engine_claim_relabelled_to_every_other_template` |
| Earlier rejections kept | Five readings pooled; the test feeds the 23,020 texts rejected at `657ef11` or `7fa690b` (row 48: one reading is unpinned) | `narrate/checker.py` | `test_every_spelling_rejected_at_657ef11_or_7fa690b_is_still_rejected` |
| A-P2 merged into main | `0f553c9` (orchestrator); both packages lens-capped FAIL at merge | main | `-m ap2` 88 |
| Engine not pushed | Held until a lens round passes (orchestrator) | - | - |

## CLI and output changes S4 must know (DEC-43)

The site builds from `a0c9abc` (an A-P1 merge before E7). Moving the pin to `fe35332` brings three lists: E7's (`2026-09-21_E.md`), lane A's A-P2 (`2026-09-22_A.md`: telemetry, `--offline`, `<out>/pseudonyms.json`), and E8's below. The E8 list in full, repair-3 additions marked **new today**:

- `run --format` (default `json,html`; `json`, `html`, case-folded; `docx`, `pdf,json` -> exit 5, no directory) and `run --templates` (default `T8`; `T1`/`T7` -> exit 0 with a typed "not built in E8" line; `T9` -> exit 5). Both read now in `run --help`.
- `<out>/T8.html` beside `run.json` on licence `ok` / `grace`; otherwise `HTML not written: licence refused (no_file); run.json only (D1 section 7: after grace, JSON only)` and exit 4 (measured now, both shells).
- Next-step lines: `Next step: open T8.html beside run.json; T1 and T7 land on E9 (docs: /docs/run)` and `Next step: proofpack licence install FILE, then run again for T8.html (docs: /docs/run)` (both measured now), plus a `--format json` variant.
- `run.json`: `claims`, `claim_rejections` (required), `guidance_refs` filled (`{id, label, draft, url}`, 3 on the e2e run), `criteria_results[].declaration_index`, `overall` non-null on clustered and `y_pred`-only tables, `overall.threshold_free.suppressed_reason`. E7 e2e fixture now: 253,943 bytes, 20 keys, 70 claims, 0 rejections.
- `claim_rejections[].reason_code` enum: 44 codes (**new today: `template_mismatch`**).
- **New today: H08 on an operating-point id `threshold_free`, `auroc` or `brier`**, `detail {field: "operating_points", reserved: [...]}`, exit 3, nothing written. A demo panel that lets a visitor name an operating point must expect it.
- `flow.excluded_missing_score` also counts blank `y_pred` on a table without a score column; `flow.indeterminate` counts a `y_pred` at a declared indeterminate value, with or without a score column. H02 fires on `y_pred`.
- `--json-log` `run` payload: `formats`, `templates`, `documents`, `document_notes`, `claims`, `claim_rejections`.
- `jinja2>=3.1` and MarkupSafe at run time. `proofpack-site/scripts/demo_sample.py` calls `run` with no `--format` (22 September reading), so the Pyodide bundle carries both or the call passes `--format json`.
- No CLI flag, default or output path changed in repair 3 (`git diff 0f553c9 fe35332 -- src/proofpack/cli.py src/proofpack/run.py` is empty, lens 4 regression).

## Re-run these

Measured now (23 September, 14:00-14:40 BST) in the main tree at `fe35332`; `python -c "import proofpack;print(proofpack.__file__)"` printed `C:\Users\joshs\GPS\ProofPack\proofpack\src\proofpack\__init__.py` in each shell first. `S` = `C:/Users/joshs/AppData/Local/Temp/claude/C--Users-joshs-GPS-ProofPack/a743e7e5-3be8-4d70-9ed0-681f5784a106/scratchpad`, `E` = `$S/e2e_shell`, `H` = `$S/handoff-E8-r4`.

| Command | Git Bash | PowerShell 5.1 |
|---|---|---|
| `python -m pytest -q -p no:cacheprovider` | `1122 passed, 1 skipped, 1 xfailed in 117.51s` | `1122 passed, 1 skipped, 1 xfailed in 120.06s` |
| `-m day8` / `day7` / `day6` / `day5` / `ap2` | `334 passed` / `139` / `285` / `54` / `88` | the same five |
| `-m day4` / `day3` / `day2` / `day1` | `182` / `29 passed, 1 xfailed` / `40` / `59 passed, 1 skipped` | the same |
| `python -m ruff check .` / `python -m ruff format --check .` | `All checks passed!` / `155 files already formatted` (lens worktrees: 153; the main tree holds untracked files) | the same |
| `python -m proofpack.cli doctor --offline` | `All essential checks passed.` / `Next step: proofpack declare --out criteria.yaml, then proofpack map --input test.csv --criteria criteria.yaml`, exit 0 | the same, exit 0 |
| `python scripts/mutation_sweep.py --list` | 250 lines: 34 day5, 112 day6, 25 day7, 63 day8, 16 ap2 | 250 lines |
| `python scripts/mutation_sweep.py --marker day8` (copies the tree to a temp directory) | `79 planted, 79 killed, 0 survived; 875 s`, exit 0 | not run |
| `PYTHONIOENCODING=utf-8 python $H/repros4.py` (lens 4's four blockers) | `B1: [('C_zero', '99/101 (98.0%) [93.1, 99.5]', '0.980', 'criterion met'), ('C_br', '99/101 (98.0%) [93.1, 99.5]', '0.287', 'criterion not met'), ('C_dot', '—', '0.842', 'criterion met')]` / `B2: [('op_lo', {'tp': 85, 'fn': 16, 'fp': 39, 'tn': 160}, 0.8416), ('op_hi', {... the same ...}, 0.8416)] warnings []` / `B3: no_score_column True Calibration statistics are not computed because the score was declared as probability, not a probability.` / `B4: [None, None, None, None, None, None]` | identical |
| `PYTHONIOENCODING=utf-8 python $S/handoff-E8/repros.py` (lens 3's five) | `engine claims: 70 accepted: 70` / `B-relabel: ... -> False \| ... -> False` / `B-homoglyph: ['free_text_verdict_word' x3]` / `B-well-calibrated: free_text_verdict_word free_text_verdict_word` / `control com­pass (soft hyphen): None` / then `HaltError: H08: operating point id is a key the run document reserves: threshold_free` (the script predates the fix) | identical first six lines |
| `PROOFPACK_HOME=$H/home_bash python -m proofpack.cli run --input $E/test.csv --criteria $E/criteria.yaml --out $E/pack_h4_bash --offline` (no licence) | `analysed 400 of 400 rows; criteria rows: 2 met, 2 not met, 5 not assessable` / `licence refused (no_file); watermark: LICENCE EXPIRED - not for submission` / `telemetry skipped (--offline); nothing was sent` / ... / `HTML not written: ...` / `Next step: proofpack licence install FILE, then run again for T8.html (docs: /docs/run)`, `exit=4`; files `ingest_report.json`, `pseudonyms.json`, `run.json` (253,943 bytes, 20 keys, 70 claims, 0 rejections, 3 refs, `declaration_index [0, 1, 2, 2, 2, 3, 4, 5, None]`) | `exit=4`, `run.json` 253,943 bytes, the same last two lines |
| `PYTHONIOENCODING=utf-8 python $S/handoff-E8/html_run.py $E $H/html_bash` (test-only licence) | `exit 0 ['T8.html', 'ingest_report.json', 'pseudonyms.json', 'run.json']` / `T8.html bytes 40145 CRLF False BOM False` | identical |
| `python $H/tr39_note.py <confusables.txt> handoffs/2026-09-23_E_lens4_fresh-attack.md` | `literals: 38 distinct look-alike chars: 34 listed in TR39: 34` | not run |

The fetched `confusables.txt` sits in the orchestrator session's scratchpad (`.../498a4e52-21b2-438d-8861-2ecc4702fe63/scratchpad/confusables.txt`, SHA-256 `6ed3ee967c9dfdf6677d563c9985182fbc50a2efb7d6059cd57b2e2ce18f5b92`, 763,128 bytes, `Version: 18.0.0`, measured now). To repeat a pre-fix figure, add a worktree and force `PYTHONPATH`; the editable `.pth` otherwise imports the main tree.

## Tomorrow needs

1. **Repair 4 on lens 4's blockers, then a fresh lens, before E9 builds on them.** 41: read the criteria row's Number from its structured fields (or refuse `.`, `[`, `]` in ids: open question 1), with a test feeding `0` / `[0]` / `t0.5` / `a[0`. 42: the rule Josh picks (needs 6); a statistical gate, so DEC-12(i). 43: bind `CALIB_NA` to `score_not_probability` and give the other two reasons their own sentence (open question 2). 44: vendor TR39 per DEC-60 (URL, version line, SHA-256 beside it; no fetch at run time) and a separator-tolerant `well calibrated` rule; the number words wait on needs 2. With it: rows 46 (sentences), 47 and 48 (one literal per surviving mutant: a reserved id in second position, `Threshold_Free` running, `ᴘassɑ`, `turn rneets`, `paꜱꜱ` for row 7), 50.
2. **E9 per `spec/briefs/day9_E.md`**: about 40 sentence templates keyed to claims, T1's 15 sections, T7 from the run, `/sample-pack`, model card v1.1, golden snapshots, every FDA-draft anchor labelled, forbidden grep 0, render under 60 s on 100k rows, D5 furniture and figures F2-F5. What E9 inherits:
   - Slot filling by pointer facet, not position (rows 51, 52): the checker inspects pointer order and counts, not which slot a pointer fills.
   - `CALIB_NA` (row 43) becomes a printed sentence on T1.
   - T1 prints `calibration: null` beside the reason (DEC-36), the IPA annotation from the row tier (DEC-35), the 0.897 cell with its tier (DEC-34), Clopper-Pearson beside Wilson at k = 0 / n if D4 §1.2 stands (row 49).
   - DEC-48's `NO LICENCE - not for submission` (row 27); section 7 with no status words when no criteria exist (row 23).
   - DEC-55 `gate_h05` under `clustering: case_id`, with its own DEC-12(i) lens.
   - Item 11 (cut), DEC-47 and DEC-49 (with lane A).
   - Rows 8 (a planted anchor row), 19, 20, 44 and 53 before any narrative carries `free_text`.
3. **S4 (day 10)** moves the pin with the three lists above.

## Needs from Josh

E7 needs 1-7 were decided on 23 September (DEC-44 to DEC-50); none carries. The 22 September E8 needs, numbered as before:

1. **D1 §5.6 H02 row and §2 `y_pred` row** should read "y_true or y_pred" and say what a blank `y_pred` does. Open.
2. **What `free_text` defends against beyond TR39** (rows 19, 20, 44, 53). DEC-60 settled the look-alike letters. Still open: number words (`ninety`), Roman numerals, `&sect;` as text, inflections and derived words (`unbiasedness`), `well calibrated` with a separator and a suffix, and prose the join rule refuses. Recommendation: vendor TR39 now; decide the rest with E9's narrative in view, since every engine claim carries `free_text: null`.
3. **A `y_pred` at a declared indeterminate value beside a valid score** (row 6): leave the analysis (as built) or be scored by its score? Open.
4. **The draft label spelling** (row 30): amend D4 §1.1 to the built "draft guidance (January 2025), not for implementation"? Open.
5. **The declarations echo** (row 34): `run.json`'s block as sorted YAML (as built) or the customer's file verbatim? Open.

New today:

6. **A `y_pred`-only table with more than one operating point** (row 42): refuse it with a typed halt, or accept it and print that every threshold row reads the same `y_pred`? Recommendation: refuse (H08, naming the operating points), because a threshold without a score is not something the engine can compute.
7. **Manufacturer words outside `.customer-text`** (row 45): D4 §1.2 says the forbidden words appear nowhere in rendered output; D4 §1.5 prescribes the model name in the header. Recommendation: mark the header's model name, version and the operating-point ids as customer text, so the grep reads them as the manufacturer's.

## Open questions

1. Row 41: resolve the criteria row's Number from `operating_point`, `metric` and `scope` (no limit on customer ids), or refuse `.`, `[`, `]` in ids at declaration?
2. Row 43: new templates for `no_score_column` and `score_not_positive_class_probability`, or no calibration claim for those two reasons?
3. Does E9 start before repair 4 closes rows 41-43, given 41 and 42 reach the T8 page today?
4. Is `json,html` still the right `--format` default while T1 / T7 are unbuilt and the demo passes no `--format`?
5. Should reserved-id matching fold case (`Threshold_Free` runs today as its own key, re-derived correct by lens 4)?

## Sentences refused

Aggregated from the build note (none: no builder note), the three repair notes and the eight lens notes, plus this note.

- "The checker binds every claim to its Number / every template to its pointers." Written instead: 42,849 swaps, 0 accepted; 3,850 relabels, 4 accepted; 8,988 mutations, 11 accepted; row 52.
- "free_text cannot spell a verdict word past the rule" / "rejects look-alike spellings" / "refuses every homoglyph". Written instead: the literals fed, the 43 accepted texts of row 44, and the TR39 figures as data, not engine behaviour.
- "free_text keeps every rejection an earlier commit made." Written instead: 0 lost on 107,776, 18,752 and 650,760 generated texts, and `ᴘassɑ` lost under a mutant with every test green (row 48).
- "TR39 maps these letters" (repair 3, before the fetch). Now measured against the fetched file for lens 4's 34 characters only.
- "The reserved-id gate closes FA-B3 for all ids / cannot be bypassed." Written instead: 27 ids fed, and row 41.
- "Rule 8b fixes slot filling." Written instead: it inspects pointer order and count.
- "The overall block is correct (on every table / declaration)." Written instead: the fixtures and deviations above, and row 42.
- "The renderer prints only numbers from run.json, correctly" / "every number traces to run.json." Written instead: 22 cells matched, 76 digit runs traced on one page, and row 41.
- "The blank-y_pred sentences are now correct." Refused: row 46.
- "H02 catches every mislabelled y_pred." Written instead: the tables fed.
- "The sweep kills every non-equivalent mutant." Refused: rows 7, 47, 48.
- "Repair 2 changes nothing beside a score column." Refused: row 6.
- "The S4 list is complete." Written instead: the three lists and the diff that is empty for `cli.py` and `run.py`.
- "The declared value prints as the manufacturer wrote it." Deleted; `0.80` in YAML prints `0.8`.
- "The checker never raises." Deleted; the nine claims and two documents fed are named.
- "A right-to-left override cannot reorder the page" / "the renderer escapes every injection." Written instead: the thirteen payloads and the byte assertions.
- "The wheel test proves the packaging." Written instead: one wheel's files and one venv's paths.
- "The new tests would have caught the lens findings." Written instead: the pre-fix counts (42, 26 + 1, 22 failed) and the pins named.
- In this note: "repair 3 closed lens 3's blockers" was written only as "closed on their literal inputs"; "vendoring TR39 closes B4" was not written (row 44 says what the file lists, not what the engine will do); "E8 is ready for E9" was not written.
