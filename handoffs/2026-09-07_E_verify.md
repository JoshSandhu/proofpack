# Verification — 2026-09-07 — Lane E (engine) — E1 "Validated ingest and declarations"

Independent verifier, fresh context. Verifying handoff `2026-09-07_E.md` against
`proofpack_spec/plan/CONSTRAINTS_from_Josh_2026-09-06.md` and `proofpack_spec/briefs/day1_E.md`.

## 1. Tests — run exactly as the handoff says, and plain `uv run pytest`

```
$ uv run pytest -q -p no:cacheprovider
51 passed, 1 skipped in 5.30s   # skip: write-access-revocation test cannot run as root

$ uv run pytest -q -p no:cacheprovider -m day1
51 passed, 1 skipped in 5.45s

$ uv run pytest        (plain, no flags)
51 passed, 1 skipped in 4.89s
```
Matches the handoff's pasted results exactly (same counts, same skip reason).

## 2. F12 and hypothesis-fuzz coverage — verified for real, not trusted

`tests/test_halt_gates.py`:
- `SCENARIOS` dict covers H01, H02, H03, H04, H05, H06, H07, H08, H09, H11 (10 codes) — each
  run through the public API (`HaltError` with the right code) **and** through the CLI
  (`test_gate_exits_3_and_writes_nothing`: `rc == EXIT_HALT` (3) and
  `not out.exists() or not any(out.iterdir())`).
- H10 verified separately as flag-only: exit 2 (`EXIT_WARNINGS`), report **is** written, per
  brief's design (H10 is explicitly a warning-flag code, not a HALT).
- H12 verified separately via `proofpack compare`: exit 3, nothing written; `--allow-unpaired`
  degrades to W12 / exit 2.
- `test_every_halt_code_has_a_scenario()` asserts `set(SCENARIOS) | {"H10","H12"} == set(HALT_CODES)`.

**Count: all 12 H-codes (H01–H12) have a real, executing test scenario.** H10's "exit 3" framing
in the brief's shorthand is correctly interpreted as HALT-family coverage (it is the one code
that is HALT-severity-adjacent but flag-only by spec — confirmed against D1 prose referenced in
the handoff, and matches Josh's open-question framing rather than a gap).

`tests/test_schema_fuzz.py`: real Hypothesis tests, not stubs —
`@settings(max_examples=300)` / `@given(cols=tables(), crit=criteria_variants())` for the main
ingest fuzz, plus three more `@given`-decorated tests (declare-only H08/H09, authored-field
blanks, date coarsening, AUROC-vs-scipy oracle). Confirmed by reading the file, not by
re-running counts from the handoff.

## 3. Acceptance check — executed independently, not trusted from the handoff

```
$ uv venv --python 3.12 /tmp/verify-venv
$ uv pip install --python /tmp/verify-venv/bin/python .
Installed 9 packages: attrs, jsonschema, jsonschema-specifications, numpy,
proofpack, pyyaml, referencing, rpds-py, typing-extensions   (no scipy)

$ /tmp/verify-venv/bin/python -c "... find_spec('scipy') is None; import proofpack; 'scipy' not in sys.modules ..."
scipy absent: OK / import proofpack: OK / scipy not imported: OK

$ /tmp/verify-venv/bin/proofpack doctor --offline
All essential checks passed.
exit code: 0
```
Also ran `doctor` without `--offline` from this venv — same result, exit 0 (network check reports
"not attempted — no outbound call exists yet", consistent with the code: no network call exists
anywhere in `src/`).

**Acceptance line is met, independently reproduced, not just trusted.**

## 4. Spot-check — three HALT codes, self-constructed inputs (not the repo's fixtures)

Built my own CSV + `criteria.yaml` by hand in `/tmp/spotcheck/` (independent of `conftest.py`
helpers) to trigger:

| Code | Trigger constructed | Result |
|---|---|---|
| H03 | one score value = 1.7 (probability type, must be in [0,1]) | `exit 3`, `HALT H03: score.type is probability but values lie outside [0, 1]`, no `out_h03/` directory created |
| H06 | all 100 rows y_true = "0" (single class) | `exit 3`, `HALT H06: single-class dataset`, no `out_h06/` directory created |
| H02 | one row y_true = "9" (not in declared classes {0,1}, not an indeterminate) | `exit 3`, `HALT H02: y_true contains value(s) outside declared classes and indeterminate values`, no `out_h02/` directory created |

All three: exit code 3 confirmed, correct HALT code fired (not a different code), no output
directory written in any case.

## 5. Paid dependencies / services / LLM calls — grepped, none found

Grepped the full repo (excluding `.venv`, `uv.lock`) for: `keygen`, `clay`, `instantly`, `attio`,
`loops`, `crisp`, `chartmogul`, `weasyprint` — **zero matches**.

Grepped `src/` and `tests/` for any LLM/API/network call surface: `openai`, `anthropic`,
`claude`, `requests.`, `urllib`, `httpx`, `http.client`, `socket.`, `api_key` — **zero matches**.
`requests`/`urllib3` appear in `uv.lock` only as transitive deps of `pip-audit` (a dev-only CI
tool), not imported by any `proofpack` module. Consistent with handoff's "no outbound call
exists anywhere in the code today." No number in this engine is authored by an LLM — everything
is numpy arithmetic plus declared, human-authored criteria.

## 6. Schema and guidance-map files

All four schema files parse as valid JSON:
- `schema/schema_v1.json` — valid
- `schema/criteria_schema.json` — valid
- `schema/claims_schema.json` — valid
- `schema/egress_schema.json` — valid

`design/guidance_map_v1.csv`: 32 rows, 32 unique `internal_id`s (matches D4 §13 count claimed).
`section` is blank in all 32/32 rows (Josh's transcription job, as required). `estar_section` is
blank in 23/32; the other 9 are explicitly `"n/a"` (FDA 2007 statistics guidance and non-US
items with no eSTAR mapping) rather than left for transcription — a legitimate distinction, not
a gap. A repo test (`test_guidance_map_structure_and_blank_sections`) enforces `section == ""`
so this can't silently regress.

## 7. Git log

```
$ git log --oneline
8e98728 day1 E: validated ingest and declarations
cdffead chore: init proofpack
```
Day-1 commit present as claimed.

## 8. Constraint compliance (Josh's CONSTRAINTS doc)

- **Zero cash to launch**: no paid tools, services, or contractors found anywhere in the stack.
  numpy/pyyaml/jsonschema (runtime) and pytest/hypothesis/ruff/pip-audit/scipy (dev) are all
  free/open-source; CI runs on GitHub Actions' free tier. Consistent.
- **Independent review by code**: AUROC is oracle-tested against `scipy.stats.mannwhitneyu`
  (skips gracefully if scipy absent) — matches constraint #3's "cross-checked in CI against
  pROC/statsmodels/scikit-learn" intent (scipy stands in for the day-1 single-statistic case;
  day 2 brings proportions/CIs per the handoff's "what tomorrow needs").
- **Cadence**: one feature (E1) with tests, in one session — matches constraint #2.
- No violations found.

## Verdict

**PASS.** Tests match the handoff verbatim (and plain `pytest` too). F12 and hypothesis fuzz
are real and complete (all 12 H-codes covered, correctly distinguishing H10's flag-only
severity). Acceptance criteria independently reproduced from a fresh venv, not taken on trust.
Three independently-constructed spot-check inputs all fired the correct HALT code with exit 3
and no document written. No paid dependencies, services, or LLM/API number-authoring surface
anywhere in the repo. All four schema files are valid JSON; the guidance map has the required
blank `section` column (with a regression test) and correct row/id count. Day-1 commit is
present. No constraint violations found.
