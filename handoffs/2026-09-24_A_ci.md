# Lane A: build day 9, CI repair (A-P3 repair 3: GitHub Actions run 36005620750 on main `78ae8dc`), 24 September 2026

Written on 24 September 2026 by the handoff session. Sources: the repair-3 note and its repair-round-1 note, and the four round-3 lens notes named below. The earlier day-9 notes (build, repairs 1 and 2) were read for context; `handoffs/2026-09-24_A.md` is the day-9 handoff for the build itself. Every figure under "Re-run these" was measured again in this session, in both shells. Engine: branch `a-p3-release` in `C:/Users/joshs/GPS/ProofPack/wt-ap3`, `78ae8dc..5b1b1f4` (three commits: `5440295`, `ad953eb`, `5b1b1f4`), plus this note's commit. Nothing is merged or pushed, and no tag exists. Main is still at `78ae8dc` (measured now), so the merge is a fast-forward.

**Verdict.** **PASS**: lens round 2 (both lenses, at `5b1b1f4`) found no blocker, and the round-1 blocker RG3-B1 is closed on the input lens 1 used. The orchestrator counts **27** items carried after the final lens. Among them are four sentence violations that the final lenses found and no repair round followed, and one gap that matters on CI: on GitHub Actions `--check` always takes the tolerance path, so a hand-edited oracle that moves a value by less than its tolerance passes there (lens 2 FA N2 / RG N1).

## What landed

All figures come from `win-amd64-cp314` (Windows 11, Python 3.14.6, numpy 2.5.1, scipy 1.18.1), with `PYTHONPATH` forced to the worktree's `src`. None comes from ubuntu / Python 3.12 or from the reference platform (`python:3.12-slim` linux/amd64). None of the three fixes has run where the three failures happened. The next CI run is the first place any of them runs.

Run 36005620750 at `78ae8dc` failed on three findings, each with its own cause.

| Item | Commit | Test ids | Literal input and measured figure (platform) |
|---|---|---|---|
| **CI-1**: in CI (ubuntu / 3.12), `tests/test_fixtures_cmd.py:232` failed `assert 1 == 0`, and `--check` printed nothing. `scripts/capture_fixture_oracles.py --check` now calls `compare()`. `compare()` prints each value whose float differs, with both figures, the absolute difference, the class and `within` / `OUTSIDE`. It then prints a count line and both capture settings. The comparison is exact (tolerance 0) when the committed file and the fresh capture name the same `captured_on_platform` and `library_versions`. Otherwise it uses the D1 section 9 class of the fixtures row that reads the value: closed form 1e-9, iterative 1e-6. `--committed PATH` names the file to compare against. `fixtures/oracles_v1.json` gains `"captured_on_platform": "win-amd64 cp314"`; the recapture changed no value. A new `ci.yml` step, `oracle capture compared with fixtures/oracles_v1.json`, runs after pytest with `if: ${{ !cancelled() }}`. | `5440295`, `ad953eb`, `5b1b1f4` | `tests/test_ap3_repair3.py::test_ci1_check_prints_each_moved_value_with_both_figures_and_exits_1`, `::test_ci1_a_value_moved_within_its_class_is_printed_and_exits_0`, `::test_ci1_chi2_p_is_compared_as_iterative_and_chi2_as_closed_form`, `::test_the_check_classes_are_the_fixtures_rows_classes`; `tests/test_ap3_r3_repair1.py::test_fan1_same_platform_f1d_cp_hi_1_0000009_exits_1`, `::test_fan1_same_platform_one_ulp_is_outside`, `::test_fan1_a_committed_file_without_its_capture_setting_is_false[captured_on_platform]`/`[library_versions]` | Measured now, `--check` on the committed file: `comparison: exact (same captured_on_platform and library_versions)`, `captured values: 65 identical, 0 differ within their tolerance, 0 differ outside it`, exit 0, in both shells. From repair 3: `wilson_lo` +2e-9 and `paired_p` +1e-12 give exit 1 with `63 identical, 1 differ within their tolerance, 1 differ outside it`. From repair round 1: `F1d-clopper-pearson.cp_hi` = `1.0000009` on the same platform gives exit 1, `exact tolerance 0 OUTSIDE`. On a committed side labelled another platform the same value gives `within` (lens 2 FA N2). |
| **RG3-B1** (lens-1 blocker): the two CI-1 subprocess tests read `fixtures/oracles_v1.json`, so they held only where a fresh capture equals the file bit for bit. They now build both sides from one in-test capture (`fresh_capture()`), with the committed side labelled `planted-platform cp0`. | `ad953eb`, `5b1b1f4` | the two CI-1 subprocess tests above | Lens 2 regression: one ulp on `F1-clopper-pearson.cp_hi`, with the file relabelled `linux-x86_64 cp312`. At `5440295`: `3 failed, 20 passed`. At `5b1b1f4`: `1 failed, 49 passed`; the one failure is the platform-label test, which fails because of the relabel. |
| **CI-2**: in CI, the wheel test failed with `No module named 'numpy'` (exit 5). `tests/test_wheel_fixtures.py` now takes `PYTHONPATH` from `dependency_paths()`: the directory each of the 14 `RUNTIME_MODULES` is imported from. The probe (`PROBE`) imports all 14 plus proofpack. A failed probe is an `AssertionError`, not a skip. | `5440295`, `ad953eb` | `tests/test_ap3_repair3.py::test_ci2_the_wheel_test_takes_its_dependency_path_from_where_they_are_imported`; `tests/test_ap3_r3_repair1.py::test_rgn4_the_probe_imports_every_runtime_module_and_proofpack`, `::test_rgn4_a_failed_probe_is_an_assertion_error_not_a_skip` | CI's layout was emulated on Windows: `PYTHONUSERBASE` pointed at an empty directory holding a copy of `uv.exe`, `PIP_NO_INDEX=1`, `UV_OFFLINE=1`, `PYTHONPATH=<wt>/src;<real user site>`. At `78ae8dc`: `internal error: ModuleNotFoundError: No module named 'numpy'`, `assert 5 == 0`. At `5440295`: `1 passed` (repair 3; lens 1 regression reproduced both). |
| **CI-3**: in CI, `docker image inspect python:3.12-slim` gave `No such image`. The build step in `ci.yml` `docker-smoke` and `release.yml` `image` is now `set -euo pipefail; docker build ... --progress plain 2>&1 \| tee docker-build.log`. The digest step is now `grep -o -m1 -E 'docker\.io/library/python:3\.12-slim@sha256:[0-9a-f]{64}' docker-build.log`. | `5440295` | `tests/test_ap3_repair3.py::test_ci3_the_digest_step_reads_the_build_log[ci.yml-docker-smoke]`/`[release.yml-image]`, `::test_ci3_no_step_inspects_the_base_image_from_the_daemon_store[ci.yml]`/`[release.yml]` | The grep on the saved docker-smoke log of run 36005620750 (803 lines) printed `docker.io/library/python:3.12-slim@sha256:2f17fc044b579bab302c2e8054d3a686e2cb9a83de48e70534b94cd8ebbe06a9`, rc 0. On a one-line log without that reference it gave rc 1 (lens 1 fresh attack). Whether `--progress plain` output carries the same line is not measured. |
| Sentences found false by lens 1 (FA-N2 to N5, RG3-N1, N2, N4): the test is renamed `test_capture_check_exits_0_on_the_committed_oracles`; "docker build has never run" becomes a citation of run 36005620750 in `Dockerfile`, `.dockerignore` and the `ci.yml` header; the `dependabot.yml` header records the nine branches `gh run list` showed; the wheel-test docstring names both of its skips. | `ad953eb` | `tests/test_ap3_r3_repair1.py::test_repair1_refused_phrase_is_absent[...]`, `::test_repair1_required_phrase_is_present[...]` (the phrase is read with each line's `#` dropped, so a phrase wrapped over two comment lines is still found) | `Dependabot reads it once the file is on the default branch`, wrapped over two lines, passed before `_prose()` and fails after (repair round 1). |
| Mutation sweep: 4 new ap3 mutants (repair 3) and 10 (repair round 1), for 39 in all | `5440295`, `ad953eb` | `tests/test_sweep_ap3.py` | See "Re-run these" (measured now). Lens 2 regression: `39 planted, 39 killed, 0 survived; 468 s`. |

Before each fix, the new tests were run against the old code: `tests/test_ap3_repair3.py` at `78ae8dc` gave `11 failed, 0 passed`, and `tests/test_ap3_r3_repair1.py` at `5440295` gave `21 failed, 6 passed`. Both were re-measured by the lenses. Of the six that passed, five are killed by new mutants. The sixth, `[Dockerfile-run 36005620750]`, is killed by none (lens 2 FA N6).

Two notes from the lenses:

- **What "PASS" means for FA-N1.** A hand-edited oracle on the capture machine is now caught (`1.0000009` → exit 1). On CI it is not caught when the edit is smaller than its class: the committed file says `win-amd64 cp314` and numpy 2.5.1, and CI has Python 3.12 and numpy 2.5.3 (`uv.lock`). So every CI `--check` takes the tolerance path, and `cp_hi = 1.0000009`, a proportion bound above 1, gives `within`, exit 0 (lens 2 FA N2, RG N1).
- **What is not yet known.** Which captured value moved on ubuntu / 3.12 in run 36005620750, and whether it lies within its class. The next CI run prints it.

## What was written but has never run

Each of these runs for the first time on the push after the merge (the `ci.yml` jobs), or on Josh's first tag (`release.yml`). Each is described as written, not as working.

- **`ci.yml` job `test` (`pytest + ruff`)**:
  - The new step `oracle capture compared with fixtures/oracles_v1.json`.
  - The steps `pytest every declared day marker (definition of done)` and `doctor in the CI venv` were skipped in run 36005620750, after `pytest (all markers)` failed (`gh run view`, read now). Neither has run on A-P3's code.
- **`ci.yml` job `docker-smoke`**:
  - The rewritten build step (`--progress plain`, `tee docker-build.log`) and the grep digest step.
  - Every step after them: `smoke inside the image with --network none`. That step runs `doctor --offline`, `fixtures --offline`, a `run --offline` expected to exit 4, and F17 at 5,000 rows inside the image. In run 36005620750 the smoke step was skipped.
- **`release.yml`** (runs only on a tag): `build`, `image` (with the rewritten build and digest steps), `testpypi`, `install-from-testpypi`, `github-release`, `zap-baseline`.
- **Ran green in run 36005620750, and did not change in this repair**: `offline-namespace` (`proofpack run inside unshare -rn (no network)`, 13 s), `audit` (pip-audit), `wheel` and `import-without-scipy`. `docker build` itself also ran: the build step completed, including both COPYs and `Successfully installed proofpack-0.1.0.dev1`.

## What was cut and why

- **The base-image digest is not pinned.** Run 36005620750's log shows `2f17fc04…06a9`. It is not written into the `Dockerfile`, because pinning is a decision separate from the repair (Needs from Josh 3). Repair 3's reading, from the 10.37 kB blob size, is that it is the multi-arch index digest; this is not measured.
- **No value was recaptured on Windows to make CI green.** Repair 3 says so in the note and in the code. CI's lines decide the question, and Josh's answer decides where a recapture is made (Open question 1).
- **The four sentence violations from the final lens round were not repaired.** The repair cap for this round (two lens rounds) was reached. Each one is a rename or a one-line comment change (below).
- **Nothing was run on ubuntu, Python 3.12 or the reference platform.** This machine's rule allows no downloads, and the Python 3.12 install on this machine has no numpy.

## What did not land (carried)

The orchestrator counts 27 items. My table groups them by finding id, as the lens notes do, and I did not reconcile my grouping with the count of 27.

**Open sentence violations from the final round (fix in the merge commit or as the next commit):**

| Id | What | Repro / fix |
|---|---|---|
| Lens 2 FA N3 | The test name `test_fan7_ci_runs_the_oracle_check_after_pytest_unless_cancelled` states what CI does. The test reads the YAML (step index, `if`, `run`), and the step has never run. | Rename it to what it reads, e.g. `test_fan7_the_oracle_step_follows_pytest_and_carries_if_not_cancelled`. |
| Lens 2 FA N4 = RG N2 | The `ci.yml` comment (lines 30-32) says the step's printed lines "reach the job log". The step has never run. | `grep -n "reach the job log" .github/workflows/ci.yml`; restore "written to print … has not run yet". |
| Lens 2 FA N5 = RG N2 | The `Dockerfile` comment (lines 5-7) says docker-smoke "greps the base reference from its build log". The grep step has never run; repair round 1 deleted the "(not yet run with that step)" qualifier. | `grep -n "greps the base reference" Dockerfile`; restore the qualifier. |
| Lens 2 FA N6 = RG N4 | In the repair-round-1 note only: "each is killed by a new mutant" does not hold for `[Dockerfile-run 36005620750]`. | Nothing shipped; this note states it correctly above. |

**Open sentence violations from the first A-P3 lens round, still in the tree at `5b1b1f4` (measured now):** FA3-S1 (`tests/test_workflows.py` says `--offline` must be "a word of its own"; `--out=--offline` passes), RG3-S1 (`test_the_zap_upload_step_runs_after_a_failed_scan`), and FA3-S3 (`test_no_secret_looking_env_or_arg_and_no_key_material`). Repros are in `handoffs/2026-09-24_A.md`.

**Record and carry (this round):**

| Id | Reason carried | One-line repro |
|---|---|---|
| Lens 2 FA N2 = RG N1 (with repair round 1 carried item 7, and lens 2 FA N8's empty labels) | On CI, `--check` always takes the tolerance path. A committed-side label edit, or a present-but-empty `captured_on_platform: ""` / `library_versions: {}`, does the same on this machine. Closing it needs either a capture made on CI's platform and committed with that label, or a CI rule. Josh's answer to Open question 1 decides which. | Copy the file; set `cp_hi` of `F1d-clopper-pearson` to `1.0000009` and `library_versions.numpy` to `"2.5.3"`; `python scripts/capture_fixture_oracles.py --check --committed <copy>` → exit 0 |
| Lens 2 FA N1 | The RG3-B1 repair is correct but no test or mutant pins it. `elsewhere(fresh_capture())` → `elsewhere(_committed())` gives `167 passed` under `-m ap3` on this machine, where the mutant is equivalent. | `sed -i 's/elsewhere(fresh_capture())/elsewhere(_committed())/' tests/test_ap3_repair3.py; pytest -m ap3` |
| Lens 2 FA N7 | `continue-on-error: true` on the oracle step survives `-m ap3` (`167 passed`), so the step can be made non-gating without a test failing. | Insert the line after the step's `run:` and run `pytest -m ap3` |
| Lens 2 FA N8 = RG N3 (L1, L3) | Two surviving mutants in `same_capture_setting()`: `library_versions` ignored, and `is None` → `not in`. | lens 2 regression N3 |
| FA-N8 | Deleting `numpy` from `RUNTIME_MODULES` is equivalent on this machine's layout. The test pins a count of 14, not which modules. | delete the `"numpy",` line → `assert 13 == 14` |
| FA-N9 | Each statsmodels, scipy or scikit-learn bump fails `--check` by design: the version is inside the compared `source` string (`scikit-learn 1.9.0 roc_auc_score`). | Dependabot's `scikit-learn-1.9.1` branch |
| RG3-N5 | `dependency_paths()` skips, naming the module, when the interpreter running pytest cannot import one of the 14. CI's `-q` output does not list skips. | read the next CI log with `-rs` |
| RG3-N6 | Environment only: lens worktrees gain one skip (`tests/test_e8_repair4.py:405`) and a larger T12 (the repo path is printed in it). | none |
| Lens 2 FA N9 | The `offline-namespace` header in `ci.yml` still says the job "had not run anywhere as of 23 September 2026". The sentence is dated, not false; the job ran green in run 36005620750. | `grep -n "had not run anywhere" .github/workflows/ci.yml` |
| Lens 2 FA N10, FA-R10, RG-N9 | `pypa/gh-action-pypi-publish@release/v1` is a branch ref in a job holding `id-token: write`. `trivy-action@0.28.0` is a movable tag in a job holding `packages: write`. | read `release.yml` |
| Repair 3 carried 3 | The build log warns `FromPlatformFlagConstDisallowed`. It is a warning only; removing it changes `test_dockerfile.py`'s `FROM` regex. | run 36005620750 build log |
| Repair 3 carried 5 | The Node 20 deprecation annotation on `actions/checkout@v4` and `astral-sh/setup-uv@v6` is not a failure. | `gh run view 36005620750` |
| Dependabot's nine PRs | Not merged, closed or edited; they are the orchestrator's. They fail for the same three reasons as main. **The `python` 3.12-slim → 3.14-slim bump (`dependabot/docker/python-3.14-slim`) would change D1 section 9's reference platform** (`python:3.12-slim` linux/amd64). It is not a routine bump. | `gh run list` |
| Earlier day-9 carries | FA3-R1 to R6, RG3-N1 to N3, RG2-N2, FA-R8, FA-R9, FA-R12 (now superseded by CI-2), RG-N3, RG-N6, RG-N7, RG-N8 are unchanged. They are listed with repros in `handoffs/2026-09-24_A.md`. | as there |

## What the lenses found

| Round | Sha | Regression | Fresh attack |
|---|---|---|---|
| 1 | `5440295` | **FAIL**, one blocker. RG3-B1: the CI-1 tests asserted exact counts against the committed file, so a one-ulp platform difference (the CI-1 case) turned one red test into two (`2 failed, 21 passed`). RG3-N1 to N6: a test name made false by `compare()`; the new ci.yml step unpinned (deleted → `140 passed`); `kind` and one-sided entries unpinned; the wheel probe unpinned; the new skip; environment effects. | **PASS**, no blocker. N1: the tolerance comparison let `cp_hi = 1.0000009` through `--check` (exit 0 at `5440295`, exit 1 at `78ae8dc`), and `fixtures` read the row as matched. N2 to N5: sentence violations (the test name, the wheel docstring's "skipped only when", three "docker build has never run" sentences, `dependabot.yml`). N6 to N8: surviving mutants. N9: Dependabot library bumps fail `--check` by design. |
| 2 | `5b1b1f4` | **PASS**, no blocker. RG3-B1 is closed on lens 1's input. N1: FA-N1's input passes on the tolerance path, which CI always takes. N2: two comments state what never-run steps do. N3: surviving mutants L1 and L3. N4: a note inaccuracy. N5: the note's file list covers round 1 only (the merge carries 14 files), and its re-run prefix is Git Bash syntax. | **PASS**, no blocker. N1: the RG3-B1 repair is unpinned. N2: FA-N1 is closed on the capture machine only. N3 to N6: sentence violations. N7: `continue-on-error` survives. N8: two mutants survive, and empty labels pass. N9: a stale dated header. N10: the publish action is on a branch ref under `id-token: write`. |

DEC-12: repair round 1 changed `--check`, the gate on the oracle file. Lens round 2's fresh attack at `5b1b1f4` is the fresh lens on that change. It found no blocker, and it found N1 and N2 above.

What no lens could break, across both rounds:

- **The fixtures tolerance edge.** Lens 2 FA: `wilson_lo` one ulp → matched; `+1e-9` → `1.0000000272e-09`, not matched, exit 6; tolerance plus one ulp → not matched, exit 6.
- **The exact path on this machine.** `cp_hi = 1.0000009` → exit 1. One ulp → False. Mutants with an exact tolerance of `1e-16` or `1e-12` are killed.
- **Sockets.** Under a `sitecustomize` that makes `socket.socket`, `create_connection` and `getaddrinfo` raise, the lenses counted 0 socket attempts (lens 2 FA) from `fixtures` (with and without `--offline`), `capture_fixture_oracles.py --check`, `sbom.py` and `f17_determinism.py --n 1000`.
- **T12.** From lens 2 FA: `validated`, `compliant`, `certified`, `approved`, `CSA-compliant`, `meets` and `pass` have 0 hits; the page says "supports, does not replace, the manufacturer's own validation"; "not for implementation" occurs 6 times, and each FDA AI-DSF anchor has "draft guidance (January 2025), not for implementation" beside it; the page says "CSA-structured"; and it says "This report was generated on win-amd64-cp314, which is not the reference platform." Injected `<script>`, `<img>` and U+202E rendered escaped, 0 raw.
- **The SBOM.** All 62 non-root `uv.lock` (name, version) pairs are present, none extra, every purl `pkg:pypi/`, `specVersion` 1.5. The CycloneDX schema file was not fetched; this is a structural check.
- **Scope.** `git diff 5440295 5b1b1f4 -- src/ uv.lock pyproject.toml fixtures/ schema/` is empty. No test was deleted, no xfail was added, and no marker was removed.

## Decisions taken today

| # | Decision | Where recorded |
|---|---|---|
| 1 | `--check` compares exactly when `captured_on_platform` and `library_versions` match on both sides, and otherwise by D1 section 9's class of the row that reads the value (`F6-homogeneity.chi2_p` iterative). A committed file without either key gives False. | repair 3 CI-1 fix; repair round 1 FA-N1 |
| 2 | `fixtures/oracles_v1.json` records `"captured_on_platform": "win-amd64 cp314"`. No value was recaptured. | repair 3 |
| 3 | The CI-1 tests build both sides from one in-test capture, and label the committed side `planted-platform cp0`: a label `platform_tag()` never produces. | repair round 1 (`5b1b1f4`) |
| 4 | The wheel test's dependency path is where the running interpreter imports the 14 runtime modules. A failed probe is a failure, not a skip. | repair 3 CI-2; repair round 1 RG3-N4 |
| 5 | The base digest is read from the build log (`tee` + `grep`), not from the daemon store. The pin itself waits for Josh. | repair 3 CI-3; repair 3 carried 2 |
| 6 | Dependabot's PRs are left to the orchestrator; the 3.14-slim bump is recorded as a reference-platform change. | repair 3 carried 4; `dependabot.yml` header |
| 7 | The two committed round-1 lens-2 notes kept their paths. The round-3 notes use the `r3_` names. | lens notes' own headers |

## For the orchestrator

- **The merge into main.** Main is at `78ae8dc` (measured now). `git merge-base --is-ancestor main a-p3-release` → true, so the merge is a fast-forward to this note's commit. The branch changes 14 files since `78ae8dc`, plus this note and the two lens-2 notes committed with it:
  - `.dockerignore`, `.github/dependabot.yml`, `.github/workflows/ci.yml`, `.github/workflows/release.yml` and `Dockerfile`;
  - `fixtures/oracles_v1.json`, `scripts/capture_fixture_oracles.py` and `scripts/mutation_sweep.py`;
  - `tests/test_ap3_r3_repair1.py`, `tests/test_ap3_repair3.py`, `tests/test_fixtures_cmd.py` and `tests/test_wheel_fixtures.py`;
  - the two lens-1 notes.

  None of these is under `templates/`, `render/`, `narrate/` or `cli.py`. If E9 commits to main before the merge, the files both sides may touch are `.github/workflows/ci.yml` and `scripts/mutation_sweep.py`; run `git merge-tree --write-tree main a-p3-release` first.
- **What to read on the next CI run:**
  1. The `oracle capture compared with fixtures/oracles_v1.json` lines. The first line should read `comparison: D1 section 9 tolerance classes`. Every differing value is printed with its class and `within` / `OUTSIDE`. If any value is OUTSIDE, the job stays red, and that is a finding for Josh (Open question 1), not something to recapture on Windows.
  2. Whether `pytest (all markers)` is green. `test_capture_check_exits_0_on_the_committed_oracles` runs the same check inside pytest, so a value OUTSIDE its class fails it too.
  3. The docker-smoke `record the base image digest` output.
  4. Whether docker-smoke reaches and passes `smoke inside the image with --network none`.
  5. Run with `-rs` where possible: RG3-N5's skip is invisible under `-q`.
- **The four open sentence violations** (the table above, plus FA3-S1, RG3-S1 and FA3-S3 from the first round). Each is a rename or a one-line comment change.
- **Dependabot's nine PRs.** Leave them until main is green. Rebase-and-read them then. Do not merge `python-3.14-slim`.
- **Line endings.** `git ls-files --eol` on the committed lens notes: `i/lf w/lf`.

## What S4 needs (day 10, DEC-43)

Unchanged by this repair. The release assets, as written in `release.yml` `github-release` (never run), are attached to the GitHub release of tag `v<version>`:

- `dist/proofpack-<version>-py3-none-any.whl`;
- `release/fixtures_report.json`;
- `release/sbom.cdx.json`;
- `release/sample/run.json`, with the display label `sample-run.json` (the file name stays `run.json`);
- `release/T12.html`.

The same files, plus `requirements.lock`, are uploaded as the workflow artefact `release-assets`. The image is `ghcr.io/<owner>/proofpack:<tag>`. None of these exists until Josh's first tag. The site stays pinned at `a0c9abc` until S4 moves `ENGINE_REF`. The schema is `schema/fixtures_report_schema.json`: `values[].oracle` and `values[].tolerance` may be `null`. The command prints `docs: /docs/fixtures`, and no such page is known to exist on the site (not checked).

## Re-run these (measured now, 24 September 2026, `wt-ap3` at `5b1b1f4`, both shells)

Prefix: Git Bash `export PYTHONPATH=C:/Users/joshs/GPS/ProofPack/wt-ap3/src`; PowerShell 5.1 `$env:PYTHONPATH = 'C:\Users\joshs\GPS\ProofPack\wt-ap3\src'` (the Git Bash `VAR=value cmd` form fails in PowerShell, as lens 2 RG N5 found). `python -c "import proofpack;print(proofpack.__file__)"` printed `C:\Users\joshs\GPS\ProofPack\wt-ap3\src\proofpack\__init__.py` in both. Platform `win-amd64-cp314`. `D` is a scratch directory.

| Command (in `wt-ap3`) | Git Bash | PowerShell 5.1 |
|---|---|---|
| `python -m pytest -q -p no:cacheprovider` | `1582 passed, 1 skipped, 1 xfailed in 127.17s` | `1582 passed, 1 skipped, 1 xfailed in 129.10s` |
| `python -m pytest -q -p no:cacheprovider -m ap3` | `167 passed, 1417 deselected in 25.38s` | `167 passed, 1417 deselected in 24.70s` |
| `tests/test_ap3_repair3.py tests/test_ap3_r3_repair1.py tests/test_fixtures_cmd.py` (+ `tests/test_wheel_fixtures.py` in PowerShell) | `50 passed in 10.24s` | `51 passed in 13.17s` |
| `python -m ruff check .` / `python -m ruff format --check .` | `All checks passed!` / `230 files already formatted` before this note; `231 files already formatted` with it | `All checks passed!` / `230 files already formatted` (before this note) |
| `python -m proofpack.cli doctor --offline` | `All essential checks passed.`, exit 0 | the same, exit 0 |
| `python -m proofpack.cli fixtures --offline --out D --r-captures` | `rows 43: matched 30, not matched 0, no oracle recorded 2, not built 6, compared by the test suite only 5`; `platform win-amd64-cp314 (reference platform linux-x86_64-cp312: no)`; `r-captures: r_captures_not_captured`; exit 0 | the same lines, exit 0 |
| `python scripts/build_t12.py --report D/fixtures_report.json --out T` | `(49371 bytes)`, exit 0 | `(49371 bytes)`, exit 0 |
| `python scripts/f17_determinism.py --out D --n 5000` | `ingest_report_json: equal d306a3aa6600d4a4`, `file_names: equal 7fe1d0490f327ef2`, `run exit codes [4, 4]; identical under the mask ['run_id', 'started', 'duration_s']: yes`, exit 0 | the same last line, exit 0 |
| `python scripts/sbom.py --out X` twice, then `cmp` | `62 components: 13 required, 1 optional, 48 excluded`; `cmp` identical; SHA-256 `e5c13c67928b10d80b8ced3fa2853374a8a83e271cc0f4d44c97560064f538d1` | the same line; `E5C13C67928B10D80B8CED3FA2853374A8A83E271CC0F4D44C97560064F538D1` |
| `python scripts/capture_fixture_oracles.py --check` | `comparison: exact (same captured_on_platform and library_versions)`, `captured values: 65 identical, 0 differ within their tolerance, 0 differ outside it`, exit 0 | the same two lines, exit 0 |
| `python scripts/mutation_sweep.py --marker ap3` | `39 planted, 39 killed, 0 survived; 427 s`; afterwards the tree's only changes were the three untracked notes | not run |
| `git merge-base --is-ancestor main HEAD` | true (main `78ae8dc7828d…`) | not run |

The skip is `tests/test_doctor_cli.py:57` (write access cannot be revoked for this user). The xfail is F13, which is pending the R capture (day 12). F17 is a same-platform repeat on `win-amd64-cp314`; hash identity is claimed only on the reference platform, and it has not been measured there.

## Tomorrow needs (day 10 = A-P4, `spec/briefs/day10_A.md`)

**Day 9 was a short day, and it spilled over.** It started late on Wednesday 23 September. The build and its three lens rounds ran through Thursday morning. The first push (`78ae8dc`) then showed three CI failures that Windows could not show, and this repair round took Thursday afternoon. Thursday 24 September is day 10 on the calendar, so **A-P4 has not started.**

What the short day cost:

- nothing of A-P3 has run green on GitHub Actions;
- nothing was measured on ubuntu, Python 3.12 or the reference platform;
- every `release.yml` job is written and not run;
- seven sentence violations are open (four from this round, three from the first);
- S4's site half waits on a tag;
- A-P4 moves back by a day.

A-P4 is the DOCX renderer. It covers:

- docxtpl templates T1, T7 and T8 (T2 if E10 lands early) in the `[docx]` extra;
- a footer on every page, and the suppression markers;
- matplotlib figures with 1e-9 coordinate parity against the SVG;
- a python-docx round-trip test (every number equals the JSON);
- HTML still rendering with `[docx]` absent;
- D5 section 3.5's named styles, whose colour values come from `design/tokens.json`, tested equal to the HTML theme's;
- fixed zip mtimes if byte-identical output is claimed.

What it inherits:

1. **A green CI on main first.** A-P4 adds files that CI's pytest step reads. If CI-1 turns out to be OUTSIDE its class, that is decided before A-P4 lands on main.
2. **`docxtpl`, `python-docx` and `matplotlib`** are not importable on this machine and are not in `uv.lock` (`handoffs/2026-09-24_A.md`). Re-locking downloads them, and that needs Josh's yes (Needs from Josh 6).
3. **The seven open sentence violations** go into the merge commit or become A-P4's first commit.
4. **FA-N9.** Any Dependabot library bump that A-P4's re-lock pulls in (scikit-learn 1.9.1 is open) fails `--check` until a recapture is made. Josh's answer to Open question 1 says where the recapture is made.

## Needs from Josh

1. **TestPyPI (free).** An account, and a trusted publisher for your GitHub owner, repository `proofpack`, workflow `release.yml`, environment `testpypi`. No token goes into the repository.
2. **The first tag, only after main's CI is green.** The tag must equal `v` + the `pyproject.toml` version (today `v0.1.0.dev1`); the `build` job refuses any other. TestPyPI refuses a second upload of the same version, so each later tag needs a version bump. The tag is when the six `release.yml` jobs first run. Read that run, not the YAML.
3. **GHCR visibility, and the base-image pin.** The first push creates the package, private by default `[unverified]`. Public or private is your call. The pin: `FROM --platform=linux/amd64 python:3.12-slim@sha256:2f17fc044b579bab302c2e8054d3a686e2cb9a83de48e70534b94cd8ebbe06a9`, the reference in run 36005620750's build log. It is a one-line change, and `tests/test_dockerfile.py` accepts it once the `[unverified]` comment goes. Say yes, or wait for the next run's printed value.
4. **Do not merge Dependabot's `python-3.14-slim` PR.** It would move D1 section 9's reference platform.
5. **The day-10 people track** (from the brief):
   - the Stripe test-mode end-to-end purchase;
   - `proofpack licence install` from the emailed licence;
   - reading T12 as the RA lead who must file it. For today, render one with `python scripts/build_t12.py --report <report> --out <dir>` after `proofpack fixtures --offline --out <dir>`. After the tag it is `release/T12.html`.
   - Also the brief's review hour on the T1 DOCX, once A-P4 lands.
6. **Permission to download `docxtpl`, `python-docx` and `matplotlib`** into the lock for A-P4's `[docx]` extra. All three are free, but this machine's rule needs your yes for any download.
7. **The VAT statement relayed with this run** ("We will have to get VAT registered at the revenue amount, current revenue this financial year is 0") is outside lane A. Nothing in the engine touches VAT, prices or invoices, and no action was taken. It belongs with lane S (the site's prices and the Stripe tax settings) and your accountant.

Nothing is paid.

## Open questions (at most five)

1. **If the next CI run shows a captured value OUTSIDE its class on ubuntu / 3.12, which capture holds?** One option is the Windows capture. The other is a capture made on the reference platform (`python:3.12-slim` linux/amd64) and committed with that label. D1 section 9 names the reference platform, and a capture made there would also make CI's `--check` exact (it would close lens 2 FA N2).
2. **Should CI refuse a committed file whose `captured_on_platform` is not the reference platform's?** That would make the tolerance path a local convenience only. It would also need a recapture on the reference platform first.
3. **Pin the base digest now from run 36005620750's log, or wait for the next run's printed value?** The two should agree unless the `python:3.12-slim` tag moved in between.
4. **Should each Dependabot library bump carry its own recapture** (FA-N9)? The alternative is to take `source` strings out of the equality check and compare values only.
5. **RG-N7 (carried).** Should the release `fixtures_report.json` and T12 be generated inside the `python:3.12-slim` image? The `build` job runs on ubuntu with Python 3.12, where `platform_tag()` equals `REFERENCE_PLATFORM`.

## Sentences refused

Aggregated from the repair-3 note, the repair-round-1 note, the four round-3 lens notes and this note:

- "The next CI run will be green" / "the next CI run will be green if the difference is within tolerance". Nothing has run there since `78ae8dc`, and RG3-B1 was a counter-example to the second.
- "The Docker smoke job now passes" / "this fixes the image job" / "the digest step prints the base digest". The smoke step has never started. The grep was run on saved log text, not inside a job.
- "The new step prints the differing values on CI" / "the oracle step fails the job when `--check` exits 1" / "`if: !cancelled()` runs the step after a failed pytest step". The step has never run, and `continue-on-error` survives (lens 2 FA N7).
- "`--check` catches a hand-edited oracle" / "`--check` now rejects a hand-edited oracle" / "`--check` detects any platform difference". Counter-examples: FA-N1 at `5440295`, lens 2 FA N2 (CI's side), lens 2 FA N8 (empty labels).
- "FA-N1 is closed". It is closed on the capture machine only (lens 2 RG N1).
- "The RG3-B1 repair is pinned" (lens 2 FA N1) / "the emulation shows CI-1 is fixed". The emulation moved one value by one ulp on Windows; it is not CI's actual difference.
- "The committed oracles agree with a fresh capture on the reference platform". This was never measured there.
- "The wheel test now passes on CI" / "the wheel test is skipped only when …". It has not run on CI, and FA-N3 is a counter-example to the second.
- "Directories on `PYTHONPATH` do not process `.pth` files" / "which on GitHub Actions holds none of them". No counter-example was run for the first; only numpy's absence was measured for the second.
- "Dependabot reads the uv ecosystem as documented". GitHub's documentation was not read; the note records what `gh run list` showed.
- This note: "A-P3 is done" / "CI is fixed". No fix has run on GitHub Actions, and seven sentence violations are open. "27 items are carried" as my own count: the count is the orchestrator's, and my table groups by finding id. "The digest is the multi-arch index digest": this is repair 3's reading of a blob size, not a measurement.
