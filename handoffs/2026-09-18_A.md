# Lane A — build day 6 (A-P1, the full mapper) — 2026-09-18

**The third lens round returned `FAIL` on `ea526e3`: the fresh-attack lens found four blockers
(two reachable from `proofpack map` on a real table — Enter at a prompt is recorded as a human
acceptance; an ignored column named `score` collides at `apply_mapping` with the column mapped
to `score` — and two shipped sentences), its regression twin one more shipped sentence, and all
five reproduce when I re-run them now on `ea526e3` (section "What did not land"). Thirty-one
open items are carried below — the five blockers, fourteen reachable non-blocking findings and
twelve out-of-scope or decision items — and nothing on the branch is merged, pushed or
committed to `main`.**

The orchestrator's roll-up handed this note "carried 22"; the table here lists every open item
once, including the decision and day-1 items each repair note declined, which is where the nine
extra rows come from. Nothing was cut.

Three commits on branch `a-p1-mapper`, forked from `4eb44f3`, not pushed: `555a5e1` (build,
13:58), `e92989b` (repair 1, 16:29), `ea526e3` (repair 2, 17:17); this note and the nine lens and
repair notes are the fourth commit. Suite at `ea526e3`, measured now in a fresh worktree with
`PYTHONPATH` forced, in both shells: **528 passed / 1 skipped / 1 xfailed**; `-m day6` **164
passed**; the `4eb44f3` baseline was 364 / 1 / 1 (build note), so the difference is the 164
day-6 tests (96 + 46 + 22 items across the three new files). Lint, format, doctor clean in
both shells; the day-6 mutation sweep **38 planted, 38 killed** in both shells. `git diff
--name-status 4eb44f3..ea526e3 -- tests/` is one `M` (`test_halt_gates.py`, one line) and three
`A`, **0 `D`**; the added test lines contain no `skip`, `xfail` or `.only` (grep count 0);
`src/proofpack/stats/`, `schema/output_schema_v1.json` and `pyproject.toml` have 0 diff lines.

---

## What landed

Every item of `spec/briefs/day6_A.md` is built. Tests are in `tests/test_mapping_full.py`
(marker `day6`) unless a file is named; the repair commits are named where they changed an item.

| # | Item | Module | Commit | Test ids | Fixture / literal input |
|---|---|---|---|---|---|
| 1 | Value-level summaries: type, `n_unique`, top <= 20 with counts, min/max, missing %, the <= 2-unique split; `SUPPRESSION_K = 10`, `SAMPLE_ROWS = 10_000` | `io/profile.py`: `profile_column`, `ColumnSummary` | `555a5e1`; `e92989b` put numeric min/max under the floor | `test_suppression_floor_count_nine_is_suppressed_and_count_ten_is_shown`, `test_two_unique_split_is_suppressed_the_same_way`, `test_sample_boundary_row_10001_does_not_reach_the_summary`, `test_free_text_column_with_thirty_distinct_values_shows_none_of_them`, `test_date_column_lists_no_values_and_coarsens_min_max_to_month`, `test_value_summaries_in_mapping_json_are_the_suppressed_ones`; `test_mapping_repair1.py::test_numeric_min_max_below_the_floor_print_as_suppressed` | `["100"] * 41 + ["7"] * 9` -> `min <suppressed> max 100`; `sepsis_2019` `Age` (50 unique) -> both extremes suppressed |
| 2 | Heuristics on top of the D1 §5.2 synonym table: canonical / synonym / affix (`pt_`, `_v2`) / partial token / date header; value candidates (two-valued -> `y_true` medium, unit-interval floats -> `score`, dates -> `event_date`, unique ints -> `row_id`); consistency check; the confidence table in the module docstring | `io/mapping.py`: `normalise_header`, `_name_claim`, `_value_candidate`, `_check_consistency`, `_decide_named` | `555a5e1`; `ea526e3` made `attr_*` / `rater_*` single-holder | `test_two_valued_column_from_each_label_set_is_a_y_true_candidate_at_medium[5]`, `test_float_within_unit_interval_is_a_score_candidate_and_outside_is_ignored`, `test_unique_integer_column_is_row_id_and_a_repeating_one_is_not_case_id`, `test_sex_coding_rules[15]`, `test_age_numeric_banded_and_neither`, `test_fixture_roles_and_confidences[35]`; `test_mapping_repair2.py::test_attr_twins_are_low_with_the_note_and_reach_the_prompt` | all 35 fixtures; `Attr Site` + `attr_site` -> both low |
| 3 | Conflicts and hostile inputs: two claims on one role -> both low with a note; a label holding scores / a score holding 0/1 -> low; `fold_header` (BOM, NFC, trim, case-fold) + the H07 duplicate halt; empty header -> ignore | `_decide_named`, `fold_header`, `check_h07` | `555a5e1` | `test_label_holding_continuous_scores_keeps_the_role_at_low_with_a_note`, `test_two_headers_claiming_y_true_are_both_low_and_yes_is_refused`, `test_duplicate_headers_after_case_folding_halt_h07_via_the_cli_without_a_traceback`, `test_bom_in_the_first_header_still_resolves_the_role`, `test_empty_string_header_is_ignore` | `sepsis_two_labels`, `sepsis_label_holds_scores`, `sepsis_score_holds_01`, `sepsis_dup_after_fold` (H07), `sepsis_nfc_nfd_twins` (H07), `sepsis_empty_header`, `sepsis_200_columns`, `sepsis_one_row`, `sepsis_zero_rows` |
| 4 | DEC-11: `E01` in `errors.MAPPING_CODES`, message ending `reduce your case key to one column`, raised (a) by `declare._check_dec11_case_key` on `clustering.unit` as a list of >= 2 or a string with a separator, and on the list keys `columns`/`column`/`key`/`keys`/`units`/`fields`; (b) by `map_headers` on two name claims for `case_id`; (c) by `apply_mapping` on two columns mapped to `case_id` | `errors.py`, `io/declare.py`, `io/mapping.py` | `555a5e1`; `e92989b` (separators = any run outside `[A-Za-z0-9_]` or ` and `, list keys); `ea526e3` (raiser (c), strings under list keys, `AND` in any case) | `test_two_case_id_headers_halt_e01_through_map_headers_and_ingest`, `test_composite_clustering_unit_halts_e01_before_the_schema_check[5]`, `test_dec11_via_the_cli_run_and_map_exit_3_with_the_message_and_no_traceback`; `repair1::test_separators_the_lens_listed_reach_e01[13]`, `repair1::test_slash_composite_unit_via_the_cli_is_e01_not_h08`; `repair2::test_two_partial_case_id_headers_reach_e01_at_apply_not_h07`, `repair2::test_list_key_strings_and_upper_case_and_reach_e01[6]` | `sepsis_two_case_ids`, `mimic_composite_key` + its `criteria.yaml`; `subject_id/hadm_id` through `map` and `run`: exit 3, first stderr line `HALT E01: clustering.unit names 2 columns; reduce your case key to one column`, no `Traceback` |
| 5 | Interactive confirm and `--yes`: the printed table `original header -> role -> conf -> value summary`; one prompt per non-high role (`a`/`e`/`q`), one whole-mapping prompt when every role is high; `--yes` accepted only with a prior whose `header_set_sha256` matches, whose `decided_by` is `interactive` or `file`, and with every role high in the prior **and** in the fresh mapping, else H07; not a terminal -> H07 before any prompt | `cli.py`: `_stdin_is_terminal`, `_ask`, `_confirm_interactive`, `_tolerant_console`, `cmd_map`; `mapping.check_h07` | `555a5e1`; `e92989b` (`decided_by` rule, all-high prompt, held-role edit refusal, EOF/Ctrl-C at a prompt -> H07, cp1252 console, `--quiet`); `ea526e3` (held-role accept refusal, all-high prompt takes `a`/`q` only, `attr_` name rule, `--out` directory check, Ctrl-C from `load_table` to the last prompt) | `test_yes_without_a_prior_mapping_halts_h07`, `test_yes_is_refused_when_the_fresh_mapping_has_a_medium_even_if_the_file_says_high`, `test_non_tty_with_stdin_closed_exits_3_within_the_timeout` (subprocess, `stdin=DEVNULL`, timeout 120 s), `test_tty_prompt_accept_edit_and_abort`; `repair1::test_yes_refuses_a_proposed_prior`, `repair1::test_ctrl_c_at_the_prompt_is_h07`, `repair1::test_cp1252_stdout_prints_the_table_with_escapes`; `repair2::test_accept_of_a_second_holder_is_refused_at_the_prompt`, `repair2::test_out_into_a_missing_directory_halts_h07_before_any_prompt`, `repair2::test_ctrl_c_during_load_is_h07_not_a_traceback` | the seeded cohort; `row_id,label,prob,patient_nbr,mrn_local` x 60 with answers `a a e ignore` -> one refusal line, one holder |
| 6 | Prior-file reading: `Mapping.read` checks `header_set_sha256` a string, each `roles` entry a mapping with string `original` and `confidence`, `role` a string or null, `value_summaries` a mapping or null; failures -> H07 `mapping.json could not be read` | `io/mapping.py` `Mapping.read` | `e92989b`; `ea526e3` | `repair1::test_malformed_prior_under_yes_halts_h07_not_exit_5[7]`, `repair2::test_prior_with_a_non_string_role_halts_h07_under_run_yes_and_map_yes[5]`, `repair2::test_prior_with_a_string_value_summaries_halts_h07` | `roles: "SECRET_HDR"`, `roles: [1, 2]`, hash `123`, `role: 123`, `role: ["a"]`, `value_summaries: "SECRET_STR"` -> exit 3, the string absent from stderr |
| 7 | Date typing for H11 by values: the six `DATE_PATTERNS` (ISO, ISO with time, `D/M/YYYY`, `D-M-YYYY`, `D.M.YYYY`, `D <English month> YYYY`), coarsened to `YYYY-MM`; `check_h11(..., mapping=)` covers a column by `period.column` equal to its header or to its role when it is the sole holder | `io/profile.py`, `mapping.check_h11`, `gates.ingest` | `555a5e1`; `e92989b` (dash); `ea526e3` (dotted, month names) | `test_h11_fires_on_a_visit_column_holding_dates_and_period_can_cover_it`, `test_h11_two_columns_sharing_the_event_date_role_are_not_covered_by_the_role_name`; `repair1::test_dash_separated_dates_are_typed_date_and_halt_h11`; `repair2::test_dotted_and_month_name_dates_are_typed_date` | `sepsis_visit_dates`, `mimic_visit_dates`; `15.03.2024` / `17 Mar 2024` x 20 -> `date`, H11 `{"date_like_columns": 1}`; `3/17/24` x 60 -> `categorical` |
| 8 | Fixtures: 35 CSVs (<= 50 rows, synthetic, seed 20260918) + 35 `expected.json` + `mimic_composite_key.criteria.yaml`, generated by `scripts/make_mapping_fixtures.py` (expected roles authored in the script, which imports nothing from `proofpack`; writes with the existing file's line ending) | `tests/fixtures/mapping/` | `555a5e1`; `e92989b` (line endings) | `test_register_has_at_least_thirty_fixtures_with_expectations`, `test_fixture_roles_and_confidences[35]`, `test_acceptance_sepsis_and_diabetes_map_high_on_the_five_named_columns` | all 35 |
| 9 | Privacy and record: `test_fixture_halts_and_two_constructed_ones_carry_no_header_or_value` (raises the 3 fixture halts + 2 constructed and greps message and detail for that table's headers and cell values of >= 3 characters); `Mapping.table()`; `mapping.json` = `{header_set_sha256, roles[{original, role, confidence, source, notes}], value_summaries (suppressed), decided_by: proposed|interactive|file, timestamp}`; `scripts/mutation_sweep.py --marker day6` (38 mutants) | `io/mapping.py`, `scripts/mutation_sweep.py`, README | `555a5e1` (17 mutants); `e92989b` (27); `ea526e3` (38) | `test_mapping_json_round_trip_and_hash_is_order_independent`, `test_cli_map_prints_the_table_for_the_sepsis_fixture_in_a_subprocess`, `test_cli_map_prints_non_ascii_headers_with_pythonutf8`; `repair2::test_header_rename_at_the_same_width_changes_the_hash_and_yes_refuses_the_prior` | `sepsis_2019`, `sepsis_unicode_headers`; `score` -> `Score` changes the hash `5fbb6eb2369b` -> `4916f8a0beca` |

**Acceptance** (brief: Sepsis and Diabetes-130 headers map `high` on label / score / site / age /
sex), executed by `test_acceptance_sepsis_and_diabetes_map_high_on_the_five_named_columns` and
re-read by every lens: `diabetes_130` five of five high; `sepsis_2019_gender_mf` (M/F) five of
five high; `sepsis_2019` with PhysioNet's real coding (`Gender: Female (0) or Male (1)`, fetched
18 September) four of five — `Gender` is `sex` at **medium** with the note `no dictionary declared
for 0/1` (build decision D5). **Not met as written on the real coding**; the lens-1 regression
note agrees with the builder that mapping 0/1 high would apply a dictionary the customer has not
declared. Needs-from-Josh 2.

The map demo on the sepsis fixture, both shells, measured now (`PYTHONUTF8=1`, stdin from
`/dev/null` / `NUL`): exit 3, **3485 bytes, equal SHA-256 across the shells**, no file written,
first stderr line `HALT H07: stdin is not a terminal: run interactively or pass --yes with a prior
mapping.json`, then the 24-line table; line 15 is `'Age' -> age high float; 50 unique; 0.0%
missing; min <suppressed> max <suppressed>; values: <suppressed> (50 distinct below k=10)` and
`'ICULOS'` keeps `min 1 max 5` (each value held by 10 rows).

## What was cut and why

Nothing in the brief's items was cut, and the orchestrator recorded no cut. Two things are not
built by decision, both recorded in the build note and re-measured by the lens-1 regression note:

- **PSV (pipe-delimited) is not read by `load_table`** — D1 §1 says CSV; the Sepsis fixtures
  are CSV with the PhysioNet header set. A two-column `a|b` file loads as one column named `a|b`
  (one `ignore` role, no halt).
- **The Diabetes-130 header set halts H11 at `proofpack map` and `run` unless a `period`
  declaration covers `time_in_hospital`**: the header matches H11's header regex (`^time_`) although it holds
  integers 1–14. H11 was left header-conservative (a value-based exemption is a privacy-gate
  change under DEC-12(i)); the fixture test drives `map_headers` directly. Carried item 26.

## What did not land, and why — the 31 carried items

Each reproduces on `ea526e3`; "measured now" means I ran it in the handoff worktree today,
otherwise the figure is the named note's. The lens that owns the construction is named so its
note can be opened.

**A. Blockers open at the final lens (five; a repair round did not follow round 3).**

| # | Item | Repro (one line) | Owner |
|---|---|---|---|
| 1 | **Enter (an empty answer) at the all-high prompt is an accept**, and at the per-role prompt too; README step 4, the `_confirm_interactive` docstring and the test id `test_all_high_prompt_reprompts_on_anything_but_a_or_q` say "anything else re-prompts". A stray Enter writes `mapping.json` with `decided_by: interactive` and `--yes` then accepts that file in CI. | measured now: `_confirm_interactive(m, ask=lambda p: "", say=print)` on the seeded cohort -> `decided_by=interactive`, no refusal line | lens-3 fresh-attack B1; lens-3 regression NB-1 / NB-5 |
| 2 | **An ignored column keeps its original name at `apply_mapping` and collides with the column mapped to that role.** Table `row_id,label,score,prob` (60 rows; `score` 0/1, `prob` floats): both claim `score` at low with `2 headers claim score; choose one`; the human does what the prompt says (`e`, `ignore`, `a`); `proofpack run --mapping` then halts `H07: two columns map to the same canonical role`, `{"role": "score"}`. The customer's only way past is renaming the column in the source export. | measured now: `map` answers `e ignore a` -> `{'score': 'ignore', 'prob': 'score'}`; `run --mapping` -> rc 3, `HALT H07: two columns map to the same canonical role` | lens-3 fresh-attack B2 (the class repair 2 recorded as its open question 3) |
| 3 | Sentence in `profile.py` line 60: two-digit years "stay `categorical`" — sixty distinct `d/m/24` values are typed `string`. The claim that matters (not `date`, no H11) holds for both. | measured now: `profile_column([f"{i % 28 + 1}/{i % 12 + 1}/24" for i in range(60)]).inferred_type == "string"`; `["3/17/24"] * 60` -> `categorical` | lens-3 fresh-attack B3 |
| 4 | Sentence in `Mapping.read`'s docstring: "the bytes decode as UTF-8 JSON" is listed as an inspected failure -> H07 — a prior of 100,000 nested `[` is exit 5 (`json.loads` raises `RecursionError`, not in the caught set). Hand-crafted file only. | measured now: `map --input t.csv --out m7.json --yes` with that file -> rc 5, `internal error: RecursionError: Stack overflow (used 2912 kB) while decoding a JSON array` | lens-3 fresh-attack B4 |
| 5 | Sentence in `Mapping.read`'s docstring: "a non-list `roles` raises AttributeError/TypeError on its own" — `roles: {}` and `roles: ""` iterate as empty, pass the check, and `map --yes` exits 0 and rewrites the file with `roles: []`. Same docstring, same class as lens-2 RG-B1. | measured now: prior with `"roles": {}`, `map --yes` -> rc 0, stderr empty, file rewritten `roles: []`, `decided_by: file` | lens-3 regression B1 |

Fix shapes the lenses named, none applied: (1) make `""` re-prompt at both prompts, or keep Enter
as accept and say so in the prompt text, README, docstring and test id; (2) in `apply_mapping`,
do not let an ignored column claim a canonical name (drop it or key it `ignored:<original>`;
`validate` already parks ignored columns in `unused_columns`), with a test feeding the table
above through `map` and `run` and asserting exit 0; (3) "is not typed `date`" in place of "stays
`categorical`"; (4) add `RecursionError` to the `except` arm or narrow the sentence; (5) one
`isinstance(data["roles"], list)` check plus a test feeding `{}` and `""`. Each needs its
counter-example recorded (the hard rule) and, for (2), a fresh-attack lens (DEC-12(i)).

**B. Reachable, non-blocking, unfixed lens findings.**

| # | Item | Repro | Owner |
|---|---|---|---|
| 6 | `--out` naming an existing directory (for example `./pack`) at a terminal: the prompt is answered, then exit 5 `internal error: PermissionError: [Errno 13]` with the full local path. The repair checks `parent.is_dir()`, not the target. | measured now: rc 5 after one prompt | lens-3 fresh-attack N1; regression NB-2 |
| 7 | `--yes` compares the hash and the all-high flags, not the prior's roles against the fresh mapping's: a prior `age high` from a numeric column, the same header set now holding `[70-80)` bands -> fresh `age_band high`; `map --yes` exit 0 and the file is rewritten with `role: age`. | lens-3 fresh-attack N2 | lens 3 |
| 8 | Date shapes outside the six: `15-Mar-2024`, `Mar 15, 2024`, `15/03/2024 10:30`, `2024/03/15 10:30` x 60 -> `categorical`, the value printed at count 60, no H11; one stray non-date cell among 59 ISO dates turns the column off the `date` type. `DD-MON-YYYY` is the Oracle/SAS export default. | lens-3 fresh-attack N3 | lens 3 |
| 9 | `clustering` extra keys `case_key`, `by`, `composite` pass `validate_dict` (the schema has no `additionalProperties: false` there); `columns: "n°patient"` -> E01 "names 2 columns" (a false count on one accented name); `unit: "case_id "` -> H08 enum with no fix line. | lens-3 fresh-attack N4 | lens 3 |
| 10 | Underscore-stripped synonym lookup: `la bel`, `g_t`, `Y_`, `out_come` -> `y_true high`; `p_1`, `r_i_s_k` -> `score`. The docstring states the rule; the lens calls `la bel` at high a stretch. | lens-3 fresh-attack N5 | lens 3 |
| 11 | Five lens-3 mutants survive `-m day6` (164 passed each): L08 `_infer_type` date on `any` instead of `all`; L11 `map_headers`'s E01 counts canonical + synonym claims only, not affix (README and `mapping.py` say "affix"; `patient_id` + `pt_subject_id` measured E01 by hand, no test feeds it); L13 `fold_header` without `.strip()`; L14 `apply_mapping`'s E01 counts non-high `case_id` columns only; L20 a dead `or original in canon` clause. | plant each; `-m day6` passes | lens-3 fresh-attack N6 |
| 12 | Ctrl-C during `Mapping.write` (after the last prompt) propagates out of `main()` — a traceback; outside the span README step 4 states. | raise `KeyboardInterrupt` from `write` with `input` answering `a` | lens-3 fresh-attack N7 |
| 13 | `--yes` on hand-edited priors with a matching hash and `decided_by: file`: `roles: []` -> exit 0 ("every role is high" vacuously); `original: NOT_A_HEADER` -> exit 0; `role: SECRET_ROLE_NAME` at high -> exit 0 (`run --yes` then halts S01 or proceeds depending on which entry was renamed). | lens-2 fresh-attack N4; lens-3 N9 and regression "cut and carried" | carried by repair 2 pending needs-from-Josh 5 |
| 14 | Dotted and slash date shapes check width, not validity: `31.02.2024` and `99.99.2024` x 20 -> `date` with `max 2024-02` / `max 2024-99`. No row value printed. | lens-3 regression NB-3 | lens-3 regression |
| 15 | List keys inspect lists and strings only: `columns: ["a, b"]`, `columns: 5`, `columns: {"a": 1, "b": 2}` pass `validate_dict`; README step 3 says "lists two or more or is a string that splits", which is what the code inspects. | lens-3 regression NB-4 | lens-3 regression |
| 16 | `hadm_id` beside `subject_id` (MIMIC's real two-column key) -> `subject_id` `case_id high` + `hadm_id` `row_id medium`, not E01; only a `clustering.unit` declaration reaches E01 for it. Follows build decision D6 (E01 on name claims only). | `mimic_iv_demo` fixture | lens-1 fresh-attack N9; needs-from-Josh 4 |
| 17 | Two-digit-year dates are not typed: `3/17/24` x 60 `categorical` (the value listed at count 60); the century and D/M order are a guess. Pinned as-is in `repair2::test_dotted_and_month_name_dates_are_typed_date`. | see blocker 3 | repair-2 §3; open question 3 |
| 18 | An injected `ask` that answers `a` at a refused second holder loops for ever (a human reads the refusal line and types `e` or `q`; the per-role prompt already loops on `x`). Recorded, not changed. | repair-2 counter-example 3 | repair 2 |
| 19 | The per-role and all-high prompts accept `accept`, `  a  `, `A` and `` without a refusal (the same tuple since `555a5e1` / `e92989b`); blocker 1 is the sentence half. | measured now (blocker 1) | lens-3 fresh-attack B1 |

**C. Out of scope by decision, or day-1 territory.**

| # | Item | Why carried | Repro | Owner |
|---|---|---|---|---|
| 20 | `proofpack run` without `--yes` and without a prior writes `./pack/mapping.json` (`decided_by: proposed`, the original headers and the value summaries) into the pack output directory — the directory a customer hands over — and proceeds on `low` roles (`patient -> case_id low` on `sepsis_2019_gender_mf.csv`, exit 0). | `cmd_run` is outside today's cli scope (the `map` subparser and `cmd_map` only, so no line of `cmd_run` is in the merge). The `--yes` half: `map --yes` on the `proposed` file `run` wrote halts H07 "was not confirmed" (`repair1::test_yes_refuses_a_proposed_prior`). Needs-from-Josh 1. | `run --input sepsis_2019_gender_mf.csv --criteria <clustering.unit: case_id>` -> exit 0, `./pack/mapping.json` present | lens-1 N1; lens-2 N3; lens-3 N8 |
| 21 | Ctrl-C during `run` / `compare` load is a traceback (`KeyboardInterrupt` is a `BaseException`; `main()`'s catch-all is `Exception`); `_tolerant_console()` runs in `cmd_map` only, so `run`/`compare` print with the interpreter's error handler. | needs a `main()` edit, outside today's scope (repair-1 R1-D8). Open question 4. | raise from `load_table` under `run` | lens-2 N10; repair 2 §3 |
| 22 | Day-1 JSON loader: `{"columns": {"label": 5}}` -> exit 5 `TypeError: object of type 'int' has no len()`; a lone surrogate in a header -> exit 5 `UnicodeEncodeError` (one character of the header in the message); a closed stdout pipe -> exit 5 `BrokenPipeError`. | day-1 `io/schema.py`; contrived inputs; not touched today | lens-2 N9 | lens 2, lens 3 N8 |
| 23 | Date `min`/`max` (`YYYY-MM`) are not put through the k = 10 floor: `["2024-03-15"] * 49 + ["1999-01-01"]` renders `min 1999-01 max 2024-03` — one row's month. | D1 §1's coarsening as built; a month held by one row is still a month. Needs-from-Josh 3. | `profile_column` on that column | lens-2 regression NB-4 |
| 24 | `clustering.unit: subject_id` (one token outside the enum `none | case_id`) halts H08 `declaration invalid at clustering/unit: enum`, which names no fix. | the generic schema-error path (`_h08_from_schema_error`) is day-1 code shared by every declaration; pinned by `repair1::test_one_column_units_outside_the_enum_stay_h08`. Open question 2. | `unit: subject_id` | lens-1 regression NB-2 |
| 25 | `--yes` halts H07 on a table with a non-high role even after a human has confirmed it (lens-1 regression measured it on `sepsis_2019_gender_mf`, `patient` at low, with no prior and with a forged all-high prior): accept and edit leave `confidence` as computed (pinned by `repair1::test_accept_and_edit_keep_the_computed_confidence`), and `check_h07` requires every role high in the prior and in the fresh mapping. The two PhysioNet-shaped fixtures (`patient` low; `Gender` 0/1 medium) do not pass `--yes`. | D1 §5 step 3 verbatim. Needs-from-Josh 2. | `map --yes` on `sepsis_2019_gender_mf.csv` -> H07 whatever the prior | lens-1 N3 / regression NB-4 |
| 26 | H11 halts the real Diabetes-130 header set on `time_in_hospital` (header regex `^time_`, integers 1–14) with no way past except a `period` declaration on a non-date column. | a value-based exemption weakens a privacy gate (an integer column under a date-like header can be an epoch or serial date); DEC-12(i) lens needed. Build open question 1. | `map --input tests/fixtures/mapping/diabetes_130.csv` -> `HALT H11 {"date_like_columns": 1}` | build note; lens-1 regression |
| 27 | PSV not read (above). | D1 §1 says CSV | `a|b` file -> one column | build note |
| 28 | Four day-6 tests pass at `4eb44f3`: `test_register_has_at_least_thirty_fixtures_with_expectations`, `test_single_column_clustering_units_are_not_e01`, `test_yes_without_a_prior_mapping_halts_h07`, `test_yes_with_matching_prior_holding_a_medium_role_halts_h07`. They guard day-1 behaviour under the `day6` marker and pin nothing new. | record | copy the file into a `4eb44f3` worktree, shim the four missing imports -> `88 failed, 4 passed` (lens-1 regression) | lens-1 regression NB-1 |
| 29 | mintty (Git Bash's own terminal) behaviour of `_stdin_is_terminal` is unmeasured (lens-1 regression's reading: Python's stdin is a pipe there and `isatty()` is False, so a human at a mintty prompt would get the non-terminal H07 — a reading, not a measurement). A fresh Windows console window measured `isatty=True terminal=True` (lens-1 regression; `Start-Process python`). | no mintty in any session; the docstring says so | — | lens-1 regression NB-6 |
| 30 | `patient-id` (a hyphenated single name) counts as two tokens -> E01 "names 2 columns". | chosen over leaving `-` out of the separator class (the lens listed `patient_id-study_id`); stated in README step 3 and the test's parameter list; the customer's fix (`unit: case_id`, map the column) is the one H08 would need | `unit: patient-id` | repair-1 §3; open question 5 there |
| 31 | `mapping.json`'s shape deviates from D1 §5 step 5's key list: no separate `confidences` key (confidence sits inside each `roles` entry, with `source` and `notes`) and `decided_by` has a third value, `proposed`. | settle before E7 hashes the file into the manifest — any later reshaping changes every manifest hash. Open question 5. | README documents the shape as built | lens-1 regression NB-5; build open question 4 |

Not carried: lens-2 N11 and lens-2 regression NB-3 (the repair-1 note's figures one below the
tree: 505 / 141 / "27 functions" against the measured 506 / 142 / 23) — corrected in the repair-2
note; lens-1 N11 (a universal in a test id) — renamed in `e92989b`; the `[unverified]` console
handle in build decision D10 — closed by measurement in lens-1 regression and recorded in the
`_stdin_is_terminal` docstring in `e92989b`.

## What the lenses found

Every lens ran in a detached worktree with `PYTHONPATH=<worktree>/src` forced and
`proofpack.__file__` printed from the worktree first; lens 2's regression and lens 3 also
measured the trap (`C:\Users\joshs\GPS\ProofPack\proofpack\src\proofpack\__init__.py` — lane E's
tree — without it). Blocker titles are quoted as the lenses wrote them.

### Round 1, at `555a5e1` — fresh-attack `FAIL` (2 blockers, 12 non-blocking); regression `PASS` (0 blockers, 7 non-blocking)

> **B1.** "`min`/`max` print single-row values that the k=10 floor has just suppressed" —
> `profile_column(["100"]*41 + ["7"]*9).render()` printed `min 7 max 100` beside
> `<suppressed> (1 distinct below k=10)`; 50 random 7-digit integers under `mrn` printed
> `min 1035333 max 9852152`; the committed `diabetes_130.csv` `encounter_id` printed
> `min 500001 max 500050`. Reaches the printed table, `--json-log` stdout, `mapping.json` and
> `./pack/mapping.json`. Falsified `profile.py`'s first sentence ("Suppression is applied before
> anything is displayed or written"), `ColumnSummary`'s "Aggregates only." and README step 1;
> `test_two_unique_split_is_suppressed_the_same_way` asserted the leak (`s.min == "0" and
> s.max == "1"`).
>
> **B2.** "A composite case key written with `/`, `:`, `-`, a space or a tab halts H08, not
> E01" — `clustering.unit: "patient_id / study_id"` -> `H08: declaration invalid at
> clustering/unit: enum`, which names no fix; `subject_id/hadm_id` is the ordinary MIMIC
> spelling. E01 fired only for `,` `+` `&` `;` `|` and ` and `. Falsified the `errors.py`
> comment and README step 3 ("names two or more columns"); no traceback in any of the six
> shapes, so "never a traceback" held.

The regression lens re-measured every figure of the build note in both shells to the same value
(460 / 1 / 1; 96 day6; 17 / 17 / 0; the 24-line table character-equal), found the four tests that
pass pre-build (carried 28), and independently found B2 (its NB-2) and B1 (its NB-3).

Fixed in `e92989b` (twelve findings, 46 test items in `tests/test_mapping_repair1.py`): numeric
min/max withheld per extreme when fewer than 10 sampled rows hold that number (summed over
strings that parse to it, `"1"` + `"1.0"`); the separator class `\s+and\s+|[^A-Za-z0-9_]+` and
the six list keys; `--yes` requires `decided_by` in `{interactive, file}` (`proposed`, what `run`
writes, is refused); one whole-mapping prompt on an all-high table; an edit to a held role refused
at the prompt; EOF and Ctrl-C at a prompt -> H07; seven malformed-prior shapes -> H07; the dash
date shape; `_tolerant_console()`; `--quiet` at a terminal prints the table; the eight surviving
lens mutants pinned (L09, L13, L14, L15, L18, L26, L20, L22 — 8 planted, 8 killed after); the
universal test id renamed; the fixture generator matches the existing line ending. Regression
proof, re-run by me now: the new file in a worktree at `555a5e1` -> **30 failed, 15 passed, 1
deselected** on the assertions the note quotes (`assert ('7', '100') == ('<suppressed>',
'100')`, `assert 'H08' == 'E01'` x 8, `assert 5 == 3` x 3, ...); the deselected Ctrl-C test run
alone interrupts pytest itself (`!!! KeyboardInterrupt !!!`, no tests ran).

What round 1 could not break: declarations inferred (no recoding dictionary, no write of positive
class, orientation, threshold, reference type or clustering in the diff; `sex` 1/2 and 0/1 stay
the strings with the note); `case_id` from values (`_value_candidate` has no such outcome);
floor boundaries in the values list (count 9 hidden, 10 and 11 shown, in `top` and in `split`;
a 9,947-unique column reported `n_suppressed_values` 9947); the sample (`column[:10_000]`;
`SECRET_ROW_10001` in row 10,001 appeared nowhere); closed stdin (30 hostile files, each halted
in 0.3 s; the 4.8 MB single-column file in 0.8 s); tracebacks (none in the 30 files: 0 bytes,
binary, NFD/NFC twins, `LABEL`/`label`, BOM mid-header, ragged, JSON junk, 200 free-text
notes, Excel serials); headers or values in halts (counts, 12-character hash prefixes and
canonical names only); lane E's files (0 diff lines); tests not weakened; fixtures not rigged
(the generator imports nothing from `proofpack`); `GetConsoleMode` on a real console handle (1,
mode 503).

### Round 2, at `e92989b` — fresh-attack `FAIL` (1 blocker, 12 non-blocking; 24 mutants, 22 killed, 2 survived); regression `FAIL` (1 blocker, 5 non-blocking)

> **B1.** "Two headers the mapper itself resolves to `case_id` (by partial token) halt H07,
> not the DEC-11 E01" — `row_id,label,prob,patient_nbr,mrn_local` x 60: both `case_id low`
> with "confirm or ignore"; `proofpack run` -> `HALT H07: two columns map to the same
> canonical role`, `{"role": "case_id"}`, not the DEC-11 sentence; `map` at a terminal accepted
> both (`a`, `a`) and wrote two holders. Falsified `mapping.py` line 36 ("Two headers resolving
> to `case_id` halt with E01 (DEC-11), not `low`"). Mutant M05 (count partial-token claims too)
> survived — no test pinned the exclusion either way.
>
> **RG-B1.** "`Mapping.read`'s docstring claims 'any shape or I/O failure is H07'; a `role`
> that is not a string is accepted by `map --yes` and is exit 5 under `run --yes`" — `role: 123`
> -> `run --yes` exit 5 `internal error: AttributeError: 'int' object has no attribute
> 'startswith'`; `role: ["a"]` -> exit 5 `TypeError`; `map --yes` on both -> exit 0, the file
> rewritten. The docstring's next sentence listed what is checked and `role` was not in it.

The regression lens also found every figure in the repair-1 note one below the committed tree
(506 not 505; 142 not 141; 23 test functions not 27 — "derived, not measured"), corrected in
the repair-2 note; re-ran the seven repair-1 prior shapes (all H07) and the eight lens-1 mutants
(8 killed); and measured the cp1252 console directly in PowerShell.

Fixed in `ea526e3` (eleven findings, 22 test items in `tests/test_mapping_repair2.py`): E01
raised in `apply_mapping` when two columns are mapped to `case_id` whatever their source
(`map_headers` still counts name claims only, because `patient_weight` + `patient_height` carry
the token and no key — that table is the M05 pin); the accept prompt refuses a role held by a
settled entry; `Mapping.read` checks `role` (string or null) and `value_summaries` (mapping or
null) and its docstring lists what is inspected and what is not; the all-high prompt takes
`a`/`accept`/empty and `q`/`quit`/`abort` only; `attr_`/`rater_` edits must match `_IDENT`
with a non-empty name; `--out`'s directory checked before the table and the prompts; Ctrl-C from
`load_table` to the last prompt -> H07; `attr_*`/`rater_*` single-holder; strings under the list
keys split like `unit`, ` and ` in any case; dotted and month-name dates; the hash pinned
against a same-width rename (M24). Regression proof, re-run by me now: the new file in a
worktree at `e92989b` -> **19 failed, 2 passed, 1 deselected** on the quoted assertions
(`AssertionError: patient_nbr`, `assert [] == ['  case_id i...another role']`, `assert 5 == 3`
x 5, `Failed: DID NOT RAISE HaltError` x 6, ...); the two that pass pre-fix are the two "Pins"
(M05, M24), each killed by its sweep mutant.

What round 2 could not break: 57 hostile files through `proofpack map` with `stdin=DEVNULL` —
every stderr line carried counts, hash prefixes or canonical names only, no `Traceback`, every
run under 0.8 s (a 700,000-row single column in 0.76 s with both extremes suppressed); the
floor at both extremes and in the list (`["100"]*41+["7"]*9` -> `min <suppressed> max 100`,
`"007"x9 + "7"x1` -> `min 7` — the documented row-count rule; a site held by 9 rows absent from
the interactive `mapping.json`, `--json-log` stdout, `./pack/mapping.json` and
`ingest_report.json`); `--yes` (`INTERACTIVE`, `Interactive `, `5`, `["interactive"]` -> H07;
`file` and absent accepted; M06, M07, M19, M22 killed); fourteen `clustering.unit` shapes
-> E01 (lens-1 B2 closed); `_value_candidate` has no `case_id` outcome (M11 killed by
`[diabetes_130]`); the fixture expectations (`sepsis_sex_12` medium with the note,
`sepsis_two_case_ids` and `mimic_composite_key` E01); lane E's files (0 diff lines); shell
parity on the sepsis table (3485 bytes each, byte-identical).

### Round 3, at `ea526e3` — fresh-attack `FAIL` (4 blockers, 10 non-blocking entries of which N10 is the figures; 20 mutants, 15 killed, 5 survived); regression `FAIL` (1 blocker, 6 non-blocking)

The five blockers are carried items 1–5 above, quoted there; the regression lens's own summary:
"every figure in the repair-2 note re-measures exactly in both shells (528 passed / 1 skipped
/ 1 xfailed; 164 day6; ruff clean on 60 files; doctor exit 0; 38 mutants planted and 38 killed;
the sepsis table 3485 bytes and byte-identical across shells), the 19 pre-fix failures fail on
the assertions the note quotes, lane E's files are untouched, nothing in `tests/` is weakened,
every lens-2 blocker and non-blocking item the note says it closed is closed on re-run".

Lens-2 closures executed by round 3: FA-B1 — `run` on the partial pair with `clustering.unit:
case_id` -> exit 3 `HALT E01: 2 columns are mapped to case_id (proofpack map: keep one, set the
others to ignore); reduce your case key to one column`, nothing written, neither header in
stderr; a prior holding both -> the same E01; the prompt `a a e ignore` -> one refusal and one
holder, three `y_true` claims `a a a` -> two refusals. RG-B1 — `role: 123`, `["a"]`, `{"x": 1}`,
`1.5`, `true` -> H07 under `run --yes` and `map --yes`, bytes unchanged. RG-N1 — `n no e x` ->
four `answer a or q` (and then reopened for `""` and `accept`, blocker 1). The seven name-claim
`case_id` pairs (`case_id`+`patient_id`, `case_id`+`mrn_hash`, `study_id`+`subject_id`,
`Patient ID`+`subject id`, `pt_case_id`+`case_id`, `case_id_v2`+`case_id`,
`CASE_ID`+`Case-Id`) each E01 in `map_headers`; 26 prior shapes through `map --yes` and `run
--yes` with no exit 5 and no `SECRET` string, the two that pass unexpectedly being blocker 5; 69 hostile files with
`SECRETV_*` cells and `SECRETH_*` headers — every stderr line counts, hash prefixes or canonical
names; the floor at 9 and 10 including rows 9,992–10,001 (10 rows, 9 sampled -> `<suppressed>`)
versus rows 9,991–10,000 (`AGG_TEN (10)`); closed stdin `0<&-` -> H07 in 0.25 s; Ctrl-C at
four points inside `cmd_map`'s span -> H07; the E01-at-apply message carrying a count and no
header; `sex` 1/2, 0/1, M/F, 1/2/9, M/F/U -> medium with the note and levels unchanged; lane E's
files 0 diff lines; the committed sweep 38 / 38 in both shells.

Could not check, all three rounds: mintty; a human at a real console end to end (every prompt
path was driven by an injected `input` and a patched `_stdin_is_terminal`, or by subprocesses
with `stdin=DEVNULL`); the E7 manifest hash of `mapping.json` (E7 not built); real PhysioNet /
UCI / MIMIC exports (offline; the fixtures carry the header sets and synthetic values); the
three web-page provenance claims marked `[unverified]` in `scripts/make_mapping_fixtures.py`'s
docstring (PhysioNet's prediction-column names, UCI's later variables, MIMIC's page fields) —
not re-fetched by any lens.

## The decisions taken

Build decisions (build note D1–D12), then the repair decisions that changed them.

| id | Decision | Where it bites |
|---|---|---|
| D1 | **Suppression floor k = 10** (`profile.SUPPRESSION_K`). D1 §5 states no floor; the egress cell rule `n < 10` of D1 §6 is reused. A value with count 9 prints `<suppressed>`, count 10 prints; the suppressed count covers every distinct value below the floor, not only the inspected top 20. Free text: categorical/string, `n_unique > 2` and `n_unique > 0.5 x` non-missing sample -> no values listed; the `<= 2`-unique split stays visible. | `test_suppression_floor_count_nine_is_suppressed_and_count_ten_is_shown` |
| R1-D1 | **Numeric min/max follow the floor by withholding**, per extreme, when fewer than 10 sampled rows hold that number (rows summed over strings that parse to it), not by banding — D1 gives no band rule and a band of one row is still one row. `signals["min"/"max"]` keep the true extremes in memory and are not serialised. Date min/max stay `YYYY-MM` unfiltered (carried 23). | `repair1::test_numeric_min_max_below_the_floor_print_as_suppressed` |
| D2 | **Sample rule: the first 10,000 rows after the header**, a prefix, no seed (`profile.SAMPLE_ROWS`). | `test_sample_boundary_row_10001_does_not_reach_the_summary` |
| D3 | **Sepsis site column = `hospital` with values A/B** (the two public hospital systems, R3); the per-file patient id arrives as a column `patient`; prediction columns `PredictedProbability` / `PredictedLabel` `[unverified: the fetched page says only "the risk of sepsis (a real number) and a binary sepsis prediction (0 or 1)"]`. Diabetes-130 has no site or model output in the public file: `hospital` (H01–H05) and `pred_prob` are appended. Dataset synonym rows: `sepsis_label`, `readmitted`, `predicted_probability`, `predicted_label`, `hospital_system`. | `scripts/make_mapping_fixtures.py` docstring; `test_fixture_roles_and_confidences` |
| D4 | **Duplicate-header code = H07**, message `two headers are identical after case-folding, whitespace trimming and unicode normalisation; rename one of them`, detail `n_duplicate_headers`. Identity is `fold_header` (BOM stripped, NFC, trimmed, case-folded), not the camel-split key: `SepsisLabel`/`sepsislabel` and NFC/NFD twins halt; `Sepsis Label`/`sepsis_label` are two claims on one role (both low). `load_table`'s exact-duplicate halt stays S04. | `sepsis_dup_after_fold`, `sepsis_nfc_nfd_twins` |
| D5 | **Sex coded 0/1 is medium**, like 1/2, with the note `no dictionary declared for 0/1`; the brief names only 1/2 and treating 0/1 differently could not be justified. The one place the acceptance is not met on the real-coded Sepsis fixture. Needs-from-Josh 2. | `sepsis_2019`, `diabetes_sex_01` |
| D6 | **DEC-11 code = `E01`** in a new `errors.MAPPING_CODES` table (the letter DEC-11 names; a separate table so the H-code table stays exact). Raised by `declare` (list of >= 2 or a separated string), `map_headers` (two name claims — canonical / synonym / affix; a partial token such as `patient_nbr` is not a resolved key) and, since `ea526e3`, `apply_mapping` (two columns mapped to `case_id` whatever the source). | `test_e01_is_registered_and_constructs`; needs-from-Josh 6 |
| R1-D2 / R2-D9 | **Separators** = ` and ` (any case since `ea526e3`) plus any run outside `[A-Za-z0-9_]`; `patient-id` therefore counts two (carried 30). Strings under the six list keys split the same way. | `repair1::test_separators_the_lens_listed_reach_e01[13]` |
| D7 | **Confidence table**: canonical/synonym/affix + consistent values -> high; synonym/affix with no values -> medium (day-1 behaviour kept); conflict -> low + note; partial token -> medium/low; heuristic alone -> medium; two claims on one role -> low for both; unmatched -> ignore at high. `--yes` checks every role high in the prior file and in the fresh mapping. | `test_header_only_call_keeps_the_day1_confidences` |
| R1-D4 | **`--yes` requires `decided_by` in `{interactive, file}`**; absent reads as `file` (day-1 files); the check reads the string only, so a hand-edited `interactive` passes (stated in `check_h07`'s docstring). | `repair1::test_yes_refuses_a_proposed_prior` |
| D8 | `ignore` is stored as the string `ignore` in `mapping.json` and as `None` in memory; `decided_by` is `proposed` for an unconfirmed mapping (`cmd_run` still writes one — carried 20). | `Mapping.read` |
| D9 | **H11 by values**: a column whose summary type is `date` joins the header-regex set; covered by `period.column` equal to its header, or to its role when it is the sole holder. | `test_h11_two_columns_sharing_the_event_date_role_are_not_covered_by_the_role_name` |
| D10 | **Windows stdin**: `_stdin_is_terminal` adds `GetConsoleMode` (0 for NUL, measured; 1 with mode 503 on a real console handle, lens-1 regression); `_confirm_interactive` maps EOFError to H07. mintty unmeasured. | `test_non_tty_with_stdin_closed_exits_3_within_the_timeout` |
| D11 / R1-D11 / R2-D11 | **Mutation sweep**: `Mutant.marker` (default `day5`); `--marker` selects its own list; `--only` picks by id across lists; the 38 day-6 mutants are the 17 build gates, the 10 repair-1 gates and the 11 repair-2 gates (the eight lens-1 survivors are pinned by tests, not by the committed sweep). See the merge notes — lane E made the same extension with a different field. | `scripts/mutation_sweep.py --list` |
| D12 | Empty-string header -> `ignore` at high with the note `empty header` (a pandas index export); its unique integers are not proposed as `row_id`. | `test_empty_string_header_is_ignore` |
| R2-D1 / R2-D2 | **E01 at apply for `case_id` only** (two columns on any other role stay H07); the prompt's held-role check (accept and edit) counts entries already settled — high ones and those answered earlier — so the first of two low holders can be accepted and the second must be edited. | `repair2::test_two_partial_case_id_headers_reach_e01_at_apply_not_h07`, `repair2::test_accept_of_a_second_holder_is_refused_at_the_prompt` |
| R2-D4 | `Mapping.read` checks types (`role` string or null, `value_summaries` mapping or null) and nothing about role names; the docstring lists both sides — and is where blockers 4 and 5 sit. | `repair2::test_prior_with_a_non_string_role_halts_h07_under_run_yes_and_map_yes[5]` |
| R2-D5 / R2-D7 / R2-D8 | The all-high prompt takes `a`/`accept`/empty and `q`/`quit`/`abort` (the empty answer is blocker 1); `--out`'s directory is checked before the table and the prompts, the H07 message carries no path; Ctrl-C inside `cmd_map` from `load_table` to the last prompt is H07, `main()` untouched. | `repair2::test_all_high_prompt_reprompts_on_anything_but_a_or_q`, `repair2::test_out_into_a_missing_directory_halts_h07_before_any_prompt`, `repair2::test_ctrl_c_during_load_is_h07_not_a_traceback` |
| R2-D10 | Date shapes: `D.M.YYYY` and `D <English month name or 3-letter abbreviation> YYYY` added (the twelve names spelled out, so `17 Foo 2024` stays categorical); two-digit years not (carried 17). | `repair2::test_dotted_and_month_name_dates_are_typed_date` |

## Re-run these

Every command below was run now, at `ea526e3`, in a fresh worktree of the branch
(`scratchpad/a-p1-handoff/wt`), in **both** Git Bash and PowerShell 5.1, with
`PYTHONPATH=<worktree>/src` exported first; `python -c "import proofpack;print(proofpack.__file__)"`
printed `...\scratchpad\a-p1-handoff\wt\src\proofpack\__init__.py` in each shell before any
figure below was taken, and without `PYTHONPATH` it printed
`C:\Users\joshs\GPS\ProofPack\proofpack\src\proofpack\__init__.py` — lane E's main tree (the
editable `.pth` trap, measured). The outputs are pasted as printed; the shells differ only in
seconds.

| Command | Output, Git Bash | Output, PowerShell 5.1 |
|---|---|---|
| `python -m pytest -q -p no:cacheprovider` | `528 passed, 1 skipped, 1 xfailed in 46.44s` | `528 passed, 1 skipped, 1 xfailed in 45.74s` |
| `python -m pytest -q -p no:cacheprovider -m day6` | `164 passed, 366 deselected in 3.16s` | `164 passed, 366 deselected in 2.75s` |
| `python -m ruff check .` | `All checks passed!` | `All checks passed!` |
| `python -m ruff format --check .` | `60 files already formatted` | `60 files already formatted` |
| `python -m proofpack.cli doctor --offline` | `All essential checks passed.`, exit 0 | `All essential checks passed.`, exit 0 |
| `python scripts/mutation_sweep.py --marker day6` | `38 planted, 38 killed, 0 survived; 99 s` | `38 planted, 38 killed, 0 survived; 100 s` |
| `PYTHONUTF8=1 python -m proofpack.cli map --input tests/fixtures/mapping/sepsis_2019.csv --out ../m.json < /dev/null` (PowerShell: `cmd /c "... < NUL > file 2>&1"`) | exit 3; 3485 bytes, 27 lines; no `../m.json` | exit 3; 3485 bytes; `Get-FileHash` SHA-256 equal to the Git Bash capture; no `..\m.json` |
| pre-fix proof, repair 1: `tests/test_mapping_repair1.py` copied into a detached worktree at `555a5e1`, `python -m pytest -q -p no:cacheprovider tests/test_mapping_repair1.py --deselect tests/test_mapping_repair1.py::test_ctrl_c_at_the_prompt_is_h07` | `30 failed, 15 passed, 1 deselected in 1.15s` | not repeated |
| pre-fix proof, repair 2: `tests/test_mapping_repair2.py` copied into a detached worktree at `e92989b`, `... --deselect tests/test_mapping_repair2.py::test_ctrl_c_during_load_is_h07_not_a_traceback` | `19 failed, 2 passed, 1 deselected in 0.77s` | not repeated |
| the five lens-3 blocker repros (section A above; a probe script, not a test) | each reproduces: rc 0 / `interactive` (1), `HALT H07 ... {"role": "score"}` (2), `string` (3), rc 5 `RecursionError` (4), rc 0 with `roles: []` (5) | not repeated |

The skip is `tests/test_doctor_cli.py:57` (write access cannot be revoked for this user); the
xfail is `test_f13_matches_proc_on_the_asah_dataset` (pending the R capture) — the day-5 pair.
Collected: `test_mapping_full.py` 96 items / 50 functions; `test_mapping_repair1.py` 46 / 23;
`test_mapping_repair2.py` 22 / 13. Line endings: all 13 changed non-fixture files and all 71
fixture files are `i/lf w/crlf` in the fresh checkout. Do not paste the bare `proofpack doctor
--offline`: the console script is not on `PATH` in either shell; `python -m proofpack.cli doctor
--offline` is the form that runs.

## Merge notes for the orchestrator

**The branch touches** 84 files (+9442 / −67): `README.md`, `scripts/make_mapping_fixtures.py`
(new), `scripts/mutation_sweep.py`, `src/proofpack/cli.py` (the `map` subparser's help strings,
`import os`, and the block from `_stdin_is_terminal` through `cmd_map`; `main()`, `cmd_run`,
`cmd_compare` unchanged), `src/proofpack/errors.py` (`MAPPING_CODES = {"E01"}` and a comment),
`src/proofpack/gates.py` (7 lines: `ingest` maps with values and passes the mapping to H11),
`src/proofpack/io/declare.py`, `src/proofpack/io/mapping.py`, `src/proofpack/io/profile.py`
(new), `tests/test_halt_gates.py` (1 line), three new test files, and 71 files under
`tests/fixtures/mapping/`; this commit adds ten files under `handoffs/` (`2026-09-18_A*.md`).

**Against lane E's `main` at `a5a5ed8`** (`b93e050` E6 build + `a5a5ed8` E6 repair 1 — a
`repair-E6-r2` worktree existed when I measured, so lane E's tip may move — re-check against
the final E tip), measured now with `git merge-tree --write-tree a5a5ed8 ea526e3` (read-only):
**one conflict, `scripts/mutation_sweep.py`, three hunks**; `cli.py`, `errors.py`, `gates.py`,
`test_halt_gates.py` and the `io/` files are untouched by lane E since `4eb44f3`, so they merge
without a hunk. Lane E's day-6 files (`stats/calibration.py`, `stats/descriptive.py`,
`stats/number.py`, `schema/output_schema_v1.json`, `scripts/coverage_calibration.py`,
`fixtures/f4_*`, `tests/test_calibration.py`, `tests/test_descriptive.py`,
`tests/test_subgroups_carried.py`, `tests/assembler.py`) are untouched by the branch. Handoff
file names do not collide (`_E_` versus `_A_`).

**The three hunks and a resolution I tested** (in a throw-away detached worktree at `ea526e3`
with `a5a5ed8` merged `--no-commit`; discarded afterwards, nothing committed): both lanes
extended `Mutant` with a day-selection field — lane E `day: int = 5` with `--day` and the
marker-derived day, in the one `MUTANTS` tuple; lane A `marker: str = "day5"` with a second
tuple `MUTANTS_DAY6_A` appended (`MUTANTS = MUTANTS + MUTANTS_DAY6_A`, which merges cleanly).

1. Usage lines (docstring): keep both, one line for `--marker day6` naming the E and A lists.
2. The dataclass field: keep lane E's `day: int = 5`; in `MUTANTS_DAY6_A` rewrite the 38
   keyword lines `        marker="day6",` to `        day=6,` (a byte-level replace; the file is
   `i/lf w/crlf`, so do it with `newline=''` or on bytes, not `sed -i`, which turned the trial
   copy `w/lf`).
3. The `chosen` / `--list` block in `main()`: keep lane E's (`--day`, the day derived from
   `--marker dayN`, `--only` by id). One semantic change for lane A's usage: `--only <id>` is
   now also filtered by the marker's day, so a day-6 id needs `--marker day6` beside it.

Measured on the resolved trial tree: 0 conflict markers; `ruff check` and `ruff format --check`
clean on the file; `--list` prints 34 `day5` and **68 `day6`** mutants (30 lane E on
`stats/calibration.py` / `subgroups.py` / the schema + 38 lane A) with 0 duplicate ids; merged
`-m day6` **225 passed, 366 deselected in 10.55s**; merged full suite **589 passed, 1 skipped,
1 xfailed in 53.41s**; the merged `--marker day6` sweep: **68 planted, 68 killed, 0 survived; 435 s** (Git Bash only; concurrent with the other runs, so the wall time is inflated).

**Verdict for the merge decision**: the branch's tests are green and nothing of lane E's is
touched, but five blockers are open at `ea526e3` (items 1–5), two of them reachable by a
customer at `proofpack map`. The options as I see them: (a) a repair-3 commit on the branch
before the merge, with a fresh-attack lens on item 2's `apply_mapping` change (DEC-12(i) — it
is the gate between the confirmed mapping and the pack); (b) merge as is and take the five as the
first item of the next A engine slot. The orchestrator's call; this note does not make it.

## Tomorrow needs

Day 7 A is **A3b, the support answerer** (`spec/briefs/day7_A.md`, in `gps-automation`): hourly
sweep of `support@` threads plus the docs "Ask" Pages Function; regex traps first, data trap
first, short-circuit with no API call, attachments never opened; a yes/no LLM check on the
residue; a docs-grounded answer from Postgres full-text retrieval with >= 1 anchor or
`INSUFFICIENT`; escalation email to Josh (X4). Tests: the 45-case corpus (40 + 5 pasted-data
cases) at 100 % trap recall with a mocked API client asserting zero calls on trap cases; 0
digits attributed to a customer run; 0 acceptance phrases. What day 7 inherits from today, in
order:

1. **The five open blockers on `a-p1-mapper`** (carried 1–5), whichever side of the merge they
   are fixed on. Item 2 changes `apply_mapping` (a gate) and needs its own fresh-attack lens;
   items 1, 3, 4, 5 are one line of code and one sentence each, plus the test that feeds the
   literal input (`""` and `accept` at both prompts; sixty distinct `d/m/24`; the nested-`[`
   prior; `roles: {}` and `""`). Record each counter-example before rewriting the sentence.
2. **A3b's data trap is the same rule as today's floor**: nothing pasted by a customer (a
   header, a cell, a number) may be echoed in an answer, a log or an escalation — the
   `SECRETV_*` / `SECRETH_*` construction of lens 3 (69 files, every cell held by fewer than
   10 rows tagged) is the shape to reuse for the 5 pasted-data cases.
3. **A3a round 5** once lane S's `invoice.voided` / `invoice.marked_uncollectible` rows carry
   the invoice id in the payload (DEC-19; the `a3a-lifecycle` branch is unmerged at the lens-5
   FAIL on the hold).
4. **E7 will hash `mapping.json` into the manifest**: settle its shape first (carried 31) and
   whether `run` may still write a `proposed` one into `./pack` (carried 20), or every manifest
   hash changes later.
5. **The mutation sweep after the merge** carries two lists under one `--marker day6`; a sweep
   that lists 68 and kills 68 is the figure to re-measure on the merged tree, in both shells.

## Needs from Josh

Josh's standing instruction (15 September) is "make the decisions needed"; each item below
carries the builder's recommendation so a nod is enough.

1. **`proofpack run` without `--yes` and without a prior** proceeds on the computed mapping
   (day-1 behaviour) and writes `./pack/mapping.json` — original headers and value summaries —
   into the directory a customer hands over (carried 20). Should `run` require a confirmed
   `mapping.json` the way `map --yes` now does, and should the file live outside the pack
   directory? Recommendation: both yes — `run` halts H07 `run proofpack map first` when no
   confirmed prior exists, and the mapping is written beside the pack, not in it. A `cmd_run`
   change, outside today's scope.
2. **Sex coded 0/1** (PhysioNet's coding) maps `medium` with `no dictionary declared for 0/1`,
   so the acceptance is four of five on the real-coded Sepsis header set and `--yes` is refused
   on it even after a human accepts (carried 25; a human accept leaves `confidence` as computed,
   D1 §5 step 3 verbatim). Recommendation: keep medium (mapping it high applies a dictionary the
   customer did not declare), and let an interactive accept record `confirmed: true` on the
   entry so that `--yes` reads "every role high **or** confirmed" — a `check_h07` change that
   needs its own lens.
3. **Date `min`/`max`** stay `YYYY-MM` whatever the row count (carried 23) while numeric extremes
   are withheld below 10 rows. Recommendation: the same withholding rule for the month.
4. **`hadm_id`**: a `case_id` synonym (so `subject_id` + `hadm_id` halts E01 by name on every
   MIMIC export) or left to the `clustering.unit` declaration as built (carried 16).
   Recommendation: as built.
5. **`--yes` beyond the hash and `decided_by`**: re-verify that every `original` is in the header
   set and every `role` is canonical or `attr_`/`rater_` (carried 13; two lines each).
   Recommendation: yes, both.
6. **The DEC-11 code is `E01`** in `errors.MAPPING_CODES`. If the E family should be named
   otherwise (or be H13), say so before another lane depends on the string.

## Open questions (at most five)

1. **Ignored columns at `apply_mapping`** (blocker 2): drop them, key them `ignored:<original>`,
   or have the prompt refuse `ignore` on a column whose original name is a canonical role another
   column holds? The first keeps day-1's "ignored columns keep their name" false for exactly the
   colliding case; the last leaves the customer renaming a source column.
2. **`clustering.unit: subject_id`** (one token outside the enum) halts H08 `enum` with no fix
   line (carried 24); worth a one-line special case in `_h08_from_schema_error`, or leave the
   generic path?
3. **Two-digit-year dates** (carried 17): leave untyped, or add the shape with a stated century
   and D/M rule?
4. **`_tolerant_console()` and Ctrl-C for `run` / `compare`** need a `main()` edit (carried 21);
   which slot, and does it want the same lens as `cmd_map`'s?
5. **`mapping.json`'s shape** (carried 31): the D1 §5 step 5 key list (`roles` + `confidences` as
   two maps) or the list-of-entries with `source` and `notes` as built, before E7 hashes it?

## Sentences refused

Aggregated from the build note, the two repair notes and the six lens notes; each was not
written because its counter-example ran, or because no counter-example was constructed.

Build note (nine): "No HaltError raised here carries an original header or a cell value" (the
test raises 3 fixture halts + 2 constructed and greps for that table's headers and cells of
>= 3 characters); "Declarations are never inferred here" (replaced by "reads no declaration and
writes none"); "The engine never guesses which code is which"; "Original headers never leave the
machine" (replaced by "written to mapping.json and nowhere else by this module"); "`GetConsoleMode`
succeeds only for a real console handle" (not measured then; `[unverified]`); "never case_id /
never a categorical attribute" (replaced by "not among this function's outcomes");
"…_is_never_case_id" as a test name (renamed `_is_not_case_id`); "Suppression before display
prevents a row-level value from appearing"; "The non-TTY path never hangs" (the test states
`stdin=DEVNULL`, timeout 120 s, measured 0.29 s).

Repair 1 (ten): "min/max can no longer leak a row-level value"; "E01 now catches every way of
writing a composite key"; "`--yes` now requires a human decision" (it reads the `decided_by`
string); "A prompt can never end in a traceback"; "A malformed mapping.json is always H07";
"The profiler recognises date columns"; "Non-UTF-8 consoles are handled"; "Nothing sensitive
can reach the H07 message"; "The suite now defends the floor"; "`_stdin_is_terminal` is correct
on Windows consoles".

Repair 2 (ten): "Two columns on case_id can no longer reach the pack"; "The prompt never writes
two holders of one role"; "Every malformed mapping.json is H07" (the lens-2 blocker was this
sentence's sibling — and the lens-3 blockers 4 and 5 are its docstring's remaining
generalisations); "The E01 gate is complete" / "DEC-11 is closed"; "`map` handles Ctrl-C";
"The profiler recognises German/Swiss dates"; "The hash is now pinned against any weakening";
"`--out` errors are handled"; "attr_/rater_ names are validated"; "The all-high prompt is safe
against a stray key" — five literal answers were fed, and Enter was not one of them (blocker 1).

Lens notes: "The floor cannot leak through the values list" / "the floor cannot leak"
(counts 9/10/11, then nine and thirteen literal columns and three boundary files); "no halt
carries a header" / "no halt carries a value" (30, 57 and 69 files, the tokens listed); "H11
catches date columns" / "H11 misses non-ISO dates" (four, then fifteen literal shapes);
"E01 now fires for every composite key" / "every composite key reaches E01" / "DEC-11 is
closed" (fourteen, then 34 declaration shapes and one, then eight header pairs); "the hash is
untested" (two tests feed a width change; none fed a rename until repair 2); "Malformed priors
are now H07" / "`Mapping.read` now rejects every malformed prior" (26 shapes are not every
shape; two pass); "the prompts are safe against Ctrl-C" / "Ctrl-C is handled in `map`"
(`write` is outside the span); "Enter always accepts" (measured at two prompt kinds on one
cohort each); "every date export outside the six shapes prints its dates" (at 60 distinct values
the column is free text and lists nothing); "`--yes` is safe" / "`--yes` is unsafe" (nine
priors were fed); "the prompt cannot write two holders" (three tables, five answer sequences);
"the six date shapes are validated" (`99.99.2024` is `date`).

Sentences the lenses falsified in shipped text, and the commit that replaced each: "Suppression
is applied before anything is displayed or written" / "Aggregates only." (`e92989b`: the three
fields the floor filters, the literal column that leaked, the test id); "a typed halt whose
message ends ..., never a traceback" and "E01 when `clustering.unit` names two or more columns"
(`e92989b`: the separator class, the list keys, `patient-id`); "without reading stdin"
(`e92989b`: "before any prompt"); "Two headers resolving to `case_id` halt with E01, not `low`"
(`ea526e3`: the name-claim pair, the token pair, the three test ids); "any shape or I/O failure is
H07" (`ea526e3`: the inspected / not-inspected list — whose "the bytes decode as UTF-8 JSON" and
"a non-list `roles` raises on its own" are blockers 4 and 5, not yet replaced); "(`a` or `q`;
anything else re-prompts)" and the test id `..._on_anything_but_a_or_q` (blocker 1, not yet
replaced); "the column stays `categorical`" (blocker 3, not yet replaced).

In this note: "the branch is ready to merge" was not written (five blockers are open); "the
merge is clean" was not written (one conflict; the resolution was tested against `a5a5ed8` only,
and lane E's tip may move); "the 31 items are harmless" was not written (items 1 and 2 change
what a customer sees at `proofpack map`); "22 items are carried" was not written as a measured
figure (I count 31 and say where the difference is); "every lens finding is closed" was not
written for any round.
