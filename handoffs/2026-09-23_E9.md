# Lane E - build day 9 (E9: T1, T7, furniture, figures, sample pack) - 23-24 September 2026

**E9 closes on FAIL: the final lens round (3, at `e17a510`) leaves one blocker open, lens-3 FA-B1, where on a run declaring two operating points T1 draws an op2 criterion's line on the F5 plot captioned "at operating point op1" and prints its op2 status ("criterion met") in op1's Table T1-11 (reproduced now on `e17a510` in both shells).** Fifteen items are carried from the final round (rows 93-107; I closed row 107, the PowerShell day-9 sweep, this morning, so fourteen are open), and thirteen earlier E9 rows are still open (108-120).

The regression lens of round 3 is `PASS` with no blocker, and lens 2's blocker (sentences that did not name their operating point) no longer reproduces. Neither fact changes the FAIL above.

This note is the E9 handoff. It summarises three working notes that sit outside the repository: `workflows/notes-day9/E9_build.md`, `E9_repair1.md` and `E9_repair2.md`. It also summarises the six lens notes in `handoffs/2026-09-23_E_lens{1,2,3}_{fresh-attack,regression}.md`. The two lens-3 notes are committed with this one. Engine `main` has 14 commits since `eda8a35` (`git rev-list --count eda8a35..e17a510` = 14, measured now). **Nothing is pushed.** I measured these on `e17a510` this morning, in both shells:

- full suite: **1406 passed, 1 skipped, 1 xfailed**;
- `-m day9`: 150;
- `git diff eda8a35 e17a510 -- src/proofpack/stats/ src/proofpack/gates.py src/proofpack/criteria.py`: 0 lines;
- `design/tokens.json`: 0 lines.

Day 9 was short. It ran 21:30-00:22 on Wednesday, stopped when the computer was switched off, and lens round 3 ran on Thursday morning. There was no third repair (see "Tomorrow needs").

## What landed

Figures are quoted from the note named. "Now" means I measured it this morning.

| Item | Commit | Test ids (`day9`) | Oracle or literal input -> figure |
|---|---|---|---|
| 1 Template library: facet slots, keyed variants, `render/sentences.py` (E8 rows 51, 52) | `30598b3` | `tests/test_e9_templates.py` (70 skeleton cases + 5) | Build note: 56 templates + 6 variants = 62 skeletons. D4 §1.2's own examples print `81/263 (30.8%) [25.5, 36.6]` and `−3.2 [−6.1, −0.4]`. A typed-reason Number with est 0.7777 prints `n.e. (clustered_data_analytic_ci_invalid)` and no `77` |
| 2 T7 methods appendix | `0346f9f` | `tests/test_render_t7.py` (6) | Build note: the page's method set equals the set walked out of `run.json` on three runs (i.i.d., clustered, `y_pred`-only). i.i.d.: 8 methods (`wilson` 135 ... `none` 17). Lens 3 FA re-derived the counts on 7 documents with no difference |
| 3 T1, 15 sections | `cb37338` | `tests/test_render_t1.py` (14) | Lens 3 FA: 926 `data-ref` cells re-formatted by its own D4 §1.2 formatter. 28 differ, each one of three kinds: the typed `ci` cell reads `no interval`, `auprc` reads "not computed in this run (v1.1)", and 2 are the exact-half rounding in row 108 |
| 4 D5 furniture; DEC-48 | `5078a03` | `tests/test_render_furniture.py` (16) | Now: a no-licence run writes `watermark: NO LICENCE - not for submission`, exits 4, and `run.json` is 253,938 bytes. Lens 3 FA: `TRIAL` / `LICENCE EXPIRED - not for submission` on every page of T1 (9), T7 (3) and T8 (3) |
| 5 Figures F2-F5 (inline SVG); the theme block no longer entity-escaped | `196a0fa` | `tests/test_render_figures.py` (8), `test_render_furniture.py::test_the_theme_block_reaches_the_style_unescaped_so_the_system_fonts_apply` | Lens 3 FA: 2,333 coordinates inverted from `data-map` alone against `run.json`; maximum deviation `2.22e-16` |
| 6 `/sample-pack` script; model card v1.1 box; row 23 | `951cd08` | `tests/test_sample_pack.py` (4) | Now, both shells: run.json 236,843, T1 142,924, T7 47,830, T8 35,178 bytes |
| 7 Timing on 100,000 rows | `526c309` | `tests/test_render_timing.py` (`slow`, `day9`) | Now, inside the full suite with both shells' suites running at once: `run --templates T1,T7,T8` 5.72 s (Git Bash) / 5.69 s (PowerShell); three renders 0.50 / 0.51 s. The test uses `B = 200`; lens 1 RG measured 8.93 s at `B = 2000` |
| 8 Day-9 sweep list (15 mutants) | `0d65c03`; four stale patterns `8c5dbfd` | `tests/test_sweep_day9.py` (3) | Now: `15 planted, 15 killed, 0 survived` in Git Bash (169 s) and in **PowerShell (168 s, the first PowerShell run)** |
| Comparator labels PPA/NPA; F5 on comparator runs | `71b00d2` | `test_render_t1.py::test_a_comparator_run_prints_ppa_and_npa_everywhere_it_printed_se_and_sp` | Build note: 27 forest points drawn, 0 before the fix |
| Repair 1 (lens-1 FA-B1..B3, RG-B1, RG-B2 and 11 sentence items) | `051ebef` | `tests/test_e9_repair1.py` (11), one theme test | Repair-1 note: 13 failed, 96 passed at `71b00d2`. Lens 3 FA: `verdict` 0 on 11 T7 pages; the clustered T1 prints "Wilson score for proportions, DeLong for AUROC" 0 times; bin 1 (`boundary_estimate`) is not drawn |
| Repair 2 (lens-2 FA-B1 and 6 sentence items) | `63b547a` | `tests/test_e9_repair2.py` (7) | Repair-2 note: 11 failed, 236 passed at `dd94874`. Now: the two-op CLI input prints "For age = 0-40 at operating point op1, sensitivity was 30/38 (78.9%ᶜ) [63.7, 88.9] ..." and the op2 twin "... 36/38 (94.7%) [82.7, 98.5] ..." |

## What was cut and why

- **No item on the brief's list was cut.** Five scope limits inside items are carried as rows 100 and 120:
  - no new claim generation (ten D4 templates exist in the library only);
  - F3 has no data in v1 (no `pr` array before AUPRC in v1.1);
  - F5 draws the first operating point only;
  - Clopper-Pearson is not built (r6 row 49);
  - the indeterminate both-ways tables print the no-data box.
- **No day-9 sweep mutant was added for repairs 1 or 2 (DEC-12 (ii)).** The list stays at 15 (row 105). This was a time cut.
- **The day-8 sweep was not re-run after repair 2** (row 106). It takes about 25 minutes per shell. `git diff dd94874 e17a510` over the day-8 code paths is 0 lines (lens 3 RG).
- **No third repair.** The short day ended before lens round 3 reported, so lens 3's blocker is open.

## What did not land and why

### Carried from the final lens round (15; one closed this morning)

| # | Item | Why carried | Repro (one line) |
|---|---|---|---|
| **93** | **Lens 3 FA-B1 (blocker).** `render/figures.py::_criterion_lines` (F5) and `render/t1.py::subgroup_blocks` (the T1-11 criteria column) match criteria by attribute, level and metric, but not by `operating_point`. An op2 criterion is drawn on op1's F5 and printed in both operating points' T1-11. The T1-11 captions name no operating point | Found after the last repair; no repair round left | Now, both shells: `python <scratch>/E9handoff/b1_repro.py <repo>` prints `criteria_results [('C3', {... 'F'}, 'op2', 'met')]`, `F5 C3 line count: 1`, `F5 caption op: op1`, `T1-11 plain 'C3 (row 1)': 2 ['C3 (row 1) → criterion met', ...]` |
| 94 | Lens 3 FA-N1 (sentence): `FAIRNESS_GAP` prints the threshold-free AUROC gap "at operating point op1" and again "at operating point op2" (`+0.079 [−0.002, +0.160]` twice) | Repair 2's open question 1; needs 31 | Now: `twoop_cli.py` prints both `For F versus M at operating point op1/op2: ... AUROC gap +0.079` |
| 95 | Lens 3 FA-N2 = RG-N1 (sentence): the test name `test_the_repair_1_test_name_and_docstring_state_what_they_inspect` checks four literal substrings, and its name says more than that | Round cap | `sed -n '/def test_the_repair_1_test_name/,/^$/p' tests/test_e9_repair2.py` |
| 96 | Lens 3 FA-N3 = RG-N3 (sentence): T7's "z and the two-sided p are written to run.json beside the difference" prints on clustered and `y_pred`-only T7s whose 15 / 18 `detail` blocks are empty. No test covers the complement half | Round cap | Lens 3 RG: clustered 400 rows, `case_id = c{i//2}`; count `subgroups[*].diff_vs_*.auroc.detail` |
| 97 | Lens 3 FA-N4: mutant `f5_caption_last_op` survives (`616 passed`). It is non-equivalent: no test renders F5 on a two-op document | Round cap | lens 3 FA `probe/mutants.py` |
| 98 | Lens 3 FA-N5: op id `op/1~x` -> the checker rejects all 60 engine claims at it (`operating_point_mismatch`). This is r6 row 21 re-measured (58 of 60 then) | Checker (E8), unchanged since `eda8a35`; needs 30 | lens 3 FA `probe/opids.py` |
| 99 | Lens 3 FA-N6: `op1` and `op1 ` are both accepted, so T1 prints "at operating point op1" and "at operating point op1 " with different numbers | A declaration rule; needs 30 | lens 3 FA `probe/op_op1_.json` |
| 100 | Lens 3 FA-N7: F5 draws the first operating point only, and the page does not say that op2's forest is not drawn | Build scope limit 3; open question 4 | two-op T1: 9 F5 captions, all "op1" |
| 101 | Lens 3 FA-N8: the F5 AUROC caption joins phrases that contain commas with ", " ("DeLong, Wald interval, DeLong, logit-transformed interval") | Round cap | i.i.d. T1, F5 age / site AUROC |
| 102 | Lens 3 FA-N9: a suppressed Number with `k`/`n` prints `3/4 (‡)` in a sentence slot. Constructed only: 0 `suppressed: true` in seven `run.json` files | No engine path found | lens 3 FA hand-built Number |
| 103 | Lens 3 RG-N2: the two pins in `test_e9_repair2.py` pass at `dd94874`. Each fails when its mutant is planted (`1 failed, 6 passed`) | Record | lens 3 RG `wtpin` |
| 104 | Lens 3 RG-N4: the repair-2 note greps `proofpack-site` "at `f289501`". The site is at `02c42b8` (now); lens 3 RG re-ran the grep there and found no match | Record | `git -C proofpack-site log --oneline -1` |
| 105 | DEC-12 (ii): no day-9 sweep mutant for repair 1 or 2. Lens 3 RG: none of its 14 repair-2 mutants is in the list | Time | `python scripts/mutation_sweep.py --list` -> 15 day9 |
| 106 | Day-8 sweep not re-run after repair 2 | Time (about 25 min per shell) | `python scripts/mutation_sweep.py --marker day8` |
| 107 | ~~PowerShell day-9 sweep not run~~ **Closed now:** `15 planted, 15 killed, 0 survived; 168 s`, exit 0 | - | - |

### Earlier E9 rows still open

| # | Item | Why carried | Repro |
|---|---|---|---|
| 108 | Exact-half rounding: `51/80` prints `63.7` (lens 1 FA-N5), and `/overall/op2/accuracy` 0.6225 prints `62.3` (lens 3). `format.py` rounds the binary double | A rounding rule; needs 26 | clustered T1 "PPV was 51/80 (63.7%)" |
| 109 | F5 criterion line for value −0.03 drawn at x = 183.1, left of the plot origin at 190; a `site = S3` criterion drawn across every site row (lens 1 FA-N6) | Figure rule not decided | lens 1 FA |
| 110 | T1-11 skips the reference level's criterion row (lens 1 FA-N7; re-measured by lens 2) | Round cap | three `C1` rows: T1-11 prints rows 4, 5 |
| 111 | T1's `<h1>` "T1 · FDA AI-DSF performance evidence attachment set" carries no draft label: 1 per T1 (lens 1 FA-N9 / RG-N8; lens 3) | Needs 27 | read the title |
| 112 | T7 prints the engineering record: DEC ids, "Lens 2", pytest ids, script paths, "(cited from memory)", "the page returned 403 in the research session" (lens 1 FA-N1 residue; lens 2 FA-N3; lens 3) | Writing item with its own lens; needs 25 | grep `DEC-` in T7 |
| 113 | T7 section numbering has gaps (i.i.d. 1-5, 7-10); no print page counter (D5 §3.5) (lens 1 FA-N10, N11) | Open question 5 | read T7 |
| 114 | A consistent change to a `PlotMap` constant is killed by the T1 golden only (lens 1 RG-N4; lens 2 `X0` 70->71 `1 failed`) | Test design | plant in `figures.py` |
| 115 | `scope.LONG_FORM_ITEMS[6]` types "the 200/200 calibration-curve convention" (D4 7.2 verbatim) (lens 2 RG-N3) | A D4 text change; `minimum` is an engine constant | lens 2 RG repro |
| 116 | `tests/test_render_t8.FORBIDDEN_ON_PAGE` lacks 7 of the checker's words. With them, each page has 1 hit: `endorsed` in the cover-note negation "no regulator has endorsed this tool" (lens 2 RG-N5; lens 3) | Needs 28 | lens 2 RG |
| 117 | `test_licence.py::test_the_shipped_key_is_the_site_s_live_key_...` reads `proofpack-site`'s working tree (lens 2 FA-N9) | Needs 29 | lens 2 FA |
| 118 | A null overall `sensitivity` labels F2's caption `ppa` (lens 2 FA-N8; constructed only) | No engine path | lens 2 FA |
| 119 | Surviving mutants with no test. Lens 1: `ne_tiers_dropped`, `f5_ref_line_other_op`, `prev_row_label_pi_dropped`, and the `AUROC_METHOD_PHRASE_KEYS.get(method, "iid_wald")` default. Lens 2: `ipa_footnote_unconditional`, `f5_desc_always_dashed`, `t1_table_methods_without_overall` (non-equivalent); `method_list_default_phrase`, `t7_printed_numbers_includes_analytic` (equivalent on the methods seen) | A test per mutant | lens notes' mutant tables |
| 120 | Scope limits: ten D4 templates in the library only (FLOW_COUNTS, SITE_COUNT, THRESH_PROVENANCE, DECL_ECHO, MONITORING_POINTER, PRESPEC_FLAG, CALIB_CURVE_FLAG, FAIRNESS_INTRO and the two label sets); F3 no-data on every v1 run; both-ways tables print the no-data box | Build note "Cut or carried" 1, 2, 5; open questions 1, 4 | build note |

### r6 rows (E8) that E9 touched or inherited

**Closed by E9:** 23 (section 7 prints no status word without criteria; `test_sample_pack.py`), 27 (DEC-48 built), 29 (T1 prints `calibration: null` beside the reason, and the DEC-35 footnote). 52 is closed on the render side: a subset of a family's pointers prints `not reported` in the missing slots. The checker's rule 8b still accepts the subset.

| r6 # | State at `e17a510` |
|---|---|
| 87 | Open, unchanged. Now, both shells: `k1_checker.py` prints `engine claims rejected: 0` and the same 15 `free_text accepted` texts. E9 wrote no `free_text` (DEC-64) |
| 54, 55, 56 | Open. Now: `repros6.py` `B1: [None x5]`, `B2: [None, None, None, 'free_text_verdict_word', 'free_text_verdict_word']`, `B3: [None, None, None, 'free_text_certification_word']` |
| 78, 70, 44, 19, 20, 53 | Open (DEC-64 class; no word list widened today) |
| 49 | Open. Clopper-Pearson is not built. Repair 1 removed "with Clopper-Pearson alongside at k = 0 or k = n" from the long-form item, so no page now states CP (needs 20) |
| 51 | Render half addressed (slots bind by facet). The 11 checker-accepted mutations were not re-measured |
| 67, 71 | Open. T1 prints the reason on `y_pred`-only and `lower_is_positive` runs; no `CALIB_NA` claim for those reasons |
| 81 | Open, and more exposed: T1 prints level labels in many slots. Lens 3 counted 134 raw U+2028 and 134 raw U+200B on its op-id injection T1 |
| 21 | Re-measured by lens 3 as row 98 (60 of 60 at `op/1~x`) |
| 22, 31, 8 | Open; not touched |
| All other r6 rows (6-92) | As in r6; E9 touched none of their code (`git diff eda8a35 e17a510` over `stats/`, `gates.py`, `criteria.py`, `narrate/checker.py`, `narrate/claims.py`, `egress/` is 0 lines now) |

## What the lenses found

Every lens ran in detached worktrees with `PYTHONPATH=<wt>/src` forced and `proofpack.__file__` printed first. Every CLI run carried `--offline`, and most also ran with sockets refused (0 calls). The blockers below are quoted as each note heads them, unsoftened.

| Round, sha | Fresh attack | Regression | Re-derivation deviations quoted |
|---|---|---|---|
| 1, `71b00d2` | **FAIL, 3.** B1: forbidden words on T7 (every run, including the public sample pack: `verdict` 4x, `effective` on clustered) and on T1 (`biased` on a derived threshold). B2: on a clustered run, four fixed captions and one T7 sentence name interval methods the run did not use. B3: the figures plot the `est` of a Number that carries a typed reason, as a bare estimate | **FAIL, 2.** RG-B1: F4 prints a typed `200/200 convention` that does not come from the run. RG-B2: T7 prints `verdict` four times, and no test reads T7 for it | Cells: 141/135/116/141 matched per run; the one mismatch per run is the intended `auprc` cell. F2 max deviation `1.11e-16`. Sentence slots: one mismatch per run, the 51/80 tie |
| 2, `dd94874` | **FAIL, 1.** B1: on a run with two operating points, T1 prints 92 claim sentences that are bound to one operating point and do not name it ("For age = 0-40, sensitivity was 30/38 ..." and "... 36/38 ...") | **PASS, 0 blockers.** N1 docstring; N2 two unpinned branches; N3 `200/200` in `scope`; N4 S4 context keys; N5 page grep narrower than the checker | 902 cells, only the 6 intended `auprc` mismatches. Lens 2 RG: 114/114 and 119/119 after its own U+2212 correction |
| 3, `e17a510` | **FAIL, 1.** B1: "On a run that declares two operating points, T1 prints a customer criterion's status and draws its line against the other operating point's estimates." It reproduces byte for byte at `dd94874` too | **PASS, 0 blockers.** N1 test name; N2 pins pass pre-fix; N3 z/p sentence on clustered / `y_pred`; N4 stale site sha | 926 cells, 28 mismatches of three kinds (item 3 of "What landed"). Figures: 2,333 coordinates, max `2.22e-16`. Slot bindings: 884 checked |

What round 3 could not break (quoted from its notes):

- **Lens-1 and lens-2 blockers.** FA-B1 of lens 2: 0 unnamed of 70 / 130 / 62 / 59 / 70 / 70 op-bound claims on six documents. Lens 1's B2, B3, RG-B1 and RG-B2 repros no longer reproduce.
- **Injection.** Through an op id, every author, justification, level and source: `html.parser` finds no `script`, `img`, `iframe`, `object` or `link` start tag and no `on*` attribute on 12 pages. `[unverified] planted` survives every time.
- **Draft label.** On the documents lens 3 rendered, every `FDA_AIDSF_*` anchor carries "draft guidance (January 2025), not for implementation" (T1 27, T7 3, T8 6).
- **Wheel.** Installed into a fresh venv with the editable `.pth` inactive, the wheel rendered a two-op T1 identical to the source tree's (200,793 bytes).
- **Forbidden words.** Outside `.status` / `.disclaimer` / `.customer-text` the only hits are `endorsed` in the cover note and `guarantee` in "not a guarantee" (T7).
- **No statistics changed.** Six documents built at both `dd94874` and `e17a510` are byte-identical.

## The decisions taken today

Each one is a builder's or repairer's choice. None is a Josh decision (the decisions file is unchanged since DEC-67, read now).

| Decision | Rule as shipped | Where | Test that feeds the input |
|---|---|---|---|
| Facet-slot syntax | `FACET_BINDINGS[template][slot]` = `<selector>.<facet>`; a missing selector prints `not reported` | `narrate/templates.py`, `render/sentences.py` | `test_e9_templates.py::test_a_family_claim_fills_each_slot_by_pointer_facet_not_by_position` |
| Library key | `{template_id, metric_id, subgroup_id, comparator_id, status}` via `lookup()`; six variants | `narrate/templates.py` | `::test_the_cases_cover_every_template_every_variant_and_every_phrase_key` |
| AUROC phrase | `iid_wald` = "DeLong, Wald interval", chosen from the Number's method (D4 said "logit-transformed") | `METHOD_PHRASES` | `[auroc_*]` skeleton cases |
| Deviations from D4 / Master text | Each is recorded as a deviation in the repair note that made it. D4 §8 THRESH_PROVENANCE: "derived from this dataset: the threshold was chosen on the same data its performance is estimated on" (D4: "... optimistically biased", needs 24). D4 7.2 item 7 loses the CP clause. Master 5.1 X1 loses its method clause (methods print from the run). D1 §9's F17 sentence is removed from T7. D4 §2 row 12: section 12 always renders (DEC-08). Subgroup / fairness sentences carry " at operating point {op_id}" | `narrate/templates.py`, `scope.py`, `render/t7.py` | `test_e9_repair1.py::test_t1_with_a_derived_threshold_carries_no_forbidden_word`, `::test_the_sentences_repair_1_falsified_are_gone_from_the_shipped_text`, `test_e9_repair2.py::test_a_two_operating_point_t1_names_the_operating_point_in_each_bound_sentence` |
| Figure marks | A mark is drawn only from a Number with an interval (`format.has_interval`). Anything not drawn is named in the caption with what `format.number` prints | `render/figures.py` | `test_e9_repair1.py::test_a_decile_bin_with_a_typed_reason_is_not_plotted`, `::test_f5_draws_no_reference_line_and_f2_no_marker_for_a_typed_reason` |
| Caption methods | T1-10, T1-11, F4 bars and T7 X1 name the methods of the Numbers they print; an unmapped id prints as the id | `render/t1.py::cell_methods`, `figures.method_list`, `t7.difference_methods` | `::test_a_clustered_t1_and_t7_name_the_methods_their_numbers_carry` |
| T7 sources | Methods from every Number in `run.json`. Citations from `design/citations.yaml`, all ten `verified: false`, printed `[unverified] citation pending verification` | `render/t7.py` | `test_render_t7.py::test_a_planted_unverified_citation_survives_to_the_page` |
| Placeholder rule | An unfilled CT slot prints `scope.PLACEHOLDER`. `pack_text.yaml` is not read, so the build note records CT-01..09 and CT-11 as outstanding on every run (11 on the fixtures) | `render/t1.py`, `_furniture.html` | `test_render_t1.py` placeholder test |
| Marks | ASCII hyphen in `PLACEHOLDER` / `INCOMPLETE_MARK` (D4 prints an em dash; open question 3). DEC-48's `NO LICENCE` only for `reason_code == no_file`; a refused file keeps `LICENCE EXPIRED` (open question 2) | `scope.py`, `run.watermark_for` | `test_render_furniture.py` (12 parametrised) |
| Sample-pack entry point | `scripts/build_sample_pack.py --out <dir>`: no input path, no criterion, `SYNTHETIC - illustrative` beside `NO LICENCE - not for submission` | `scripts/` | `test_sample_pack.py` |
| Model card | v1.1 (Master §2, D5 PK-16); T1 §15 prints D4's heading note and a no-data box | `templates/T1.html` | `test_sample_pack.py` |
| Default `--templates` | Stays `T8` (needs 21) | `cli.py` | `test_render_t8.py::test_templates_t1_...` |

## CLI and output changes S4 must know (DEC-43)

The site builds from `a0c9abc` (read now: `proofpack-site/.github/workflows/ci.yml` line 18; site HEAD `02c42b8`). **Do not move the pin to `e17a510`: it carries lens 3's open blocker (row 93).** Move it to the sha after that blocker's repair and lens. From `d36c767` (r6's list) to `e17a510`, `git diff --stat` over `cli.py`, `run.py`, `schema/`, `manifest.py`, `licence/` and `pyproject.toml` is 7 files, +84 / −27 (measured now from `eda8a35`). In full:

- **Flags and defaults.** `run --templates` accepts `T1`, `T7`, `T8` (comma list). Each one writes `<out>/T1.html`, `<out>/T7.html` or `<out>/T8.html` on licence `ok` or `grace`. The default is still `T8`. The help text reads `comma list of T1, T7, T8 (default T8; T1,T7,T8 writes all three)`. The typed "not built in E8" line is gone. `T9` exits 5 with `error: unknown --templates id 'T9'; choose from T1, T7, T8` (lens 3). `--format` is unchanged.
- **Printed lines** (all read now or in `cli.py`'s diff):
  - licensed: `Next step: open the documents beside run.json; --templates T1,T7,T8 writes all three (docs: /docs/run)` (was `... T1 and T7 land on E9 ...`);
  - no usable licence: `Next step: proofpack licence install FILE, then run again for <T>.html[, <T>.html] (docs: /docs/run)`, naming the templates asked; with a `--format` without `html`, `... then run again with --format json,html for <T>.html ...`;
  - licensed with `--format json`: `Next step: run again with --format json,html for <T>.html[, ...] (docs: /docs/run)`;
  - DEC-48: summary `licence refused (no_file); watermark: NO LICENCE - not for submission` and `run.json was written with the watermark 'NO LICENCE - not for submission'` (both now). A present but refused file keeps `LICENCE EXPIRED - not for submission`.
- **`run.json` keys** (`schema/output_schema_v1.json`):
  - `manifest.watermark` enum gains `"NO LICENCE - not for submission"`;
  - new optional `manifest.data_marking` (`null | "SYNTHETIC - illustrative" | "DEMO"`), written only when set (only the sample-pack script sets it);
  - no other key moved. The e2e fixture now gives `253938 bytes 20 keys 70 claims 0 rejections 3 refs` in both shells.
- **T8 bytes.** The e2e `T8.html` is 43,961 bytes (40,375 at r6; now). What changed on the page:
  - the cover stamps `<p class="stamp" data-mark="licence|data|incomplete">`, including `INCOMPLETE - customer sections outstanding: 11`;
  - margin notes add ` · section ... · eSTAR: ...`;
  - section 10 lists 11 CT slots;
  - section 7 prints status rows only when criteria exist;
  - the long-form item no longer carries the CP clause;
  - the `<style>` custom properties carry real quotes, so the system fonts apply.
- **Paths and wheel.** New `proofpack/_schema/conventions_T7.md` and `_schema/citations.yaml`. New templates: `T1.html`, `T7.html`, `_criteria.html`, `_figures.html`, `_furniture.html`, `_print.css.j2`. New modules: `render/{sentences,t1,t7,figures,markdown}.py` and `proofpack/synthetic.py`. **A T8-only render imports `render.t1`** (for the outstanding slots), so the Pyodide bundle needs the new modules and templates even when the demo renders T8 alone. `proofpack-site/scripts/demo_sample.py` line 62 calls `run ... --quiet` with no `--format` and no `--templates` (read now), so it still writes T8 only.
- **`/sample-pack`.** `python scripts/build_sample_pack.py --out <dir>` writes `run.json`, `T1.html`, `T7.html` and `T8.html`. Now: 236,843 / 142,924 / 47,830 / 35,178 bytes. Every page carries `SYNTHETIC - illustrative` and `NO LICENCE - not for submission`, plus eleven `[CUSTOMER TEXT REQUIRED ...]` boxes.
- **Python API.**
  - Removed: `run.LICENCE_FIX` (now `run.licence_fix(watermark)`).
  - New: `run.watermark_for`, `run.document_writers`, `assemble_run(data_marking=)`, `manifest.build_manifest(data_marking=)`, export `proofpack.licence.WATERMARK_NO_LICENCE`, `render.figures.method_list`, `render.t1.cell_methods`, `render.t7.printed_numbers`, `render.t7.difference_methods`.
  - Context keys: `f4_calibration` returns `flag_short`; T1 context carries `table_methods` / `diff_methods`; `t7_context` carries `x1_methods`.
  - Changed text: `X1_SENTENCE`, `TOLERANCE_POLICY`, `scope.LONG_FORM_ITEMS[6]`, the `theme.css_root_block` error message, and the `LIBRARY` / `VARIANTS` text of `SUBGROUP_ESTIMATE(_WITH_DIFF)` and `FAIRNESS_GAP`. `templates.slots()` of those ids includes `op_id`.
  - Lens 3 RG: `proofpack-site` at `02c42b8` imports none of these.
- **`design/tokens.json`: unchanged** (`git diff eda8a35 e17a510 -- design/tokens.json` 0 lines, now). E9 added no token.

## Re-run these

I measured these on 24 September, 10:00-10:25 BST, in the main tree at `e17a510` with the two lens-3 notes untracked. In each shell, `python -c "import proofpack;print(proofpack.__file__)"` printed `C:\Users\joshs\GPS\ProofPack\proofpack\src\proofpack\__init__.py` first. `PROOFPACK_TR39_FULL=C:/Users/joshs/GPS/ProofPack/workflows/data/confusables-18.0.0.txt` was set for pytest. `<scratch>` = `C:/Users/joshs/AppData/Local/Temp/claude/C--Users-joshs-GPS-ProofPack/498a4e52-21b2-438d-8861-2ecc4702fe63/scratchpad`. The r6 block is `bash <scratch>/rerun/rerun.sh hbash` in Git Bash and `<scratch>/E9handoff/rerun_h.ps1` in PowerShell. Both suites ran at the same time, and both sweeps ran at the same time.

| Command | Git Bash | PowerShell 5.1 |
|---|---|---|
| `python -m pytest -q -p no:cacheprovider` | `1406 passed, 1 skipped, 1 xfailed in 142.91s` | `1406 passed, 1 skipped, 1 xfailed in 139.30s` |
| `-m day9` / `day8` / `day7` / `day6` / `day5` / `ap2` | `150` / `468` / `139` / `285` / `54` / `88 passed` | the same six |
| `-m day4` / `day3` / `day2` / `day1` | `182` / `29 passed, 1 xfailed` / `40` / `59 passed, 1 skipped` | the same four |
| timing line (inside the suite, `-s`) | `run --templates T1,T7,T8 5.72 s; ... renders from run.json 0.50 s; T1 3693048 bytes` | `5.69 s; 0.51 s; T1 3693048 bytes` |
| `python -m ruff check .` / `ruff format --check .` | `All checks passed!` / `193 files already formatted` (`ruff format -v` includes `.md` files: 191 at `e17a510` plus the two untracked lens-3 notes; 194 with this note) | the same |
| `python -m proofpack.cli doctor --offline` | exit 0, 17 `[ok` lines | exit 0, 17 `[ok` lines, `All essential checks passed.` once |
| `python scripts/tr39_subset.py ../workflows/data/confusables-18.0.0.txt --check` | `vendored subset matches`, exit 0 | the same |
| `python scripts/mutation_sweep.py --list` | 289 lines: 16 ap2, 34 day5, 112 day6, 25 day7, 87 day8, 15 day9 | 289 lines |
| `python scripts/mutation_sweep.py --marker day9` | `15 planted, 15 killed, 0 survived; 169 s`, exit 0 | `15 planted, 15 killed, 0 survived; 168 s`, exit 0 (first PowerShell run) |
| `python scripts/build_sample_pack.py --out <dir>` | run.json 236,843 / T1 142,924 / T7 47,830 / T8 35,178 bytes | identical sizes |
| no-licence `run ... --offline` on r6's `e2e_shell` | exit 4; `watermark: NO LICENCE - not for submission`; `Next step: proofpack licence install FILE, then run again for T8.html (docs: /docs/run)`; `253938 bytes 20 keys 70 claims 0 rejections 3 refs [0, 1, 2, 2, 2, 3, 4, 5, None] CRLF 0 BOM False` | the script prints `exit=-1` (`Select-Object -First` stops the process); run without it: `nolicence exit=4`; the same `runjson.py` line |
| no mapping / `licence show` | exit 3 `HALT H07`, `dir=no` / exit 4 | without `Select-Object`: `nomap exit=3 dir=False` / `licence show exit=4` |
| `html_run.py` (test-only licence) | `exit 0 ['T8.html', 'ingest_report.json', 'pseudonyms.json', 'run.json']`, `T8.html bytes 43961 CRLF False BOM False` | identical |
| `repros5.py` / `repros6.py` | `C_zero '99/101 (98.0%) [93.1, 99.5]'` ...; `B1: [None, None, None, None, None]`, `B2`, `B3`, `HALT H08: ... control character U+0000` (then the r5 `FileNotFoundError`) | identical |
| `p3_fair` / `p2_star` / `p7_site` / `alias` / `offline` / `paths` / `imports` | S02 halts (`p7_site`'s `sex` gives `H07`, r6 row 85); `{{ 7*7 }}` literal once, `<script>` 0 raw; `exit 3 ... pack exists: False`; `socket calls 0`; the three H08 lines; `imports ok; loaded: []` | identical (the ps1 runs no `p7_site` and one `p2_star` line) |
| `k1_checker.py` (r6 row 87) | `engine claims rejected: 0`; `free_text accepted: ['unbiassed (British)', ... 'guidance plural']` (15) | the same line, cut at 90 characters by the script |
| verdict grep / E6 schema test | `5 passed, 1403 deselected` / `1 passed` | - |
| **Lens-3 B1:** `PYTHONIOENCODING=utf-8 python <scratch>/E9handoff/b1_repro.py <repo>` | `criteria_results [('C3', {'attribute': 'sex', 'level': 'F'}, 'op2', 'met')]` / `F5 C3 line count: 1` / `T1-11 plain 'C3 (row 1)': 2 ['C3 (row 1) → criterion met', 'C3 (row 1) → criterion met']` / `F5 caption op: op1` | identical |
| Two-op CLI: `python <scratch>/E9r2/twoop_cli.py <repo> <out>` | `rc 0 socket calls 0` and the four sentences in "What landed" (op1 / op2 named; `AUROC gap +0.079 [−0.002, +0.160]` in both, row 94) | identical |
| `git diff eda8a35 e17a510 --` `stats/ gates.py criteria.py` / `tokens.json` / `checker.py claims.py egress/` | `0` / `0` / `0` lines | - |

To repeat a pre-fix figure, add a worktree and force `PYTHONPATH=<wt>/src`. The editable `.pth` otherwise imports the main tree.

## Tomorrow needs

Day 9 was a short day: 2 h 50 min of building and two repairs on Wednesday night, then lens round 3 on Thursday morning. What that cost:

- no third repair, so E9 closes on FAIL with row 93 open;
- no sweep mutants for repairs 1 and 2 (DEC-12 (ii));
- no day-8 sweep after repair 2;
- no browser check of the SVG or the print CSS (every lens lists this under "could not check").

In order:

1. **Repair row 93 before anything is pushed or the site's pin moves.** Filter the F5 criterion lines and T1-11's criteria by `operating_point`, name the operating point in the T1-11 caption, and add a regression test on lens 3's input (C3 at op2) that fails at `e17a510`. Add sweep mutants for repairs 1-3 (row 105). It is a render change, not a statistical gate, but it gets its own lens round. Rows 94-97 and 101 fit the same repair; row 94 waits for needs 31.
2. **E10 per `spec/briefs/day10_E.md`:**
   - `stats.comparison`: McNemar, Newcombe paired, paired DeLong Δ / cluster-bootstrap Δ, Brier and slope bootstrap Δ;
   - the unpaired fallback labelled "not like-for-like"; the margin from `criteria.yaml` only;
   - H12; T2 (D4 §3) as HTML;
   - tests: fixture F5 with a frozen Newcombe paired value, H12, forbidden-word grep on T2;
   - accept: F5 at margin −0.05 -> `not_met`.

   **E10 is a statistics day**, so DEC-12 (i) applies to every repair of its gates. E10 inherits from E9:
   - the library (`lookup()`, facet bindings, `render/sentences.py`), which T2's sentences should use;
   - `base.html` furniture and the `_criteria.html` macro;
   - the multi-operating-point defects (rows 93, 94, 99, 100), which a version comparison with several operating points would repeat;
   - the page grep's narrower list (row 116); row 87 (no `free_text`);
   - rows 49 (Clopper-Pearson) and DEC-55 (`gate_h05` under clustering), both statistics items still not built;
   - row 108 if Josh asks for decimal half-even.
   
   The brief's people track mentions a "T1 DOCX" and a "design partner signed". The engine renders T1 as HTML only, and **there is no design partner**. Josh's review hour would read the sample pack's T1 HTML.
3. **S4 (day 10)** moves the engine pin with the S4 list above, to the sha after item 1's lens, not to `e17a510`. The orchestrator pushes the engine first.
4. **Carried process items:** the day-8 sweep in both shells (row 106). Rows 108-120 need Josh's readings first (needs 24-29).

## Needs from Josh

E8 needs still undecided, numbered as in r6 (no decision recorded after DEC-67):

1. D1 §5.6 H02 row and §2 `y_pred` row should read "y_true or y_pred" and say what a blank `y_pred` does.
2. What `free_text` covers beyond TR39 (rows 19, 20, 44, 53).
3. A `y_pred` at a declared indeterminate value beside a valid score (row 6).
4. Draft label spelling in D4 §1.1 (row 30).
5. Declarations echo: sorted YAML or the customer's file verbatim (row 34).
8. Unicode licence text (row 72).
15. DEC-66's halt code: keep S02 and add a D1 line?
16. Rows 84 and 86: `map` prints cell values raw; a shared alias chain expands `run.json` to 155,418,186 bytes.
17. Row 87: regrade to a blocker, or keep carrying? E9 wrote no `free_text`, and the checker still accepts `approves`, `certifies`, `endorses`, `clears`, `authorised`, `unbiassed` (now).
18. DEC-66 scope: the columns `validate` reads, or every column (row 91)?
19. Row 90: `RecursionError` / JSON `TypeError` in `load_yaml` as a typed H08?

New from E9:

20. **Clopper-Pearson.** Repair 1 removed the CP clause from D4 7.2 item 7, so no page states CP. Confirm that deviation, and build CP later as a statistics item with its own lens (row 49)? Recommendation: confirm.
21. **Default `--templates`**: keep `T8`, or `T1,T7,T8`? This moves the demo's render time and S4's expectations.
22. **The public `/sample-pack`** carries `NO LICENCE - not for submission` beside `SYNTHETIC - illustrative`, and eleven `[CUSTOMER TEXT REQUIRED ...]` boxes. Publish it as built? The alternative is sample customer text, which ProofPack would then be drafting. Recommendation: as built.
23. **Citations.** All ten in `design/citations.yaml` are `verified: false`. Who reads the primary sources? A fetch needs your approval in chat.
24. **THRESH_PROVENANCE wording.** D4 §8 mandates "optimistically biased", but the verdict-word rule refuses `biased`. Keep the built phrase, or exempt D4's phrase in `.provenance`?
25. **T7's conventions text** (row 112). Keep the engineering record (DEC ids, lens rounds, test ids, "cited from memory") on a regulator-facing page, or commission a rewrite with its own lens?
26. **Exact-half rounding** (row 108). Confirm the binary-double rule, or ask for decimal half-even (a format change with its own lens)?
27. **T1's title** (row 111). Add the map's draft label under the `<h1>`, or keep D4's title?
28. **Page grep** (row 116). Add the checker's seven words, then exempt or reword the cover-note negation "no regulator has endorsed this tool"?
29. **Day-7 key test** (row 117). Read `proofpack-site`'s committed blob (`git show HEAD:`) instead of its working tree?
30. **Operating-point ids** (rows 98, 99; r6 row 21). Refuse at declaration (H08) ids containing `/` or `~`, or ids that differ only in white space?
31. **`FAIRNESS_GAP`'s AUROC gap** (row 94). Give it its own sentence (a new template), or keep D4's one-sentence shape and drop the op clause from the AUROC half?

## Open questions

1. Should the engine generate claims for the ten library-only templates? That moves the claim count from 70 and needs a checker lens (row 120).
2. A present but refused licence file: `LICENCE EXPIRED` (as built), or DEC-48's `NO LICENCE`?
3. The ASCII hyphen in `PLACEHOLDER` / `INCOMPLETE_MARK`, where D4 prints an em dash: keep, or follow D4?
4. F3 prints a no-data line on every v1 run, and F5 draws the first operating point only (rows 100, 120). Drop F3 until v1.1? One F5 set per operating point?
5. T7 section numbers (row 113): keep D4's fixed numbers with gaps, or renumber as printed?

## Sentences refused

Aggregated from the build note, repair notes 1 and 2, the six lens notes and this note.

- "T1 prints every sentence D4 section 2 lists." Written instead: prose comes from the 70 engine claims; ten templates are library-only.
- "Every citation in T7 is verified." Written instead: all ten are `verified: false` and printed pending verification.
- "The figures are exact" / "Every figure coordinate is correct." Written instead: 2,333 coordinates on seven documents, maximum `2.22e-16`.
- "T1 matches D4 exactly." Written instead: the deviations under "The decisions taken today".
- "The sample pack is ready to publish." Not written (needs 22).
- "The sweep kills every non-equivalent day-9 mutant." Written instead: 15 planted, 15 killed; rows 97, 105 and 119.
- "E8's pages rendered as designed." Refused: they rendered in the serif default until `196a0fa`.
- "Clopper-Pearson is shown alongside Wilson." Refused. The clause is deleted (row 49).
- "T7 carries no forbidden word." Written instead: the T7 pages fed (i.i.d., clustered 400 rows, sample pack) give 0 `FORBIDDEN_ON_PAGE` hits outside the exempt scopes.
- "Every caption now names the methods the run used." Written instead: T1-10, T1-11, F4 bars and T7 X1 read the methods of their Numbers.
- "No figure plots a typed-reason estimate." Written instead: F2 markers, F4 points and bars, and F5 reference lines are drawn only from Numbers with an interval; F3 was not fed.
- "Two runs of the same input differ only at run_id, started and duration_s" as T7 text. Refused; the F17 sentence is deleted.
- "The conventions file is ready for a regulator." Refused (row 112).
- "The F4 note is right for every curve_flag." Written instead: `minimum` 200 and 150 fed.
- "Every sentence on T1 names its operating point" / "No sentence on T1 is ambiguous about its operating point." Written instead: 126 op-bound claims on the two-op document each carry their id. Row 99 is a counter-example to the second sentence.
- "T1 is correct for several operating points" / "T1 attaches each criterion to the right table." Refused: row 93.
- "No page prints z or p" as T7 text; "T7's z/p sentence is true of every run." Refused (row 96).
- "The next-step line is right for every --format." Written instead: `--format json` with and without a licence, and the default, fed.
- "Every repaired branch is pinned by a test" / "Every test in the repair fails before the fix." Refused (rows 103, 119).
- "No page carries a certification word" / "No page prints a verdict word." Written instead: the pages rendered, with `endorsed` in the cover-note negation (row 116).
- "The typed 200 is gone from the pack." Refused (row 115).
- "The wheel ships the repair." Written instead: a wheel built at `e17a510`, installed in a fresh venv, rendered one two-op T1 identical to the source tree's.
- "The F17 property holds." Written instead: two runs in separate homes differ at `run_id`, `started`, `duration_s` only; one home also differs at the ledger count.
- "The repair is complete." Refused (row 93).
- In this note: "E9 is done", "E9 passed", "the engine is ready to push" and "the pin can move to `e17a510`" were not written. PASS appears only as the regression lenses' own verdicts.
