# Lane E - build day 10 (E10) - lens 2, regression-and-record, at `667a201` (repair 1 of `322c5a4`; 25 September 2026, 22:30-23:20 BST)

**PASS. No blocker. Every figure in the repair note that I re-measured reproduces in a fresh `core.autocrlf true` worktree of `667a201` (the F5 CSVs checked out CRLF, `i/lf w/crlf`), in both shells: full suite `1649 passed, 1 skipped, 1 xfailed` (Git Bash 222.30 s, PowerShell 189.64 s); `-m day10` `65 passed` with the timing page at 73214 bytes in both; `day9 328`, `day8 470`, `ap3 167`, `ap2 90`; lint clean; the day-10 sweep `14 planted, 14 killed, 0 survived` (Git Bash 164 s, PowerShell 139 s); the sample pack `236,843 / 142,924 / 47,666 / 35,178 / 328,965 / 69,017` in both shells; the fixtures report `rows 46: matched 33, ... no independent oracle 1, ...`; the capture check `72 identical`. Every one of the eight new or changed tests fails at `322c5a4` on the assertion the note quotes, not on an import. Lens 1's blocker (FA-B1) is closed on the literal input: the six sensitivity / specificity cells of the F5 pair under `case_id = c{i // 2}` carry a `cluster_bootstrap_percentile` interval that my own `clustered_flat` + `bootstrap_percentile` re-run with the engine's cell key reproduces bit for bit (nine proportion cells, `IDENTICAL=True` each), where `322c5a4` printed `ci [None, None] method none reason insufficient_clusters` on all six. Lens 1's other blocker (RG-B1 = FA-N1, the CRLF golden) is closed: the golden test passes in this CRLF worktree, and with the two mask keys removed it fails on exactly the two hash cells. Eight non-blocking records follow, three of them sentences.**

Setup: detached worktrees `scratchpad/lens-E10-r2-regression/wt667` (`667a201`) and `wt322` (`322c5a4`); `PYTHONPATH=<wt>/src` on every call, proved by `python -c "import proofpack;print(proofpack.__file__)"` printing the worktree's `src/proofpack/__init__.py` (both shells; without it the main tree's path prints - the `.pth` trap is real). Nothing committed; the main tree carries only this note; both worktrees removed at the end (`git worktree list` shows none under `lens-E10-r2-regression`). A sibling fresh-attack lens ran concurrently with its own worktrees; timings below were measured under that load and are longer than the note's.

## Blockers

None.

## Non-blocking records

| # | Record | Evidence |
|---|---|---|
| N1 | **Sentence (hard rule): "The mask makes the golden independent of the checkout"** (repair note, the `.gitattributes` row of the carried table). It generalises over checkouts; the runs behind it are the main tree (LF) and CRLF copies of the three F5 files. The same note refuses "The golden test now passes in any checkout" three paragraphs earlier and then writes it in other words. What was measured: the golden test passes in a fresh CRLF worktree of `667a201` (both shells) and from CRLF copies (`test_the_golden_is_the_same_page_from_crlf_copies_of_the_f5_files`); the page's only checkout-dependent cells are the two hashes (my unmasked CLI page against the golden: 30 differing lines, every one `run_id`, `started` or the two hashes). | `scratchpad/lens-E10-r2-regression/golden_regen_out.txt` |
| N2 | **The two subgroup accuracy cells moved too, and the note names only the overall one.** The DEC-12 table says the fix "also moves the accuracy cell ... `[-0.160, +0.020]` to `[-0.1805, +0.020]`" and the post-fix figures list overall accuracy only. Measured at B 200, seed 20240101: `sex = M` accuracy `322c5a4` `[-0.1800, +0.0005]` -> `667a201` `[-0.1800, +0.0200]`; `sex = F` `[-0.1800, +0.0200]` -> `[-0.1600, +0.0200]`. The AUROC, Brier and slope cells are byte-identical between the commits (overall and both entries), as the note says. The next lens of this gate should have the full list. | `clustered_667.txt`, `clustered_322.txt` |
| N3 | **The ledger skeletons now differ from D4.** D4 section 5 row 7 and the section 8 template table (lines 325-326) give `LEDGER_STATEMENT` / `LEDGER_WARNING` verbatim as the `322c5a4` text ("...the manufacturer's declared limit is {limit}." / "The declared limit on comparisons against this test set has been reached or exceeded."). The repair changed both (FA-N3) and the note does not say D4 needs amending, while the same note carries FA-N6 because a `not declared` variant "is a D4 section 8 skeleton change" needing a decision. One rule for both: record the D4 deviation for Josh. The new text carries no forbidden word and names the field the value comes from; I am not asking for it to be reverted. | `spec/design/D4_pack_templates.md:100,325-326`; `git diff 322c5a4 667a201 -- src/proofpack/narrate/templates.py` |
| N4 | **Sentence (hard rule): "Not reachable from `assemble_compare` (a plan is clustered only with ids)"** - asserted with a reason and no run. I ran the counter-example: `plan_clustering("case_id", None, 100)` raises `ValueError("clustering.unit is 'case_id' but no case_id column was supplied")` at the plan, before `compare_versions`; and `run.py::_version_arrays` (line 599) builds `case_ids` from the same `table.case_id[mask]` the plan reads (line 702). The sentence holds; the note should cite the run. | this note |
| N5 | **Note figure: the Git Bash E9 re-run block differs from the orchestrator's in six lines, not two.** The note: "identical ... except `243 files already formatted` (v 241) and `5 passed, 1646 deselected`". Mine (246 lines v 243), after masking run ids, temp names and the tag: those two, plus the two `wc -l` diff counts (`7852 -> 7919`, `831 -> 880`: the repair's `comparison.py` and `discrimination.py` hunks), the two lens-1 notes as `A` lines and `M src/proofpack/stats/discrimination.py` in the name-status. All six are the repair itself; none is a regression. The PowerShell block (71 lines, both) differs in the one line the note says (`241 -> 243`). | `rerun_bash_r2.txt`, `rerun_ps_r2.txt` |
| N6 | **The note names a backup that is not where it says.** "the pre-repair copy is `E10_build.md.pre-repair1.bak`": no file of that name exists under `scratchpad/notes/`, `scratchpad/repair-E10-r1/` or anywhere under `AppData/Local/Temp/claude` (`find -name "E10_build.md*"` returns the note itself only). `fix_note.py` writes the note in place (line 76) and no backup. The thirteen corrections themselves are in `E10_build.md` (each names its lens record; I read lines 3, 5, 15, 16, 18, 20, 21, 65, 69, 76, 82, 93, 103, 104). | `find` over the temp tree |
| N7 | **Docstring wording, `stats/discrimination.py` line 93-95:** "the round-7 fresh attack of 2026-09-10 recorded the gap this closed", citing `test_f5_case_id_c_i_over_2_carries_dec_09s_refusal_on_all_fourteen_cells`. That test's AUROC assertions were already true at `322c5a4` (the AUROC cell was a cluster bootstrap there; my `clustered_322.txt` shows it); the test fails at `322c5a4` on the sensitivity cell, not on anything the sentence is about. The closing commit is `cc3cf9f` / `00dee45` (E10 build), pinned by the five day-10 modules failing at collection at `81f1102` (lens 1 regression). Name that, or drop "closed". | `clustered_322.txt`; lens 1 regression note |
| N8 | **Ruff file count (RG-N8 carried, now measured in a worktree):** `243 files already formatted` in a fresh worktree of `667a201`, both shells, and in the main tree; lens 1 measured 241 in a worktree of `322c5a4`. `ruff --verbose` lists 140 `.py` files included (12 `scripts`, 55 `src`, 73 `tests`), the same 140 `git ls-files '*.py'` shows; where ruff's 243 comes from I did not find, and the +2 since lens 1 is not two `.py` files (the diff adds none). Not chased further. | `ruff_verbose.txt` |

## What I could not break (re-measured, with deviations)

**Suite, markers, lint (worktree at `667a201`, F5 fixtures CRLF).** Git Bash: `1649 passed, 1 skipped, 1 xfailed in 222.30s`; PowerShell: `1649 passed, 1 skipped, 1 xfailed in 189.64s`; `-m day10` Git Bash `65 passed, 1586 deselected in 54.65s` (timing line `compare --templates T2 30.17 s; T2 render from run.json 0.04 s; T2 73214 bytes`, under the concurrent suite), PowerShell `65 passed ... in 43.95s` (`23.84 s; 0.03 s; 73214 bytes`); `day9 328`, `day8 470`, `ap3 167`, `ap2 90` (Git Bash). `ruff check .` `All checks passed!`, `ruff format --check .` `243 files already formatted` (both shells). 1649 - 1646 = the three new tests (`_se_sp_cells_equal_a_clustered_flat_rerun`, `_assesses_a_paired_se_criterion_and_t2_prints_the_ci`, `_from_crlf_copies_of_the_f5_files`); the fourth new function replaces the deleted `test_a_clustered_plan_bootstraps_every_difference_and_carries_dec_09s_refusal` (the false name lens 1 recorded). `day10` is in CI: `ci.yml` reads every `day\d+` marker from `pyproject.toml`, where `day10` carries E10's text (line 92).

**Nothing weakened.** `git diff --name-status 322c5a4 667a201 -- tests/`: 5 `M`, 0 `D` (`test_e10_compare_cli.py`, `test_e10_comparison.py`, `test_e10_criterion.py`, `test_e10_t2.py`, `test_e9_templates.py`) plus the golden. Grep of the diff for added `skip`, `skipif`, `xfail`, `.only`, `TODO` and removed `pytest.mark.` lines: none. `git diff 322c5a4 667a201 -- schema/ design/tokens.json src/proofpack/cli.py src/proofpack/run.py src/proofpack/criteria.py src/proofpack/stats/proportions.py src/proofpack/stats/bootstrap.py src/proofpack/gates.py`: 0 lines. `discrimination.py`'s two hunks are docstring lines only (every `+`/`-` line quoted in my capture is prose). The one interval-function change is `comparison.py::_paired_proportion_cell` (the resampler for the conditioned cell and the `ValueError` on missing ids), lensed here.

**Fails pre-fix (`322c5a4` worktree, the five changed test files copied in, `PYTHONPATH` forced).** `8 failed in 4.34s`, each on the assertion the note quotes: the three FA-B1 tests on `assert 'none' == 'cluster_bootstrap_percentile'` (the first `E` line of the first is `AssertionError: ('overall', 'sensitivity', {'est': -0.08, 'ci_lo': None, 'ci_hi': None, ...})`); the two ledger skeleton cases on `'...declared limit is 5.' == '...e_runs) is 5.'` and `'The declared... or exceeded.' == 'The count of...ptance_runs).'`; the compare-ledger CLI test on the banner string; the CRLF golden test and the golden test on the two cover cells. With `322c5a4`'s nine-key mask (the two keys removed from the copied file) the CRLF test fails on **exactly** the hash cells: `b5a7a966...` / `08709dca...` expected, `c3a45a47...` / `ffdbabf0...` got - the leak lens 1 found. Conversely at `667a201` with the two keys removed, both golden tests fail in this CRLF worktree on the same two cells: the new test pins the mask in both directions. Worktrees restored with `git checkout -- tests/` (status clean).

**FA-B1 on the literal input (B 200, seed 20240101, `case_id = c{i // 2}`, `sex` entries), `667a201` v `322c5a4`, `scratchpad/lens-E10-r2-regression/clustered.py`.**

| cell | `322c5a4` | `667a201` | my `clustered_flat` re-run (engine's key) |
|---|---|---|---|
| overall Se | `[None, None]` none `insufficient_clusters` n 50 n_cases 26 | `-0.08 [-0.2000, +0.0196]` n_cases 26 | identical |
| overall Sp | refused, n_cases 26 | `-0.08 [-0.2000, +0.0204]` | identical |
| overall accuracy | `[-0.1600, +0.0200]` n_cases 50 | `[-0.1805, +0.0200]` | identical |
| overall AUROC | `-0.0776 [-0.1734, +0.0001]` | unchanged | - |
| Brier / slope | `0.0388 [-0.0068, +0.0906]` / `-0.6456 [-2.4701, +0.0230]` | unchanged | - |
| M Se / Sp | refused (26 / 24) | `-0.0769 [-0.2308, +0.0394]` / `-0.0833 [-0.2500, 0.0000]` | identical / identical |
| M accuracy | `[-0.1800, +0.0005]` | `[-0.1800, +0.0200]` (N2) | identical |
| F Se / Sp | refused (24 / 26) | `-0.0833 [-0.2083, 0.0000]` / `-0.0769 [-0.2308, +0.0385]` | identical / identical |
| F accuracy | `[-0.1800, +0.0200]` | `[-0.1600, +0.0200]` (N2) | identical |
| M / F AUROC | `-0.0529 [-0.1732, +0.0690]` / `-0.0994 [-0.2230, +0.0001]` | unchanged | - |

Every proportion and AUROC cell at `667a201`: `analytic_status refused_clustered`, `analytic.not_estimable_reason clustered_data_analytic_ci_invalid`, the `*_refused_clustered` flag, `est` equal to the analytic's; `clustering_route declared`. Through the CLI (`compare --offline`, the test licence, the ephemeral registry, `Cse` sensitivity `ci_lower_bound >= -0.30`): exit 0, `run.json` validates against `output_schema_v1.json` (0 errors), `Cse` `met / statistic_compared / compared_value -0.19999999999999996`, the page has `insufficient_clusters` 0, `n.e.` 0, `−8.0 [−20.0, +2.0]` 6, `not like-for-like` 0, 36 claims, 0 rejections. The `bootstrap.proportion_ci` route for the run's own clustered proportions is `clustered_flat(ids, n_rows=n)` (`bootstrap.py:1280`), so the comparison's proportion cells and the run's own now share one resampler, as the note says.

**McNemar against statsmodels 0.15.0 (`hand.py`, my own exact binomial in integers and `(|b - c| - 1)^2 / (b + c)` with `erfc`).** Twelve tables (`(10, 2)`, `(12, 12)`, `(13, 12)`, `(0, 7)`, `(0, 0)`, `(1, 0)`, `(13, 13)`, `(100, 0)`, `(24, 0)`, `(25, 0)`, `(0, 10)`, `(7, 30)`): hand v statsmodels exact `0.0`, corrected statistic `0.0`, corrected p `1.11e-16`; engine v statsmodels exact `0.0` on every exact route (`0.03857421875` on F5), corrected p `1.11e-16` worst, corrected statistic `0.0`; the route is exact at `b + c = 24` and corrected at `25`.

**Newcombe paired - NOT an oracle.** My recollection of method 10 (Wilson arms, `phi` from `eh - fg` with the `n/2` correction when positive, square-and-add) on seven tables: engine deviation worst `8.33e-17` (F5 accuracy `(80, 2, 10, 8)` -> `-0.08 [-0.155356, -0.010249]`, `phi 0.4946`; `(40, 1, 5, 4)` -> `[-0.194203, +0.026030]`; `phi < 0` `(5, 30, 30, 5)`; all-agree; `g = 0`; `f = 0`). Two recollections agree; the paper was not fetched; the fixtures report's `no independent oracle 1` row is the right status (need 35 stands).

**Paired DeLong on R2's F3 pair (`hand.py`, O(m n) placement components, covariance sums written out).** `AUC1 0.80, AUC2 0.64, S = [[0.024, 0.028], [0.028, 0.0392]]`, `Var(diff) 0.0072` (no-covariance `0.0632`), `z 1.8856180831641263`, `p 0.05934644`, Wald `(-0.0063084589, 0.3263084589)`. Engine (`paired_delong`, `method delong_wald`): est deviation `1.11e-16`, bounds `1.11e-16`, `variance_difference 0.007200000000000005` (deviation 0), `z 1.8856180831641252` (`1.1e-15`), `p_value 0.0593464387919201`.

**Golden T2 through the CLI (`golden_regen.py`, CRLF fixtures, `sha256(f5_new.csv) = c3a45a47...`).** `compare --offline` exit 0, five files, 61,179-byte page; against the golden unmasked: 30 differing lines, all `run_id` (title, five headers, cover), `started` (five footers, the print footer) and the two hash cells. Re-rendered from `run.json` with **only** `run_id`, `started`, `input_sha256`, `prior_input_sha256` masked to the test's values: 0 differing lines, 61,072 bytes both sides; `duration_s` reaches nothing on the page. So the task's "masking touches only run_id, started and duration_s" is now: `run_id`, `started` and the two hashes reach the page; the test's eleven-key mask is over-broad by seven keys and harmless. Sample pack (`--compare`, both shells): `run.json` 236,843, `compare.json` 328,965, `T2.html` 69,017 (`SYNTHETIC - illustrative` 8, `NO LICENCE - not for submission` 7); `compare.json` stores each claim as `template_id` + `value_refs` with no rendered text, so the ledger wording moves no byte of it (`LEDGER_STATEMENT` once, `declared ledger limit` 0). Schema at `667a201` (unchanged from `322c5a4`): the F5 CLI `run.json`, the clustered `run.json`, the sample pack's `run.json` and `compare.json`, and the site's `dist/sample-pack/run.json` (`81f1102`, read only): 0 errors each.

**Sweep, fixtures, captures, doctor.** `mutation_sweep.py --marker day10` Git Bash `1 failed, 57 passed, 1586 deselected | 14 planted, 14 killed, 0 survived; 164 s`, exit 0; PowerShell `1 failed, 57 passed, 1586 deselected in 17.26s | 14 planted, 14 killed, 0 survived; 139 s`, exit 0. `--list` 345 lines: 16 ap2, 39 ap3, 14 day10, 34 day5, 112 day6, 25 day7, 87 day8, 18 day9. `fixtures --offline`: `rows 46: matched 33, not matched 0, no oracle recorded 2, no independent oracle 1, not built 5, compared by the test suite only 5`, exit 0 (both shells). `capture_fixture_oracles.py --check`: `72 identical, 0 differ`. `tr39_subset.py --check`: `vendored subset matches`. `doctor --offline`: exit 0, 17 `[ok` lines (both shells).

**E9 r3 re-run blocks (main tree at `667a201`, no `PYTHONPATH`).** Git Bash 246 lines: N5 above and nothing else; every r3 line (doctor, tr39, the no-licence run's `253938 bytes 20 keys 70 claims 0 rejections`, H07, html_run, repros5 / 6, p3_fair, p2_star, p7_site, alias, offline `socket calls 0`, paths, imports `loaded: []`, verdict grep `5 passed`, E6 schema `1 passed`, k1_checker) reproduces. PowerShell 71 lines, one differing line (`241 -> 243`). Main tree status clean before and after.

**Carried items still reproduce at `667a201` (so the carried list is accurate, not stale):** FA-N4 - a `y_pred`-only pair (score and probability `None`) prints `brier` / `slope` reason `score_not_probability` (`comparison.py:511`) beside McNemar `b 10, c 2, p 0.03857421875`; FA-N2 - `Cfirst` (`point_estimate >= -0.10`, met) and `C2` (`ci_lower_bound >= -0.05`, not_met) on one accuracy cell: the T2-3 accuracy row names `Cfirst` and not `C2`, and the lower-case record string appears 0 times on the page.

## What I could not check

- **Newcombe 1998 (paired) itself** - no fetch; my derivation is a recollection, the same status as the note's and lens 1's.
- **The PowerShell sweep's timing** in an uncontended shell: it ran beside the sibling lens.
- **Ruff's 243** (N8): what ruff counts beyond the 140 `.py` files it lists.
- **The E10_build.md backup** (N6): the corrections are visible in the note, the pre-repair text is not anywhere I searched, so RG-N2..N7 / FA-N12 are checked against the corrected note only.
- **T2 in a browser or print.** Bytes only.

## Sentences I refused to write

- "The clustered comparison is fixed." Written instead: the twelve proportion / AUROC cells of the F5 pair under `case_id = c{i // 2}`, their figures, and the re-run that reproduces nine of them.
- "The golden is independent of the checkout" (N1). Written: LF main tree, CRLF worktree, CRLF copies; the two hash cells are the only checkout-dependent cells on the page.
- "The accuracy cell moved as the note says." It moved as the note says overall and in two more cells the note does not name (N2).
- "The Newcombe paired interval is verified."

## Re-run these

`<S>` = `C:/Users/joshs/AppData/Local/Temp/claude/C--Users-joshs-GPS-ProofPack/0e5d0ccc-8634-4ba1-aad8-15203d8e0928/scratchpad/lens-E10-r2-regression`; `<wt>` a detached worktree of `667a201`; `PYTHONPATH=<wt>/src`, `PYTHONIOENCODING=utf-8`, `PROOFPACK_TR39_FULL=C:/Users/joshs/GPS/ProofPack/workflows/data/confusables-18.0.0.txt`.

| Command | Result |
|---|---|
| `python -m pytest -q -p no:cacheprovider` (CRLF worktree) | `1649 passed, 1 skipped, 1 xfailed` (Git Bash `suite_bash.txt`, PowerShell `suite_ps.txt`) |
| `python -m pytest -q -p no:cacheprovider -m day10` | `65 passed, 1586 deselected`, `T2 73214 bytes` (both shells) |
| `python <S>/hand.py` (from `<wt>`) | the McNemar / Newcombe / DeLong deviations above (`hand_out.txt`) |
| `python <S>/clustered.py <wt>` (at `667a201` and at `322c5a4`) | the cell table above (`clustered_667.txt`, `clustered_322.txt`) |
| `python <S>/golden_regen.py <wt> <S>/gr <site run.json> <sp run.json> <sp compare.json>` | 0 differing lines after the four-key mask; 0 schema errors x5 (`golden_regen_out.txt`) |
| `python scripts/mutation_sweep.py --marker day10` | `14 planted, 14 killed, 0 survived` (`sweep_day10_bash.txt`, `sweep_day10_ps.txt`) |
| copy the five changed test files into a `322c5a4` worktree; run the eight test ids | `8 failed in 4.34s` (`prefix/prefix_all.txt`) |
| `bash <scratch9>/rerun/rerun.sh e10r2` (main tree) / `rerun_r3h.ps1` | `rerun_bash_r2.txt` 246 lines, `rerun_ps_r2.txt` 71 lines; diffs as N5 |
