# Lane E - build day 8, repair 6 and lens round 7 - 2026-09-23 (night)

**The final lens round (7, at `d36c767`) is `PASS` from both lenses with no blocker listed, but the fresh-attack lens writes that the attack block's literal rule would have made its N1 a blocker - `free_text` accepts 15 of 27 new texts, among them `The agency approves the device.`, `The body certifies the device.` and `The estimate is unbiassed.` - and graded it record-and-carry because no code at `d36c767` prints `free_text` on T8; Josh may regrade it (row 87, needs 17).** Sixty-nine items are carried (the table below; none is a lens blocker), so E8 lands with them open: lens 6's two blockers no longer reproduce on the lens-6 inputs (table-cell control characters now halt S02 at ingest, exit 3; `zz_extra: &a [*a]` now halts H08, exit 3) nor on the ten alias positions and the further cell inputs (a `compare` prior, `case_id`, a JSON list-of-rows table) lens 7 added.

This note supplements `2026-09-22_E.md`, `2026-09-23_E.md`, `2026-09-23_E_r4.md` and `2026-09-23_E_r5.md`, which are not rewritten; its carried table is the r5 table plus this round's. Since `941f8a7`: `98aa935` (repair 6) and `d36c767` (two docstrings); the two lens-7 notes are committed with this note. Nothing is pushed by this run. Main tree now, both shells: **1256 passed, 1 skipped, 1 xfailed**; `-m day8` 468; `git diff 7b2ca2a HEAD -- src/proofpack/stats/` 0 lines; `tests/` since `7b2ca2a`: 162 `A`, 7 `M`, 0 `D` (all measured now).

## What landed

Day 8 on `main` since `7b2ca2a`, 26 commits (`git log --oneline 7b2ca2a..d36c767`, read now): 18 `E:` and 8 `A:` (lane A's A-P2 and the `0f553c9` merge). Figures are quoted from the note named, or measured now where marked.

| Item | Commit | Test ids (`day8`) | Oracle or literal input -> figure |
|---|---|---|---|
| 1 `overall` on a clustered plan and a `y_pred`-only table (E7 carried 42) | `86b5fdd`, `7fa690b` | `tests/test_overall_carried.py` (8), `test_e8_repair2.py` | Lens 7 FA: 300 rows in 100 cases, two-by-two `{tp 80, fn 15, fp 48, tn 157}` equal to its numpy count, est deviation `0.0` on four proportions; LR+, LR-, DOR, F1, MCC, Youden, balanced accuracy `clustered_data_analytic_ci_invalid` |
| 2-3 jinja2 isolated; `design/tokens.json` from D5 §3.1 | `a2246c1`; `98aa935` (DEC-67) | `tests/test_render_theme.py` (13) | Lens 7 RG: 19 token / hex pairs equal to D5 §3.1, 0 mismatches; its own sRGB formula 6.1890 / 8.6520 / 7.8809 against the `-bg` surfaces |
| 4-6 claims, 56 templates, checker, corpus | `84f1b11`, `ad7cc57`, `7fa690b`, `fe35332`, `375719c` | `tests/test_claims.py` (161), `test_e8_repair1.py`-`4.py` | Corpus 146 files (now). Lens 3: 42,849 value-ref swaps, 0 accepted. Repair 3: 3,850 relabels, 4 accepted and named. Repair 4: 52,694 one-letter TR39 substitutions, 0 accepted |
| 7-8 `render/format.py`, `anchors.py`, `html.py`, T8, `run --format` / `--templates` | `ee295cc`, `ad7cc57`, `375719c` | `tests/test_render_t8.py` (24), `tests/test_render_format.py` (13) | Lens 7 RG: 22 cells re-derived by D4 §1.2, 0 mismatches; draft label on 14 AI-DSF mentions; `[unverified]` 7 times. Now: `T8.html` 40,375 bytes, CRLF False, BOM False |
| 9 golden, footer, anchor label, section 7, verdict grep over HTML | `afe59b9` | `tests/test_render_t8.py` | Lens 7 RG: golden 40,030 bytes equal; footer line removed -> `assert 0 == 3`; AI-DSF status set to `final` -> `assert ([])`. Now: verdict grep `5 passed` |
| 10 day-8 mutation list (DEC-12 ii) | `29fc04e` and each repair | `tests/test_sweep_day8.py` (2) | Now: `--list` 274 lines (87 day8); sweep under "Re-run these" |
| Repair 1 (lens-1 five blockers) | `ad7cc57` | `test_e8_repair1.py` (10) | See `2026-09-22_E.md` |
| Repair 2 (blank `y_pred`, rule 4, fairness-only rows) | `7fa690b` | `test_e8_repair2.py` (9) | Lens 3: 120 rows, 60 blank `y_pred` -> `{tp 21, fn 3, fp 7, tn 29}` equal to numpy |
| Repair 3 (relabels, homoglyphs, reserved op ids) | `fe35332` | `test_e8_repair3.py` (12) | `threshold_free` op id -> H08 (`2026-09-23_E.md`) |
| Repair 4 (dotted `metric_ref`, DEC-61, CALIB_NA, TR39) | `375719c` | `test_e8_repair4.py` (18) | Now: ops `0` / `t0.5` print `99/101 (98.0%) [93.1, 99.5]` / `85/101 (84.2%) [75.8, 90.0]`; `op_lo`/`op_hi` with no score -> H08 |
| Repair 5 (DEC-65, controls in `criteria.yaml` and mapping text) | `ea2f743` | `test_e8_repair5.py` (68) | Now: `Tri\0age` model name -> `HALT H08: declaration invalid at model/name: control character U+0000` |
| **Repair 6, row 73 (DEC-66)** | `98aa935` | `test_e8_repair6.py::test_the_cli_run_with_soh_and_esc_in_race_levels_exits_3_naming_the_column` (2), `::test_the_lens_6_level_star_inputs_exit_3_naming_the_column` (4), `::test_a_control_character_in_a_cell_of_each_read_column_halts_s02` (19) | `make_cohort(n=400)`, `race` `re\x01d` / `bl\x1bue`, `tpr_gap` bound 0.2 -> now `HALT S02: column role 'race' holds a control character (U+0001) in 400 row(s)`, exit 3, `{}` written (exit 0 with `race = re\x01d` at byte 25,164 at `ea2f743`, r5) |
| **Repair 6, row 74** | `98aa935` | `::test_a_self_referential_alias_halts_h08_naming_the_path` (root, `model`, `criteria[0]`), `::test_the_cli_run_with_a_root_self_referential_alias_exits_3_and_writes_nothing` (2), `::test_an_alias_chain_24_deep_is_walked_in_under_two_seconds`, `::test_a_list_nested_5000_deep_is_walked_without_recursion` | Now: `alias.py` -> `exit 3`, `No document was written.`, `pack exists: False`. Lens 7 FA: list chain depth 22, 0.001 s against 2.658 s at `941f8a7` |
| **Repair 6, row 80 (DEC-67)** | `98aa935` | `tests/test_render_theme.py::test_the_declared_contrast_is_the_computed_wcag_ratio[pair1,6,8]`, `test_e8_repair6.py::test_tokens_json_pairs_honesty_stop_and_ok_with_their_own_bg_surfaces` | `on` = `honesty-bg` / `stop-bg` / `ok-bg`; `contrast` D5's `6.2:1` / `8.7:1` / `7.9:1`; `contrast_computed` `6.19:1` / `8.65:1` / `7.88:1`; the "do not match the formula" sentence deleted |
| Repair 6, row 76 (sentences) | `98aa935`, `d36c767` | `::test_the_three_declare_sentences_lens_6_measured_false_are_gone` (3) | Three `declare.py` sentences deleted |
| Repair 6, DEC-12(ii) | `98aa935` | `::test_the_sweep_lists_the_repair6_mutants` | Four mutants (`schema_dec66_check_not_called`, `schema_dec66_c1_dropped`, `declare_self_reference_check_dropped`, `declare_walk_enters_shared_objects_again`), each killed with 1 failure (both lens-7 notes) |

Repair 6 pre-fix: `test_e8_repair6.py` plus the new `test_render_theme.py` in a `941f8a7` worktree give **42 failed, 13 passed** (repair note; both lens-7 notes re-measured it). Lens 7 RG N5: two of the 42 fail only on an `AttributeError` (row 92). Ordinary tables (repair note; lens 7 re-ran it): 41 CSV files, 82,451 cells, 0 control hits; the site's synthetic cohort gives `run.json` byte-identical to `941f8a7` once `run_id`, `started`, `duration_s`, `mapping_sha256` are removed. DEC-66 and the alias guard are input gates; `git diff 941f8a7 HEAD` over `stats`, `render`, `narrate`, `templates`, `cli.py`, `run.py`, `errors.py`, `criteria.py`, `gates.py`, `pyproject.toml`, `uv.lock`, `schema/` is 0 lines (now).

## What was cut and why

1. **Item 11 (E7 carried 24, 20, 21-23)** - not started. The builder hit the weekly usage limit on 22 September; the limit, not a decision.
2. **FA5-B1..B3 and the look-alike class** - carried to E9 by DEC-64 (rows 54-56, 78).
3. **Lens 7's findings** - not repaired: this run was one repair, one lens round and a handoff (orchestrator scope).
4. **T1, T7** - `--templates T1` / `T7` print a typed "not built in E8" line (E9's).
5. **The full Unicode License v3 text** - not fetched; a download needs Josh's approval in chat (needs 8).

## What did not land and why - the 69 carried items

Rows 6-72 keep their numbers and repros from the earlier notes; 73-83 from r5; 84-92 are new. **Closed since r5:** 73 and 74 (lens-6 blockers; both lens-7 notes), 76 (sentences), 80 (DEC-67). Row 77's second half (the 0.51 s walk) is closed; its first half stays. Lens-7 RG N1 (the S4 list) is corrected in this note's S4 section. Rows 84-92 and the lens-7 figures are quoted from the notes named; row 87 was re-run now in both shells.

| # | Item | Why carried | Repro (one line) |
|---|---|---|---|
| **87** | `free_text` accepts 15 of 27 lens-7 texts: `unbiassed`, `biassed`, `meet`, `meeting`, `satisfies`, `succeeds`, `approves`, `certifies`, `endorses`, `clears`, `authorised`, `authorized`, `granted` (De Novo), `see &sect;`, `guidances`. `CERTIFICATION_WORDS` holds `approved`, `approval`, `certified`, `certification`, `endorsed`, `endorsement`, `cleared`, `clearance` only | Lens 7 FA N1, graded record-and-carry against its literal rule; no code prints `free_text` on T8 (0 references in `T8.html` template and `html.py`). Josh (needs 2, 17) | `LENS_WT=<tree> python <lens7-fa>/att/k1_checker.py` -> `free_text accepted: ['unbiassed (British)', ... 'guidance plural']` (now, both shells) |
| 88 | Repair-6 sentences measured false: `tokens.json` `source` "Each token's surface is in its on key" (`brand-strong`, a text token, has none; 10 of 19 entries have none); `columns_read_by_validate` docstring and the test name `test_the_plants_cover_every_column_validate_reads_from_the_table` (`period`, `event_date` listed when `validate` does not read them); `_CONTROL_CELL` comment "the table's cells" (`rater_1`, `notes` unread); `check_control_characters` "H01-H12 are gates on declarations and aggregates" (H03 on `score` 1.5 and H05 on a duplicate `row_id` read table values); `_check_control_characters` "after the first character of `clustering.unit` halts E01" (`case_id\x01` is H08; the test inserts at index 1); the repair note heading "An ordinary table is unaffected" | Round cap | lens 7 FA N2-N5, RG N4 |
| 89 | Repair-6 mutants surviving `-m day8`: `walk_skips_tuples` (`model.extra: !!omap [{k: "v\x01"}]` -> rc 0, four files written), `selfref_skips_tuples` (`&a !!omap [{k: *a}]` -> exit 5 `ValueError: Circular reference detected`), `pcol_clause_dropped` = `b_period_col_dropped` (survives the full suite; `rater_visit` period column `2024-03-15\x01` accepted), `ident_clause_dropped` (stricter) | No test feeds `!!omap`, `!!pairs` or a period column neither canonical nor `attr_` | lens 7 FA `att/mut.py`; RG `probe/mutants.py b_period_col_dropped` |
| 90 | Exit 5 on customer YAML outside the walk: a flow list 500+ deep under `model.extra` -> `RecursionError` in `yaml.safe_load` (300, 400 load); `!!set`, `!!binary`, an unquoted timestamp -> `TypeError ... not JSON serializable`; the walk does not enter a `set`. Nothing written; same at `941f8a7` | A typed halt is a new rule (needs 19) | lens 7 FA `att/y2_cli.py` |
| 91 | DEC-66 as built reads the columns `validate` reads: `notes` `n\x01`, `rater_1` `x\x1b`, unmapped `zz_free` `z\x85` -> rc 0 (0 raw, 0 escaped bytes in the four outputs); the halt names the canonical role, not the header; on `compare` it does not say which table; a JSON cell `["a\u0001"]` is read through `str()` and accepted | Repair 6's recorded choice; Josh confirms (needs 18) | lens 7 FA `att/t1_cells.py` A |
| 92 | Two "fails pre-fix" tests fail at `941f8a7` only on `AttributeError` (`::test_a_shared_but_not_self_referential_alias_is_accepted`, `::test_the_plants_cover_every_column_validate_reads_from_the_table`) | Record | lens 7 RG N5 |
| 84 | `proofpack map` prints a column's top values raw: `race` `re\x01d` / `bl\x1bue` -> 1 raw 0x01, 1 raw 0x1b on the terminal, at both commits | DEC-66 is "at ingest"; Josh or E9 (needs 16) | repair-6 `measure.py` section 4 |
| 85 | `sex` `M\x01`, `score` `0.196917\x01`, `age` `65\x01` halt **H07** (confidence) before the DEC-66 check: exit 3, nothing written, the message names confidence (widened by lens 7 RG N2) | H07 runs before `validate`; moving it moves a mapping gate | lens 7 RG `probe/test_zz_lens7_probe.py`; now: lens-6 `p7_site.py` `sex 3 {} HALT H07` |
| 86 | A shared (not self-referential) alias chain is expanded: 20 levels under `model.extra` -> `run.json` 155,418,186 bytes, rc 0, both commits; under `criteria[0].value` jsonschema's error path is exponential (depth 22 0.55 s, 24 2.38 s at `d36c767`) | A size or node bound is a new rule (needs 16) | repair-6 `measure.py` section 6; lens 7 RG `probe/chain_value.py` |
| 75 | Repair-5 mutant survivors (C1 dropped from the multi-line pattern, keys with the multi-line pattern, `decided_by` unwalked, walk below the authored-field loop, detail prefix) | Not in repair 6's scope | lens-6 FA `att/mutdemo.py` |
| 77 | Lone surrogate `"Tri\ud800age"` model name -> exit 5 `UnicodeEncodeError`, nothing written (not re-run) | A typed halt is a decision | lens-6 RG N4 |
| 78 | Regional-indicator `pass` accepted by `free_text` | DEC-64 -> E9 | lens-6 FA N5 |
| 79 | Mapping fields the DEC-65 walk does not read (`source`, `value_summaries`, `examples`): 0 raw or escaped control bytes in outputs on the inputs fed | Record | lens-6 FA N6 |
| 81 | Format and separator characters: with `race` `re<ch>d`, U+2028 and U+200B each once raw in `T8.html`, U+202E 0; U+2028, U+2029, U+202E, U+200B, U+FEFF, U+00AD, U+2066, U+061C each 8 times in `run.json`, once in `pseudonyms.json` (lens 7 FA N9) | Outside DEC-65/66's ranges | lens 7 FA `att/t2_invis_t8.py` |
| 82 | A CSV header holding a control character (headers are not cells; not measured) | Headers reach `mapping.json` only | repair 5 |
| 83 | `_IDENT` matches `attr_x\n` | `io/schema.py` uses it; unchanged | `re.match(..., 'attr_x\n')` -> `True` |
| **54** | TR39 sources with a non-ASCII Latin target (`oк`, `мeets`): 826 of 1,264 accepted (lens 5) | DEC-64 -> E9 | `repros6.py` `B1: [None, None, None, None, None]` (now) |
| **55** | l-prototype in an `i` position (`unbꓲased`): 2,803 of 4,674 accepted (lens 5) | DEC-64 -> E9 | `B2: [None, None, None, 'free_text_verdict_word', 'free_text_verdict_word']` (now) |
| **56** | TR39 before NFD (`appѓoved`): 1,550 of 3,084 accepted (lens 5) | DEC-64 -> E9 | `B3: [None, None, None, 'free_text_certification_word']` (now) |
| 61 | Mutant `fairness_auroc_gap_path_dropped` survives | No `auroc_gap` criterion row fed | lens-5 FA 4 |
| 62 | Mutant `tr39_rn_both_dropped` survives (`ՠeets`) | One literal | lens-5 FA 4 |
| 63 | DEC-61's `ingest` call unpinned; `compare` depends on it | A `compare` test | lens-5 FA 4 |
| 64 | Mutants `tr39_second_pass_dropped`, `tr39_first_pass_dropped` survive | One literal each | lens-5 RG N3 |
| 65 | Mutant `calib_na_seq_gap` survives | One test | lens-5 RG N3 |
| 66 | TR39 regeneration test skips without `workflows/data/confusables-18.0.0.txt` | Open question 2 | run in a worktree |
| 67 | `y_pred`-only / `lower_is_positive` run: no `FDA_AIDSF_CALIBRATION` ref, no calibration anchor on T8 | Open question 1; in the S4 list | lens-5 RG `probe/refs.py` |
| 69 | Docstrings naming a lens id and no D1/D4/D5 line; now also `self_reference_at`, `columns_read_by_validate`, `_path_text`, `_children`, `_string_hit` (no docstring) | Documentation | lens-5 RG N7; lens-6 RG N7; lens-7 RG N7 |
| 70 | 155 Latin letters outside TR39 accepted (`Ƥass`) | DEC-60/64 | repair-4 note |
| 71 | No `CALIB_NA` for `no_score_column` / `score_not_positive_class_probability` | Open question 1 | 59 claims on a `y_pred`-only run |
| 72 | Unicode notice: three header lines, full licence text absent, **[unverified]** whether that suffices | Needs 8 | read the module header |
| 44 | `&sect; eight`, `ninety`, `Stage IV.` accepted | Needs 2 | `repros5.py` `B4:` last item `None` (now) |
| 47 | Reserved-id mutants survive (second position, case variants) | Pins | lens-4 FA 3 |
| 48 | Four lens-4 checker mutants unpinned | One literal each | lens-5 RG N3 |
| 49 | Clopper-Pearson beside Wilson at k = 0 / n not printed | T1 (E9) | lens-4 FA 5 |
| 50 | `claim_id` with full-width or Arabic-Indic digits accepted (`\d`); lens 7 FA: `CL-００００１` | `[0-9]` | lens-4 FA 6 |
| 51 | 8,988 single-field mutations, 11 accepted | Family skeletons | lens-2 `b_checker.py` |
| 52 | Rule 8b accepts a subset of a family's pointers | E9 slot filling | lens-4 RG N3 |
| 53 | Capital-`I` reading refuses some prose | Errs to refusal | repair-3 carried 4 |
| 6 | `y_pred` at a declared indeterminate value beside a score leaves the analysis | Needs 3 | 120 rows, 12 such |
| 7 | `checker_smallcap_s_unmapped` pin | Record | plant; `-m day8` |
| 8 | `anchors_month_year_fixed` survives | E9 anchor work | plant |
| 9 | `_declaration_for` wrap | Unreachable | plant |
| 14 | Docstrings cite lens ids; H08 text names no reserved ids | Documentation | read |
| 15 | `check` "reads no environment" | Wording | read |
| 16 | Repair-2 pre-fix artefact 25/137 vs 26/136 | Record | - |
| 17 | Golden mask `manifest.numpy` only | Record | unmasked render |
| 18 | `python -m proofpack doctor` fails; `.cli` runs | `__main__.py` or docs | run it |
| 19 | Token join refuses "in significant detail" | Needs 2 | `f(...)` |
| 20 | Derived words accepted (`unbiasedness`, `safely`, `approvals`); see row 87 | Needs 2 | `f('unbiasedness')` |
| 21 | Op ids `a/b`, `op~1`: 58 of 60 claims refused | False refusals | declare `op~1` |
| 22 | Fully excluded subgroup level vanishes | DEC-08, E9 | sex `F`, blank `y_pred` |
| 23 | Section 7 prints status words with no criteria | T8 template | `criteria: []` |
| 24 | `resolve` keeps two accepted claims on one slot | Open | two ids |
| 25 | `declared(1e22)` positional | Record | `fmt.declared(1e22)` |
| 26 | `--format HTML` folds; `''` asymmetry | S4 wording | three calls |
| 27 | No-licence watermark `LICENCE EXPIRED - not for submission` (now, both shells) | DEC-48, E9 | e2e run |
| 28 | `guidance_ref` any map row | As specified | swap |
| 29 | `calibration: null` + reason, DEC-35 IPA not on T8 | T1 (E9) | - |
| 30 | Draft label spelling vs D4 §1.1 | Needs 4 | read page |
| 31 | Fairness mixed scales; `font-weight` literals | Design / E9 | read page |
| 32 | No `jinja2` -> exit 5 after `run.json` | S4 bundle | hide `jinja2` |
| 33 | `guidance_refs` 3 vs page 7 | Open | count |
| 34 | Declarations echo sorted YAML | Needs 5 | section 3 |
| 35 | `integrity.halts` never printed | Not in D4 §11 | read |
| 36 | Wheel test `pytest.skip` path | One line | `_build_wheel` |
| 37 | `declaration_index: true` reads entry 0 | Unreachable | hand-built |
| 38 | `y_true` `NA` indeterminate -> missing label | Day-1 loader | `NA` |
| 39 | Bare YAML `date:` -> H08 | Customer quotes | unquoted |
| 40 | `unicode-bidi: isolate`, `@page var()` unchecked in a browser | No browser | print |

**E7 items still open, under their old numbers** (repros in `2026-09-21_E.md`; repair 6 touched none of their code; not re-run today):

| E7 # | State at `d36c767` |
|---|---|
| 1-16 | Open (16 is the ledger race) |
| 17 | Open: a fairness-only `criteria.yaml` is not counted (DEC-47) |
| 18 | Decided (DEC-47); not built |
| 19-23 | Open; 20-23 were item 11 (cut) |
| 24 | Half closed (rows by position); H08 uniqueness was item 11 |
| 25 | Decided (DEC-48); not built (row 27) |
| 26, 28, 29, 33-37, 39-41, 43 | Open |
| 27 | Closed (DEC-50) |
| 30, 32 | Lane A (merged at `0f553c9`) |
| 31 | Decided (DEC-49); not built |
| 38 | Open |
| 42 | Closed (E8 item 1) |

## What the lenses found

Every lens ran in detached worktrees with `PYTHONPATH=<worktree>/src` forced and `proofpack.__file__` printed first. Blockers as the notes head them.

| Round, sha | Fresh attack | Regression | Deviations quoted |
|---|---|---|---|
| 1, `29fc04e` | FAIL, 5: pointer not bound to `metric_id` / op / subgroup (339 wrong-number bindings accepted); nine `free_text` spellings; a `y_pred`-only table with undeclared labels prints `0/42 (0.0%) [0.0, 8.4]` and `criterion not met`; declared values rounded to three decimals under "as the manufacturer wrote the value"; no `model.prior_version` -> exit 5 after `run.json` | PASS | Wilson vs statsmodels `1.11e-16`; AUROC vs sklearn `0` |
| 2, `657ef11` | FAIL, 4: blank / indeterminate `y_pred` scored "predicted negative"; rule 4 reads eight pointer shapes, 225 of 418 Numbers unbound; a fairness bound with no `criteria` list gives a `not_met` row beside "No acceptance criteria were declared"; six spellings | FAIL, 2: 60 blank `y_pred` in 120 rows -> `21/42 (50.0%) [35.5, 64.5]` exit 0; `pa\x01ss`, `ᴘᴀss` accepted | six pooled ratios `0.00e+00` |
| 3, `7fa690b` | FAIL, 3: template relabels bound by nothing; nine homoglyphs; op id `threshold_free` loses its block | FAIL, 3: repair 2 re-opened 180 `well-calibrated` spellings; 18 criterion-claim mutations; a withdrawn mutant not equivalent | worst `2.22e-16` |
| 4, `fe35332` | FAIL, 4: T8 resolves `metric_ref` as a dotted path; `y_pred`-only with two ops prints one two-by-two under both (statistical gate); `CALIB_NA` false reason accepted; 43 spellings | PASS | `1.11e-16`; 22 T8 cells, 0 mismatches |
| 5, `375719c` | FAIL, 4: TR39 non-ASCII targets 826/1,264; I/l 2,803/4,674; TR39 before NFD 1,550/3,084; NUL, SOH, ESC in the model name raw in `T8.html` | PASS | `2.22e-16`; 23 T8 cells, 0 |
| 6, `ea2f743` | FAIL, 1: C0/C1 in a table cell raw in `T8.html`, exit 0 | FAIL, 1: self-referential alias -> exit 5 `RecursionError` | overall est `0.0`; subgroup interval up to `0.0181` (explained by lens 5) |
| **7, `d36c767`** | **PASS, 0 blockers** (N1 graded record-and-carry against the literal rule; row 87); nine non-blocking: N1 row 87, N2-N5 row 88, N6 row 89, N7 row 90, N8 row 91, N9 row 81 | **PASS, 0 blockers**; eight non-blocking: N1 the S4 list (corrected below), N2 row 85, N3 row 89, N4 row 88, N5 row 92, N6 row 86, N7 row 69, N8 records | overall est `0.0` on four proportions; subgroup interval up to `0.0181` "the same figures lens 5 and lens 6 explained" (FA); WCAG `6.1890 / 8.6520 / 7.8809` (both) |

What round 7 could not break (quoted from its two notes):

- **Repair 6's figures.** `42 failed, 13 passed` at `941f8a7`; suite `1256 passed, 1 skipped, 1 xfailed` in both shells; sweep `103 planted, 103 killed, 0 survived` (FA 1315 s, RG 1463 s), tree clean after.
- **DEC-66 on inputs the tests do not feed.** Leading `\x01red`, `r\x7fed`, `re\rd` halt S02; ESC only in the `compare` prior -> S02, no directory (H09 at `941f8a7`); a JSON list-of-rows table -> S02; `attr_Colour` -> S02 `attr_colour`; 100,000 rows x 19 columns 0.041 s clean, 0.048 s with one hit.
- **Self-reference in ten positions** (RG) and `&a {me: *a}`, `&a [[[*a]]]`, `!!omap` and `!!pairs` self-alias (FA): H08 naming the path, exit 3.
- **Unchanged gates.** Lens 3-6 battery of 55 scripts at both commits: the only differing lines are the lens-6 closures and two traceback line numbers. Renderer, wiring, `--offline` (0 socket calls), the wheel in a fresh venv with no `.pth`, `uv lock --check --offline` rc 0.

## The decisions taken today

| Decision | Rule as shipped | Where | Test that feeds the input |
|---|---|---|---|
| DEC-60 (TR39) | 1,905-entry TR39 18.0.0 subset vendored; two readings | `narrate/tr39_confusables.py` | `test_e8_repair4.py::test_the_lens4_lookalike_literals_are_rejected` |
| DEC-61 | `y_pred`-only table with more than one op -> H08 naming them | `gates.gate_h08_y_pred_operating_points` | `test_e8_repair4.py::test_a_y_pred_only_table_with_two_operating_points_halts_h08` |
| DEC-62 | Model name, version, op ids are `.customer-text` on T8 | `render/html.py`, `T8.html` | `::test_model_name_version_and_operating_point_are_customer_text_on_t8` |
| DEC-64 | Look-alike class stops at the TR39 subset for day 8 | nothing in `narrate/` | - |
| DEC-65 | C0/C1 in `criteria.yaml` and mapping text -> H08 naming the field | `io/declare.py`, `io/mapping.py` | `test_e8_repair5.py` (68) |
| DEC-66 ("refuse at ingest") | C0/C1 in a cell of a column `validate` reads (canonical names, `attr_` names matching `_IDENT`, the declared period column), after `strip()` -> **S02** naming the role, count, code point; exit 3, nothing written | `io/schema.py::check_control_characters` | `test_e8_repair6.py` (25 of 42) |
| DEC-66, halt code (repair 6's choice) | D1 §5 step 6 (lines 257-272) assigns no code to a malformed table value; S02 (`errors.SCHEMA_CODES`, "column value cannot be coerced to its declared type"; already in the egress enum) was used, so `errors.py` and `egress_schema.json` are untouched. Lens 7 FA N4: H02-H05 also read table values | docstring | needs 15 |
| Row 74 (orchestrator) | A list or mapping containing itself -> H08 `the value contains itself`; the walk is iterative and enters each object once; a shared, non-cyclic alias is accepted | `io/declare.py::self_reference_at` | `::test_a_self_referential_alias_halts_h08_naming_the_path` |
| DEC-67 ("withdraw DEC-51") | `honesty`/`stop`/`ok` paired with their `-bg` surface; D5's figure verbatim beside the recomputed one; the "do not match" sentence deleted. DEC-51 marked withdrawn (spec `dd76c68`) | `design/tokens.json` | `test_render_theme.py` pairs 1, 6, 8 |
| DEC-67, second half ("repair first") | The engine is repaired before it is pushed; this run does not push | - | - |
| Trailer | `Co-Authored-By: Claude Opus 5.5` (the harness's attribution line) on repair 6 and this commit; the orchestrator text named another model (lens 7 RG N8) | commits | - |

## CLI and output changes S4 must know (DEC-43)

The site builds from `a0c9abc` (`proofpack-site/.github/workflows/ci.yml` line 18, read now; site HEAD `95e9575`). Moving `ENGINE_REF` to `d36c767` brings E7's list (`2026-09-21_E.md`), lane A's A-P2 list (`2026-09-22_A.md`), and E8's, in full:

- **Flags.** `run --format` (default `json,html`; case-folded; `docx`, `pdf,json` -> exit 5, no directory); `run --templates` (default `T8`; `T1`/`T7` -> exit 0 and a typed line; `T9` -> exit 5).
- **Paths.** `<out>/T8.html` beside `run.json`, `ingest_report.json`, `pseudonyms.json` on licence `ok` / `grace` (now: `['T8.html', 'ingest_report.json', 'pseudonyms.json', 'run.json']`); otherwise `HTML not written: licence refused (no_file); run.json only (D1 section 7: after grace, JSON only)`, exit 4 (now, both shells).
- **Printed lines.** `Next step: open T8.html beside run.json; T1 and T7 land on E9 (docs: /docs/run)` and `Next step: proofpack licence install FILE, then run again for T8.html (docs: /docs/run)` (both now).
- **`run.json` keys.** `claims`, `claim_rejections` (required; `reason_code` enum of 44), `guidance_refs` `{id, label, draft, url}`, `criteria_results[].declaration_index`, `overall` non-null on clustered and `y_pred`-only tables, `overall.threshold_free.suppressed_reason`; `flow.excluded_missing_score` counts a blank `y_pred` without a score column; `flow.indeterminate` counts a declared indeterminate `y_pred`. E2e fixture now: `253943 bytes 20 keys 70 claims 0 rejections 3 refs`.
- **`--json-log` `run` payload:** `formats`, `templates`, `documents`, `document_notes`, `claims`, `claim_rejections`.
- **Halts added (each exit 3, nothing written):**
  - H02 on `y_pred`; H08 on op id `threshold_free` / `auroc` / `brier`; H08 on a table with no score column declaring more than one op (`detail {field: "operating_points", reason: "no_score_column", ...}`), on `run` and `compare`.
  - H08 on `criteria.yaml` (`run`, `compare`, `map --criteria`): U+0000-U+001F, U+007F, U+0080-U+009F in a key or value, tab and LF allowed under `justification`, `description`, `source`; message `declaration invalid at <path>: control character U+XXXX; remove it`. On `mapping.json` (`run`, `compare`, `map --yes`): the same in `role`, `notes`, `decided_by`, `timestamp`; `map --yes` leaves the file unchanged (now).
  - **New in repair 6: S02 on a table cell** (`run`, `compare`, either table): `column role '<role>' holds a control character (U+XXXX) in N row(s); remove it from the table`, detail `{"role", "reason": "control_character", "codepoint", "count"}`. The six demo CSVs in `proofpack-site/public/demo` hold none (repair 6; lens 7 RG hash-matched them at `95e9575`).
  - **New in repair 6: H08 on a self-referential alias**: `declaration invalid at <path>: the value contains itself (a YAML alias inside its own anchor); remove the alias`, detail `{"field", "reason": "self_reference"}`.
- **Halt codes and exit codes moved on existing inputs:**
  - `y_true` cell `0\x01` through `run`: `HALT H02 ...` at `941f8a7` -> `HALT S02: column role 'y_true' ...` (lens 7 RG N1; missing from repair 6's list).
  - `row_id` `r000000\x01` and `case_id` `c\x01`: exit 0 -> S02; `site` `S2\x01`: exit 2 with 1 raw control byte in `T8.html` -> S02 (lens 7 RG N1).
  - `score`, `indeterminate`, `age`, `dataset` cells with a control character, **through `io.schema.validate` called directly**: S02 with the type message -> S02 with the control-character message. **Through `run`, `score` and `age` give H07 at both commits** (lens 7 RG N1, N2; row 85); `sex` gives H07 (now).
  - `zz_extra: &a [*a]` at the root: exit 3 H08 `<root>: additionalProperties` at `2a9a6e2`, exit 5 at `ea2f743`, exit 3 H08 `self_reference` now; the same alias under `model:` or in `criteria[0]`: exit 5 -> exit 3.
  - From repair 5: DEC-11's E01 comes before the block-shape and authored-field H08s; the control-character H08 before the authored-field H08 and H09 (r5 note).
- **T8.** Headers carry `<span class="customer-text">name vX</span>`; `<title>` `T8 · ProofPack v… · run …`; op-id cells `customer-text`; e2e page now 40,375 bytes. On a `y_pred`-only or `lower_is_positive` run: no `CALIB_NA` claim, no `FDA_AIDSF_CALIBRATION` in `guidance_refs`, no calibration anchor (row 67).
- **Packaging.** `jinja2>=3.1` and MarkupSafe at run time; `design/tokens.json` ships as `proofpack/_schema/tokens.json` (`on` for honesty/stop/ok now the `-bg` token; no colour changed; the site reads it only in a `docs.css` comment). `proofpack-site/scripts/demo_sample.py` line 62 calls `run` with `--quiet` and no `--format` (read now), so the Pyodide bundle carries jinja2 and MarkupSafe or the call passes `--format json`.
- **Not moved by repair 6:** `git diff 941f8a7 HEAD -- src/proofpack/cli.py src/proofpack/run.py src/proofpack/errors.py pyproject.toml uv.lock schema/` is 0 lines (now). Since `7b2ca2a` the same paths differ in 9 files, 1,047 insertions, 87 deletions (now; the lists above).

## Re-run these

Measured now (23 September, 20:26-20:48 BST) in the main tree at `d36c767`, the two lens-7 notes untracked; `python -c "import proofpack;print(proofpack.__file__)"` printed `C:\Users\joshs\GPS\ProofPack\proofpack\src\proofpack\__init__.py` in each shell first. `PROOFPACK_TR39_FULL=C:/Users/joshs/GPS/ProofPack/workflows/data/confusables-18.0.0.txt` set for pytest. `S` = `C:/Users/joshs/AppData/Local/Temp/claude/C--Users-joshs-GPS-ProofPack/a743e7e5-3be8-4d70-9ed0-681f5784a106/scratchpad`, `E` = `$S/e2e_shell`, `H` = `$S/handoff-E8-r6`; the Git Bash commands are `$H/rerun.sh`, the PowerShell ones `$H/rerun.ps1`. Both day-8 sweeps ran in the background during the suites.

| Command | Git Bash | PowerShell 5.1 |
|---|---|---|
| `python -m pytest -q -p no:cacheprovider` | `1256 passed, 1 skipped, 1 xfailed in 96.52s` | `1256 passed, 1 skipped, 1 xfailed in 93.41s` |
| `-m day8` / `day7` / `day6` / `day5` / `ap2` | `468` / `139` / `285` / `54` / `88 passed` | the same five |
| `-m day4` / `day3` / `day2` / `day1` | `182` / `29 passed, 1 xfailed` / `40` / `59 passed, 1 skipped` | the same four |
| `python -m ruff check .` / `python -m ruff format --check .` | `All checks passed!` / `169 files already formatted` (before this note) | the same |
| `python -m proofpack.cli doctor --offline` | 17 `[ok` lines, `All essential checks passed.`, exit 0 | the same |
| `python scripts/tr39_subset.py ../workflows/data/confusables-18.0.0.txt --check` | `vendored subset matches`, exit 0 | the same |
| `python scripts/mutation_sweep.py --list` | 274 lines: 34 day5, 112 day6, 25 day7, 87 day8, 16 ap2 | 274 lines |
| `python scripts/mutation_sweep.py --marker day8` | `103 planted, 103 killed, 0 survived; 1267 s`, exit 0 (87 day-8 + 16 `ap2`; in the sweep's own temp copy, concurrently with the PowerShell sweep and both suites); `git status --short` afterwards: the three handoff notes only | `103 planted, 103 killed, 0 survived; 1265 s`, exit 0 |
| `PROOFPACK_HOME=<empty> python -m proofpack.cli run --input $E/test.csv --criteria $E/criteria.yaml --out <out> --offline`; then `python $S/handoff-E8-r4w/runjson.py <out>/run.json` | `analysed 400 of 400 rows; criteria rows: 2 met, 2 not met, 5 not assessable` / `licence refused (no_file); watermark: LICENCE EXPIRED - not for submission` / ... / `Next step: proofpack licence install FILE, then run again for T8.html (docs: /docs/run)`, `exit=4`; `253943 bytes 20 keys 70 claims 0 rejections 3 refs [0, 1, 2, 2, 2, 3, 4, 5, None] CRLF 0 BOM False` | `exit=4`, the same `runjson.py` line |
| the same with no mapping beside the input | `HALT H07: run proofpack map first: ...` / `No document was written.`, `exit=3 dir=no` | `exit=3 dir=False` |
| `python -m proofpack.cli licence show` (empty home) | `licence: none found` / `status: refused (no_file)`, `exit=4` | the same, `exit=4` |
| `python $S/handoff-E8/html_run.py $E <dir>` (test-only licence) | `exit 0 ['T8.html', 'ingest_report.json', 'pseudonyms.json', 'run.json']` / `T8.html bytes 40375 CRLF False BOM False` | identical |
| `python $S/repair-E8-r4/handoff/repros5.py` | `C_zero '99/101 (98.0%) [93.1, 99.5]'`, `C_br '29/101 (28.7%) [20.8, 38.2]'`, `C_dot '85/101 (84.2%) [75.8, 90.0]'` / `B2: HaltError H08: ... (op_lo, op_hi) ...` / `B3: no_score_column engine CALIB_NA claims 0 hand-built: False template_mismatch` / `B4:` five `free_text_verdict_word` and `None` | identical |
| `python $S/handoff-E8-r4w/repros6.py $E` | `B1: [None, None, None, None, None]` / `B2: [None, None, None, 'free_text_verdict_word', 'free_text_verdict_word']` / `B3: [None, None, None, 'free_text_certification_word']` / `HALT H08: declaration invalid at model/name: control character U+0000; remove it` (then `FileNotFoundError` on the absent `T8.html`, as in r5) | identical to the `HALT` line |
| `LENS_WT=$PWD python $S/lens-E8-r6-fresh-attack/att/p3_fair.py` (old row 73) | `3 {} b'HALT S02: column role \'race\' holds a control character (U+0001) in 400 row(s); remove it from the table ...'` | identical |
| `... att/p2_star.py` (old row 73, `level: "*"`) | `attr_colour` and `race`: `soh` rc 3 U+0001 400 rows, `c1` rc 3 U+0085 400, `nul` rc 3 U+0000 200, each `{}`; `html` rc 0, `<script>x</script>` 0 raw, `{{ 7*7 }}` literal once | identical (all eight lines) |
| `... att/p7_site.py` | `site 3 {} ... (U+001B) in 266 row(s)` / `device 3 {} ... (U+0000) in 200 row(s)` / `sex 3 {} HALT H07: ...` (row 85) | identical |
| `python $S/handoff-E8-r5/alias.py $E <dir>` (old row 74) | `exit 3` / `stderr last line:   No document was written.` / `pack exists: False` | identical |
| `WT=$PWD python $S/repair-E8-r5/offline_and_imports.py` | `run --offline --format json,html: rc 0 socket calls 0` | identical |
| `WT=$PWD python $S/repair-E8-r5/paths.py` | `map --criteria (3, [... model/name ... U+0000 ...])` / `compare (3, [...]) dir False` / `map --yes (note) (3, [... roles/0/notes/0 ... U+0001 ...]) file unchanged True` | identical |
| `python $H/imports.py` (`jinja2`, `markupsafe`, `scipy`, `cryptography`, `statsmodels`, `sklearn` set to `None`; import `proofpack`, `.stats`, `.io.declare`, `.io.mapping`, `.io.schema`, `.narrate.checker`, `.gates`) | `imports ok; loaded: []` | identical |
| `LENS_WT=<tree, forward slashes> python $S/lens-E8-r7-fresh-attack/att/k1_checker.py` (row 87) | `engine claims rejected: 0` / `free_text accepted: ['unbiassed (British)', 'biassed (British)', 'meet (base form)', 'meeting', 'satisfies', 'succeeds', 'approves', 'certifies', 'endorses', 'authorised', 'authorized', 'granted (De Novo)', 'clears', 'sect entity', 'guidance plural']` | identical |
| E7 verdict grep: `-k "status_words_appear_only_under_criteria_results or verdict_word"` | `5 passed, 1253 deselected` | identical |
| E6: `tests/test_calibration.py::test_the_assembled_document_validates_against_the_output_schema` | `1 passed` | identical |
| `git diff 941f8a7 HEAD --` the twelve paths under "What landed" / `git diff 7b2ca2a HEAD -- src/proofpack/stats/` | `0` / `0` lines | `0` / `0` |

To repeat a pre-fix figure, add a worktree and force `PYTHONPATH`; the editable `.pth` otherwise imports the main tree. A lens-7 script's harness asserts `proofpack.__file__` starts with `LENS_WT`, so in PowerShell pass it with forward slashes.

## Tomorrow needs

1. **Josh's reading of row 87 before E9 writes any `free_text`** (needs 17): the certification verbs (`approves`, `certifies`, `endorses`, `clears`, `authorised`, `granted`) and the British `unbiassed` / `biassed` are accepted today.
2. **E9 per `spec/briefs/day9_E.md`**: about 40 sentence templates keyed to claims (D4 §8), T1's 15 sections, T7 from the run, `/sample-pack`, model card v1.1, golden snapshots, every FDA-draft anchor labelled, forbidden grep 0, render under 60 s on 100k rows, D5 §3.5 furniture and figures F2-F5. E9 inherits:
   - DEC-64: rows 54-56, 78, 87 with 19, 20, 44, 53, 70; the 70 engine claims of the e2e run carry `free_text: null`;
   - level labels in T1's subgroup tables: C0/C1 in columns `validate` reads halt S02 now; row 81's U+2028 and U+200B reach `T8.html` raw today and would reach each slot that prints a level;
   - slot filling by pointer facet (rows 51, 52);
   - T1 printing `calibration: null` beside its reason (DEC-36), the IPA tier annotation (DEC-35), the 0.897 cell with its tier (DEC-34), a sentence or silence for rows 67 and 71, Clopper-Pearson if D4 §1.2 stands (row 49);
   - DEC-48's `NO LICENCE - not for submission` (row 27); section 7 without status words when no criteria exist (row 23);
   - DEC-55 `gate_h05` under `clustering: case_id` with its own DEC-12(i) lens; item 11; DEC-47 and DEC-49 with lane A; row 8 with the anchor work.
3. **A short sentence-and-pin repair** (not a statistical gate): row 88's six sentences; row 89's `!!omap` literals and a period-column test with their sweep mutants; rows 61-65, 75.
4. **S4 (day 10)** moves the pin with the list above; the orchestrator pushes the engine first.

## Needs from Josh

E7 needs 1-7 were decided on 23 September (DEC-44 to DEC-50); none carries. E8 needs, numbered as before:

1. **D1 §5.6 H02 row and §2 `y_pred` row** should read "y_true or y_pred" and say what a blank `y_pred` does. Open.
2. **What `free_text` covers beyond TR39**: number words, Roman numerals, `&sect;`, derived words (rows 19, 20, 44, 53). Open.
3. **A `y_pred` at a declared indeterminate value beside a valid score** (row 6). Open.
4. **Draft label spelling** (row 30): amend D4 §1.1 to "draft guidance (January 2025), not for implementation"? Open.
5. **Declarations echo** (row 34): sorted YAML (as built) or the customer's file verbatim? Open.
8. **Unicode licence text** (row 72): the three-line notice, or the full Unicode License v3 text beside the subset (one download, your approval in chat)? Open.

6, 7 (DEC-61, 62), 9-11 (DEC-65), 12 (DEC-66), 13 and 14 (DEC-67): decided and built.

New today:

15. **DEC-66's halt code.** D1 §5 step 6 (lines 257-272) assigns no code to a malformed table value; repair 6 used S02. Keep S02, or amend D1 with a row? Recommendation: keep S02 and add a D1 line naming it (no egress schema change).
16. **Rows 84 and 86**: `map` prints cell values raw to the terminal; a shared alias chain expands `run.json` to 155,418,186 bytes at 20 levels. Rules for E9 or later?
17. **Row 87 (lens 7 FA N1)**: keep it record-and-carry until E9, or regrade it a blocker? Recommendation: add the certification verbs and the `-ss-` British spellings to the word lists in E9's first repair, with literal tests, before any `free_text` is written.
18. **DEC-66 scope**: the columns `validate` reads (as built) or every column `ingest` profiles, including `notes`, `rater_` and unmapped ones (row 91)?
19. **Row 90**: `load_yaml` catching `RecursionError` and the JSON `TypeError` as H08 is a new typed halt. Yes or record?

## Open questions

1. `CALIB_NA` for `no_score_column` / `score_not_positive_class_probability`: a template on T1, or silence plus the printed reason; and should `FDA_AIDSF_CALIBRATION` stay in `guidance_refs` on those runs (rows 67, 71)?
2. Should the full `confusables.txt` (763,128 bytes) be committed as a test fixture so the regeneration test runs in CI (row 66)?
3. Is `json,html` still the right `--format` default while T1 / T7 are unbuilt and the demo passes no `--format`?
4. Row 85: should H07 name a control character when one is the cause, or stay as it is?
5. DEC-65's multi-line fields are named from D1 §6, a section about egress; should D1 carry a field list for them (row 69)?

## Sentences refused

Aggregated from the build note (the orchestrator's; no builder note), repair notes 1-6, the fourteen lens notes and this note.

- "The checker binds every claim to its Number / template." Written instead: 42,849 swaps, 0 accepted; 3,850 relabels, 4 accepted; 8,988 mutations, 11 accepted.
- "`free_text` rejects look-alike spellings" / "the look-alike class is closed" / "the checker rejects every inflection." Written instead: 52,694 substitutions, 0 accepted; rows 54-56, 70, 78, 87 (15 of 27 accepted).
- "The new readings refuse no English." Written instead: 3,257 sentences and 36,038 word forms, 0 newly rejected.
- "The criteria table prints the right Number for every operating-point id." Written instead: `0`, `[0]`, `t0.5`, `a[0`, `a.b`, `x]y`.
- "DEC-61 closes FA-B2" / "the overall block is correct." Written instead: the tables fed and the deviations.
- "The renderer escapes every injection" / "control characters can no longer reach T8 / a document" / "DEC-66 covers every cell" / "DEC-66 refuses every control character in the table." Written instead: the 19 columns and literals fed, the six lens inputs, and rows 81, 84, 85, 91.
- "The ingest check leaves every ordinary table unaffected." Written instead: 41 files, 82,451 cells, 0 hits; the cohort's `run.json` byte-identical.
- "The DEC-65 gate refuses every control character in `criteria.yaml`" / "every string field is covered" / "the mapping text is refused everywhere." Written instead: 43 fields, 14 literals, four mapping fields, rows 79, 90.
- "The DEC-65 walk is now safe on any YAML" / "the self-reference class is closed" / "alias chains are fast now." Written instead: the positions and timings fed, and rows 86, 89, 90.
- "S02 is the code D1 assigns." Written instead: D1 assigns none (lines 257-272); S02 and why.
- "D5's figures were always right" (general). Written instead: three recomputed figures against three `-bg` surfaces.
- "The repair's mutants pin the gate" / "the sweep kills every non-equivalent mutant." Refused: rows 47, 48, 61-65, 75, 89.
- "Repair 5 / repair 6 introduced no regression." Not written (row 74 was one).
- "The S4 list is complete." Refused (lens-5 RG N1, lens-6 FA N3, RG N5, lens-7 RG N1); written instead: the diffs that are 0 lines and each moved halt measured.
- "The new tests would have caught the lens findings." Written instead: the pre-fix counts (42; 26; 22; 20; 64; 42 failed) and the guards named.
- Older, still refused: "the declared value prints as the manufacturer wrote it", "the checker never raises", "a right-to-left override cannot reorder the page", "the wheel test proves the packaging", "H02 catches every mislabelled y_pred".
- In this note: "E8 is done", "the engine is ready to push" and "lens 7 found nothing" were not written; "PASS" is written only beside lens 7 FA's own grading remark on N1.
