# A-P2 lens 2 (regression and record) - build day 8, lane A, repair round 1 - 23 September 2026

**Verdict: PASS.** No blocker. Engine `a-p2-egress` at `0cf9ba5` (repair of `4d61b6e`, branch from `7b2ca2a`); site `main` at `2b5c7de` (repair of `b8242c1`). Cold lens, no prior context. Every figure below was measured in this session in detached worktrees under `scratchpad/lens-AP2-r2-regression/` (`eng-0cf9ba5`, `eng-4d61b6e`, `eng-7b2ca2a`, `eng-a0c9abc`; `site-2b5c7de`, `site-b8242c1` with `node_modules` junctioned and `PROOFPACK_ENGINE_DIR` at `eng-a0c9abc`), with `PYTHONPATH` forced and proved in both shells (`import proofpack` printed `...\lens-AP2-r2-regression\eng-0cf9ba5\src\proofpack\__init__.py` in Git Bash and in PowerShell 5.1; unforced it printed lane E's main tree). The engine's main tree was not touched (its two untracked files are lane E's lens notes). Nothing was committed. The worktrees and junctions were removed at the end.

Each of lens 1's four blockers is closed at the sha and stays closed under the re-run the lens named; every lens-1 "could not break" property re-measured unchanged; every new test fails pre-fix on the assertion the note quotes; no test was deleted, weakened, skipped or marked; every figure in the note re-measures except two that are the environment, not the code (below).

## Blockers

None.

## Lens-1 blockers, re-checked at the sha (each reproduces its fix, none reproduces its defect)

| Lens-1 id | Re-run | Measured at `0cf9ba5` / `2b5c7de` |
|---|---|---|
| FA-B1 (`/legal/sub-processors` "since 22 September 2026") | `grep -rl "since 22 September 2026" dist` after a build | no file. `dist/legal/sub-processors/index.html` carries `runner telemetry records, table <code>runs</code>, once its migration <code>db/migrations/2026-09-22_runs.sql</code> has been applied to the project: as of 2026-09-23 it had not been applied, the table did not exist and no telemetry record was held; this row is dated when it is` |
| FA-B2 / RG-B1 (`/trust` dated 2026-09-21, unit suite red) | `npm run test:unit` | `tests 658 / pass 657 / fail 0 / skipped 1` (with `GPS_AUTOMATION_DIR` set - see environment note); `grep -o "Last updated 2026-09-23\." dist/trust/index.html \| wc -l` = 1 |
| RG-B2 / FA-N7 / RG-N4 (`/trust` "its CI job runs", "every socket call") | `grep -o "[^.]*unshare[^.]*\." dist/trust/index.html`; `grep -c "every socket call is made to raise"` | `A CI job that runs one whole run inside a network namespace with no network (unshare -rn), with and without the flag, is written on the engine branch and had not run anywhere as of 23 September 2026; ...` (twice: the FAQ and its JSON-LD); count 0 |
| RG-B3 (test id "no_digit_of_its_estimates") | the new scan test copied under another name into the `4d61b6e` worktree, so it reads the old `tests/test_egress.py` | `AssertionError: assert ['test_egress..._bytes(f19):'] == []` - 1 failed; at `0cf9ba5` it passes |

## Non-blocking findings

- **N1 (figure, environment)** `python -m ruff format --check .` at `0cf9ba5` prints `123 files already formatted`, not the note's `121`. `ruff format --check -v` lists the two files the tree has that `4d61b6e` (121) has not: `handoffs/2026-09-22_A_ap2_lens1_fresh-attack.md` and `handoffs/2026-09-22_A_ap2_lens1_regression.md`, which commit `0cf9ba5` added after the note's measurement. No `.py` differs between the two shas (`git diff --name-status 4d61b6e 0cf9ba5` lists no `.py`). The figure the note gives is the one before its own last commit.
- **N2 (figure, environment)** `npm run test:unit` in a detached site worktree prints `658 / 656 / 0 / skipped 2`; the second skip is `the vendored copy equals the branch, table for table, where the branch is reachable # gps-automation branch a3a-lifecycle not reachable at <worktree>/../gps-automation`, a pre-existing test that looks beside the checkout. With `GPS_AUTOMATION_DIR=C:/Users/joshs/GPS/ProofPack/gps-automation` it prints the note's `657 / 1`. The builder measured in the main tree, where `../gps-automation` resolves. Not a defect; recorded so the next lens does not chase it.
- **N3 (sentence, shipped in a comment)** The note's first refused sentence is "the transport makes one attempt (kept only as 'one HTTP request')". Two source comments still carry it and one reaches the built page: `src/content/legal/dpa.md:120` `... telemetry.py is the one sender, urllib imported inside the transport function, one attempt, 5 s, W16 on failure` (rendered as an HTML comment in `dist/legal/dpa/index.html`: `grep -rl "one attempt" dist` lists that file alone) and `src/content/legal/privacy.md:82` `the sender, one urllib attempt`. Both comments also say `repaired 2026-09-23: a 3xx answer is not followed`, and after the fix the transport does make one attempt (section C below: `main_got=1`, `other_host_got=[]` for every 3xx), so the words are true; they contradict the note's own account of what was kept. One edit each.
- **N4 (stale date, true)** `src/data/egress_channels.json` `runner-telemetry.state_note` was re-dated to `0cf9ba5` in this commit and still ends `no record has been received as of 22 September 2026` (rendered once on `dist/trust/index.html`). True on 23 September too; the row's other dates moved and this one did not.
- **N5 (sentence, understated)** `schema/egress_schema.json` `$defs/level` description now reads "a longer level is pseudonymised (src/proofpack/egress/pseudonymise.py)". Measured: `build_map({}, levels={"sex": {"A"*17}})` → `Level A` (longer, pseudonymised); `{"A"*16}` → `{}` (passes); `{"Jane Doe 1961"}` (13 characters, a space) → `Level A`. The rule is the `TOKEN` pattern `^[A-Za-z0-9][A-Za-z0-9_.+-]{0,15}$`, so a short level with a space, a slash or a non-ASCII letter is pseudonymised as well; "longer" names one of the ways a level fails the pattern. This description is published on `/trust` at the pin move.
- **N6 (design divergence the note does not restate)** D2 §5.1 and D3 §4 name the telemetry fields `manifest_hash`, `duration_ms` and an exit code; the engine's ten keys are `manifest_sha256`, `duration_s` and no exit code, and `db/migrations/2026-09-22_runs.sql` has no exit-code column (its comment says why the names differ; the build commit `4cad727` records the exit-code cut). The repair note carries neither. Record for the day D2 §5.1 is brought to the schema or the schema to it.
- **N7 (gate reads the author's word - lens-1 RG-N9 / FA-N11 reproduced, carried by the note)** With `runner-telemetry.state` edited to `not_built` in the worktree and no rebuild: `FAIL row "runner-telemetry": state is "live", manifest says "not_built"`, exit 1. After `npm run build`: `egress check: 0 error(s)`, exit 0, and `dist/trust/index.html` renders `Not built yet` 3 times (2 before the edit). `grep -nE "functions/|telemetry\.js|existsSync" scripts/check-egress.mjs scripts/lib/egress-check.mjs` finds nothing: the gate compares the page with the manifest and never opens `functions/api/telemetry.js`. Restored and rebuilt afterwards.
- **N8 (a sentence the task text carries, measured false, and the note does not claim it)** "removing any one of the three socket patches makes a test fail": with the code at `0cf9ba5` and the fixture patching only two of `socket`, `getaddrinfo`, `create_connection` (each in turn), `tests/test_offline.py` is `8 passed` all three times - the patches are detectors, and correct code reaches none of them. What does make a test fail is removing a short-circuit in the code (below). With the `--offline` short-circuit removed *and* one patch removed, `test_run_offline_opens_no_socket_and_calls_no_transport` still fails all three times (`['https://pro...pi/telemetry'] == []`: the recording transport is what catches it there).

## Suites and figures, re-measured

Engine, `eng-0cf9ba5`, `PYTHONPATH` forced (Git Bash | PowerShell 5.1 | note):

| Command | Git Bash | PowerShell 5.1 | Note |
|---|---|---|---|
| `python -m pytest -q -p no:cacheprovider` | `873 passed, 1 skipped, 1 xfailed in 80.46s` | not repeated | 873 / 1 / 1 |
| `-m ap2` | `85 passed, 790 deselected in 13.52s` | `85 passed, 790 deselected in 15.09s` | 85 |
| `-m day8` | `85 passed, 790 deselected` | not repeated | 85 |
| `-m day7` | `139 passed, 736 deselected in 7.21s` | `139 passed, 736 deselected in 7.18s` | 139 / 736 |
| `python -m ruff check .` | `All checks passed!` | `All checks passed!` | same |
| `python -m ruff format --check .` | `123 files already formatted` | not repeated | **121** (N1) |
| `python -m proofpack.cli doctor --offline` | `All essential checks passed.`, exit 0 | same, exit 0 | same |
| `scripts/mutation_sweep.py --list \| grep -c " ap2 "` | 16 | not repeated | 16 |
| `scripts/mutation_sweep.py --marker ap2` | `16 planted, 16 killed, 0 survived; 76 s` (the sweep forces `PYTHONPATH` to its own copy's `src`, `scripts/mutation_sweep.py:1654`) | not repeated | 16 planted, 16 killed, 0 survived; 67 s |
| F19 smoke, fresh empty `PROOFPACK_HOME`, `scratchpad/smoke/test.csv` + `criteria.yaml`, `--offline` | the six lines of lens-1's `smoke_out/bash_out.txt` verbatim (run_id and paths aside), `exit=4`, `ingest_report.json pseudonyms.json run.json` | identical, exit 4, same three files | same |
| the same with `--json-log` | `{'exit_code': 4, 'licence_status': 'refused', 'telemetry': {'skipped': '--offline'}, 'warnings': []}` | identical (read with `utf-8-sig`) | same |
| the skip / the xfail | `tests/test_doctor_cli.py:57` write access cannot be revoked; F13 pending the R capture | | same |

The 11 new tests (873 - 862 = 85 - 74): the scan, the whitelist newline, the date-shaped level, the RG-N5 socket test, five redirect codes, the CLI 302, the send newline.

Site, `site-2b5c7de`, Git Bash, `PROOFPACK_ENGINE_DIR` at `eng-a0c9abc` (DEC-43):

| Command | Measured | Note |
|---|---|---|
| `check:bytes` | `276 text file(s)`, OK | same |
| `build` | wheel `152257 bytes, sha256 9b7d6986cd96…` from `a0c9abc`; `30 page(s) built` | same |
| `check:links` | `30 page(s), 1242 internal link(s), 1 distinct external URL(s)`, OK | same |
| `check:schema` | OK | same |
| `check:pricing` | `30 page(s), 37 currency figure(s), 0 error(s)` | same |
| `check:egress` | `vendored schema sha256_lf=c98b85aec4a1de89… canonical=1c914910cae38b8c…`; `engine copy compared: ../eng-a0c9abc/...`; `claim scan: 36 built file(s), 523 attribute value(s), 2 sent document(s)`; `0 error(s)` | same |
| `check:validation` | `40 check(s): 0 pass, 0 fail, 40 pending, 4 [unverified]`, OK | same |
| `check:seo` | `0 error(s)`, OK (3 WARN lines) | 0 errors |
| `check:keys` | `key_id=pp-2026-09 status=live`, 0 errors | same |
| `check:legal` | red by design: `2 of 2 gated page(s) failing` | same |
| `test:unit` | `658 / 656 / 0 / skipped 2` in the worktree; `658 / 657 / 0 / 1` with `GPS_AUTOMATION_DIR` (N2) | 658 / 657 / 0 / 1 |
| `test:e2e` | `9 passed (36.6s)` | 9 passed |
| `node --test tests/day8-telemetry.test.mjs` | `20 / 20 / 0 skipped` with the engine dir at `0cf9ba5`; `20 / 19 / 1 skipped` at `a0c9abc` | same |

## Fails pre-build (the new tests against the pre-fix trees)

Engine: the four test files from `0cf9ba5` copied over `eng-4d61b6e/tests/`, `PYTHONPATH=eng-4d61b6e/src` (proved), the twelve new or renamed ids selected: **6 failed, 6 passed**. The six failures and their first assertion lines, verbatim:

```
test_egress.py::test_whitelist_refuses_a_trailing_newline_that_re_search_accepts     E  Failed: DID NOT RAISE WhitelistError
test_telemetry.py::test_a_redirect_answer_is_not_followed_and_the_location_host_receives_nothing[301]  E  AssertionError: assert SendResult(se...son_code=None) == SendResult(se...='http_error')
...[302]  the same line
...[303]  the same line
test_telemetry.py::test_a_302_through_the_cli_prints_w16_and_the_location_host_receives_nothing  E  AssertionError: assert [] == ['[W16] telem...re unchanged']
test_telemetry.py::test_send_refuses_a_trailing_newline_in_licence_id_and_platform  E  assert SendResult(se...son_code=None) == SendResult(se...load_invalid')
```

The six that pass at `4d61b6e` are the six the note says pass there: `[307]`, `[308]` (urllib did not follow those), `test_a_date_shaped_level_of_the_sex_column_passes_the_token_rule_verbatim` (a recorded fact), `test_telemetry_false_with_the_real_transport_and_sockets_refused_opens_no_socket` (the property held; the sweep's `ap2_telemetry_false_ignored` is its killer, and removing the short-circuit fails it - below), the renamed n = 7 test (`== 10` holds because suppression did not change), and the scan test once the new `test_egress.py` is in place (it fails against the old file, RG-B3 above). The `4d61b6e` worktree's `tests/` was restored with `git checkout -- tests`.

Site: `tests/day8-ap2-repair1.test.mjs` and the modified `tests/day7-unlensed-repair2.test.mjs` copied into the built `site-b8242c1` worktree. The new file: `tests 5 / pass 0 / fail 5`, on exactly the five lines the note quotes: `expected to not match ... /since 22 September 2026/`; `did not match ... /db\/migrations\/2026-09-22_runs\.sql/`; `expected to not match ... /every socket call is made to raise/`; `did not match ... /a redirect answer \(3xx\) not followed/`; `did not match ... /for 24 months \(the purge that enforces the 24 months is not built yet\)/`. The modified file: `7 / 5 / fail 2` - the git-date test (`'src/pages/trust/index.astro: declares 2026-09-21, last changed 2026-09-23 (src/pages/trust/index.astro, src/data/egress_channels.json)'`) and the moved dist pin (`expected: /Last updated 2026-09-23\./` against a footer reading 2026-09-21). Restored.

## Nothing weakened

`git diff --name-status` on `tests/`: engine repair `M conftest.py, M test_egress.py, M test_offline.py, M test_telemetry.py`, branch-wide `M conftest.py, M test_doctor_cli.py, A test_egress.py, A test_offline.py, A test_telemetry.py`; site repair `M day7-unlensed-repair2, A day8-ap2-repair1`, branch-wide `M day4-repair4, M day7-unlensed-repair1, M day7-unlensed-repair2, A day8-ap2-repair1, A day8-telemetry`. No deletion in either. Added lines grepped for `skip|xfail|.only|todo`: engine repair - none (the one hit is the assertion string `"telemetry skipped (egress.telemetry: false); nothing was sent"`); site repair - none. Branch-wide the two build-day skips lens 1 recorded remain the only ones (`test_offline.py` `pytest.skip("no .github/workflows/ci.yml here (the mutation sweep's copy)")`; `day8-telemetry.test.mjs` `t.skip(\`no engine checkout at ...\`)`). No `pytestmark` or `pytest.mark` line removed. The renamed n = 7 test tightened `>= 6` to `== 10`.

## What I tried to break and could not (with the bytes and figures)

- **The redirect fix, through the lens-1 script.** `attack_socket.py` (lens 1's, `LENS_WT=eng-0cf9ba5`, `PYTHONPATH` forced) section C, a fresh `PROOFPACK_HOME` per case, the real `urllib_transport` pointed at two loopback listeners: `301, 302, 303, 307, 308 → rc=0`, `[W16] telemetry not sent (http_error, http NNN); run.json and the exit code are unchanged`, `other_host_got=[]`, `main_got=1`, `run.json(normalised)==offline:True`; `204 → telemetry sent (http 204)`; `500`, `413 → http_error`; hang → `timeout` at 5.06 s (`send()` alone 5.03 s); closed port → `connection_refused` (2.11 s); self-signed TLS → `transport_error`, 0 bodies received; `run.json` deleted inside the transport → rc 0. Section A: `run`, `compare`, `map --yes`, `doctor`, `licence show`, `--offline licence verify`, `--offline licence install` (rc 5 `SameFileError`, pre-existing FA-N9), `licence verify FILE --offline` (argparse 2, pre-existing) → `sockets=[]` in every case under a trap on `socket.socket.__init__`, `getaddrinfo`, `create_connection`. Section B: `egress.telemetry: false`, no `--offline`, default transport → `sockets=[]`, the skip line. Exit 0.
- **The whitelist fix, through the lens-1 script.** `attack_payload.py`: `licence_id "lic_abc\n"` and `platform "win-amd64-cp314\n"` → `whitelist refuses (the string does not match the schema's pattern)` with `jsonschema errors: []` (the trap the note names), `send() → payload_invalid`; `manifest_sha256`, `timestamp`, `run_id` with a newline → refused by length as before. `min_n 9, 0, -1, "10", 10.5, true` and `telemetry "false", 0, null` → `HALT H08`, rc 3, 0 transport calls, no pack; `min_n 10.0` → rc 0 and sent while `thresholds_from_declarations` raises H08 (carried FA-N4, reproduced); unknown keys under `egress` / `suppression` → accepted, `Thresholds(10, 5, 5)` (carried FA-N5, reproduced); H08 and H06 → rc 3, 0 calls. Exit 0.
- **The two code short-circuits, each removed in the `0cf9ba5` worktree and restored.** `if offline or not telemetry_enabled(...)` → `if not telemetry_enabled(...)`: `test_run_offline_opens_no_socket_and_calls_no_transport` fails, `Left contains one more item: 'https://proofpack.globalphoenix.co.uk/api/telemetry'`. → `if offline:`: `test_telemetry_false_with_the_real_transport_and_sockets_refused_opens_no_socket` fails, `assert (['create_connection'] == []`. `git status --short` empty afterwards.
- **The n = 7 cell's ten bounds, counted independently.** A scratch test on the `f19` fixture (deleted afterwards) listed every `ci_lo` / `ci_hi` float on the `St Mary's` row (`n 7, events 2`): 12 bounds on `op1` - `accuracy 0.4868721707256812 / 0.9743203756552565`, `npv 0.4364971778135299 / 0.9699466302516933`, `ppv 0.2065493143772375 / 1.0`, `selection_rate 0.025679624344743558 / 0.5131278292743189`, `sensitivity 0.09453120573423074 / 0.9054687942657692`, `specificity 0.5655175352168254 / 1.0` - of which 10 have `len(repr(v)) >= 10`; the two `1.0` are the ones the length rule excludes. The test asserts `len(locals_) == 10` and each of the ten absent from the aggregates bytes and from `rec.calls[0]["body"]`.
- **The F19 needles test reads the literal inputs the note names.** `tests/test_egress.py:62-65` `ST_MARYS = "St Mary's"`, `KONIGSBERG = "Königsberg"`, `ROYAL_FREE = "Royal Free"`; `test_f19_the_forbidden_bytes_are_absent_from_both_payloads_and_the_captured_bytes` asserts each needle `not in tele_text` and `not in agg_text`, each site label absent from both, every ROC threshold (`len(thresholds) >= 300`) absent, `'"roc"'` and `'"declarations"'` absent, and `doc["manifest"]["started"]` absent from `agg_text`.
- **The schema.** An independent walk of `schema/egress_schema.json` at `0cf9ba5`: 12 object nodes; every one with a `type` of object carries `additionalProperties: false`; the six without it are the `if` / `then` narrowers of `number`, `two_by_two_cell` and `calibration_bin`, which have no `type` and sit under closed parents; 14 string nodes, none unconstrained, every `pattern` with a `maxLength`; `$defs/telemetry` has exactly the ten keys and requires all ten.
- **The vendored Function copy.** `functions/_lib/telemetry-schema.mjs` moved from `ENGINE_SHA 4d61b6e…` / `1541b3b8…` to `0cf9ba5fb603c56b819e49a963dac6164af5b99c` / `f323709c97c59f77104b1c4e22f4a9e05806e9ade5820e03612b70e12e0d95bc`; the only other line changed is the `manifest_sha256` description. `PROOFPACK_ENGINE_REQUIRED=1 npm run check:egress` with the engine at `a0c9abc` → `engine copy compared`, 0 errors; at `0cf9ba5` → `FAIL the vendored egress schema has DRIFTED ... engine canonical sha256 f323709c97c59f77104b1c4e22f4a9e05806e9ade5820e03612b70e12e0d95bc, vendored 1c914910cae38b8c62040020342af7600db74f801f14f136619cd965a6e4225a`, exit 1. So at the branch merge nothing on the site changes (the pin stays `a0c9abc`); at the pin move the drift job is red until `src/data/egress_schema.json` and `egress_schema.provenance.json` are re-made, as the note says. The site's `canonicalHash` (sorted keys, `JSON.stringify`) is not the engine's `manifest.canonical_json` (that recipe gives `ad7079da…` for the same file); the gate compares its own recipe on both sides, so this is a note, not a defect.
- **The migration.** `db/migrations/2026-09-22_runs.sql`: four statements, `create table if not exists` and three `create ... index if not exists`; no statement without a guard; the `create table ... runs (...)` block is byte-equal to `db/schema.sql`'s (`diff` after CRLF strip: identical) and the three index lines equal `schema.sql:199-201`.
- **The Function.** Unchanged in the repair (`git diff --stat b8242c1 2b5c7de` lists `telemetry-schema.mjs`, not `telemetry.mjs`), so lens 1's endpoint measurements stand. Read again: the only header reads are `request.headers.get('content-type')` (line 110) and `('content-length')` (line 114); `tests/day8-telemetry.test.mjs:363` asserts `doesNotMatch(code, /request\.headers\.get\((?!'content-(type|length)')/)`; the row is `TELEMETRY_DEF.required` in schema order (`COLUMNS`), `received_at` is the column default; no `cookie` in the code; `set-cookie` asserted `null` on every response (line 244). The test uses `freshEnv()` from `tests/harness.mjs` (line 64) as `day2-checkout` (lines 55, 78) does.
- **The YAML.** `.github/workflows/ci.yml` at `0cf9ba5` parses (`yaml.safe_load`): jobs `test, offline-namespace, audit, wheel, import-without-scipy`; `offline-namespace` on `ubuntu-latest`, 5 steps; the `run` step tries `unshare -rn true`, falls back to `sudo unshare -n`, exits 1 if `getaddrinfo('proofpack.globalphoenix.co.uk', 443)` still resolves inside the namespace, then runs `scripts/ci_namespace_run.py --offline` and `--online` and greps the four lines the note names. The comment now reads `(expected, not measured: this job had not run anywhere as of 23 September 2026 ...)`.
- **The conftest guard (FA-N6).** On a `7b2ca2a` worktree (no `src/proofpack/egress`): the `4d61b6e` conftest → `ModuleNotFoundError: No module named 'proofpack.egress'` at conftest import, nothing collected; the `0cf9ba5` conftest with the three ap2 files present → `790 tests collected, 3 errors` (`test_egress.py`, `test_offline.py`, `test_telemetry.py`, at their own imports). Restored.
- **The branch's touch outside `egress/`.** `git diff --name-status 7b2ca2a 0cf9ba5 -- src/proofpack/stats src/proofpack/narrate src/proofpack/render` → 0 lines. Outside `src/proofpack/egress/`: `.github/workflows/ci.yml`, `handoffs/` (the two lens notes), `pyproject.toml`, `schema/egress_schema.json`, `scripts/ci_namespace_run.py` (new), `scripts/mutation_sweep.py`, `src/proofpack/cli.py`, `doctor.py`, `errors.py`, `run.py`, `tests/conftest.py`, `test_doctor_cli.py`, `test_egress.py`, `test_offline.py`, `test_telemetry.py`. `narrate/`, `render/`, `templates/`, `claims_schema.json`, `output_schema_v1.json`: untouched.
- **The rendered legal sentences.** `dist/trust/index.html`: `Last updated 2026-09-23.` once; the unshare sentence as quoted above; `every socket call` 0; `no record has been received as of 22 September 2026` once (N4). `dist/legal/sub-processors/index.html`: the Supabase sentence verbatim; `since 22 September 2026` nowhere under `dist/`. `/legal/dpa` 2.1 in source: `one HTTP request after run.json is written, a 5 s timeout, a redirect answer not followed`, `the purge that enforces the 24 months is not built yet`; `lastUpdated: 2026-09-23` on `dpa.md` and `sub-processors.md`. Each is true of the code at `0cf9ba5` as measured above (one request, no follow; no purge exists: `grep -rn "24 months" functions/ db/` finds the comment in the migration saying the purge is NOT built).
- **The engine tree stayed clean.** `git status --short` in `eng-0cf9ba5` empty after every mutation; the builder's `wt-a-p2` is at `0cf9ba5` with nothing modified; the engine main tree's `handoffs/` holds only `2026-09-22_E_lens1_*.md` untracked (lane E's).

## Sentences I refused to write

- "removing any one socket patch makes a test fail" - measured false with correct code (N8); the code's short-circuits are what the tests pin.
- "the site gate chain gives the note's unit figure" - it gives 656 / 2 in a detached worktree and 657 / 1 only with `GPS_AUTOMATION_DIR` (N2).
- "ruff counts 121" - it counts 123 at `0cf9ba5` (N1).
- "the endpoint was re-attacked" - `attack_endpoint.mjs` was not re-run; the Function file is byte-identical between `b8242c1` and `2b5c7de`, which is the measured reason not to.
- "the gate inspects the state change" - it inspects page-versus-manifest equality (N7).

## What I could not check

- The `offline-namespace` job under `unshare -rn`: no Linux with Python here; the YAML parses and the script's lines are as the note says; the first CI run decides it (open question 3 stands).
- The Postgres path of the insert (the `runs` table does not exist in the project; no credentials here); the memory adapter only, and unchanged in this round.
- The live deploy: `2b5c7de` is unpushed; the endpoint answers 404 on the live site until it is. The migration (needs-from-Josh 1) is still to apply before the push.
- The full engine suite in PowerShell 5.1 (the note did not repeat it either; `-m ap2`, `-m day7`, `ruff check`, `doctor --offline`, the smoke and `--json-log` were repeated there and match).

## Worktrees

Created and removed: engine `eng-0cf9ba5`, `eng-4d61b6e`, `eng-7b2ca2a`, `eng-a0c9abc`; site `site-2b5c7de`, `site-b8242c1` (the two `node_modules` junctions deleted as links first). The builder's `wt-a-p2` and `wt-engine-a0c9abc` and the parallel lens's `lens-AP2-r2-fresh-attack/` worktrees were not touched.
