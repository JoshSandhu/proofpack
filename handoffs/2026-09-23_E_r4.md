# Lane E - build day 8, repair 4 and lens round 5 - 2026-09-23 (evening)

**The final lens round (5, at `375719c`) is `FAIL`: the regression lens passed with no blockers, and the fresh attack found four open blockers - `free_text` accepts listed words spelled with TR39 sources whose target is a non-ASCII Latin letter (`oк`, `мeets`, `cerтified`; B1), with an I-shaped character in an `i` position (`unbꓲased`, `faӀl`; B2), and with a precomposed letter whose base TR39 lists (`appѓoved`, `oќ`; B3), and a `criteria.yaml` model name holding the YAML escapes `\0`, `\x01`, `\x1b` reaches `T8.html` as 4 raw NUL, 4 SOH and 4 ESC bytes with exit 0 (B4).** Fifty-eight items are carried (the table below, the four blockers among them), so the engine stays unpushed and E8 is not done; the four lens-4 blockers no longer reproduce on their literal inputs, and neither lens broke the DEC-61 overall-block gate on the tables it fed.

This note supplements `2026-09-23_E.md` and `2026-09-22_E.md`, which are not rewritten. It replaces the repair-4 builder's version of this file (committed in `375719c`), three of whose sentences lens 5 measured false (row 58). Since `23f3d9f`: one commit, `375719c` (repair 4). Nothing is pushed. Main tree now, both shells: **1146 passed, 1 skipped, 1 xfailed**; `-m day8` 358; `git diff 7b2ca2a HEAD -- src/proofpack/stats/` is 0 lines; `tests/` since `7b2ca2a`: 160 `A`, 7 `M`, 0 `D` (all measured now).

## What landed

Items 1-10 and repairs 1-3: `2026-09-22_E.md` and `2026-09-23_E.md`. Today: repair 4 (`375719c`, note `scratchpad/notes/E8_repair4.md`) on lens 4's four blockers, under DEC-60, DEC-61 and DEC-62. Test file `tests/test_e8_repair4.py` (`day8`) unless named.

| Lens-4 id | Change | Test ids | Literal input -> figure |
|---|---|---|---|
| FA-B1 | T8, the claim builder and checker rule 8 look a criteria row's dotted `metric_ref` up in `claims.metric_ref_pointers(doc)`; the string is not split | `::test_an_operating_point_id_holding_a_dot_or_brackets_prints_its_own_number`, `::test_an_operating_point_id_with_an_unclosed_bracket_runs_and_prints_its_number` | Now, both shells: ops `0` / `[0]` / `t0.5` -> `99/101 (98.0%) [93.1, 99.5]`, `29/101 (28.7%) [20.8, 38.2]`, `85/101 (84.2%) [75.8, 90.0]`. `a[0` via the CLI: exit 0 (repair-4 note; lens 5) |
| FA-B2 (DEC-61; statistical gate) | `gates.gate_h08_y_pred_operating_points`, called in `ingest` and first in `run.overall_block` | `::test_a_y_pred_only_table_with_two_operating_points_halts_h08`; guard `::test_the_tables_dec61_leaves_running_still_run` | Now: `op_lo` 0.1 / `op_hi` 0.9, no score column -> `HaltError H08 ... (op_lo, op_hi) ...`, detail `{field: operating_points, reason: no_score_column, operating_points: [op_lo, op_hi]}`. Through the CLI: exit 3, no directory (lens 5) |
| FA-B3 | `CALIB_NA` built only on reason `score_not_probability`; the checker refuses it elsewhere (`template_mismatch`) | `::test_calib_na_is_claimed_and_accepted_only_on_score_not_probability` (3) | Now: `no_score_column engine CALIB_NA claims 0 hand-built: False template_mismatch`. `y_pred`-only, 200 rows: 59 claims (60 at `23f3d9f`), 0 rejected (lens 5) |
| FA-B4 (DEC-60) | `narrate/tr39_confusables.py`: 1,905 TR39 18.0.0 entries whose target, marks removed, is ASCII letters; header with source URL, Version, Date, full-file SHA-256 and the file's three notice lines; generated by `scripts/tr39_subset.py`. Two TR39 readings; ASCII `I` and `m` left out as sources; `well calibrated` rule over letters a-z | `::test_the_lens4_lookalike_literals_are_rejected` (35), `::test_well_calibrated_with_a_separator_and_letters_attached_is_rejected` (8), `::test_each_reading_has_a_literal_that_pins_it`, `::test_the_vendored_tr39_subset_*` (2); corpus `141`-`146` | Sources with a one-ASCII-letter target, substituted into the listed words: 52,694 texts, 21,948 accepted at `fe35332`, 0 now (repair 4; lens 5 re-ran it). Now: `tr39_subset.py --check` -> `vendored subset matches`. Rows 54-56 are the part of the class this figure does not cover |
| FA-N1 (DEC-62) | Page header `<span class="customer-text">name vX</span>`; op-id cells `customer-text`; `<title>` carries only `T8 · ProofPack v… · run …` | `::test_model_name_version_and_operating_point_are_customer_text_on_t8`; golden (13 lines) | Model `Safe triage`, version `2 unbiased`, op `pass` -> 0 forbidden hits outside `.customer-text` and the disclaimer (4 at `23f3d9f`) |
| RG-N1, FA-N2, FA-N3 | Blank-`y_pred` sentences name the `dev` condition; the three skeletons that differ from D4; checker comments | `::test_a_dev_row_with_a_blank_y_pred_counts_under_dev_rows`, `::test_the_lens4_false_sentences_are_gone_from_the_shipped_text` | 120 rows, 20 `dev` -> `dev_rows 20, label 5, score 5, analysed 90` |
| DEC-12(ii) | 13 day-8 mutants added, 1 re-targeted | `::test_the_sweep_lists_the_repair4_mutants` | Lens 5 regression, committed tree: `92 planted, 92 killed, 0 survived; 1297 s` |

Pre-fix: the new and changed test files in a `23f3d9f` worktree give `20 failed, 183 passed` (repair 4; both lens-5 notes re-measured it). Two of the 20 are `ImportError` (the TR39 module is absent there).

## What was cut and why

1. **Item 11 (E7 carried 20-24)** - not started; the builder hit the weekly usage limit on 22 September. The limit, not a decision.
2. **Repair 5** - not run: this run was one repair, one lens round and a handoff, as the orchestrator set it.
3. **T1, T7** - `--templates T1` / `T7` print a typed "not built in E8" line (E9's).
4. **The full Unicode License v3 text** - not fetched: a download needs Josh's approval in chat (needs 8).

## What did not land and why - the 58 carried items

Rows 6-53 keep their numbers from `2026-09-22_E.md` / `2026-09-23_E.md` (repros there); 54-72 are new. Rows 54-57 were re-run now in both shells (`repros6.py` below); the others are as the named note measured them.

**Closed since the 23 September note, on their literal inputs** (lens 5, and rows 41-43 re-run now): 41 (FA-B1), 42 (FA-B2), 43 (FA-B3), 45 (DEC-62), 46 (sentences). Row 44 in part (the 35 look-alikes and 8 separator spellings).

| # | Item | Why carried | Repro (one line) |
|---|---|---|---|
| **54** | **Blocker (lens-5 FA-B1).** TR39 sources whose target is a non-ASCII Latin letter (`к`->`ĸ`, `т`->`ᴛ`, `м`->`ʍ`, `ꭰ`->`ᴅ`; 91 such targets) are not in the subset: 1,264 texts, 826 accepted | Found in round 5; round cap | `f('o\u043a'), f('\u043ceets'), f('cer\u0442ified')` -> `None` x3 (now) |
| **55** | **Blocker (lens-5 FA-B2).** 114 sources TR39 maps to `l` are accepted in an `i` position: 4,674 texts, 2,803 accepted; `unbIased` is rejected | Round cap | `f('unb\ua4f2ased'), f('fa\u04c0l')` -> `None None`; `f('unbIased')` -> `free_text_verdict_word` (now) |
| **56** | **Blocker (lens-5 FA-B3).** TR39 applied before NFD: 3,084 texts, 1,550 accepted; the decomposed spelling is rejected | Round cap | `f('app\u0453oved')` -> `None`; `f('app\u0433\u0301oved')` -> `free_text_certification_word` (now) |
| **57** | **Blocker (lens-5 FA-B4).** C0 controls in customer text reach `T8.html` raw (model name here; version, author, justification through the assembler). Present at `23f3d9f` | Refuse at declaration or escape at render is a decision (needs 11) | model name `"Tri\0age \x01\x1b[31m"` -> exit 0, `NUL 4 SOH 4 ESC 4` (now) |
| 58 | Sentences in the builder's version of this note measured false: the 52,694 figure without its one-ASCII-letter restriction; "outside TR39" for the accepted remainder; `159 files` (160 then). Repair-4 note: 3,253 / 2,907 sentences measured before the README edit (3,257 / 2,911 at `375719c`, 0 newly rejected) | Corrected in this note | lens-5 FA non-blocking 1; RG N2 |
| 59 | `unmet` stays rejected (`An unmet clinical need.` -> `free_text_verdict_word`); the orchestrator listed it as prose to keep | Josh's (needs 9) | `f('An unmet clinical need.')` |
| 60 | DEC-62 names the `<title>`; the repair removed the manufacturer words from it instead | Josh to confirm (needs 10) | read `<title>` |
| 61 | Mutant `fairness_auroc_gap_path_dropped` survives, non-equivalent: T8 prints `—` beside `criterion not met` where `375719c` prints `+0.079 [−0.002, +0.160]` | No day-8 test feeds an `auroc_gap` criterion row | lens-5 FA non-blocking 4 |
| 62 | Mutant `tr39_rn_both_dropped` survives, non-equivalent: `ՠeets` (U+0560 -> `rn`) | One literal | lens-5 FA non-blocking 4 |
| 63 | DEC-61 call sites: the `ingest` call is not pinned, and `proofpack compare` (ingest only) depends on it (`HALT H08`, exit 3 at `375719c`); `stats.subgroups.subgroup_analysis` called alone is not guarded | A test through `compare`; a guard or a docstring | lens-5 FA non-blocking 4; repair-4 carried 5 |
| 64 | Mutant `tr39_second_pass_dropped` survives, non-equivalent: 1,167 texts rejected now, accepted under it (`satisfactorˠ`, `faևl`). Its twin `tr39_first_pass_dropped` also survives (70 and 62 characters normalise differently without each pass); the fresh attack found no listed word that depends only on those characters, multi-character combinations not enumerated | One literal and a sweep mutant each | lens-5 RG N3; FA non-blocking 4 |
| 65 | Mutant `calib_na_seq_gap` survives: claim ids contiguous (`CL-0001`..`CL-0059`) on a `y_pred`-only run, unpinned | One test | lens-5 RG N3 |
| 66 | The TR39 regeneration test skips where `workflows/data/confusables-18.0.0.txt` is absent (CI, worktrees, public clones): `1145 passed, 2 skipped` there | As asked ("when present"); open question 4 | lens-5 FA 5; RG N4 |
| 67 | On a `y_pred`-only or `lower_is_positive` run, `guidance_refs` loses `FDA_AIDSF_CALIBRATION` (2 refs, 3 at `23f3d9f`) and T8 its anchor (0 against 3); the S4 list said "Nothing else" | Follows from open question 1; added to the S4 list below | lens-5 RG N1 `probe/refs.py` |
| 68 | Three guard-test docstrings do not say they pass at `23f3d9f` | Wording | lens-5 RG N6 |
| 69 | New functions' docstrings name a decision or lens id but no D1 / D4 / D5 line (`calibration_suppression`, `criteria_row_pointers`, `_tr39_mapped`, `gate_h08_y_pred_operating_points`) | Documentation | lens-5 RG N7 |
| 70 | 155 Latin letters not in TR39 whose Unicode name carries the letter are accepted (`Ƥass` U+01A4, `pⱥss` U+2C65) | DEC-60: the class stays open beyond the data; open question 2 | repair-4 note |
| 71 | `CALIB_NA` for `no_score_column` / `score_not_positive_class_probability`: no claim, no sentence | Open question 1 | `y_pred`-only run: 59 claims |
| 72 | Unicode notice: the module carries the file's three header lines; the full licence text is not beside it, **[unverified]** whether that suffices | Needs 8 | read the module header |
| 44 | Residue: `&sect; eight`, `ninety`, `Stage IV.` accepted | Needs 2 | `repros5.py` line `B4:` last item `None` (now) |
| 47 | Reserved-id mutants survive `-m day8` (second position, case variants) | Pins for a statistical gate | lens-4 FA 3 |
| 48 | Four of six lens-4 checker mutants unpinned (`capital_i_reads_text_not_raw`, `wellcal_substring_full_only`, `shapes_subgroup_estimate_op_only`, `flow_scalar_slots_site_count_any`); the two readings are now killed | One literal each | lens-5 RG N3 |
| 49 | Clopper-Pearson beside Wilson at k = 0 / n not printed | T1 (E9) | lens-4 FA 5 |
| 50 | `CL-０００１` (fullwidth digits) accepted | `[0-9]` | lens-4 FA 6 |
| 51 | 8,988 single-field mutations, 11 accepted | Family skeletons print every slot | lens 2's `b_checker.py` (lens 5: 11) |
| 52 | Rule 8b accepts a subset of a family's pointers | E9 slot filling | lens-4 RG N3 |
| 53 | Capital-`I` reading refuses some prose | Errs to refusal | repair-3 carried 4 |
| 6 | `y_pred` at a declared indeterminate value beside a score leaves the analysis | Needs 3 | 120 rows, 12 such |
| 7 | `checker_smallcap_s_unmapped`: pinned by `paꜱꜱ`; TR39 also maps U+A731 to `s` (measured now), so whether the mutant still dies is not measured | Record | plant; `-m day8` |
| 8 | `anchors_month_year_fixed` survives | E9 anchor work | plant |
| 9 | `_declaration_for` wrap | Unreachable | plant |
| 14 | Docstrings cite lens ids; `errors.py` H08 text names no reserved ids | Documentation | read |
| 15 | `check` "reads no environment" | Wording | read |
| 16 | Repair-2 pre-fix artefact 25/137 vs 26/136 | Record | - |
| 17 | Golden mask `manifest.numpy` only | Record | unmasked render |
| 18 | `python -m proofpack doctor` fails; `.cli` runs | `__main__.py` or docs | run it |
| 19 | Token join refuses "in significant detail" | Needs 2 | `f(...)` |
| 20 | Derived words accepted (`unbiasedness`, `safely`, `approvals`; lens 5) | Needs 2 | `f('unbiasedness')` |
| 21 | Op ids `a/b`, `op~1`: 58 of 60 claims refused | False refusals | declare `op~1` |
| 22 | Fully excluded subgroup level vanishes | DEC-08, E9 | sex `F`, blank `y_pred` |
| 23 | Section 7 prints status words with no criteria (lens 5) | T8 template | `criteria: []` |
| 24 | `resolve` keeps two accepted claims on one slot | Open | two ids |
| 25 | `declared(1e22)` positional | Record | `fmt.declared(1e22)` |
| 26 | `--format HTML` folds; `''` asymmetry | S4 wording | three calls |
| 27 | No-licence watermark `LICENCE EXPIRED - not for submission` (now) | DEC-48, E9 | e2e run |
| 28 | `guidance_ref` any map row | As specified | swap |
| 29 | `calibration: null` + reason, DEC-35 IPA not on T8 (lens 5) | T1 (E9) | - |
| 30 | Draft label spelling vs D4 §1.1 | Needs 4 | read page |
| 31 | Fairness mixed scales; `font-weight` literals | Design / E9 | read page |
| 32 | No `jinja2` -> exit 5 after `run.json` | S4 bundle | hide `jinja2` |
| 33 | `guidance_refs` 3 vs page 7 | Open | count |
| 34 | Declarations echo sorted YAML | Needs 5 | section 3 |
| 35 | `integrity.halts` never printed | Not in D4 §11 | read |
| 36 | Wheel test `pytest.skip` path | One line | `_build_wheel` |
| 37 | `declaration_index: true` reads entry 0 | Unreachable | hand-built |
| 38 | `y_true` `NA` indeterminate -> missing label | Day-1 loader | `NA` |
| 39 | Bare YAML `date:` -> H08 | Customer quotes | unquoted |
| 40 | `unicode-bidi: isolate`, `@page var()` unchecked in a browser | No browser | print |

**E7 items still open, under their old numbers** (repros in `2026-09-21_E.md`; not re-run today):

| E7 # | State at `375719c` |
|---|---|
| 1-16 | Open (16 is the ledger race) |
| 17 | Open: a fairness-only `criteria.yaml` is not counted (DEC-47) |
| 18 | Decided (DEC-47); not built |
| 19-23 | Open; 20-23 were item 11 (cut) |
| 24 | Half closed (rows by position); H08 uniqueness was item 11 |
| 25 | Decided (DEC-48); not built (row 27) |
| 26, 28, 29, 33-37, 39-41, 43 | Open |
| 27 | Closed (DEC-50) |
| 30, 32 | Lane A (merged at `0f553c9`) |
| 31 | Decided (DEC-49); not built |
| 38 | Open |
| 42 | Closed (E8 item 1) |

## What the lenses found

Every lens ran in detached worktrees with `PYTHONPATH=<worktree>/src` forced and `proofpack.__file__` printed first.

- **Round 1 at `29fc04e`:** fresh attack FAIL (5), regression PASS. Deviations: Wilson vs statsmodels `1.11e-16`; AUROC vs sklearn `0`.
- **Round 2 at `657ef11`:** fresh attack FAIL (4), regression FAIL (2). Deviations: six pooled ratios `0.00e+00`.
- **Round 3 at `7fa690b`:** fresh attack FAIL (3), regression FAIL (3). Deviations: 60 Wilson cells, worst `2.22e-16`.
- **Round 4 at `fe35332`:** fresh attack FAIL (4: rows 41-44), regression PASS. Deviations: `1.11e-16` on the reserved-id re-derivation; 22 T8 cells, 0 mismatches.

**Round 5 at `375719c` - fresh attack `FAIL` (4 blockers, rows 54-57); regression `PASS` (no blockers, seven non-blocking: rows 58, 59, 60, 64-69).** The blockers as the fresh-attack note heads them:

- B1: "TR39 sources whose target is a non-ASCII Latin letter are not mapped: 826 of 1,264 texts accepted".
- B2: "TR39's I/l asymmetry: an I-shaped character in an `i` position is accepted, 2,803 of 4,674 texts".
- B3: "TR39 is applied before decomposition: a precomposed letter whose base TR39 lists is accepted, while its decomposed spelling is rejected".
- B4: "C0 control characters in customer text are written raw into T8.html (NUL, SOH, ESC), through the CLI". The lens grades it a blocker under "an unescaped injection" and says that if Josh reads the rule as markup injection only, it is record-and-carry.

What round 5 could not break (quoted from the two notes):

- **DEC-61 and the overall block (DEC-12(i)).** Fresh attack, 300 rows in 100 cases, seed 777: two-by-two `{tp 80, fn 15, fp 48, tn 157}` equal to its numpy count; four proportions deviate `0.0`; `proportion_ci` intervals equal; seven metrics carry `clustered_data_analytic_ci_invalid`. A one-stratum subgroup gives equal estimates and intervals that differ by 0.0026 to 0.0181 (its own bootstrap stream). Two ops at one threshold, duplicate ids, and a blank score column beside `y_pred` each halt; one op runs. Regression: five documents assembled at `375719c` and `23f3d9f` differ only in `guidance_refs` on the two `y_pred`-only ones (row 67); `y_pred`-only block against statsmodels Wilson, max deviation `2.22e-16`.
- **FA-B1:** 23 cells re-derived with a `Decimal` half-even formatter over ops `0`, `[0]`, `t0.5`, `a.b`, `x]y` and a `level: *` row: 0 mismatches. Lens 3's script: 22 cells, 0; 60 Wilson cells, worst `2.22e-16`.
- **The checker:** `swaps: 42849 accepted: 0`; 36,038 word forms, 0 newly rejected; 21,971 sentences, 12 newly rejected, each a lens literal quoted in a handoff.
- **Renderer, wiring, packaging:** `[unverified]` 7 times; the draft label on 14 AI-DSF mentions and 3 `guidance_refs`; golden 40,030 bytes equal; `--offline` with every socket entry point raising, 0 calls; wheel in a fresh venv, 1,905 entries, `proofpack.__file__` inside it; the vendored data re-derived from the full file (6,712 / 1,661 / 1,905 entries, SHA-256 equal).

## The decisions taken today

| Decision | Rule as shipped | Where | Test that feeds the input |
|---|---|---|---|
| DEC-60 (Josh) | TR39 18.0.0 subset vendored (1,905 entries), never fetched at run time; class open beyond it (rows 54-56, 70) | `narrate/tr39_confusables.py`, `checker.py`, `scripts/tr39_subset.py` | `test_the_vendored_tr39_subset_regenerates_from_the_full_file` (skips without the file) |
| DEC-60, ASCII sources | TR39's `I`->`l` and `m`->`rn` left out of the map; two readings read `rn` as `m`, one reads ASCII `I` as `l`; `\|`->`l` applied | `checker.py` | `test_each_reading_has_a_literal_that_pins_it` |
| DEC-61 (Josh: "H08") | No score column and more than one operating point -> H08 naming them | `gates.py`, `ingest`, `run.overall_block` | `test_a_y_pred_only_table_with_two_operating_points_halts_h08` |
| DEC-62 (Josh: "mark as customer text") | Header name/version and op-id cells in `.customer-text`; `<title>` carries engine text only (row 60) | `render/html.py`, templates | `test_model_name_version_and_operating_point_are_customer_text_on_t8` |
| FA-B1 | Look up, not refuse: customer ids keep `.`, `[`, `]` | `narrate/claims.py` | the two FA-B1 tests |
| FA-B3 | No calibration claim for the two other reasons (row 71) | `narrate/claims.py`, checker 8b | `test_calib_na_is_claimed_and_accepted_only_on_score_not_probability` |
| `unmet` | Kept in `VERDICT_WORDS` (row 59) | `checker.py` | `test_prose_the_new_readings_leave_accepted` |
| DEC-63 (Josh: "schedule off") | Lane A: the digest and page-watch crons off until the Supabase adapter is wired. Nothing in the engine | `gps-automation` | - |

## CLI and output changes S4 must know (DEC-43)

The site builds from `a0c9abc` (`proofpack-site/.github/workflows/ci.yml` line 18, read now). Moving the pin to `375719c` brings E7's list (`2026-09-21_E.md`), lane A's A-P2 list (`2026-09-22_A.md`) and E8's, in full:

- `run --format` (default `json,html`; case-folded; `docx`, `pdf,json` -> exit 5, no directory); `run --templates` (default `T8`; `T1`/`T7` -> exit 0 and a typed line; `T9` -> exit 5).
- `<out>/T8.html` beside `run.json` on licence `ok` / `grace`; otherwise `HTML not written: licence refused (no_file); run.json only (D1 section 7: after grace, JSON only)`, exit 4 (now, both shells).
- Next-step lines: `Next step: open T8.html beside run.json; T1 and T7 land on E9 (docs: /docs/run)` and `Next step: proofpack licence install FILE, then run again for T8.html (docs: /docs/run)` (both now).
- `run.json`: `claims`, `claim_rejections` (required; `reason_code` enum of 44), `guidance_refs` `{id, label, draft, url}` (3 on the e2e run), `criteria_results[].declaration_index`, `overall` non-null on clustered and `y_pred`-only tables, `overall.threshold_free.suppressed_reason`. E2e fixture now: 253,943 bytes, 20 keys, 70 claims.
- Halts, each exit 3 with nothing written: H02 on `y_pred`; H08 on an op id `threshold_free` / `auroc` / `brier` (`detail {field, reserved}`); **new in repair 4: H08 on a table without a score column declaring more than one operating point** (`detail {field: "operating_points", reason: "no_score_column", operating_points: [...]}`), on `run` and on `compare`. A demo panel that sends a `y_pred`-only sample with two thresholds would get this halt.
- **New in repair 4:** no `CALIB_NA` claim when `calibration_suppressed_reason.reason` is `no_score_column` or `score_not_positive_class_probability`, so later claim ids shift down by one, and on those runs `guidance_refs` has no `FDA_AIDSF_CALIBRATION` entry and T8 no calibration anchor (row 67; lens 5's page 30,037 bytes against 30,576).
- **New in repair 4, T8:** headers carry `<span class="customer-text">name vX</span>`; `<title>` is `T8 · ProofPack v… · run …`; op-id cells carry `class="customer-text"`. E2e page now 40,375 bytes.
- `flow.excluded_missing_score` counts blank `y_pred` without a score column; `flow.indeterminate` counts a declared indeterminate `y_pred`, with or without a score column.
- `--json-log` `run` payload: `formats`, `templates`, `documents`, `document_notes`, `claims`, `claim_rejections`.
- `jinja2>=3.1` and MarkupSafe at run time. `proofpack-site/scripts/demo_sample.py` line 62 calls `run` with no `--format` (read now), so the Pyodide bundle carries both or the call passes `--format json`.
- Unchanged by repair 4: `git diff 23f3d9f HEAD -- src/proofpack/cli.py src/proofpack/errors.py src/proofpack/criteria.py src/proofpack/stats pyproject.toml uv.lock schema/egress_schema.json` is empty (now). No flag, default or output path moved.

## Re-run these

Measured now (23 September, 16:00-16:20 BST) in the main tree at `375719c`; `python -c "import proofpack;print(proofpack.__file__)"` printed `C:\Users\joshs\GPS\ProofPack\proofpack\src\proofpack\__init__.py` in each shell first. `S` = `C:/Users/joshs/AppData/Local/Temp/claude/C--Users-joshs-GPS-ProofPack/a743e7e5-3be8-4d70-9ed0-681f5784a106/scratchpad`, `E` = `$S/e2e_shell`, `H` = `$S/handoff-E8-r4w`.

| Command | Git Bash | PowerShell 5.1 |
|---|---|---|
| `python -m pytest -q -p no:cacheprovider` | `1146 passed, 1 skipped, 1 xfailed in 97.04s` | `1146 passed, 1 skipped, 1 xfailed in 101.61s` |
| `-m day8` / `day7` / `day6` / `day5` / `ap2` | `358` / `139` / `285` / `54` / `88 passed` | the same five |
| `-m day4` / `day3` / `day2` / `day1` | `182` / `29 passed, 1 xfailed` / `40` / `59 passed, 1 skipped` | the same four |
| `python -m ruff check .` / `python -m ruff format --check .` | `All checks passed!` / `162 files already formatted` (ruff counts `.md`; two lens-5 notes were untracked) | the same |
| `python -m proofpack.cli doctor --offline` | `All essential checks passed.`, exit 0 | the same, exit 0 |
| `python scripts/tr39_subset.py ../workflows/data/confusables-18.0.0.txt --check` | `vendored subset matches`, exit 0 | the same |
| `python scripts/mutation_sweep.py --list` | 263 lines | 263 lines |
| `python scripts/mutation_sweep.py --marker day8` | not run now; lens 5 regression: `92 planted, 92 killed, 0 survived; 1297 s`, exit 0 | not run |
| `PROOFPACK_HOME=$H/home_bash python -m proofpack.cli run --input $E/test.csv --criteria $E/criteria.yaml --out $E/pack_w4_bash --offline` | `analysed 400 of 400 rows; criteria rows: 2 met, 2 not met, 5 not assessable` ... `Next step: proofpack licence install FILE, then run again for T8.html (docs: /docs/run)`, `exit=4`; `python $H/runjson.py <out>/run.json` -> `253943 bytes 20 keys 70 claims 0 rejections 3 refs [0, 1, 2, 2, 2, 3, 4, 5, None] CRLF 0 BOM False` | `exit=4`, the same `runjson.py` line |
| the same with `test.csv.mapping.json` moved aside | `HALT H07: run proofpack map first: ...`, `exit=3`, no directory | not run |
| `PYTHONIOENCODING=utf-8 python $S/handoff-E8/html_run.py $E $H/html_bash` (test-only licence) | `exit 0 ['T8.html', 'ingest_report.json', 'pseudonyms.json', 'run.json']` / `T8.html bytes 40375 CRLF False BOM False` | identical |
| `PYTHONIOENCODING=utf-8 python $S/repair-E8-r4/handoff/repros5.py` (lens 4's four) | `B1: [('C_zero', '99/101 (98.0%) [93.1, 99.5]', ...), ('C_br', '29/101 (28.7%) [20.8, 38.2]', '0.287', 'criterion not met'), ('C_dot', '85/101 (84.2%) [75.8, 90.0]', '0.842', 'criterion met')]` / `B2: HaltError H08: ... (op_lo, op_hi) ...` / `B3: no_score_column engine CALIB_NA claims 0 hand-built: False template_mismatch` / `B4: ['free_text_verdict_word' x5, None]` | identical |
| `PYTHONIOENCODING=utf-8 python $H/repros6.py $E` (lens 5's four) | `B1: [None, None, None, None, None]` / `B2: [None, None, None, 'free_text_verdict_word', 'free_text_verdict_word']` / `B3: [None, None, None, 'free_text_certification_word']` / `B4: exit 0 NUL 4 SOH 4 ESC 4` | identical |

To repeat a pre-fix figure, add a worktree and force `PYTHONPATH`; the editable `.pth` otherwise imports the main tree. The TR39 regeneration test runs only where `../workflows/data/confusables-18.0.0.txt` exists (or `PROOFPACK_TR39_FULL` names it).

## Tomorrow needs

1. **Repair 5 on rows 54-57, then lens round 6, before E9 builds on the checker.** 54: close the subset over TR39's own targets (`к`->`ĸ`->`k`), pinning `oк`, `мeets`, `cerтified`. 55: a reading that takes each character TR39 maps to `l` as `i`, pinning `unbꓲased`, `faӀl`, then the prose check again. 56: apply the map after NFD, pinning `appѓoved`, `oќ`. 57: the rule Josh picks (needs 11), with a byte assertion like the U+202E test. With them: literals for rows 61, 62, 64, 65 and a `compare` test for 63; row 67 decided or accepted.
2. **E9 per `spec/briefs/day9_E.md`**: about 40 sentence templates keyed to claims (D4 §8), T1's 15 sections, T7 from the run, `/sample-pack`, model card v1.1, golden snapshots, every FDA-draft anchor labelled, forbidden grep 0, render under 60 s on 100k rows, D5 furniture and figures F2-F5. E9 inherits:
   - rows 54-56 and 19, 20, 44, 53, 59, 70 the day any narrative carries `free_text` (the 70 engine claims of the e2e run carry `free_text: null`, measured now);
   - row 57 in every new customer-text slot T1 adds;
   - slot filling by pointer facet (rows 51, 52);
   - T1 printing `calibration: null` beside its reason (DEC-36), the IPA tier annotation (DEC-35), the 0.897 cell with its tier (DEC-34), a sentence or silence for rows 71 / 67, Clopper-Pearson if D4 §1.2 stands (row 49);
   - DEC-48's `NO LICENCE - not for submission` (row 27); section 7 without status words when no criteria exist (row 23);
   - DEC-55 `gate_h05` under `clustering: case_id` with its own DEC-12(i) lens; item 11; DEC-47 and DEC-49 with lane A; row 8 with the anchor work.
3. **S4 (day 10)** moves the pin with the three lists above.

## Needs from Josh

E7 needs 1-7 were decided on 23 September (DEC-44 to DEC-50); none carries. E8 needs, numbered as before:

1. **D1 §5.6 H02 row and §2 `y_pred` row** should read "y_true or y_pred" and say what a blank `y_pred` does. Open.
2. **What `free_text` covers beyond TR39**: number words (`ninety`), Roman numerals, `&sect;` as text, derived words (`unbiasedness`), prose the join rule refuses (rows 19, 20, 44, 53). Open.
3. **A `y_pred` at a declared indeterminate value beside a valid score** (row 6). Open.
4. **Draft label spelling** (row 30): amend D4 §1.1 to "draft guidance (January 2025), not for implementation"? Open.
5. **Declarations echo** (row 34): sorted YAML (as built) or the customer's file verbatim? Open.
6. Decided (DEC-61), built.
7. Decided (DEC-62), built; see 10.

New today:

8. **Unicode licence text** (row 72): is the three-line notice from `confusables.txt` enough, or should the full Unicode License v3 text sit beside the subset? The second needs one download, which needs your approval in chat.
9. **`unmet`** (row 59): keep it refused (as built; it is in `VERDICT_WORDS`), or accept it so "an unmet clinical need" passes? Recommendation: keep it refused until E9's narrative shows a sentence that needs it; the 70 engine claims of the e2e run carry `free_text: null` (measured now), so the rule reads no engine sentence yet.
10. **DEC-62 and `<title>`** (row 60): confirm the title carries engine text only, since a `<title>` cannot hold a `.customer-text` element.
11. **Control characters in customer text** (row 57): refuse C0/C1 controls at declaration with H08 naming the field, or replace them at render? Recommendation: refuse at declaration, because replacing a byte rewrites the customer's words (D4 §1.3) and a refusal names what to fix.

## Open questions

1. `CALIB_NA` for `no_score_column` / `score_not_positive_class_probability`: a template on T1, or silence plus the printed reason - and should `FDA_AIDSF_CALIBRATION` stay in `guidance_refs` on those runs (rows 67, 71)?
2. Beyond TR39 (row 70): a rule read from Unicode names ("LATIN CAPITAL LETTER P WITH HOOK" as `p`), or leave the class open?
3. Does E9 start before repair 5, given rows 54-56 reach no page today and row 57 does?
4. Should the full `confusables.txt` (763,128 bytes) be committed as a test fixture so the regeneration test runs in CI (row 66)?
5. Is `json,html` still the right `--format` default while T1 / T7 are unbuilt and the demo passes no `--format`?

## Sentences refused

Aggregated from the build note (none: no builder note), repair notes 1-4, the ten lens notes and this note.

- "The checker binds every claim to its Number / template." Written instead: 42,849 swaps, 0 accepted; 3,850 relabels, 4 accepted; 8,988 mutations, 11 accepted.
- "free_text rejects look-alike spellings" / "covers the TR39 confusables" / "every TR39 confusable is refused". Written instead: 52,694 one-ASCII-letter-target substitutions, 0 accepted, and rows 54-56 and 70.
- "The new readings refuse no English." Written instead: 3,257 sentences and 36,038 word forms, 0 newly rejected.
- "The TR39 readings make the hand maps redundant." Not written; each hand-mapped letter has a literal only a hand-map reading rejects.
- "The criteria table prints the right Number for every operating-point id" / "the lookup reads every id correctly." Written instead: `0`, `[0]`, `t0.5`, `a[0`, `a.b`, `x]y`.
- "DEC-61 closes FA-B2" / "the overall block is correct" / "leaves every other table unchanged." Written instead: the tables fed, the halts, exit 3, and the five documents compared.
- "The renderer escapes every injection." Written instead: the payloads fed, the bytes counted, and row 57.
- "The S4 list is complete" / "Nothing else changed." Refused: row 67.
- "Every repair-4 line is pinned" / "the sweep kills every non-equivalent mutant." Refused: rows 61-65, 47, 48.
- "The new tests would have caught the lens findings." Written instead: the pre-fix counts (42; 26; 22; 20 failed) and the guards named.
- "The declared value prints as the manufacturer wrote it", "the checker never raises", "a right-to-left override cannot reorder the page", "the wheel test proves the packaging", "H02 catches every mislabelled y_pred", "TR39 maps these letters" (before the fetch): deleted or replaced by the literals fed.
- In this note: "repair 4 closed lens 4's blockers" was written only as "no longer reproduce on their literal inputs"; "vendoring TR39 closes the homoglyph class", "E8 is ready for E9" and "the DEC-61 gate is correct" were not written.
