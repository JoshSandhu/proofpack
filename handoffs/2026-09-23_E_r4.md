# Lane E - build day 8, repair 4 - 2026-09-23 (evening)

**Repair 4 is in (commit on `main` after `23f3d9f`, not pushed). It changes the four lens-4 blockers and the sentence findings. Measured now: T8 prints each operating point's own Number for ids `0`, `[0]`, `t0.5` and `a[0` (FA-B1). A table without a score column that declares two operating points halts H08 and names them (FA-B2, DEC-61). No `CALIB_NA` claim is made on `no_score_column` or `score_not_positive_class_probability`, and the checker refuses one there (FA-B3). The TR39 18.0.0 subset is vendored, and the 35 look-alike literals and 8 separator spellings of lens 4 are rejected (FA-B4, DEC-60). The model name, version and operating-point id are `.customer-text` on T8 (DEC-62).** `overall_block` gained the DEC-61 gate. That is a statistical gate, so lens round 5 attacks it (DEC-12 i). E8 is not done until round 5 passes.

This note adds to `2026-09-23_E.md` and `2026-09-22_E.md`; it does not rewrite them. The two lens-4 notes were already committed in `23f3d9f`. Full detail, with every pre-fix E line: `scratchpad/notes/E8_repair4.md`.

## What landed

| Lens-4 id | Change | Test (`day8`) | Literal input -> measured now |
|---|---|---|---|
| FA-B1 (blocker) | T8, the claim builder and the checker's rule 8 read a criteria row's Number from `claims.metric_ref_pointers(doc)`. That function maps each dotted `metric_ref` that `criteria` writes, built from the document's keys, to its RFC 6901 pointer. The string is looked up and never split. The dotted `metric_ref` in `run.json` is unchanged. | `test_e8_repair4.py::test_an_operating_point_id_holding_a_dot_or_brackets_prints_its_own_number`, `::test_an_operating_point_id_with_an_unclosed_bracket_runs_and_prints_its_number` | ops `0`/`[0]`/`t0.5` -> `99/101 (98.0%) [93.1, 99.5]`, `29/101 (28.7%) [20.8, 38.2]`, `85/101 (84.2%) [75.8, 90.0]`, 0 rejections; `a[0` through the CLI -> exit 0 and its own cell |
| FA-B2 (blocker; DEC-61; statistical gate) | `gates.gate_h08_y_pred_operating_points`: no score column and more than one operating point -> H08 `{field: operating_points, reason: no_score_column, operating_points: [...]}`. It is called in `ingest` and as the first line of `run.overall_block`. | `::test_a_y_pred_only_table_with_two_operating_points_halts_h08`; guard `::test_the_tables_dec61_leaves_running_still_run` | `op_lo` 0.1 / `op_hi` 0.9, 300 rows -> `HALT H08 ... (op_lo, op_hi) ...`, exit 3, no directory |
| FA-B3 (blocker) | `CALIB_NA` is built only when `calibration_suppressed_reason.reason` is `score_not_probability`. The checker refuses it on any other reason (`template_mismatch`). | `::test_calib_na_is_claimed_and_accepted_only_on_score_not_probability` (3 cases) | `y_pred`-only on 200 rows: 59 engine claims (60 at `23f3d9f`), 0 rejected. A hand-built `CALIB_NA` claim -> `template_mismatch`. `logit`: `CALIB_NA` is kept and accepted |
| FA-B4 (blocker; DEC-60) | `narrate/tr39_confusables.py` holds 1,905 entries whose target, marks removed, is ASCII letters. It carries the source URL, `Version: 18.0.0`, the date, the SHA-256 of the full file and the file's Unicode notice. It is generated by `scripts/tr39_subset.py`. `_readings` gains two TR39 readings: with every `rn` read as `m`, and the same with ASCII `I` read as `l`. ASCII-letter sources (`I`, `m`) are left out of the map. The `well calibrated` substring rule now reads letters a-z only. | `::test_the_lens4_lookalike_literals_are_rejected` (35), `::test_well_calibrated_with_a_separator_and_letters_attached_is_rejected` (8), `::test_each_reading_has_a_literal_that_pins_it`, `::test_the_vendored_tr39_subset_*` (2); corpus `141`-`146` | 52,694 single-letter TR39 substitutions into the listed words: 21,948 accepted at `23f3d9f`, **0 now**. 650,760 lens-4 fuzz texts: 0 lost against the frozen `657ef11` / `7fa690b` / `fe35332` rules. 3,253 prose sentences (spec D1-D5, R1-R6, README, claim skeletons; digits removed): 0 newly rejected |
| FA-N1 (DEC-62) | Page header: `<span class="customer-text">name vX</span> · T8 · ...`. The criteria table's operating-point cell carries `customer-text` (the dash is not marked). `<title>` carries the engine part only, because a title cannot hold an element. | `::test_model_name_version_and_operating_point_are_customer_text_on_t8`; golden regenerated (13 lines) | model `Safe triage`, version `2 unbiased`, op `pass` -> 0 forbidden hits outside `.customer-text` and the disclaimer (4 at `23f3d9f`) |
| RG-N1 / FA-N2 / FA-N3 (sentences) | README, both schema descriptions, the `analysis_mask` docstring (the `dev` condition named); the `templates.py` list of skeletons that differ from D4 (three, read against spec `74837c8`); the comments on `_BEFORE_NFKC`, `_HOMOGLYPHS_R3` and `_SMALL_CAPITALS`; the `_readings` docstring ("ASCII capital `I`") | `::test_a_dev_row_with_a_blank_y_pred_counts_under_dev_rows`, `::test_the_lens4_false_sentences_are_gone_from_the_shipped_text` | 120 rows, 20 `dev` -> `dev_rows 20, label 5, score 5, analysed 90` |
| DEC-12(ii) | 13 new day-8 mutants; `checker_well_calibrated_substring_dropped` re-targeted | `::test_the_sweep_lists_the_repair4_mutants` | `92 planted, 92 killed, 0 survived; 1211 s`, exit 0 (76 day-8 + 16 `ap2`; run before two test-only edits - the TR39 import moved into its two tests and a SHA-256 assertion added - after which the 14 repair-4 mutants were re-run with `--only`: `14 planted, 14 killed, 0 survived; 187 s`) |

Pre-fix: the new test file, `test_claims.py`, the six corpus files and the golden were copied into a `23f3d9f` worktree and run with `PYTHONPATH` forced -> **20 failed, 183 passed**. Two of the 20 first `E` lines are `ImportError` (the TR39 module does not exist at `23f3d9f`). The other 18 are assertions or `DID NOT RAISE`.

## Carried (with the reason)

1. **`&sect; eight`, `ninety`, `Roman IV`** are still accepted. Needs-from-Josh 2 is open; the orchestrator said not to decide it.
2. **Look-alikes outside TR39 18.0.0 and the hand maps.** 155 single-letter substitutions by Latin letters whose Unicode name carries the letter and which TR39 does not list are accepted, for example `Ƥass` (U+01A4) and `pⱥss` (U+2C65). DEC-60: the class stays open beyond the data.
3. **Derived words** (`unbiasedness`, `consistently`, `unsafe` ...): carried 20, needs 2.
4. **`CALIB_NA` for the other two reasons.** No claim is made; a template for each is open question 2 of the repair-3 note. T1 (E9) must print `calibration_suppressed_reason` itself (DEC-36).
5. **DEC-61 inside `stats.subgroups.subgroup_analysis`.** The function is not guarded when it is called alone. `run` and the test assembler halt before any document exists. The `ingest` call to the gate is not pinned separately, because the `overall_block` call halts the same CLI run.
6. **The Unicode notice.** The module carries the three header lines of `confusables.txt` (copyright, trademark, terms URL). The full Unicode License v3 text was not fetched. Whether the header lines are enough is **[unverified]** (needs 1 below).
7. Rows 45-53 and the E7 items of `2026-09-23_E.md` are unchanged, except row 41 (FA-B1), row 42 (FA-B2), row 43 (FA-B3), row 44 in part (see 1-3 above), row 45 (DEC-62), row 46 (sentences), and rows 48 and 7 in part (literal pins `ᴘassɑ`, `ᴄonsistentɑ`, `turn rnΙscalibrated`, `paꜱꜱ`). Row 47 (reserved-id mutants) is not pinned.

## CLI and output changes S4 must know (DEC-43)

- **New:** H08 on a table without a score column that declares more than one operating point. It prints `detail {field: "operating_points", reason: "no_score_column", operating_points: [...]}`, exits 3 and writes nothing. A demo panel with a `y_pred`-only sample and two thresholds would now halt.
- `run.json` on a `y_pred`-only table (and on `lower_is_positive` probabilities) carries no `CALIB_NA` claim, so the claim ids after it shift down by one. `claim_rejections[].reason_code` enum: unchanged (44).
- `T8.html`: page headers carry a `span.customer-text`; `<title>` is `T8 · ProofPack v… · run …` (the model name and version are gone from it); the operating-point cells carry `class="customer-text"`. The e2e page is 40,375 bytes (40,145 at `23f3d9f`).
- The schema descriptions of `y_pred` and `flow.excluded_missing_score` changed wording only. No flag, default, output path or key changed. `cli.py`, `errors.py`, `criteria.py`, `stats/`, `pyproject.toml` and `egress_schema.json` are unchanged since `23f3d9f`.

## Re-run these

See `scratchpad/notes/E8_repair4.md` for both shells. Main tree, `proofpack.__file__` = the main tree's `src\proofpack\__init__.py`:

- `python -m pytest -q -p no:cacheprovider`: `1146 passed, 1 skipped, 1 xfailed` (Git Bash and PowerShell).
- `-m day8` 358, `day7` 139, `day6` 285, `day5` 54, `ap2` 88.
- `ruff check`: `All checks passed!`. `ruff format --check`: `159 files already formatted`.
- `python -m proofpack.cli doctor --offline`: exit 0.
- `python scripts/mutation_sweep.py --marker day8`: `92 planted, 92 killed, 0 survived; 1211 s`, exit 0 (76 day-8 + 16 `ap2`; run before two test-only edits - the TR39 import moved into its two tests and a SHA-256 assertion added - after which the 14 repair-4 mutants were re-run with `--only`: `14 planted, 14 killed, 0 survived; 187 s`).
- `python scripts/tr39_subset.py ../workflows/data/confusables-18.0.0.txt --check`: `vendored subset matches`.

## Needs from Josh

1. **Unicode licence text.** Is the three-line header notice of `confusables.txt` enough, or should the full Unicode License v3 text go beside the vendored subset? That needs one more fetch, which needs your approval.
2. Needs 2-5 of the 22 September note (number words and derived words; the indeterminate `y_pred` beside a score; the draft label spelling; the declarations echo): still open.

## Open questions

1. `CALIB_NA` on `no_score_column` / `score_not_positive_class_probability`: a template for each on T1 (E9), or keep silence plus the printed reason?
2. Should look-alike letters beyond TR39 (item 2) be read by a rule from Unicode names (for example "LATIN SMALL LETTER P WITH HOOK" read as `p`), or stay open?

## Sentences refused

- "free_text rejects look-alike spellings" / "every TR39 confusable is refused". Written instead: 52,694 single-letter substitutions, 0 accepted, and the 155 of item 2.
- "The criteria table prints the right Number for every operating-point id". Written instead: the four ids fed.
- "DEC-61 closes FA-B2". Written instead: the table fed, the halt and exit 3.
- "The new readings refuse no English". Written instead: 3,253 sentences, 0 newly rejected.
