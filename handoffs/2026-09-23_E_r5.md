# Lane E - build day 8, repair 5 and lens round 6 - 2026-09-23 (evening)

**The final lens round (6, at `ea2f743`) is `FAIL`: both lenses failed it, each with one open blocker - a table level label holding C0/C1 control characters reaches `T8.html` raw through the CLI with exit 0, in the scope cell of a `level: "*"` criterion and of the fairness-bound row (`race = re\x01d` at byte 25,164; fresh-attack B1, row 73), and a `criteria.yaml` whose first line is `zz_extra: &a [*a]`, which halted H08 with exit 3 at `2a9a6e2`, now ends `internal error: RecursionError` with exit 5 inside the control-character walk repair 5 added (regression B1, row 74).** Sixty-four items are carried (the table below: the two blockers, the four look-alike rows DEC-64 carries to E9, and 58 others), so E8 is not done; lens-5's FA5-B4 literal (`"Tri\0age \x01\x1b[31m"` as the model name) no longer reproduces - it now halts H08 naming `model/name`, exit 3, nothing written.

This note supplements `2026-09-22_E.md`, `2026-09-23_E.md` and `2026-09-23_E_r4.md`, which are not rewritten; its carried table is the r4 note's rows plus this round's. Since `2a9a6e2`: one commit, `ea2f743` (repair 5); the two lens-6 notes are committed with this note. Nothing is pushed by this run. Main tree now, both shells: **1214 passed, 1 skipped, 1 xfailed**; `-m day8` 426; `git diff 7b2ca2a HEAD -- src/proofpack/stats/` is 0 lines; `tests/` since `7b2ca2a`: 161 `A`, 7 `M`, 0 `D` (all measured now).

## What landed

Items 1-10 and repairs 1-4: the three earlier notes. Today's last round: repair 5 (`ea2f743`, note `scratchpad/notes/E8_repair5.md`), scoped by the orchestrator under DEC-64 and DEC-65 to lens-5 FA5-B4 and four sentence findings. `git diff 2a9a6e2 ea2f743 -- src/proofpack/narrate` is empty (FA5-B1..B3 untouched). Test file `tests/test_e8_repair5.py` (`day8`, 68 tests); at `2a9a6e2` it gives **64 failed, 4 passed** (repair 5; both lens-6 notes re-measured it, each failure an assertion).

| Lens-5 id | Change | Test ids | Literal input -> measured figure |
|---|---|---|---|
| FA5-B4 (DEC-65) | `io/declare.py::control_character_at` walks the parsed `criteria.yaml` (keys and values) for U+0000-U+001F, U+007F, U+0080-U+009F; a hit is `HALT H08: declaration invalid at <path>: control character U+XXXX; remove it`, the value not echoed. Tab and LF pass under `justification`, `description`, `source`; CR is H08 in the fields the tests feed; E01 comes first inside `clustering.unit` (row 76). `Mapping.read` walks each entry's `role` and `notes`, plus `decided_by` and `timestamp` | `::test_u0001_in_each_string_field_of_the_criteria_document_halts` (43), `::test_named_control_characters_halt_h08_with_their_code_point` (14), `::test_a_control_character_in_a_key_halts_h08_naming_the_parent`, `::test_version_author_and_justification_halt_through_the_assembler`, `::test_the_cli_run_with_nul_soh_esc_in_the_model_name_exits_3_and_writes_nothing`, `::test_a_control_character_in_mapping_text_halts_h08`; guard `::test_tab_and_line_feed_are_accepted_in_the_three_free_text_fields` | U+0001 planted in each of 43 string fields: at `2a9a6e2` 16 reach `run.json` and 11 reach `T8.html`; at `ea2f743` all 43 exit 3, 42 H08 naming the field, `clustering/unit` E01 (repair 5; lens-6 FA re-measured). Now, both shells: `repros6.py` B4 prints `HALT H08: declaration invalid at model/name: control character U+0000; remove it`; `paths.py`: `map --criteria` 3, `compare` 3 with no directory, `map --yes` 3 with the mapping file unchanged |
| RG-N6 (sentence) | Three repair-4 guard docstrings now say they pass at `23f3d9f` | `::test_the_three_repair4_guards_say_they_pass_at_23f3d9f` (3) | At `23f3d9f` with the current `test_e8_repair4.py`: `2 failed, 3 passed` (repair 5; lens-6 RG re-measured) |
| FA5-N1, FA5-N2, RG-N1 (sentences) | Already corrected in the r4 note at `2a9a6e2`; no test can fail there, none written | - | `git grep "outside TR39\|52.694\|21.948\|159 files"` over `src tests scripts README.md`: 0 lines (lens-6 RG) |
| DEC-12(ii) | Seven day-8 mutants (`declare_dec65_*` five, `mapping_dec65_*` two) | `tests/test_sweep_day8.py` | `--list` 270 lines now (83 day8). Sweep: see "Re-run these" |

DEC-65 is a declaration gate, not a statistical gate; `git diff 2a9a6e2 ea2f743 -- src/proofpack/{stats,render,narrate,templates,run.py,cli.py,errors.py,criteria.py} pyproject.toml uv.lock schema` is empty (lens-6 FA).

## What was cut and why

1. **Item 11 (E7 carried 20-24)** - not started; the builder hit the weekly usage limit on 22 September. The limit, not a decision.
2. **FA5-B1, FA5-B2, FA5-B3** - not worked: DEC-64 carries them to E9 (rows 54-56).
3. **Repair 6** - not run: this run was one repair, one lens round and a handoff, as the orchestrator set it. Rows 73 and 74 are open.
4. **T1, T7** - `--templates T1` / `T7` print a typed "not built in E8" line (E9's).
5. **The full Unicode License v3 text** - not fetched; a download needs Josh's approval in chat (needs 8).

## What did not land and why - the 64 carried items

Rows 6-72 keep their numbers from the earlier notes (repros there); 73-83 are new. Rows 54-56, 73 and 74 were re-run now in both shells; the others are as the named note measured them.

**Closed since the r4 note:** 57 (FA5-B4 on its literal input; its bytes reach the page by another input, row 73), 58 (sentences; lens-6 RG grep 0 lines), 59 and 60 (decided by DEC-65: `unmet` stays refused, `<title>` engine text only), 68 (repair 5's docstring test).

| # | Item | Why carried | Repro (one line) |
|---|---|---|---|
| **73** | **Blocker (lens-6 FA-B1).** C0/C1 characters in a table **cell** used as a level label reach `T8.html` raw, exit 0: a `level: "*"` criterion's scope cell and the fairness-bound row print the level. Measured by the lens on `attr_colour`, `race`, `site`, `device` with SOH, ESC, NUL, U+0085, U+009F. Same bytes at `2a9a6e2`. Repair 5's "`T8.html` 0 raw" figure covered site cells with no criterion naming the site | Round cap; DEC-65 names `criteria.yaml` and the mapping, so the rule for table cells is Josh's (needs 12) | `LENS_WT=$PWD python <lens6-fa>/att/p3_fair.py` -> `0 {... 'T8.html': 1}` and `race = re\x01d` at byte 25164 (now, both shells) |
| **74** | **Blocker (lens-6 RG-B1).** `control_character_at` recurses with no visited set: `zz_extra: &a [*a]` as line 1 of `criteria.yaml` is exit 5 `RecursionError` (exit 3 H08 `<root>: additionalProperties` at `2a9a6e2`). The same alias under `model:` or in `criteria[0]` is exit 5 at both commits. Nothing is written in any exit-5 case | Round cap | `python $H/alias.py $E $H/alias_bash` -> `exit 5`, `internal error: RecursionError: maximum recursion depth exceeded`, `pack exists: False` (now, both shells) |
| 75 | Repair-5 mutants survive `-m day8`, non-equivalent (lens-6 FA N1, RG N2): C1 dropped from the multi-line pattern (justification `a\x9b31mb` accepted and printed on T8); keys checked with the multi-line pattern (key `x\ty`); `decided_by` not walked (`file\x1b[31m`); the walk moved below the authored-field loop (id `C\x1b[31m1` with an empty author prints a raw ESC in the halt message); detail prefix dropped (cosmetic) | One literal and one sweep mutant each | lens-6 FA `att/mutdemo.py` |
| 76 | Repair-5 sentences measured false: `declare.py` module docstring line 6 ("so is a string holding a control character"), the `MULTILINE_FIELDS` comment line 45 ("... are H08 naming the field"), `control_character_at` docstring ("`None` if none does"), and the repair note's "each halts there". Counter-examples: justification `"a\tb"` accepted; `clustering.unit: "pat\x00ient"` is E01; a missing `prevalence` block halts H08 naming no field; row 74 | Round cap | lens-6 FA N2, RG N1 |
| 77 | Exit 5 on customer files outside DEC-65's ranges: a lone surrogate `"Tri\ud800age"` in the model name -> `UnicodeEncodeError` at both commits, nothing written; an alias chain of 1,048,576 expanded leaves takes 0.51 s in `validate_dict` at `ea2f743` (0.00 s at `2a9a6e2`) | Record; a typed halt is a decision | lens-6 RG N4, FA N4 (`p9_laughs.py`) |
| 78 | Regional-indicator `pass` (U+1F1F5 U+1F1E6 U+1F1F8 U+1F1F8) accepted by `free_text` | DEC-64: carried to E9 with rows 54-56 | lens-6 FA N5 |
| 79 | Mapping fields the DEC-65 walk does not read (`source`, `value_summaries`, `examples`, extra keys; `confidence` holding a control is H07): 0 raw C0, 0 C1, 0 escaped bytes in `run.json`, `ingest_report.json`, `pseudonyms.json`, `T8.html` or the printed stream on the inputs fed | Record against DEC-65's "the mapping" | lens-6 FA N6, RG "Mapping text the repair does not read" |
| 80 | `design/tokens.json` `source`: "Three of D5's stated ratios do not match the formula" is false: against their own `-bg` surfaces D5's figures hold (`#8a4b00`/`#fdf3e3` 6.19, `#7f1d1d`/`#fdeaea` 8.65, `#14532d`/`#e4f2e8` 7.88); the file pairs them with `bg`. Since the E8 build; lenses 1-5 did not raise it; the E8 build need and DEC-51 rest on it | Josh or the design day (needs 13) | lens-6 RG N3 |
| 81 | Format and separator characters (U+2028, U+2029, U+200B, U+202E, U+FEFF) in customer text pass `validate_dict`; U+2028 reaches T8 raw; U+202E is written as `&#x202e;` | Outside DEC-65's ranges | repair 5; lens-6 FA "YAML edge cases" |
| 82 | A CSV header holding a control character | Headers reach `mapping.json` only (module docstring); not measured | repair 5 |
| 83 | `_IDENT` (`^[a-z][a-z0-9_]{0,31}$`) matches `attr_x\n`; the DEC-65 mapping walk on `role` halts first | `io/schema.py` also uses it; unchanged | `re.match(..., 'attr_x\n')` -> `True` (repair 5; lens-6 both) |
| **54** | TR39 sources whose target is a non-ASCII Latin letter (`oк`, `мeets`, `cerтified`): 1,264 texts, 826 accepted (lens 5) | DEC-64 -> E9 | `repros6.py` `B1: [None, None, None, None, None]` (now) |
| **55** | l-prototype characters in an `i` position (`unbꓲased`, `faӀl`): 4,674 texts, 2,803 accepted (lens 5) | DEC-64 -> E9 | `B2: [None, None, None, 'free_text_verdict_word', 'free_text_verdict_word']` (now) |
| **56** | TR39 applied before NFD (`appѓoved`, `oќ`): 3,084 texts, 1,550 accepted (lens 5) | DEC-64 -> E9 | `B3: [None, None, None, 'free_text_certification_word']` (now) |
| 61 | Mutant `fairness_auroc_gap_path_dropped` survives (T8 prints `—` beside `criterion not met`) | No test feeds an `auroc_gap` criterion row | lens-5 FA 4 |
| 62 | Mutant `tr39_rn_both_dropped` survives (`ՠeets`) | One literal | lens-5 FA 4 |
| 63 | DEC-61's `ingest` call unpinned; `compare` depends on it; `subgroup_analysis` alone unguarded | A `compare` test | lens-5 FA 4 |
| 64 | Mutants `tr39_second_pass_dropped` (1,167 texts, `satisfactorˠ`) and `tr39_first_pass_dropped` survive | One literal each | lens-5 RG N3 |
| 65 | Mutant `calib_na_seq_gap` survives (claim ids contiguous, unpinned) | One test | lens-5 RG N3 |
| 66 | TR39 regeneration test skips without `workflows/data/confusables-18.0.0.txt`: `1213 passed, 2 skipped, 1 xfailed` in a worktree, `1214` with `PROOFPACK_TR39_FULL` set (lens-6 RG N6) | Open question 3 | run in a worktree |
| 67 | `y_pred`-only / `lower_is_positive` run: no `FDA_AIDSF_CALIBRATION` in `guidance_refs`, no calibration anchor on T8 (2 refs against 3; page 30,037 bytes against 30,576) | Open question 1; in the S4 list | lens-5 RG `probe/refs.py` |
| 69 | Docstrings name a decision or lens id and no D1/D4/D5 line: `calibration_suppression`, `criteria_row_pointers`, `_tr39_mapped`, `gate_h08_y_pred_operating_points`, and now `control_character_at` (its comment cites D1 §6, an egress sentence) and `mapping._check_control_characters` | Documentation | lens-5 RG N7; lens-6 RG N7 |
| 70 | 155 Latin letters outside TR39 accepted (`Ƥass`, `pⱥss`) | DEC-60/DEC-64: class open | repair-4 note |
| 71 | No `CALIB_NA` for `no_score_column` / `score_not_positive_class_probability` | Open question 1 | 59 claims on a `y_pred`-only run |
| 72 | Unicode notice: three header lines beside the subset, full licence text absent, **[unverified]** whether that suffices | Needs 8 | read the module header |
| 44 | `&sect; eight`, `ninety`, `Stage IV.` accepted | Needs 2 | `repros5.py` `B4:` last item `None` (now) |
| 47 | Reserved-id mutants survive (second position, case variants) | Pins for a statistical gate | lens-4 FA 3 |
| 48 | Four lens-4 checker mutants unpinned | One literal each | lens-5 RG N3 |
| 49 | Clopper-Pearson beside Wilson at k = 0 / n not printed | T1 (E9) | lens-4 FA 5 |
| 50 | `claim_id` with fullwidth (`CL-０００１`) or Arabic-Indic (`CL-٠٠٠١`) digits accepted: `\d` | `[0-9]` | lens-4 FA 6; lens-6 FA N5 |
| 51 | 8,988 single-field mutations, 11 accepted | Family skeletons print every slot | lens 2's `b_checker.py` |
| 52 | Rule 8b accepts a subset of a family's pointers | E9 slot filling | lens-4 RG N3 |
| 53 | Capital-`I` reading refuses some prose | Errs to refusal | repair-3 carried 4 |
| 6 | `y_pred` at a declared indeterminate value beside a score leaves the analysis | Needs 3 | 120 rows, 12 such |
| 7 | `checker_smallcap_s_unmapped` pin | Record | plant; `-m day8` |
| 8 | `anchors_month_year_fixed` survives | E9 anchor work | plant |
| 9 | `_declaration_for` wrap | Unreachable | plant |
| 14 | Docstrings cite lens ids; `errors.py` H08 text names no reserved ids | Documentation | read |
| 15 | `check` "reads no environment" | Wording | read |
| 16 | Repair-2 pre-fix artefact 25/137 vs 26/136 | Record | - |
| 17 | Golden mask `manifest.numpy` only | Record | unmasked render |
| 18 | `python -m proofpack doctor` fails; `.cli` runs | `__main__.py` or docs | run it |
| 19 | Token join refuses "in significant detail" | Needs 2 | `f(...)` |
| 20 | Derived words accepted (`unbiasedness`, `safely`, `approvals`) | Needs 2 | `f('unbiasedness')` |
| 21 | Op ids `a/b`, `op~1`: 58 of 60 claims refused | False refusals | declare `op~1` |
| 22 | Fully excluded subgroup level vanishes | DEC-08, E9 | sex `F`, blank `y_pred` |
| 23 | Section 7 prints status words with no criteria | T8 template | `criteria: []` |
| 24 | `resolve` keeps two accepted claims on one slot | Open | two ids |
| 25 | `declared(1e22)` positional | Record | `fmt.declared(1e22)` |
| 26 | `--format HTML` folds; `''` asymmetry | S4 wording | three calls |
| 27 | No-licence watermark `LICENCE EXPIRED - not for submission` (now, both shells) | DEC-48, E9 | e2e run |
| 28 | `guidance_ref` any map row | As specified | swap |
| 29 | `calibration: null` + reason, DEC-35 IPA not on T8 | T1 (E9) | - |
| 30 | Draft label spelling vs D4 §1.1 | Needs 4 | read page |
| 31 | Fairness mixed scales; `font-weight` literals | Design / E9 | read page |
| 32 | No `jinja2` -> exit 5 after `run.json` | S4 bundle | hide `jinja2` |
| 33 | `guidance_refs` 3 vs page 7 | Open | count |
| 34 | Declarations echo sorted YAML | Needs 5 | section 3 |
| 35 | `integrity.halts` never printed | Not in D4 §11 | read |
| 36 | Wheel test `pytest.skip` path | One line | `_build_wheel` |
| 37 | `declaration_index: true` reads entry 0 | Unreachable | hand-built |
| 38 | `y_true` `NA` indeterminate -> missing label | Day-1 loader | `NA` |
| 39 | Bare YAML `date:` -> H08 | Customer quotes | unquoted |
| 40 | `unicode-bidi: isolate`, `@page var()` unchecked in a browser | No browser | print |

**E7 items still open, under their old numbers** (repros in `2026-09-21_E.md`; unchanged by repair 5; not re-run today):

| E7 # | State at `ea2f743` |
|---|---|
| 1-16 | Open (16 is the ledger race) |
| 17 | Open: a fairness-only `criteria.yaml` is not counted (DEC-47) |
| 18 | Decided (DEC-47); not built |
| 19-23 | Open; 20-23 were item 11 (cut) |
| 24 | Half closed (rows by position); H08 uniqueness was item 11 |
| 25 | Decided (DEC-48); not built (row 27) |
| 26, 28, 29, 33-37, 39-41, 43 | Open |
| 27 | Closed (DEC-50) |
| 30, 32 | Lane A (merged at `0f553c9`) |
| 31 | Decided (DEC-49); not built |
| 38 | Open |
| 42 | Closed (E8 item 1) |

## What the lenses found

Every lens ran in detached worktrees with `PYTHONPATH=<worktree>/src` forced and `proofpack.__file__` printed first.

- **Round 1 at `29fc04e`:** fresh attack FAIL (5), regression PASS. Deviations: Wilson vs statsmodels `1.11e-16`; AUROC vs sklearn `0`.
- **Round 2 at `657ef11`:** fresh attack FAIL (4), regression FAIL (2). Deviations: six pooled ratios `0.00e+00`.
- **Round 3 at `7fa690b`:** fresh attack FAIL (3), regression FAIL (3). Deviations: 60 Wilson cells, worst `2.22e-16`.
- **Round 4 at `fe35332`:** fresh attack FAIL (4), regression PASS. Deviations: `1.11e-16`; 22 T8 cells, 0 mismatches.
- **Round 5 at `375719c`:** fresh attack FAIL (4: rows 54-57), regression PASS. Deviations: `y_pred`-only block against statsmodels Wilson `2.22e-16`; 23 T8 cells, 0 mismatches.

**Round 6 at `ea2f743` - fresh attack `FAIL` (1 blocker, row 73); regression `FAIL` (1 blocker, row 74).** The blockers as the notes head them:

- Fresh attack B1: "C0 and C1 control characters in a table cell reach T8.html raw, through the CLI, exit 0 (FA5-B4's bytes, another input)". The lens grades it under "an unescaped injection", as lens 5 graded FA5-B4, and says that if Josh reads DEC-65 as covering declarations only, it is record-and-carry.
- Regression B1: "`control_character_at` recurses without a guard: a self-referential alias that halted H08 at `2a9a6e2` is exit 5 `RecursionError` at `ea2f743`". The lens follows lens 3's grading of an exit-5 crash on a customer file and says Josh may downgrade it; the class existed at `2a9a6e2` in the `model` and `criteria[0]` positions.

What round 6 could not break (quoted from the two notes):

- **Repair 5's own figures.** `64 failed, 4 passed` at `2a9a6e2` in both shells; 43 planted fields, all exit 3 at `ea2f743`; `map --criteria`, `compare`, `map --yes` each exit 3 with nothing written; `\N` (U+0085), an int key `7: "a\x01"` (exit 5 `TypeError` at `2a9a6e2`) and `clustering.declared_by` with ESC each H08.
- **The overall block (unchanged; DEC-12(i)).** 300 rows in 100 cases, seed 777: two-by-two `{tp 80, fn 15, fp 48, tn 157}` equal to the lens's numpy count; four proportions deviate `0.0`; each interval equal to a direct `proportion_ci` call; seven metrics `clustered_data_analytic_ci_invalid`; one cluster `insufficient_clusters`; two ops without a score `HALT H08`.
- **The renderer.** 22 cells re-derived by D4 §1.2, 0 mismatches; 23 more (`cells.py`), 0; `[unverified]` 7 times; the draft label on 14 AI-DSF mentions; golden 40,030 bytes equal; nine injection payloads escaped or literal, 0 raw U+202E; forbidden words outside `.customer-text` 0.
- **Wiring and packaging.** `--offline` 0 network calls; the wheel in a fresh venv with no `.pth`, `proofpack.__file__` inside it; imports with six packages hidden; `uv lock --check --offline` rc 0.
- **The sweep.** Both lenses: `99 planted, 99 killed, 0 survived` (1188 s, 1191 s), exit 0, tree clean after.
- **Nothing weakened.** `tests/` diff: `M test_e8_repair4.py` (docstrings only), `A test_e8_repair5.py`; no skip, xfail or marker change.

## The decisions taken today

| Decision | Rule as shipped | Where | Test that feeds the input |
|---|---|---|---|
| DEC-64 (Josh: "go with the recommendation") | The look-alike class stops at the vendored TR39 subset for day 8; FA5-B1..B3 carried to E9 (rows 54-56, and 78 under it) | nothing changed in `narrate/` | - |
| DEC-65, controls | C0/C1 in `criteria.yaml` keys and values, and in the mapping's `role`, `notes`, `decided_by`, `timestamp` -> H08 naming the field, exit 3, nothing written, on the 43 fields and 14 literals the tests feed; E01 inside `clustering.unit` (row 76); exit 5 on a self-referential alias (row 74) | `io/declare.py`, `io/mapping.py` | `test_e8_repair5.py` (68) |
| DEC-65, multi-line (repair 5's choice, asked for by DEC-65) | Tab and LF pass under `justification`, `description`, `source` (D1 §6's three free-text fields) and are H08 under the other keys the tests feed; CR is H08 in the fields the tests feed; E01 comes first inside `clustering.unit` (row 76) | `MULTILINE_FIELDS` comment | `test_tab_and_line_feed_are_accepted_in_the_three_free_text_fields` |
| DEC-65, need 9 | `unmet` stays in `VERDICT_WORDS` | `checker.py` (unchanged) | `test_prose_the_new_readings_leave_accepted` |
| DEC-65, need 10 | `<title>` carries engine text only | `render/html.py` (unchanged since `375719c`) | `test_model_name_version_and_operating_point_are_customer_text_on_t8` |
| Repair 5, order | DEC-11's E01 runs before the DEC-65 walk; the walk runs before any halt that prints a criterion id (the second half unpinned: row 75) | `validate_dict` | `test_mapping_repair1.py::test_separators_the_lens_listed_reach_e01` |
| Repair 5, mapping | `original` (the table header) is not walked; H08, not H07 | `io/mapping.py` | `test_a_control_character_in_mapping_text_halts_h08` |
| Need 8 | Full Unicode License v3 text: still open (DEC-65) | - | - |

## CLI and output changes S4 must know (DEC-43)

The site builds from `a0c9abc` (`proofpack-site/.github/workflows/ci.yml` line 18, read now). Moving `ENGINE_REF` to `ea2f743` brings E7's list (`2026-09-21_E.md`), lane A's A-P2 list (`2026-09-22_A.md`), the E8 list in `2026-09-23_E_r4.md` ("CLI and output changes S4 must know", unchanged), and repair 5's:

- **New halt, H08 at declaration** on `run`, `compare`, `map --criteria`: a `criteria.yaml` key or value holding U+0000-U+001F, U+007F or U+0080-U+009F, except tab and LF under `justification`, `description`, `source`; inside `clustering.unit` the halt is E01 (row 76). Message `declaration invalid at <path>: control character U+XXXX; remove it`; detail `{"field": <path>, "reason": "control_character", "codepoint": "U+XXXX"}`; a key is `<parent> (key)`. Exit 3, nothing written.
- **New halt, H08 on `mapping.json`** (`run`, `compare`, `map --yes`): a control character in an entry's `role` or `notes`, or in `decided_by` / `timestamp`. Message `mapping.json invalid at <path>: control character U+XXXX; remove it`; detail field `mapping.json <path>`; `map --yes` leaves the file unchanged (measured now).
- **Halt precedence moved** (exit 3 in each case):
  - DEC-11's E01 now comes before the block-shape and authored-field H08s: with `clustering.unit: "a and b"`, `criteria: "x"`, `criteria: [5]` and `fairness: "x"` each halt E01 (at `2a9a6e2`: `H08 criteria block must be a list`, `H08 criteria[0] is not a mapping`, `H08 fairness block is not a mapping`) (lens-6 FA N3); so does a document lacking a criterion's author.
  - The control-character H08 now comes before the authored-field H08 and H09: id `C\x1b[31m` with an empty author, and op id `op\x001`, were `criterion ... lacks author` and H09 at `2a9a6e2` (lens-6 RG N5).
- **Exit code moved on one input:** `criteria.yaml` whose root holds a self-referential alias (`zz_extra: &a [*a]`) is exit 5 `internal error: RecursionError` (exit 3 H08 at `2a9a6e2`); row 74, open.
- The demo YAMLs (the site's `synthetic_cohort.criteria.yaml` copies and `mimic_composite_key.criteria.yaml`) give no hit from `control_character_at` (repair 5; lens-6 both).
- **No CLI flag, default, output path or JSON key changed:** `git diff 2a9a6e2 HEAD -- src/proofpack/cli.py src/proofpack/run.py src/proofpack/errors.py pyproject.toml uv.lock schema` is 0 lines (now).
- Still true from r4: `proofpack-site/scripts/demo_sample.py` line 62 calls `run` with no `--format` (read now), so the Pyodide bundle carries jinja2 and MarkupSafe or the call passes `--format json`; on a `y_pred`-only or `lower_is_positive` run `guidance_refs` has no `FDA_AIDSF_CALIBRATION` and T8 no calibration anchor (row 67).

## Re-run these

Measured now (23 September, 18:15-18:50 BST) in the main tree at `ea2f743`, the two lens-6 notes untracked; `python -c "import proofpack;print(proofpack.__file__)"` printed `C:\Users\joshs\GPS\ProofPack\proofpack\src\proofpack\__init__.py` in each shell first. `S` = `C:/Users/joshs/AppData/Local/Temp/claude/C--Users-joshs-GPS-ProofPack/a743e7e5-3be8-4d70-9ed0-681f5784a106/scratchpad`, `E` = `$S/e2e_shell`, `H` = `$S/handoff-E8-r5`. Both day-8 sweeps ran in the background during the first suite run, so its Git Bash time is inflated.

| Command | Git Bash | PowerShell 5.1 |
|---|---|---|
| `python -m pytest -q -p no:cacheprovider` | `1214 passed, 1 skipped, 1 xfailed in 171.61s` | `1214 passed, 1 skipped, 1 xfailed in 90.48s` |
| `-m day8` / `day7` / `day6` / `day5` / `ap2` | `426` / `139` / `285` / `54` / `88 passed` | the same five |
| `-m day4` / `day3` / `day2` / `day1` | `182` / `29 passed, 1 xfailed` / `40` / `59 passed, 1 skipped` | the same four |
| `python -m ruff check .` / `python -m ruff format --check .` | `All checks passed!` / `165 files already formatted` (163 tracked plus the two untracked lens-6 notes; ruff counts `.md`) | the same |
| `python -m proofpack.cli doctor --offline` | 17 `[ok` lines, `All essential checks passed.`, exit 0 | the same |
| `python scripts/tr39_subset.py ../workflows/data/confusables-18.0.0.txt --check` | `vendored subset matches`, exit 0 | the same |
| `python scripts/mutation_sweep.py --list` | 270 lines: 34 day5, 112 day6, 25 day7, 83 day8, 16 ap2 | 270 lines |
| `python scripts/mutation_sweep.py --marker day8` | `99 planted, 99 killed, 0 survived; 1246 s`, exit 0 (83 day-8 + 16 `ap2`; run in the sweep's own temp copy, concurrently with the PowerShell run) | `99 planted, 99 killed, 0 survived; 1245 s`, exit 0 |
| `PROOFPACK_HOME=$H/home_bash python -m proofpack.cli run --input $E/test.csv --criteria $E/criteria.yaml --out $E/pack_r5h_bash --offline` (empty home) | `analysed 400 of 400 rows; criteria rows: 2 met, 2 not met, 5 not assessable` / `licence refused (no_file); watermark: LICENCE EXPIRED - not for submission` ... `Next step: proofpack licence install FILE, then run again for T8.html (docs: /docs/run)`, `exit=4`; `python $S/handoff-E8-r4w/runjson.py <out>/run.json` -> `253943 bytes 20 keys 70 claims 0 rejections 3 refs [0, 1, 2, 2, 2, 3, 4, 5, None] CRLF 0 BOM False` | `exit=4`, the same `runjson.py` line |
| the same with no mapping file beside the input | `HALT H07: run proofpack map first: ...` / `No document was written.`, `exit=3`, no pack directory | the same |
| `python -m proofpack.cli licence show` (empty home) | `licence: none found` / `status: refused (no_file)`, `exit=4` | the same, `exit=4` |
| `PYTHONIOENCODING=utf-8 python $S/handoff-E8/html_run.py $E $H/html_bash` (test-only licence) | `exit 0 ['T8.html', 'ingest_report.json', 'pseudonyms.json', 'run.json']` / `T8.html bytes 40375 CRLF False BOM False` | identical |
| `PYTHONIOENCODING=utf-8 python $S/repair-E8-r4/handoff/repros5.py` (lens 4's four) | `C_br '29/101 (28.7%) [20.8, 38.2]'`, `C_dot '85/101 (84.2%) [75.8, 90.0]'` / `HaltError H08: ... (op_lo, op_hi) ...` / `no_score_column engine CALIB_NA claims 0 hand-built: False template_mismatch` / five `free_text_verdict_word` and `None` | identical |
| `PYTHONIOENCODING=utf-8 python $S/handoff-E8-r4w/repros6.py $E` (lens 5's four) | `B1: [None, None, None, None, None]` / `B2: [None, None, None, 'free_text_verdict_word', 'free_text_verdict_word']` / `B3: [None, None, None, 'free_text_certification_word']` / `HALT H08: declaration invalid at model/name: control character U+0000; remove it` / `No document was written.` (the script then raises `FileNotFoundError` on the absent `T8.html`) | identical |
| `LENS_WT=$PWD PYTHONIOENCODING=utf-8 python $S/lens-E8-r6-fresh-attack/att/p3_fair.py` (row 73) | `0 {'ingest_report.json': 0, 'pseudonyms.json': 0, 'run.json': 0, 'T8.html': 1}` / `at 25164 b'...<td class="customer-text">race = re\x01d</td>...'` | identical |
| `LENS_WT=$PWD ... python $S/lens-E8-r6-fresh-attack/att/p2_star.py` (row 73, `level: "*"`) | `attr_colour soh 0 {... 'T8.html': 2}` with `attr_colour = re\x01d` and `= bl\x1bue` raw; `c1`: `re\xc2\x85d`, `bl\xc2\x9fue`; `nul`: `re\x00d` | the `soh` case identical |
| `python $H/alias.py $E $H/alias_bash` (row 74) | `exit 5` / `internal error: RecursionError: maximum recursion depth exceeded` / `pack exists: False` | identical |
| `WT=$PWD python $S/repair-E8-r5/offline_and_imports.py` (socket entry points raising) | `run --offline --format json,html: rc 0 socket calls 0` | identical |
| `WT=$PWD python $S/repair-E8-r5/paths.py` | `map --criteria (3, [... model/name ... U+0000 ...])` / `compare (3, [...]) dir False` / `map --yes (note) (3, [... roles/0/notes/0 ... U+0001 ...]) file unchanged True` | identical |
| `jinja2`, `markupsafe`, `scipy`, `cryptography`, `statsmodels`, `sklearn` set to `None`; import `proofpack`, `.stats`, `.io.declare`, `.io.mapping`, `.narrate.checker`, `.gates` | `imports ok; loaded: []` | identical |

To repeat a pre-fix figure, add a worktree and force `PYTHONPATH`; the editable `.pth` otherwise imports the main tree. The TR39 regeneration test runs only where `../workflows/data/confusables-18.0.0.txt` exists or `PROOFPACK_TR39_FULL` names it (row 66).

## Tomorrow needs

1. **Repair 6 on rows 73 and 74, then a lens round, before E9 adds customer-text slots.** 74: a visited set (or depth bound) in `control_character_at` ending in a typed H08 naming the path; the regression test feeds `zz_extra: &a [*a]` at the root, under `model:` and in `criteria[0]`, and fails at `ea2f743` with exit 5. 73: the rule Josh picks (needs 12), pinned with `make_cohort(n=400)`, `race` levels `re\x01d` / `bl\x1bue`, a `tpr_gap` fairness block on `race` bound 0.2 and a `level: "*"` criterion on `race`, asserting the halt or 0 bytes matching `[\x00-\x08\x0b\x0c\x0e-\x1f\x7f]|\xc2[\x80-\x9f]` in `T8.html`. With them: row 75's four literals and mutants, row 76's sentences (name the inputs and the E01 and missing-block precedence), rows 61-65.
2. **E9 per `spec/briefs/day9_E.md`**: about 40 sentence templates keyed to claims (D4 §8), T1's 15 sections, T7 from the run, `/sample-pack`, model card v1.1, golden snapshots, every FDA-draft anchor labelled, forbidden grep 0, render under 60 s on 100k rows, D5 furniture and figures F2-F5. E9 inherits:
   - DEC-64: rows 54-56 and 78 revisited with a real sentence in view, with 19, 20, 44, 53, 70; the 70 engine claims of the e2e run carry `free_text: null` (measured now);
   - row 73 in each slot T1 adds that prints a level label (T1's subgroup tables will print level labels);
   - slot filling by pointer facet (rows 51, 52);
   - T1 printing `calibration: null` beside its reason (DEC-36), the IPA tier annotation (DEC-35), the 0.897 cell with its tier (DEC-34), a sentence or silence for rows 67 and 71, Clopper-Pearson if D4 §1.2 stands (row 49);
   - DEC-48's `NO LICENCE - not for submission` (row 27); section 7 without status words when no criteria exist (row 23);
   - DEC-55 `gate_h05` under `clustering: case_id` with its own DEC-12(i) lens; item 11; DEC-47 and DEC-49 with lane A; row 8 with the anchor work.
3. **S4 (day 10)** moves the pin with the four lists above.

## Needs from Josh

E7 needs 1-7 were decided on 23 September (DEC-44 to DEC-50); none carries. E8 needs, numbered as before:

1. **D1 §5.6 H02 row and §2 `y_pred` row** should read "y_true or y_pred" and say what a blank `y_pred` does. Open.
2. **What `free_text` covers beyond TR39**: number words (`ninety`), Roman numerals, `&sect;` as text, derived words (`unbiasedness`), prose the join rule refuses (rows 19, 20, 44, 53). DEC-64 settles the look-alike class for day 8 only. Open.
3. **A `y_pred` at a declared indeterminate value beside a valid score** (row 6). Open.
4. **Draft label spelling** (row 30): amend D4 §1.1 to "draft guidance (January 2025), not for implementation"? Open.
5. **Declarations echo** (row 34): sorted YAML (as built) or the customer's file verbatim? Open.
6. Decided (DEC-61), built. 7. Decided (DEC-62), built.
8. **Unicode licence text** (row 72): the three-line notice, or the full Unicode License v3 text beside the subset (one download, your approval in chat)? Open (DEC-65).
9. Decided (DEC-65): `unmet` refused. 10. Decided (DEC-65): `<title>` engine text only. 11. Decided (DEC-65), built.

New today:

12. **Control characters in table cells** (row 73): refuse at ingest (a typed halt naming the column, exit 3), or have the renderer escape or refuse them in every string it prints? Recommendation: refuse at ingest for any column whose values can print as a level label, for DEC-65's reason (replacing a byte rewrites the customer's data; a halt names what to fix).
13. **DEC-51 and `design/tokens.json`** (row 80): DEC-51 schedules D5 §3.1's honesty, stop and ok ratios to be "corrected" to 6.80, 10.02 and 9.11 on the design day. Lens 6 measured D5's own figures against each token's `-bg` surface: 6.19, 8.65, 7.88, which round to D5's 6.2, 8.7, 7.9; the 6.80 / 10.02 / 9.11 figures come from pairing the tokens with `bg`. Recommendation: withdraw DEC-51's correction, re-pair the three tokens with their `-bg` surfaces in `tokens.json`, and delete the sentence (lane S's design day reads DEC-51).
14. **The push.** DEC-64 accepts the look-alike rows; rows 73 and 74 are outside it. Does the engine push with them open? Measured so far: row 73 put control bytes on T8 through a `level: "*"` criterion's scope cell and the fairness-bound row; site cells with no criterion naming the site gave 0 bytes on T8 (repair 5; lens-6 RG). Row 74 wrote no file in any exit-5 case the lens fed (four documents).

## Open questions

1. `CALIB_NA` for `no_score_column` / `score_not_positive_class_probability`: a template on T1, or silence plus the printed reason - and should `FDA_AIDSF_CALIBRATION` stay in `guidance_refs` on those runs (rows 67, 71)?
2. Does E9 start before repair 6, given row 73 reaches T8 today?
3. Should the full `confusables.txt` (763,128 bytes) be committed as a test fixture so the regeneration test runs in CI (row 66)?
4. DEC-65's multi-line fields are the three D1 §6 names, a section about egress; should D1 carry a field-definition list for them (row 69)?
5. Is `json,html` still the right `--format` default while T1 / T7 are unbuilt and the demo passes no `--format`?

## Sentences refused

Aggregated from the build note (none: no builder note), repair notes 1-5, the twelve lens notes and this note.

- "The checker binds every claim to its Number / template." Written instead: 42,849 swaps, 0 accepted; 3,850 relabels, 4 accepted; 8,988 mutations, 11 accepted.
- "free_text rejects look-alike spellings" / "covers the TR39 confusables" / "the look-alike class is closed" / "FA5-B1..B3 are fixed." Written instead: 52,694 one-ASCII-letter-target substitutions, 0 accepted, and rows 54-56, 70, 78 (DEC-64).
- "The new readings refuse no English." Written instead: 3,257 sentences and 36,038 word forms, 0 newly rejected.
- "The TR39 readings make the hand maps redundant." Not written.
- "The criteria table prints the right Number for every operating-point id." Written instead: `0`, `[0]`, `t0.5`, `a[0`, `a.b`, `x]y`.
- "DEC-61 closes FA-B2" / "the overall block is correct." Written instead: the tables fed, the halts, and the deviations.
- "The renderer escapes every injection" / "is now safe against injection" / "control characters can no longer reach the page (T8)." Written instead: the payloads fed, the bytes counted, the 43 fields and 14 literals, and row 73.
- "The DEC-65 gate refuses every control character in `criteria.yaml`" / "covers every customer string" / "every string field is covered." Written instead: 43 fields of one document, tab and LF in three fields, E01 on `clustering.unit`, and rows 73, 74.
- "The mapping text is refused everywhere it could appear" / "fully covered." Written instead: the four fields read, the literals fed, and row 79.
- "The repair's mutants pin the gate" / "every repair-4 line is pinned" / "the sweep kills every non-equivalent mutant." Refused: rows 61-65, 75, 47, 48.
- "Repair 5 introduced no regression." Refused: row 74.
- "Self-referential documents are a new defect." Refused: two positions were exit 5 at `2a9a6e2`.
- "The three sentence findings are fixed by this repair." Written instead: corrected in the `2a9a6e2` note; no test can fail there.
- "The S4 list is complete" / "nothing else changed." Refused (lens-5 RG N1; lens-6 FA N3, RG N5); written instead: the diff that is 0 lines for `cli.py`, `run.py`, `errors.py`, `pyproject.toml`, `uv.lock`, `schema/`, and the moved precedences and exit code.
- "The new tests would have caught the lens findings." Written instead: the pre-fix counts (42; 26; 22; 20; 64 failed) and the guards named.
- Older, still refused: "the declared value prints as the manufacturer wrote it", "the checker never raises", "a right-to-left override cannot reorder the page", "the wheel test proves the packaging", "H02 catches every mislabelled y_pred".
- In this note: "repair 5 closed FA5-B4" was written only as "no longer reproduces on its literal input"; "E8 is ready for E9" and "the engine is ready to push" were not written.
