# Lane E, build day 10 (E10) - lens 3, fresh attack at `6c4e253` (E10 repair 2), run on Saturday 26 September 2026

**Verdict: PASS.** No blocker. Every statistic E10 ships reproduces from my own arithmetic with no repository code: McNemar on sixteen tables against my integer binomial and against statsmodels 0.15.0 (worst deviation 0.0 / 1.1e-16); the Newcombe paired method-10 interval on nineteen tables as I can state the steps (worst 8.3e-17; still not an oracle, need 35 / DEC-70(a)); the paired DeLong difference on R2's F3 pair with the covariance written out (variance 6.9e-18, bounds 1.9e-16) and on the F5 pair's 100 rows and both `sex` levels (2.8e-17 / 0.0 / 1.1e-16); every paired cell of the F5 fixture (overall, `M`, `F`; Se / Sp / accuracy) from the CSVs by hand (worst 6.9e-17); the calibration-slope difference against `statsmodels.Logit` (2.2e-16); the twelve clustered Se / Sp / accuracy cells, the three clustered AUROC cells, the clustered Brier and slope cells and the two i.i.d. Brier / slope cells by my own draw loops (0.0 on all fourteen proportion / AUROC / Brier bounds, 2.0e-14 / 8.4e-15 on the slopes). No margin reaches run.json or the page without a customer criterion; 24 criterion variants agree with the literal rule 24 / 24; no forbidden word reaches T2 outside the disclaimer's negations; injection is escaped; the marks and footers are on every page; the committed sweep is `18 planted, 18 killed`. Three of my own 24 mutants survive behaviourally - all three in the repaired `_margins_for` / T2-3 placement rule - and the shipped code is right on the inputs that expose them; graded record-and-carry with the other six records below.

Setup: detached CRLF worktrees `scratchpad/lens-E10-r3-fresh-attack/wt-6c4e253` and `wt-667a201` (`git ls-files --eol`: `i/lf w/crlf` on the three F5 files, the golden and `T2.html`); `PYTHONPATH=<wt>/src` on every call and proved (`proofpack.__file__` printed the worktree path first in every capture; the one main-tree run is marked so). Nothing committed; both worktrees removed at the end; the main tree carries only this note. Every number below is mine, measured today in Git Bash.

## Blockers

None.

## Non-blocking (record and carry)

### N1. Three of my mutants on the repaired T2-3 placement rule survive every behavioural test - test gaps on a repaired status rule (DEC-12 (i))

`scratchpad/lens-E10-r3-fresh-attack/mutants.py` plants 24 mutants one at a time in the worktree, runs the four E10 test files with `-x`, then `tests/test_criteria.py`, the five `test_e9_*` files, `test_sweep_day10.py` and `test_run_cli.py`, and restores the file from git (`git status` clean after each run). 21 are killed by a behavioural test (the golden kills L6 McNemar labels swapped, L7 b / c on every row, L13 traceability sections swapped, L17 ledger off by one, L18 T2-5 delta, L24 `.status` dash; `test_criteria.py` kills L14 `ci_upper_bound` reading `ci_lo`; the hand-derivation tests kill L10 / L11 / L15 / L16 / L20 / L22; `test_two_paired_criteria...` kills L3 / L5). Three survive every behavioural test and are non-equivalent (each changes T2-3 rows on a document I rendered):

| Mutant | What it changes | Non-equivalence measured (`render_t2` on my saved documents, file restored after) |
|---|---|---|
| L1 `_margins_for` drops `same_scope` | a subgroup-scoped paired criterion also prints on the overall row | block D document: the overall Se row gains `CseF (row 8)` / `criterion met`; the overall Sp row goes from `— / —` to two `CspStar` lines with two record strings |
| L2 `_margins_for` drops the `operating_point` match | a criterion on op1 prints on op2's row too | block M two-op document: both accuracy rows print `C2` and `C2op2` |
| L4 `_margins_for` `"record": status != "met"` | the record string on a `not_assessable` T2-3 line | the unpaired document: the accuracy row's Status cell becomes `not assessable (not_like_for_like) - record of criterion not met (...)`; killed only by `test_sweep_day10.py::test_every_day10_pattern_matches_its_file_the_declared_number_of_times` (a source-text count), by no test that renders a page |

L9 (`_mcnemar_by_op` reading `new.pos` for the prior indicator) also survives and is equivalent on every paired join (the join refuses a pair whose `y_true` differs, block F `label_flipped: HALT H12`), so it is not a gap. L19 (`has_margin` true only for an overall-scoped criterion) is killed by the same pattern-count test only; whether a run with subgroup-only paired criteria should print the T2-3 Margin / Status columns is not pinned by a rendering test either (the shipped code prints them with dashes; block D). The shipped placement is right on blocks D and M (below). Repair shape: one test on block D's shape (a subgroup-scoped, an op1 and an op2 paired criterion) asserting each T2-3 row's Margin cell holds exactly its own criterion ids and that no `not_assessable` line carries the record string.

### N2. The ledger counts a compare that wrote no document (inherited; DEC-47 says it must not)

`run.py::assemble_compare` calls `ledger.record_run` before the CLI decides the exit code, and `record_run` writes the count immediately. Two `compare --offline` runs with a licence whose `features` lack `compare` (exit 4, `run.json` only, no `T2.html`) leave `ledger.json` at `{"compare:6d4d3d3c...": 2}`, the second printing `prior_acceptance_runs 1`. `run` with no licence file behaves the same (two exit-4 runs, no document, `ledger_count 1` then `2`). DEC-47: "counts every run with a `criteria` list that **wrote a document** - the count is recorded after the write, so a HALT or a licence refusal that wrote nothing does not count"; DEC-70(c) calls `run.json` alone "no document". The E8 handoff (`2026-09-22_E.md`, row 18) already records "DEC-47: count after the write - decided, not built", so this is inherited, not E10's; recorded because T2 section 7 and `LEDGER_STATEMENT` now print the count ("used in N prior version comparisons"), which after refused attempts includes runs that produced no comparison document. Belongs with DEC-70(b)'s day-11 ledger item. Repro: `python <scratch>/cx.py <wt> <out> K` (the two `ledger probe` lines) and the `run` probe in this note's re-run block.

### N3. A paired criterion on a metric the comparison does not compute prints a false typed reason

`criteria.yaml` with `paired_difference_vs_prior` on `ppv` (`Cppv`) and on `f1` (`Cf1`), overall, op1: `Cppv` is `not_assessable / comparison_not_computed_for_scope`; `Cf1` is `not_assessable / metric_not_computed`. The comparison **was** computed for the overall scope; PPV is not a compared metric by design (`comparison.py` line 116: "PPV and NPV are not paired proportions ... and are not compared"). `criteria.py::_resolve_comparison` reaches `_comparison_cell(per_op, ...)` for any `SUBGROUP_OP_METRICS` id and that helper's only reason is `comparison_not_computed_for_scope`. On the page: "Criterion Cppv (PPV difference against the prior version, overall, op1, CI lower bound >= −0.05; ...): not assessable (comparison_not_computed_for_scope)." Status right, reason false - the same class as lens 1's FA-N4. Repro: block M (`no-scope / ppv / f1`).

### N4. The Margin cell and the criterion sentences mix scales; D4 section 5.7 prints the margin in percentage points

T2-3's Δ column prints `−8.0 [−15.5, −1.0]` (percentage points, per the caption) and the Margin cell on the same row prints the declared value verbatim, `ci lower bound >= −0.05` (proportion scale); the `CRITERION_STATUS` and `CRITERION_NOT_MET_RECORD` sentences read "observed −8.0 [−15.5, −1.0] against >= −0.05" with no unit on the observed figure. D4 section 5.7's worked row prints `Δ −8.0 [...]` beside `≥ −3.0 (C2)` - the margin rescaled to the Δ column's unit. Every number is right (the est is `−0.08`, the customer's value is `−0.05`, the status follows the unrounded bound: `ci_lower_bound >= -0.1554` is `met` and `>= -0.1553` is `not_met`, block C) and `fmt.declared` prints the customer's digits as the E9 lens asked; the two units sit in one row without a label. For Josh with D4: either print the margin in the Δ column's unit as 5.7 draws it, or label the units in the cell and the sentence. Not a wrong number; a reader comparing `−15.5` with `−0.05` has to know the caption.

### N5. Carried records re-measured at `6c4e253`, unchanged

FA-N4 (a `y_pred`-only pair: `brier` / `slope` refused `score_not_probability` while the run's own block says `no_score_column`; block M); FA-N5 (unpaired AUROC, new arm 10 positives / 1 negative: `insufficient_positives`, `n_pos 16`, `n_neg 6`; main tree, read only); FA-N6 (`prior_version` absent: "from version    to  1.3", cover `not declared`); FA-N7 (a `y_true` flip under `--allow-unpaired`: `W12` "unmatched row_ids" with `label_mismatch 1`); FA-N10 (a payload with no `features` key: exit 0, `T2.html` written; `features: []` and `features` without `compare`: exit 4, `run.json` only, watermark `None`); FA-N11 (U+2028 raw 2, U+2029 raw 1, both inside `.customer-text`; U+202E / U+200F escaped); FA-F4 (prior `case_id = c{(i + 1) // 2}`: no warning, `differences.op1.sensitivity.n_cases 26` beside `prior.overall.op1.sensitivity.n_cases 28`); FA-F6 (a) (no `ledger` block: "...(ledger.warn_after_acceptance_runs) is no limit declared."); lens 1 item 14 (T7 from `compare --templates T7`: `no difference` 2, `stable` 2 - E9 method text). Not re-graded.

### N6. Records from this round's probes

- A `sex` level value carrying markup (`F<b>`, `F` + U+202E, `F{{ 7*7 }}`) halts `H07` ("non-interactive mode requires every mapped role at high confidence"; `low_or_medium: 1`) before any page - typed, so injection through a level value did not reach T2 in my runs; the criteria-file paths did (below).
- `run.json` records the bootstrap cell key (`"cell_key"` present), so the `comparison.py` module docstring's "the cell key recorded" holds; my first Se / Sp draw loop differed from the engine by 1.6e-2 because I keyed the cells `sensitivity` / `specificity` where the engine keys them `se` / `sp` (`PROPORTIONS`); with the engine's names the deviation is 0.0 on all six cells. The key names in run.json are the way a reader reproduces a cell; noted for T7's text.
- The pre-fix run: the two repair-2 test files copied into the `667a201` worktree, `PYTHONPATH` forced: `2 failed, 3 passed` - the FA-F5 test fails on `assert (1 == 1 and 0 == 1)` (`C2</span> (row 2)` absent) and the FA-F1 test on the docstring string; the three FA-F3 tests pass on the unmutated `667a201` code, as the repair note states (they exist to kill M5 / M10 / M11, which the committed sweep kills: `18 planted, 18 killed`).

## What I could not break (this is evidence)

Derivations in `scratchpad/lens-E10-r3-fresh-attack/hand.py` (pure Python and integer arithmetic above the line marked ENGINE), the CSV re-derivations in `cx.py` block H (numpy on the fixture rows, my own `e f g h` counts and my own O(m n) placements), the draw loops in `cx.py` block G and `g2.py` (my own case grouping, stratum order and `rng.integers(0, m, m)` draws; only `rng_for_cell` and `CLIP_EPS` are the engine's).

- **McNemar.** Sixteen tables by hand - `(10, 2)`, `(12, 12)`, `(24, 0)`, `(13, 12)`, `(25, 0)`, `(0, 7)`, `(7, 0)`, `(0, 0)`, `(1, 0)`, `(0, 1)`, `(13, 13)`, `(100, 0)`, `(0, 10)`, `(2, 10)`, `(11, 13)`, `(12, 13)`: F5 exact p `0.03857421875` = `2 (1 + 12 + 66) / 4096`, corrected statistic `49 / 12 = 4.083333`, p `0.04330814281079198`; `(24, 0)` exact `1.19e-07`, `(25, 0)` corrected `23.04` / `1.59e-06`; `(0, 7)` p `0.015625`; `(0, 0)` p 1 and no statistic; `(1, 0)` p 1; `(13, 13)` statistic `1/26`. Engine v hand: worst `0.0`; engine v statsmodels (`exact=True`; `exact=False, correction=True`): worst `1.11e-16`; `method` named the route on every table, exact at `b + c = 24`, corrected at 25. One version all correct through the assembly: `b 0, c 10`, exact, p `0.001953125`; sensitivity difference `+0.10 [+0.0090, +0.2136]`. From the F5 CSVs by hand: `b 10, c 2` at threshold 0.5.
- **Newcombe paired method 10.** Nineteen tables as I can state the steps (Wilson arms; phi from `eh - fg`, corrected to `max(eh - fg - n/2, 0)` when positive, uncorrected when negative, 0 when a margin is 0; square-and-add): F5 accuracy `(80, 2, 10, 8)` `-0.08 [-0.155356, -0.010249]` phi `0.4946`; F5 Se and Sp `(40, 1, 5, 4)` `[-0.194203, +0.026030]`; `g = 0`, `f = 0`, difference 0 `(40, 3, 3, 4)`, all correct `(100, 0, 0, 0)`, none correct, phi negative `(5, 30, 30, 5)` and `(50, 20, 30, 0)`, `e = 0`, `h = 0`, `n = 3` at both ends (`[+0.2059, 1.0]` / `[-1.0, -0.2059]`), `(1, 1, 1, 1)`, `(45, 5, 5, 45)` phi `0.78`, `(98, 1, 1, 0)`. Worst deviation `8.33e-17`; the frozen pin `(-0.1554, -0.0102)` reproduced at four decimals. The unpaired method 10 on the same margins (`82/100 v 90/100`) is `[-0.177554, +0.017463]` by my hand and by `difference_unpaired`; the paired interval differs from it by `0.0222` / `0.0277`, so the paired case does not reuse the unpaired method. Two recollections agreeing is not verification: the register row `F5-newcombe-paired` prints `no_independent_oracle`, `matched false`, `n_values_compared 0`, the `[unverified]` reason verbatim (37 `unverified` occurrences in `fixtures_report.json`) - the right status until DEC-70(a)'s fetch.
- **Paired DeLong.** R2's F3 pair by hand: `AUC1 0.80, AUC2 0.64, S_aa 0.024, S_bb 0.0392, S_ab 0.028, Var(A - B) = 0.0072` (without the covariance `0.0632`), `z 1.8856180831641274`, `p 0.0593464387919198`, Wald `(-0.0063084589, +0.3263084589)`. Engine: est `1.1e-16`, variance `6.9e-18`, bounds `1.9e-16` / `5.6e-17`, z `2.2e-15`, p `3.0e-16`; the engine's variance differs from the covariance-dropped one by `0.056`. Ties in both arms (`s3`, `s4`, two tied pairs each): variance `0.016`, deviation 0.0; a positive tied with a negative: variance `0.0052`, deviation 0.0; swapped vectors negate the estimate exactly; identical vectors refused `boundary_estimate`, method `none`. On the F5 pair's 100 rows and both `sex` levels by the same code: overall `-0.0776 [-0.158225, +0.003025]` (var `1.692e-03`, no-cov `2.904e-03`), `M` `-0.052885 [-0.172476, +0.066707]`, `F` `-0.099359 [-0.210093, +0.011376]`; engine deviations `2.8e-17`, `0.0`, `1.1e-16`. The F5 `sex` subgroup cells by hand from the CSVs: `M` Se `(20, 1, 3, 2)` `-0.076923 [-0.257257, +0.100168]`, `M` Sp `(20, 0, 2, 2)` `-0.083333 [-0.255192, +0.072045]`, `F` the mirror, each level's accuracy `(40, 1, 5, 4)`; worst deviation `6.9e-17`, every `n` equal to my `e + f + g + h`. Brier difference `0.038815` by hand, deviation 0.0. Slope difference via `statsmodels.Logit` on `logit(p)` clipped at `CLIP_EPS`: new `1.070344`, prior `1.715991`, difference `-0.645647`, engine deviation `2.2e-16`. A clustered declaration routes the AUROC to `cluster_bootstrap_percentile` with the DeLong refusal as `analytic` and `delong_refused_clustered` on the Number; T2-3 prints `cluster_bootstrap_percentile` in the Method cell of all six rows; `insufficient_clusters` 0, `n.e.` 0 on the page.
- **The clustered cells by my own draw loops** (F5 pair, `case_id = c{i // 2}`, B 200, seed 20240101, the engine's cell keys with `se` / `sp`): overall Se `[-0.200000000, +0.019607843]`, Sp `[-0.200000000, +0.020408163]`, `M` Se `[-0.230769, +0.039423]`, `M` Sp `[-0.250000, 0.0]`, `F` Se `[-0.208333, 0.0]`, `F` Sp `[-0.230769, +0.038462]` - deviation `0.0` on all twelve bounds, `n_cases` 26 / 26 / 26 / 24 / 24 / 26 equal to my group counts; the three accuracy cells `[-0.1805, +0.02]`, `[-0.18, +0.02]`, `[-0.16, +0.02]` deviation `8.3e-17` / 0.0 / 0.0; AUROC overall `[-0.173420, +0.000110]`, `M` `[-0.173197, +0.068990]`, `F` `[-0.222957, +0.000120]` deviation `2.8e-17` each; Brier `[-0.006848, +0.090637]` `1.7e-18`; slope (my own Newton IRLS at `CLIP_EPS`, `nan` on non-convergence: 197 usable of 200) `[-2.470147, +0.023018]` deviation `2.0e-14`; the i.i.d. slope `[-1.594831, +0.084238]` `8.4e-15` and Brier `[0.005039, 0.084199]` `1.1e-17`. A mixed case (`c0` holding a positive and a negative row): AUROC `[-0.1743, +0.0086]`, `n_cases 50`, Se unchanged. `Cse` (sensitivity, `ci_lower_bound >= -0.30`) `met` on `-0.19999999999999996`; `C2` `not_met` on `-0.18049999999999986`, both `cluster_bootstrap_percentile`.
- **The margin.** No paired criterion (block B): run.json `margin` 0, `paired_difference` 0, `default` 0; every `0.02` / `0.03` / `0.05` / `-0.02` / `-0.03` / `-0.05` in run.json is inside an estimate, a bound, a resample sd or an ROC coordinate (contexts captured); T2-3 has seven columns, `Margin` 0, `Status` 0, `class="status"` 0 inside the table, the caption's "no paired_difference_vs_prior criterion is declared, so no margin and no status column is printed"; on the visible page `0.02` x0, `0.03` is `0.039` x5, `0.05` is `−0.053` x2, `delta` once in "quantitative deltas", `margin` only in CSS. 24 criterion variants on the F5 accuracy cell (`est -0.08`, ci `[-0.1553564, -0.0102493]`) against my literal rule: `ci_lower_bound >= -0.05 / -0.10 / -0.1553 / 0.0 / 0.05` not_met, `>= -0.16 / -0.1554 / -0.15536 / -1.0` met, `<= -0.10` met; `ci_upper_bound <= 0.0 / -0.0102 / 1.0` met, `<= -0.10 / -0.01025` not_met, `< -0.01` met, `>= -0.05` met; `point_estimate >= -0.08 / -0.10 / -0.0800000001` met, `> -0.08` not_met, `<= -0.08` met, `<= -0.10` not_met, `>= 0.0` not_met: 24 of 24 agree, each `statistic_compared` with the compared value printed. `-0.10` on `ci_lower_bound` is `not_met` (the bound is `-0.1554`), which is what D1 section 2's literal rule gives. Row 121 on T1 (main tree, read only, `run` with the F5 criteria): T1-17 prints `paired difference vs prior version` in the Type column and the sentence "Criterion C2 (accuracy difference against the prior version, ...): not assessable (requires_compare)."
- **T2 with eleven criteria** (block D: `C1`; on the accuracy cell `Cfirst` met, `C2` not_met, `Cthird` not_met; `Cauc` met, `Cbrier` not_met, `Cslope` met; `CseF` on `sex = F` met; `CspStar` on `sex = *` not_met at both levels; `Cop1sens` not_met): every T2-3 row carries exactly its own criteria in criteria-table order (`Cfirst (row 2) ... C2 (row 3) ... Cthird (row 4)` on the accuracy row, `Cop1sens (row 11)` on the Se row, `—` on the Sp row, `Cauc` / `Cbrier` / `Cslope` on their rows); T2-4 prints `CseF` and `CspStar` on `F`, `CspStar` on `M`; the record string appears 6 times on the page = the 6 `not_met` paired rows (C1's record is a 4.7 sentence, not the table string); status words outside `.status` 0; traceability 11 rows, `4.2 (paired comparison)` x10, `4.1 (criteria echo)` x1; all 18 criterion sentences carry the `type_clause` for the ten paired rows and none for `C1`. Two operating points (block M): op1 `b 10 / c 2, 0.039 (exact)`, op2 `b 15 / c 6, 0.078 (exact)`, `C2` on op1's row only and `C2op2` on op2's, two `MCNEMAR_RESULT` sentences, nine `PAIRED_DIFF` sentences. A paired criterion with no `scope` key (D1 section 2's example) evaluates as overall.
- **Pairing.** Extra prior row, a prior row dropped, a new row dropped, a blank prior `row_id`: `HALT H12 {"unmatched": 1}` (the count, not the ids); a `y_true` flip: `H12 {"n_pairs": 99, ..., "label_mismatch": 1}`; a duplicate `row_id` in either table: `HALT H05 duplicate row_id values`; the `row_id` column absent from the prior: `HALT H07` (header-set hash); the prior in reverse row order: paired, `b 10 / c 2`, identical figures. `--allow-unpaired` on the extra-row pair: `paired false`, `label 'not like-for-like'`, `W12`, `n_prior 101`, `mcnemar null`, `subgroups []` / `unpaired_not_like_for_like`; the six difference Numbers `newcombe10_not_like_for_like` x3 with `flags ['not_like_for_like']`, `delong_wald_not_like_for_like`, Brier / slope `method none`, reason `unpaired_not_like_for_like`, flagged; `UNLABELLED []`; `C2` `not_assessable / not_like_for_like`; the page: the T2-3 caption `UNPAIRED - not like-for-like; Newcombe 10 / unpaired DeLong (new version rows: 100; prior version rows: 101; rows in one version only: 1)`, section 4.6's label, T2-4 not printed, record string 0. `--allow-unpaired` on a pair that pairs: paired, no `W12`. Identical tables: `b = c = 0`, p 1.0 exact, every proportion difference `0.0` with a Newcombe paired interval (`±0.0593` Se / Sp, `±0.0305` accuracy), AUROC / Brier / slope refused `boundary_estimate`, `C2 met` on `-0.0305`, `no difference` 0, `unchanged` 0 on the page.
- **T2 text and bytes.** Base F5 page: 5 pages, one footer each, 39 claims, 0 rejections; every `data-ref` cell resolves (`test_every_printed_number_cell_traces_to_run_json` in the suite) and, of the 128 digit runs outside those cells, every one is a raw run.json substring, a rounded Number, section / table numbering or a citation except three - `14532`, `16202`, `2740` - which are CSS hex colours (`#14532d`, `#16202b`, `#0c2740`). Forbidden words (my list of 46 including `non-inferior`, `equivalent`, `superior`, `improved`, `degraded`, `regressed`, `better`, `worse`, `significant`, `no difference`, `unchanged`, `stable`, `pass`, `fail`, `verdict`, `design partner`): on every T2 page I rendered (base, no-margin, eleven-criteria, unpaired, identical, clustered, two-op, injected, trial, expired, synthetic) the only hits are `approved` x1, `certification` x1, `endorsed` x8, all inside the cover note and long-form disclaimer as negations ("No regulator has ... approved or endorsed this tool"; "It is not a certification"). The ten T2 skeletons, `T2.html`'s and `base.html`'s static text: 0 hits. Status words outside `.status`: 0 on every page. Anchors: 19 `FDA_PCCP_*` links each labelled `(2024-12-04, updated 2025-08-18, final)`; 4 `FDA_AIDSF_*` links each `draft guidance (January 2025), not for implementation`, `draft` and `not for implementation` within 300 characters of each. Injection through the criterion id (`C<i>2</i>`), author (`{{ 7*7 }}<script>x</script>`), justification (`<script>` + `{{ 7*7 }}` + U+202E + U+200F + U+2028 + 10,000 `A`), the other justification (U+2028 + U+2029), model name (`<img src=x onerror=...>`), version (`"><b>`), prior version, operating-point source and reference description: `<script>` 0 raw / 3 escaped, `<img` / `<b>` / `<i>` 0 raw, `{{ 7*7 }}` literal x12 and `49` inside customer text 0, U+202E / U+200F escaped, the 10,000 characters inside `<div class="customer-text">`, 0 rejections, the injected criterion `not_met`, `<title>` engine text only. Marks: trial licence `TRIAL` on every page (2 / 2 / 1 / 1 / 1); expired 10 days inside a 30-day grace: `LICENCE EXPIRED - not for submission` (2 / 1 / 1 / 1 / 1), exit 0; expired past grace: exit 4, `run.json` only, the mark in the manifest; no licence: exit 4, `NO LICENCE - not for submission`, the one-line fix; sample pack `--compare` (sizes `compare.json 328,965`, `run.json 236,843`, `T1 142,924`, `T2 69,017`, `T7 47,666`, `T8 35,178` - the repair note's): `SYNTHETIC` 2 / 2 / 1 / 1 / 1 and `NO LICENCE` 2 / 1 / 1 / 1 / 1 per page, `data_marking SYNTHETIC - illustrative`, `margin` 0 in `compare.json`, `design partner` 0. 28 counter-example documents validate against `output_schema_v1.json` with 0 errors.
- **Wiring.** `socket.socket` and `create_connection` patched to raise: `compare --offline` rc 0, five files, 0 socket calls. `--templates T1`: rc 5, `error: unknown --templates id 'T1'; choose from T2, T7, T8`, no directory; `T7,T8` and `T2,T7,T8` written; `--format json` writes the four JSON files. F17: two compares in one home differ, after masking `run_id` / `started` / `duration_s`, only in `manifest.ledger_count` 1 -> 2, `ledger.acceptance_runs` 1 -> 2, `comparison.ledger.prior_acceptance_runs` 0 -> 1, and the pages in 9 lines (run id in the title / headers / cover). `import proofpack`, `proofpack.stats`, `stats.comparison`, `stats.proportions`, `stats.discrimination`, `stats.bootstrap`, `criteria` with `statsmodels`, `sklearn`, `scipy` and `jinja2` hidden by a meta-path blocker: `imports ok`, `loaded: []`. Timing (`tests/test_e10_timing.py -s`): `compare --templates T2 23.91 s; T2 render from run.json 0.03 s; T2 73214 bytes`, `1 passed in 24.77s`.
- **Suite, markers, lint, sweep (CRLF worktree at `6c4e253`).** `python -m pytest -q -p no:cacheprovider`: `1651 passed, 2 skipped, 1 xfailed in 198.00s` (the second skip is `PROOFPACK_TR39_FULL` unset in my run; the note's `1652 / 1` has it set); `-m day10 68 passed`; `day9 328`; `day8 469 passed, 1 skipped` (the same skip); `day7 139`; `day6 285`; `day5 54`; `ap3 167`; `ap2 90`. `ruff check .` `All checks passed!`; `ruff format --check .` `245 files already formatted`. `scripts/mutation_sweep.py --marker day10`: `18 planted, 18 killed, 0 survived; 202 s`, exit 0, worktree clean after; `--list` 349 lines. `proofpack fixtures --offline`: `rows 46: matched 33, not matched 0, no oracle recorded 2, no independent oracle 1, not built 5, compared by the test suite only 5`, exit 0; `F5-mcnemar matched 7.6e-17`, `F5-register matched 3.3e-05`, `F5-delong-pair matched 2.5e-16` (against the capture script's own O(m n) variance, named as a formula capture, not a library), `F5-newcombe-paired no_independent_oracle`. `capture_fixture_oracles.py --check`: `72 identical, 0 differ`. `tr39_subset.py --check`: `vendored subset matches`. `doctor --offline`: exit 0, 17 `[ok` lines.
- **Damage elsewhere.** `git diff 667a201 6c4e253 --name-status`: 8 files (the two lens-2 notes, `mutation_sweep.py`, `render/t2.py`, `stats/discrimination.py`, `templates/T2.html`, `test_e10_comparison.py`, `test_e10_criterion.py`). `discrimination.py`'s change is docstring-only (both versions parsed with `ast`, docstrings stripped, dumps equal); `t2.py`'s is the `_margins_for` code. `stats/comparison.py`, `stats/proportions.py`, `stats/bootstrap.py`, `criteria.py`, `gates.py`, `run.py`, `cli.py`, `narrate/`, `schema/`, `design/tokens.json`, `tests/fixtures/`: 0 diff lines, so no interval function, constant or gate changed in this repair. The tests diff adds no `skip` / `xfail` / `TODO` and removes no `assert`; the golden is untouched and passes in the CRLF worktree. The `day10` marker carries E10's text in `pyproject.toml` line 92.

## What I could not check

- Newcombe 1998 (paired) itself: not fetched (DEC-70(a) is day 11's); my derivation is a fourth recollection of the same steps, and the `no_independent_oracle` row is the right status.
- The subgroup AUROC cells' PowerShell figures, and any PowerShell run at all: Git Bash only today.
- T2 in a browser or in print: bytes only.
- A pROC capture of the paired DeLong: none exists (F13 xfail stands).
- Injection through a subgroup level value: `H07` halts before the page (N6), so what T2 would print for a level value carrying markup was not measurable from the CLI.

## Sentences I refused to write

- "The T2-3 placement rule is tested" - refuted by N1's three survivors.
- "The clustered comparison is verified" - twelve cells of one fixture under one grouping plus one mixed case; FA-F4 (a prior grouping that disagrees) is still unresolved.
- "The Newcombe paired interval is verified" - no primary source.
- "The ledger counts comparisons as DEC-47 says" - refuted by N2's two exit-4 compares.
- "No forbidden word reaches T2" - written instead: none outside the disclaimer's negations, on the eleven pages I rendered, with the three words and their counts.
- "The full suite passes" - written with the count and the one environment-dependent skip.

## Re-run these

`<scratch>` = `C:/Users/joshs/AppData/Local/Temp/claude/C--Users-joshs-GPS-ProofPack/0e5d0ccc-8634-4ba1-aad8-15203d8e0928/scratchpad/lens-E10-r3-fresh-attack`; `<wt>` = a detached `6c4e253` worktree; `PYTHONPATH=<wt>/src`, `PYTHONIOENCODING=utf-8`, run from `<wt>`.

| Command | Result |
|---|---|
| `python <scratch>/hand.py` | McNemar worst `0.0` (hand) / `1.11e-16` (statsmodels); Newcombe paired worst `8.33e-17`; DeLong F3 deviations above (`hand_out.txt`) |
| `python <scratch>/cx.py <wt> <out> A B N I` / `C` / `D` / `F` / `G` / `H` / `J K` / `M` / `S` | blocks above (`cx/*.txt`); block D is the eleven-criteria page, block K the ledger probe (N2), block M `ppv` / `f1` (N3) |
| `python <scratch>/g2.py <wt> <out>` | the six Se / Sp cells deviation `0.0` with the engine's key names; the two slope loops `2.0e-14` / `8.4e-15` |
| `python <scratch>/mutants.py <wt>` (then the six survivors with `LENS_TESTS` set to the criteria / E9 / CLI files) | 24 planted; L1, L2, L9 survive every test; L4, L19 survive every behavioural test (`mutants_out.txt`, `mutants_out2.txt`) |
| `python -m pytest -q -p no:cacheprovider` | `1651 passed, 2 skipped, 1 xfailed in 198.00s` (`full_suite.txt`) |
| the eight markers and lint | `markers.txt` (counts above) |
| `python scripts/mutation_sweep.py --marker day10` | `18 planted, 18 killed, 0 survived; 202 s` (`sweep_day10.txt`) |
| `python -m pytest -q -s tests/test_e10_timing.py` | `23.91 s; 0.03 s; 73214 bytes` |
| pre-fix: the two repair-2 test files in a `667a201` worktree | `2 failed, 3 passed in 2.43s` |
| `python <scratch>/diffcheck.py` | `stats/discrimination.py docstring-only change: True`; `render/t2.py CODE CHANGED` |
