# Handoff - 2026-09-07 - Lane E (engine) - E1 "Validated ingest and declarations"

## 1. What landed

Repo `proofpack`, branch `main`, one commit `day1 E: validated ingest and declarations`.
Python 3.12.11 via `uv python install 3.12` (no fallback to 3.11 was needed).

**Tooling**
- `pyproject.toml` (hatchling; runtime deps numpy, pyyaml, jsonschema; optional extra `stats` = scipy; dev group pytest, hypothesis, ruff, pip-audit, scipy), `uv.lock`, `.gitignore`.
- ruff (E, F, W, I, B, UP, N; line length 100) - clean, formatted.
- pytest markers `day1` … `day10`, `fixture`, `slow`.
- `.github/workflows/ci.yml`, four jobs on every push/PR: (1) ruff + `pytest` + `pytest -m day1` + `proofpack doctor --offline`; (2) `pip-audit --strict`; (3) `uv build --wheel`, checks the wheel carries the packaged schemas, uploads the wheel artefact; (4) clean venv with runtime deps only, asserts scipy is absent, imports every day-1 module, asserts `scipy` not in `sys.modules`, runs `doctor --offline`.

**Files**
- `schema/schema_v1.json` - JSON Schema 2020-12 for one canonical row (D1 §1 prose), plus `x-proofpack` block: canonical column list, attribute set, missing-token convention, identifier pattern, H-code list.
- `schema/criteria_schema.json` - full `criteria.yaml` schema (D1 §2): mandatory blocks, authored fields, operating-point rule/provenance enums, egress suppression minimums (customer may only tighten), metric-id enum in `$defs`.
- `schema/claims_schema.json`, `schema/egress_schema.json` - skeletons (D1 §4.4, §6): telemetry payload with `additionalProperties: false`, aggregate cell shape with pseudonymised level pattern, forbidden-token list recorded for the later checker.
- `design/guidance_map_v1.csv` - 32 internal ids (all D4 §13 ids, split out one per row) with document, version_date and status filled; **`section` and `estar_section` are blank for Josh's transcription**; D4's seed hints are in `notes`. A test enforces that `section` stays blank until transcribed.
- `handoffs/README.md`, `README.md`.

**Modules (`src/proofpack/`)**
- `errors.py` - exit codes 0/2/3/4/5, `HALT_CODES` H01-H12 (verbatim meanings), S01-S04 for structural schema failures (also exit 3), `HaltError`, `Finding` (warnings W06/W10/W12).
- `io/schema.py` - CSV/JSON loader, missing tokens -> None, typed `Table` (numpy), attributes keep "Unknown/missing" rows, `attr_*` numeric/categorical split, `rater_*` echo-only, unused-column list, **date coarsening at ingest** (quarter/month/year; the raw date column never reaches `Table`), flow counts (included / excluded-missing-y_true / excluded-missing-score / indeterminate; dev rows never analysed), order-independent `header_set_sha256`.
- `io/declare.py` - YAML loader + jsonschema validation; H08 for missing mandatory blocks (`classes`, `score`, `operating_points`, `prevalence`, `subgroups`) and for the declared-never-inferred blocks (`reference_standard`, `indeterminates`, `clustering`), and for empty author/date/justification on any criterion or the fairness block; H09 for unknown metric id / operating point; `check_references()` gives H09 for attribute/level references not observed in the table and H08 for a numeric `age` column with no declared bands. Nothing here defaults anything.
- `io/mapping.py` - **header-only mapper stub**: exact canonical name -> high, D1 §5.2 synonym table -> medium, else unmapped; `mapping.json` read/write (hash, roles, confidences, `decided_by`, timestamp; `value_summaries` empty until the mapping day); `--yes` rule (prior mapping.json + equal hash + every role high) -> otherwise H07; H11 from header names (`event_date`, or any header containing date/dt/time/timestamp/datetime/dob) not covered by the `period` declaration.
- `gates.py` - H01 (AUROC by Mann-Whitney midranks, numpy only, oracle-tested against `scipy.stats.mannwhitneyu`), H02, H03, H04 (per operating point, mismatch count in detail), H05 (row_id duplicates; case_id conflicts only when `clustering.unit: none`), H06 (HALT overall; W06 per single-class site), H10 (flag W10 only, exit 2), H12 (`check_paired`, `--allow-unpaired` -> W12 "not like-for-like"); `ingest()` pipeline; `IngestResult.report()` aggregate-only.
- `doctor.py` + `cli.py` - `proofpack doctor [--offline]`, `map`, `run` (ingest + gates; writes `ingest_report.json` + `mapping.json` on success only), `compare` (day-1: paired ingest + H12 only). Global flags accepted before or after the subcommand. HALT -> exit 3, message + coarse detail on stderr, "No document was written."
- `scope.py` - D4 §7.1 short form and §7.2 eight long-form items, verbatim, as constants for the renderer.
- `resources.py` - schemas and the guidance map ship inside the wheel as `proofpack/_schema/`; dev checkout reads `schema/` and `design/` directly.

**Tests (`tests/`, 52 collected, all `day1`)**
- `conftest.py` - seeded `make_cohort()` (numpy Generator; sigmoid scores with class separation, sex/age/site, optional row_id/case_id/y_pred) and `make_criteria()` (complete valid declaration) - test scaffolding only, not the F18 generator.
- `test_halt_gates.py` (F12) - each of H01-H09 and H11 fires through the API with the right code **and** through the CLI with exit 3 and an empty/absent `--out`; H10 verified as flag-only (exit 2, report written); H12 via API and via `proofpack compare` (exit 3, nothing written; `--allow-unpaired` -> exit 2); a completeness test asserts the scenario set equals `HALT_CODES`; plus H05/H06/H07/H08/H09/H11 variants and an aggregates-only assertion (HALT detail never echoes a cell value or an original header).
- `test_schema_fuzz.py` (hypothesis) - 300 examples of mixed-type tables (NaNs, junk strings, booleans, duplicate ids, single-class sites, unknown levels, bad dates, missing score) x mutated declarations: ingest either succeeds with consistent flow counts, "Unknown/missing" retained, dates coarsened, probabilities in [0,1] - or raises a known H/S code, never anything else; declaration fuzz (dropped keys, junk values) yields only H08/H09; authored-field blanks always H08; date coarsening never keeps the day; AUROC oracle vs scipy (skips if scipy absent). The fuzz found one real crash during the session (non-list `criteria` block -> TypeError), fixed.
- `test_doctor_cli.py` - doctor passes, both flag positions, `--json-log`, `--version`, no scipy in the import graph, schemas load, H-code list, metric enum, guidance-map structure with blank sections, scope strings, egress skeleton rejects a row-level field.

**Exact commands and pasted results**
```
$ uv run pytest -q -p no:cacheprovider
51 passed, 1 skipped in 5.00s          # skip: write-access failure test cannot run as root
$ uv run pytest -q -p no:cacheprovider -m day1
51 passed, 1 skipped
$ uv run ruff check . && uv run ruff format --check .
All checks passed! / 15 files already formatted
$ uv run pip-audit --progress-spinner off
No known vulnerabilities found
$ uv build --wheel --out-dir dist      -> dist/proofpack-0.1.0.dev1-py3-none-any.whl (contains proofpack/_schema/*)
$ uv venv --python 3.12 .venv-noscipy && uv pip install --python .venv-noscipy/bin/python .
$ .venv-noscipy/bin/python -c "import importlib.util,sys; assert importlib.util.find_spec('scipy') is None; import proofpack.cli, proofpack.gates; assert 'scipy' not in sys.modules"
$ .venv-noscipy/bin/proofpack doctor --offline   -> "All essential checks passed."  exit 0
```

## 2. What was cut and why

Nothing from the brief was cut. H09-H12 landed (the "cut if behind" was not exercised). `io.declare` is complete.

Deliberately *not* built today because the brief scopes them to later days: value-level mapper summaries and interactive acceptance (mapping day), `proofpack declare` scaffold, licence verification (F20), telemetry POST (no outbound call exists anywhere in the code today), both-way indeterminate tables, any statistic other than the H01 AUROC check.

## 3. Acceptance line

"`proofpack doctor` passes in a clean 3.12 venv; scipy-free import passes; every H-code has a test." - **True**, demonstrated above: doctor exit 0 in a fresh venv with runtime deps only and scipy absent; the scipy-free import assertion passes locally and is a CI job; `test_every_halt_code_has_a_scenario` proves H01-H12 coverage against `HALT_CODES`.

Note: CI has not run yet because nothing was pushed (per instructions). The workflow was written to mirror the local commands exactly; first push will tell.

## 4. What tomorrow needs

- Start from `src/proofpack/gates.py::ingest` - it returns `IngestResult` with `table`, `declarations`, `mask` (analysable rows) and `flow`; the statistics modules consume `table.score[mask]`, `table.y_true[mask] == declarations.positive`, `table.attributes[...]`, `declarations.operating_points`.
- `stats.proportions` (Day 2 per D1 §8) can use `gates.auroc_mann_whitney` as the discrimination seed; Clopper-Pearson needs scipy - import it *inside* the function so the scipy-free job stays green.
- Both-way indeterminate handling: `schema.analysis_mask` currently excludes indeterminate rows and counts them; the as-positive/as-negative masks are a small extension there.
- `io.mapping.Mapping.value_summaries` is the slot for the suppressed value-level summaries.
- Fixture register F1-F8 values come from R2 §9 - do not retype them from memory; read them from the research doc.

## 5. Needs from Josh

- Transcribe section numbers into `design/guidance_map_v1.csv` (`section`, `estar_section`) from the three fetched PDFs. Keep `internal_id` unchanged - tests and (later) templates key on it. `notes` may be edited freely.
- Confirm the mandatory-block decision in §6 Q1 below before the design partner writes a real `criteria.yaml`.
- No secrets or accounts were needed today and none are needed for Day 2.

## 6. Open questions (≤5)

1. **Mandatory blocks.** The brief lists five mandatory declarations (positive class, orientation, threshold, prevalence, attributes). D1 §5.4 also says `reference_standard`, `indeterminates` and `clustering` are "never inferred". I made all eight HALT H08 when absent, on the "engine never supplies defaults" rule. If a trial customer should be allowed to omit the last three, say so and I will downgrade them to W-codes.
2. **H06 site rule.** D1 reads "single class in every site when `site` present -> warning W06 instead". I implemented: overall single-class -> H06 HALT; otherwise any single-class site -> W06 (count in detail). Is "every" meant literally (warn only when *all* sites are single-class, which cannot coexist with a two-class overall)?
3. **H11 breadth.** Today H11 triggers on header *names* only (`event_date` or containing date/dt/time/timestamp/datetime/dob). Value-level date sniffing (an ISO-looking column named `visit`) waits for the mapper. Acceptable for the design partner's first pack, or should value sniffing move up?
4. **Period column coverage.** A second date-like column not named in `period.column` HALTs H11 even when `period` is declared (privacy reading). Confirm that is the intended strictness.
5. **Criteria `type` enum.** D1 §2 shows `paired_difference_vs_prior`; I added `point` as the implicit default so the schema has a closed enum. Name acceptable?
