# Lane E - build day 10 (E10) - lens 3, regression-and-record, at `6c4e253` (repair 2 of `667a201`; round ran on Saturday 26 September 2026)

**PASS. No blocker. Every figure in the repair-2 note that I re-measured reproduces in a fresh CRLF worktree of `6c4e253`, in both shells: full suite `1652 passed, 1 skipped, 1 xfailed` (Git Bash 181.17 s, PowerShell 178.81 s); `-m day10` `68 passed` with the timing page at `73214 bytes` in both; `day9 328`, `day8 470`, `ap3 167`, `ap2 90`; `ruff check` clean, `ruff format --check` `245 files already formatted` (`--verbose`: 246 included paths = 140 `.py` + 105 `.md` + `pyproject.toml`, RG-N8 as the note explains it); the sweep list 349 lines with 18 day-10 mutants; the day-10 sweep `18 planted, 18 killed, 0 survived` (Git Bash 203 s; PowerShell 204 s, exit 0 in both); the sample pack `236,843 / 142,924 / 47,666 / 35,178 / 328,965 / 69,017` in both shells and, after masking run ids and timestamps, `T2.html`, `T1.html` and `compare.json` are byte-identical between `6c4e253` and `667a201` (0 differing lines each - the note inferred this from sizes; I diffed); the fixtures report `rows 46: matched 33, not matched 0, no oracle recorded 2, no independent oracle 1, not built 5, compared by the test suite only 5`; the capture check `72 identical`; `tr39` `vendored subset matches`; `doctor --offline` exit 0, 17 `[ok`; F17 `identical under the mask: yes`. The two tests the note says fail at `667a201` fail there on the assertions it quotes; the three FA-F3 tests pass at `667a201` unmutated (as the note says) and each kills the mutant it names when that mutant is planted at `667a201` with the new files. My own McNemar, Newcombe method 10 and Sun-Xu DeLong derivations agree with the engine to at most `2.2e-15` (the Newcombe agreement is between two recollections, not an oracle). One sentence violation (N1: a docstring and a mutant description say a second criterion's status "reached no table" at `667a201`; it reached two), one pre-existing word on every page that the brief's list names (N2, `non-inferiority margins`, in `scope.py` since day 1), and four records follow.**

Setup: detached worktrees `scratchpad/lens-E10-r3-regression/wt6c4` (`6c4e253`) and `wt667` (`667a201`), both CRLF (`git ls-files --eol`: `i/lf w/crlf` on `render/t2.py` and the golden); `PYTHONPATH=<wt>/src` on every call and proved (`proofpack.__file__` printed the worktree's `src/proofpack/__init__.py` first in every capture, both shells). Nothing committed; the main tree carries only this note (`git status --short` empty before and after the one main-tree run, the E9 rerun block); both worktrees removed at the end. All figures below are mine, measured on 26 September 2026. The suite in each shell ran beside other jobs of mine; timings are contended.

## Blockers

None.

## Non-blocking records

| # | Record | Evidence |
|---|---|---|
| N1 | **Sentence (hard rule), two places: "at 667a201 the row printed the first such criterion only, so a second criterion's `not_met` status and its record string reached no table"** (`src/proofpack/render/t2.py` module docstring, the Margins paragraph added by `6c4e253`) and the mutant `t2_first_paired_criterion_only`'s `what` ("a second one's status and record string reach no table", `scripts/mutation_sweep.py`). Counter-example run at `667a201` (worktree, `PYTHONPATH` forced): the two-criteria F5 page (`Cfirst` met, `C2` not_met) carries `C2</span>` twice and `criterion not met` four times - in the criteria table ("One row per evaluated criterion", `C2`'s row: `−0.155 / criterion not met / statistic_compared`), in section 4.7 (the `C2` statement "observed −8.0 [−15.5, −1.0] - criterion not met." and the line "Record of criterion not met: C2, overall, op1, ...") and in table T2-6 (`2 / 4.2 (paired comparison) / C2 / criterion not met`). What reached no table at `667a201` is the lower-case record string `record of criterion not met (PCCP §VII.B(3) requires recording)` (0 on the page) and `C2` on the T2-3 accuracy row (0) - which is what lens 2's block I measured (`Cfirst 1 C2 0 record string on page 0`, on the row). The second half of the sentence is true; the first half is not. The test's docstring ("the row printed `Cfirst` only, `C2` nowhere, and the record string reached the page 0 times") is about the row and holds. Write: "the T2-3 row printed the first such criterion only; a second criterion's status reached the criteria table, section 4.7 and T2-6 but not T2-3, and the record string reached no table". | `scratchpad/lens-E10-r3-regression/probe667_two/` (run.json); the context dump in this session |
| N2 | **`non-inferiority margins` is visible on every T1 and T2 page** (once each on the sample pack's `T1.html` and `T2.html`, once on each of my seven F5 T2 pages): the scope statement `src/proofpack/scope.py:40` "...the acceptance criteria, non-inferiority margins, subgroup bounds and fairness criterion of ..." - the sentence naming what the manufacturer authors. Added by `8e98728` (day 1, 7 September); not touched by E10 or its repairs. The brief lists `non-inferior` among the words that appear on no rendered page; lens 2's whole-word grep (0 hits) does not match the longer word, and the checker's list (`narrate/checker.py:247`, `non-inferior` / `noninferior`) is a claim-sentence rule the scope paragraph does not pass through. It names the customer's declared margin, not a verdict, and no default margin is anywhere near it (T2-3 with no paired criterion: seven headers, `Margin` 0, `Status` 0). Recorded for Josh / the day-11 E list, not graded against `6c4e253`. | `sp_wt6c4/T1.html`, `sp_wt6c4/T2.html`; `git log -S"non-inferiority margins" -- src/` |
| N3 | **"Fails pre-build" for the three FA-F3 tests is by mutant, not by commit.** The note says so plainly and I confirm it: with the two changed test files copied into the `667a201` worktree, the five new or changed ids give `2 failed, 3 passed in 2.48s` - `test_two_paired_criteria_on_the_accuracy_cell_both_print_on_the_t2_3_row` on `assert (1 == 1 and 0 == 1)` (the `Cfirst</span> (row 1)` / `C2</span> (row 2)` counts) and `test_paired_delong_has_three_callers_in_src_as_its_docstring_names` on `assert 'Its callers in ``src`` are three' in '"""``stats.discrimination``...'`, both the assertions the note quotes. The three FA-F3 tests pass on the unmutated `667a201` (the engine code is right there) and fail under the mutant each names, planted at `667a201` with the new files (`-x` off, all three E10 files): M5 -> `3 failed, 44 passed`, the Se/Sp re-run test plus the two tests above; M10 -> `3 failed, 44 passed`, the `clustered_by_case` re-run test plus the two; M11 -> `4 failed, 43 passed`, the fourteen-cells test and the `clustered_by_case` re-run test plus the two. Under the note's `-x` figures (`1 failed, 11 / 13 / 12 passed`) the same ids fail first. | `scratchpad/lens-E10-r3-regression/plant.py` (my planter; CRLF normalised before the regex, or M10's `^...$` pattern matches 0 times in a CRLF worktree - the sweep reads in text mode, so the sweep is not affected) |
| N4 | **The note's "Table T2-4 already listed every criterion per level" - measured, holds.** Two `paired_difference_vs_prior` criteria on `sensitivity`, scope `sex = F` (`CsF1` `point_estimate >= -0.10` met, compared value `-0.0833`; `CsF2` `ci_lower_bound >= -0.05` not_met, `-2552`): T2-4 carries `CsF1` x1, `CsF2` x1, the record string x1, three status cells; T2-3 carries neither and its own accuracy criterion; the record string twice on the page (one per `not_met` line, one table each). | `probe_out/subgroup/pack/run.json` |
| N5 | **The task's "the acceptance case (margin -0.05 -> not_met) is a test that fails at 667a201"** cannot hold in this round: that test (`C2` in `tests/fixtures/f5/criteria.yaml`, `ci_lower_bound >= -0.05` on the F5 accuracy difference `-0.08 [-0.1554, -0.0102]`, `not_met`) was built at `322c5a4`'s ancestors and passes at `667a201` by design; the commit it fails against is `81f1102` (lens 1 regression: the five day-10 modules fail at collection there). At `6c4e253` the CLI run of the F5 pair gives `criteria rows: 0 met, 2 not met, 0 not assessable` (`C1` and `C2`), run.json validates, and my twelve-variant probe of lens 2 is not repeated (`stats/comparison.py` and `criteria.py` are unchanged between `667a201` and `6c4e253`: `git diff` 0 lines). | `probe_out.txt` |
| N6 | **Golden masking, measured at `6c4e253`:** the CLI's own T2 page differs from the golden in 30 lines, every one the run id (title, page headers, the Run id cell), `started` (footers) or the two input hashes; re-rendered from `run.json` with the four keys `run_id`, `started`, `input_sha256`, `prior_input_sha256` masked: 0 differing lines, 61,072 bytes both sides; with `duration_s` set to `12345.678` under the mask: 0 differing lines (it reaches nothing on the page); with `started` left unmasked: 12 lines. The test's eleven-key mask is over-broad by seven harmless keys, as lens 2 recorded; the task's "run_id, started and duration_s" is two keys short (the hashes) and one key over (duration_s). `git diff 667a201 6c4e253 -- tests/fixtures/golden/` is empty: the golden was not regenerated and `test_golden_t2_matches_the_committed_render` passes in the CRLF worktree without regeneration. | `probe_out.txt` |

## What I could not break (re-measured, with deviations)

**Suite, markers, lint (worktree `wt6c4`, CRLF).** Git Bash `1652 passed, 1 skipped, 1 xfailed in 181.17s`; PowerShell `1652 passed, 1 skipped, 1 xfailed in 178.81s` (the skip: `test_doctor_cli.py:57` "write access cannot be revoked for this user"; the xfail: F13 pROC pending the day-12 capture). `-m day10` `68 passed, 1586 deselected` (Git Bash 42.59 s, PowerShell 40.17 s); the timing test with `-s`: `compare --templates T2 23.96 s; T2 render from run.json 0.03 s; T2 73214 bytes` (Git Bash), `23.22 s; 0.03 s; 73214 bytes` (PowerShell). `-m day9` `328 passed`, `day8` `470 passed`, `ap3` `167 passed`, `ap2` `90 passed` (Git Bash). `ruff check .` `All checks passed!`; `ruff format --check .` `245 files already formatted` (both shells). 1652 - 1649 = the three new tests (`_both_print_on_the_t2_3_row`, `_has_three_callers_in_src_as_its_docstring_names`, `_auroc_brier_slope_cells_equal_a_clustered_by_case_rerun`); 68 - 65 the same three. `day10` is in CI: `ci.yml` reads every `day\d+` marker from `pyproject.toml`, where `day10` carries E10's text (line 92). The old id `test_f5_case_id_c_i_over_2_carries_dec_09s_refusal_on_all_fourteen_cells` at the new tree: `ERROR: not found`.

**Nothing weakened.** `git diff --name-status 667a201 6c4e253 -- tests/`: 2 `M` (`test_e10_comparison.py`, `test_e10_criterion.py`), 0 `D`. Grep of the test diff for added or removed `pytest.mark`, `skip`, `xfail`, `.only`, `todo`: nothing (the module-level `pytestmark = pytest.mark.day10` covers the new functions). `git diff 667a201 6c4e253 -- schema/ design/tokens.json src/proofpack/cli.py src/proofpack/run.py src/proofpack/criteria.py src/proofpack/gates.py src/proofpack/stats/proportions.py src/proofpack/stats/bootstrap.py src/proofpack/stats/comparison.py src/proofpack/narrate/`: 0 lines - so no interval function changed and no statistical gate moved in this round; `stats/discrimination.py`'s two hunks are docstring prose only (every `+`/`-` line read). The diff stat is 8 files: the two lens-2 notes, `mutation_sweep.py` (+49), `render/t2.py` (48 lines), `discrimination.py` (29), `templates/T2.html` (6), the two test files (+136 / +44).

**The FA-F5 fix on inputs beyond the test's (all at `6c4e253`, F5 pair, through `assemble_compare` + `write_run` + `render_t2`; every run.json 0 schema errors).** Two criteria on accuracy/op1 (the test's): Margin cell two `<br>` lines `Cfirst (row 1): point estimate >= −0.1` / `C2 (row 2): ci lower bound >= −0.05`, Status `criterion met` / `criterion not met - <record>`, record string once, status words outside `.status` elements 0. Three (met, not_met, not_met with `C3` `ci_upper_bound <= -0.20`): three lines each, record string twice (one per `not_met` line). Declared in the other order (`C2` first): `C2 (row 1)` then `Cfirst (row 2)`, statuses in that order. Two on the AUROC row (no operating point; `Cauc1` `ci_lower_bound >= -0.20` met, `Cauc2` `point_estimate >= 0.0` not_met): both lines on the `auroc` row, record once. Identical tables (prior = new): two AUROC criteria both `not_assessable / no_interval`, the Status cell `not assessable (no_interval)` twice, record 0, the accuracy criterion `met` on its own row as `(row 3)`. No paired criterion at all: T2-3's header is seven columns, `Margin` 0, `Status` 0, `class="status"` 0 inside T2-3, record 0. Whole-word grep for `non-inferior`, `equivalent`, `superior`, `improved`, `degraded`, `regressed`, `better`, `worse`, `significant`, `inferior` on the visible text of every page: 0 (N2 is the longer word).

**Bytes unchanged where the note says they are.** Sample pack `--compare` at `6c4e253` and at `667a201`, same shell, masked for run ids and ISO timestamps: `T2.html` 0 differing lines, `T1.html` 0, `compare.json` 0; sizes `run.json 236,843 / T1 142,924 / T7 47,666 / T8 35,178 / compare.json 328,965 / T2.html 69,017` at both commits and in PowerShell. Marks on the sample T2: `SYNTHETIC` 8, `NO LICENCE` 7. F17 `--n 400 --compare`: `run exit codes [4, 4]; identical under the mask ['run_id', 'started', 'duration_s']: yes`. Schema (unchanged since `322c5a4`): the F5 CLI `run.json`, seven probe `run.json` documents, the sample pack's `run.json` and `compare.json`, F17's two, and the site's `dist/sample-pack/run.json` and `public/sample-pack/run.json` (read only): 0 errors each.

**Oracles, by my own arithmetic (`scratchpad/lens-E10-r3-regression/hand.py`, pure Python above its ENGINE line).** From the two F5 CSVs at threshold 0.5 `>=`: `n 100, b 10, c 2`, accuracy table `(e, f, g, h) = (80, 2, 10, 8)`, Se and Sp tables both `(40, 1, 5, 4)` - R2's figures. McNemar exact `2 * (1 + 12 + 66) / 4096 = 0.03857421875`: engine `0.03857421875` (`exact_mcnemar`, the route `exact=None` takes), statsmodels 0.15.0 `exact=True` `0.03857421875`, deviation `0.0`; corrected statistic `(|10 - 2| - 1)^2 / 12 = 4.083333333333333`: engine and statsmodels (`exact=False, correction=True`) both, deviation `0.0`; corrected p `erfc(sqrt(s / 2)) = 0.04330814281079198`: engine the same, statsmodels `0.04330814281079206`, deviation `7.6e-17`. Newcombe paired method 10 by my own Wilson bounds and the phi step as recalled (`max(eh - fg - n/2, 0)` when `eh > fg`): accuracy `-0.08 [-0.1553563811, -0.0102492919]`, phi `0.4946`, engine deviations `6.9e-17 / 0.0 / 1.2e-16`; Se `[-0.1942033208, +0.0260296201]`, deviations `1.1e-16 / 9.7e-17`. Two recollections agreeing is not verification: the register row `F5-newcombe-paired` is `no_independent_oracle`, `matched false`, `n_values_compared 0`, reason `[unverified] ...`, and `fixtures_report.json` carries 37 `unverified` occurrences. Paired DeLong on R2's F3 pair, O(m n) psi placements and the covariance sums written out: `AUC1 0.80, AUC2 0.64, S = [[0.024, 0.028], [0.028, 0.0392]], Var(diff) 0.0072` (no-covariance `0.0632`), `z 1.8856180831641274, p 0.0593464387919198, CI (-0.0063084589, +0.3263084589)`; engine (`delong_wald`, `n_pos 5, n_neg 5`) deviations est `1.1e-16`, variance `6.9e-18`, bounds `1.9e-16 / 5.6e-17`, z `2.2e-15`, p `3.0e-16`; the engine's variance differs from the no-covariance variance by `0.0560`. The bootstrap cells: the committed `clustered_by_case` and `clustered_flat` re-run tests pass in the worktree (the same resampler, seed and cell key; `-0.0776 [-0.1734, +0.0001]` and `0.0388 [-0.0068, +0.0906]` pinned); I did not write a third draw loop (lens 2 did, deviations `2.8e-17` / `1.7e-18`, and `comparison.py` is unchanged since).

**Carried items still reproduce at `6c4e253` (so the carried list is current, not stale).** Lens 2's block K3 (FA-F4): the prior's `case_id` grouping shifted -> `no halt; warnings []`, the comparison's Se cell `n_cases 26` beside `prior own sens n_cases 28`. Block L (FA-F6 a): no `ledger` block -> `warn_limit None`, the page sentence "...declared ledger limit ( ledger.warn_after_acceptance_runs ) is  no limit declared ." Block I (the fix): `Cfirst 1 C2 1 record string on page 1`. The repair-1 note in `workflows/notes-day10/E10_repair1.md` carries the corrections the repair-2 note lists: "independent of the checkout" 0 occurrences; the two subgroup accuracy cells named (`sex = F [-0.180, +0.020] -> [-0.160, +0.020]`, `sex = M [-0.180, +0.0005] -> [-0.180, +0.020]`); the `plan_clustering("case_id", None, 100)` `ValueError` run cited; the non-existent backup corrected (RG-N6).

**Sweep, fixtures, captures, doctor.** `mutation_sweep.py --marker day10` in the worktree: Git Bash `18 planted, 18 killed, 0 survived; 203 s`, exit 0, every row `killed` including the four new ones (`clustered_refusal_n_is_all_pairs`, `clustered_auroc_brier_slope_resampled_unstratified`, `clustered_auroc_bootstrap_statistic_sign_flipped`, `t2_first_paired_criterion_only`); PowerShell `18 planted, 18 killed, 0 survived; 204 s`, exit 0. `--list` 349 lines, 18 `day10`. `fixtures --offline`: `rows 46: matched 33, not matched 0, no oracle recorded 2, no independent oracle 1, not built 5, compared by the test suite only 5`, exit 0 (both shells). `capture_fixture_oracles.py --check`: `captured values: 72 identical, 0 differ within their tolerance, 0 differ outside it`. `tr39_subset.py --check`: `vendored subset matches`. `doctor --offline`: exit 0, 17 `[ok` lines, last line `Next step: proofpack declare ...` (both shells).

**E9 r3 re-run block (main tree at `6c4e253`, `bash <scratch9>/rerun/rerun.sh e10r3`, `<scratch9>` = `.../498a4e52-21b2-438d-8861-2ecc4702fe63/scratchpad`).** 248 lines against the repair-2 capture's 249 (`scratchpad/repair-E10-r2/rerun_bash_r2.txt`); after masking run ids, `pp-b4-*` temp names, the tag and timings: 0 differing block lines - the one extra line in the repair-2 capture is a trailing `exit=0` its runner appended after the block. So every line the note lists (doctor `exit=0 ok_lines=17`, tr39, `lines=349` with `18 day10`, the no-licence run, H07, html_run, repros5 / 6, p3_fair, p2_star, p7_site, alias, offline `socket calls 0`, paths, imports `loaded: []`, the verdict grep `5 passed, 1649 deselected`, E6 schema, k1_checker, `245 files already formatted`, the `wc -l` diff counts `7934` / `887`) prints as the note prints it. Main tree `git status --short` empty before and after.

## What I could not check

- **Newcombe 1998 (paired) itself** - not fetched (DEC-70 a: day 11 E tries once for a free copy); my derivation is a recollection, the same status as the note's and both earlier lenses'.
- **The subgroup AUROC cluster-bootstrap cells by a draw loop of my own** - the committed test's re-run with the repository's resampler is what I ran.
- **The `-m day9` `1 failed` the note recorded under load** - not seen here (`328 passed` alone, and the full suite passes every day-9 test in both shells); not chased.
- **T2 in a browser or in print** - bytes only.
- **The PowerShell E9 rerun block (`rerun_r3h.ps1`)** - not repeated.

## Sentences I refused to write

- "T2-3 prints every criterion." Written: every `paired_difference_vs_prior` criterion on the row's metric, operating point and overall scope, measured with two and three on the F5 accuracy cell, two on the AUROC row, two at `not_assessable`, two on a `sex = F` cell (T2-4), and none.
- "The verdict words are absent from the page." Written: 0 whole-word hits for the ten words listed, and N2 for the longer word that is present.
- "Every mutant on the fix is killed." Written: the 18 declared mutants and lens 2's M5 / M10 / M11 at `667a201`; M3 was not re-planted (carried with FA-F4 by the note).
- "The Newcombe paired interval is verified."
- "The golden is independent of the checkout." Written: the four keys that reach the page and the 0-line diff.

## Re-run these

`<S>` = `C:/Users/joshs/AppData/Local/Temp/claude/C--Users-joshs-GPS-ProofPack/0e5d0ccc-8634-4ba1-aad8-15203d8e0928/scratchpad/lens-E10-r3-regression`; `<wt>` a detached worktree of `6c4e253`; `PYTHONPATH=<wt>/src`, `PYTHONIOENCODING=utf-8`, `PROOFPACK_TR39_FULL=C:/Users/joshs/GPS/ProofPack/workflows/data/confusables-18.0.0.txt`; run from `<wt>`.

| Command | Result |
|---|---|
| `python -m pytest -q -p no:cacheprovider` | `1652 passed, 1 skipped, 1 xfailed` (`suite_bash.txt`, `suite_ps.txt`) |
| `python -m pytest -q -p no:cacheprovider -m day10` (and `day9`, `day8`, `ap3`, `ap2`) | `68 / 328 / 470 / 167 / 90 passed` (`markers_bash.txt`, `ps_pass.txt`) |
| `python <S>/hand.py <wt>` | the F5 counts and the three deviation blocks above (`hand_out.txt`) |
| `python <S>/probe.py <wt> <S>/probe_out` | the golden masks and the criterion variants (`probe_out.txt`) |
| `python <S>/plant.py <wt667> M5 M10 M11` (the two new test files copied in first) | `3 / 3 / 4 failed, 44 / 44 / 43 passed`, the ids in N3 |
| `python scripts/build_sample_pack.py --out DIR --compare` at both commits, then the masked diff | 0 differing lines on `T2.html`, `T1.html`, `compare.json` (`sp_sizes.txt`) |
| `python scripts/mutation_sweep.py --marker day10` | Git Bash `18 planted, 18 killed, 0 survived; 203 s`, exit 0 (`sweep_day10_bash.txt`); PowerShell `18 planted, 18 killed, 0 survived; 204 s`, exit 0 (`sweep_day10_ps.txt`) |
| `python scripts/f17_determinism.py --out DIR --n 400 --compare` | `identical under the mask: yes`, exit 0 (`misc.txt`) |
| `bash <scratch9>/rerun/rerun.sh e10r3` (main tree) | `rerun_bash_r3.txt`, 248 lines; 0 block lines differ from `repair-E10-r2/rerun_bash_r2.txt` after masking run ids, `pp-b4-*` temp names, the tag and timings |
