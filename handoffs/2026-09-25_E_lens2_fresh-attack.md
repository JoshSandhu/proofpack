# Lane E, build day 10 (E10) - lens 2, fresh attack at `667a201` (E10 repair 1), 25 September 2026

**Verdict: PASS.** No blocker. Every figure the repair changed reproduces from my own arithmetic: the six clustered sensitivity / specificity differences of the F5 pair under `case_id = c{i // 2}` equal my own case-resampling draw loop (deviation 0.0 on both bounds of both overall cells), the clustered AUROC and Brier differences equal my own stratified draw loop (2.8e-17 and 1.7e-18), McNemar / Newcombe paired / paired DeLong on F5 and R2's F3 match my hand derivations and statsmodels 0.15.0 to at most 2.2e-15, no margin reaches run.json or the page without a customer criterion, no forbidden word reaches any of the seven T2 pages I rendered, injection is escaped, the marks and footers are on every page, and lens 1's blocker (FA-B1) and the CRLF golden (FA-N1 / RG-B1) are closed on a fresh CRLF worktree. Three findings, none a wrong number: two false sentences the repair added (a docstring that names one caller of `paired_delong` where `fixtures.py` is a second, and a test name that says fourteen cells carry a refusal where the test asserts twelve), and four of my own mutants on the clustered cells that survive the suite (test gaps on a statistical gate, the shipped numbers verified by hand). The rest is record-and-carry.

Setup: detached worktrees `wt667` (`667a201`) and `wt322` (`322c5a4`) under `scratchpad/lens-E10-r2-fresh-attack/`, both checked out CRLF (`git ls-files --eol`: `i/lf w/crlf` on the three F5 files and the golden); `PYTHONPATH=<wt>/src` on every call and proved (`proofpack.__file__` printed the worktree path first in every capture). Nothing committed; both worktrees removed at the end; the main tree carries only this note. All numbers below are mine, measured today in Git Bash.

## Findings

### F1. False sentence (repair FA-N8): `paired_delong` has two callers in `src`, not one - sentence violation, non-blocking

`src/proofpack/stats/discrimination.py` line 36 (module docstring, added by `667a201`): "Since build day 10 its one caller in `src` is `stats.comparison._paired_auroc_cell`". Line 390 (`paired_delong`'s docstring, added by `667a201`): "The one caller in `src` (`stats.comparison._paired_auroc_cell`, build day 10) calls it when the plan is not clustered". Counter-example: `grep -rn "paired_delong(" src/` prints `src/proofpack/fixtures.py:406` (`_f3_delong`, the F3 register row) and `src/proofpack/fixtures.py:504` (`_f5_delong_pair`, the F5-delong-pair oracle row) beside `comparison.py:456`. Both fixture callers pass independent rows, so nothing computed is wrong; the sentence is. Repro: `grep -rn "paired_delong(" <wt>/src | grep -v "def "` -> 3 lines.

### F2. False test name (repair FA-B1): "carries DEC-09's refusal on all fourteen cells" - the test asserts twelve - sentence violation, non-blocking

`tests/test_e10_comparison.py::test_f5_case_id_c_i_over_2_carries_dec_09s_refusal_on_all_fourteen_cells` collects twelve cells (`assert len(cells) == 12`) and asserts the `clustered_data_analytic_ci_invalid` refusal on each; for the other two of the fourteen (Brier, slope) it asserts `cell["analytic"] is None and cell["analytic_status"] == "used"` - no refusal. The docstring says this correctly ("the two calibration cells are cluster bootstraps with no analytic companion"); the name says the opposite. Measured on the K block: `differences.brier.analytic_status = used`, `analytic = None`. The repair note's own row for this test is right ("the twelve proportion / AUROC cells ... the two calibration cells with no companion").

### F3. Four of my mutants on the clustered comparison cells survive the suite - record-and-carry (test gaps on a statistical gate; the shipped numbers verified by hand below)

`scratchpad/lens-E10-r2-fresh-attack/mutants.py` plants twelve mutants in `stats/comparison.py` one at a time and runs `tests/test_e10_comparison.py tests/test_e10_criterion.py tests/test_e10_t2.py` (`-x`); the file is restored from git after each (`git status` clean at the end). Eight killed: the old `clustered_by_case` resampler, a prefix of the case ids, `n_cases = n`, the Se/Sp statistic's sign, the flag removed, the `analytic_status` word, the Brier statistic's sign, the slope statistic's sign (the last two by the T2 golden). Four survived, `44 passed` each:

| Mutant | What it changes | Why it matters |
|---|---|---|
| M3 `ids = prior.case_ids[sel]` | the prior version's case ids resample the cell | equivalent on every test input (both tables carry the same ids); see F4 for the input on which it is not |
| M5 refusal `n=new.n` | the DEC-09 refusal Number beside a clustered Se/Sp cell carries the pair count of the whole table (100) instead of the conditioned pairs (50) | the `analytic` companion's `n` is untested; it is in run.json |
| M10 `_resampler` -> `clustered_flat(arrays.case_ids, n_rows=arrays.n)` | the clustered AUROC / Brier / slope differences resampled in one stratum instead of within outcome class | the stratification of the three cluster-bootstrap cells is pinned by no test (the interval values move: my own unstratified draw would differ from the committed figures) |
| M11 AUROC `stat` -> `auroc(prior) - auroc(new)` | the clustered AUROC difference's resample distribution mirrored about zero while `est` keeps its sign | the interval printed on T2-3 / T2-4 / T2-5 would be wrong (`[-0.0001, +0.1734]` around `-0.0776`) and no test fails: the fourteen-cells test asserts only `ci_lo is not None`; only the six Se/Sp cells assert `ci_lo < est < ci_hi` |

The shipped code is right on these cells (my own draw loops below), so this is a coverage finding, not a wrong number. Repair shape: the Se/Sp re-run test's pattern (same resampler, seed, key, B) for the overall AUROC and Brier cells under `c{i // 2}`, and `ci_lo < est < ci_hi` on every cluster-bootstrap cell.

### F4. The prior version's `case_id` column is read and then ignored by the comparison - record-and-carry

Input: the F5 pair with `case_id = c{i // 2}` on the new table and `c{(i + 1) // 2}` on the prior (a different grouping of the same `row_id`s), `clustering.unit: case_id`. `assemble_compare` runs with no halt and no warning (`warnings: []`); every clustered difference uses the new version's grouping (`differences.op1.sensitivity.number.n_cases = 26`, identical to the same-ids run) while `comparison.prior.overall.op1.sensitivity.n_cases = 28` from the prior's own grouping, and T2-3 prints the two side by side. The same test set has one case structure; two declarations of it that disagree are a contradiction the engine does not name (H05 checks conflicting `y_true` within a case, not conflicting cases within a `row_id`). Mutant M3 above is the same fact. A prior table without a `case_id` column halts earlier as `H07` (header-set hash differs from `mapping.json`) - typed, so not a finding. (`cx667/K3_prior_case_id_shifted`.)

### F5. Two paired criteria on one metric, operating point and scope: lens 1's FA-N2 still reproduces - carried, as the note says

`Cfirst` (accuracy, `point_estimate >= -0.10`, met) and `C2` (accuracy, `ci_lower_bound >= -0.05`, not_met): the T2-3 accuracy row carries `Cfirst` once and `C2` nowhere; `record of criterion not met (PCCP §VII.B(3) requires recording)` is on the page 0 times. First item of the next round per the repair note; not re-graded here.

### F6. Wording, not a wrong number - record

- No `ledger` block in `criteria.yaml` (the fixture's removed): T2 section 7 and the `LEDGER_STATEMENT` claim read "...; the manufacturer's declared ledger limit (ledger.warn_after_acceptance_runs) is no limit declared." - the `{limit}` text slot's fallback inside the repair's new sentence. `comparison.ledger.warn_limit: null`, `limit_reached: false`, 0 rejections. (`cx667/L_no_limit`.)
- `PAIRED_DIFF` for a conditioned proportion prints "Sensitivity changed by −8.0 percentage points [−20.0, +2.0] ... on 100 paired cases (cluster bootstrap, percentile interval)": the slot is D4 section 8's `{n_pairs}` verbatim (`comparison.n_pairs`, a documented scalar), while the Number the interval belongs to carries `n: 50` (the reference-positive pairs) and `n_cases: 26`; both are on T2-3's `k/n` and Method cells. By spec; noted for Josh because the brief reads "n = the number of pairs for a paired statistic" and the sentence's n is the join's.
- The repair note's DEC-12 row says "This also moves the accuracy cell" and gives the overall figures; the two subgroup accuracy cells moved too: `sex = F` `[-0.180, +0.020]` -> `[-0.160, +0.020]`, `sex = M` `[-0.180, +0.0005]` -> `[-0.180, +0.020]` (`322c5a4` v `667a201`, B 200, seed 20240101). The AUROC, Brier and slope cells are identical at both commits, as the note says.
- Carried from lens 1 and re-measured, unchanged: FA-N4 (`score_not_probability` on a `y_pred`-only pair where the run's own block says `no_score_column`), FA-N10 (a payload with no `features` key: `compare` exits 2 and writes `T2.html`; `features` without `compare`: exit 4, `run.json` only), FA-N11 (U+2028 raw once inside `.customer-text`; U+202E escaped 12 times, U+200F once).

## What I could not break (this is evidence)

All derivations in `scratchpad/lens-E10-r2-fresh-attack/hand.py` (pure Python and integer arithmetic up to the line marked ENGINE) and the draw loops in `cx.py` block K and `auroc_rerun` (inline), which take only `BootstrapPolicy.rng` from the repository - the seed and cell-key hashing - and write their own case grouping, stratum order and `rng.integers(0, m, m)` draws.

- **McNemar.** Fourteen tables by hand (`2 * P(X <= min(b, c))` in integers with `math.comb`; `(|b - c| - 1)^2 / (b + c)` and `erfc(sqrt(s / 2))`) against the engine and `statsmodels.stats.contingency_tables.mcnemar` (`exact=True`; `exact=False, correction=True`): F5 `b 10, c 2` p `0.03857421875` = `2 * (1 + 12 + 66) / 4096`, forced corrected statistic `4.083333333333333` = `49 / 12`, p `0.04330814281079198` (statsmodels `...206`, 7.6e-17); `b + c = 24` exact (`12, 12` p 1.0; `24, 0` p `1.19e-07`), `25` corrected (`13, 12` statistic 0 p 1.0; `25, 0` statistic 23.04); `b 0, c 7` and `7, 0` p `0.015625`; `b = c = 0` p 1.0 no statistic; `b + c = 1` p 1.0; `b = c = 13` statistic `1/26`; `100, 0` p `4.16e-23`; `2, 10`. Worst deviation from either oracle `1.11e-16`; `method` named the route on every table (`EXACT_BELOW = 25`). One version all correct through the assembly (`cx667/P_allcorrect`): `b 0, c 10`, exact, p `0.001953125` = `2 / 1024`; sensitivity difference `+0.10 [+0.0090, +0.2136]` Newcombe paired.
- **Newcombe paired method 10.** My own Wilson bounds and the method-10 steps as I recall them (phi from `eh - fg` corrected to `max(eh - fg - n/2, 0)` when positive, uncorrected when negative, 0 when a margin is zero; square-and-add): fourteen tables - F5 accuracy `(80, 2, 10, 8)` `-0.08 [-0.155356, -0.010249]` phi 0.4946; F5 sensitivity and specificity `(40, 1, 5, 4)` `[-0.194203, +0.026030]`; `g = 0`, `f = 0`, difference 0 `(40, 3, 3, 4)`, all correct, none correct, phi negative `(5, 30, 30, 5)` and `(50, 20, 30, 0)`, `e = 0`, `h = 0`, `n = 3` at both ends (`[0.2059, 1.0]` / `[-1.0, -0.2059]`), `(1, 1, 1, 1)`. Worst deviation `1.94e-16`. The unpaired method 10 on the same margins (`82/100 v 90/100`) is `[-0.177554, +0.017463]` by my hand and by `difference_unpaired`; the engine's paired interval differs from it by `0.0222` / `0.0277`, so the paired case does not reuse the unpaired method. Two recollections agreeing is not verification: the fixtures report row `F5-newcombe-paired` is `no_independent_oracle`, `matched false`, `n_values_compared 0`, reason `[unverified] the value is the engine's own from the formula as recalled from Newcombe 1998 ...` - the right status, and the marking is on the report (37 `unverified` occurrences in `fixtures_report.json`).
- **Paired DeLong on R2's F3 pair.** O(mn) placement components with `psi` (1 / 0.5 / 0) and the covariance sums written out: `AUC1 0.80, AUC2 0.64, S_aa 0.024, S_bb 0.0392, S_ab 0.028, Var(diff) = 0.024 + 0.0392 - 2 * 0.028 = 0.0072, z 1.8856180831641274, p 0.05934643879191981, CI (-0.0063084589, +0.3263084589)`. Engine deviations: est `1.1e-16`, variance `6.9e-18`, bounds `1.9e-16` / `5.6e-17`, z `2.2e-15`, p `2.9e-16`; `delong_wald`, `n_pos 5, n_neg 5`. With the covariance dropped the variance is `0.0632` and the interval `(-0.3327, +0.6527)`: the engine's variance differs from that by `0.056`. Ties in both vectors (`s3`, `s4` with two tied pairs each): AUCs `0.8 / 0.6`, variance `0.016`, engine deviation est `1.1e-16`, variance `0.0`. Swapping the vectors negates the estimate exactly and keeps the variance; identical vectors are refused `boundary_estimate`, method `none`. A clustered declaration routes the AUROC to `cluster_bootstrap_percentile` with the DeLong refusal as `analytic` and `delong_refused_clustered` on the Number (K block, all three scopes).
- **The repair's cells, by my own draw loop.** F5 pair, `case_id = c{i // 2}`, B 200, seed 20240101, the engine's cell keys. Sensitivity: 26 cases holding a positive row (unit sizes not all equal, so the concatenate path), `[-0.19999999999999996, 0.019607843137254943]`, deviation `0.0` on both bounds; specificity: 26 cases, `[-0.19999999999999996, 0.020408163265306145]`, deviation `0.0`; estimates `-0.08` both. AUROC (strata positive 24 / negative 24 / mixed 2, in that order): `[-0.17342, +0.00011]`, deviations `2.8e-17` / `0.0`; Brier `[-0.006848, +0.090637]`, `1.7e-18` / `0.0`. The twelve proportion / AUROC cells all carry an interval, `cluster_bootstrap_percentile`, the `*_refused_clustered` flag and the DEC-09 refusal as `analytic` (`n_cases` 26/26/50/50 overall, 26/24/50/50 `M`, 24/26/50/50 `F`); the page has `insufficient_clusters` 0 and `n.e.` 0 (at `322c5a4` the same input prints 16 and 13; the six Se/Sp cells `method none, reason insufficient_clusters, n_cases 26/26, 26/24, 24/26`, and `Cse` `not_assessable / no_interval` - the repair's "at 322c5a4" sentences hold). `Cse` (sensitivity, `ci_lower_bound >= -0.30`) `met`, `compared_value -0.19999999999999996` = the cell's `ci_lo`. A clustered plan handed pairs with no case ids raises `ValueError("a clustered plan needs the case ids of the pairs")` from `_paired_proportion_cell`, as the note says. A subgroup level holding one case (120 rows, 10 cases of 12, `site = S2` one case; `cx667/K4_one_case_level`): its Se, Sp, accuracy and AUROC differences are refused `insufficient_clusters` with `n_cases 1`, the criterion on it `not_assessable / no_interval` with the reason in `detail`, T2-4 prints `n.e. (insufficient_clusters)` three times and `not assessable`; the nine-case level prints intervals.
- **The margin.** No paired criterion (`cx667/G_nomargin`): T2-3's header is seven columns (`Metric (operating point)` ... `McNemar p (method)`), `Margin` 0, `Status` 0, `class="status"` 0 inside the table, the caption's "no paired_difference_vs_prior criterion is declared, so no margin and no status column is printed" present; run.json `margin` 0, `paired_difference` 0; every `0.02` / `0.03` / `0.05` in run.json is inside an ROC / PR coordinate or an estimate; on the page `0.02` x0, `0.03` is `0.039` (McNemar p, Brier difference) x5, `0.05` is `−0.053` (a subgroup AUROC difference) x2. Twelve criterion variants on F5 accuracy (`est -0.08`, ci `[-0.1554, -0.0102]`) against my literal rule (the named statistic, the comparator, the value): `ci_lower_bound >= -0.05 / -0.10 / -0.02 / 0.0 / +0.05` not_met, `<= -0.10` met, `ci_upper_bound <= 0.0 / +0.05` met, `<= -0.05` not_met, `>= -0.05` met, `point_estimate >= -0.08` met, `> -0.08` not_met - all `statistic_compared` with the compared value printed; 12 of 12 agree. `-0.10` on `ci_lower_bound` is `not_met` (the lower bound is `-0.1554`), which is what D1 section 2's rule gives; the orchestrator's attack line "-0.10 -> met" is not what the F5 interval supports and the engine does not pretend otherwise. Row 121: `Type column header: True`, T1-17 cells `['sensitivity', 'paired difference vs prior version', 'sex = F']`, `row 121 repaired`.
- **Pairing.** Extra prior row `f5-999`: `HALT H12 detail {"unmatched": 1}`; a prior row dropped: the same; a `y_true` flipped: `H12 ... {"n_pairs": 99, "new_only": 0, "prior_only": 0, "label_mismatch": 1}`; a blank `row_id`: `H12 unmatched 1`; a duplicated `row_id` in the prior: `HALT H05: duplicate row_id values`. `--allow-unpaired` on the extra-row pair: `label not like-for-like`, `paired false`, `W12`, the six Numbers `newcombe10_not_like_for_like` x3, `delong_wald_not_like_for_like`, Brier / slope refused `unpaired_not_like_for_like`, none unlabelled, the caption `UNPAIRED - not like-for-like` twice on the page, `mcnemar null`, `subgroups []` with `subgroups_not_computed`, `C2 not_assessable / not_like_for_like`. Identical tables: `b = c = 0`, p 1.0 exact, every proportion difference `0.0` with a Newcombe paired interval (`±0.0593` Se/Sp, `±0.0305` accuracy), AUROC / Brier / slope refused `boundary_estimate`, `C2 met` on the lower bound `-0.0305`, 0 rejections.
- **T2.** Seven pages rendered (K, K4, G, I, J, U, U_identical), each 5 pages with one footer per page. Forbidden words (my list of 28 including `non-inferior`, `equivalent`, `superior`, `improved`, `degraded`, `regressed`, `better`, `worse`, `significant`, `no difference`, `unchanged`, `stable`, `pass`, `fail`, `verdict`, `certif`, `approv`, `endors`, `design partner`): 0 whole-word hits in the visible text of every page. Status words outside `.status` elements: 0. Anchors on the clustered page: 19 `FDA_PCCP_*` links each labelled `(2024-12-04, updated 2025-08-18, final)`, 4 `FDA_AIDSF_*` links each `draft guidance (January 2025), not for implementation`, the guidance table's Draft column `yes` for the four AI-DSF rows and `no` for the nine PCCP rows. Digit runs outside `data-ref` cells: 100 distinct; the 17 not literal substrings of run.json are rounded Numbers in the claim sentences (`0.078`, `18.0`, `20.0`, `8.0`, `50.0`, `64.7`, `94.2`, `7.2` from `-0.0776`, `-0.1805`, `-0.2`, `0.6470...`, `0.9416...`), section and table numbers (`4.1` ... `5.10`) and the disclaimer's citations (`2007`, `SI 2024/1368`, `21 CFR 807.92`). The McNemar row prints `10 / 2 | 0.039 (exact)`; the record string once, on `C2`'s row, on no `met` row. Injection through the criterion id (`C<i>2</i>`), author, justification (`<script>` + `{{ 7*7 }}` + U+202E + U+200F + U+2028 + 10,000 `A`), model name, both versions: `<script>` 0 raw / 8 escaped, `{{ 7*7 }}` literal 3 and `>49<` 0, `<img` / `<i>` / `<b>` 0 raw, the 10,000-character string once inside `<div class="customer-text">`, 0 rejections, the injected criterion evaluated `not_met`. Marks: sample pack `--compare` (`compare.json 328,965`, `T2.html 69,017` bytes - the note's figures) carries `SYNTHETIC - illustrative` 2/2/1/1/1 and `NO LICENCE - not for submission` 2/1/1/1/1 on its five pages; no licence -> exit 4, `run.json` + three JSON files, `watermark: NO LICENCE - not for submission`; `features` without `compare` -> exit 4, `run.json` only, watermark null; an annual test licence -> no mark. 22 counter-example run.json documents validate against `output_schema_v1.json` with 0 errors.
- **Wiring.** `socket.socket` and `create_connection` patched to raise: `compare --input --prior --offline --templates T2` rc 0, 0 socket calls, five files written; a second run rc 0, 0 calls. `--templates T1` rc 5, no directory created. `import proofpack`, `proofpack.stats`, `stats.comparison`, `stats.proportions`, `stats.discrimination`, `stats.bootstrap` with `statsmodels`, `sklearn`, `scipy`, `jinja2` hidden: ok, `loaded: []`. F17 across my two runs in one home: `ingest_report.json`, `compare_ingest_report.json`, `pseudonyms.json` identical after the mask; `run.json` and `T2.html` differ only in `prior_acceptance_runs` 0 -> 1, `acceptance_runs` 1 -> 2 and the two ledger sentences (the ledger counting; the sample-pack F17 test builds in separate homes).
- **Suite, markers, lint (CRLF worktree, `667a201`).** `1648 passed, 2 skipped, 1 xfailed in 219.24s` (the second skip: `PROOFPACK_TR39_FULL` unset in my run; the note's `1649 / 1` is the main tree with it set - consistent); the golden test passes in the CRLF checkout, which is what lens 1's B1 asked for. `-m day10` `65 passed`; `day9 328`; `day8 469 passed, 1 skipped` (the same TR39 skip); `day7 139`; `day6 285`; `day5 54`; `ap3 167`; `ap2 90`. `ruff check .` `All checks passed!`; `ruff format --check .` `243 files already formatted`. `mutation_sweep.py --marker day10`: `14 planted, 14 killed, 0 survived; 166 s`, exit 0 (in the CRLF worktree - at `322c5a4` lens 1 measured it refusing to run there). `--list` 345 lines. `capture_fixture_oracles.py --check`: `72 identical, 0 differ`. `tr39_subset.py --check`: `vendored subset matches`. `doctor --offline`: 17 `[ok` lines. `proofpack fixtures --offline`: `rows 46: matched 33, not matched 0, no oracle recorded 2, no independent oracle 1, not built 5, compared by the test suite only 5`, exit 0. Timing: `compare --templates T2 22.63 s; T2 render from run.json 0.03 s; T2 73214 bytes`, `1 passed in 23.43s`.
- **Fails pre-fix.** The repair's three test files and the regenerated golden copied into the `322c5a4` worktree: `5 failed, 133 passed` - the three FA-B1 tests, `test_golden_t2_matches_the_committed_render` and `test_the_golden_is_the_same_page_from_crlf_copies_of_the_f5_files`; no repair test passes on the old code. SHA-256 of the CRLF `f5_new.csv` / `f5_prior.csv` in my worktree: `c3a45a47...` / `ffdbabf0...` (the docstring's figures); the main tree's LF bytes `b5a7a966...` / `08709dca...`.
- **Damage elsewhere.** `git diff 322c5a4 667a201 --stat`: 12 files, no file under `schema/`, `design/`, `cli.py`, `criteria.py`, `gates.py`, `proportions.py`, `bootstrap.py`; `number.py` untouched. `comparison.py` changes one resampler call and its docstring; `discrimination.py` docstrings only (F1); `templates.py` two skeleton sentences; `T2.html` the section-7 sentence and banner. The E9 r3 lines I re-ran (doctor, tr39, sweep list, captures, row 121, markers day5-day9 / ap2 / ap3) are as the r3 handoff and the repair note print them.

## What I could not check

- Newcombe 1998 (paired) itself: not fetched; my derivation is a third recollection of the same steps. The `[unverified]` / `no_independent_oracle` status is the right one (need 35 stands).
- The subgroup AUROC cluster-bootstrap cells by my own draw loop: I replicated the overall AUROC and Brier cells only; the subgroup Se/Sp cells are covered by the repair's test with the repository's resampler.
- The E9 r3 `rerun.sh` in full and any PowerShell run: not repeated.
- T2 in a browser or in print: bytes only.

## Sentences I refused to write

- "The clustered comparison is now correct under clustering." Refused: I verified twelve cells of one fixture under one grouping and one one-case level; F3 and F4 name what the tests do not pin.
- "Every mutant on the repair is killed." Refused: four of my twelve survive (F3).
- "The Newcombe paired interval is verified." Refused: no primary source.
- "The full suite passes in every checkout." Written instead: in one fresh CRLF worktree on this machine, with the figures.

## Re-run these

`<scratch>` = `C:/Users/joshs/AppData/Local/Temp/claude/C--Users-joshs-GPS-ProofPack/0e5d0ccc-8634-4ba1-aad8-15203d8e0928/scratchpad/lens-E10-r2-fresh-attack`; `<wt>` = a detached `667a201` worktree; `PYTHONPATH=<wt>/src`, `PYTHONIOENCODING=utf-8`, run from `<wt>`.

| Command | Result |
|---|---|
| `python <scratch>/hand.py` | McNemar worst `1.11e-16`; Newcombe paired worst `1.94e-16`; DeLong F3 deviations as above (`hand_out.txt`) |
| `python <scratch>/cx.py <wt> <out> K K3 K4 G C I J U L N P D` | blocks above (`cx667_a.txt`, `cx667_b.txt`; the K block at `322c5a4` in `cx322/`) |
| `python <scratch>/cli_probe.py <wt> <out>` | `[W]` and `[F17]` lines (`cli667.txt`) |
| `python <scratch>/mutants.py <wt>` | 8 killed, 4 survived (`mutants_out.txt`); the file restored |
| `python -m pytest -q -p no:cacheprovider` | `1648 passed, 2 skipped, 1 xfailed in 219.24s` (`full.txt`) |
| `python scripts/mutation_sweep.py --marker day10` | `14 planted, 14 killed, 0 survived; 166 s` (`sweep_day10.txt`) |
| `grep -rn "paired_delong(" <wt>/src` | three call sites (F1) |
