# Lane A: build day 9 (A-P3: release pipeline, `proofpack fixtures`, F17, T12), 23–24 September 2026

Written on 24 September 2026 by the handoff session. Sources: the build note, the two repair notes and the six lens notes, all named below. Every figure under "Re-run these" was measured again in this session in both shells. Engine: branch `a-p3-release` in `C:/Users/joshs/GPS/ProofPack/wt-ap3`, `e17a510..43521dd` (eight commits), plus this note's commit. Nothing is merged or pushed, and no tag exists.

**Verdict.** **PASS.** Lens round 3 (both lenses, at `43521dd`) found no blocker. The orchestrator counts **18** items carried after the final lens. The pass comes with **four open sentence violations**, and no repair round followed to fix them. Two are test names (FA3-S3, RG3-S1) and one is the `OFFLINE_FLAG` docstring and comment (FA3-S1), all in shipped files. The fourth (FA3-S2) is in the repair-2 note only. The repair rule says these are fixed before the round counts as closed, so the orchestrator should fix the three in shipped files before the merge; each is a rename or a one-line change (see "What did not land").

## What landed

All figures come from `win-amd64-cp314` (Windows 11, Python 3.14.6, numpy 2.5.1, scipy 1.18.1), with `PYTHONPATH` forced to the worktree's `src`. None comes from the reference platform (`python:3.12-slim` linux/amd64): no image was built or pulled.

Commits: `a48b246` item 1 · `5d382c5` items 2-3 · `83b05a5` items 4-6 · `1a967d8` item 7 · `58c1b2e` + `a09a0ef` repair 1 · `c3bb893` + `43521dd` repair 2.

| Item | Commit(s) | Test ids | Literal input and measured figure (platform) |
|---|---|---|---|
| 1. `proofpack fixtures [--out DIR] [--html] [--r-captures]` writes `fixtures_report.json` (schema `proofpack-fixtures-report/1`). Exit 6 = `EXIT_FIXTURES_NOT_MATCHED`. | `a48b246`, `58c1b2e`, `c3bb893` | `tests/test_fixtures_cmd.py`, `tests/test_offline.py::test_fixtures_offline_opens_no_socket`, `tests/test_wheel_fixtures.py` | `fixtures --offline --out D --r-captures` at `43521dd` gives `rows 43: matched 30, not matched 0, no oracle recorded 2, not built 6, compared by the test suite only 5`, exit 0, 1.17 s (Git Bash; measured now). Largest deviation per class, measured now: closed form 1.78e-15 (F2-exact), iterative 2.19e-9 (F4-irls), register 5.77e-5 (F3-register), reported rounding 3.21e-5 (F14-newcombe). 158 values compared across the matched rows. `captured.F1-wilson.wilson_lo` planted +2e-9: not matched, exit 6. Planted +5e-10: matched, exit 0 (lens 3 regression). |
| 1a. An engine value left out, NaN or infinite, an absent `oracles_v1.json` / `f4_expected.json`, or a deleted entry is `not_matched`, exit 6, report written (lens FA-B1) | `58c1b2e` | `tests/test_ap3_repair1.py::test_an_engine_value_left_out_is_not_matched_and_exits_6`, `::test_an_engine_nan_is_not_matched_the_report_is_written_and_exits_6`, `::test_a_deleted_oracle_entry_is_not_matched_and_exits_6`, `::test_an_absent_oracle_file_is_not_matched_and_exits_6` | At `1a967d8` the NaN, deleted-entry and absent-file cases exited 5 with no report (`assert (5 == 6)`). The mutant `ap3_missing_engine_value_counted_within` is killed. |
| 1b. A value deleted from an oracle entry is `not_matched`, `oracle value missing: <name>`, exit 6. Each row declares its value names (`Row.compares`). (lens FA2-B1) | `c3bb893` | `tests/test_ap3_repair2.py::test_a_deleted_oracle_value_is_not_matched_and_exits_6` (three cases), `::test_every_compared_row_declares_the_names_its_engine_and_oracle_carry` | In an installed wheel (`uv build --wheel --offline`, `pip --no-deps --no-index` into a fresh venv), `captured.F3-delong.values.paired_p` and `register.F6.holm_2` deleted: `matched 27, not matched 2`, exit 6 (lens 3 fresh attack). At `a09a0ef` the same class gave exit 0 and `matched`. |
| 1c. A file that does not parse (`ValueError`: truncated JSON, invalid UTF-8) is `oracle_file_unreadable`, `not_matched`, exit 6 (RG2-N3) | `c3bb893` | `::test_a_truncated_oracle_file_is_not_matched_and_the_report_is_written`, `::test_a_truncated_newcombe_file_is_not_matched_not_absent` | The installed wheel's `oracles_v1.json` replaced by `{"captured": ` gives `matched 3, not matched 26, no oracle recorded 3`, exit 6, report written (lens 3 regression). **A directory in the file's place still exits 5 with no report** (FA3-S2 / RG3-N1, carried). |
| 1d. `git_sha` and the Newcombe / R-capture reads happen only when the package is `<root>/src/proofpack` and `<root>/pyproject.toml` names `proofpack` (FA-R1, FA2-R2) | `58c1b2e`, `c3bb893` | `::test_git_sha_is_null_for_a_wheel_vendored_inside_another_git_repository[customer]`/`[proofpack]`, `::test_a_newcombe_file_beside_an_installed_package_is_not_read[customer]`/`[None]` | A wheel under `git init X/vendor` gives `git_sha` null. A planted `X/fixtures/newcombe_table2.json` with status `checked` is not read under no pyproject or under one naming `customer`; F14 stays `no_oracle_recorded` with its `[unverified]` reason (lens 3 regression). |
| 2. F17: `scripts/f17_determinism.py`, two offline runs with the same input, five hashes compared | `5d382c5`, `c3bb893` (`file_names`) | `tests/test_f17_determinism.py`, `tests/test_ap3_repair2.py::test_f17_detects_a_file_added_to_one_run` | `--n 5000` (seed 20240101) gives `run_json_masked` `0c4b77b7789a0c64`, `manifest_masked` `a839493ed99f3925`, `pseudonyms_json_masked` `ca4e145eb123633e`, `ingest_report_json` `d306a3aa6600d4a4`, `file_names` `7fe1d0490f327ef2`, all `equal`; run exit codes [4, 4]; `identical under the mask ['run_id', 'started', 'duration_s']: yes`; exit 0; 2.90 s Git Bash, 2.80 s PowerShell (measured now). This repeats the run on one platform, `win-amd64-cp314`. Hash identity on the reference platform is not measured. |
| 3. T12 software assurance record: `render/t12.py`, `templates/T12.html` (extends the unchanged `base.html`), `scripts/build_t12.py`, golden `tests/fixtures/golden/T12.html` | `5d382c5`, `58c1b2e`, `c3bb893` | `tests/test_t12.py` (10), `tests/test_ap3_repair1.py::test_t12_does_not_give_w16_the_exit_2_action`, `::test_each_failure_mode_code_is_raised_somewhere_or_says_it_is_not`, `::test_t12_doctor_table_prints_doctors_mark_not_a_held_condition` | `build_t12.py` on this session's report: 49,371 bytes in both shells (measured now). From lens 3 fresh attack: in the page text, validated, compliant, certified, approved, CSA-compliant, meets and pass each have 0 hits. The page carries "This record supports, does not replace, the manufacturer's own validation", and each of the 6 AI-DSF anchors carries "draft guidance (January 2025), not for implementation". From lens 3 regression: 4 lines carry `[unverified`, and "CSA-structured" appears once. W16's row now gives exit 0 with the W16 line; before, it said exit 2 (RG-B1). H10's row reads "not raised in this version" (FA-R4). |
| 4. `Dockerfile` (+ `.dockerignore`): `FROM --platform=linux/amd64 python:3.12-slim`, digest `[unverified]`, `pip install --require-hashes --no-deps -r requirements.lock`, `USER proofpack` (uid 10001), `ENTRYPOINT ["proofpack"]` | `83b05a5`, `c3bb893` | `tests/test_dockerfile.py` (17), incl. `::test_the_copies_and_the_run_are_the_ones_written` | Parsed only; never built. `FROM python:3.12` fails 1 test; `ENV PROOFPACK_SIGNING_SEED=x`, `COPY --from=ghcr.io/x/y:latest /k /k`, `COPY signing.txt /tmp/` and `RUN python -m pip install cryptography` are each caught (lens 3 regression). Six other planted lines pass (FA3-R4, carried). |
| 5. SBOM: `scripts/sbom.py`, standard library only, CycloneDX 1.5 JSON from `uv.lock` | `83b05a5` | `tests/test_sbom.py` (4) | `62 components: 13 required, 1 optional, 48 excluded`. Two Git Bash runs are byte-identical (`cmp`), and the PowerShell file has the same SHA-256 prefix, `e5c13c67928b10d8` (measured now). Fields: `bomFormat`, `specVersion`, `serialNumber` (UUID5 of the lock's SHA-256), `version`, `metadata.component`, `metadata.tools.components`, and per component `type`, `bom-ref`, `name`, `version`, `purl`, `scope`, sdist `hashes`, plus `dependencies`. The CycloneDX schema file was not fetched: this is a structural check only. |
| 6. Workflows: `.github/workflows/release.yml` (new), `ci.yml` job `docker-smoke`, `.github/dependabot.yml` | `83b05a5`, `58c1b2e`, `c3bb893` | `tests/test_workflows.py::test_each_command_the_three_patterns_match_carries_offline[ci.yml]`/`[release.yml]`, `::test_the_lens_counter_examples_are_caught` | The three patterns match 5 commands in `ci.yml` and 6 in `release.yml`; each carries `--offline` (lens 3 fresh attack, read line by line). The only secret named is `GITHUB_TOKEN`. None of these workflows has run (next section). |
| 7. Mutation sweep, `AP3_MUTANTS` (25) | `1a967d8`, `a09a0ef`, `c3bb893` | `tests/test_sweep_ap3.py` | `python scripts/mutation_sweep.py --marker ap3`: `25 planted, 25 killed, 0 survived; 248 s` (Git Bash, measured now). |

## What was written but has never run

Each of these runs for the first time on the first push of the branch or the merge (the `ci.yml` jobs), or on Josh's first tag (the `release.yml` jobs). Describe each as written, not as working.

- **`.github/workflows/ci.yml`: `docker-smoke`.** It builds the image without pushing, prints the base image digest, and runs `doctor`, `fixtures` and one run inside the container with `--network none`, each with `--offline`.
  - `offline-namespace` (A-P2) **has** run: runs 35911876338 (23 September, read by repair 1 with `gh run view`) and 35992801938 (24 September, which the orchestrator names; I did not read it).
- **`.github/workflows/release.yml`:**
  - `build`: wheel, `fixtures_report.json`, SBOM, sample `run.json`, T12, `requirements.lock`;
  - `image`: GHCR image, smoke, Trivy `aquasecurity/trivy-action@0.28.0` `[unverified]`, push;
  - `testpypi`: trusted publishing, environment `testpypi`;
  - `install-from-testpypi`;
  - `github-release`;
  - `zap-baseline`: `ghcr.io/zaproxy/zaproxy:stable` `[unverified]`, `-I`, with `if: always()` on the upload step. Whether `-I` holds the exit at 0 is `[unverified]`.
- **`.github/dependabot.yml`**: ecosystem `uv` `[unverified]`. GitHub reads the file once it is on the default branch.

## What was cut and why

None of the brief's seven items was cut. Inside the items:

- **Five fixtures report `compared by the test suite only`: F12 and F17–F20.** They are behaviours, not values.
- **Six rows report `not built in this version`.** F5, F7 and F15 need `stats.comparison` / `stats.drift`. F16 is Pyodide (day 10). F21 has no Sepsis snapshot. F3's AUPRC is also not built: the engine emits none.
- **F13 and F13b stay `no oracle recorded` `[unverified until captured]`** until the R captures (days 11–14).
- **T12's intended-use slot has no input.** The page prints the placeholder and the INCOMPLETE stamp. No DOCX.
- **F17 is a script and a test, not part of `proofpack fixtures`.**
- **Nothing was built, pulled, installed from an index, pushed, tagged or published.** That was this day's safety rule. So the Dockerfile, the image, Trivy, TestPyPI, GHCR, the GitHub release and ZAP exist as files only, and the base-image digest is `[unverified]`.
- **The site half of A-P3 (the site consuming the release assets) is S4's, on day 10 (DEC-43).** It was not built.

## What did not land (carried)

The orchestrator counts 18 items. The table groups them by finding id as the lens notes do. I did not reconcile my grouping with the count of 18.

**Open sentence violations (fix before the merge):**

| Id | What | Repro / fix |
|---|---|---|
| FA3-S1 | `tests/test_workflows.py` says `--offline` must be "a word of its own". But `OFFLINE_FLAG` `(?<![\w./-])--offline(?![\w/-])` accepts `--out=--offline`, `OPT=--offline proofpack run ...`, `>--offline.log` and `& echo --offline`. The engine's parser reads `--out=--offline` as online. | Append `{'run':'proofpack run --input a --criteria b --out=--offline'}` to `release.yml`'s `build` steps and call `offline_violations`: it prints `[]`. Fix: add `=` and `>` to the look-behind and add the lines to `LENS_COUNTER_EXAMPLES`, or narrow the sentence. |
| RG3-S1 | The test name `test_the_zap_upload_step_runs_after_a_failed_scan` states what a never-run step does. The test reads one YAML key. | Rename it to `test_the_zap_upload_step_carries_if_always`. |
| FA3-S3 | The test name `test_no_secret_looking_env_or_arg_and_no_key_material` states more than the test inspects. A `LABEL` holding the RFC 8032 Ed25519 test secret key in hex passes every Dockerfile test. | Rename it to what it inspects (ENV/ARG names, `BEGIN`, `PRIVATE KEY`, `.lic`/`.pem`/`.key` in COPY/ADD). |
| FA3-S2 | The repair-2 note says "An unreadable oracle file is `not_matched`, exit 6". A directory in place of `oracles_v1.json` or `newcombe_table2.json` exits 5 with no report (`PermissionError`). This note states only what was measured (item 1c). The code's own docstring is accurate. | `rm fixtures/newcombe_table2.json && mkdir fixtures/newcombe_table2.json && python -m proofpack.cli fixtures --offline --out D` gives exit 5. |

**Record and carry:**

| Id | Reason carried | One-line repro |
|---|---|---|
| RG3-N1 (with FA3-S2) | `load_oracles` catches `FileNotFoundError` and `ValueError` only. Nothing reads as matched; the exit is 5. | as FA3-S2 |
| FA3-R1 | Two surviving mutants in repair-2 code: `except ValueError` narrowed to `JSONDecodeError` (no test feeds non-UTF-8 bytes), and F17 `file_names` reduced to a count. | the lens's `mut` edits; `-m "ap3 or day9 or ap2"` gave `363 passed` |
| FA3-R2 | `resources._CANDIDATES` still falls back to `../../fixtures/oracles_v1.json` and `f4_expected.json` when the in-package copy is absent. | `X/src/proofpack` with no `_fixtures`, plus a planted `X/fixtures/oracles_v1.json`: F1-wilson `matched` from the outside file |
| FA3-R3 | The reported-rounding tolerance for the bootstrap rows is read from the oracle file (`register_decimals`). This is the oracle-tampering class; an installed wheel has no `--check`. | `register_decimals.F3_bootstrap` 0 and `ci_lo` 0.8: `matched`, exit 0 |
| FA3-R4, FA-R6 | Six planted Dockerfile lines pass: `HEALTHCHECK curl`, `ONBUILD`, `SHELL`, `ENV PROOFPACK_LICENCE=...`, a `CMD` without `--offline`, and a `LABEL` with key bytes. The committed Dockerfile has none of them. | insert before `USER proofpack` and run `tests/test_dockerfile.py` |
| FA3-R5 | A Newcombe file deleted in a source checkout reads `no_oracle_recorded`, exit 0, and the reason does not say the file is absent. | delete `fixtures/newcombe_table2.json`, run `fixtures` |
| FA3-R6, FA-R10, RG-N9, lens-2 aside | Mutable action refs: `pypa/gh-action-pypi-publish@release/v1` sits under `id-token: write`, and `trivy-action@0.28.0` under `packages: write`. `--extra-index-url` in `install-from-testpypi`. `docker login` leaves the token in `~/.docker/config.json` for the job. | read `release.yml`; pins after the first run |
| RG3-N2 | `n_values_compared` counts values that were not compared, for example 2 for F1-wilson with `wilson_lo` deleted. | delete one captured value and read the row |
| RG3-N3 | `COMMENT` strips from a whitespace-led `#` inside quotes (`--criteria "a #b" --offline` is reported). It over-reports only. | one-step job fed to `offline_violations` |
| RG2-N2, FA2-R3 rest | Forms the workflow test misses (none in the committed files): `proofpack --quiet run`; `subprocess.run([...])`; `main(argv=[...])`; `\| xargs proofpack`; `proofpack $CMD`; `$(which proofpack) run`; a `docker://` step with `entrypoint: proofpack`. | append as a step and call `offline_violations` |
| FA-R8 | The report schema accepts self-contradicting reports: `matched` with a value outside tolerance, a wrong summary, or `exit_code` 0 beside a `not_matched` row. `build_t12.py` renders them as given. | hand-edit a report, then run `build_t12.py` |
| FA-R9 | Surviving mutants L1, L2 (equivalent under the current register), L5, L6, L8 and L14 (release fixtures step with `\|\| true`). | lens 1 fresh attack FA-R9 |
| FA-R12 | `[unverified, predicted]` `tests/test_wheel_fixtures.py` may skip in GitHub Actions (no numpy in the user site). | read the first CI log |
| RG-N3 (lens 1, second half) | A-P2 item 4, the release-image network capture, is still open. `docker-smoke` runs `--network none` and records no capture. | first CI run |
| RG-N6 | Eleven register rows carry `oracle_source.kind` `published_table`. D1 says R2 computed them. | read `fixtures_report.json` |
| RG-N7 | `[unverified, never run]` The release `build` job runs on `ubuntu-latest` with Python 3.12, so `platform_tag()` would equal `REFERENCE_PLATFORM` and T12 would print "is the reference platform" for a report made outside the image. | first tagged run |
| RG-N8 | T12 carries D4 section 7.1's footer, which describes a customer run. | open T12 |

Found while writing this note (read, not measured): two `release.yml` step names state behaviour of a never-run job. They are `proofpack fixtures (exit 0 only when no row with an oracle is not matched)` and `sample run.json of the synthetic cohort (assemble_run; no socket, no telemetry)`. No lens graded them. They belong with RG3-S1's rename.

## What the lenses found

| Round | Sha | Regression | Fresh attack |
|---|---|---|---|
| 1 | `1a967d8` | **FAIL**, two blockers. RG-B1: T12 gave W16 "exit 2", but the engine exits 0. RG-B2: `release.yml`'s header said the test covers "every line that runs the engine"; a planted `main(['run', ...])` passed all 10 tests. | **FAIL**, one blocker. FA-B1: a mutant counting a missing engine value as within tolerance survived the full suite (`1462 passed`), and `fixtures` then read a not-matched row as matched, exit 0. FA-R2: a NaN, a deleted entry or an absent `f4_expected.json` gave exit 5 and no report. FA-R1: `git_sha` recorded a customer repository's HEAD. Thirteen findings in all. |
| 2 (run cold at `a09a0ef` after the 12:50 cut-off) | `a09a0ef` | **PASS**, no blocker. RG2-S1: the `IMAGE_CALL` comment claimed digest-pinned images, and `proofpack@sha256:...` was missed. RG2-N1: three surviving mutants. RG2-N3: a truncated oracle file exited 5. | **FAIL**, one blocker. FA2-B1: a value deleted from an oracle entry was not compared, and the row read matched, exit 0, in an installed wheel. Sentence violations FA2-R2 to R6 and R8. |
| 3 | `43521dd` | **PASS**, no blocker. RG3-S1 (ZAP test name) open; RG3-N1 to N3 recorded. | **PASS**, no blocker. FA3-S1, S2 and S3 open; FA3-R1 to R6 recorded. |

What no lens could break, across the three rounds:

- **Planted oracle cells at the tolerance edge.** Lens 2 fresh attack: 9.999999717e-10 matched and 1.0000000272e-9 not matched on `wilson_lo`. Lens 3 fresh attack: 1e-6 − 1e-15 matched and 1e-6 not matched on `F6-p.chi2_p`.
- **Independent re-derivations.** Wilson, Clopper-Pearson, DeLong (1988) and Newcombe method 10 each equal the engine to ≤ 1e-16.
- **Sockets.** Under an inherited `sitecustomize` audit hook, `fixtures` (with and without `--offline`), `f17_determinism.py`, `build_t12.py`, `build_sample_pack.py` and `sbom.py` produced only `socket.gethostname`.
- **T12 escaping.** Injected script, BiDi and `<img>` text rendered escaped: 0 raw.
- **Byte-identical outputs.** The golden regenerates byte-identically, and the SBOM is byte-identical across shells.
- **The trial merge onto `9806d3d`.** It is clean, and its suite gives `1544 passed, 1 skipped, 1 xfailed` (lens 3 regression, with `PROOFPACK_TR39_FULL`).

## Decisions taken today

| # | Decision | Where recorded |
|---|---|---|
| 1 | Exit 6 = `EXIT_FIXTURES_NOT_MATCHED`. The licence never changes the fixtures exit code. | build note 1 |
| 2 | Report shape `proofpack-fixtures-report/1`: one row per comparison, five statuses, `matched: true` refused on a row without an oracle. `values[].oracle` and `values[].tolerance` are nullable. The schema id is unchanged (never published). | build note 2; repair 1 decision 3; repair 2 decision 3 |
| 3 | Four tolerance classes. Closed form 1e-9 and iterative 1e-6 (D1 section 9). `register` 1e-4 (D1 section 3.2, "tolerance 1e-4 on the shown rounding"). `reported_rounding` (half a unit plus 1e-12). Each rule lists its rows by id. | build note 3; repair 2 (FA2-R4) |
| 4 | F17 hashes run.json and the manifest with `run_id`, `started` and `duration_s` masked; `pseudonyms.json` with `run_id` masked; `ingest_report.json` as written; and the file list. The egress `manifest_sha256` is recorded, not compared. | build note 4; repair 2 (FA2-R9) |
| 5 | `fixtures --html` writes T12 only under an `ok`/`grace` licence. The release T12 is rendered by `build_t12.py` with DEC-48's `NO LICENCE - not for submission` mark. | build note 5 |
| 6 | The Dockerfile digest is not pinned and is marked `[unverified]`. CI prints the digest; the test accepts the tag only while the marker is present. | build note 6 |
| 7 | Workflow permissions: release top level `{}`; `image` holds `packages: write`; `testpypi` holds `id-token: write`; `github-release` holds `contents: write`; the rest `contents: read`. | build note 7 |
| 8 | The `[unverified]` Newcombe table stays out of the wheel. It is read only when the package is `<root>/src/proofpack` and `<root>/pyproject.toml` names `proofpack`; `git_sha` and the R captures use the same test. | build note 8; repair 1 FA-R1; repair 2 decision 5 |
| 9 | An absent or damaged shipped oracle file (`oracles_v1.json`, `f4_expected.json`), a deleted entry or a deleted value is `not_matched`, exit 6. An absent Newcombe table is `no_oracle_recorded`. | repair 1 decision 1; repair 2 decisions 1, 4 |
| 10 | `max_abs_deviation` is null when any value in the row was not compared, and T12 prints "—". NaN and infinity are written as null. | repair 1 decisions 2, 3 |
| 11 | T12's doctor column is "Flagged by doctor" yes/no, because `ok`/`FAIL` are verdict words. H10 is printed "not raised in this version". `errors.py` is unchanged (lane E's table). | repair 1 decisions 4, 5 |
| 12 | F14's engine inputs are constants (`F14_CASES`), not read from the Newcombe file. | repair 2 decision 2 |
| 13 | Markers: every `ap3` test also carries `day9`, and `ci.yml` runs `-m day9`. | build note 10 |

## For the orchestrator

- **The merge into main (after E9, which is closed at `9806d3d`).** Measured now: `comm -12` of `git diff --name-only e17a510 main` and `e17a510 a-p3-release` gives **`scripts/mutation_sweep.py` only**. The branch appends `AP3_MUTANTS` (25) after `AP2_MUTANTS` and adds `Dockerfile`, `.dockerignore`, `.github` and `uv.lock` to `COPIED`. `git merge-tree --write-tree main a-p3-release` exits 0.
  - The files the branch changes that E9 might also have touched are `src/proofpack/cli.py` (the `fixtures` subparser, `cmd_fixtures` and the handler table), `pyproject.toml` (four force-include lines and the `ap3` marker), `.github/workflows/ci.yml` (`docker-smoke` before `audit`), `src/proofpack/errors.py` (exit 6), `src/proofpack/resources.py`, `tests/test_offline.py` and `.gitignore`. `templates/base.html` is not changed.
  - Lens 3 regression's trial merge at `43521dd` gave `1544 passed, 1 skipped, 1 xfailed`.
- **What to check after the merge:**
  1. `PROOFPACK_REGEN_GOLDEN=1 python -m pytest tests/test_t12.py -k golden`, then `git diff` on the golden. It depends on `errors.py`'s code tables, `scope.SHORT_FORM`, the guidance map and `templates/base.html`; E9 touched `templates/`.
  2. `test_each_failure_mode_code_is_raised_somewhere_or_says_it_is_not`: its unraised list is `["H10"]`.
  3. `python scripts/mutation_sweep.py --marker ap3` (25 / 25 at `43521dd`).
  4. `tests/test_fixtures_cmd.py::test_the_committed_oracles_equal_a_fresh_capture` fails on any statsmodels or scikit-learn last-digit move until `scripts/capture_fixture_oracles.py` is re-run.
- **The three open sentence violations in shipped files** (FA3-S1, RG3-S1, FA3-S3). Each is a rename or a one-line change; fix them before the merge or in the merge commit.
- **Line endings.** `git ls-files --eol` shows every committed file as `i/lf` (measured now). The working copy of `scripts/mutation_sweep.py` is CRLF (repair 2 note).
- **The day-8 carry.** `proofpack fixtures` is now in `tests/test_offline.py` (`test_fixtures_offline_opens_no_socket`). A-P2 "Tomorrow needs" item 2 is done.

## What S4 needs (day 10, DEC-43)

- **Release assets.** `release.yml`'s `github-release` job (never run) attaches these to the GitHub release of tag `v<version>`:
  - `dist/proofpack-<version>-py3-none-any.whl`;
  - `release/fixtures_report.json`;
  - `release/sbom.cdx.json`;
  - `release/sample/run.json`, attached with the display label `sample-run.json` (the file name stays `run.json`);
  - `release/T12.html`.
- **Workflow artefact and image.** The same files plus `requirements.lock` are uploaded as the workflow artefact `release-assets`. The image is `ghcr.io/<owner>/proofpack:<tag>`.
- **T9 is not among the assets.** It belongs to S4 / E10.
- **Nothing exists until Josh's first tag.** The site stays pinned at `a0c9abc` until S4 moves `ENGINE_REF`.
- **Schema change.** `fixtures_report.json`'s `values[].tolerance` may be `null`, only on a `not_matched` row whose oracle lacks a declared value, and `values[].oracle` may be `null`. The schema is `schema/fixtures_report_schema.json`.
- **A page that may not exist.** The command prints `docs: /docs/fixtures`, and no such page is known to exist on the site (not checked). S4 either writes it or the hint changes.

## Re-run these (measured now, 24 September 2026, `wt-ap3` at `43521dd`, both shells)

Prefix: Git Bash `export PYTHONPATH=C:/Users/joshs/GPS/ProofPack/wt-ap3/src`; PowerShell 5.1 `$env:PYTHONPATH = 'C:\Users\joshs\GPS\ProofPack\wt-ap3\src'`. `python -c "import proofpack;print(proofpack.__file__)"` printed `C:\Users\joshs\GPS\ProofPack\wt-ap3\src\proofpack\__init__.py` in both. Platform `win-amd64-cp314`.

| Command (in `wt-ap3`) | Git Bash | PowerShell 5.1 |
|---|---|---|
| `python -m pytest -q -p no:cacheprovider` | `1535 passed, 1 skipped, 1 xfailed in 119.79s` | `1535 passed, 1 skipped, 1 xfailed in 119.00s` |
| `-m ap3` / `-m ap2` / `-m day8` / `-m day9` | `129 passed` / `89 passed` / `469 passed` / `279 passed` | the same four |
| `python -m ruff check .` / `python -m ruff format --check .` | `All checks passed!` / `213 files already formatted` (before this note was written; the two lens-3 notes were untracked) | the same |
| `python -m proofpack.cli doctor --offline` | `All essential checks passed.`, exit 0 | the same, exit 0 |
| `python -m proofpack.cli fixtures --offline --out D --r-captures` | `rows 43: matched 30, not matched 0, no oracle recorded 2, not built 6, compared by the test suite only 5`; `r-captures: r_captures_not_captured`; exit 0; 1.17 s; `git_sha` `43521dddb74b…` | the same `rows 43` line, exit 0, 1.16 s |
| `python scripts/build_t12.py --report D/fixtures_report.json --out T` | `49371 bytes`, exit 0 | `49371 bytes`, exit 0 |
| `python scripts/f17_determinism.py --out D --n 5000` | five `equal` (prefixes above), `[4, 4]`, `identical ...: yes`, exit 0, 2.90 s | the same five prefixes, exit 0, 2.80 s |
| `python scripts/sbom.py --out X` twice, then `cmp` | `62 components: 13 required, 1 optional, 48 excluded`; identical; SHA-256 `e5c13c67928b10d8…` | the same line; `E5C13C67928B10D8…` |
| `python scripts/capture_fixture_oracles.py --check` | exit 0 | exit 0 |
| `python scripts/mutation_sweep.py --marker ap3` | `25 planted, 25 killed, 0 survived; 248 s` | not run |
| `git merge-tree --write-tree main a-p3-release` | exit 0 (main `9806d3d`) | not run |

The skip is `tests/test_doctor_cli.py:57` (write access cannot be revoked for this user). The xfail is F13, which is pending the R capture (day 12).

## Tomorrow needs (day 10 = A-P4, `spec/briefs/day10_A.md`)

**Day 9 was a short day, and it spilled over.** It started late on Wednesday 23 September. Lens round 2 was cut off at about 12:50 on Thursday and re-run cold, and the lane closes on Thursday 24 September. Thursday is day 10 on the calendar, so A-P4 has not started.

What the short day cost:

- nothing was measured on the reference platform;
- every release job is written and not run;
- a third repair round for the four lens-3 sentence violations did not happen;
- S4's site half waits on a tag.

A-P4 is the DOCX renderer. It covers:

- docxtpl templates T1, T7 and T8 (T2 if E10 lands early) in the `[docx]` extra;
- a footer on every page, and the suppression markers;
- matplotlib figures with 1e-9 coordinate parity against the SVG;
- a python-docx round-trip test (every number equals the JSON);
- HTML still rendering with `[docx]` absent;
- D5 section 3.5's named styles, whose colour values come from `design/tokens.json`, tested equal to the HTML theme's;
- fixed zip mtimes if byte-identical output is claimed.

What it inherits:

1. **None of `docxtpl`, `docx` (python-docx) or `matplotlib` is importable on this machine**, and none is in `uv.lock` (measured now: 0 lock entries). Adding them to the `[docx]` extra and re-locking downloads packages, which this machine's rule makes an ask (Needs from Josh 5). All three are free.
2. **T12 has no DOCX.** If a T12 DOCX is wanted, it is an A-P4 decision.
3. **The merge first.** A-P4 builds on engine `main` with A-P3 merged, or the T12 and `errors.py` exit-6 changes are absent.
4. **The three open sentence violations** land in the merge commit or as A-P4's first commit.

## Needs from Josh

1. **TestPyPI (free).** An account, and a trusted publisher for your GitHub owner, repository `proofpack`, workflow `release.yml`, environment `testpypi`. No token goes into the repository.
2. **The first tag, after the merge.** It must equal `v` + the `pyproject.toml` version (today `v0.1.0.dev1`); the `build` job refuses any other. TestPyPI refuses a second upload of the same version, so each later tag needs a version bump. The tag is when the six `release.yml` jobs first run. Read that run rather than the YAML.
3. **GHCR visibility.** The first push creates the package, private by default `[unverified]`. Public or private is your call. After the first CI run, also approve two pins: the base-image digest that `docker-smoke` prints, and the Trivy action's commit sha.
4. **The day-10 people track** (from the brief):
   - the test-mode end-to-end purchase;
   - `proofpack licence install` from the emailed licence;
   - reading T12 as the RA lead who must file it. It is at `release/T12.html` after the tag; for today, render one with `python scripts/build_t12.py --report <report> --out <dir>`.
   - Also the brief's review hour on the T1 DOCX once A-P4 lands.
5. **Permission to download `docxtpl`, `python-docx` and `matplotlib`** into the lock for A-P4's `[docx]` extra. All are free, but this machine's rule needs your yes for any download.

Nothing is paid.

## Open questions (at most five)

1. **RG-N7 / RG-N8.** Should the release `fixtures_report.json` and T12 be generated inside the `python:3.12-slim` image, so that "reference platform: yes" is measured there? And should T12 get its own footer in place of D4 section 7.1's customer-run footer?
2. **RG-N6.** Should the eleven register rows get `oracle_source.kind` `spec_register` (a schema enum change), or keep `published_table` with the D1 line quoted?
3. **FA-R10 / RG-N9.** Should `install-from-testpypi` drop `--extra-index-url` and install the runtime dependencies from `requirements.lock` with hashes, then `proofpack` alone with `--no-deps`?
4. **Build note Q3.** D1 section 3.2 prints F3's logit lower bound as 0.3748; the engine and the independent DeLong capture give 0.374858. Correct the register, or keep the 1e-4 class as the record?
5. **Build note Q2.** With scipy absent, `fixtures` exits 6 (four Clopper-Pearson rows `optional_dependency_missing: scipy`). Right for an install without `[stats]`, or should those rows be `not built`?

## Sentences refused

Aggregated from the build note, both repair notes, the six lens notes and this note:

- "`proofpack fixtures` validates / qualifies the installation" and "the engine passed its fixtures". The report says matched or not matched per row; 30 of 43 rows were compared.
- "`proofpack fixtures` never raises" / "the report is always written" / "reports every oracle defect as not matched" / "every unreadable oracle file is not matched" / "writes a report for every damaged oracle file". A directory in place of an oracle file exits 5 with no report (FA3-S2, RG3-N1).
- "`proofpack fixtures` makes no network call". Lenses 1–3 measured named invocations under one audit hook, and nothing beyond them.
- "Two runs give an identical manifest hash" / "F17 proves hash identity" / "byte-identical on the reference platform". Identity holds under a three-key mask, measured on `win-amd64-cp314` only.
- "F17 compares everything a run writes" / "F17 compares the run directories' file names". It compares five named checks, and a count-only mutant of `file_names` survives (FA3-R1).
- "Every engine run in the workflows is offline" / "the workflow test catches every engine run without `--offline`" / "the workflow test requires `--offline` as a word of its own". The test inspects what its three patterns match; RG2-N2 and FA3-S1 list forms it misses.
- "The release pipeline / Docker image / Trivy / ZAP / TestPyPI install works". None has run.
- "The ZAP job does not fail on findings" / "the ZAP artefact is uploaded after a failed scan". The job has never run, and `-I` is `[unverified]`.
- "No key material enters the image" / "the Dockerfile test finds any key material". The wheel carries the public verify key, docker build has never run, and a planted key `LABEL` passes (FA3-S3).
- "The SBOM is valid CycloneDX 1.5". The check was structural, and the schema was not fetched.
- "Dependabot keeps uv.lock current". The `uv` ecosystem is `[unverified]`.
- "T12 is CSA-compliant". It is CSA-structured.
- "The Newcombe `[unverified]` marking always reaches the page". In a tree whose `pyproject.toml` names `proofpack`, the marking is the file's own.
- "`git_sha` always names ProofPack's own commit". It names HEAD of a directory that meets two conditions, measured on the layouts the tests feed.
- "Doctor's conditions held" (T12) and "each code is raised by the engine". Doctor passes `ok=True` as a literal for four rows, and H10 is not raised.
- "Deleting an oracle is always reported as not matched" (lens 2, before repair 2), and "M5, M12 and M14 are harmless" (lens 2 regression).
- This note: "A-P3 is done" and "the lane is closed". Four sentence violations are open and no workflow job of A-P3's has run. Also refused: "18 items are carried" as my own count; the count is the orchestrator's, and my table groups by finding id.
