# Lane E, build day 10 (E10) - lens 1, fresh attack, 25 September 2026

**Verdict: FAIL.** One blocker: under a clustered plan every sensitivity / specificity (PPA / NPA) paired difference is refused with the typed reason `insufficient_clusters` and printed as `n.e. (insufficient_clusters)` on Table T2-3, T2-4 and T2-5, and a `paired_difference_vs_prior` criterion on sensitivity or specificity is `not_assessable / no_interval`, while the same run prints a `cluster_bootstrap_percentile` interval for the new version's own sensitivity on the same 26 cases and the day-5 resampler for a conditioned cell (`clustered_flat`) gives the difference an interval of `[-0.2159, +0.0204]` on the same data. The reason is false on the page and the criterion status is wrong by the engine's own rules. Everything else I tried held: the McNemar, Newcombe paired and DeLong figures match my hand derivations to at most 1.7e-16, no margin reaches run.json or the page without a customer criterion, no forbidden word reaches T2, injection is escaped, the marks and footers are on every page.

Lens worktrees: `322c5a4` and `81f1102`, detached, under the scratchpad; `PYTHONPATH=<wt>/src` forced and proved (`proofpack.__file__` printed the worktree path in every run). Nothing committed; the worktrees are removed. All numbers below are mine, measured today in the Git Bash shell.

## Blocker

### B1. Clustered plan: the Se / Sp paired differences carry no interval and a false reason; a paired Se / Sp criterion is `not_assessable`

`stats/comparison.py::_paired_proportion_cell` (lines 396-399) builds the resampler for a **conditioned** cell (the reference-positive pairs for sensitivity, the reference-negative pairs for specificity) with `clustered_by_case(sub.pos, ids)`, which stratifies cases by outcome class. On a conditioned cell every row is one class, the other stratum is empty, `Resampler.deficient_class` is set, and `bootstrap_percentile` refuses before drawing with `_refusal_reason("clustered", ...)` = `insufficient_clusters`. The day-5 module documents the resampler for exactly this cell: `clustered_flat` ("for a proportion inside one cell. The cell is already conditioned on the outcome ... so there is no second class left to stratify on"), which is what `stats/bootstrap.py::proportion_ci` uses for the run's own clustered sensitivity. Accuracy (both classes present) goes through and prints; sensitivity and specificity do not, overall and in every subgroup entry.

Evidence (F5 pair with a `case_id` column `c{i // 2}`, `clustering.unit: case_id`, plus a criterion `Cse`: sensitivity, `paired_difference_vs_prior`, op1, overall, `ci_lower_bound >= -0.30`; `scratchpad/lens-E10-r1-fresh-attack/cx/K2_cluster_se`):

```
comparison.differences.op1.sensitivity.number:
  {"est": -0.08, "ci_lo": null, "ci_hi": null, "method": "none", "flags": [], "n": 50, "n_cases": 26,
   "not_estimable_reason": "insufficient_clusters"}
comparison.differences.op1.accuracy.number:
  {"est": -0.08, "ci_lo": -0.16, "ci_hi": 0.02, "method": "cluster_bootstrap_percentile",
   "flags": ["newcombe_refused_clustered"], "n": 100, "n_cases": 50}
overall.op1.sensitivity (the new version's own, same 26 cases):
  {"est": 0.82, "ci_lo": 0.6470588235294118, "ci_hi": 0.9416666666666668, "method": "cluster_bootstrap_percentile", "n": 50, "n_cases": 26}
criteria_results Cse: status not_assessable, reason_code no_interval, detail {"not_estimable_reason": "insufficient_clusters"}
T2-3 sensitivity row: ... |n.e. (insufficient_clusters)||none||— / —||— (—)||—||—|
T2 sentence: "The change in sensitivity from version 1.2 to 1.3 on 100 paired cases was not estimable with an interval (insufficient_clusters)."
T2 sentence: "Criterion Cse (sensitivity difference against the prior version, overall, op1, CI lower bound >= −0.3; lens, 2026-09-25): not assessable (no_interval)."
cases holding a positive row, counted by hand from the CSV: 26
the same statistic through the two resamplers on the same 50 positive pairs, B = 200, seed 20240101:
  clustered_flat:     ci = [-0.215906362545018, 0.02042987407729052]  reason None
  clustered_by_case:  ci = [None, None]                                 reason insufficient_clusters
```

Under a clustered plan on the base F5 pair (`cx/K_cluster`) the same refusal appears on `differences.op1.sensitivity`, `differences.op1.specificity` and on both subgroup entries' `op1.sensitivity` / `op1.specificity` (n_cases 24 / 26), eight cells of fourteen; T2-4 and T2-5 print `n.e.` in each. The `comparison.py` module docstring says "The paired proportions take the same route under clustering ... with `newcombe_refused_clustered` on the rendered Number": false for Se / Sp (no interval, no flag).

Grade: **blocker** - a wrong status (`not_assessable / no_interval` on a quantity the engine's own method estimates) and a false typed reason on the page beside `n_cases: 26`.

Repro (one line, from a `322c5a4` checkout with `PYTHONPATH=<wt>/src`): `python <scratch>/lens-E10-r1-fresh-attack/cx.py <wt> <out>` prints the `K` block; or add `,case_id` = `c{i//2}` to both F5 CSVs, set `clustering.unit: case_id`, `proofpack compare ... --offline` and read `comparison.differences.op1.sensitivity.number.not_estimable_reason`.

Sentence violations attached to B1 (each asserts what a counter-example refutes):

- `tests/test_e10_comparison.py::test_a_clustered_plan_bootstraps_every_difference_and_carries_dec_09s_refusal` - the test inspects the accuracy, AUROC and Brier cells only; sensitivity and specificity are not bootstrapped.
- E10 note, item 1: "Clustered: every difference `cluster_bootstrap_percentile` with `analytic` = `clustered_data_analytic_ci_invalid` and the `*_refused_clustered` flag."
- E10 note, "Sentences refused": "on a clustered plan every paired difference is a cluster-bootstrap Number with DEC-09's typed refusal beside it".
- `src/proofpack/stats/comparison.py` docstring, lines 42-44 (quoted above).

Repair shape (for the repairer, not a decision): route the conditioned cells through `clustered_flat(ids, n_rows=n)` as `proportion_ci` does, keep the DEC-09 refusal as `analytic`, and add the counter-example above as a test that reads the sensitivity and specificity cells (overall and one subgroup entry) and fails at `322c5a4`; then a lens (DEC-12 (i)).

## Non-blocking (record and carry)

1. **The T2 golden test, and with it the day-10 sweep, fail in a CRLF checkout.** `tests/test_e10_t2.py::test_golden_t2_matches_the_committed_render` masks nine manifest keys but not `input_sha256` / `prior_input_sha256`, which hash the fixture CSV bytes; a fresh `git worktree add` on this machine (`core.autocrlf=true`) checks `tests/fixtures/f5/*.csv` out CRLF (`git ls-files --eol`: `i/lf w/crlf`; the main tree has `w/lf`), the hashes move (`c3a45a47...` / `ffdbabf0...` against the golden's `b5a7a966...` / `08709dca...`) and the page differs. Measured: full suite in the worktree `1 failed, 1644 passed, 2 skipped, 1 xfailed in 220.22s` (the second skip is `PROOFPACK_TR39_FULL` unset); `-m day10` `1 failed, 61 passed`; `scripts/mutation_sweep.py --marker day10` stops at its baseline (`1 failed, 47 passed ... stopping after 1 failures`, exit 2, 24 s). After `git -c core.autocrlf=false checkout -- tests/fixtures/f5/*.csv`: `62 passed`, sweep `14 planted, 14 killed, 0 survived; 146 s`, exit 0. The note's "compared LF-normalised" is true of the HTML only. Fix: mask the two hashes as well, or `.gitattributes` `eol=lf` on the fixtures.
2. **Two paired criteria on one metric and operating point: T2-3 prints the first only.** `Cfirst` (accuracy, `point_estimate >= -0.10`, met) and `C2` (accuracy, `ci_lower_bound >= -0.05`, not_met): the T2-3 accuracy row reads `Cfirst (row 1): point estimate >= −0.1 | criterion met`, `C2` is on the row nowhere, and the string `record of criterion not met (PCCP §VII.B(3) requires recording)` appears on the page 0 times (the sentence "Record of criterion not met: C2, ..." appears once). `render/t2.py::_margin_for` returns the first match. Table T2-2 and the sentences carry both. (`cx/I_two`.)
3. **The ledger sentence attributes a declaration the customer did not make.** T2 §7 and `LEDGER_STATEMENT` print "the manufacturer's declared limit is 3" for comparisons; the declaration is `ledger.warn_after_acceptance_runs: 3`, a limit on acceptance runs (the builder's need 36). The counts themselves are right: run and compare are kept under separate keys (`<sha>` and `compare:<sha>`), the second compare prints `prior_acceptance_runs 1`, `manifest.ledger_count 2` (`cx/V_ledger`).
4. **A y_pred-only pair prints a wrong typed reason.** With the score block declared `probability` and no score column mapped, the run's own block says `calibration_suppressed_reason.reason: no_score_column`, but `comparison.differences.brier` / `slope` carry `score_not_probability` and T2 prints "The change in Brier score ... was not estimable with an interval (score_not_probability)" (`_paired_calibration_cells` line 498). Sensitivity, specificity, accuracy and McNemar (`b 10, c 2`) are right; AUROC is `not_computed_this_run`. (`cx/S2_ypred`.)
5. **Unpaired AUROC: an arm short of negatives is labelled `insufficient_positives`.** `_unpaired_auroc_cell` collapses the four small-arm conditions into one reason and pools the counts: a new arm of 10 positives and 1 negative against a prior of 6 / 5 prints `insufficient_positives`, `n_pos 16`, `n_neg 6`. Measured through the function.
6. **`prior_version` absent from `criteria.yaml`: the PAIRED_DIFF sentence prints an empty slot.** "Sensitivity changed by −8.0 percentage points [−19.4, +2.6] from version  to 1.3 on 100 paired cases (Newcombe paired)." while the cover prints "not declared" (`cx/J_noprior`; 0 rejections).
7. **W12 wording on a label mismatch.** One pair whose `y_true` differs, with `--allow-unpaired`: the whole comparison is unpaired (as designed) but the warning reads "unmatched row_ids; unpaired methods used ..." with `{"label_mismatch": 1, "n_pairs": 99, "new_only": 0, "prior_only": 0}` - the row_ids match. (`cx/U_flip`.)
8. **Stale sentence in `stats/discrimination.py::paired_delong`** (not in the diff, made false by it): "Nothing in `src` calls this yet; the day-5 caller must not call it on a clustered table". `comparison._paired_auroc_cell` calls it on independent rows and routes clustered plans to the bootstrap, which is what the docstring asked for; the sentence should say so.
9. **False sentence in `tests/test_e10_comparison.py::newcombe_paired_by_hand`**: "the same status as `fixtures/newcombe_table2.json`'s three paired examples". That fixture is Newcombe's Table II of the *independent*-proportions paper (three unpaired examples, `56/70 - 48/80` etc.); there is no paired example in the repository.
10. **A licence payload with no `features` key is licensed for compare.** `run.compare_licensed`: `not isinstance(features, list) or "compare" in features`. Measured: payload without the key -> exit 0, `T2.html` written; `features: []` -> exit 4, `run.json` only, "does not carry the 'compare' feature". D1 §7's payload always carries the list, so this is reachable only from a hand-made licence signed with the key; recorded.
11. **U+2028 (LINE SEPARATOR) passes raw into T2** (2 raw occurrences from an author field); the same happens in T8 from `run` (2 raw), so this is parity, not a regression. U+202E and U+200F are escaped (`&#x202e;` x11, `&#x200f;` x2). No exploit found.
12. **`attainability_not_computed: method_not_wilson`** prints in T2-2's Detail column on every paired row (the builder's own carry 6); the reason named is not the reason.
13. **Figures in the note that differ from a fresh worktree**: "1646 passed, 1 skipped, 1 xfailed in both shells" holds for the main tree; in a detached worktree see item 1. The 15-byte difference in the timing test's T2 (the note: 73102 PowerShell v 73087 Git Bash): my Git Bash run prints 73102, so the shell is not the cause; no path or platform string reaches the T2 page or run.json (`AppData` count 0 in both); the builder's 73087 page is not available to diff.
14. **T7 from `compare --templates T7`** carries "no difference is reported against the overall cohort ..." (x2) and "stable sort order" - E9 method text, not on T2, not about the model; noted because the brief's list names both words. "non-inferiority margins" appears on T2 once, inside the long-form disclaimer's "does not select acceptance criteria, thresholds or margins" list.

## What I could not break (this is evidence)

All derivations in `scratchpad/lens-E10-r1-fresh-attack/hand.py`, pure Python, no repository code until the comparison line.

- **McNemar.** By hand (`2 * P(X <= min(b, c) | Bin(b + c, 1/2))` exactly in integers; `(|b - c| - 1)^2 / (b + c)`, `p = erfc(sqrt(s / 2))`) and `statsmodels mcnemar(exact=True)` / `(exact=False, correction=True)` on twelve tables: F5 `b 10, c 2` exact p `0.03857421875` = `2 * (1 + 12 + 66) / 4096`, corrected `4.083333 / 0.043308`; `b + c = 24` exact, `25` corrected; `b 0, c 7` p `0.015625`; `b = c = 0` p 1, no statistic; `b + c = 1` p 1; `b = c = 13` corrected statistic `1/26`; `b 100, c 0`; `b 24 / 25, c 0`. Worst deviation from either oracle `1.67e-16`; the method field named the route on every table. One version all correct through the CLI (`cx/P_allcorrect`): `b 0, c 10`, exact, p `0.001953125` = `2 / 1024`.
- **Newcombe paired method 10.** My own Wilson and the method-10 steps as I recall them (φ from `eh - fg`, corrected to `max(eh - fg - n/2, 0)` when positive, uncorrected when negative, 0 when a margin is zero; square-and-add) on eleven tables: F5 accuracy `(80, 2, 10, 8)` -> `-0.08 [-0.155356, -0.010249]`, φ `0.4946`; F5 sensitivity and specificity `(40, 1, 5, 4)` -> `-0.08 [-0.194203, +0.026030]` (the cells counted by hand from the CSVs); `g = 0`, `f = 0`, difference 0, identical all correct, identical none correct, φ negative `(5, 30, 30, 5)`, `B` in `(0, n/2]`, `h = 0`. Worst deviation from the engine `1.11e-16`; the frozen figure `(-0.1554, -0.0102)` reproduced at four decimals. The unpaired method 10 on the same margins (`82/100 v 90/100`) gives `(-0.1776, +0.0175)`, so the engine is not reusing the unpaired method. My recollection and the test's statement of the formula agree; neither is the paper, so the `no_independent_oracle` status is right and stays.
- **DeLong paired on R2 F3.** O(mn) placement components in pure Python: `AUC1 0.80, AUC2 0.64, S_aa 0.0240, S_bb 0.0392, S_ab 0.0280, Var(diff) 0.0072, z 1.88562, p 0.05935`, Wald `[-0.0063084589, 0.3263084589]`. Engine deviation: variance `6.9e-18`, estimate `1.1e-16`, bounds `1.9e-16` / `5.6e-17`. With the covariance dropped the variance is `0.0632` and the interval `[-0.3327, 0.6527]`: the engine differs from that by `0.056` in variance. Ties: a two-vector tie case deviation 0; an 80-row pair with one-decimal scores (ties in both arms) deviation `8.7e-19`. A clustered declaration routes AUROC to `cluster_bootstrap_percentile` with the DeLong refusal as `analytic` and `delong_refused_clustered` on the Number; T2 names the method in the cell.
- **The margin.** No paired criterion (`cx/G_nomargin`): T2-3's header has seven columns (`Metric ... McNemar p (method)`), `Margin` 0, `Status` 0 in the table, the caption says "no paired_difference_vs_prior criterion is declared, so no margin and no status column is printed"; run.json has `margin` 0, `paired_difference` 0; every `0.02` / `0.03` / `0.05` in run.json is a substring of an estimate; on the page `0.03` is `0.039` (the McNemar p and the Brier difference) and `0.05` is `−0.053` (a subgroup AUROC difference). Ten criterion variants on F5 accuracy (`ci [-0.1554, -0.0102]`, est `-0.08`) follow the declared statistic and comparator literally: `ci_upper_bound <= 0.0` met, `<= -0.10` not_met, `ci_lower_bound >= +0.05 / 0.0 / -0.10` not_met, `>= -0.16` met, `point_estimate >= -0.10` met, `<= -0.10` not_met, `ci_lower_bound <= -0.10` met, `ci_upper_bound >= -0.05` met - each `statistic_compared` with the compared value printed; D1 §2 says the same (one of three words, by the named statistic). Row 121: T2-2 prints `paired difference vs prior version` in the Type column and the sentence "Criterion C2 (accuracy difference against the prior version, overall, op1, CI lower bound >= −0.05; ...)".
- **Pairing.** Extra prior row `f5-999`, a prior row dropped, a label flipped: `HALT H12`, exit 3, nothing written (`detail {"unmatched": 1}` / `{"label_mismatch": 1, ...}`). A duplicated row_id in either table is `HALT H05: duplicate row_id values` at ingest, before H12. `--allow-unpaired` on the extra-row pair: exit 2, `W12`, label `not like-for-like`, seven difference Numbers each `newcombe10_not_like_for_like` / `delong_wald_not_like_for_like` or reason `unpaired_not_like_for_like`, all flagged (`UNLABELLED: []`), 22 occurrences in run.json, the T2-3 caption "UNPAIRED - not like-for-like; Newcombe 10 / unpaired DeLong (new version rows: 100; prior version rows: 101; rows in one version only: 1)", §4.6 and the UNPAIRED_LABEL sentence; C2 `not_assessable / not_like_for_like`. `--allow-unpaired` on a pair that pairs stays paired (no W12). Clustered and unpaired: every analytic refused `clustered_data_analytic_ci_invalid`, all labelled. Identical tables: `b = c = 0`, p 1, every proportion difference `0.0` with a Newcombe interval, AUROC / Brier / slope refused `boundary_estimate` (zero variance), C2 met on lower bound `-0.0305`, 0 rejections, no forbidden word.
- **T2.** Base F5 page: 5 pages, one footer each, 39 claims, 0 rejections, every `data-ref` cell resolves (the day-10 test's own check passed in my run) and every digit run outside those cells traces to `criteria_results` (`0.692`, `0.929`, `41/50 (82.0%) [69.2, 90.2]`, `−0.155`), the declarations (`0.7`, `−0.05`, `-0.08` and `-0.05` inside the fixture's justification), the manifest (run id, engine version, timestamp, SHA-256 prefix), the guidance map (`2024-12-04, updated 2025-08-18`, `FDA-2024-D-4488`, `January 2025`) or section and table numbering; the McNemar row prints `10 / 2 | 0.039 (exact)`; the record string once, on C2's row, and on no `met` row (`cx/I_two`, `cx/R_star`); status words only inside `.status` (the day-10 grep test, re-run). Forbidden words (my list of 35 including `non-inferior`, `equivalent`, `superior`, `improved`, `degraded`, `better`, `worse`, `significant`, `no difference`, `unchanged`, `stable`, `pass`, `fail`, `verdict`): none outside the disclaimer on any of the 33 pages I rendered. Every PCCP anchor is labelled `(2024-12-04, updated 2025-08-18, final)`; 14 draft labels for the 4 AI-DSF anchors cited. Injection through the criterion id, author, justification (10,000 characters + `<script>` + `{{ 7*7 }}` + U+2028), model name, both versions and a `site` level label: `<script>` 0 raw / 1 escaped, `{{ 7*7 }}` literal x20 and `>49<` 0, `<i>` 0 raw, `<b>` 0 raw, U+202E escaped x11, U+200F escaped x2, the 10,000-character string once inside `.customer-text`, 0 rejections, the criterion `{{ 7*7 }}` evaluated `not_met` (`cx/M2_inject`). Marks: the sample pack's T2 (`build_sample_pack.py --compare`: compare.json 328,965 bytes, T2 68,905, the note's PowerShell figures) carries `NO LICENCE - not for submission · SYNTHETIC - illustrative` on every page (2, 2, 1, 1, 1) and one footer per page, 0 criteria, 400 pairs; a licence expired 10 days inside a 30-day grace writes T2 with `LICENCE EXPIRED - not for submission` on every page (2, 1, 1, 1, 1); a trial licence `TRIAL` on every page; no licence -> exit 4, run.json only, `NO LICENCE - not for submission`; `features` without `compare` -> exit 4, run.json only, watermark null.
- **Wiring.** `socket.socket` patched to raise: `compare --offline` 0 calls, exit 0; `compare` without `--offline` on the fixture (`egress.telemetry: false`) 0 calls, "telemetry skipped (egress.telemetry: false)". `--templates T1` -> exit 5, `error: unknown --templates id 'T1'; choose from T2, T7, T8`, nothing written; `T7,T8` written beside run.json. `import proofpack`, `proofpack.stats`, `stats.comparison`, `stats.proportions`, `stats.discrimination` with `statsmodels`, `sklearn`, `scipy` and `jinja2` hidden: ok, none loaded. 33 counter-example run.json documents validate against `output_schema_v1.json` with 0 errors (paired, unpaired, clustered, clustered-unpaired, y_pred-only, injected). Ledger: run and compare counted apart (item 3).
- **Damage elsewhere.** In the `322c5a4` worktree: `-m day9 328 passed` (326 at `81f1102` + the two `PAIRED_DIFF` cases), `day8 470`, `day7 139`, `day6 285`, `day5 54`, `ap2 90`, `ap3 167` (collected 62 / 328 / 470 / 139 / 285 / 54 / 90 / 167 of 1648); lint `All checks passed!` / `241 files already formatted`. The E9 r3 handoff's "Re-run these" block (`rerun.sh`, run from the main tree at `322c5a4`, 244 lines) reproduces the r3 lines: doctor exit 0 with 17 `[ok`, tr39 `vendored subset matches`, sweep list 345 lines (16 ap2, 39 ap3, 14 day10, 34 day5, 112 day6, 25 day7, 87 day8, 18 day9), no-licence run exit 4 with the DEC-48 mark and `253938 bytes 20 keys 70 claims 0 rejections`, H07 `exit=3 dir=no`, `html_run` exit 0 with T8 44607 bytes, repros5 / repros6 / p3_fair / p2_star / p7_site / alias / offline (`rc 0 socket calls 0`) / paths / imports (`loaded: []`) / verdict grep `5 passed` / E6 schema `1 passed` / k1_checker `engine claims rejected: 0` all as the r3 note prints them; `git diff 81f1102 322c5a4` touches no file under `stats/` other than the new `comparison.py` and `number.py`'s three enum additions, no threshold and no existing interval function (`proportions.py`, `discrimination.py`, `bootstrap.py`, `calibration.py` unchanged), `design/tokens.json` unchanged. The six day-10 test files copied into the `81f1102` worktree: five fail at collection (`stats.comparison`, `render.t2`, `assemble_compare` absent), `test_sweep_day10.py` `2 failed`; no day-10 test passes on the old code. Sweep (LF checkout): `14 planted, 14 killed, 0 survived; 146 s`.
- **Timing, fixtures, captures.** `tests/test_e10_timing.py` (100,000-row pair, B = 200, a paired criterion): `compare --templates T2 23.78 s; T2 render from run.json 0.03 s; T2 73102 bytes`, `1 passed in 24.59s` (budget 120 s); my Git Bash run reproduces the orchestrator's 73102 bytes, so the builder's 73087 is not a shell effect (still unexplained, item 13). `proofpack fixtures --offline`: `rows 46: matched 33, not matched 0, no oracle recorded 2, no independent oracle 1, not built 5, compared by the test suite only 5`, exit 0, on `win-amd64-cp314`. `scripts/capture_fixture_oracles.py --check`: `captured values: 72 identical, 0 differ within their tolerance, 0 differ outside it`.

## What I could not check

- The Newcombe paired formula against Newcombe 1998 (paired) itself: the paper is not in the repository and I fetched nothing. My hand derivation is a second recollection of the same steps, not an oracle; the `no_independent_oracle` row and the `[unverified]` marks are the right status.
- The 15-byte T2 difference between the two shells in the timing test (item 13): no PowerShell run.
- A pROC / R capture of the paired DeLong on F3: none exists in the repository.
- T2 in a browser or in print (the CSS furniture is E9's; I read bytes only).
- A `y_pred`-only pair declared as anything but `score.type: probability` (`H08: declaration invalid at score/type: enum`; the enum is `probability | logit | other`).

## Sentences I refused to write

- "The clustered comparison carries a cluster-bootstrap interval on every difference" - refuted by B1.
- "The Newcombe paired interval is verified" - two recollections agree; no primary source.
- "The full suite passes at 322c5a4" - it passes in the main tree and fails in a CRLF checkout (item 1).
- "T2 prints every not_met criterion's record string" - refuted by item 2 (two criteria on one cell).

## Re-run these

`<scratch>` = `C:/Users/joshs/AppData/Local/Temp/claude/C--Users-joshs-GPS-ProofPack/0e5d0ccc-8634-4ba1-aad8-15203d8e0928/scratchpad/lens-E10-r1-fresh-attack`; `<wt>` = a detached `322c5a4` worktree; `PYTHONPATH=<wt>/src`, `PYTHONIOENCODING=utf-8`.

| Command | Result |
|---|---|
| `python <scratch>/hand.py <wt>` | the McNemar / Newcombe / DeLong deviations above (worst `1.67e-16` / `1.11e-16` / `6.9e-18`) |
| `python <scratch>/cx.py <wt> <out>` and `python <scratch>/cx2.py <wt> <out>` | blocks A-W above; the `K` block is B1 |
| `python -m pytest -q -p no:cacheprovider` (CRLF worktree) | `1 failed, 1644 passed, 2 skipped, 1 xfailed in 220.22s` |
| `git -c core.autocrlf=false checkout -- tests/fixtures/f5/*.csv; python -m pytest -q -p no:cacheprovider -m day10` | `62 passed, 1586 deselected in 39.52s` |
| `python scripts/mutation_sweep.py --marker day10` (LF) | `14 planted, 14 killed, 0 survived; 146 s` |
| `bash <scratch9>/rerun/rerun.sh lens1e10` (main tree) | `<scratch>/rerun_r3.txt`, 244 lines |
