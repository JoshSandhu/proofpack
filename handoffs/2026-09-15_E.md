# Lane E — build day 5 — 2026-09-15

**The third fresh-attack lens returned `PASS` on `e4c4f7e` (0 blockers, 7 non-blocking, 0
sentence violations) and its regression twin `PASS` (0 blockers, 5 non-blocking), after two
`FAIL` rounds whose four blockers each have a test that fails on the pre-fix tree and a
re-execution of the finding's own construction by the next lens on the repaired tree. Thirty-three items are carried — nineteen behaviours
with no observer in the suite, four imprecise sentences or records, and ten annotation-policy,
declaration-check and design items, each with a one-line repro below; two of them are
decisions for you (needs-from-Josh 1 and 2), including the DEC-08 half under which a
clustered cell of 10–40 cases at a high proportion still renders an interval measured too
narrow, with only a tier annotation.**

Three commits on `main` on top of `3ea2bfd`, not pushed (`ahead 11` of `origin/main` at
`e4c4f7e`; the commit carrying this note and the two lens-3 notes makes it 12): `d1fd20a`
(build), `9da4401` (repair round 1), `e4c4f7e` (repair round 2). Suite at
`e4c4f7e`, measured now in both shells: **364 passed / 1 skipped / 1 xfailed**; `-m day5`
**54 passed**; the `3ea2bfd` baseline was 310 / 1 / 1 (lens-1 regression note, worktree),
so the difference is exactly the 54 day-5 tests. Lint, format, doctor clean in both shells.
`git diff --name-status 3ea2bfd HEAD -- tests/` is `A tests/test_subgroups.py` alone; the
day's added test lines contain no `skip`, `xfail` or `.only` (grep count 0), and every lens
confirmed nothing deleted or un-marked.

---

## What landed

Every item of `spec/briefs/day5_E.md` (1–7) is built. The build commit is `d1fd20a`; the
repair commits are named where they changed an item. Oracle figures are the lens notes'
re-derivations (their own code from the published formulae, no repo code) unless marked
"measured now".

| # | Item | Module | Commit | Test ids (`tests/test_subgroups.py`) | Oracle and measured figure |
|---|---|---|---|---|---|
| 1 | Per-level metrics: six proportions, AUROC behind the R2 §3.3 10/10 rule, Brier for declared probabilities, Unknown row, reference rule, tiers in resampling units | `stats/subgroups.py`: `subgroup_analysis`, `_level_row`, `_proportion_cell`, `_auroc_cell`, `_brier_cell`, `_choose_reference`, `band_ages` | `d1fd20a`; `9da4401` added the numpy pin | `test_every_recognised_attribute_is_tabulated_and_undeclared_ones_are_exploratory`, `test_age_is_banded_by_the_declared_bands_half_open_and_labelled_lo_hi`, `test_age_falls_back_to_the_d1_default_bands_when_not_declared_and_says_so`, `test_missing_attribute_values_form_the_unknown_row_which_joins_every_complement`, `test_the_unknown_row_is_never_the_reference_even_when_it_is_the_largest_level`, `test_reference_ties_go_to_the_first_level_in_the_level_order_and_the_rule_says_so`, `test_a_declared_reference_level_that_is_not_observed_halts_h09`, `test_a_declared_reference_whose_rows_are_all_excluded_halts_h09_naming_the_analysed_rows`, `test_auroc_below_ten_positives_or_negatives_is_shown_not_estimable_never_omitted`, `test_brier_is_the_plain_mean_squared_error_with_a_bootstrap_interval_only_for_probabilities`, `test_tiers_count_resampling_units_not_rows_under_clustering`, `test_two_by_two_counts_per_level_sum_to_the_level_and_selection_rate_is_the_called_share`, `test_every_per_level_proportion_matches_numpy_on_the_raw_columns_with_its_k_and_n` | Wilson vs `statsmodels.proportion_confint(method="wilson")` on 300 random (k, n): max deviation 2.2e-16 to 3.3e-16 (lens 1, 2 and 3 fresh-attack); F8 half-widths 0.0851 / 0.0596 / 0.0341 reproduced (all six lens notes); Brier vs `sklearn.metrics.brier_score_loss` in the test; PPV / NPV / accuracy / sensitivity for `sex = F` on `make_cohort(400)` equal the lens-3 regression note's numpy hand counts to 6 dp (0.6375 / 0.881356 / 0.782828 / 0.784615) |
| 2 | Differences: Newcombe-10, unpaired DeLong (new `discrimination.unpaired_delong`), two-sided cluster bootstrap with independent sides, span refusal, frozen-side refusal | `_proportion_difference`, `_auroc_difference`, `_bootstrap_difference`, `_frozen_sides`, `_shared_cases`; `stats/discrimination.py: unpaired_delong` | `d1fd20a`; `9da4401` (frozen-arm refusal, i.i.d.); `e4c4f7e` (frozen-side refusal, clustered) | `test_f14_newcombe_table_ii_examples_drive_diff_vs_complement`, `test_f14_diff_vs_complement_hand_computed_and_frozen`, `test_unpaired_delong_difference_is_the_sum_of_two_oracled_variances`, `test_the_auroc_difference_carries_z_and_p_as_detail_never_as_a_verdict`, `test_unpaired_delong_refuses_the_difference_when_either_arm_has_zero_variance`, `test_a_case_spanning_two_levels_refuses_the_difference_with_a_typed_reason`, `test_without_a_spanning_case_the_differences_are_cluster_bootstrapped_with_refused_flags`, `test_the_two_sided_bootstrap_is_deterministic_and_the_policy_seed_moves_only_the_bounds`, `test_the_two_sided_bootstrap_draws_side_a_then_side_b_from_the_cell_generator`, `test_the_two_sided_cluster_bootstrap_refuses_a_difference_whose_one_side_is_frozen`, `test_frozen_sides_applies_the_single_cell_percentile_rule_to_each_side`, `test_x1_there_is_no_diff_vs_overall_anywhere` | Newcombe-10 (square-and-add of the Wilson limits) vs `newcombe10_bounds` on 200–220 random pairs incl. boundary pairs: 2.2e-16 / 3.6e-16; F14 Table II (0.0524, 0.3339), (0.1705, 0.8090), (0.6791, 1.0000) and the frozen 30/40 vs 100/120 → −0.0833 (−0.2453, 0.0493) reproduced to 4 dp with `[unverified against the primary PDF]` carried (lens 1–3 regression); DeLong 1988 placement-value variance vs `unpaired_delong` on 50 random score/label pairs: AUC 1.1e-16 to 3.3e-16, relative variance 2.6e-16 to 4.2e-16, p 2.9e-15 to 1e-9 (lens 1–3 fresh-attack) and 2.1e-15 across est, variance, bounds and p on 20 cohorts (lens-1 regression); F18 injection re-measured at seeds the builder did not use, three times: B's `diff_vs_complement` excludes 0 in 50/50 (lens 1, seeds 30000–30049), 49/50 (lens 2, 41000–41049), 50/50 i.i.d. and 50/50 clustered twin (lens 3, 61000–61049) |
| 3 | Fairness block: gaps are the reference differences, `fpr_gap` the mirrored specificity gap, selection-rate gap `descriptive_not_target`, criterion and bound echoed, no status | `_fairness`, `_mirror`, `_mirror_cell` | `d1fd20a` | `test_fairness_gaps_are_the_reference_differences_and_fpr_gap_is_the_mirrored_specificity_gap`, `test_fairness_records_the_criterion_and_bound_verbatim_and_emits_no_status`, `test_fairness_gaps_cover_every_non_reference_level_including_unknown` | `fpr_gap` = −(specificity difference) with mirrored bounds and equal to Newcombe-10 on the FP counts directly to 1e-16 (lens-1 fresh-attack); `[unverified` in `impossibility_citation` enforced by the schema pattern; lens 3 re-read the frozen refusals through the block (`tpr_gap` / `ppv_gap` / `npv_gap` `boundary_estimate` with `frozen ['a']`, `fpr_gap` mirrored with the reason kept, 0 schema errors) |
| 4 | Heterogeneity footnote: Pearson χ² (no Yates) / Fisher at exactly two levels with an expected cell < 5, one Holm family per attribute, refused under clustering (DEC-09 reason), `insufficient_levels` first on every route, `clustering_route` required with no default | `heterogeneity_footnote`, `_homogeneity_test`, `_clustered_refusal`, `holm`, `FISHER_EXPECTED_BELOW` | `d1fd20a`; `9da4401` (clustered refusal); `e4c4f7e` (precedence, no default) | `test_f6_chi_square_homogeneity_of_sensitivity_across_three_sites`, `test_f6_fisher_exact_site1_vs_site2`, `test_fisher_replaces_chi_square_only_for_two_levels_with_a_small_expected_cell`, `test_f6_holm_adjustment_matches_the_fixture_and_statsmodels`, `test_the_footnote_is_exploratory_with_no_status_key_beside_any_p_value`, `test_holm_is_one_family_per_attribute_across_operating_points_and_both_metrics`, `test_the_module_imports_and_the_footnote_degrades_with_scipy_hidden`, `test_no_module_level_scipy_import_in_subgroups`, `test_the_heterogeneity_footnote_is_refused_on_clustered_rows_with_the_dec09_typed_reason`, `test_fewer_than_two_evaluable_levels_is_insufficient_levels_on_every_route`, `test_the_heterogeneity_footnote_has_no_default_clustering_route` | F6: χ² 4.6327, df 2, p 0.0986 (hand `sum (O−E)²/E`, p = e^(−χ²/2)) and vs `chi2_contingency(correction=False)`; Fisher 0.1084 vs `fisher_exact` (hypergeometric sum); Holm [0.036, 0.08, 0.30] and 100 random p-sets vs `multipletests(method="holm")` at 0.0; 200 random r×2 tables, Fisher branch taken exactly when r = 2 and min expected < 5: 0.0 / 1.1e-16 (lens 1 and 3 fresh-attack; lens 2: 5.6e-17). F6 tripled under a `case_id`: χ² 13.898, p 0.00096 on rows at `d1fd20a`; refused on `declared` and `detected` at `9da4401` (lens-2 regression) and `e4c4f7e` (lens-3 regression) |
| 5 | DEC-08 coverage bar as a committed script | `scripts/coverage_bar.py` v1, seed 20260915 | `d1fd20a` (docstring timing `9da4401`) | none (a script, run and recorded; table in `design/conventions_T7.md` and the constants' docstrings) | `--full` re-run now in both shells and by the lens-1 regression note: see the coverage section |
| 6 | DEC-12(ii) mutation sweep as a committed script | `scripts/mutation_sweep.py` (copy of the working tree, `PYTHONPATH` forced and asserted from the copy) | `d1fd20a`; mutants added in `9da4401` (8) and `e4c4f7e` (5) | none (a script) | **34 planted, 34 killed, 0 survived** now in both shells (see Re-run these); 21 / 21 at `d1fd20a`, 29 / 29 at `9da4401` (lens 1 and 2 notes) |
| 7 | Schema and invariants: `Cell`, `subgroupRow`, `subgroupAttribute`, `fairnessBlock`, `heterogeneityTest`; flag `newcombe_refused_clustered`; reasons `cases_span_both_groups`, `insufficient_levels`; DEC-10 census; X1 grep; verdict-word grep | `schema/output_schema_v1.json`, `stats/number.py` | `d1fd20a`; `9da4401` (`clustering_route` required); the verdict word set grew in `9da4401` and `e4c4f7e` | `test_the_day5_output_validates_against_the_output_schema`, `test_the_schema_rejects_a_verdict_beside_a_p_value_and_a_status_in_fairness`, `test_every_number_in_the_output_has_a_ci_or_a_typed_reason`, `test_no_verdict_word_appears_in_any_key_or_engine_string_of_the_output`, `test_the_verdict_grep_flags_every_phrase_the_brief_names`, `test_dec10_every_proportion_number_is_wilson_or_the_cluster_bootstrap`, `test_a_case_id_column_is_used_even_when_not_passed_and_detected_clustering_routes_everything`, `test_a_supplied_plan_that_contradicts_the_case_column_is_refused`, `test_comparator_reference_standard_routes_to_ppa_npa_everywhere`, `test_the_enums_added_today_agree_between_code_and_schema`, `test_cells_carry_the_day4_audit_shape_and_cell_keys_are_unique`, `test_the_frozen_share_rule_refuses_the_share_exactly_at_the_constant`, `test_the_sentences_lens_2_falsified_are_gone_from_the_shipped_text` | jsonschema Draft 2020-12, 0 errors on every lens construction; DEC-10 census over fourteen constructed reports: 354 Numbers, 160 `cluster_bootstrap_percentile`, 194 `none`, no `wilson` / `newcombe10` / `delong_*` under a declared or detected plan (lens-2 fresh-attack); every serialised Number re-validates through `Number(**d)` with a CI or a reason in `NOT_ESTIMABLE_REASONS` (all three regression lenses) |

Day 4's three "tomorrow needs" are honoured and the lenses tried to defeat each:
`cluster_ids` on every cell (when the caller passes none, the table's `case_id` column over
the analysed rows is used; the `detected` route fires under `clustering.unit: none`); ids
sliced with the cell's rows; `paired_delong` and `two_by_two_metrics` are not called by the
module, every proportion goes through `proportion_ci` (DEC-10) and every AUROC difference
through `unpaired_delong` or the two-sided cluster bootstrap. The three attempts to reach
Wilson by a forgotten argument are recorded in the lens-1 regression note (N7) and re-run by
lens 2: each raises or routes to the cluster bootstrap. DEC-09 (version comparison) is day
10's and untouched.

## What was cut and why

Nothing in the brief's items 1–7 was cut, and the orchestrator recorded no cut. Two things
are not built, by decision rather than omission, and the build note's "Nothing was cut" was
corrected in repair round 1 to say so:

- **DEC-08's refusal below the bar is not implemented on either route for the
  `MIN_UNITS_PER_STRATUM` half.** The measurement (coverage section) shows no value of that
  constant on the grid renders only shapes at or above 0.90; a floor high enough would refuse
  fixture F3 (five positives, the R2 §9 pin of the resampling algorithm). The builder kept 2,
  did not edit a regulatory-facing fixture to make a constant look right, and escalated
  (needs-from-Josh 1). Today a clustered proportion cell of 10 cases at p = 0.9 renders
  `0.9 (0.7, 1.0)` with `very_low_precision` and `imprecise` and no refusal (lens-1
  regression N3, re-verified by lens 2), and the AUROC route renders the u = 2, m = 0 shape
  (lens-3 fresh-attack).
- **Intersectional cells** are v1.1 by the brief and were not attempted.

## What did not land, and why — the 33 carried items

Each is a lens finding that reproduces on `e4c4f7e` unless marked otherwise. "Survives"
means the mutant passes `-m day5` (or the full suite where stated) on this tree, as measured
by the lens named; the engine's behaviour on each was checked correct by construction by at
least one lens. The lens that owns the repro is named so its note can be opened for the full
construction.

**A. Behaviours with no observer in the suite (a regression would ship green).**

| # | Item | Why carried | Repro (one line) | Owner |
|---|---|---|---|---|
| 1 | Two-sided deficient-class check inspects side a only | Each needs its own oracle test; a repair round fixes blockers and false sentences (repair-1 decision) | plant `for res in (res_a,)` in `_bootstrap_difference`; `-m day5` passes | lens 2 N6 / lens 3 regression |
| 2 | The difference's own `lo == hi` refusal removed while the per-side check stays | Same; lens-3 regression tried 10,000 two-valued constructions, 5,239 with neither side frozen, 0 reached `lo == hi` — measured, not a proof the branch is dead | `if frozen or lo == hi:` → `if frozen:`; `-m day5` passes | lens 2 N6 / lens 3 regression |
| 3 | Yates on the two-level χ² | Same | `correction=True` in `_homogeneity_test`; `-m day5` passes | lens 1 N4 / lens 2 N6 |
| 4 | Clustered difference `n_cases = max(...)` | Same | `n_cases = max` in `_bootstrap_difference`; `-m day5` passes | lens 1 N4 / lens 2 N6 |
| 5 | `event_units` counted in rows under clustering | `test_tiers_count_resampling_units_not_rows_under_clustering` pins `n_units` only | `event_units = events`; `-m day5` passes | lens-1 regression N1 / lens 2 N6 |
| 6 | AUROC-difference evaluability checked on side a only | `delong_covariance` raises on a 1-positive complement | mutate the evaluability check to side a; `-m day5` passes | lens 1 N4 / lens 2 N6 |
| 7 | `lower_is_positive` orientation ignored | Lens 2 and 3 checked per-level AUROC = `roc_auc_score(y, −score)` to 6 dp today | `score = raw_score` always; `-m day5` passes | lens 1 N4 / lens 2 N6 |
| 8 | Single-class Brier not refused `single_class` | Decision 9 unpinned | bootstrap instead of refuse in `_brier_cell`; `-m day5` passes | lens-1 regression N1 / lens 2 N6 |
| 9 | Clustered proportion difference without tier flags | Also the FA-N3 policy question (item 24) | drop `precision_flags` on the clustered difference; `-m day5` passes | lens 1 N4 / lens 2 N6 |
| 10 | Declared `reference_level` not stripped | `" S1"` → H09 under the mutant | remove `.strip()` in `_choose_reference`; `-m day5` passes | lens 1 N4 / lens 2 N6 |
| 11 | A (0, 0) level entering the χ² table | scipy raises on the zero expected cell | let a level with no positives into the homogeneity table; `-m day5` passes | lens 1 N4 |
| 12 | Overlapping bands: last band wins | Convention inverted, no observer | flip the first-band-wins loop in `band_ages`; `-m day5` passes | lens 1 N4 |
| 13 | Brier computed on a `lower_is_positive` probability | Today `not_computed_this_run` with `score_type` in detail | remove the orientation gate in `_brier_cell`; `-m day5` passes | lens 1 N4 |
| 14 | Clustered Brier with the i.i.d. resampler | Today `cluster_bootstrap_percentile`, `resampling.kind == "clustered"` | resample rows in `_brier_cell` under a clustered plan; `-m day5` passes | lens-1 regression N1 |
| 15 | The Unknown row entering the homogeneity test | Repair-2 note lists lens 2's spelling as surviving; lens-3 regression's own spelling (`evaluable = list(attr.levels)`) is killed by `test_fewer_than_two_evaluable_levels_...`; lens 2's exact spelling was not available to it. Carried until one spelling is pinned by a test that names it | plant the Unknown row into `evaluable`; run `-m day5` and record which spelling survives | lens 1 N4 / lens 2 N6 / lens 3 regression N5 |
| 16 | The rendered two-sided cluster-bootstrap interval has no oracle test: `values_a − values_b.mean()` and `values_a.mean() − values_b` each survive the **full suite** (364 passed) | This is lens 2's FA-B1 defect class (an interval that is one side's alone) for every rendered clustered difference; the engine is right today (lens 3's thirteen-shape table, 0.904–0.990). Close with a test that recomputes one clustered difference's percentile bounds from the two sides' recorded draws (`_RecordingResampler` exists) or from an independent two-sided bootstrap written in the test. **First pick for the next E slot.** | plant either replacement in `_bootstrap_difference`; run the suite | lens 3 N1 |
| 17 | No test runs any bootstrap at a level other than 0.95: `percentile_bounds(usable, DEFAULT_LEVEL)` in day-4 `bootstrap_percentile`, and `_frozen_sides(..., DEFAULT_LEVEL)`, survive the full suite | Unreachable today (`level` is a keyword of `subgroup_analysis` only; `cli.py` calls no statistics; `criteria_schema.json` has no interval level); reaches every customer the day a level is declarable | `run(cols, crit, level=0.90)` and compare a bootstrap cell's bounds to `level=0.95` | lens 3 N2 |
| 18 | Reported counts on the clustered difference unpinned: `n = max(n1, n2)` (i.i.d. route reports `n = min`) and `resample_sd` with `ddof=0` survive the full suite | Numbers printed in the pack; neither moves the estimate or bounds | plant either in `_proportion_difference` / the cell; run the suite | lens 3 N3 |
| 19 | `_frozen_sides` with `percentile_bounds(finite, 0.5)` survives `-m day5` and the full suite | Unit-test arrays (all-constant, 199-of-200, linspace) do not separate the 25/75 from the 2.5/97.5 quantiles; lens 3 found no cohort producing such a side through the public API. Add an 80/20 array to `test_frozen_sides_applies_the_single_cell_percentile_rule_to_each_side` | plant `0.5`; `-m day5` passes | lens 3 N4 |

**B. Sentences and records that are imprecise, not false.**

| # | Item | Why carried | Repro | Owner |
|---|---|---|---|---|
| 20 | `test_the_two_sided_bootstrap_draws_side_a_then_side_b_from_the_cell_generator` docstring quotes (0.0, 0.4643) → (0.0696, 0.4762), measured at **B = 200**, seed 1; the test draws 50, where the pair is (0.0741, 0.4048) → (0.0741, 0.4946) | A number without its condition; add "at B = 200" | re-run the test's `_record_two_sided` at 50 and at 200 | lens 3 N5 / lens-3 regression N2 |
| 21 | T7's "refused ... when either side is frozen" is narrower than its plain reading: a side of exactly one case is refused `insufficient_clusters` with `frozen_sides []` before any draw | Wording; the typed reason printed is the more informative one and the code docstring states the precedence | level A of one case beside B of 100: accuracy difference `est 0.2, insufficient_clusters, resampling.a.deficient_class 'all'` | lens 3 N6 |
| 22 | The `imprecise` tier flag that rode on the pre-fix rendered interval is gone from a refused frozen difference (no interval, no half-width); the FA-B1 test asserts only the clustered flag; the repair-2 note's "20 cells, 0 differ" is silent on the four `clustered_pair()` cells that changed (`age 80-200` sensitivity 24/24 and NPV 32/32, vs reference and vs complement) | Record incomplete, not a defect | dump `clustered_pair(span=False)` at `9da4401` and `e4c4f7e`, diff the leaves | lens-3 regression N1 |
| 23 | `_record_two_sided` (test helper) has no docstring | Documentation | — | lens-3 regression N4 |

**C. Annotation policy, declaration checks and design items.**

| # | Item | Why carried | Repro | Owner |
|---|---|---|---|---|
| 24 | i.i.d. Newcombe differences carry no tier and no `imprecise` flag; the clustered route's differences carry both (`difference_unpaired` never calls `with_precision_flags()`) | Which flags ride on an analytic difference Number is an annotation policy on a rendered quantity — a gate-adjacent change DEC-12(i) would lens; the renderer's reading of `tier` (day 8/9) decides it. Open question 2 | `make_cohort(n=80)` site S3 sensitivity difference: `n = 5`, half-width 0.34, `flags []`, row tier `very_low_precision` | lens 1 N3; re-verified lens 2, lens 3 N7 |
| 25 | The `delong_wald` difference bound leaves [−1, 1] unclipped and unflagged: max `ci_hi` 1.088 (lens 2, 3,000 draws), 1.082 (lens 3 fresh-attack, 2,000), 1.0796 (lens-3 regression, 3,000) on 10/10 vs 10/10 binormal ±2 arms | Same policy question as 24; the day-3 single-AUROC clip and `wald_interval_exceeds_unit_range` is the pattern. One slot for 24 + 25 together | 10/10 vs 10/10 arms with separation ±2, `flags == []`, `ci_hi > 1` | lens 2 N1 |
| 26 | Overlapping / inverted / empty age bands accepted silently: `[[0,50],[40,65],[65,200]]` → the row labelled `40-65` holds ages 50–64 (45 forty-somethings sit in `0-50`); `[[65,0]]`, `[[40,40]]` → 300 in Unknown, no halt | Belongs in `io.declare.check_references` (H08: disjoint, `lo < hi`) — day-1 territory with its own halt-gate tests; not half-built here. Open question 3 | declare those bands; read `n_outside_bands` and the `40-65` row's ages | lens 1 N5; re-verified lens 2, 3 |
| 27 | Level names colliding with the Unknown row's missing tokens (`Unknown`, `nan`, ``, `NA`, `None`, `unknown`, `missing` merge; `-` is a level); NFC and NFD `Zürich`, and `S1` / `S1\u200b`, are two levels each | Day-1 normalisation; recorded because the subgroup table is where a reviewer sees it | a level literally `Unknown`; a level in NFD | lens 1 N6; re-verified lens 2, 3 |
| 28 | Two operating points at one (threshold, rule) double the Holm family: `family_size 4`, `p_holm` 0.3451 where one copy gives 0.1726; declared thrice → `family_size 6` | Declaration-side duplicate check (H08); same home as 26 | declare `op1` and `op2` both at 0.5 | lens 1 N8; re-verified lens 2, 3 |
| 29 | Fisher only at r = 2; for r ≥ 3 with a small expected cell the χ² is reported with `min_expected` carried | Documented deviation: no Freeman–Halton in scipy and no paid dependency (zero cash); T7 says "exactly two levels" | three levels with an expected cell < 5 | lens 1 N9 |
| 30 | A level with zero analysed rows leaves no trace (all S3 scores missing → `level_order ['S1', 'S2']`, `n_unknown_missing 0`); a declared band with no rows appears nowhere; a declared `reference_level` on an all-Unknown attribute returns `reference_level: None` without H09 | Needs a place in the attribute block and a schema change; the halt ordering in `_choose_reference` is a halt-gate change day-1's `check_references` also owns. Open question 3 | set every S3 score to NaN; read `level_order` | lens 2 N7; re-verified lens 3 |
| 31 | DEC-08's refusal below the bar not implemented on either route for the `MIN_UNITS_PER_STRATUM` half | Your decision (needs-from-Josh 1); T7 says so plainly | a clustered sensitivity cell of 10 cases × 1 row at 9/10 renders `0.9 (0.7, 1.0)` with tier flags | lens-1 regression N3; lens 2 N3 |
| 32 | `_brier_cell`, `_auroc_cell`, `_level_row`, `_attribute_block` have no docstring | Documentation; the Brier formula and oracle live in the module docstring and its test | — | lens-1 regression N8 |
| 33 | `test_no_module_level_scipy_import_in_subgroups` is a source grep (it passed against a stub with no scipy line) | Kept beside the behavioural test `test_the_module_imports_and_the_footnote_degrades_with_scipy_hidden`, as the lens advised; informational | — | lens-1 regression N9 |

Not carried: lens-2 FA-N11 (a sweep mutant carrying the next mutant's text) — does not reproduce
at `9da4401` or `e4c4f7e` (repair-2 note, confirmed by lens 3); the note-arithmetic and
timing items (lens 2 N10, lens-2 regression N3/N4, lens-3 regression N5) — corrected in the
repair notes, not shipped text; lens-3 regression N3 (`test_frozen_sides_...` aborts on
`AttributeError` pre-fix, unavoidable for a unit test of a function the repair introduces) —
recorded, no action.

## What the lenses found

Every lens ran in a detached worktree with `PYTHONPATH=<worktree>/src` forced and
`proofpack.__file__` printed from the worktree before any count was trusted. Blocker titles
are quoted as the lenses wrote them.

### Round 1, at `d1fd20a` — fresh-attack `FAIL` (2 blockers, 9 non-blocking, 4 sentence violations); regression `FAIL` (1 blocker, 9 non-blocking)

> **B1.** "the heterogeneity p-value is computed on clustered rows with no refusal and no
> flag (blocker, wrong number)" — F6 with every patient's row copied three times under a
> declared `case_id`: χ² 13.898, p 0.00096 (Holm 0.0019) where one row per patient gives
> 4.6327, p 0.0986; every interval in the same block refused
> `clustered_data_analytic_ci_invalid`, the p-value beside them not.
>
> **B2.** "a `delong_wald` difference is rendered with one arm's variance frozen at zero
> (blocker, wrong interval, no flag)" — 20,000 replicates, A = 10/10 at true AUROC 0.898, B
> = 60/60: A perfectly separated in 1,074, and there the rendered difference covered the
> truth in **0.269** at a nominal 0.95; the row showed "AUROC n.e." beside "difference 0.31
> (0.21, 0.41), p 0.0002".
>
> **RG-B1.** "PPV, NPV and accuracy per level are unpinned: inverting all three leaves the
> whole suite green" — `352 passed, 1 skipped, 1 xfailed in 53.07s` with `ppv`, `npv` and
> `accuracy` each computed as `1 − x`.

Fixed in `9da4401`: FA-B1 as a typed refusal (`clustered_data_analytic_ci_invalid`, no
p-value, Holm family empty, `clustering_route` recorded and required by the schema); FA-B2 as
`boundary_estimate` on the difference when either arm's DeLong variance is `<= 0`, estimate
carried, `z` / `p_value` `None`; RG-B1 with a numpy oracle over all six proportions and the
reference differences on sex and site (30 cells) plus eight sweep mutants. Regression proof:
the new test file in a worktree at `d1fd20a` → `3 failed, 4 passed` on the quoted assertions
(`KeyError: 'clustering_route'`; `assert (None == 'boundary_estimate')` beside the
`delong_wald` Number; `Expected regex: 'analysed rows'`), and the four tests that pass
pre-fix each proved by a planted mutant (original `-m day5` `42 passed`, new test `1 failed`)
— lens-2 regression re-planted every one.

What round 1 could not break: X1 (no `diff_vs_overall`, no string `overall`, in any of the
constructions; the row schema rejects one planted); DEC-10 (54 per-level proportion Numbers
`wilson` i.i.d., every one `cluster_bootstrap_percentile` with `wilson_refused_clustered`
clustered; `paired_delong` and `two_by_two_metrics` not imported); the Number invariant on 20
constructed outputs; no verdict word; clustered refusals (`cases_span_both_groups` on exactly
the rows that enter each difference; `insufficient_clusters` on a single-case complement);
the reference rule (Unknown largest → not reference; `" S1"` accepted; `1.0` → H09);
boundary levels; fairness mirror to 1e-16; injection at 50 unused seeds (B excludes 0 in
50/50); 100,000 rows in 3.3 s; schema 0 errors; determinism. The regression lens re-ran
`--full` (781 s in each shell) and reproduced every one of the 60 coverage cells in T7
byte-for-byte, and re-measured F18 exactly (49 / 19 / 43 of 50).

Re-derivation deviations (lens-1 fresh-attack): Wilson 2.2e-16; Newcombe-10 2.2e-16; DeLong
AUC 1.1e-16, relative variance 4.2e-16, Wald bounds 2.4e-16, p 1e-9; Holm 0.0; χ² / Fisher
0.0 / 1.1e-16. Regression lens: F14 fixture deviations 3.2e-5 / 2.7e-5, 2.3e-5 / 1.8e-5,
1.4e-5 / 0 against the test's 5e-5; unpaired DeLong 2.1e-15 on 20 cohorts.

### Round 2, at `9da4401` — fresh-attack `FAIL` (1 blocker, 11 non-blocking, 5 sentence violations); regression `PASS` (0 blockers, 4 non-blocking)

> **B1.** "under clustering, a difference with one side frozen is rendered from the other
> side alone (blocker, wrong interval, no flag)" — the repair test's own
> `separated_level_cohort()` with each row duplicated under a `case_id`: own AUROC `1.0,
> boundary_estimate`, `diff_vs_reference` `0.2256 (0.1599, 0.3054)
> cluster_bootstrap_percentile, reason None`; own sensitivity 20/20 `boundary_estimate`,
> its difference `0.35 (0.25, 0.4671)` rendered. Conditional on the shape (R = 200, B =
> 200, seed 2026): the AUROC difference covered the truth in **0.295**, the sensitivity
> difference in **0.615**.

Fixed in `e4c4f7e`: `_frozen_sides()` applies the single-cell percentile rule to each side's
own draws and `_bootstrap_difference` refuses with `boundary_estimate` when any side is
frozen, estimate carried, clustered flags kept, `bootstrap.resampling.frozen_sides` naming the
side; 200/200 AUROC and 200/200 sensitivity samples refused on the lens's own shapes. Also
taken: the three falsified sentences ("at every seed tried" beside 0.870; "not implemented for
the proportion route"; a test name asserting a draw order nothing recorded), `insufficient_levels`
before the clustered reason, and `clustering_route` with no default. Regression proof: `5
failed, 2 passed` at `9da4401` on the quoted lines; the two tests that pass pre-fix proved by
the b-before-a mutant (`49 passed` → new test `AssertionError: ['b', 'a', 'b', 'a', ...]`)
and by stripping the seven inflections from the verdict set (`AssertionError: passing`).

The regression lens's own finding (N1): the share-0.20 cell at seed 20260916 gives **0.880**
with one generator shared across the cells (0.925 with a fresh generator per cell) — the
sentence "met the bar at every seed tried" was contradicted by a fifth run of equal standing.
The constant did not move; the sentence did (e4c4f7e).

What round 2 could not break: FA-B1 closed on `declared` and `detected` (F6 tripled, a
spanning-case cohort, one row per case declared); FA-B2 closed on the i.i.d. route
(separation in either arm, reversed, constant scores, both arms; an arm at `S10 = 0`,
`S01 > 0` still renders — the guard is at zero); RG-B1 closed; X1; DEC-10 census on
fourteen reports (354 Numbers: 160 `cluster_bootstrap_percentile`, 194 `none`); the Number
invariant; no verdict word in the extended set; reference rule incl. `S1\u200b` → H09 and
`" S1 "` → S1; boundary levels; orientation (`roc_auc_score(y, −score)` to 6 dp); 100,000
rows 3.0 s, 30,000 rows over 15,000 cases 29.6 s; sweep isolation (no third way to defeat
`assert_imports_from_copy` beyond RG-N6's two, both of which resolved to the copy).
Injection at seeds 41000–41049: 49/50. DEC-08 at seed 7777: minimum coverage over the 30
rendered shapes **0.160**, 14 of 30 below the bar (the escalated half); the frozen-share
half's rendered shapes ≥ 0.935.

Re-derivation deviations (lens-2 fresh-attack, seed 20260915): Wilson 2.2e-16; Newcombe-10
2.2e-16; DeLong AUC 3.3e-16, relative variance 2.8e-16, Wald bounds 3.3e-16, p 2.9e-15;
Holm 0.0; χ² / Fisher 0.0 / 5.6e-17.

### Round 3, at `e4c4f7e` — fresh-attack `PASS` (0 blockers, 7 non-blocking, 0 sentence violations); regression `PASS` (0 blockers, 5 non-blocking)

Lens 2's blocker closed by execution: on its two shapes rebuilt at seed 777 (A separated in
100 of 2,206 samples; A at 10/10 in 100 of 256) the pre-fix tree covers the truth in
**0.35** / **0.58** and `e4c4f7e` refuses every one of the 200 with `boundary_estimate`,
`frozen_sides ('a',)`. The regression lens re-measured the pre-fix interval on its own
conditioned shapes at `9da4401`: **0.335** / **0.560** (mean widths 0.167 / 0.192), and
200 / 200 refused post-fix on both (draw counts 4,127 and 563 — its generator, not lens 2's
3,713 / 600).

The measurement nobody had made (lens-3 fresh-attack, R = 300, B = 200, seed 20260915, MC
se 0.017): coverage of the two-sided cluster bootstrap on shapes that **render** — eight
proportion-difference shapes 0.904–0.990 (the 0.904 is a 5-case level of w = 3 rows against
60, the `not_evaluable_shown_for_transparency` tier) and five AUROC-difference shapes
0.923–0.946; refusals counted beside the coverage (95 of 300 `boundary_estimate` on 10
cases at p = 0.9 vs 60 at 0.8). Injection at seeds 61000–61049: B excludes 0 in **50/50**
i.i.d. and **50/50** with every row duplicated under a `case_id` (171 s). DEC-08 at seed
424242 (one shared generator, as `main()` does): minimum 0.220, 14 of 30 rendered shapes
below the bar; the refused 0.20 shape 0.945 / **0.895**.

The five-cohort output diff `9da4401` → `e4c4f7e`: i.i.d. byte-identical; clustered changes
only the added `frozen_sides` key and the cells whose one side is frozen (3 on the 3-site
cohort, 14 on the separated cohort); 0 rendered non-frozen cells differ. Lens 2's 18
mutants rebuilt: 18 / 8 / 10 with exactly the ten survivors the repair note names (lens-3
regression); seven of lens 3's own fifteen survive the full suite (items 16–19 above). The
zero-width branch: 0 of 5,239 constructions reached it.

What round 3 could not break: every frozen construction (0/n sensitivity; constant scores;
both sides separated; a separated level made only of mixed cases; a frozen level against a
complement containing the Unknown row; levels 0.90 and 0.99; a level frozen on specificity
and PPV but not sensitivity — exactly those two refused); near-frozen renders by the
documented rule only (a side frozen in more than 95 % but not all resamples was not reachable
through the public API: a proportion at k/n resamples to n/n with probability `(k/n)^n ≤
0.37`); FA-N8 / RG-N2 and FA-N9 closed (`TypeError` on a missing route, `ValueError` on
`"bogus"`); the three falsified sentences gone and their replacements counted true; no
guarantee-class sentence added (the diff's `+` lines grepped); X1; DEC-10; the Number
invariant; no verdict word with `passed`, `failed`, `unmet`, `compliant`, `significant`,
`approved` probed by hand; reference rule; boundary levels; orientation; 100,000 rows 3.3 s;
determinism of the restructured loop.

Re-derivation deviations (lens-3 fresh-attack, seed 31337): Wilson 2.2e-16 / 3.3e-16;
Newcombe-10 3.6e-16 (220 pairs, 20 at a boundary); DeLong AUC 2.2e-16, relative variance
2.6e-16, sklearn 2.2e-16, Wald bounds 3.3e-16, p 3.3e-15; Holm 0.0; χ² / Fisher 0.0 /
1.1e-16. Regression lens: F14 and F8 and F6 each equal to the test's `approx4` (5e-5).

Could not check, all three rounds: `pROC roc.test(paired = FALSE)` for the unpaired
difference (no R capture; `[unverified]` in the `unpaired_delong` docstring); Newcombe 1998
Table II against the primary PDF (`[unverified against the primary PDF]` carried in the
fixture and the test); Kleinberg / Chouldechova (`[unverified - cited from memory]` in the
output, enforced by the schema pattern); the renderer's reading of `tier` and `frozen_sides`
(no renderer yet).

## The two DEC-08 constants and the coverage table

`scripts/coverage_bar.py v1 (build day 5, 2026-09-15)`, seed **20260915**, R = 400 cohorts
per shape, B = 1000, nominal 0.95, bar 0.90, Monte-Carlo error about 0.015 per cell. The
deficient-class refusal is bypassed so every shape is measured; the constants are judged
against the measurement. **Re-run now, `--full`, in both shells:** 1128 s in Git Bash and
1100 s in PowerShell under concurrent load (the notes' 743–781 s were on an idle machine);
the two 83-line outputs are byte-identical apart from the elapsed line, and the tables below
are that output.

**`MAX_FROZEN_VARIANCE_SHARE = 0.20` — kept.** The refusal rule is `frozen >= 0.20`
(`bootstrap.py` line 780; pinned by `test_the_frozen_share_rule_refuses_the_share_exactly_at_the_constant`,
which fails when `>=` is flipped to `>` — lens-2 regression). So the shapes the constant
**renders** are the 0.05 and 0.10 rows, both at or above the bar in the recorded run, and
the 0.20 row is the first shape it **refuses**. That refused shape has now been measured in
six runs, twelve cells (N = 30 / N = 120): 0.932 / 0.915 (recorded, R = 400, reproduced now);
0.900 / 0.870 (`--quick`, R = 100, measured now in both shells); 0.925 / 0.915 (seed
20260916, R = 200, fresh generator per cell) and 0.880 / 0.915 (same seed, one shared
generator) — both reproduced by the lens-2 and lens-3 regression notes; 0.910 / 0.930 (lens
2 fresh-attack, seed 7777); 0.945 / 0.895 (lens 3 fresh-attack, seed 424242). Three of the
twelve cells are below the bar: the shape sits near the bar, not above it. The 0.30 row
straddles too (0.863 / 0.873 recorded; 0.930 / 0.915 at lens 1's seed 7; 0.910 / 0.890 and
0.920 / 0.900 at seed 20260916, fresh and shared generator; 0.930 / 0.885 at seed 7777;
0.895 / 0.870 at seed 424242). Kept as the conservative side of a boundary whose
seed-to-seed spread is about two and a half Monte-Carlo standard errors; whether the rule
should be `>` so a shape near the bar renders is needs-from-Josh 2.

**`MIN_UNITS_PER_STRATUM = 2` — kept, and it does not meet the bar.** No value on the grid
renders only shapes at or above 0.90. At p = 0.9 with one row per case the clustered
proportion cell reaches 0.90 only at 40 cases; the AUROC route's u = 2, m = 0 shape is at
0.715 / 0.620. Every shape below the bar carries the R2 §3.3 tier annotation (fewer than 10
units "not evaluable, shown for transparency", fewer than 30 "very low precision"), but a
cell of 10–40 cases at a high proportion renders with an interval that is too narrow and at
most a "very low precision" annotation. A floor high enough to clear it would refuse fixture
F3. Lens 2 (seed 7777) and lens 3 (seed 424242) each found 14 of the 30 rendered shapes
below the bar, minima 0.160 and 0.220. Needs-from-Josh 1.

### AUROC cell: one frozen pure-positive case of 3 rows beside m mixed cases (`--full`, measured now)

| frozen share | m | N = 30: coverage | width | N = 120: coverage | width |
|---|---|---|---|---|---|
| 0.050 | 171 | 0.912 | 0.061 | 0.958 | 0.059 |
| 0.100 | 81 | 0.943 | 0.087 | 0.935 | 0.082 |
| **0.200** | 36 | 0.932 | 0.120 | 0.915 | 0.114 |
| 0.300 | 21 | 0.863 | 0.145 | 0.873 | 0.137 |
| 0.409 | 13 | 0.877 | 0.174 | 0.823 | 0.157 |
| 0.500 | 9 | 0.843 | 0.181 | 0.770 | 0.165 |

### AUROC cell: u pure-positive cases of 3 rows, no frozen stratum (`--full`, measured now)

| u | m = 0 (pure stratum alone), N = 30 | N = 120 | m = 36 mixed beside it, N = 30 | N = 120 |
|---|---|---|---|---|
| 2 | 0.715 | 0.620 | 0.910 | 0.927 |
| 3 | 0.787 | 0.715 | 0.930 | 0.907 |
| 4 | 0.815 | 0.810 | 0.912 | 0.912 |
| 5 | 0.870 | 0.795 | 0.935 | 0.900 |

### Proportion cell: u cases of w rows (`--full`, measured now)

| u | w = 1, p = 0.5 | w = 3, p = 0.5 | w = 1, p = 0.9 | w = 3, p = 0.9 |
|---|---|---|---|---|
| 1 | refused (share 1) | refused | refused | refused |
| 2 | 0.505 | 0.502 | 0.190 | 0.343 |
| 3 | 0.762 | 0.723 | 0.237 | 0.522 |
| 4 | 0.870 | 0.868 | 0.347 | 0.618 |
| 5 | 0.940 | 0.830 | 0.407 | 0.667 |
| 10 | 0.975 | 0.950 | **0.672** | 0.920 |
| 20 | 0.938 | 0.940 | **0.850** | 0.902 |
| 40 | 0.953 | 0.943 | 0.922 | 0.953 |

Feasibility block, as printed: `loosest feasible pair (smallest MIN, then largest MAX):
None`; the minimum rendered coverage per candidate `MIN_UNITS_PER_STRATUM` is 0.19 (MIN 1
and 2), 0.24 (3), 0.35 (4), 0.41 (5) and 0.67 (6, 8, 10), the same at every `MAX` column —
in this grid the `MAX` column changes no row's minimum; the unit floor sets it. Every cell of the
three tables equals the cell `design/conventions_T7.md` records (56 coverage and width cells
compared by script, 0 mismatches) and the figures the two constants' docstrings quote. T7's
"Cluster-bootstrap coverage bar" section is the paragraph the day-9 template inserts.

## Re-run these

Every command below was run now, at `e4c4f7e`, in the main tree, in **both** Git Bash and
PowerShell 5.1; `python -c "import proofpack;print(proofpack.__file__)"` printed
`C:\Users\joshs\GPS\ProofPack\proofpack\src\proofpack\__init__.py` in each first. The
outputs are pasted as printed; the two shells differ only in seconds.

```bash
cd C:/Users/joshs/GPS/ProofPack/proofpack
```

| Command | Output, Git Bash | Output, PowerShell 5.1 |
|---|---|---|
| `python -m pytest -q -p no:cacheprovider` | `364 passed, 1 skipped, 1 xfailed in 42.85s` | `364 passed, 1 skipped, 1 xfailed in 42.20s` |
| `python -m pytest -q -p no:cacheprovider -m day5` | `54 passed, 312 deselected in 19.49s` | `54 passed, 312 deselected in 19.22s` |
| `python -m pytest -q -p no:cacheprovider -m day1` | `59 passed, 1 skipped, 306 deselected in 4.39s` | `59 passed, 1 skipped, 306 deselected in 4.32s` |
| `python -m pytest -q -p no:cacheprovider -m day2` | `40 passed, 326 deselected in 1.85s` | `40 passed, 326 deselected in 1.88s` |
| `python -m pytest -q -p no:cacheprovider -m day3` | `29 passed, 336 deselected, 1 xfailed in 2.34s` | `29 passed, 336 deselected, 1 xfailed in 2.31s` |
| `python -m pytest -q -p no:cacheprovider -m day4` | `182 passed, 184 deselected in 19.46s` | `182 passed, 184 deselected in 17.36s` |
| `python -m ruff check .` | `All checks passed!` | `All checks passed!` |
| `python -m ruff format --check .` | `55 files already formatted` (ruff's count includes the Markdown files it formats fenced blocks in: 52 when the lens-3 notes ran, 54 with those two notes in the tree, 55 with this one; `git ls-files` counts 28 `.py`) | `55 files already formatted` |
| `python -m proofpack.cli doctor --offline` | `All essential checks passed.` (17 `[ok  ]` lines; `docx extra not installed - HTML/JSON only`) | `All essential checks passed.` |
| `python scripts/mutation_sweep.py --marker day5` | `34 planted, 34 killed, 0 survived; 656 s` (the two shells' 73-line outputs are identical apart from the elapsed line and the temp-dir names; each mutant's line reads `killed` with a `1 failed, N passed, 312 deselected` under it — the sweep runs pytest with `-x`, so N is where the first failure stopped it) | `34 planted, 34 killed, 0 survived; 653 s` |
| `python scripts/coverage_bar.py --quick` | share-0.20 row `0.900` / `0.870`; `loosest feasible pair (smallest MIN, then largest MAX): None`; `elapsed 43 s` | identical apart from `elapsed 40 s` (the 82-line outputs diffed: byte-identical bar the elapsed line) |
| `python scripts/coverage_bar.py --full` | header `coverage_bar.py v1 (build day 5, 2026-09-15); seed 20260915; R=400, B=1000; nominal 0.95; bar 0.9`; share-0.20 row `0.932` / `0.915`; `loosest feasible pair (smallest MIN, then largest MAX): None`; `elapsed 1128 s` | identical apart from `elapsed 1100 s` (83-line outputs diffed: byte-identical bar the elapsed line) |
| repair-r7's 27 (`python -m pytest tests/test_bootstrap.py -q -p no:cacheprovider -k "one_id_per_row or not_a_case_column or composite_case_key or one_dimensional_case_column or contradict_each_other or w13_beside_the_interval or cell_is_degenerate or four_faults or shape_it_checks or surfaces_no_clustering"`) | `27 passed, 155 deselected in 0.67s` | `27 passed, 155 deselected in 0.66s` |
| round-1's seven (`python -m pytest tests/test_subgroups.py -q -p no:cacheprovider -k "refused_on_clustered_rows or either_arm_has_zero_variance or matches_numpy_on_the_raw_columns or exactly_at_the_constant or naming_the_analysed_rows or every_phrase_the_brief_names or side_a_then_side_b"`) | `7 passed, 47 deselected in 1.76s` | `7 passed, 47 deselected in 1.73s` |
| round-2's seven (`python -m pytest tests/test_subgroups.py -q -p no:cacheprovider -k "one_side_is_frozen or applies_the_single_cell or every_route or no_default_clustering_route or falsified_are_gone or side_a_then_side_b or every_phrase_the_brief"`) | `7 passed, 47 deselected in 3.03s` | `7 passed, 47 deselected in 3.00s` |

The two sweeps and the two `--full` runs ran concurrently on a 16-thread machine, so their
wall times are inflated against the notes' (sweep 341–433 s, `--full` 743–781 s); the
killed / survived counts and the coverage cells are what the runs record, and those are
deterministic. Do not paste the bare `proofpack doctor --offline`: the console script is not
on `PATH` in either shell (re-checked now: exit 127 in Git Bash, `Get-Command` finds nothing
in PowerShell); `python -m proofpack.cli doctor --offline` is the form that runs.

The regression proofs for the two repair rounds are in the repair notes and were re-run by
the regression lenses: the round-1 tests copied into a worktree at `d1fd20a` → `3 failed, 4
passed`; the round-2 tests into a worktree at `9da4401` → `5 failed, 2 passed, 47
deselected in 1.65s`. To repeat one, follow the repair-r7 note's worktree recipe with
`PYTHONPATH=<worktree>/src` forced and `proofpack.__file__` printed first — the editable
`.pth` on this machine otherwise imports the main tree and gives a false green.

## Tomorrow needs

Day 6 is **E6 — calibration and descriptive tables** (`spec/briefs/day6_E.md`):
`stats.calibration` with O:E, intercept, IRLS slope / intercept, decile curve, Brier / IPA,
two ECE schemes with the bin scheme reported, suppression unless the score is declared a
probability, the 200/200 flag (a convention after Van Calster 2019); `table1`, flow counts,
missingness; F4 vs statsmodels GLM with the vectors committed as `fixtures/f4_calibration.csv`;
`score.type: logit` → null with a reason; acceptance 1e-6 / 1e-9, both ECE schemes, the
suppression reason rendered. Similarity / overlap is v1.1. What day 6 inherits from today,
in order:

1. **Every calibration cell takes the day-4/5 cell shape** `{number, analytic,
   analytic_status, clustering_route, bootstrap[, detail]}` (build decision 5), passes
   `cluster_ids` sliced with its rows, and refuses the analytic interval on clustered rows
   with the DEC-09 typed reason as `proportion_ci` / `auroc_ci` do. The IRLS slope and
   intercept Wald intervals are analytic: under clustering they need the same companion
   refusal and either a cluster-bootstrap route or a typed refusal — not a silent Wald.
   `test_dec10_every_proportion_number_is_wilson_or_the_cluster_bootstrap` is the census
   pattern to copy for calibration's own methods.
2. **Brier must agree with `_brier_cell`.** Day 5 computes Brier per level as the plain mean
   squared error through the day-4 resampler (stratified by outcome, or clustered), only for
   a declared probability with `higher_is_positive`, refused `single_class` in a single-class
   level and `not_computed_this_run` with `score_type` in detail otherwise. The overall
   Brier / IPA should equal the subgroup module's value on the whole cohort taken as one
   level — write that cross-check as a test, and reuse the typed reasons rather than
   inventing a second spelling of "not a probability". A new reason for `score.type: logit`
   goes into `NOT_ESTIMABLE_REASONS` and the schema enum together
   (`test_the_enums_added_today_agree_between_code_and_schema` is the pattern).
3. **The 200/200 flag is an annotation.** DEC-08 is annotate-never-omit; a new entry in
   `FLAGS` plus the schema enum, never a suppression. The decile curve's per-bin observed
   rates are proportions: `proportion_ci` is the route with a clustered form (DEC-10's), and
   a bin of few cases inherits the `MIN_UNITS_PER_STRATUM` shortfall in the coverage table
   above — the same tier annotations and the same T7 paragraph apply to them until
   needs-from-Josh 1 is decided.
4. **No calibration verdict.** `VERDICT_WORDS` (40 words in `tests/test_subgroups.py`)
   holds `calibrated` and `miscalibrated`; `calibration` — the key a day-6 block will carry
   — is deliberately not flagged and `test_the_verdict_grep_flags_every_phrase_the_brief_names`
   asserts that. The output grep walks the reports its `reports` fixture builds — add the
   day-6 output to that walk (or a day-6 twin) so `calibrated` in any key or engine string
   fails a test, and keep every criteria status for day 7.
5. **Calibration by group** is `not_computed_this_run` in the fairness block today (T7). If
   day 6 lands it per level, the fairness block and `fairnessBlock` schema change; if not,
   leave the reason as it is.
6. **`lower_is_positive` scores.** Day 5 negates the score for AUROC; a calibration on a
   `lower_is_positive` "probability" is not a probability of the positive class. Decide
   refuse-with-reason or transform-with-flag before writing the O:E — do not fall back.
7. **If the build finishes with time left, close carried item 16 first** (the two-sided
   interval oracle test): it is lens 2's blocker class for every rendered clustered
   difference and the only observer today is a lens re-measuring coverage. Items 17–19 each
   need one assertion or one array; lens 3 named which.

Nothing else in the engine changed today: `Number` is untouched, `MIN_UNITS_PER_STRATUM`,
`MAX_FROZEN_VARIANCE_SHARE`, `LOW/VERY_LOW_PRECISION_UNITS`, `FEW_EVENTS` and
`MIN_USABLE_FRACTION` are unchanged at every sha (lens 3), and the day-1 to day-4 test
files are unchanged.

## Needs from Josh

1. **DEC-08, the `MIN_UNITS_PER_STRATUM` half.** On the grid no value of that constant
   renders only shapes at or above the bar (the feasibility block prints `None` at the
   recorded seed in `--quick` and `--full`, and at lens 1's seed 7; lens 2 and lens 3 each
   found 14 of the 30 rendered shapes below the bar at seeds 7777 and 424242), and a floor
   high enough for p = 0.9 (40 cases) would refuse F3. Options in the order the builder
   would take them:
   (a) a design-effect-adjusted Wilson interval for clustered proportions (Wilson on
   `n_eff = n / DEFF`, a new method string, a build-day item with its own coverage run);
   (b) an effective-units (Kish) floor per cell — but the table says it is not one number
   (about 10 at p = 0.5, about 40 at p = 0.9); (c) T7 carries the table as the honest
   statement and the cells keep their tier annotations. Builder's recommendation: (a), and
   (c) until it lands. A reporting decision, so yours.
2. **The frozen-share boundary.** Keep `>=` (the share-0.20 shape refused; three of its
   twelve measured cells are below the bar) or change to `>` so a shape near the bar
   renders. A gate change either way; not made.
3. Read `design/conventions_T7.md` as the T7 paragraph; the day-9 template inserts it
   verbatim.

## Open questions (at most five)

1. **Default age bands** when `age` is not declared: D1 §2's `[0,40), [40,65), [65,80),
   [80,200)` (used, `bands_source: engine_default`) or R2 §3.1's `<18, 18–39, 40–64, 65–79,
   ≥80`? One line switches `DEFAULT_AGE_BANDS`.
2. **What rides on the Number and what on the cell.** Carried items 24 and 25 (tier flags
   on i.i.d. differences; a unit-range clip and flag on the `delong_wald` difference bound)
   and repair-2's question (should `frozen_sides` also be named in the Number's `flags` for a
   renderer that reads only the Number?) are one policy: should analytic difference Numbers
   carry the annotations, or should the renderer read the row tier and the cell's
   `bootstrap.resampling` and clip at render? Decides where all three are fixed and whether
   the day-8/9 renderer reads cells or Numbers.
3. **Where the declaration checks live.** Items 26, 28 and 30 (bands disjoint with `lo < hi`;
   duplicate (threshold, rule) pairs; a declared reference on an all-Unknown attribute; a
   level observed in the table but absent from the analysed rows listed in the attribute
   block) — day-1's `io.declare.check_references` (H08 / H09) or a day-6 E item?
4. **The cell wrapper versus D1 §4.2's bare Numbers.** Keep the day-4 audit shape in the
   subgroup JSON (so the X2 companion refusal reaches the page), or flatten to Numbers and
   move the companions elsewhere before the renderer lands? Fairness gaps are also nested
   per operating point where D1 writes them flat.
5. **Heterogeneity under clustering**: stay a typed refusal in the pack, or offer a
   case-level test later (a new method string, its own oracle)? And should a case spanning
   two levels of an attribute be a run-level warning code, seen once, as well as the
   per-difference refusal?

## Sentences refused

Aggregated from the build note, the two repair notes and the six lens notes; each is a
sentence that was not written because its counter-example ran or because no counter-example
was constructed.

- "The DEC-08 bar guarantees 0.90 coverage" / "the coverage bar holds" — measured on the
  grid's shapes at one seed; 14 of 30 rendered shapes below it on the escalated half at
  two other seeds (lens 2, lens 3).
- "`MIN_UNITS_PER_STRATUM` meets the bar" — it does not (the table).
- "Setting `MIN_UNITS_PER_STRATUM` to 40 satisfies DEC-08" — it would refuse F3; escalated.
- "The 0.20 shape meets the bar" / "is below the bar" / "met the bar at every seed tried" —
  three of twelve cells below, nine at or above; the sentence written is that it straddles.
- "The DEC-08 bar is met by 0.20" — 0.915 is one Monte-Carlo standard error above it
  (lens-1 regression).
- "The subgroup module is now correct under clustering" / "the repair closes clustering for
  the subgroup module" — the footnote is refused there, not computed; the frozen shapes are
  refused on the shapes constructed; carried items 1–19 are unobserved.
- "`delong_wald` now covers at the nominal level" — the repair removes the shape measured at
  0.269; the remaining shapes' coverage was not re-measured.
- "The clustered difference is now correct" / "FA-B1 is closed" / "the frozen-side rule is
  closed" — refused on every shape constructed and on 200/200 conditioned samples of two
  shapes; a side frozen in fewer than about 95 % of resamples renders by the same rule that
  renders its own cell, and the coverage of those shapes was not measured.
- "A frozen side can no longer reach a rendered difference" — the rule inspects the draws'
  2.5 % and 97.5 % quantiles, nothing more.
- "The two-sided cluster bootstrap is now correct" — rendered coverage measured on thirteen
  shapes at one seed; nothing in the suite would notice it becoming wrong (item 16).
- "The zero-width branch is dead code" — 0 of 5,239 constructions reached it; not a proof.
- "The suite now pins the subgroup table" / "pins the clustered differences" / "pins the
  two-sided bootstrap" — ten of lens 2's mutants and seven of lens 3's survive.
- "The mutation sweep shows the tests have teeth" — it shows 34 declared mutants die.
- "The day-5 suite would catch a wrong PPV/NPV" (at `d1fd20a`) — it would not; closed in
  round 1 by `test_every_per_level_proportion_matches_numpy_on_the_raw_columns_with_its_k_and_n`.
- "A clustered table cannot reach Wilson by a forgotten argument" — removed from the
  docstring; three attempts recorded (lens-1 regression N7), none a proof.
- "The seven tests pin the repair" — three fail pre-fix on their assertion; four pin
  behaviour only through the tabulated mutants.
- "The scipy-hidden test proves the package imports without scipy" — it proves
  `subgroups.py` does; the cold-process run with scipy blocked before any import is the
  evidence for the package (all three regression lenses ran it).
- "F6's Fisher p = 0.1084 arises under the module's own rule" — site 1 / site 2 have expected
  cells ≥ 8.5; the Fisher branch is exercised on that pair only by lifting the threshold, and
  the test says so.
- "The unpaired DeLong difference was checked against pROC" — no capture; `[unverified]`.
- "Newcombe Table II values verified" / "Kleinberg / Chouldechova impossibility" — the
  fixture's and R2's `[unverified]` markings carried unchanged into the test docstring and
  the output string.
- "The 0.86 coverage at 50 seeds shows the DeLong interval is short" — 300 seeds gave
  0.933 / 0.943; the F18 docstring records both.
- "`frozen_sides` explains every refusal" — it names the side for `boundary_estimate` only;
  a deficient-class refusal returns before any draw and records `()`.
- "The `clustered_pair()` cohort is unchanged" — four cells changed, correctly (item 22).
- "Nothing was cut" (build note) — DEC-08's refusal below the bar is not implemented.
- "No customer can obtain a wrong number from this module" — none of the lenses'
  constructions produced one; the CLI does not call `subgroup_analysis` yet, so the customer
  path is not yet the path that was attacked.
- Any status or verdict beside a p-value, a gap or the criterion of interest — none emitted;
  the schema rejects `status` there and `test_the_schema_rejects_a_verdict_beside_a_p_value_and_a_status_in_fairness`
  plants one.

In this note: "the 33 items are harmless" was not written — each is a coverage gap or a
policy question, and item 16 is the defect class of a blocker with no observer; "the lenses
found nothing" was not written for round 3 — they found seven and five items and they are
carried above.

---

## Evening addendum (orchestrator, 15 September)

Pushed to GitHub at 14:51 (`1facbb0`). The public repo's `ci` workflow is red on one job, `pip-audit`: `proofpack: Dependency not found on PyPI and could not be audited: proofpack (0.1.0.dev1)` — pip-audit is auditing the project itself; it has failed the same way since the 7 September push. `pytest + ruff` (364 tests) and `import proofpack (scipy uninstalled)` pass on the runner. First item of the next lane-E slot: `pip-audit --skip-editable` (or exclude the local package) in `.github/workflows/ci.yml`, then E6 calibration. DEC-08's `MIN_UNITS_PER_STRATUM` half (needs 1 above) is recorded as reopened in `spec/DECISIONS_2026-09-13_Josh.md` (spec `0b6e726`) for Josh.
