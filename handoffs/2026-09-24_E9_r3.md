# Lane E - build day 9, repairs 3 and 4, lens rounds 4 and 5 - 2026-09-24

**The final lens round (5, at `e62d329`) is `PASS` from both lenses with no blocker listed, but the fresh-attack lens writes that its N1 becomes a blocker, and the verdict FAIL, if Josh reads lens-4 FA-B1 as covering text as well as figures: T1 prints a `paired_difference_vs_prior` margin as "CI lower bound >= −0.05" on the subgroup's own sensitivity, with no word saying it is a difference against the prior version (row 121, needs 32; reproduced now in both shells).** Thirty-three items are carried (22 earlier E9 rows still open, 11 new from rounds 4 and 5), plus one row of records, none of them a lens blocker (the orchestrator's run summary gave 26; the tables below are my count).

This note supplements `2026-09-23_E9.md`, which is not rewritten; its carried rows 93-120 keep their numbers and new rows start at 121. Since `952bddc`: `f72a9af` (repair 3), `1d19e62` (repair 4 and the two lens-4 notes) and `e62d329` (a test fix inside repair 4). The two lens-5 notes are committed with this note. Repair 3's note (`workflows/notes-day9/E9_repair3.md`) is the orchestrator's: the repairer's session ended before it wrote one. Repair 4's note is `scratchpad/notes/E9_repair4.md` (outside the repository). **Nothing is pushed.**

Measured now, main tree at `e62d329`, both shells (11:55-12:30 BST):

- full suite: **1415 passed, 1 skipped, 1 xfailed** (Git Bash 124.08 s, PowerShell 124.23 s);
- `-m day9`: 159;
- `git diff eda8a35 e62d329 -- src/proofpack/stats/ src/proofpack/gates.py src/proofpack/criteria.py`: 0 lines; `design/tokens.json`: 0 lines; `narrate/checker.py narrate/claims.py egress/`: 0 lines;
- `git rev-list --count eda8a35..e62d329`: 18.

## What landed

| Item | Commit | Test ids (`day9`) | Oracle or literal input -> figure |
|---|---|---|---|
| Repair 3: F5 criterion lines and each T1-11 read the criteria row's `operating_point` (lens-3 FA-B1, row 93) | `f72a9af` | `tests/test_e9_repair3.py::test_an_op2_criterion_draws_no_line_on_the_op1_f5_plot`, `::test_the_sex_f_row_of_the_op1_and_op2_t1_11_prints_its_own_criteria_rows_and_c5` | Now, both shells, lens 3's input (C3 at op2): `F5 C3 line count: 0`, `T1-11 plain 'C3 (row 1)': 1 ['C3 (row 1) → criterion met']` (at `e17a510` the E9 handoff recorded line count 1 and 2 T1-11 hits). Lens 4 RG, 23 criteria rows on three operating points: `T1-11 printed 20 expected 20 F5 lines 2 defects 0`; at `952bddc` `printed 48 ... defects 48` |
| Repair 3: `FAIRNESS_GAP` prints the AUROC gap outside the op clause (lens-3 FA-N1, row 94) | `f72a9af` | `::test_the_fairness_gap_sentence_prints_the_auroc_gap_outside_the_operating_point_clause` | Now, both shells, two-op CLI: `... PPV gap +8.9 [−6.2, +23.3]; AUROC gap (no operating point) +0.079 [−0.002, +0.160].` at op1 and at op2 |
| Repair 3: T7's z/p location sentence deleted (lens-3 FA-N3, row 96); the repair-2 test name (row 95) | `f72a9af` | `::test_t7_of_the_iid_and_clustered_documents_prints_no_z_p_location_sentence`, `::test_the_repair_2_file_no_longer_carries_the_name_state_what_they_inspect` | Lens 4 FA: "z and the two-sided p" 0 times on the i.i.d., two-op, clustered and `y_pred` T7s. Sample-pack T7 now 47,666 bytes (47,830 at `952bddc`) |
| Repair 3: two day-9 sweep mutants for the op filters | `f72a9af` | `tests/test_sweep_day9.py` | Lens 4 RG: `f5_criterion_line_for_any_operating_point`, `t1_11_criteria_for_any_operating_point`, each killed |
| Repair 4: F5 draws no line for a criterion whose declaration entry has `type: paired_difference_vs_prior` (lens-4 FA-B1) | `1d19e62` | `tests/test_e9_repair4.py::test_a_paired_difference_criterion_draws_no_f5_line`; sweep mutant `f5_criterion_line_for_paired_difference` | Now, both shells, repairer's CLI input (`Cpd` −0.05 op1, `Cauc_pd` 0.55, `C3` op2): all three sex F5 plots `[] 0`; at `f72a9af` `Cpd` was drawn at `M178.5,20.0 L178.5,72.0` (repair-4 note). Lens 5 RG: 0 of 7 paired variants drawn, 3 of 3 expected lines drawn (4 of 7 drawn at `f72a9af`) |
| Repair 4: three sentence findings (lens-4 FA-N1..N3 = RG-N1..N3); the `subgroup_blocks` docstring names its filter and the reference-level rule | `1d19e62`, `e62d329` | `test_e9_repair4.py::test_the_repair_3_file_names_where_its_e_lines_are_and_what_its_t1_11_test_reads`, `::test_t1_py_no_longer_calls_an_op_less_row_an_auroc_criterion`, pin `::test_the_sex_t1_11_rows_still_print_the_paired_rows_status_and_not_cref` | `e62d329` removes two lines that read `handoffs/`; at `1d19e62` the sweep refused to run (`baseline (no mutant) does not pass -m day9`, exit 2; confirmed by lens 5 FA) |
| Pre-fix, measured now | - | - | `test_e9_repair3.py` (the `e62d329` text) alone in a `952bddc` worktree, `PYTHONPATH` forced: **`5 failed`**. `test_e9_repair4.py` alone in a `f72a9af` worktree: **`3 failed, 1 passed`** (the pin passes, as its commit message says) |

## What was cut and why

- **No item on the brief's list was cut** (see `2026-09-23_E9.md`). Its five in-item scope limits stay carried (rows 100, 120).
- **No day-9 sweep mutant was added for repairs 1 or 2 (DEC-12 (ii)).** Repairs 3 and 4 added three; the list is 18 (row 105).
- **The day-8 sweep was not re-run** (row 106). Repairs 3 and 4 change no day-8 path: `git diff --stat f72a9af e62d329` over `narrate/`, `egress/`, `run.py`, `cli.py` is empty (lens 5 RG).
- **No wheel was built in a fresh venv since `e17a510`.** Lens 5 FA: `hatchling` is not installed and a download needs approval. Repair 3 changed `design/conventions_T7.md`, which the wheel force-includes; repair 4 changed no packaged non-Python file.
- **No repair after lens 5.** The round cap was reached with no blocker listed.

## What did not land and why

### Carried from rounds 4 and 5 (new rows)

| # | Item | Why carried | Repro (one line) |
|---|---|---|---|
| **121** | **Lens 5 FA-N1: T1 prints a paired-difference criterion as a bound on the subgroup's own statistic.** The `CRITERION_STATUS` sentence, T1-17 and T1-11 print `Cpd ... CI lower bound >= −0.05 ... not assessable (requires_compare)`; `paired`, `prior version` and `evaluated by compare` occur 0 times on T1. The declared `type` reaches only T8's echo. Lens 5 grades it record-and-carry because D4 §5.6 and §8 have no place for the type; it is a blocker if Josh reads lens-4 FA-B1 as covering text | A skeleton change (D4 §8, DEC-64); needs 32 | Now, both shells: `<scratch>/E9r3handoff/n1_text.py <repo>` prints `CI lower bound >= −0.05 count: 1`, `paired count: 0`, `F5 Cpd label count: 0` |
| 122 | Lens 4 FA-N5 = RG-N4: T1-13, T1-10's AUROC column and T1-11's Δ AUROC column print the op-less AUROC gap in rows labelled op1 and op2 (`+0.079 [−0.002, +0.160]` twice). The table form of row 94 | Layout or column note; needs 33 | two-op CLI T1, read T1-13 |
| 123 | Lens 4 FA-N4 = lens 5 FA-N7: mutant `f5_dedupe_by_id_only` survives (`158 passed`). Non-equivalent: `C1` declared twice at op1 on sex = F (0.4, 0.3) draws two lines, the mutant one | No test declares one id twice | lens 4 FA `probe/p6.py` |
| 124 | Lens 5 FA-N5: mutant `paired_after_dedupe` (the paired skip below `seen.add(key)`) is killed only by the sweep-pattern test. Non-equivalent: `Cdup` paired then point at 0.4 loses its line | No test declares a paired and a point criterion with one id and value | lens 5 FA `probe/mut2.py <wt> dedupe` |
| 125 | Lens 5 FA-N6: mutant `label_author_first_entry` survives the full suite (`1415 passed`). With `Ca` (Dr A) and `Cb` (Dr B), the second F5 label reads `Cb (Dr A, 2026-02-02)` | No test declares two drawn criteria with different authors | lens 5 FA `probe/p_auth.py` |
| 126 | Lens 5 FA-N9: a paired criterion at `level: "*"` appears in no T1-11 and on no F5; only in T1-17 and its sentence. Same shape as row 110 | Record | lens 5 FA `probe/p1.py`, case "paired level *" |
| 127 | Lens 5 FA-N2 = RG-N1 (sentence): `test_e9_repair3.py` docstring says the `E` lines are quoted "in the repair-4 note"; that note quotes none of the five, and it is not in the repository | Round cap | `grep -c state_what_they_inspect <scratch>/notes/E9_repair4.md` -> `0` |
| 128 | Lens 5 FA-N3 = RG-N2 (sentence): `test_e9_repair4.py` docstring says "each" test's `E` line is in the commit message (one test passes pre-fix) and "Each test feeds the input a lens fed" (the fixture adds `Cpt`, `Cauc`, `Cref`) | Round cap | read the module docstring |
| 129 | Lens 5 RG-N4: re-run commands that fail as written: `b1_repro.py` without `PYTHONIOENCODING=utf-8` (`UnicodeEncodeError` on `→`, both shells); `rerun/rerun.ps1`'s `k1` line with a backslash `LENS_WT` | Scratch scripts | this note's table sets `PYTHONIOENCODING=utf-8` and uses `E9handoff/rerun_h.ps1` |
| 130 | Lens 5 FA-N8, extending row 109: `Cbig` (auroc, 1.3) drawn at x = 489 against an axis ending at 420; `Cabs` (sex = X, `level_absent`) drawn on the sex plot | Figure rule not decided; needs 34 | lens 5 FA `probe/p1.py`, cases "point value 1.3 on auroc", "level absent X" |
| 131 | Lens 5 RG-N6: the goldens also pin `manifest.numpy` to `x.y.z`, beyond the three volatile values | Record; documented in the test docstrings | `tests/test_render_t1.py` line 54 |
| rec | Records, no action: the repair-4 pin passes at `f72a9af` by design (lens 5 FA-N4 = RG-N3; it is the one test that kills lens 5 RG's `t1_11_drops_paired_rows`); equivalent survivors lens 4 RG-N5 (`row_op is None or ...`) and lens 5 RG (`entry.get("type") in (None, "point")`); `f72a9af`'s commit message keeps "(an auroc criterion ...)" (history not rewritten); lens 5 RG-N5's one-byte `run.json` difference: I measured 253,938 bytes in both shells, and on the sample pack 236,842 (Git Bash) against 236,843 (PowerShell), equal after masking the three volatile values, with `duration_s` `0.75` against `0.727` | - | - |

### Earlier E9 rows (numbers from `2026-09-23_E9.md`)

**Closed since `952bddc`** (each no longer reproduces on the inputs named under "What landed"): **93** (lens-3 FA-B1), **94** (sentence form; the table form is row 122), **95**, **96**, **97** (`f5_caption_last_op` now killed, lens 4 FA and RG). 107 was closed in the E9 handoff. Lens 4 RG-N6 (the S4 list) is answered by the S4 section below.

Still open:

| # | State at `e62d329` |
|---|---|
| 98, 99 | Open (op ids with `/` or `~`; `op1` and `op1 `); needs 30. Lens 4 FA re-measured |
| 100 | Open and **more exposed** (lens 4 RG-N7): F5 draws the first operating point only, so an op2 criterion and, since repair 4, a paired criterion are drawn on no plot, and no caption says so. Open question 1 |
| 101, 102 | Open, re-measured unchanged by lens 4 FA |
| 103, 104 | Records, unchanged |
| 105 | Partly: 18 day-9 mutants, three for repairs 3 and 4; none for repairs 1 and 2 |
| 106 | Open: day-8 sweep not re-run |
| 108 | Open. Lens 4 FA: −0.0345 prints `−3.5`; the double is −0.034500000000000017..., so it follows the documented rule; needs 26 |
| 109 | Open; extended by row 130 |
| 110 | Open. The `subgroup_blocks` docstring now states that a criterion scoped on the reference level is printed in no T1-11, and the pin test reads `Cref`; the behaviour is unchanged |
| 111-120 | Open, unchanged (lens 4 FA and lens 5 FA re-measured 111, 116: `endorsed` 1 per page in the cover-note negation; `guarantee` 2 on T7) |

**r6 rows (E8) that E9 closed:** 23, 27, 29 (and 52 on the render side), as in the E9 handoff. Every other r6 row is as the E9 handoff left it: `git diff eda8a35 e62d329` over `stats/`, `gates.py`, `criteria.py`, `narrate/checker.py`, `narrate/claims.py`, `egress/` is 0 lines (now). Row 87 now, both shells: `engine claims rejected: 0` and the same 15 `free_text accepted` texts.

## What the lenses found

Rounds 1-3 are in `2026-09-23_E9.md`: round 1 FA FAIL (3) / RG FAIL (2); round 2 FA FAIL (1) / RG PASS; round 3 FA FAIL (1, row 93) / RG PASS. Every lens below ran in detached worktrees with `PYTHONPATH=<wt>/src` forced and `proofpack.__file__` printed first, every CLI run with `--offline` and sockets refused (`socket calls 0`).

| Round, sha | Fresh attack | Regression | Re-derivation deviations quoted |
|---|---|---|---|
| 4, `f72a9af` | **FAIL, 1.** B1: "F5 draws a `paired_difference_vs_prior` criterion as a threshold on the level-estimate axis." `Cpd` −0.05 drawn at x = 178.5 left of x0 = 190 on "Sensitivity by sex at operating point op1"; its `criteria_results` row is `not_assessable`, `requires_compare`. Reproduces byte for byte at `952bddc`. N1-N3 sentences in repair 3's text; N4 survivor (row 123); N5 table form (row 122) | **PASS, 0 blockers.** N1-N3 the same sentences; N4 = FA-N5; N5 equivalent survivor; N6 the S4 list and stale T7 size; N7 row 100 more exposed | 603 `data-ref` cells, 0 real mismatches ("The 8 my script flags are its own"). 23-row probe: 20 printed, 20 expected. Goldens regenerate to the committed md5s |
| 5, `e62d329` | **PASS, 0 blockers.** Nine records: N1 (row 121, "the one Josh should read"), N2-N3 sentences (rows 127, 128), N4 pin, N5-N7 survivors (rows 124, 125, 123), N8 off-axis lines (row 130), N9 paired `*` (row 126) | **PASS, 0 blockers.** N1-N2 = FA-N2, N3; N3 pin; N4 commands (row 129); N5 one byte; N6 `numpy` in goldens | Cells on six documents: 978 matched, 0 mismatched. F2 operating-point markers `2.8e-14`, every other mark 0. T7 method counts equal an independent count (licensed: wilson 249, newcombe10 384, delong_wald 45, ...). F5 x recomputed from `data-map`: equal |

What rounds 4 and 5 could not break (quoted from their notes):

- **Lens-3 FA-B1** (lens 4 FA and RG): inputs with two and three operating points, operating points declared in reversed order, a comparator run and an injected op id; each T1-11 prints the rows by position with their own status.
- **Lens-4 FA-B1**: 11 inputs (lens 5 FA) and 7 paired variants (lens 5 RG); no paired criterion drawn, every other `ci_lower_bound` row at the plotted operating point drawn.
- **Injection**: `html.parser` finds no `script`, `img`, `iframe`, `object`, `embed` or `b` start tag and no `on*` attribute on T1, T7, T8; U+202E only as `&#x202e;`; `[unverified] planted` survives (T1 11).
- **Draft label**: every `FDA_AIDSF_*` anchor carries "draft guidance (January 2025), not for implementation": T1 27/27, T7 3/3, T8 6/6.
- **Words**: outside `.status` / `.disclaimer` / `.customer-text`, the only hits are `endorsed` (cover-note negation) and `guarantee` (T7 negations).
- **Imports** with jinja2 and markupsafe blocked: `proofpack`, `proofpack.stats`, `cli`, `run`, `render.sentences`, `render.figures`, `render.format` import; `jinja2 loaded: False`.
- **No statistics changed**: `git diff --stat f72a9af e62d329` over `stats`, `gates.py`, `criteria.py`, `narrate`, `egress`, `io`, `schema`, `render/format.py`, `run.py`, `cli.py`, `design`, `.github` prints nothing.

## The decisions taken today

Each is a repairer's choice. None is a Josh decision (no row after DEC-67 in `spec/DECISIONS_2026-09-13_Josh.md`, read now).

| Decision | Rule as shipped | Where | Test that feeds the input |
|---|---|---|---|
| F5 op filter | A criteria row is drawn only when its `operating_point` equals the plot's (None for the AUROC plot) | `render/figures.py::_criterion_lines` | `test_e9_repair3.py::test_an_op2_criterion_draws_no_line_on_the_op1_f5_plot` |
| T1-11 op filter | A row is printed under a table when scope attribute and level match and `operating_point` is the table's or None; status from `criteria_results` by position | `render/t1.py::subgroup_blocks` | `::test_the_sex_f_row_of_the_op1_and_op2_t1_11_prints_its_own_criteria_rows_and_c5` |
| `FAIRNESS_GAP` shape | One sentence per operating point; the AUROC gap after the op clause, labelled "AUROC gap (no operating point)". This is the repairer's answer to needs 31 | `narrate/templates.py` | `::test_the_fairness_gap_sentence_prints_the_auroc_gap_outside_the_operating_point_clause` |
| T7 z/p sentence | Deleted, not replaced; T7 keeps "(estimate carried, no z, no p)" | `design/conventions_T7.md` | `::test_t7_of_the_iid_and_clustered_documents_prints_no_z_p_location_sentence` |
| Paired criteria on F5 | Not drawn; the type is read from `declarations.criteria[declaration_index]` (the criteria row has no `type`). T1-11 and T1-17 still print them with `not assessable` | `render/figures.py::_criterion_lines` | `test_e9_repair4.py::test_a_paired_difference_criterion_draws_no_f5_line` |
| Reference-level criteria | Printed in no T1-11 (existing behaviour, now stated in the docstring) | `render/t1.py::subgroup_blocks` | `::test_the_sex_t1_11_rows_still_print_the_paired_rows_status_and_not_cref` |

## CLI and output changes S4 must know (DEC-43)

The site still pins `a0c9abc` (`proofpack-site/.github/workflows/ci.yml` line 18, `deploy.yml` line 31; site HEAD `02c42b8`, read now). The full list from `eda8a35` is in `2026-09-23_E9.md` and stands, with the T7 size corrected below. **Since `952bddc`:**

- **Flags, defaults, output paths, `run.json` keys: none moved.** `git diff --stat 952bddc e62d329 -- src/proofpack/cli.py src/proofpack/run.py schema/ src/proofpack/manifest.py src/proofpack/licence/ pyproject.toml uv.lock src/proofpack/render/format.py` prints nothing (now). `criteria_results` still carries no `type`.
- **Page bytes.** Sample pack now, both shells: run.json 236,842 / 236,843, T1 142,924, **T7 47,666** (was 47,830), T8 35,178. The e2e `T8.html` is 43,961 bytes (unchanged).
- **Page text.** Every T1 with a fairness declaration prints the new `FAIRNESS_GAP` text; every T7 loses the z/p sentence (`_schema/conventions_T7.md`); on runs whose criteria name more than one operating point, T1-11 prints fewer criteria rows and F5 draws fewer lines; F5 draws no line for a paired criterion.
- **Python API.** `templates.LIBRARY["FAIRNESS_GAP"]` skeleton text changed. `render.figures._criterion_lines` gained an `op` parameter (private). Lens 4 RG: `proofpack-site` at `02c42b8` greps for none of `FAIRNESS_GAP`, `AUROC gap`, `_criterion_lines`, `subgroup_blocks`, `47830`, `conventions_T7`.
- **`design/tokens.json`: unchanged** (0 lines since `eda8a35`, now). E9 added no token.
- **Pin target.** The candidate is `e62d329` (this note's commit changes only `handoffs/`). It waits on Josh's reading of row 121: if he grades it a blocker, the pin moves to the sha after that repair and its lens. The orchestrator pushes the engine first.

## Re-run these

Measured 24 September, 11:55-12:30 BST, main tree at `e62d329` with the two lens-5 notes untracked. In each shell `python -c "import proofpack;print(proofpack.__file__)"` printed `C:\Users\joshs\GPS\ProofPack\proofpack\src\proofpack\__init__.py` first. `PROOFPACK_TR39_FULL=C:/Users/joshs/GPS/ProofPack/workflows/data/confusables-18.0.0.txt` for pytest; `PYTHONIOENCODING=utf-8` for the scripts. `<scratch>` = `C:/Users/joshs/AppData/Local/Temp/claude/C--Users-joshs-GPS-ProofPack/498a4e52-21b2-438d-8861-2ecc4702fe63/scratchpad`. The r6 block is `bash <scratch>/rerun/rerun.sh <tag>` (Git Bash) and `<scratch>/E9handoff/rerun_h.ps1` (PowerShell). Both suites ran at the same time; both sweeps ran at the same time.

| Command | Git Bash | PowerShell 5.1 |
|---|---|---|
| `python -m pytest -q -p no:cacheprovider` | `1415 passed, 1 skipped, 1 xfailed in 124.08s` | `1415 passed, 1 skipped, 1 xfailed in 124.23s` |
| `-m day9` / `day8` / `day7` / `day6` / `day5` / `ap2` | `159` / `468` / `139` / `285` / `54` / `88 passed` | the same six |
| `-m day4` / `day3` / `day2` / `day1` | `182` / `29 passed, 1 xfailed` / `40` / `59 passed, 1 skipped` | the same four |
| `python -m pytest -q -s tests/test_render_timing.py` | `run --templates T1,T7,T8 4.78 s; T1+T7+T8 renders from run.json 0.27 s; T1 3693048 bytes` | `4.81 s; 0.30 s; T1 3693048 bytes` |
| `python -m ruff check .` / `ruff format --check .` | `All checks passed!` / `200 files already formatted` (198 tracked + the two lens-5 notes; 201 with this note) | the same |
| `python -m proofpack.cli doctor --offline` | exit 0, 17 `[ok` lines | exit 0, 17 `[ok` lines, `All essential checks passed.` once |
| `python scripts/tr39_subset.py ../workflows/data/confusables-18.0.0.txt --check` | `vendored subset matches`, exit 0 | the same |
| `python scripts/mutation_sweep.py --list` | 292 lines: 16 ap2, 34 day5, 112 day6, 25 day7, 87 day8, 18 day9 | 292 lines |
| `python scripts/mutation_sweep.py --marker day9` | `18 planted, 18 killed, 0 survived; 205 s`, exit 0 | `18 planted, 18 killed, 0 survived; 203 s`, exit 0 |
| `python scripts/build_sample_pack.py --out <dir>` | exit 0; run.json 236,842 / T1 142,924 / T7 47,666 / T8 35,178 | exit 0; 236,843 / 142,924 / 47,666 / 35,178 (the two run.json equal after masking `run_id`, `started`, `duration_s`) |
| no-licence `run ... --offline` on r6's `e2e_shell` | exit 4; `watermark: NO LICENCE - not for submission`; `Next step: proofpack licence install FILE, then run again for T8.html (docs: /docs/run)`; `253938 bytes 20 keys 70 claims 0 rejections 3 refs [0, 1, 2, 2, 2, 3, 4, 5, None] CRLF 0 BOM False` | `exit=-1` (`Select-Object -First` stops the process, as in the E9 handoff); the same `runjson.py` line |
| no mapping / `licence show` | `exit=3 dir=no` (`HALT H07`) / exit 4 | `exit=3 dir=False` / `exit=-1` (the same `Select-Object` cause) |
| `html_run.py` (test-only licence) | `exit 0 ['T8.html', 'ingest_report.json', 'pseudonyms.json', 'run.json']`, `T8.html bytes 43961 CRLF False BOM False` | identical |
| `repros5.py` / `repros6.py` | `B1: [('C_zero', '99/101 (98.0%) [93.1, 99.5]', ...`; `B1: [None, None, None, None, None]`, `B2: [None, None, None, 'free_text_verdict_word', 'free_text_verdict_word']`, `B3: [None, None, None, 'free_text_certification_word']`, `HALT H08 ... U+0000` (then the known `FileNotFoundError`) | identical |
| `p3_fair` / `p2_star` / `p7_site` / `alias` / `offline` / `paths` / `imports` | S02 halts (`sex` gives `H07`); `{{ 7*7 }}` literal once, `<script>` 0 raw; `exit 3 ... pack exists: False`; `rc 0 socket calls 0`; three `HALT H08` lines; `imports ok; loaded: []` | identical (the ps1 runs no `p7_site` and one `p2_star` line) |
| `k1_checker.py` (r6 row 87) | `engine claims rejected: 0`; `free_text accepted: ['unbiassed (British)', ... 'guidance plural']` (15) | the same line, cut at 90 characters by the script |
| verdict grep / E6 schema test | `5 passed, 1412 deselected` / `1 passed` | - |
| `python <scratch>/E9handoff/b1_repro.py <repo>` (row 93) | `F5 C3 line count: 0` / `T1-11 plain 'C3 (row 1)': 1 ['C3 (row 1) → criterion met']` / `F5 caption op: op1` | identical |
| `python <scratch>/E9r2/twoop_cli.py <repo> <out>` (row 94) | `rc 0 socket calls 0`; both sentences end `AUROC gap (no operating point) +0.079 [−0.002, +0.160].` | identical |
| `python <scratch>/E9r4/cli_b1.py <repo> <out>` (lens-4 B1) | `rc 0 socket calls 0`; `F5-sex-sensitivity [] 0`, `-specificity [] 0`, `-auroc [] 0`; T1-11 op1 `Cpd 1 not assessable`, `Cauc_pd 2 not assessable`, `fairness:tpr_gap 4 criterion not met`; op2 `Cauc_pd 2`, `C3 3 criterion met`, `fairness:tpr_gap 5` | identical |
| `python <scratch>/E9r3handoff/n1_text.py <repo>` (row 121) | `CI lower bound >= −0.05 count: 1`, `paired count: 0`, `F5 Cpd label count: 0` | identical |
| pre-fix: `test_e9_repair3.py` in a `952bddc` worktree / `test_e9_repair4.py` in a `f72a9af` worktree, `PYTHONPATH` forced | `5 failed in 2.40s` / `3 failed, 1 passed in 1.72s` | - |
| `git diff eda8a35 e62d329 --` `stats/ gates.py criteria.py` / `tokens.json` / `checker.py claims.py egress/` | `0` / `0` / `0` lines | - |

## Tomorrow needs

Day 9 was a short day: 21:30-00:22 on Wednesday, then lens rounds 3-5 and repairs 3-4 on Thursday morning (repair 3 committed 10:35 BST, repair 4 11:39 BST). What that cost:

- Thursday morning, which was day 10's time, went to day 9's repairs; E10 starts late;
- no sweep mutants for repairs 1 and 2; three repair-4-area survivors with no test (rows 123-125);
- no day-8 sweep since repair 2; no wheel in a fresh venv since `e17a510`; no browser check of the SVG or the print CSS.

In order:

1. **Josh grades row 121 (needs 32) before the engine is pushed.** If it is a blocker: add the declared `type` to T1-17 and a "difference vs prior version" phrase to `CRITERION_STATUS` (a D4 §8 skeleton change), with a test on lens 5's `Cpd` input that fails at `e62d329`, and a lens round.
2. **Push, then S4 (day 10) moves the pin** to `e62d329` or to item 1's sha, with the S4 section above plus the E9 handoff's list.
3. **E10 per `spec/briefs/day10_E.md`:** `stats.comparison` (McNemar, Newcombe paired, paired DeLong Δ / cluster-bootstrap Δ, Brier and slope bootstrap Δ); the unpaired fallback labelled "not like-for-like"; the margin from `criteria.yaml` only; H12; T2 (D4 §3) as HTML. Tests: fixture F5 with a frozen Newcombe paired value, H12, forbidden-word grep on T2. Accept: fixture F5 at margin −0.05 -> `not_met`. The brief's "F5" is the fixture, not T1's forest figure. **E10 is a statistics day**, so DEC-12 (i) applies to every repair of its gates. E10 inherits from E9:
   - `paired_difference_vs_prior` criteria, today `not_assessable` / `requires_compare` and drawn on no F5 (repair 4). Once `compare` evaluates them, row 121's wording is live on every compare run, and T2 is where a paired margin belongs;
   - the library (`lookup()`, facet bindings, `render/sentences.py`), `base.html` furniture and the `_criteria.html` macro;
   - the multi-operating-point items (rows 98-100, 122), which a version comparison over several operating points would meet again;
   - the page grep's narrower list (row 116); row 87 (no `free_text`); rows 49 (Clopper-Pearson) and DEC-55 (`gate_h05` under clustering), both still not built.

   The brief's people track mentions a "T1 DOCX" and a "design partner signed". The engine renders T1 as HTML only, and **there is no design partner**. Josh's review hour would read the sample pack's T1 HTML.
4. **Carried process items:** the day-8 sweep in both shells (row 106); sweep mutants for repairs 1-2 and tests for rows 123-125; a wheel in a fresh venv (needs `hatchling`, a download to approve).

## Needs from Josh

E8 needs still undecided, numbered as in r6: 1, 2, 3, 4, 5, 8, 15, 16, 17 (row 87: `free_text` still accepts `approves`, `certifies`, `endorses`, `clears`, `authorised`, `unbiassed`, now), 18, 19. Text as in `2026-09-23_E9.md`.

E9 needs still undecided, as numbered there: 20 (Clopper-Pearson deviation), 21 (default `--templates`), 22 (publish the sample pack as built), 23 (citations), 24 (THRESH_PROVENANCE), 25 (T7 conventions text), 26 (exact-half rounding), 27 (T1 title), 28 (page grep), 29 (day-7 key test), 30 (op ids).

31. **`FAIRNESS_GAP`** (row 94): repair 3 kept one sentence per operating point and labels the AUROC gap "(no operating point)". Confirm, or ask for a separate AUROC sentence? The table form is row 122.

New today:

32. **Row 121: a paired-difference margin printed as a subgroup bound.** Grade it a blocker (repair before push, a D4 §8 skeleton change), or carry to E10, where `compare` makes these criteria assessable and T2 prints them? Lens 5 FA's recommendation is record-and-carry; the lens says the verdict becomes FAIL if you read lens-4 FA-B1 as covering text.
33. **Row 122:** a column note "AUROC: no operating point" on T1-10, T1-11 and T1-13, or move the AUROC columns out of the per-operating-point tables?
34. **F5 criterion lines outside the axis or on an absent level** (rows 109, 130): clip, omit with a caption line, or draw only on the level's row? (Repair 1's open question 1.)

## Open questions

1. F5 draws the first operating point only (row 100), so op2 criteria and paired criteria now appear on no figure and no caption says so. One F5 set per operating point, or a caption line naming what is not drawn?
2. Should the engine generate claims for the ten library-only templates (row 120)?
3. A present but refused licence file: `LICENCE EXPIRED` (as built), or DEC-48's `NO LICENCE`?
4. The ASCII hyphen in `PLACEHOLDER` / `INCOMPLETE_MARK`, where D4 prints an em dash: keep, or follow D4?
5. Should repair notes live in the repository? Two test docstrings now point to a note outside it (row 127).

## Sentences refused

The list in `2026-09-23_E9.md` (build note, repairs 1-2, lenses 1-3) stands. Added from repair 4, lenses 4 and 5, and this note (repair 3's note is the orchestrator's and lists none):

- "F5 now draws each criterion on the right plot" / "F5 draws each criterion on the right plot." Written instead: on `Cpd`, `Cauc_pd`, `Cpt`, `Cauc`, `Cref`, the three sex F5 plots draw `Cpt`, `Cref`, `Cauc` only (repair 4); lens 4 FA B1 is a counter-example at `f72a9af`.
- "No declared value is drawn on a plot of a quantity it does not constrain." Refused: only `type` was added to the filter, and row 130's `Cabs` is drawn.
- "F5 now draws only criteria that constrain the plotted statistic" / "F5 never draws a paired-difference criterion." Written instead: on 11 named inputs and 7 paired variants, none drawn.
- "The page no longer presents a paired margin as a level threshold." Refused: row 121.
- "Every op-less criteria row is an AUROC criterion." Refused (lens 4 FA-N2, RG-N2).
- "Every criterion is now attached to the right operating point." Written instead: 20 of 20 on lens 4 RG's two documents.
- "Every repair-4 test fails before the fix" / "Every test in the repair fails before the fix." Refused: `3 failed, 1 passed`.
- "The repair-3 E lines are in the repair note" / "The repair-3 docstring now points to where its E lines are." Refused: row 127.
- "S4 has nothing to move." Refused: the page changes above.
- "The F5 filter has no surviving mutant" / "`_criterion_lines` has no surviving mutant." Refused: rows 123-125.
- "Repair 3 introduced no defect." Refused: lens 4 FA B1 predates it, and N1-N3 are in its text.
- "No page prints a verdict word." Written instead: the pages each lens names, with `endorsed` and `guarantee` in negations.
- "The note's commands all run in both shells" / "The re-run block matches byte for byte." Refused: rows 129 and the records row.
- "Repair 4 closed every lens-4 sentence finding." Refused: row 127.
- In this note: "E9 is done", "E9 passed", "the engine is ready to push" and "the pin can move to `e62d329`" were not written. PASS appears only as the lenses' own verdicts, beside lens 5's statement of when it becomes FAIL.
