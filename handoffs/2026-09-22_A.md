# Lane A — build day 8 (A-P2: egress, telemetry, `--offline`), 22–23 September 2026

Written on 23 September 2026 by the handoff session from the build note, the two repair notes and the six lens notes (all named below), with every suite and gate figure in "Re-run these" measured again in this session. Engine: branch `a-p2-egress`, `7b2ca2a..028170e` (six commits, not merged, not pushed). Site: `main`, `994f460..7e7e460` (three commits, not pushed).

**Verdict.** **FAIL**: lens round 3 (both lenses, at `028170e` / `7e7e460`) ended FAIL on two open blockers, both sentences on `/legal/dpa` clause 2.1 written in repair round 2: (B1) "a later revision of that definition, with the same ten keys and stricter value patterns" (the later revision admits a null `licence_id`, `lic_` ids, macOS platform tags and the E/S halt codes that the published copy refuses), and (B2) "in the engine's tests for a 500, a timeout, a refused connection, a 3xx and an unreadable schema resource the run's exit code and run.json are the same as with --offline" (no engine test compares `run.json` with an `--offline` run; a mutant that appends a byte to `run.json` on a 3xx or `schema_unavailable` passed all 876). The orchestrator counts **46** items carried after the final lens; no egress byte, socket or exit-code defect was found in round 3, and the site's `main` must not be pushed until B1 and B2 are repaired, because a push deploys `/legal/dpa`.

## What landed — engine (`proofpack`, branch `a-p2-egress`)

Commits: `4cad727` feature · `4d61b6e` sweep-copy skip · `47d1aeb` repair 1 · `0cf9ba5` lens-1 notes · `466ab15` repair 2 · `028170e` lens-2 notes. Test ids are in `tests/test_egress.py`, `tests/test_telemetry.py` and `tests/test_offline.py` unless named otherwise.

| Item | Commit | Test ids | Literal input → figure asserted |
|---|---|---|---|
| k-suppression (subgroup rows, gap rows, calibration bins, the overall 2×2, every Number on its own counts; defaults n 10 / events 5 / non-events 5; unknown `n` suppresses) | `4cad727` | `test_f19_the_n7_cell_is_suppressed_and_its_ten_wilson_bounds_are_absent_from_the_bytes` (renamed in `47d1aeb`), `test_is_suppressed_on_the_defaults` (7 params), `test_a_stricter_threshold_suppresses_the_sixty_row_cells_too` | F19 cohort (seed 20240101, 400 rows; `St Mary's` rows 0–6 = 2 positives / 5 negatives): its 7 cells are null; its **10** Wilson bounds of ≥ 10 characters are absent from the aggregates bytes and the captured telemetry bytes; `min_n 61` suppresses every `site` cell but `Royal Free` (n 273) |
| A looser `egress.suppression` value is H08 | `4cad727` | `test_a_looser_suppression_value_is_refused_h08_at_the_cli_and_nothing_is_written`, `test_a_looser_value_handed_directly_is_h08_naming_field_declared_and_default` (4 params) | `min_n: 9` → `HALT H08 … egress/suppression/min_n: minimum`, exit 3, no `pack/`, 0 transport calls; dict route detail `{"field": "egress.suppression.min_n", "declared": 9, "default": 10}` |
| Pseudonymisation (`site`/`device`/`protocol`/`attr_*` → `Site A…` in code-point order; map to `<out>/pseudonyms.json` only) | `4cad727` | `test_f19_pseudonyms_json_holds_the_map_beside_run_json_and_is_not_inside_any_payload`, `test_f19_a_site_named_like_a_pseudonym_is_remapped_and_the_map_is_a_bijection` | F19 map `{"Königsberg": "Site A", "Royal Free": "Site B", "Site A": "Site C", "St Mary's": "Site D"}`; pack = `ingest_report.json`, `pseudonyms.json`, `run.json` |
| Whitelist projection onto `schema/egress_schema.json` (unknown keys dropped; strings must match `enum`/`const`/`pattern` by `re.fullmatch` since `47d1aeb`) | `4cad727`, `47d1aeb` | `test_whitelist_drops_unknown_keys_and_refuses_unconstrained_or_mismatched_strings`, `test_whitelist_refuses_a_trailing_newline_that_re_search_accepts` | candidate with `roc`, `original_header`, `site` → the ten keys; `platform: "St Mary's"` → `WhitelistError`; `"lic_abc\n"`, `"win-amd64-cp314\n"` → refused (were accepted at `4d61b6e`) |
| Telemetry payload = D1 §6's ten literal keys, validated by `jsonschema` before return and again inside `send()` | `4cad727`, `47d1aeb` | `test_f19_the_telemetry_payload_validates_and_carries_only_the_schema_keys`, `test_row_count_bucket_boundaries` (8 params), `test_send_refuses_a_trailing_newline_in_licence_id_and_platform` | F19 captured payload 374 bytes (lens 1, lens 2), keys `schema, licence_id, run_id, engine_version, platform, manifest_sha256, duration_s, halt_code, row_count_bucket, timestamp`; `999 → <1k`, `1000 → 1k-10k`, `100000 → >100k` |
| F19 forbidden bytes | `4cad727` | `test_f19_the_forbidden_bytes_are_absent_from_both_payloads_and_the_captured_bytes` | 14 named needles + 4 site labels (the collision label excepted) + every one of 400 ROC thresholds (E7 RG-N8) + `"roc"`, `"declarations"`, `manifest.started` → absent from the telemetry bytes, the aggregates bytes and the recorder's bytes |
| `schema/egress_schema.json` final (`additionalProperties: false` on every object; every string with `enum`/`const`/`pattern`) | `4cad727` (+ descriptions in `47d1aeb`, `466ab15`) | `test_every_string_property_in_the_schema_is_constrained_and_every_object_is_closed`, `test_schema_enums_and_constants_agree_with_the_engine` | 0 unconstrained strings, 0 open objects; lens 1–3 independent walks: 12 object nodes, 14 string nodes |
| Transport: one HTTP request, 3xx not followed (`47d1aeb`), a 5 s socket timeout on connect/handshake and a 5 s deadline on reads and writes after it (`466ab15`); `SendResult` for every `Exception` (`schema_unavailable` added in `466ab15`) | `4cad727`, `47d1aeb`, `466ab15` | `test_a_redirect_answer_is_not_followed_and_the_location_host_receives_nothing[301..308]`, `test_a_302_through_the_cli_prints_w16_and_the_location_host_receives_nothing`, `test_a_receiver_that_answers_one_byte_at_a_time_is_cut_off_at_the_deadline`, `test_send_with_the_schema_resource_unreadable_is_schema_unavailable` | 301/302/303 → `SendResult(False, NNN, "http_error")`, second listener receives nothing (at `4d61b6e`: `sent: True`); drip 1 byte / 0.3 s with `timeout=1.0` → `timeout` in 1.00 s (at `0cf9ba5`: sent after 13.5 s); `egress_schema` raising `OSError` → `schema_unavailable`, 0 transport calls |
| W16: one line on a failed send, exit code unchanged | `4cad727`, `466ab15` | `test_a_failed_send_prints_one_w16_line_and_leaves_exit_code_and_documents`, `test_the_exit_code_with_a_failed_send_equals_the_exit_code_offline`, `test_a_run_with_the_schema_resource_unreadable_prints_w16_and_exits_as_the_run` | 500 / `TimeoutError` / `ConnectionRefusedError` → exit 0 and the three documents' hashes equal before and after the send; a 503 → exit code equal to the `--offline` run's; unreadable schema → exit 0 (at `0cf9ba5`: exit 5) |
| The one call site, `cli.cmd_run`, after `write_run`; `--offline` and `egress.telemetry: false` short-circuit before anything is built | `4cad727`, `466ab15` (`cli.py` down to +20 −3) | `test_offline_never_calls_the_transport`, `test_telemetry_false_never_calls_the_transport`, `test_telemetry_false_with_the_real_transport_and_sockets_refused_opens_no_socket`, `test_an_absent_egress_block_is_opt_out_so_the_send_happens` | 0 transport calls, 0 calls on `socket.socket` / `getaddrinfo` / `create_connection` |
| `--offline` socket test | `4cad727` | `test_run_offline_opens_no_socket_and_calls_no_transport`, `test_map_yes_offline_opens_no_socket`, `test_compare_offline_opens_no_socket`, `test_doctor_offline_opens_no_socket`, `test_licence_verify_offline_opens_no_socket` | `run`, `map --yes`, `compare`, `doctor`, `--offline licence verify` with the three entry points raising → 0 calls each |
| CI job `offline-namespace` (`unshare -rn`, fallback `sudo unshare -n`) + `scripts/ci_namespace_run.py` | `4cad727` | `test_the_ci_namespace_job_and_its_script_exist_and_assert_both_invocations` (presence only) | **[unverified] — has not run anywhere** (branch unpushed; no Linux with Python on this machine) |
| Mutation sweep, 16 `ap2` mutants | `4cad727` | `test_the_mutation_sweep_declares_an_ap2_list_of_at_least_eight` | `16 planted, 16 killed, 0 survived` (builder 75 s; lens 3 fresh attack 77 s) |

## What landed — site (`proofpack-site`, `main`)

| Item | Commit | Test ids | Literal input → figure asserted |
|---|---|---|---|
| `runs` table (`id`, the ten payload keys, `received_at`; unique `run_id`) in `db/schema.sql` + idempotent `db/migrations/2026-09-22_runs.sql` | `b8242c1` | `tests/day8-telemetry.test.mjs` "db/schema.sql and the migration declare the runs table with exactly the ten keys plus id and received_at" | both parse to `['id', …ten…, 'received_at']`; no `ip`, `user_agent`, `headers` column |
| `POST /api/telemetry` (`functions/api/telemetry.js`, `functions/_lib/telemetry.mjs`, vendored `functions/_lib/telemetry-schema.mjs`) | `b8242c1` (schema copy regenerated in `2b5c7de`, `7e7e460`) | `tests/day8-telemetry.test.mjs` (20 tests) | 405 + `Allow: POST` on other methods; 413 over 4096 bytes; 415 non-JSON type; twelve bad values → 400 naming the key only; duplicate `run_id` → 204, one row; no `Set-Cookie`, no `Access-Control-*` |
| Egress row `runner-telemetry` `not_built → live`; six-endpoint sentences on `/trust`, `/legal/privacy`, `/legal/sub-processors`; `/docs/egress`; DPA 2.1 | `b8242c1`, `2b5c7de`, `7e7e460` | `tests/day8-ap2-repair1.test.mjs` (AP2R1-B1 ×2, -B2, -N1, -N7), `tests/day8-ap2-repair2.test.mjs` (six), `day4-repair4` S4R4-NB2, `day7-unlensed-repair1` FA-B1/RG-B2 | `check:egress` 0 errors; see the site note `proofpack-site/handoffs/2026-09-22_S_ap2-site.md` for the sentences |

## What was cut, and why

- **`exit_code`, `manifest_hash`, `duration_ms` are not in the payload.** D1 §6's literal ten keys are what `/legal/dpa` 2.1 and `/trust`'s exhaustive field list already promise, and the drift gate compares the trust page's vendored copy against `ENGINE_REF` (`a0c9abc`). The payload carries `manifest_sha256` and `duration_s`; D2 §5.1 / D3 §4 still name the other three (lens 2 regression N6).
- **A HALT sends nothing.** The single call site runs after `run.json` is written; `halt_code` is null on every record today. Open question 1.
- **`proofpack fixtures`** is not a subcommand at `7b2ca2a`, so the `--offline` socket test covers `run`, `map`, `compare`, `doctor` and `licence verify` only.
- **No intersectional cell** exists at `7b2ca2a`; F19's n = 7 cell is the site level `St Mary's`.
- **The `unshare -rn` job is written and not run.**
- **The purge of `runs` rows older than 24 months is not built** (the DDL and every 24-month sentence say so).

## What did not land (carried) — the headline items of the 46

| Item | Reason | Repro |
|---|---|---|
| **Lens-3 B1**: DPA 2.1 "same ten keys and stricter value patterns"; the `/trust` row's "tightens licence_id …" | False; pinned by `day8-ap2-repair2.test.mjs:74` (AP2R2-N4). Round cap reached | lens 3 regression `schemacmp.py`, fresh-attack `r3/prop_cmp.py`: `licence_id=None`, `platform=macosx-14.0-arm64-cp312`, `halt_code=E01` published refuse / later admit |
| **Lens-3 B2**: "the run's exit code and run.json are the same as with --offline" (DPA 2.1, `/trust` row `limits`, `/docs/egress`, `errors.py` comment) | The 3xx and unreadable-schema tests never read `run.json`; no test compares with `--offline` | the mutant in lens 3 regression B2 → `876 passed`, `-m ap2 88 passed`. Fix: hash `run.json` in those two tests, reword to what each test reads |
| `/legal/sub-processors` Supabase row "as of 2026-09-23 it had not been applied, the table did not exist" | False since 02:18 BST on 23 September (the migration was applied, DEC-59); the orchestrator's addendum to the build note names it | `grep -n "had not been applied" src/content/legal/sub-processors.md` → one row plus the "what changed" sentence; `tests/day8-ap2-repair1.test.mjs:52` already admits `applied on 2026-\d{2}-\d{2}` |
| HTTPS path untested: hand mutants R1 (no deadline wrap), **R2 (certificate verification off)**, R3 (HTTPS handler dropped), R5, R7, R10 survive 88 `ap2` tests | Repair 2's new code; round cap | lens 3 fresh attack `r3/mutants_r3.py`; R2 against a self-signed loopback TLS server → `sent=True`, 1 body received |
| FA-L2-N1: `egress: {Telemetry: false}` sends; unknown keys under `egress`/`suppression` accepted; `min_n: 10.0` runs | Lane E's `criteria_schema.json` (`additionalProperties: false`, `multipleOf: 1`), a suppression-gate change | lens 3 fresh attack N6: `rc=0 transport calls=1` |
| Sentences: "an `--offline` process never imports a network module" (`telemetry.py`); test id `test_send_never_raises_and_classifies_the_failure`; `/trust` "a 5 s deadline … on every read and write" versus a proxy that drips its CONNECT answer (20.62 s); "45-byte" answer (46 bytes); `/docs/egress` "the same definition" | Round cap | lens 3 fresh attack N3–N5; lens 3 regression N1–N3 |
| Endpoint nits: `looksLikeDuplicate` `/409/` matches `4096` (answers 204, stores nothing); content-type substring; `timestamptz` of `2026-99-99` | Kept for one endpoint lens | lens 2 fresh attack N8 (`attack_r2_endpoint.mjs` section 5) |
| Aggregates rules (unsent at launch): short customer tokens pass into `level`; a gap is unsuppressed when the reference level is the small cell; an `attr_*` header is serialised | D1 §6 rule changes, own lens | lens 1 fresh attack N2, lens 2 fresh attack N5, N6 |
| `send()` posts `NaN` when handed it directly (not reachable from `cmd_run`); the telemetry sentence prints under a skip | Record | lens 3 fresh attack N7, N8 |
| `licence verify FILE --offline` → argparse exit 2; `--offline licence install <installed path>` → exit 5 | Pre-existing at `7b2ca2a`; lane E's CLI | lens 1 fresh attack N10 |
| `check:egress` reads the row's `state` as page-versus-manifest equality; the Function's schema copy is compared by no CI job until the pin moves | Scanners frozen; DEC-43 | lens 2 regression N7; lens 1 regression N11 |
| Supabase's contracting entity on the sub-processor row is `[unverified]` | The dashboard notice reads "The data controller is now Supabase Pte. Ltd."; not read from Supabase's terms | build note, orchestrator addendum 2 |

## What the lenses found

| Round | Shas | Regression | Fresh attack |
|---|---|---|---|
| 1 | `4d61b6e` / `b8242c1` | **FAIL**, 3 blockers: `test:unit` red (FA-B4, `/trust` dated 2026-09-21 on a page changed on the 23rd); `/trust` "its CI job runs" a job that never ran; a test id asserting a sentence the builder had measured false | **FAIL**: `/legal/sub-processors` "since 22 September 2026" for an undeployed receiver and an absent table; the same FA-B4; N1 the transport followed a 301/302/303 to another host and printed `telemetry sent (http 204)` |
| 2 | `0cf9ba5` / `2b5c7de` | **PASS**, no blocker | **PASS**, nine findings: a receiver dripping bytes held `send()` 27.0 s and counted as sent (N2); an unreadable schema turned the exit into 5 (N3); `/trust` published the `a0c9abc` skeleton as "the schema the engine validates its own outbound payload against" (N4); `merge-tree 29fc04e` conflicted in `cli.py` and `mutation_sweep.py` (N7). Repair 2 took all four |
| 3 | `028170e` / `7e7e460` | **FAIL**, B1 + B2 above | **FAIL**, the same B1 + B2; HTTPS mutants survive (N1); the merged tree fails one test (N2) |

What no lens could break, with the bytes grepped: lens 1's `attack_payload.py`, re-run in rounds 2 and 3, grepped **424 needles** (every header, every site level, `St Mary's`, `Königsberg` in both spellings, `Royal Free`, `clinician_note` and its value, the ledger key, the licence signature and its base64 payload, 40 bytes of `mapping.json`, the serialised `pseudonyms.json` map, the criterion date, `started`, the model name, all 400 distinct ROC thresholds, every `YYYY-MM-DD` in `run.json`) against the F19 telemetry and aggregates bytes: the telemetry hits were only today's date inside the payload's own `timestamp`; the aggregates hits only the canonical names `sex`, `age`, `site`. Synthetic cohort (5,000 rows): 377 bytes, 12 needles, no hit. Lens 1's `attack_socket.py` under a trap on `socket.socket.__init__`, `getaddrinfo`, `create_connection`: `sockets=[]` for every `--offline` command in all three rounds; exit 0 and normalised `run.json` equal to the offline run's for 204, 500, 413, 301, 302, 303, 307, 308, a hang (5.07 s), a closed port and a self-signed certificate (0 bodies received). Looser values (`min_n 9 / 0 / -1 / "10" / 10.5 / 9.0 / true`, `min_events 4`) → H08, exit 3, 0 transport calls. The endpoint echoed no input, set no cookie, and its medians for an unknown, a known and a duplicate licence were 0.036 / 0.046 / 0.044 ms (memory adapter; Postgres not measured).

## Decisions taken

| # | Decision | Where recorded |
|---|---|---|
| 1 | A looser suppression value is **H08** (exit 3, nothing written), not a W-code | build note decision 1 |
| 2 | Pseudonym order = Unicode code-point order within each attribute; a real `Site A` takes its sorted position (F19: `Site C`) | build note decision 2 |
| 3 | Row buckets `<1k`, `1k-10k`, `10k-100k`, `>100k` over `flow.rows_read` | build note decision 3 |
| 4 | `TELEMETRY_URL` one constant; the CLI passes no url and reads no variable for one | build note decision 4; repair 1 wording |
| 5 | Every Number judged on its own counts; unknown `n` suppresses | build note decision 5 |
| 6 | `licence_id` null unless the licence signature verified (`ok`/`grace`/`expired`); pattern `^lic_[A-Za-z0-9]{1,60}$` | build note decision 6 |
| 7 | Opt-out: absent `egress.telemetry` = on; only boolean `false` turns it off | build note decision 7 |
| 8 | Two schema copies on the site: `/trust`'s at `a0c9abc` (unchanged), the Function's generated by `scripts/vendor-telemetry-schema.mjs` (now at `028170e`) | build note decision 8 |
| 9 | `runs` columns are the payload's key names (`manifest_sha256`, `duration_s`) | build note decision 9 |
| 10 | Endpoint count five → six; the runner-telemetry row's state word `live` | build note decisions 10, 11 |
| 11 | A 3xx is `http_error`, never followed; a 5 s deadline on reads and writes; `schema_unavailable` reason code | repair notes 1 and 2 |
| 12 | The migration applied by the orchestrator from the Supabase SQL editor, 23 September 02:18 BST | DEC-59; build note addendum 2 |
| 13 | This session removed the repair-2 worktree (clean at `028170e`) to check the branch out, and deleted the main tree's two untracked lens-3 copies after committing byte-identical copies on the branch (the precedent of repairs 1 and 2; untracked copies would block the merge) | this note |
| 14 | One byte-level change to a lens note: the counter-example block in `2026-09-22_A_ap2_lens3_regression.md` is fenced `text` instead of `python`, because `ruff format --check .` reads Python blocks in Markdown and measured `1 file would be reformatted` on it (the block's code is unchanged) | this note |

## For the orchestrator

**The merge.** The branch touches, outside `src/proofpack/egress/` (`git diff --numstat 7b2ca2a 028170e`): `.github/workflows/ci.yml` +42, `pyproject.toml` +1 (the `ap2` marker), `schema/egress_schema.json` +125 −30, `scripts/ci_namespace_run.py` +74 (new), `scripts/mutation_sweep.py` +172 −2, `src/proofpack/cli.py` +20 −3, `doctor.py` +8 −1, `errors.py` +11 −1, `run.py` +12 −1, `tests/conftest.py` +37, `tests/test_doctor_cli.py` +3 −1, plus the three new test files and the six lens notes. Engine `main` has also changed `ci.yml`, `pyproject.toml`, `mutation_sweep.py`, `cli.py`, `errors.py` and `run.py` since `7b2ca2a`. Measured today: `git merge-tree --write-tree 7fa690b 028170e` → exit 0, no conflict, tree `2109b084`. That tree, checked out and run with `PYTHONPATH` forced: **`1 failed, 1099 passed, 1 skipped, 1 xfailed in 101.47s`**; `-m ap2` `1 failed, 87 passed`. The failure is `tests/test_egress.py::test_f19_pseudonyms_json_holds_the_map_beside_run_json_and_is_not_inside_any_payload` line 307: `['T8.html', '...', 'run.json'] == ['ingest_repo...', 'run.json']` — E8 writes `T8.html` into the pack, and the test lists the pack exactly. In the merged `cli.py` the telemetry call (line 487–489) sits after `write_run` (480) and `write_documents` (481). The resolution belongs in the merge commit: the test asserts `pseudonyms.json` beside `run.json` and the pack listing equal to E8's documents plus `pseudonyms.json`. Anything lane E commits after `7fa690b` is not in this measurement.

**The migration.** `db/migrations/2026-09-22_runs.sql` is **already applied** (DEC-59, 23 September 02:18 BST, 12 columns and 4 indexes verified by the orchestrator; this session holds no database credentials and did not re-measure it). Nothing further to apply. The `/legal/sub-processors` sentence saying it had not been applied must now be dated to 23 September 2026.

**The drift gate.** Measured today in the site main tree: `PROOFPACK_ENGINE_REQUIRED=1 npm run check:egress` with the engine at `a0c9abc` → exit 0; with the engine at `028170e` → `FAIL the vendored egress schema has DRIFTED from the engine repository.` At the **merge** of `a-p2-egress` into engine `main`: nothing on the site changes; `check:egress` and the `egress-drift` job compare `src/data/egress_schema.json` against `ENGINE_REF` (`a0c9abca846b…`, `ci.yml` line 18), which does not move. At the **pin move** (S4, day 10): the job is red until `src/data/egress_schema.json` and `egress_schema.provenance.json` are re-copied from the engine; in the same move re-run `scripts/vendor-telemetry-schema.mjs` against the new `ENGINE_REF` (the Function's copy names `028170e`, and `tests/day8-telemetry.test.mjs` compares it only when a checkout at that sha is reachable, so it skips in CI until then), and rewrite DPA 2.1 and the row's `state_note` (AP2R2-N4 pins the two-revision wording and will fail on purpose).

**The CI job that runs only on ubuntu.** `offline-namespace` (`.github/workflows/ci.yml` on the branch) has never run. It runs on the first push of the branch or of the merge to the public repository; read the first run, not the YAML. If `ubuntu-latest` refuses both `unshare -rn` and `sudo unshare -n`, the job fails by design and needs a container step. The `egress-drift` job (site) is ubuntu as well.

**Worktrees.** Lens 3 regression found `dead0240-lens-AP2-r3-regression/*` worktrees registered before it started; `git worktree list` today still shows `eng-028170e`, `eng-0cf9ba5`, `eng-a0c9abc` under that folder. Not mine; remove them. `C:/Users/joshs/GPS/ProofPack/wt-engine-a0c9abc` is the pinned engine the site gates use; keep it.

## Re-run these (measured now, 23 September 2026)

Engine, worktree on `a-p2-egress` at `028170e` under `scratchpad/handoff-AP2/engine-wt`. Git Bash: `export PYTHONPATH=<wt>/src`; PowerShell 5.1: `$env:PYTHONPATH = "<wt>\src"`. `python -c "import proofpack;print(proofpack.__file__)"` printed `...\handoff-AP2\engine-wt\src\proofpack\__init__.py` in both.

| Command | Git Bash | PowerShell 5.1 |
|---|---|---|
| `python -m pytest -q -p no:cacheprovider` | `876 passed, 1 skipped, 1 xfailed in 79.66s` | not repeated |
| `python -m pytest -q -p no:cacheprovider -m ap2` | `88 passed, 790 deselected in 15.03s` | `88 passed, 790 deselected in 14.41s` |
| `python -m pytest -q -p no:cacheprovider -m day7` | `139 passed, 739 deselected in 7.55s` | `139 passed, 739 deselected in 7.68s` |
| `python -m ruff check .` | `All checks passed!` | `All checks passed!` |
| `python -m ruff format --check .` | `128 files already formatted` (with this note and the two lens-3 notes in the tree; 125 before) | `128 files already formatted` |
| `python -m proofpack.cli doctor --offline` | `All essential checks passed.`, exit 0 | `All essential checks passed.`, `exit=0` |
| `git merge-tree --write-tree 7fa690b 028170e` | exit 0, tree `2109b084…`; that tree: `1 failed, 1099 passed, 1 skipped, 1 xfailed` | not repeated |

The skip is `tests/test_doctor_cli.py:57` (write access cannot be revoked for this user); the xfail is F13 pending the R capture (day 12).

Site, main tree at `7e7e460`, Git Bash, `PROOFPACK_ENGINE_DIR="C:/Users/joshs/GPS/ProofPack/wt-engine-a0c9abc"` (DEC-43), `GPS_AUTOMATION_DIR="C:/Users/joshs/GPS/ProofPack/gps-automation"`:

| Command | Output |
|---|---|
| `npm run check:bytes` | `control-byte check: 277 text file(s) via git ls-files, 0 binary skipped.` / OK |
| `npm run build` | `build-wheel: proofpack-0.1.0.dev1-py3-none-any.whl (152257 bytes, sha256 9b7d6986cd96…) from engine a0c9abc`; `30 page(s) built` |
| `npm run check:links` | `30 page(s), 1243 internal link(s), 1 distinct external URL(s) (not fetched).` / OK |
| `npm run check:schema` | `schema check: OK` |
| `npm run check:pricing` | `30 page(s), 37 currency figure(s), 0 error(s).` |
| `npm run check:egress` | `vendored schema sha256_lf=c98b85aec4a1de89… canonical=1c914910cae38b8c…` / `engine copy compared: ../wt-engine-a0c9abc/schema/egress_schema.json` / `claim scan: 36 built file(s), 523 attribute value(s), 2 sent document(s)` / `egress check: 0 error(s).` |
| `npm run check:validation` | `40 check(s): 0 pass, 0 fail, 40 pending, 4 [unverified] · 0 error(s).` / OK |
| `npm run check:seo` | `seo check: 0 error(s), 5 warning(s).` |
| `npm run check:keys` | `key check: key_id=pp-2026-09 status=live — 0 error(s).` |
| `npm run test:unit` | `tests 664 / pass 663 / fail 0 / skipped 1` (the skip: the Function's schema comparison, no checkout at `028170e`) |
| `npm run test:e2e` | `9 passed (40.2s)` |
| `npm run check:legal` | red by design: `legal-placeholder check: 2 of 2 gated page(s) failing.`, exit 1 |
| `PROOFPACK_ENGINE_REQUIRED=1 npm run check:egress` (engine at `a0c9abc` / at `028170e`) | exit 0 / `FAIL the vendored egress schema has DRIFTED from the engine repository.` |
| `node --test tests/day8-telemetry.test.mjs tests/day8-ap2-repair1.test.mjs tests/day8-ap2-repair2.test.mjs` with `PROOFPACK_ENGINE_DIR` at the `028170e` worktree | `tests 31 / pass 31 / fail 0 / skipped 0` |

## Tomorrow needs (day 9 = A-P3, `spec/briefs/day9_A.md`)

A-P3 is the release pipeline and T12: pinned `python:3.12-slim` amd64 Dockerfile, GHCR image, TestPyPI wheel, CycloneDX SBOM, Trivy + ZAP baseline on tag, Dependabot, F17 (two runs → identical hash), `proofpack fixtures` → `fixtures_report.json`, T12, release assets for the site build. What it inherits from A-P2:

1. **Telemetry is on by default.** Any `proofpack run` in the release pipeline, the Docker smoke run, F17 or the TestPyPI install-then-run test attempts one POST to the live `https://proofpack.globalphoenix.co.uk/api/telemetry` unless it carries `--offline` or `egress.telemetry: false` (the pytest suite is guarded by `conftest`; scripts and CI steps outside pytest are not). Every pipeline run needs one of the two.
2. **`proofpack fixtures` does not exist yet.** When it is built it joins `tests/test_offline.py`'s list (five commands today) and the day-8 cut closes.
3. **F17's hash set.** Each run writes `pseudonyms.json`, which carries the manifest's `run_id` (build note item 3); F17 must name whether that file is in the hashed set.
4. **The `offline-namespace` job's first run** happens when the branch or the merge reaches GitHub; A-P3's CI work is where its result is read. `/trust` says the release-image network capture does not exist yet; the Docker image is where it would be built.
5. **The merge first.** A-P3 builds on engine `main`; the branch must be merged (with the one test fix above) before the release pipeline packages it, or the wheel ships without `egress/`.

## Needs from Josh

1. **Do not push `proofpack-site` main** until lens-3 B1 and B2 and the "had not been applied" row are repaired: a push deploys `/legal/dpa` 2.1 with two sentences measured false. The orchestrator's repair or tomorrow's slot takes them; no decision is needed from you beyond not pushing.
2. When convenient, read Supabase's terms page for the contracting entity (the dashboard said "Supabase Pte. Ltd."), so the sub-processor row loses its `[unverified]`. No account, no key, no spend.

## Open questions (at most five)

1. **Should a HALT send telemetry?** Today it sends nothing and `halt_code` is null on every record; a second call site in `cli.main`'s `HaltError` handler would need a nullable `manifest_sha256` (a schema change and a DPA sentence).
2. **Is `exit_code` wanted in the payload?** D3's lifecycle names it; adding it is a schema key, a column, DPA 2.1 and the `/trust` list, at the pin move, under one lens.
3. **FA-L2-N1 (`Telemetry: false` accepted and sent)** is in lane E's `criteria_schema.json`: E on day 9, or A at the pin move? It is the line a customer is likeliest to mistype.
4. **The aggregates rules** (short customer tokens in `level`, a gap row decided by the small reference level, `attr_*` headers serialised) need a D1 decision before any code sends the aggregates document.
5. **Should `REASON_CODES` (including `schema_unavailable`) be published** in the egress schema's `x-proofpack` block, so W16's reasons have a public home?

## Sentences refused

- "A-P2 passed" / "the egress work is done": the final lens verdict is FAIL.
- "the evening merge is clean": measured `merge-tree 7fa690b × 028170e` exit 0, and the merged suite `1 failed`; whatever lane E commits after `7fa690b` is not in it.
- "the migration is applied and verified": not measured by this session (no credentials); quoted from the orchestrator's addendum and DEC-59.
- "46 items are carried": the count is the orchestrator's; the table above lists the headline items, not 46 rows.
- "the drift gate stays green at the pin move": it goes red until the copy is re-made (measured with the engine at `028170e`).
- "the engine's tests show run.json unchanged on a 3xx": no test reads it there (lens-3 B2).
- "the later revision is stricter": measured false (lens-3 B1).
- "the release pipeline will not send telemetry": nothing guards scripts outside pytest; written instead as the instruction in "Tomorrow needs" 1.
- "the `offline-namespace` job passes": it has never run.
