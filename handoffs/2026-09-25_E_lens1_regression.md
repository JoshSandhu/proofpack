# Lane E - build day 10 (E10) - lens 1, regression-and-record, at `322c5a4` (25 September 2026, 21:35-23:10 BST)

**FAIL. One blocker: `tests/test_e10_t2.py::test_golden_t2_matches_the_committed_render` fails on a fresh checkout of `322c5a4` on this machine (both shells), because the golden T2 embeds the SHA-256 of the raw bytes of `tests/fixtures/f5/f5_new.csv` and `f5_prior.csv`, and a fresh checkout under `core.autocrlf true` with no `.gitattributes` gives those files CRLF while the main tree (where the builder and the orchestrator measured) has them LF. The full suite in a detached worktree of the commit is `1 failed, 1645 passed, 1 skipped, 1 xfailed`, `-m day10` is `1 failed, 61 passed`, and `scripts/mutation_sweep.py --marker day10` refuses to run (`baseline (no mutant) does not pass -m day10; refusing to sweep`, exit 2). After `tr -d '\r'` on the three F5 fixture files every figure in the builder's note reproduces in both shells: `1646 passed, 1 skipped, 1 xfailed`, `62 passed`, `14 planted, 14 killed, 0 survived`.**

Everything else the note claims I re-measured and it holds: every oracle comparison, every marker count, lint, the fixtures report, the sample pack, the timing, the F17 mask, the E9 re-run blocks in both shells, the pre-build failures. Eight non-blocking records follow the blocker, the largest a sentence in the note that generalises over run-document code paths E10 did touch.

Setup: detached worktrees `scratchpad/lens-E10-r1-regression/wt322` (`322c5a4`) and `wt811` (`81f1102`), `PYTHONPATH=<wt>/src` on every call, proved by `python -c "import proofpack;print(proofpack.__file__)"` printing the worktree's `src/proofpack/__init__.py` (without `PYTHONPATH` the same command prints the main tree's - the `.pth` trap is real). Both worktrees removed at the end; the main tree carries only this note (and the sibling fresh-attack lens's untracked note).

## Blocker

### B1. The golden T2 pins a checkout artefact: the raw-byte hash of the F5 CSVs

- **What the test does.** `test_golden_t2_matches_the_committed_render` builds the F5 compare document through `assemble_compare` on `tests/fixtures/f5/`, masks nine manifest keys (`run_id`, `started`, `duration_s`, `mapping_sha256`, `numpy`, `scipy`, `python`, `platform`, `reference_platform`), renders T2 and compares LF-normalised bytes to `tests/fixtures/golden/T2.html`. `manifest.input_sha256` and `manifest.prior_input_sha256` are **not** masked; T2's cover prints both (`Dataset SHA-256 (new version)` / `(prior version)`). `manifest.sha256_file` hashes the file's bytes as checked out.
- **Measured.** `git ls-files --eol tests/fixtures/f5/`: main tree `i/lf w/lf` for all three files; a fresh `git worktree add --detach` of `322c5a4` gives `i/lf w/crlf`. `core.autocrlf` is `true`; there is no `.gitattributes`. `scripts/make_f5_fixture.py` wrote the CSVs with `newline=""` and `lineterminator="\n"` and the YAML with `newline="\n"`, so the main tree's copies are LF and git never re-normalised them. SHA-256 of `f5_new.csv`: LF bytes `b5a7a9666e0afd37...` (the golden's value), CRLF bytes `c3a45a47eb0d6d6d...` (the worktree's value); `f5_prior.csv`: `08709dca067e4ea1...` / `ffdbabf03355e930...`. The assertion's diff shows exactly those two cells.
- **Consequence.** In a fresh worktree of the commit, both shells: full suite `1 failed, 1645 passed, 1 skipped, 1 xfailed in 206.85s` (Git Bash); `-m day10` `1 failed, 61 passed, 1586 deselected in 39.76s` (PowerShell); `python scripts/mutation_sweep.py --marker day10` prints `baseline (no mutant) does not pass -m day10; refusing to sweep` and exits 2. CI (Linux, LF checkout) will show green; a Windows clone, `git stash`/`checkout` that rewrites these files, or the S4 lane vendoring a later pin on Windows will show red. This is the split the working agreement warns about.
- **Repro (one line, from any fresh checkout on this machine):** `git -C C:/Users/joshs/GPS/ProofPack/proofpack worktree add --detach X 322c5a4 && PYTHONPATH=X/src python -m pytest -q -p no:cacheprovider "X/tests/test_e10_t2.py::test_golden_t2_matches_the_committed_render"` -> `1 failed`.
- **Counter-example that the rest of the golden is right.** With the three F5 files LF-normalised in the worktree, `proofpack compare --offline` on F5 through the CLI under the test licence (exit 0, `run.json` + `T2.html` + the three JSON files, 39 claims, 0 rejections), the manifest masked at **only** `run_id`, `started`, `duration_s`, rendered from `run.json`: 0 differing lines against the golden. The nine-key mask is over-broad but harmless: `mapping_sha256`, `numpy`, `scipy`, `python`, `platform`, `reference_platform` and `duration_s` are not printed on T2 (string search on the page: absent); only `run_id` and `started` reach it. The CLI's own `T2.html` is 61,067 bytes against the golden's 61,048: the 19-byte difference is a 36-character run_id against the 17-character mask `test-only-compare`.
- **Repair shape (the builder's choice, then a lens, DEC-12 (i)):** a `.gitattributes` line pinning `tests/fixtures/f5/* text eol=lf` (and every other committed fixture whose bytes are hashed into a golden), or mask the two hash keys in the golden test, or hash the analysed table rather than the file bytes. The regression test must fail against the old code on a CRLF copy of the fixtures, not only on this machine's main tree.

## Non-blocking records

| # | Record | Evidence |
|---|---|---|
| N1 | **The schema change is not strictly additive.** `comparison` moved from `{"type": ["object", "null"]}` to `oneOf [null, $defs/comparisonBlock]`. Counter-example: `{"comparison": {"x": 1}}` on an otherwise valid document validates at `81f1102` and not at `322c5a4`. No engine version wrote such a document: `81f1102`'s sample-pack `run.json` (`proofpack-site/dist/sample-pack/run.json`) has no `comparison` key; `322c5a4`'s `run` omits it and `compare` writes the typed block. Everything else in the four schema files is an enum extension or a new `$defs` block; nothing was removed. The sample pack's `run.json` and `compare.json` built at `322c5a4` validate against `output_schema_v1.json`. | `git diff -U2 81f1102 322c5a4 -- schema/` |
| N2 | **Sentence violation in the note (hard rule).** "Every other E9 row (98-119, 122-131) is as the r3 note left it; E10 touched none of their code paths on `run` documents except the criteria table's new column." False as written: the diff changes `narrate/templates.py` (`CRITERION_STATUS` gains `{type_clause}` - every run document's criteria sentence passes through it), `render/sentences.py` (`render_parts(value_is_difference=)`), `render/t7.py` (`METHOD_DESCRIPTIONS` +2), `narrate/checker.py` (`POINTER_SHAPES` 12, `SCALAR_SLOTS`), `narrate/claims.py`, `stats/number.py` (`METHODS`, `FLAGS`), `render/html.py`, `render/t1.py`, `render/t12.py`, `templates/T1.html`, `templates/T12.html`, `templates/_criteria.html`. What the tests pin is that the **rendered output** of point criteria is unchanged (the T1/T8 goldens; `test_a_point_criterion_prints_type_point_and_no_prior_phrase`; T7 method counts in the E9 re-run block). The sentence should name those tests, not the class of code paths. | `git diff --name-status 81f1102 322c5a4 -- src/` |
| N3 | **Note figure: "seven allow-listed lines"** in the no-margin grep. `ALLOWED` in `tests/test_e10_criterion.py` holds nine entries (six `fixtures.py`, one `figures.py`, two `bootstrap.py`). The grep itself gives zero hits outside them (the test passed in both shells). | `tests/test_e10_criterion.py` lines 279-296 |
| N4 | **Note figure: diff stat.** Note: 57 files, +5,928 / -176. Measured `git diff --shortstat 81f1102 322c5a4`: 57 files, +5,930 / -178. | |
| N5 | **The 15-byte T2 difference in the timing row is not a shell difference.** Commit `322c5a4` changed the criteria-table caption from `a paired difference vs prior version` to `a paired difference against the prior model version` (+15 bytes) after the builder measured item 7 at `6887e4a`. At `322c5a4` the timing test prints `T2 73102 bytes` in both shells (Git Bash `23.33 s`, PowerShell `22.39 s`; render `0.03 s`). The note's Git Bash cell (`73087`) is stale, not wrong for its commit. | `git show 322c5a4 -- src/proofpack/templates/_criteria.html` |
| N6 | **Sample-pack byte sizes are per-run, not per-shell.** Note: Git Bash `run.json 236,843` / `compare.json 328,963`, PowerShell `236,842` / `328,965`. Mine: `236,842` / `328,965` in **both** shells; T1 142,924, T7 47,666, T8 35,178, T2 68,905 in both. Comparing my Git Bash and PowerShell `run.json` / `compare.json` as JSON: only `manifest.duration_s`, `run_id`, `started` differ; every other key is identical. The digit count of `duration_s` moves the size by a byte or two. The note should say the two JSON files are not byte-stable across runs rather than tabulate per-shell sizes. | `scratchpad/lens-E10-r1-regression/sp_bash`, `sp_ps` |
| N7 | **Note wording: the site scripts.** "`proofpack-site` at `4775dd8` greps for none of `compare`, `T2`, `comparison`, `paired_difference`". At the site's current `1fc20e6`: `comparison` occurs in five script comments (`build-wheel.mjs:265`, `check-control-bytes.mjs:124`, `check-egress.mjs:247`, `check-tokens.mjs:17,19`), none an engine-contract dependency; `\bT2\b`, `paired_difference`, `proofpack compare`: 0 hits. The site pins `81f1102` in `ci.yml`, `deploy.yml`, `dist/demo/sample.json` and `dist/sample-pack/provenance.json` (read only). | `grep -rnE` over `proofpack-site/scripts/` |
| N8 | **Ruff file count in the main tree during the run: 242, not 241.** The E9 r3 PowerShell re-run block differs from the orchestrator's capture in one line (`242 files already formatted`); the worktree at the same sha gives `241` in both shells and `git ls-files` shows 140 tracked `.py` files. The extra file is a transient of the sibling fresh-attack lens's concurrent session (not found by `git status --ignored` afterwards), not a change in `322c5a4`. | `scratchpad/lens-E10-r1-regression/rerun_ps_lens.txt` |

## What I could not break (re-measured, with deviations)

**Suite, markers, lint (worktree, after LF-normalising the three F5 files; before it, B1).** Git Bash: `1646 passed, 1 skipped, 1 xfailed` (the CRLF run: `1 failed, 1645 passed ... in 206.85s`). PowerShell: `1646 passed, 1 skipped, 1 xfailed in 166.96s (0:02:46)`; `-m day10` `62 passed, 1586 deselected in 37.90s`. `--collect-only`: day10 62, day9 328, day8 470, day7 139, day6 285, day5 54, ap2 90, ap3 167 of 1648 - the note's figures exactly. `-m day9` `328 passed`, `-m day8` `470 passed`, `-m ap3` `167 passed`, `-m ap2` `90 passed` (Git Bash). `ruff check .` `All checks passed!`; `ruff format --check .` `241 files already formatted` (both shells, worktree). 1646 - 1582 = 64 = 62 `day10` + the two `PAIRED_DIFF` cases in `test_e9_templates.py`. `day10` is in CI: `ci.yml`'s marker loop reads every `day\d+` marker out of `pyproject.toml`, where `day10` is declared with E10's text.

**Nothing weakened.** `git diff --name-status 81f1102 322c5a4 -- tests/`: 13 `A`, 12 `M`, 0 `D`. No `skip`, `skipif`, `xfail`, `.only`, `TODO` added; no `pytest.mark.` line removed. The twelve modified test files change counts (`43 -> 46` rows, `30 -> 33` matched, `64 -> 71` captured, `18 -> 22` iterative, `19 -> 18` unbound templates, `62 -> 64` skeleton variants), shift T8 cell indices by the new Type column (`cells[3] == "point"`), add `confirmed_mapping` and `ephemeral_registry` to the H12 CLI test (which now also asserts `run.json` exists), and add one offline test; none drops an assertion.

**Fails pre-build (`81f1102` worktree, the new and modified test files copied in).** Five new modules abort at collection: `test_e10_comparison.py` (`cannot import name 'comparison' from 'proofpack.stats'`), `test_e10_criterion.py` (`assemble_compare`), `test_e10_sample_pack.py`, `test_e10_t2.py`, `test_e10_timing.py` (`proofpack.render.t2`). The fifteen runnable ones fail on their assertions: `test_sweep_day10.py` (2), `test_offline.py::test_compare_with_t2_offline_opens_no_socket`, `test_e9_templates.py` (4: the two new `PAIRED_DIFF` cases, `ledger_statement`'s new slot names, the variant count), `test_render_t8.py::test_twenty_printed_cells...`, `test_halt_gates.py::test_h12_unmatched_row_ids`, `test_e8_repair2.py::test_the_lens_b2_literal_claims...`, `test_fixtures_cmd.py` (4), `test_t12.py::test_every_report_row_appears_in_t12` - `15 failed, 160 passed`. Because the import aborts say nothing about behaviour, two probes: (a) **row 121** - `scratchpad/lens-E10-r1-regression/probe_row121.py` (lens 5's `Cpd` input on the thirty-row-site cohort through `assemble` + `render_t1`, no E10 import): at `81f1102` `phrase count: 0`, `Type column header: False`, exit 1; at `322c5a4` `phrase count: 2`, `True`, exit 0 (`PYTHONIOENCODING=utf-8`); (b) **the acceptance case** - `proofpack compare` at `81f1102` on the F5 files writes only `compare_ingest_report.json` and prints `Next step: paired statistics land on the comparison build day.`; no `run.json`, no `criteria_results`, so C2 has no status there at all.

**F5 by hand from the CSVs (no engine).** 100 rows, `y_true` 50/50, `sex` 50/50, threshold `>= 0.5`: both correct 80, prior-only correct (b) 10, new-only correct (c) 2, neither 8; accuracy new 0.82, prior 0.90, difference -0.08. `criteria.yaml` carries `C2: type paired_difference_vs_prior, accuracy, ci_lower_bound >= -0.05` with author, date and justification, and `C1` a point criterion - the margin is in the fixture only.

**McNemar against statsmodels 0.15.0 (my own call, no engine).** `mcnemar([[80, 10], [2, 8]], exact=True).pvalue` = `0.03857421875` = `2 * P(X <= 2 | Bin(12, 1/2))` computed with `math.comb`: deviation `0.0`; the engine's `mcnemar(10, 2).p` = the same to `0.0`. `exact=False, correction=True`: statistic `4.083333333333333` (hand `(|10 - 2| - 1)^2 / 12`, deviation `0.0`), p `0.04330814281079206`; the engine's `0.04330814281079198` via `erfc` deviates by `-7.6e-17`. Routes: `b + c = 24` exact (statsmodels p `0.5413`), `25` corrected (`0.64`, p `0.4237`), both equal to the engine's to 1e-6 (the test) and `EXACT_BELOW == 25`. The captured oracle `F5-mcnemar` in `fixtures/oracles_v1.json` holds these three statsmodels values; the fixtures report row is `matched`, 3 values, max deviation `7.6e-17`.

**Newcombe paired - NOT an oracle.** I wrote the method-10 formula from my own recollection (Wilson intervals of p1 and p2, `phi = (eh - fg) / sqrt((e+f)(g+h)(e+g)(f+h))` with `eh - fg` replaced by `max(eh - fg - n/2, 0)` when positive, `L = d - sqrt(a^2 - 2 phi a b + b^2)`, `U` by symmetry) with my own Wilson bounds, no `proofpack` import: est `-0.08`, lo `-0.15535638110228397`, hi `-0.010249291949335146`, `phi 0.4946`. The engine's `difference_paired(80, 2, 10, 8)`: `-0.1553563811022839` / `-0.010249291949335215` (deviations `8.3e-17` / `-6.9e-17`), method `newcombe_paired`, n 100. Two recollections agreeing is not verification: the paper was not fetched (a download needs Josh, need 35), and the fixtures report correctly says so - row `F5-newcombe-paired` is `no_independent_oracle`, `matched false`, `n_values_compared 0`, reason `[unverified] the value is the engine's own from the formula as recalled from Newcombe 1998 ...`; the register row `F5-register` compares only `accuracy_diff` (`F5_REGISTER_NAMES`), not the interval; R2's `(-0.1461, -0.0139)` is the Wald interval, which I reproduce by hand as `(-0.14606, -0.01394)` - a different interval, not printed by the engine. The frozen four-decimal pin `(-0.1554, -0.0102)` is what the test asserts.

**Paired DeLong on R2's F3 pair (my own O(m n) components, explicit covariance sums, no engine).** AUC1 `0.80`, AUC2 `0.64`, `S = [[0.024, 0.028], [0.028, 0.0392]]` (`S10/m + S01/n`), `Var(diff) = 0.024 + 0.0392 - 2 * 0.028 = 0.0072`, `z 1.8856180831641263`, `p 0.05934644`, Wald CI `(-0.0063084589, 0.3263084589)`. The engine's `paired_delong`: est `0.15999999999999992` (deviation `-1.1e-16`), variance `0.007200000000000005` (`0.0`), lo/hi deviations `-8.3e-17` / `-1.7e-16`, `z 1.8856180831641252`, `p 0.0593464387919201`, method `delong_wald`, `n_pos 5, n_neg 5`. Without the covariance term the variance would be `0.0632`, so the term is exercised. Captured oracle `F5-delong-pair` (scikit-learn AUCs + the script's hand variance): report row `matched`, 4 values, max deviation `2.5e-16`. `capture_fixture_oracles.py --check`: `72 identical, 0 differ`, exit 0.

**Fixtures command, both shells.** `rows 46: matched 33, not matched 0, no oracle recorded 2, no independent oracle 1, not built 5, compared by the test suite only 5`, exit 0; the summary object is identical in both shells. `doctor --offline` exit 0 both shells. `tr39_subset.py --check`: `vendored subset matches`. `mutation_sweep.py --list`: 345 lines (16 ap2, 39 ap3, 14 day10, 34 day5, 112 day6, 25 day7, 87 day8, 18 day9).

**Sweep (after LF normalisation).** Git Bash `14 planted, 14 killed, 0 survived; 145 s`; PowerShell `14 planted, 14 killed, 0 survived; 139 s`. The 14 mutants plant on `comparison.py` (b/c swap, `EXACT_BELOW`, continuity correction, sign, label), `proportions.py` (the phi term's sign), `discrimination.py` (the covariance term), `criteria.py` (comparator, unpaired criterion), `run.py` (H12 flag), `t2.py` (record string, margin column), `checker.py`, `claims.py`. `git diff --name-status 81f1102 322c5a4 -- src/proofpack/stats/proportions.py src/proofpack/stats/discrimination.py src/proofpack/stats/bootstrap.py src/proofpack/gates.py` is empty: no existing interval function moved; `number.py` gained two methods, one flag and one reason (enums, no arithmetic).

**CLI behaviour outside pytest (worktree, `--offline` on every call, telemetry never attempted).** H12 on a flipped label in row 1 of the prior: exit 3, `detail: {"n_pairs": 99, "new_only": 0, "prior_only": 0, "label_mismatch": 1}`, `No document was written.`, no output directory. No licence file: exit 4, `run.json` + the three JSON files, no `T2.html`, `"watermark": "NO LICENCE - not for submission"` (DEC-48). `compare --templates T1`: `error: unknown --templates id 'T1'; choose from T2, T7, T8`, exit 5, nothing written. `test_offline.py::test_compare_with_t2_offline_opens_no_socket` (the socket entry points refused, with and without `--offline`) passed in the suite.

**T2 page (golden and sample pack).** Anchors on the golden: ten `FDA_PCCP_*` ids and `PP_SCOPE`, each followed by `(2024-12-04, updated 2025-08-18, final)` (34 occurrences of the date label), and two `FDA_AIDSF_*` ids (`SUBGROUP_PERF`, `PERF_VALIDATION`), each followed by `draft guidance (January 2025), not for implementation` - the label from `design/guidance_map_v1.csv`'s row (`2025-01-07, draft - not for implementation`), and the same in the reference list. `record of criterion not met (PCCP §VII.B(3) requires recording)` once (C2's row); `not like-for-like` 0 times on the paired golden; the words `non-inferior, equivalent, superior, improved, degraded, regressed, better, worse, significant`: 0 on the golden, 0 in `templates/T2.html`; no `certif`, `approv`, `endors`, `clear(ed|ance)` in the template. `design partner`: 0 in the diff. Sample pack `--compare` (both shells): `compare.json` validates, `data_marking: SYNTHETIC - illustrative`, seven `SYNTHETIC - illustrative` marks on `T2.html` plus one inside the echoed enum.

**E9 r3 re-run blocks, main tree at `322c5a4`.** `bash <scratch9>/rerun/rerun.sh e10lens`: 243 lines, identical to the orchestrator's `e10_rerun_bash.txt` after stripping run ids, temp names and the worktree tag. `rerun_r3h.ps1`: 68 lines, identical to `e10_rerun_ps.txt` except N8.

## What I could not check

- **Newcombe 1998 itself.** No download; the formula is a recollection on both sides (need 35 stands). The row is labelled honestly; that is all a lens can say.
- **The `run --templates T2` typed line and the licence-without-`compare`-feature exit outside pytest.** The CLI's default registry does not trust the test licence, so those paths exit 4 at the licence check before the line prints; they are pinned by `tests/test_e10_compare_cli.py` (passed, 13 tests) and I did not re-derive them by hand.
- **PowerShell `--collect-only` counts**: interpreter-only, as the note says; not repeated.
- **The hash-of-bytes exposure in other goldens.** T1/T7/T8/T12 goldens build their tables in the test (`write_csv`), so the same defect cannot reach them today; I did not audit every committed fixture file for a future golden.
- **The `comparison` schema tightening against documents I do not have** (N1): I validated the sample pack (`run` and `compare`), the F5 compare `run.json` and the `81f1102` site sample pack's key set only.

## Sentences I refused to write

- "The golden is regenerated identically by the renderer at the commit." Refused as stated: it is identical only when the fixture bytes are LF (B1).
- "Masking touches only run_id, started and duration_s." Written instead: the test masks nine keys; only `run_id` and `started` reach the page; the two input hashes are not masked and leak the checkout.
- "The Newcombe paired interval is verified." Refused: two recollections, no source.
- "The schema change is additive." Refused (N1); written as what was added, what was tightened, and the counter-example.
- "E10 touched none of the E9 rows' code paths." Refused (N2).

## Files

- This note: `C:/Users/joshs/GPS/ProofPack/proofpack/handoffs/2026-09-25_E_lens1_regression.md`.
- Probes and captures: `C:/Users/joshs/AppData/Local/Temp/claude/C--Users-joshs-GPS-ProofPack/0e5d0ccc-8634-4ba1-aad8-15203d8e0928/scratchpad/lens-E10-r1-regression/` (`probe_row121.py`, `rederive.py` + `rederive.json`, `golden_regen.py`, `full_suite_bash.txt` (the CRLF run), `day10_ps.txt` (the CRLF run), `markers_bash.txt`, `sweep_day10_bash.txt`, `sweep_list.txt`, `fx_bash/`, `fx_ps/`, `sp_bash/`, `sp_ps/`, `rerun_bash_lens.txt`, `rerun_ps_lens.txt`). Worktrees `wt322` and `wt811` removed; `git worktree list` shows none under this directory.
