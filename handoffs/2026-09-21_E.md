# Lane E — build day 7 — 2026-09-21

**The third fresh-attack lens returned `PASS` on `2cd7e16` (0 blockers, 9 non-blocking, none of
them a sentence a counter-example falsifies) and its regression twin `PASS` (0 blockers, 5
non-blocking, three of them sentence-precision items), after a round-1 `FAIL` / `FAIL` whose two
blockers (a `point_estimate` criterion compared on a Number that has no interval, found by both
lenses; `met` beside `attainable_at_n: false` on a cluster-bootstrap cell) each have a test
measured failing on the pre-fix tree and were re-executed by the round-2 lenses on the
constructions that raised them, and a round-2 `PASS` / `PASS` that still found three shipped
sentence items false (the attainability annotation at n = 0, the F17 key list, the
`expires_unparsable` detail). Forty-three items from today's three rounds are carried — ten
behaviours with no observer in the suite, five imprecise sentences or records, and twenty-eight
design, policy and reachability items — each with a one-line repro below, four of them
decisions for you (needs-from-Josh 5–7) beside four build decisions that need your reading
(needs 1–4); twenty-nine of the E6 note's thirty-six items remain open under their old numbers
(five closed today, two closed by DEC-34 / DEC-35).**

Four commits on `main` on top of `a0c9abc` (lane A's mapper merge), none pushed by this lane:
`27e6bbc` (carried item 25 alone), `ab729d3` (the E7 build), `02d00c5` (repair round 1),
`2cd7e16` (repair round 2). `origin/main` stands at `27e6bbc`, so `git status -sb` reads
`ahead 3` now; the commit carrying this note and the two lens-3 notes makes it 4. Suite at
`2cd7e16`, measured now in both shells: **788 passed / 1 skipped / 1 xfailed** (Git Bash 55.54 s,
PowerShell 54.74 s); `-m day7` **139 passed** (4 collected in `tests/test_bootstrap_carried.py`,
56 in `test_criteria.py`, 8 in `test_ledger.py`, 43 in `test_licence.py`, 5 in
`test_manifest.py`, 23 in `test_run_cli.py`, counted now with `--collect-only`; the marker count
includes parametrisations); `-m day6` 285; `-m day5` 54; `-m day1`..`day4` 59+1 / 40 / 29+1 /
182. The `a0c9abc` baseline was 649 / 1 / 1 (lens-1 regression note, worktree), so the
difference is exactly the 139 day-7 tests. Lint, format and doctor clean in both shells (`ruff
format --check` counts 110 files with the two untracked lens-3 notes in the tree; 108 at
`2cd7e16` clean). `git diff --name-status a0c9abc HEAD -- tests/` is 7 `A` and 13 `M`, no `D`
(measured now); the added test lines contain no `xfail` or `.only` and one `skipif` (carried item
36); no `pytest.mark` line was removed. `git diff --stat a0c9abc HEAD -- src/proofpack/stats/`
touches six files, 146 insertions and 7 deletions, not only the item-25 branch of `bootstrap.py`
("Not carried" below says which and why): lens 3 assembled eight documents with each tree's own
`tests/assembler.py` at `a0c9abc` and `2cd7e16` and diffed them path by path — **0 values
differ** on the seven `a0c9abc` can build; the one changed value is the ROC origin threshold
`inf` → `null`. The three `lens-S7-*` / `s7-engine` worktrees registered on this repo at
`a0c9abc` belong to today's site workflow and were left alone.

---

## What landed

Every item of `spec/briefs/day7_E.md` is built, plus the E6 note's "Tomorrow needs" 2–5
(carried item 25 first; items 26, 27, 28, 30 inside `cmd_run`; the verdict-grep twin; F8 on the
day-5/6 Numbers as they are). The build commit is `ab729d3`; the repair commits are named where
they changed an item. Figures marked "now" were measured in this session in the main tree
(`proofpack.__file__` printed as the main tree's first in each shell); the rest are quoted from
the note named.

| # | Item | Module / function | Commit | Test ids (marker `day7`) | Oracle or literal input, and the measured figure |
|---|---|---|---|---|---|
| 1 | Carried item 25: `resample_sd` was `inf`. The non-finite branch of day-4 `bootstrap_percentile` now gives `resample_sd: null` with the typed reason `resample_sd_not_finite` beside it; the percentile bounds are untouched | `stats/bootstrap.py`: `_resample_sd` (new), `RESAMPLE_SD_NOT_FINITE`, `BootstrapDraw.sd_reason`, `CellCI.resample_sd_reason`; eight `CellCI` keyword pass-throughs (3 in `bootstrap.py`, 3 in `subgroups.py`, 2 in `calibration.py`) | `27e6bbc` | `tests/test_bootstrap_carried.py` (4): `test_item25_resample_sd_is_none_with_the_reason_and_the_block_serialises` and its three siblings | Now, the 57-plus-3 input (57 rows at `1e-160` beside 3 at `0.5`, 20 events, 30 two-row declared cases, B = 200, seed 20240101): O:E `est 13.333333333333334`, `ci (4.0, 3.999999999999999e+159)`, `method cluster_bootstrap_percentile`, `resample_sd None`, `resample_sd_reason resample_sd_not_finite`, `json.dumps(block, allow_nan=False)` succeeds (24,164 chars). At `a0c9abc` (worktree, `PYTHONPATH` forced) the test fails on `E       AssertionError: inf` / `E       assert inf is None` — measured by the build session and re-measured by all three regression lenses. The rest of the block equals the `a0c9abc` snapshot (`tests/fixtures/item25_oe_block_a0c9abc.json`) once the two sd keys are stripped |
| 2 | Criteria engine: one row per criterion (one per level under `level: "*"`, one per non-reference level for the fairness bound), exactly one of `met` / `not_met` / `not_assessable` with a `reason_code` from a closed set of 14; bounds, margins and comparators read from `criteria.yaml` only | `src/proofpack/criteria.py` (package root, not `stats`: it reads the assembled document by path and computes no statistic): `evaluate`, `_reads_for`, `_resolve_overall` / `_resolve_subgroup` / `_resolve_gap`, `_row`, `_fairness_rows`; `REASON_CODES`, `STATUSES`, `STATISTICS`, `COMPARATORS` | `ab729d3`; `02d00c5` (B1: a Number with `not_estimable_reason` set or `method none` is routed to `not_assessable` / `no_interval` *before* any statistic is read); `2cd7e16` (the `method_not_wilson` annotation no longer depends on `n`) | `tests/test_criteria.py` (56 collected): the twelve literal-reading cases; `test_a_number_with_method_none_is_not_assessable_under_each_of_the_three_statistics` (4 params); `test_a_method_none_number_at_n_zero_or_n_null_is_annotated_method_not_wilson` (2 params); H08 / H09 cases; `tests/test_run_cli.py::test_point_estimate_criteria_on_f1_and_mcc_read_method_none_as_not_assessable` | Hand-built Number `est 0.90, ci (0.82, 0.95)`: `ci_lower_bound >= 0.82` met, `>= 0.8201` not met, `> 0.82` not met, `ci_upper_bound <= 0.95` met, `ci_lower_bound <= 0.83` met (the lower end, literally). Now, the lens-1 one-liner (hand-built `est 0.9, method none, single_class, n 100` under `point_estimate >= 0.95`) prints `not_assessable no_interval {'not_estimable_reason': 'single_class'} None None`; at `ab729d3` it printed `not_met`. The synthetic run's `f1 point_estimate >= 0.70` and `mcc point_estimate >= 0.40` rows: `not_assessable` / `no_interval` / `analytic_ci_unavailable`, the Numbers' `est` asserted `0.6620689655172414` and `0.4821183187439812` at 1e-12 (repair 1) |
| 3 | `stats.attainability`: `max_lower_bound_at_n = wilson_bounds(n, n, level)[0]` (never a second Wilson) and `attainable_at_n`, filled on a `ci_lower_bound` criterion over a proportion metric whose Number is `method wilson` with an `int` `n > 0`; every other method gets `null` + `detail.attainability_not_computed: method_not_wilson` | `src/proofpack/stats/attainability.py`: `max_lower_bound_at_n`, `attainable`, `PROPORTION_METRICS`; `criteria.ATTAINABILITY_METHOD = "wilson"` | `ab729d3`; `02d00c5` (B2: `wilson` only); `2cd7e16` (annotation whatever `n`) | `tests/test_criteria.py::test_the_hand_figures_at_30_50_200`, the attainability block (17 incl. parametrisations), `test_a_cluster_bootstrap_cell_gets_no_attainability_flag`, `test_b2_the_clustered_s3_cell_is_met_with_no_attainability_flag_in_the_assembled_document`; `tests/test_run_cli.py::test_b2_a_met_clustered_cell_carries_no_attainability_flag_through_run`; the invariant helper `assert_no_met_row_is_marked_unattainable` over the four documents the tests build | Now, against a Wilson lower bound at p = 1 written in this session with `z = NormalDist().inv_cdf(0.975)`: n = 30 engine `0.8864866068260313`, hand `0.8864866068260314` (1.11e-16); n = 50 `0.9286524008666414` (0.0); n = 200 `0.9811546736227335` (0.0); the closed form `n / (n + z^2)` 0.0 at each. **F8 now, both shells:** `C_n30` (accuracy at S3 = 30 rows, `ci_lower_bound >= 0.90`) → `not_met`, `n 30`, `attainable_at_n False`, `max_lower_bound_at_n 0.8864866068260313`, `compared_value 0.5212421254128505`, `method wilson`; `C_met` → `met`, `True`, `0.9708630437264916`. The lens-1 B2 construction (S3 = 30 negatives in 15 two-row cases, one wrong row) → `met`, `compared_value 0.9`, both fields `null`, `detail {attainability_not_computed: method_not_wilson}` (lens 2 and 3 re-ran it) |
| 4 | `io.ledger`: a per-user `ledger.json` keyed by SHA-256 of the analysed `y_true` strings + `\0` + the little-endian float64 `score` bytes, incremented once per `run` whose declarations carry a `criteria` list; W14 past the customer's `warn_after_acceptance_runs`; W15 when the file cannot be read or written | `src/proofpack/io/ledger.py`: `test_set_key`, `record_run`, `LedgerResult`, `ledger_path`; `src/proofpack/home.py`; `errors.py` W14 / W15 | `ab729d3` | `tests/test_ledger.py` (8); `tests/test_run_cli.py` (W14 at the third run with limit 2 → exit 2) | The key recomputed with `hashlib` from the documented bytes; counts 1, 2, 3 then `W14 {count: 4, limit: 3}` at the fourth; no `ledger` block → 50 runs, no warning; W15 on `not json`, a negative count, a directory in the file's place. Now: the home directory holds `ledger.json` and nothing else; the document's `ledger` block reads `{acceptance_runs 1, counted True, test_set_sha256 b9fdad3b…0252, warn_limit 3}` in both shells; two runs in one process read 1 then 2 |
| 5 | Manifest and canonical JSON (F17): `input_sha256`, `criteria_sha256`, `mapping_sha256` (DEC-27: the file's bytes), `engine_version`, `python`, `numpy`, `scipy`, `platform`, `reference_platform`, `seed`, `B`, `run_id`, `started`, `duration_s`, `ledger_count`, `licence_id`, `tier`, `watermark`; `run.json` written as `sort_keys, indent 1, ensure_ascii False, allow_nan False`, one trailing `\n`, UTF-8 without BOM | `src/proofpack/manifest.py`: `build_manifest`, `canonical_json`, `platform_tag`, `sha256_file`, `REFERENCE_PLATFORM = "linux-x86_64-cp312"`, `VOLATILE_KEYS`, `HISTORY_KEYS` | `ab729d3`; `02d00c5` and `2cd7e16` (the F17 sentences rewritten to what the test asserts) | `tests/test_manifest.py` (5): `test_f17_two_runs_in_one_process_differ_only_in_run_id_started_duration_s_and_the_ledger_count` and four on `canonical_json` / hashes | Now, two `proofpack run` in one process on the same inputs: manifest keys that differ `['duration_s', 'ledger_count', 'run_id', 'started']`, `ledger.acceptance_runs` 1 → 2, top-level blocks that differ `['ledger', 'manifest']`, the three SHA-256 fields equal, byte-identical after those five are blanked; each SHA-256 equals `hashlib.sha256(file bytes)` of `test.csv`, `criteria.yaml`, `test.csv.mapping.json`; the written `run.json` equals `canonical_json` of its own parse byte for byte; `platform win-amd64-cp314`, `reference_platform False`. Across shells (one empty home each): `run.json` 216,303 bytes in both, no CRLF, no BOM, differing in `duration_s`, `run_id`, `started` only, 0 top-level blocks differ once those are blanked, the two `ingest_report.json` byte-identical |
| 6 | Licence package: Ed25519 verify of `<base64 payload>.<base64 signature>` against the shipped public key (`pp-2026-09`) with a typed result from a closed set of 14 reason codes; trial (30 days from issue), quarterly, annual; 24 h clock skew; grace watermark; `licence show` / `verify` / `install`; `doctor` verifies the file it finds | `src/proofpack/licence/keys.py` (`SHIPPED_PUBLIC_KEY_B64`, `PUBLISHED_PUBLIC_KEY_B64`, `SHIPPED_KEY_ID`, `decode_canonical_base64`, `KeyRegistry`), `verify.py` (`verify`, `LicenceResult`, `parse_utc`, `REASON_CODES`, `CLOCK_SKEW`, `TRIAL_DAYS`), `__init__.py` (`installed_path`, `resolve`, `install`); `cli.py` `cmd_licence`; `doctor.py` | `ab729d3`; `02d00c5` (three `OverflowError` sites turned into `expires_unparsable` refusals); `2cd7e16` (docstring names the literal inputs) | `tests/test_licence.py` (43 collected): **`test_f20`** (8 params: `valid_annual`, `valid_quarterly`, `trial`, `expired_within_grace`, `expired_past_grace`, `tampered_payload`, `wrong_key`, `unknown_key_id`), `test_the_four_spellings_of_the_key_check_lesson`, `test_extreme_dates_and_grace_days_are_refused_expires_unparsable` (6 params), `test_random_and_hostile_bytes_never_raise`, `test_the_site_reference_verifier_agrees_on_the_same_file`, `test_the_shipped_key_is_the_site_s_live_key_by_bytes_and_the_published_copy_is_the_same`, `test_import_proofpack_with_cryptography_hidden_still_imports`, `test_no_private_key_material_is_in_the_package` | F20 as the brief lists it: valid → `ok`; trial → `ok` + `TRIAL`; expired 10 d → `grace` + `LICENCE EXPIRED - not for submission`; expired 40 d → `expired` + watermark; tampered payload / ephemeral signature under the shipped `key_id` / wrong key → `refused signature_invalid`; ephemeral `key_id` on the shipped registry → `refused unknown_key_id`. Now: the shipped literal equals `proofpack-site/keys/licence_pub.b64` and `licence_pub.json` (`key_id pp-2026-09`, `status live`), 32 bytes, hex `5d82e76854675953ac2e86f04a691ba3fab2dfb6a73b36be691a6e9f39ab3000`; with `cryptography` (six module names), `scipy`, `statsmodels` and `sklearn` set to `None` in `sys.modules`, `proofpack`, `.stats`, `.licence`, `.licence.verify`, `.criteria`, `.manifest`, `.run`, `.cli` import and `verify(b"abc")` → `refused not_two_segments`; `grep` of `src/` for `PRIVATE KEY`, `-----BEGIN`, `Ed25519PrivateKey`, `importSigningKey`, `LICENCE_SIGNING_KEY` hits one docstring line naming the site's secret *by name* and its `.pyc`. Lens 1 regression: three licences issued by the site's own `functions/_lib/licence.mjs` under an ephemeral WebCrypto seed verify `ok` / `ok` + `TRIAL` (`days_left 29`) / `grace` |
| 7 | `cmd_run` wiring (DEC-26; E6 items 26, 27, 28, 30): the full document assembled and written; H07 `run proofpack map first` without a confirmed `--mapping FILE` or `<input>.mapping.json` beside the input (every prior through lane A's `check_h07` `--yes` rule); S05 on a blank `case_id`; `flow.dev_rows`; y-pred-only → `calibration null` + `no_score_column`; exit 4 with `run.json` and the watermark when the licence is refused or expired past grace | `src/proofpack/run.py` (`assemble_run`, `write_run`, `resolve_mapping_path`, `default_mapping_path`, `overall_block`, `RunOutcome.exit_code`); `cli.py` `cmd_run`, `main(argv, *, registry=None)`; `io/schema.py` S05; `stats/descriptive.py` `dev_rows`; `stats/calibration.py` `no_score_column`; `stats/discrimination.py` (ROC origin threshold `inf` → `None`) | `ab729d3` | `tests/test_run_cli.py` (23 collected): the full synthetic run, H07 ×3, H08 / H09 exit 3 with no directory, `--offline` with `socket.socket` and `create_connection` raising, no-licence / expired / grace / trial / garbage-file exits, S05, y-pred-only, dev rows, clustered → `overall null`, `test_assemble_run_writes_only_the_ledger_file_under_home`, the subprocess run | Now, both shells, the e2e fixture (400 rows, S3 = 30, a `file`-confirmed `test.csv.mapping.json`, an empty `PROOFPACK_HOME`): the five lines under "Re-run these", `exit=4`, the pack directory holding `run.json` and `ingest_report.json` only; the nine criteria rows `2 met, 2 not met, 5 not assessable`; without the mapping file `HALT H07: run proofpack map first: no confirmed mapping.json was given (--mapping FILE) and none exists beside the input as <input>.mapping.json`, `exit=3`, nothing written; `flow {analysed 400, dev_rows 0, rows_read 400, n_sites 3, clustering_route none, …}`. Lens 3: `""`, `NA`, `null`, `nan`, `NaN`, `N/A`, `None`, `na`, a space → `HALT S05: case_id is blank in 1 row(s); fill every case id or drop the column`, exit 3; 50 `dev` rows → `dev_rows 50`, `analysed 350`, the flow sums to `rows_read`. Now: `socket.socket`, `create_connection`, `getaddrinfo`, `urlopen` replaced with raisers → `run` with and without `--offline` completes with 0 calls; 0 network imports under `src/proofpack` |
| 8 | Schema: 17 top-level `required` blocks (item 30); `manifest` closed with 18 required keys; `criterionResult` (closed; status and reason enums; a `met` / `not_met` row must carry `statistic_compared` and a numeric `compared_value`); `ledger` block; `calibrationSuppressedReason` + `no_score_column`; `flow.dev_rows`; `criteria_schema.json` gains `ci_upper_bound` and the fairness `statistic` / `comparator` `if` / `then`; no `default` keyword anywhere | `schema/output_schema_v1.json`, `schema/criteria_schema.json` | `ab729d3` (`02d00c5`, `2cd7e16`: descriptions only — 0 changed `required` / `enum` / `type` lines, lens-2 and lens-3 regression) | `tests/test_criteria.py` (enum equality with the code's registers), `tests/test_run_cli.py` (every written document validates), the day-5/6 schema tests | Now: the run document carries 18 top-level keys and validates with 0 errors; lens 3: 115 `run.json` files this session, 115 distinct `run_id`s, 0 schema errors, no `NaN` / `Infinity` / BOM / CRLF byte, no `row_id` / `case_id` literal |
| 9 | Mutation sweep, day-7 list (DEC-12 ii) | `scripts/mutation_sweep.py` `MUTANTS_DAY7` | `ab729d3` (21); `02d00c5` (+3, two re-targeted); `2cd7e16` (+1, one re-targeted) | `tests/test_run_cli.py::test_the_mutation_sweep_declares_a_day7_list_with_no_duplicate_ids` | Now: `--list` 171 lines, 34 day5 + 112 day6 + 25 day7, 0 duplicate ids; `--marker day7` **25 planted, 25 killed, 0 survived** — 108 s Git Bash, 103 s PowerShell |
| 10 | The verdict grep, E7's twin: the day-5 and day-6 walks are scoped to exclude `criteria_results` by key; the new walk asserts `met` / `not_met` / `not_assessable` appear only under it, and `pass`, `fail`, `verdict` in no key or string | `tests/test_criteria.py`, `tests/test_run_cli.py`, `tests/test_subgroups.py`, `tests/test_calibration.py` | `ab729d3` | `tests/test_criteria.py::test_the_status_words_appear_only_under_criteria_results_and_no_verdict_word_anywhere`, `::test_no_reason_code_or_status_key_carries_a_verdict_word_outside_the_three_statuses`, `tests/test_run_cli.py::test_the_status_words_appear_only_under_criteria_results`, plus the two scoped day-5/6 walks | Now: the five tests `5 passed`. Lens 3's own walker over 53 `run.json` files: 0 hits on the 40 `VERDICT_WORDS` plus the brief's seven outside `criteria_results`; inside it only the three status tokens; `pass`, `fail`, `verdict`, `meets`, `acceptable`, `consistent`, `unbiased` absent as substrings; `assessable` outside the block in 0 strings |

Also in `ab729d3`: `pyproject.toml` gains `cryptography>=42` (imported inside
`proofpack.licence.verify` only; `uv.lock` re-locked, 146 added lines, nothing removed); the
`day7` marker beside `day6` (`pyproject.toml` line 73), which `.github/workflows/ci.yml` lines
30–53 read out of the file; README status and a `proofpack run` section; the ROC origin
threshold changed from `float("inf")` to `None` in day-3 `roc_curve`, its test and the schema
(`canonical_json` refuses `inf`).

**Acceptance line** (the brief: "F8 (n=30 criterion → `not_met`, `attainable_at_n: false`);
ledger count; manifest stable across runs; F20 (tampered/expired/wrong-key refused); grep finds
no pass/fail/verdict"). F8 is row 3's `C_n30` (measured now in both shells); the ledger count is
row 4's 1 → 2 in one process and `W14 {count 4, limit 3}`; F17 is row 5 with the keys that
differ named; F20 is row 6's eight parametrised cases; the grep is row 10. The brief's F8
sentence says `not_met`; the day's binding text adds that a Number whose interval is unavailable
is `not_assessable`, never `not_met`, under every statistic — row 2's B1 test feeds all three.

## What was cut and why

1. **The clustered `overall` block.** `two_by_two_metrics` and `auroc_number` take no
   `cluster_ids`; on a clustered plan `overall` is `null` (the subgroup rows carry the
   cluster-bootstrap cells) and a criterion at `scope: overall` on a clustered run is
   `not_assessable` / `overall_not_computed`. Building it from `proportion_ci` / `auroc_ci` with
   LR / DOR / F1 / MCC typed `clustered_data_analytic_ci_invalid` is about forty lines and its
   own tests: **E8's first build item**, together with the y-pred-only `overall` (carried item
   42: the operating-point metrics from `y_pred` alone are not built, so `overall` is `null`
   there too while the subgroup rows carry them).
2. **`cmd_compare`** is untouched (AST-identical to `a0c9abc` per the lens-1 regression): no
   licence check, no DEC-26 gate, day-1 ingest of both tables. **E10** (comparison) rewires it.
3. **Gap criteria on a non-fairness attribute** (`tpr_gap` on `site` when `fairness.attribute`
   is `sex`): `not_assessable` / `gap_requires_fairness_attribute` rather than re-reading
   `diff_vs_reference` with the fpr mirror — one path, the fairness block's.
4. **E6 carried items 9–13, 15 and 16** (one assertion each, the E6 note's "if time left"): not
   started. They are listed below with the other E6 items still open.

## What did not land, and why — the 43 carried items

Each reproduces on `2cd7e16` unless marked otherwise; the lens that owns the repro is named so
its note can be opened for the full construction. "Survives" means the mutant passes `-m day7`
on this tree as the lens measured it; on each, at least one lens checked the shipped behaviour by
construction and found the code right and the suite blind.

**A. Behaviours with no observer in the suite (a regression would ship green).**

| # | Item | Why carried | Repro (one line) | Owner |
|---|---|---|---|---|
| 1 | The `Unknown/missing` row included under `level: "*"`: removing the `is_unknown_row` skip in `_subgroup_rows` survives | No day-7 test runs a `level: "*"` criterion on a table with a missing attribute value; each of 1–10 needs its own observer, and the repair rounds fixed blockers and false sentences | plant the removal; `-m day7` passes | lens-1 N4 L3; lens-2 M05 |
| 2 | A wrong-length signature falling through to `ok`: `except ValueError: pass` in `verify` survives. Lens 1 measured a 63-byte, a 65-byte and an all-zero 64-byte signature each `refused signature_invalid` today | No test feeds a wrong-length signature beside an otherwise valid payload; `test_random_and_hostile_bytes_never_raise` asserts only that nothing raises | plant the `pass`; `-m day7` passes | lens-1 N4 L15; lens-2 M06 |
| 3 | `grace_days < 0` → `< -1`: a signed `grace_days: -1` is accepted by the mutant | No test feeds a signed negative `grace_days` | plant `< -1`; `-m day7` passes | lens-2 M08; lens-3 M09 |
| 4 | `has_criteria=True` in `run.py`: every run counted | No test runs `proofpack run` without a `criteria` list and reads the ledger count back (`test_ledger.py` inspects `record_run` only) | plant `True`; `-m day7` passes | lens-2 M17 |
| 5 | The fairness bound emitting a row for the Unknown level | No test has a bound on an attribute with a missing value | drop the Unknown skip in `_fairness_rows`; `-m day7` passes | lens-2 M26; lens-3 M07 |
| 6 | A named `level` matched case-insensitively (`str(r.get("level")).lower()`) | No day-7 test has two levels that differ in case only | plant `.lower()`; `-m day7` passes | lens-3 M03 |
| 7 | A `prevalence` criterion reading the `auroc` Number, and `prevalence` dropped from `PROPORTION_METRICS`. Lens 3 measured the shipped route right: `prevalence ci_lower_bound >= 0.2` → `met`, `compared_value 0.2761841236239046`, `n 400`, `wilson`, `attainable_at_n True` | No day-7 test feeds a `prevalence` criterion | plant `tf.get("auroc")`; `-m day7` passes | lens-3 M08, M19 |
| 8 | The ledger key over every row rather than the analysed rows (`table.y_true` without `mask`). Lens 3 measured the shipped route right: two tables differing only in the scores of two rows whose labels are blank give the same `test_set_sha256` and different `input_sha256` | No test runs a table with excluded rows and reads the key | plant the unmasked column; `-m day7` passes | lens-3 M18 |
| 9 | F20's tampered-signature case pins `refused`, not the reason: no test flips a bit of a valid signature and asserts `signature_invalid` (D1's F20 row lists a tampered payload, which is pinned). Lens-3 regression measured byte 0 of the signature flipped → engine `refused signature_invalid`, the site verifier `InvalidSignature` | A one-param addition; the class is asserted only at `refused` | flip one bit of a good signature; read `reason_code` | lens-3 RG N5 |
| 10 | A metric outside `PROPORTION_METRICS` under `ci_lower_bound`: the clause "or on a metric outside the set … `detail` has no attainability key (the same test)" in `stats/attainability.py` lines 33–35 and `criteria.py` lines 36–38 cites `test_a_method_none_number_at_n_zero_or_n_null_is_annotated_method_not_wilson`, which feeds a Wilson Number under `ci_upper_bound` and `point_estimate` only; `test_attainability_fields_are_filled_for_a_lower_bound_proportion_criterion_only` feeds `f1` and asserts the two fields `None`, not `detail`. Lens-3 regression measured `f1` `wilson` n 30 → `met`, both `None`, `detail {}` | The behaviour holds on the three Numbers fed; the citation names a test that does not inspect the clause | assert `"attainability_not_computed" not in detail` in the `f1` test and cite it | lens-3 RG N2 |

**B. Sentences and records that are imprecise, not false.**

| # | Item | Why carried | Repro | Owner |
|---|---|---|---|---|
| 11 | "at `ab729d3` the first five of those six raised `OverflowError`" (`src/proofpack/licence/verify.py` line 20; `tests/test_licence.py` line 257): all six raised there — lens 3 fed `expires 9999-12-31T23:59:59-05:00` in the `ab729d3` worktree and got `OverflowError: date value out of range`, the same line as the `+05:00` payload; the sixth simply joined the test in repair 2 | True of the five and silent on the sixth; reads as a contrast that is not there | write "all six raised at `ab729d3`; the sixth joined the test in repair 2" | lens-3 FA N2 = RG N1 |
| 12 | Eight public functions without a docstring: `manifest.platform_tag`, `sha256_file`, `scipy_version`, `utc_now_iso`; `licence.install_location`; `ledger.ledger_path`; `criteria.needs_operating_point`; `declare.metric_needs_operating_point` (the module docstrings carry the formula or D1 line for each) | Documentation | — | lens-1 RG-N10 |
| 13 | `REASON_CODES["no_calibration_block"]` reads "the calibration block is absent (no score column)", but a y-pred-only run gives `calibration_suppressed` with `no_score_column` in `detail` (lens 1 measured), so the code is unreachable from `run` and its description names the wrong case | A description; no behaviour | run the y-pred-only fixture; read the `brier` row's `reason_code` | lens-1 N8 |
| 14 | `metric_not_computed` is the reason on `sensitivity` under a comparator reference standard (the PPA/NPA routing); its description ("no Number with an interval exists for this metric id in v1") is not why; a `routed_to_ppa_npa` code would say | A reason-code shape choice; a new enum value in code and schema | `reference_standard: comparator` with a `sensitivity` criterion | lens-1 N8; lens 3 |
| 15 | Counts the committed notes disagree on: the lens-1 regression's "`git diff --name-status a0c9abc ab729d3 -- tests/`: 8 `A`" is 7 `A` (measured now: 7 `A`, 13 `M`, 0 `D`); repair 1's "closed form identical to 12 places (difference 0.00e+00 at each)" is 1.11e-16 at n = 30 (measured now; 0.0 at 50 and 200) | Records in `handoffs/`; no behaviour | the two commands above | lens-2 RG-N5 |

**C. Design, policy and reachability items.**

| # | Item | Why carried | Repro | Owner |
|---|---|---|---|---|
| 16 | **The ledger loses updates under concurrent runs.** `record_run` reads, increments and rewrites `ledger.json` with no lock: six `proofpack run` processes started together → file count **5 / 3 / 6** over three trials (lens 1); two processes → 1 / 1 / 2 / 2 (lens 2), 2 / 1 / 2 / 2 (lens 3). The docstring's "incremented once per run" is true per process | An `O_EXCL` lock file (or `os.replace` on a temp file with a retry) and a two-process test — its own item | six `python -m proofpack.cli run --offline` on one `PROOFPACK_HOME` at once; read `counts` | lens-1 N1 |
| 17 | **A run whose only criterion is the fairness bound is not counted:** `has_criteria=bool(decl.criteria)` reads the `criteria` list; no list + `fairness.bound: 0.10` → one `fairness:tpr_gap not_met` row, count unchanged | A counting-rule change on the ledger; record the decision "count when any `met` / `not_met` row exists" first | a `criteria.yaml` with `fairness.bound` and no `criteria` list; read `ledger.acceptance_runs` | lens-1 N2 |
| 18 | **The ledger counts a run that wrote no document:** `record_run` (`run.py` line 228) runs before `write_run` (line 250); on one home a normal run → 1, `value: .inf` (exit 5, no `run.json`) → 2, no licence → 3, `--out` naming an existing file (`FileExistsError`, exit 5) → 4; the next pack's `ledger_count` includes the two aborted runs | Record after the write, or say in the docstring that aborted runs count — a ledger-semantics decision (needs-from-Josh 7) | `value: .inf` on a fresh home; `ledger.json` absent before, `{<key>: 1}` after, no `run.json` | lens-2 FA-N4 |
| 19 | A ledger `counts` value of JSON `true` is read as 1 (`isinstance(v, int)` admits `bool`) → next run `acceptance_runs 2`, no W15; the eleven other corruptions lens 3 fed (`-1`, `1.5`, `"2"`, `1e400`, a nested object, an empty file, a BOM, `[]`, a trailing NUL, trailing garbage, `PROOFPACK_HOME` naming a file) give `W15` with `counted false` and the file left as it was | One line (`type(v) is int`) plus a test on the ledger read gate | edit the count to `true`; run once | lens-2 FA-N8 |
| 20 | `scope` absent from a criterion is read as `overall` (`c.get("scope", "overall")` in `_reads_for` and `check_references`); the file's own description says the engine never supplies defaults; a criterion without `scope` runs to `met` on `overall.op1.sensitivity` | Add `scope` to `required` (H08 naming the field) — a declaration-gate change with its own test | delete `scope` from one criterion; run | lens-1 N5 |
| 21 | `date: "2026-13-40"` passes `^\d{4}-\d{2}-\d{2}$` and the run proceeds (exit 0) | An H08 rule with a literal test | set the date; run | lens-1 N6 |
| 22 | `value: .nan` / `.inf` / `1e309` in `criteria.yaml` pass `type: number`; the whole run computes and `canonical_json` refuses: `internal error: ValueError: Out of range float values are not JSON compliant: nan`, exit 5, nothing written (and counted — item 18) | H08 at `io.declare` naming the field | `value: .nan`; run | lens-1 N6 / RG-N5 |
| 23 | A criterion on an age band that does not exist (`level: "41-66"` with bands `[40, 65]`) is `not_assessable` / `level_absent`, not H09: `check_references` skips level checks on the numeric `age` attribute | H09 at declaration | `level: "41-66"`; run | lens-1 N6 |
| 24 | Two criteria sharing an `id` produce two rows with the same `criterion_id` and different values (`met` at 0.5, `not_met` at 0.99); no uniqueness check; `level: "*"` already makes the id non-unique, so **the renderer must key on the row, not the id** | An H08 rule, and an E8 reading rule | two criteria `id: X`; run | lens-1 N6 |
| 25 | **With no licence file at all `manifest.watermark` reads `LICENCE EXPIRED - not for submission`**, `licence_id null`, `tier null`, exit 4 (measured now in both shells): the watermark asserts an expiry that never happened | A second string (`NO LICENCE - not for submission`) is a D4 footer decision — needs-from-Josh 5 | the "Re-run these" run | lens-1 N7 |
| 26 | `licence install`: (a) replaces an installed, valid licence with an expired-past-grace one (status is not `refused`, so it copies) and then prints `Next step: proofpack run …` beside exit 4 (`cmd_licence` keys the hint on `status != "refused"`); (b) on the installed path itself → `internal error: SameFileError`, exit 5; on a read-only target → `internal error: PermissionError`, exit 5 — untyped exits on a customer command | Three CLI-shape items: two typed exits and a hint keyed on `status == "ok"`, each its own test | install a valid file, then an expired one; `install` the installed path; a read-only `proofpack.lic` | lens-1 N8; lens-2 FA-N5 |
| 27 | A validly signed licence at `expires 9999-12-30T12:00:00Z` with `grace_days 30` is `refused expires_unparsable {field: grace_days}` because `grace_with_skew` is summed before the `ok` branch; `grace_days 0` → `ok`. Reach: a hand-signed file only (the issuer's `expiresAtMs` comes from the plan periods) | Refuse-at-the-edge is defensible; moving the sum after the `ok` test is a licence-check change (DEC-12 i) — needs-from-Josh 7 | sign the payload with an ephemeral key; `verify` | lens-2 FA-N6 |
| 28 | `detail.field` names the term that overflowed (`grace_days` when `expires` is 9999-12-30), not the extreme value; `issued > expires` is not inspected anywhere (`issued 9999-12-31` on a 2027 annual licence → `ok valid, days_left 320`; a swapped pair re-signed → `expired`) | A different pointer, or an `issued > expires` refusal, is a licence-check change (DEC-12 i); the docstring now says what the field names | the two payloads above | lens-2 RG-N4; lens 3 |
| 29 | `REQUIRED_FIELDS` omit `company_no`, `models` and `features`, which D1 §7 lists and the issuer always writes: `models 0` / `-1` / `"1"`, `features null`, `licence_id` empty or with a newline, `licensee` empty → `ok` | A shape decision on the issuer contract; widening the refusal set is a licence-gate change (DEC-12 i) | a payload without `models`; `verify` | lens-1 RG-N11; lens 3 |
| 30 | A `mapping.json` with no `decided_by` key is read as `file` (lane A's `Mapping.read` default, `mapping.py` line 384) and passes DEC-26 (key deleted from a confirmed file → run exit 0). Lane A's documented convention; DEC-26's words are "`decided_by` interactive or file" | A lane-A rule; a note for A-P2 | delete `decided_by`; run | lens-1 N8 |
| 31 | `map --out` defaults to `mapping.json` in the working directory; `run` reads `<input>.mapping.json` beside the input; a customer who follows `HALT H07: run proofpack map first` literally reaches the same H07 a second time. DEC-26 says "written beside the pack directory"; the build put the default beside the input | One default for both commands, or `run`'s H07 line naming `map --out <input>.mapping.json` — needs-from-Josh 6 | `proofpack map --input test.csv` then `proofpack run --input test.csv …` with no `--mapping` | lens-1 RG-N4 |
| 32 | `overall.threshold_free.roc` carries every distinct score value into `run.json` (401 points, 400 distinct scores on the synthetic run; the first row's score string occurs once, at `roc[333][2]`): permitted in the local pack (D1 §4), and **A-P2's egress suppression must strip it** | A lane-A note; no engine change | read `overall.threshold_free.roc` | lens-1 RG-N8 |
| 33 | `resample_sd_reason` / `resample_sd_not_finite` are pinned by no schema enum (the cell's `bootstrap` block is `{"type": ["object", "null"]}`); `warnings[].code` is a free string (pre-existing) | Schema tightening, its own item | drop the key from a block; validate | lens-1 RG-N9 |
| 34 | `detail.attainability_not_computed` / `method_not_wilson` and the two repair-1 rules (`method none` → `not_assessable`; a filled attainability field → `method wilson`) are pinned by no schema constraint (`criterionResult.detail` is `{"type": "object"}`); an `allOf` would make `jsonschema.validate` in the run test a second observer | Same shape as item 33 | a `met` row with `method none`; validate | lens-2 RG-N3 |
| 35 | The `ledger` block is not in the output schema's top-level `required` (a document without it validates; every run writes it — the five E6 keys and `criteria_results` / `manifest` are required, measured by deleting each); `ledger.warn_after_acceptance_runs: 2.0` passes `type: integer` and the document carries `warn_limit: 2.0` (`0`, `-1`, `"3"`, `true`, `{}` are H08) | Two schema lines | delete `ledger`; validate; declare `2.0` | lens-3 FA N6 |
| 36 | `tests/test_licence.py::test_the_site_reference_verifier_agrees_on_the_same_file` carries a `skipif` on `C:/Users/joshs/GPS/ProofPack/proofpack-site/scripts/verify_licence.py` — the one `skip` line in the day's tests diff (measured now); it ran here and skips in CI (the private site repo is not beside the engine), so "the site's verifier agrees" is a local measurement (three lenses re-ran it) | Vendor the ten-line verifier into `tests/` or read the path from an environment variable with a stated reason | run the suite where the site checkout is absent | lens-1 RG-N6 |
| 37 | `tests/test_mapping_repair3_2.py::test_a_header_spelled_like_the_ignored_key_is_h07_without_the_header` loosened its CLI-route assertion to `startswith("HALT H07: ")` because the DEC-26 `--yes` rule now halts first; the direct `apply_mapping` assertion of the exact message is unchanged | `confirmed: True` on that prior entry would restore the original assertion | read the test | lens-1 RG-N7 |
| 38 | The CLI summary prints `criteria rows: 2 met, 2 not met, 5 not assessable` on stdout (both shells, now) and `--json-log` emits `criteria_status_counts` with those keys — the status vocabulary in a second place beside `criteria_results` | Whether the summary counts by status is E8's rendering question (open question 3 of the E6 rule) | the "Re-run these" run | lens-1 RG-N14 |
| 39 | The attainability level is `evaluate()`'s argument (0.95), not the Number's `ci_level`: a hand-built Wilson Number at `ci_level 0.80`, k = n = 30, `ci_lo 0.948096` under `specificity ci_lower_bound >= 0.9` → `met` beside `attainable_at_n False`, `max_lower_bound_at_n 0.8864866068260313`. Not reachable from `run`: no level key in `criteria_schema.json`, `run.py` calls `evaluate(decl, doc)` with the default, every Number in three scanned documents (406 / 384 / 390) carries `ci_level 0.95` | Reading `number["ci_level"]` in `_row` is an attainability-rule change — a statistical gate, DEC-12(i) — with the lens's Number as its test | the hand-built Number above | lens-2 RG-N2 |
| 40 | **O:E `est inf` on a score column of denormals:** 60 rows, 20 events, every score `1e-310`, i.i.d. → `internal error: ValueError: Out of range float values are not JSON compliant: inf`, exit 5, no `run.json`; `5e-324` the same; `1e-300` runs (`est 3.33e299`, finite bounds); through `calibration_block` directly the i.i.d. cell is `est inf`, `ci (inf, inf)`, `method log_delta`, **no typed reason**. Scores at exactly `0.0` are `zero_denominator`. Pre-existing day-4 route, surfaced by `allow_nan=False`; a softmax export underflows to `0.0`, not to a denormal | Guard the O:E denominator in `calibration.py` with a typed reason (`expected_below_float_range` or the like) — a `calibration.py` change, DEC-12(i) | `probes/p11_inf_run.py` in the lens-3 fresh-attack scratchpad, or the construction above | lens-3 FA N1 |
| 41 | Hand-built shapes the `_row` guard admits, none produced by the routes the lenses read: `n: True` on a Wilson Number fills the fields at n = 1 (`max_lower_bound_at_n 0.2065493143772375`, `attainable_at_n False` against 0.9); a Wilson Number with `suppressed: true`, `n 3` → `not_assessable / suppressed` beside `attainable_at_n False`, `0.4385029682449546` (the annotation branch runs before the `suppressed` return); a `None` where a Number should be → `not_assessable` with `reason_code None`, outside the enum; a Wilson `ci_lo 0.9` at `n 30` → `met` beside `False`. On the engine's own Numbers `proportion(n, n).ci_lo == max_lower_bound_at_n(n)` for n in {1, 2, 5, 30, 128, 1000}; every run-route `n` is a Python `int` (366 of 366, lens 2); `grep -rn suppressed src/proofpack` finds the `Number` field, its validator and `subgroups.py`'s pass-through only (k-suppression is A-P2's, at egress) | Move the annotation branch below the `suppressed` return; `type(n) is int`; a typed reason for a missing Number — own item if E8 or A-P2 starts building suppressed Numbers | the four Numbers above through `criteria.evaluate` | lens-3 FA N3; RG N4 |
| 42 | A y-pred-only table: `overall` is `null` (`overall_block` returns `None` when `table.score is None`), so `sensitivity ci_lower_bound >= 0.5` at `scope: overall` is `not_assessable / overall_not_computed` while `subgroups[0].metrics.op1.sensitivity.number` on the same table is a Wilson Number (`est 0.789`, `ci_lo 0.637`, `k 30`, `n 38`) and a site-scoped criterion on the same metric is compared — the asymmetry a customer would see in T8. The document validates; `calibration null` beside `calibration_suppressed_reason {reason: no_score_column, …}` (DEC-36) | Closes with cut 1 (the `y_pred` operating-point `overall`, E8) | the y-pred-only fixture with an overall and a site criterion | lens-3 FA N5 |
| 43 | `doctor` marks a refused licence `[ok  ]`: with `PROOFPACK_LICENCE` at a file signed by an unknown key it prints `[ok  ] licence  <path>: refused (unknown_key_id)` — the check's own status (non-essential, "ran") beside the word `refused`; the run route is unaffected (exit 4, watermark) | Cosmetic; one line in `doctor.py` and a test | `PROOFPACK_LICENCE=<ephemeral-signed file> python -m proofpack.cli doctor --offline` | lens-3 FA N7 |

**Not carried:** lens-2 M01 (`or` → `and` on the B1 route) and lens-3 M22 (the level
hard-coded 0.95 in `_row`) — equivalent on the run route per the lenses (over 1,941 Numbers
`(reason set) xor (method none)` is empty; `evaluate` is only ever called with the default);
the six-file `stats/` diff (lens-1 N8) — `discrimination.py` (ROC origin threshold `inf` →
`None`, needed for `allow_nan=False`), `calibration.py` (`no_score_column`, item 28),
`descriptive.py` (`dev_rows`, item 27), `subgroups.py` / `calibration.py` (the
`resample_sd_reason` pass-through), each recorded in the build note, and lens 3's eight-document
diff found 0 values moved; the five feature test files failing at `a0c9abc` at collection
(`ModuleNotFoundError: No module named 'proofpack.licence'`), not on an assertion (lens-1
RG-N13) — expected for tests of new modules, recorded so nobody reads "fails pre-build" as
pinning a behaviour; lens-3 N9's three records (`run --out <the input's own directory>` leaves
the customer's `<input>.mapping.json` beside `run.json` — `run` wrote nothing named
`mapping.json` into any of the 115 pack directories; a `grace` licence with no other warning
exits 0 with the watermark, as `run.py`'s "exit 0 / 2" reads; `expires` spellings
`fromisoformat` accepts beyond the issuer's `isoUtc` — a space separator, basic format, `.123Z`,
`+00:00` — verify `ok`); lens-3 RG N3 (a sentence in the repair-2 scratchpad note, now with the
135-row grid behind it); the observer tests that pass on the pre-fix tree by design (repair 1's
RG-N1 and RG-N2, repair 2's F17 and `-05:00` params — each named in the repair notes and
re-planted by the next regression lens); lens-2 FA-N9 and lens-3 FA N8, which re-verify items
above.

**E6 items still open (old numbers; the E6 note holds each repro).** Not re-run today; lens 3
diffed eight documents assembled with `tests/assembler.py` at `a0c9abc` and `2cd7e16` path by
path and found 0 values differ on the seven `a0c9abc` can build, and `git diff a0c9abc 2cd7e16
-- src/proofpack/stats/` changes no constant, so the E6 repro lines stand as written there.
Closed today: **25** (`resample_sd` `inf`, row 1), **26** (blank `case_id` → S05), **27**
(`flow.dev_rows`), **28** (y-pred-only → `no_score_column`), **30** (the required set). Closed
by decision: **35** (DEC-34, the 0.897 cell renders), **31** (DEC-35, the annotation policy
stays); the E6 needs 3 became DEC-36. Open: **1–19** (the eighteen mutant-survivor observers
and the `usable < 200` assertion; of these, **9–13, 15, 16** were on E7's "if time left" list
and were cut, see above), **20–24** (the five imprecise sentences and records), **29** (equal-mass
`edges` under ties), **32** (a second DGP in `coverage_calibration.py`, goes with DEC-18 a),
**33** (`detail.exit` for the IRLS exit), **34** (`brier_ref` precedence on a refused
resampler, to record in T7), **36** (one spelling for "not a probability" in day-5
`_brier_cell`). DEC-18 (a), the design-effect Wilson, is still without an E slot (E6 needs 4).

## What the lenses found

Every lens ran in a detached worktree with `PYTHONPATH=<worktree>/src` forced and
`proofpack.__file__` printed from the worktree before any count was trusted (each also printed
the unforced form, which imported the main tree — the `.pth` trap). Blocker titles are quoted
as the lenses wrote them.

### Round 1, at `ab729d3` — fresh-attack `FAIL` (2 blockers, 8 non-blocking); regression `FAIL` (1 blocker, 14 non-blocking, four of them sentences)

> **B1 / RG-B1.** "a criterion on a Number with no interval is `met` / `not_met` when the
> statistic is `point_estimate` (wrong status; sentence violation)" — `criteria._row` read
> `est` whatever the method and routed to `no_interval` only when the statistic was `None`;
> from `proofpack run` on the synthetic cohort `f1 point_estimate >= 0.70` came back
> **`not_met`** and `mcc point_estimate >= 0.40` **`met`** on Numbers carrying `method none`,
> `not_estimable_reason analytic_ci_unavailable`, `ci (None, None)` (`est 0.6620689655172414`
> and `0.4821183187439812`); a clustered PPV cell at `boundary_estimate`, `est 0.0`, gave
> `not_met`. The `criteria.py` docstring, the README and the test name
> `..._never_not_met` said the opposite of what the code did; the test's second half asserted
> `met` on `point_estimate`.
>
> **B2.** "`attainable_at_n: false` beside `status: met` on a clustered cell (wrong
> attainability flag; sentence violation)" — with 29 of 30 negatives right in 15 two-row cases
> the case holding the wrong row is absent from `(14/15)^15 = 35.5 %` of resamples, so the 2.5th
> percentile of the cluster-bootstrap proportion sits at 27/30 = **0.9**, above the i.i.d.
> Wilson k = n figure **0.8865**; the row read `met`, `compared_value 0.9`, `attainable_at_n
> False`. The attainability docstring's "cannot be met by any outcome at that `n`, whatever the
> model does" and "`attainable_at_n: false` is exact" were falsified by that run.

Fixed in `02d00c5`: B1 by routing on `not_estimable_reason is not None or method == "none"`
before any statistic is read (`not_assessable` / `no_interval` with the reason in `detail`
under all three statistics); B2 by filling the two attainability fields on `method wilson` only,
`null` + `detail.attainability_not_computed: method_not_wilson` otherwise (DEC-08: annotated,
not omitted), and by deleting the two falsified sentences; also the lens-1 fresh-attack N3
(`verify` raised `OverflowError` on `grace_days 3_000_000`, `10**9`, `expires
9999-12-31T23:59:59Z`, `0001-01-01T00:00:00+05:00` and a trial `issued 9999-12-31` — each
now `refused expires_unparsable`), the regression's N1 ("`assemble_run` writes nothing" → it
writes `<PROOFPACK_HOME>/ledger.json`, the test made recursive), N2 (the F17 sentences named
three keys where four manifest keys and `ledger.acceptance_runs` differ) and N3 (the n = 50
hand figure `0.928655` re-derived as `0.928652400867`; the literals now at 1e-9). Regression
proof: the four edited test files copied into a worktree at `ab729d3` → **`12 failed, 111
passed`**, each on the quoted assertion (`assert ('not_met' == 'not_assessable'`, `assert ('met'
== 'not_assessable'`, `assert (True is None)`, `assert (False is None)` twice, the three
`OverflowError` spellings, the run test's `{'attainable_at_n': None, … 'criterion_id':
'F1_pe'}`) — re-measured by the lens-2 regression (`12 failed, 111 passed in 7.81s`) and the
lens-2 fresh-attack (`6.62s`); the repair note's `14 failed, 109 passed` was measured before the
invariant helper took the literal `"wilson"`.

What round 1 could not break (lens-1 fresh-attack, own code from the published formulae): the
Wilson k = n bound over n = 1..500 at 0.80 / 0.90 / 0.95 / 0.99 to **0** (closed form 1.1e-16);
26 malformed criteria files each H08 / H09 with nothing written; the manifest's three hashes
equal to `hashlib` and `mapping.json` re-indented → `mapping_sha256` changes (DEC-27, the
bytes); `canonical_json` refusing `nan` / `inf` (`ValueError`) and `np.int64` / `np.bool_` /
`datetime` / `Path` / `set` (`TypeError`) while `np.float64(2.5)` serialises;
the ledger's counts 1, 2, 3, W14 at the fourth, W15 on four corruptions, `criteria: []` not
counted; 61 licence constructions refused or accepted as D1 §7 says except the `OverflowError`
family; zero sockets with and without `--offline`; item 25 finite on the 57 + 3 construction and
nine neighbours through the full run; six documents figure-identical to `a0c9abc` apart from
the four documented additions; DEC-26's H07 in 0.01 s against 0.71 s for a confirmed run; 22
documents schema-valid with 0 verdict-word hits outside `criteria_results`. The regression lens
additionally found the engine accepts three licences issued by the site's own `licence.mjs`
under an ephemeral seed and that `cmd_map` and `cmd_compare` are AST-identical to `a0c9abc`.

### Round 2, at `02d00c5` — fresh-attack `PASS` (0 blockers, 9 non-blocking, four of them sentences a counter-example falsifies); regression `PASS` (0 blockers, 5 non-blocking, one a sentence)

Both lenses found the same sentence: "on any other method both are `null` and
`detail.attainability_not_computed` is `method_not_wilson`" (`criteria.py`, `attainability.py`,
README, the schema description) was false at **n = 0** — the annotation sat under `isinstance(n,
int)` and `elif n > 0`, so a `method none` proportion Number with `n 0` (`zero_denominator`: a
site whose every `y_true` is `0` under a `sensitivity` criterion) or `n null` got neither the
figure nor the annotation (the status, `not_assessable` / `no_interval`, was right). The
fresh-attack also found the F17 sentences reporting `started` as a key that *differed* in the
test's measurement (six consecutive pairs: `started` never differed, `duration_s` twice did not;
the test asserts only `run_id` and `ledger_count`) and the `expires_unparsable` description
claiming `detail.field` on every out-of-range date (the two `parse_utc` refusals carry `{}`;
only the three sums carry a field) plus "three truncations" that were two truncations and one
same-length replacement. The regression lens found the `ci_level` item (carried 39) and that no
schema line pins the two repair-1 rules (carried 34). The lens-1 blockers were re-executed by
both: B1's four hand-built params and the synthetic `F1_pe` / `MCC_pe` rows all
`not_assessable`; B2's construction through the assembler and through `run --offline` `met`
with both fields `null` and the annotation — and the regression lens re-derived B2's arithmetic
without repo code (case c000000 drawn m ~ Binomial(15, 1/15) times; P(m ≥ 4) = 0.014 < 0.025 <
P(m ≥ 3) = 0.074, so the 2.5th percentile is 27/30).

Fixed in `2cd7e16`: the annotation branch no longer depends on `n` (`if method == wilson and
isinstance(n, int) and n > 0: fill / elif method != wilson: annotate` — the fill condition
unchanged); the four sentences rewritten to the guard as the code has it; the F17 sentences in
`manifest.py`, the schema and `tests/test_manifest.py` rewritten to what the test asserts;
`verify.py`'s docstring and the reason description rewritten to the literal inputs (a sixth
`-05:00` param added, `detail {}`); two sweep mutants (one re-targeted, one new, `1 failed, 38
passed`). Regression proof: the four edited test files copied into a worktree at `02d00c5` →
**`3 failed, 124 passed`** (the two params of the n-zero hand test and the S3 all-negative run
test, each on `assert {'not_estimab…_denominator'} == {'not_estimab…d_not_wilson'}` / `Right
contains 1 more item: {'attainability_not_computed': 'method_not_wilson'}`) — re-measured by
the lens-3 regression (`6.83s`) and fresh-attack (`6.96s`); the `-05:00` param and the F17 test
pass there (sentence repairs, disclosed).

What round 2 could not break (lens-2 fresh-attack): the k = n bound at **0.00e+00** over
n = 5..500 at four levels; through `run` on an i.i.d. site of 30 negatives all right
(`specificity` Wilson `est 1.0`, `ci_lo 0.8864866068260313`): `>= 0.8864866068260312` `met` +
`True`, `>= 0.886486606827` `not_met` + `False`, `<= 0.5` `not_met` + `None` with the figure
filled; 61 documents walked with no `met` row beside `attainable_at_n False`; 60 licence
constructions (`expires == now` `ok` 0 days, `now − 24 h` exactly `ok`, one second more
`grace`, grace 30 expired 31 d exactly `grace`, 31 d + 1 s `expired`; `issued` after `expires`
`ok`; duplicate `tier` keys last-wins; a 3 MB payload `ok`); item 25 on 19 constructions; seven
documents 0 values different from `a0c9abc`; every `n` in the in-memory document a Python `int`
(366 Numbers), so `isinstance(n, int)` is not defeated by `numpy.int64` from `run`; 42
documents schema-valid, no row identifier. The regression lens ran a 918-criterion sweep (30
metrics × 3 statistics × 2 comparators × 3 scopes) over the i.i.d. and the clustered document:
0 `met` / `not_met` rows on `method none`, 0 `met` beside `False`, 0 filled attainability
fields off a `wilson` row; and the full suite in PowerShell (`784 passed … in 67.42s`, the
repair note's blank cell).

### Round 3, at `2cd7e16` — fresh-attack `PASS` (0 blockers, 9 non-blocking, none a falsified sentence); regression `PASS` (0 blockers, 5 non-blocking, three sentence-precision)

Lens 2's three sentence items closed by execution: the n-zero and n-null hand Numbers and the
S3 all-negative run carry the two-key `detail`; a 135-row grid (n in `0, 1, 30, −3, None, 30.0,
np.int64(30), True, False` × five methods × three statistics) evaluated with each tree's own
`criteria.py` — **0 rows differ** in status, reason code or the two attainability fields between
`02d00c5` and `2cd7e16`, 24 rows gain the annotation, every one a `ci_lower_bound` row on a
non-`wilson` method (regression lens); the F17 and `verify.py` sentences read against the test
bodies and six signed payloads. What round 3 found is carried items 6–11, 35 and 40–43 above
(the `n: True` / `suppressed` shapes are 41); the one with a customer-facing consequence is item
40 (an `inf` O:E on a denormal score column, exit 5 — a day-4 route E7 surfaced).

What round 3 could not break (lens-3 fresh-attack): 95 `criteria.yaml` variants through `run
--offline` (every H08 / H09 gate named; the statistic named is the one compared; `age` `level:
"*"` four rows; a comparator reference standard routing `sensitivity` → `metric_not_computed` and
`ppa` → `met` on 0.668; two operating points; `lower_is_positive` mirrored to the same 2 × 2 and
`auroc 0.8353`); 33 hand-built Numbers; the k = n bound at **0.0** against its own Wilson over n =
1..500 at four levels and `proportion(n, n).ci_lo == max_lower_bound_at_n(n)` for n in {1, 2, 5,
30, 128, 1000}; 116 licence constructions (88 against `verify`, 17 through the CLI, nine date
payloads at two commits) — no signature segment in the output of 17 CLI calls to `show` / `verify`
/ `install` / `--json-log`, checked by substring; 21 mapping-file variants (DEC-26: H07 in 0.01 s
on 400 rows and 0.10 s on 50,000 against 0.71 s / 1.96 s confirmed); 12 ledger corruptions and 4
two-process races; 19 item-25 constructions (finite on 18; the overflow is the O:E numerator, item
40); 8 documents 0 values different from `a0c9abc`; 115 `run.json` files schema-valid, 115
distinct `run_id`s; the committed sweep 25 / 25 and 15 of its own 22 mutants killed. The
regression lens re-measured every repair-2 figure in both shells (full suite PowerShell `61.53s`;
the sweep `106 s` Git Bash, `110 s` PowerShell), read the schema diff (two `description` strings,
0 changed `required` / `enum` / `type` / `additionalProperties` / `const` lines), counted the
registers (`METHODS` 18, `FLAGS` 15, `NOT_ESTIMABLE_REASONS` 21, licence `REASON_CODES` 14,
`STATUSES` 4 — unchanged by the repair) and re-ran the E6 block.

Could not check, all three rounds: the reference platform (`linux-x86_64-cp312`) and F17's
cross-platform half (`reference_platform false` on this machine); a licence signed by the
production key (none exists off the issuer — the shipped key's bytes and id, the site's own
`verify_licence.py` and its `licence.mjs` issuer under an ephemeral seed are what was
exercised); `licence install` across a symlink (Windows privilege); the `/docs/run` and
`/docs/licence` anchors the CLI prints (lane S); E8's rendering of `criteria_results`,
`detail.attainability_not_computed` and `manifest.watermark`; the day-6 sweep (`--marker day6`)
after `02d00c5` and `2cd7e16` (neither touches a file it plants in; lens 1 ran it at `ab729d3`:
`112 planted, 112 killed, 0 survived; 960 s`).

## The decisions taken today

Each is stated in the module docstring named and, where a customer sees it, in the README's
`proofpack run` section; the test that feeds the literal input is named.

| Decision | Rule as shipped | Where recorded | Test that feeds the input |
|---|---|---|---|
| `ci_lower_bound` under `<=` / `<` | **Read literally**: the Number's `ci_lo` is compared with the comparator as written; `_row` has no code path that swaps to the conservative end (that would be the engine choosing the statistic). To let a customer name the conservative end, **`ci_upper_bound` is a third value in the `statistic` enum** (D1 §2 lists two) — needs-from-Josh 1 | `criteria.py` docstring; `schema/criteria_schema.json`; README | `test_criteria.py` (`ci_lower_bound <= 0.83` met on `ci_lo 0.82`; `ci_upper_bound <= 0.95` met); lens 3: `ci_lower_bound <` `met` on 0.668 < 0.7 with the figure filled and `attainable_at_n null` |
| Which field each criterion reads | `overall.<op>.<metric>` and `overall.threshold_free.auroc` / `prevalence` are Numbers read directly; subgroup, calibration and fairness quantities are day-4 cells and the criterion reads **`number`** (the rendered Number), never `analytic`; `metric_ref` is the JSON path with `.number` appended for a cell. Measured now: `C_met` → `overall.op1.sensitivity`; `C_n30` → `subgroups[8].metrics.op1.accuracy.number`; `C_auroc` → `overall.threshold_free.auroc`; `fairness:tpr_gap` → `fairness.gaps[0].operating_points.op1.tpr_gap.number`. `balanced_accuracy`, `f1`, `mcc`, `lr_pos`, `lr_neg`, `dor`, `youden` overall only (a subgroup scope is `metric_not_computed_for_scope`); `brier` overall from `calibration.brier`, per level from `subgroups[i].metrics.brier`; `ipa`, `oe`, `calibration_slope`, `calibration_intercept` from the calibration cells; the five gaps from `fairness.gaps[j]` on the fairness attribute's levels; `ece`, `auprc`, `psi`, `ks_d` and the lite ids `metric_not_computed` (no Number with an interval in v1); `level: "*"` = every tabulated level except the explicit `Unknown/missing` row; `paired_difference_vs_prior` → `requires_compare` | `criteria.py` docstring (`_reads_for`) | the synthetic run (`test_run_cli.py`), the `metric_ref` values above |
| Evaluability before any statistic | A Number with `not_estimable_reason` set **or** `method none` is `not_assessable` / `no_interval` with the reason in `detail`, whatever the statistic (repair 1); a suppressed Number `suppressed`; a missing block `overall_not_computed` / `no_calibration_block` / `no_fairness_block` | `criteria.py` docstring lines 25–43 | `test_a_number_with_method_none_is_not_assessable_under_each_of_the_three_statistics` (4 params), the F1 / MCC run test |
| Attainability | `max_lower_bound_at_n = wilson_bounds(n, n, level)[0]` (closed form `n / (n + z^2)`), `attainable_at_n = bound >= value` for `>=`, `bound > value` for `>`, `null` for `<=` / `<` (the figure still reported); filled only for a `ci_lower_bound` criterion on `sensitivity`, `specificity`, `ppa`, `npa`, `ppv`, `npv`, `accuracy`, `prevalence` whose Number is **`method wilson`** with an `int` `n > 0`; any other method → both `null` and `detail.attainability_not_computed: method_not_wilson` whatever `n`; a `wilson` Number whose `n` is not an `int` above 0 → both `null`, no annotation (the guard as written, stated not defended). No sample-size advice in the row: it carries the bound and `n`, nothing else | `stats/attainability.py` docstring; `criteria.py`; README; the schema's `criteria_results` description | `test_the_hand_figures_at_30_50_200`, the B2 tests, the n-zero test |
| Fairness bound | Needs `statistic` and `comparator` beside `bound` (schema `if` / `then`; H08 naming the field); one row per non-reference level, id `fairness:<criterion_of_interest>`; `calibration_by_group` → `not_estimable_this_run`; absent bound → no row — needs-from-Josh 3 | `criteria_schema.json`; `criteria.py` `_fairness_rows` | `test_criteria.py` (H08 on a bound without `statistic`); the run's `fairness:tpr_gap` row (`ci_upper_bound <= 0.1` → `not_met` on `0.217046664854876`, `newcombe10`) |
| Ledger store | `ledger.json` under `PROOFPACK_HOME` (`%LOCALAPPDATA%\proofpack` on Windows, `~/.proofpack` elsewhere), JSON not SQLite, `platformdirs` not added; key = SHA-256 of the analysed rows' `y_true` strings joined by `\n`, the byte `0x00`, then the little-endian float64 bytes of `score` (or `y_pred\n` + the `y_pred` strings); the count includes the run that wrote it; W14 when `count > warn_after_acceptance_runs`; W15 (`count null`, `counted false`) when the file cannot be read or written; `manifest.ledger_count` and a top-level `ledger` block carry it for T8 — needs-from-Josh 4 | `io/ledger.py` docstring; `home.py` | `test_ledger.py` (8); `test_assemble_run_writes_only_the_ledger_file_under_home` |
| Licence file location | `PROOFPACK_LICENCE` (a path) first, else `<home>/proofpack.lic`, else `./proofpack.lic`; `licence install` copies to the home location after a signature check (a refused file is not installed) | `licence/__init__.py` (`resolve`, `install`) | `test_licence.py`, `test_run_cli.py` |
| After grace (D1 §7 "Expiry") | `ok` and `grace` are full runs (exit 0 / 2; `grace` sets `manifest.watermark` to `LICENCE EXPIRED - not for submission`); `expired` past grace, `refused` and no file still compute and write `run.json` with that watermark and **exit 4** with the one-line fix; the renderer (E8 / E9) writes HTML / DOCX only on `ok` / `grace` — "emit JSON only" read as: the JSON is emitted, the documents are not, and the exit code is the licence code so a pipeline sees it — needs-from-Josh 2 | `run.py` (`RunOutcome.exit_code`); `cli.py` docstring; README | `test_run_cli.py` (no licence → exit 4 with `run.json`; expired past grace exit 4; grace exit 0 + watermark; trial `TRIAL`); measured now in both shells |
| Trial and skew | Effective expiry = min(payload `expires`, `issued + 30 days`); 24 h skew on the customer's side at both the expiry and the grace end; the "until first full pack" half of the trial is E9's | `licence/verify.py` docstring (`TRIAL_DAYS`, `CLOCK_SKEW`) | `test_a_trial_expires_after_thirty_days_from_issue_whatever_the_payload_says`; the 23 h / 25 h skew tests |
| Base64 | Canonical only: base64url and unpadded → `base64_invalid`, trailing bits → `base64_not_canonical`; the shipped key decoded once at import and compared as bytes; the "published" key is the same literal, never fetched, kept equal by an import-time assertion (a one-bit change to either literal alone fails at import) | `licence/keys.py` | `test_the_four_spellings_of_the_key_check_lesson`; `test_the_shipped_key_is_the_site_s_live_key_by_bytes_and_the_published_copy_is_the_same` |
| Blank `case_id` (E6 item 26) | `S05` at `io.schema.validate` naming the count (`case_id is blank in N row(s); fill every case id or drop the column`), before any statistic; exit 3, nothing written | `io/schema.py` | `test_run_cli.py` (S05 on 3 blank ids); lens 3's nine spellings |
| `flow.dev_rows` (E6 item 27) | Rows whose `dataset` reads `dev`, so the flow sums to `rows_read`; D1 §4.2's `flow` needs the key added (amendment) | `stats/descriptive.py` | `test_run_cli.py` (100 rows with 10 dev: `rows_read = dev_rows + the two exclusions + indeterminate + analysed`) |
| y-pred-only (E6 item 28) | `calibration_from_table` returns `None` with `no_score_column` (added to the `calibrationSuppressedReason` enum, not to `NOT_ESTIMABLE_REASONS` — it is not a Number's reason); `overall` is `null` too (cut 1) | `stats/calibration.py` | `test_run_cli.py` (y-pred-only validates) |
| Canonical JSON | `sort_keys=True, indent=1, separators=(",", ": "), ensure_ascii=False, allow_nan=False`, one trailing `\n`, UTF-8 without BOM; a non-finite float raises `ValueError` and nothing is written; numpy scalars other than `np.float64` raise `TypeError` | `manifest.py` (`canonical_json`) | `test_manifest.py` (the exact bytes of `{"b": 1, "a": {"y": [1.5, None], "x": "é"}}`; `inf` / `-inf` / `nan` at any depth raise) |
| F17 and the ledger | Two identical runs differ in `run_id`, `started`, `duration_s` (`VOLATILE_KEYS`, may or may not differ — the test does not assert either way) and, because the ledger counts every run with criteria, in `ledger_count` / `ledger.acceptance_runs` (`HISTORY_KEYS`); the three SHA-256 fields equal; everything else byte-identical | `manifest.py` docstring; the schema's `manifest.description` | `test_f17_two_runs_in_one_process_differ_only_in_run_id_started_duration_s_and_the_ledger_count` |
| DEC-26 in `cmd_run` | `--mapping FILE` or `<input>.mapping.json` beside the input (`test.csv` → `test.csv.mapping.json`); `run` is always non-interactive (`check_h07`'s `--yes` rule applies to every prior; `--yes` on `run` is accepted and does nothing); no `mapping.json` is written into the pack or anywhere; `ingest_report.json` is still written beside `run.json`; `manifest.mapping_sha256` is the file's bytes (DEC-27). DEC-26's "beside the pack directory" versus the build's "beside the input" is carried item 31 | `run.py` (`resolve_mapping_path`, `default_mapping_path`); README | `test_run_cli.py` (H07 ×3); measured now in both shells |
| Test-only key hook | `main(argv, *, registry=None)` / `assemble_run(registry=)` / `verify(registry=)`; `conftest` generates one Ed25519 pair per session and writes a signed licence into a session `PROOFPACK_HOME` (an autouse fixture points every test there, so the tests read the session home, not the machine's licence or ledger); the shipped constant is not rebound by the hook (the registry is passed as an argument) | `tests/conftest.py` | every day-7 licence and run test |
| H09 extended | A threshold metric (`sensitivity` … `npv_gap`) without `operating_point`, and a threshold-free one (`auroc`, `brier`, …) with one, are H09 at declaration with `field: operating_point` | `io/declare.py` | `test_criteria.py` (`auroc` at `op1`; `sensitivity` without an operating point) |
| `criteria_results` row shape | Beyond D1's five keys the row carries `metric`, `operating_point`, `scope`, `statistic`, `comparator`, `value`, `metric_ref`, `compared_value`, `n`, `method`, `attainable_at_n`, `max_lower_bound_at_n`, `detail` (16 keys, measured now) — the facts T8's table 5.6 needs without re-reading the declarations (open question 3) | `criteria.py`; schema `criterionResult` (closed) | the run test's schema validation |

## Re-run these

Every command below was run now, at `2cd7e16`, in the main tree, in **both** Git Bash and
PowerShell 5.1 unless a cell says otherwise; `python -c "import proofpack;
print(proofpack.__file__)"` printed `C:\Users\joshs\GPS\ProofPack\proofpack\src\proofpack\__init__.py`
in each first. Outputs are pasted as printed; the shells differ only in seconds and `run_id`.
The fixture directory `<scratchpad>/e2e_shell` holds the synthetic cohort (`test.csv`, 400 rows,
S3 = 30), the test `criteria.yaml` (`warn_after_acceptance_runs: 3`) and a `file`-confirmed
`test.csv.mapping.json`; each shell used its own empty `PROOFPACK_HOME` with
`PROOFPACK_LICENCE` unset.

```bash
cd C:/Users/joshs/GPS/ProofPack/proofpack
```

| Command | Output, Git Bash | Output, PowerShell 5.1 |
|---|---|---|
| `python -m pytest -q -p no:cacheprovider` | `788 passed, 1 skipped, 1 xfailed in 55.54s` | `788 passed, 1 skipped, 1 xfailed in 54.74s` |
| `python -m pytest -q -p no:cacheprovider -m day7` | `139 passed, 651 deselected in 7.15s` | `139 passed, 651 deselected in 7.03s` |
| `python -m pytest -q -p no:cacheprovider -m day6` | `285 passed, 505 deselected in 11.52s` | `285 passed, 505 deselected in 11.08s` |
| `python -m pytest -q -p no:cacheprovider -m day5` | `54 passed, 736 deselected in 17.53s` | `54 passed, 736 deselected in 17.48s` |
| `python -m pytest -q -p no:cacheprovider -m day4` | `182 passed, 608 deselected in 16.87s` | `182 passed, 608 deselected in 17.03s` |
| `python -m pytest -q -p no:cacheprovider -m day3` | `29 passed, 760 deselected, 1 xfailed in 2.32s` | `29 passed, 760 deselected, 1 xfailed in 2.30s` |
| `python -m pytest -q -p no:cacheprovider -m day2` | `40 passed, 750 deselected in 1.78s` | `40 passed, 750 deselected in 1.81s` |
| `python -m pytest -q -p no:cacheprovider -m day1` | `59 passed, 1 skipped, 730 deselected in 4.10s` | `59 passed, 1 skipped, 730 deselected in 4.31s` |
| `python -m ruff check .` | `All checks passed!` | `All checks passed!` |
| `python -m ruff format --check .` | `110 files already formatted` (108 at `2cd7e16` clean; 110 with the two lens-3 notes in the tree; 111 with this note) | `110 files already formatted` |
| `python -m proofpack.cli doctor --offline` | 17 `[ok  ]` lines (`licence  no licence file found - doctor/map/fixtures always work; run/compare need one`; `reference platform  windows-amd64-cp314 (reference is linux-x86_64-cp312; tolerances per T7 apply)`), `All essential checks passed.`, `exit=0` | `All essential checks passed.`, `exit=0` |
| `python scripts/mutation_sweep.py --list` | 171 lines, 34 `day5` + 112 `day6` + 25 `day7`, 0 duplicate ids | not repeated |
| `python scripts/mutation_sweep.py --marker day7` | `25 planted, 25 killed, 0 survived; 108 s` (every mutant's line `killed` with a `1 failed, N passed, 651 deselected` under it); `git status --short` unchanged afterwards | `25 planted, 25 killed, 0 survived; 103 s` |
| `PROOFPACK_HOME=<empty dir> python -m proofpack.cli run --input <e2e_shell>/test.csv --criteria <e2e_shell>/criteria.yaml --out <e2e_shell>/pack_handoff_bash --offline` | `run written: …\pack_handoff_bash\run.json (run_id 3e69e3c9-1781-437d-a865-4a7fe182fc67)` / `  analysed 400 of 400 rows; criteria rows: 2 met, 2 not met, 5 not assessable` / `  licence refused (no_file); watermark: LICENCE EXPIRED - not for submission` / `  no usable licence: install one with \`proofpack licence install FILE\` (docs: /docs/licence); run.json was written with the watermark 'LICENCE EXPIRED - not for submission'` / `Next step: proofpack licence show, then the T1/T7/T8 renderers (docs: /docs/run)` / `exit=4`; the pack directory holds `ingest_report.json` and `run.json`, the home `ledger.json` | identical lines with `run_id ffd6b3ff-bd57-4a37-a0f5-96dcf6daa9fe`, `exit=4`, the same two files and the ledger |
| the rows of that `run.json` | `C_met` met `wilson` `attainable_at_n True` `max_lower_bound_at_n 0.9708630437264916` on `compared_value 0.6684443704768314` `n 128`; `C_n30` not_met `wilson` `False` `0.8864866068260313` on `0.5212421254128505` `n 30`; `C_auroc` met `delong_wald` nulls on `0.7948394595133246`; `fairness:tpr_gap` not_met `newcombe10` nulls on `0.217046664854876` `n 63`; three `C_f1_site` `metric_not_computed_for_scope`; `C_ece` `metric_not_computed`; `C_prior` `requires_compare`; every `detail {}`; 18 top-level keys; `manifest.ledger_count 1`, `licence_id null`, `tier null`, `platform win-amd64-cp314`, `reference_platform false`; validates against `schema/output_schema_v1.json` with 0 errors; the file equals `canonical_json` of its own parse | the same rows; the two files 216,303 bytes each, no CRLF, no BOM; manifest keys that differ across the shells `duration_s`, `run_id`, `started`; the three SHA-256 fields equal and each equal to `hashlib.sha256(file bytes)`; 0 top-level blocks differ once those three are blanked; the two `ingest_report.json` byte-identical |
| the same `run` with the mapping file absent (`<scratchpad>/handoff-E7/h07/`) | `HALT H07: run proofpack map first: no confirmed mapping.json was given (--mapping FILE) and none exists beside the input as <input>.mapping.json` / `  detail: {"mapping_given": false, "exists": false}` / `  No document was written.` / `exit=3`; no pack directory | identical, `exit=3` |
| `python -m proofpack.cli licence show` (no file) | `licence: none found` / `  status: refused (no_file)` / `Next step: proofpack licence install FILE (docs: /docs/licence)` / `exit=4` | identical, `exit=4` |
| `python -m proofpack.cli licence verify <absent path>` | `licence: <path>` / `  status: refused (no_file)` / `Next step: ask licences@globalphoenix.co.uk for a re-issue (docs: /docs/licence)` / `exit=4` | not repeated |
| F17 in one process (two `cli.main([... run ...])` calls on the fixture, one empty home) | exit codes `4 4`; manifest keys that differ `['duration_s', 'ledger_count', 'run_id', 'started']`; `ledger.acceptance_runs` `1 2`; top-level blocks that differ `['ledger', 'manifest']`; SHA-256 fields equal; byte-identical after blanking those five | not repeated |
| `socket.socket`, `create_connection`, `getaddrinfo`, `urllib.request.urlopen` replaced with raisers, then `run` with and without `--offline` | `run --offline: exit 4, socket calls 0` / `run (no flag): exit 4, socket calls 0`; `grep` for `socket` / `urllib` / `http` / `requests` imports under `src/proofpack`: 0 lines | not repeated |
| the hidden-module import (`cryptography` ×6, `scipy` ×3, `statsmodels`, `sklearn` set to `None` in `sys.modules`) | `proofpack`, `.stats`, `.licence`, `.licence.verify`, `.criteria`, `.manifest`, `.run`, `.cli` import; `verify(b"abc")` → `{'status': 'refused', 'reason_code': 'not_two_segments', … 'detail': {'segments': 1}}`; no scipy module loaded | not repeated |
| the shipped key against `proofpack-site/keys/` | `shipped == published literal: True`; `== licence_pub.b64: True`; `== licence_pub.json public_key: True`; `key_id pp-2026-09`, `status live`; 32 bytes `5d82e76854675953ac2e86f04a691ba3fab2dfb6a73b36be691a6e9f39ab3000`; `grep -rnE "PRIVATE KEY\|-----BEGIN\|Ed25519PrivateKey\|importSigningKey\|LICENCE_SIGNING_KEY" src/` → one docstring line (`keys.py:25`, the secret's *name*) and its `.pyc` | not repeated |
| the lens-1 B1 one-liner (the `ab729d3` repro from the lens-1 regression note) | `not_assessable no_interval {'not_estimable_reason': 'single_class'} None None` (printed `not_met` at `ab729d3`) | not repeated |
| the item-25 construction (57 + 3, B = 200, seed 20240101) through `calibration_block` | `O:E est 13.333333333333334 ci (4.0, 3.999999999999999e+159) method cluster_bootstrap_percentile` / `resample_sd None reason resample_sd_not_finite` / `json.dumps(block, allow_nan=False): ok, 24164 chars` | not repeated |
| the attainability hand oracle (Wilson at p = 1 written in the session, `z = NormalDist().inv_cdf(0.975)` = `1.9599639845400536`) | n = 30 engine `0.8864866068260313` hand `0.8864866068260314` closed-form `0.8864866068260313` (`|engine−hand| 1.11e-16`, `|engine−cf| 0.00e+00`); n = 50 `0.9286524008666414` all three (0 / 0); n = 200 `0.9811546736227335` all three (0 / 0); `attainable(0.90, 30, ">=")` → `False` | not repeated |
| the five verdict-grep tests (`-k "status_words_appear_only_under_criteria_results or verdict_word"`) | `5 passed, 785 deselected in 3.17s` | not repeated |
| the E6 block: `tests/test_calibration.py::test_the_assembled_document_validates_against_the_output_schema`; repair-r7's 27; day-5 round-1's seven; round-2's seven (the `-k` selections as written in the E6 note) | `1 passed in 2.31s` / `27 passed, 155 deselected in 0.69s` / `7 passed, 47 deselected in 1.74s` / `7 passed, 47 deselected in 3.07s` | not repeated |
| the round-5 probe `PYTHONIOENCODING=utf-8 python -c "from proofpack.io.profile import profile_column as p; s=p(['17 \u017fep 2024']*60); print(s.inferred_type, s.min, s.max)"` | `categorical None None` | not repeated |
| `git diff --name-status a0c9abc HEAD -- tests/` | 7 `A`, 13 `M`, 0 `D`; added lines matching `skip\|xfail\|\.only`: 1 (the `skipif`, item 36); removed `pytest.mark` lines: 0 | not repeated |

Not re-run: the day-5 and day-6 sweeps and `coverage_calibration.py` (`bootstrap.py` changed
only in the non-finite sd branch and the `CellCI` pass-through; `calibration.py` only in
`no_score_column` and the pass-through; lens 1 ran `--marker day6` at `ab729d3`, 112 / 112, and
lens 3's eight-document diff found 0 values moved). The pre-fix proofs for the two repair rounds
(`12 failed, 111 passed` at `ab729d3`; `3 failed, 124 passed` at `02d00c5`) are in the repair
notes and were re-measured by the round-2 and round-3 lenses; to repeat one, add a worktree with
`git worktree add --detach <path> <sha>`, copy the test files in, run with
`PYTHONPATH=<worktree>/src` forced and `proofpack.__file__` printed first — the editable `.pth`
on this machine otherwise imports the main tree and gives a false green. Do not paste the bare
`proofpack doctor --offline`: the console script is not on `PATH` in either shell;
`python -m proofpack.cli doctor --offline` is the form that runs.

## Tomorrow needs

Day 8 is **E8 — claims, checker, HTML renderer, T8** (`spec/briefs/day8_E.md`): `narrate.claims`
from the output JSON → `claims_schema.json` (final); the claim-binding checker (number ↔
`metric_id` ↔ subgroup ↔ direction; the status word from the enum; forbidden tokens) with a
**≥ 50-case adversarial corpus**; the jinja2 HTML renderer with footer, suppression markers and
anchors from the guidance map carrying "(draft) … not for implementation"; T8 (D4 §11) from the
synthetic cohort; tests: corpus 100 % rejected, golden HTML T8, footer on every page, the
anchor-label test, T8 §7 counts. The D5 line: `render/theme.py` reads `design/tokens.json`,
created on this day from D5 §3.1 verbatim as three files (`design/tokens.json`, one
force-include line in `pyproject.toml`, a `resources.py` entry) plus a test that
`resource_path("tokens.json")` resolves inside a built wheel; the theme is the only source of
colour, type and spacing in the templates; T8's furniture follows D5 §3.5; a test greps rendered
HTML for `@import`, `url()` and `<link>` (must be none). What E8 inherits from E7, in order:

1. **The clustered `overall` block and the y-pred-only `overall` first** (cut 1, carried 42):
   the T1 / T8 overall section otherwise renders a typed absence (`overall: null`,
   `overall_not_computed`) on every clustered table and every `y_pred`-only table, while the
   subgroup rows beside it carry the metrics. About forty lines from `proportion_ci` /
   `auroc_ci`, with LR / DOR / F1 / MCC typed `clustered_data_analytic_ci_invalid` (DEC-09), and
   tests; the E7 clustered run test asserts `overall null` today and changes with it.
2. **`jinja2` is not installed and not a dependency** (`ModuleNotFoundError` now; `[project.
   dependencies]` is `numpy`, `pyyaml`, `jsonschema`, `cryptography`). Add it to
   `pyproject.toml` and re-lock `uv.lock` as E7 did for `cryptography` (free; zero cash; `uv` is
   not on `PATH` but `python -m uv` runs, `uv 0.12.13`, pip-installed on day 7 for the purpose);
   `import proofpack` must still work with it hidden — import it inside the renderer.
   `design/tokens.json` does not exist (`design/` holds `conventions_T7.md` and
   `guidance_map_v1.csv`, 32 internal ids per `doctor`) and `resources.py`'s `_CANDIDATES`
   has no `tokens.json` entry: the D5 three-file creation is E8's, with the wheel test.
3. **What the renderer reads** (decision table above): a criteria row's `status` from the
   three-word enum only; `attainable_at_n`, `max_lower_bound_at_n`, `n`, `method`,
   `compared_value`, `detail.attainability_not_computed`, `detail.not_estimable_reason` per
   row for T8 table 5.6 — **rows keyed by position, never by `criterion_id`** (carried 24:
   duplicate ids and `level: "*"` both repeat the id); `manifest.watermark` on every page's
   footer (`LICENCE EXPIRED - not for submission` on grace *and* on no file today — carried 25 /
   needs 5; `TRIAL` on a trial); HTML / DOCX written only on licence `ok` / `grace` (decision
   "After grace"); `calibration: null` beside `calibration_suppressed_reason` (DEC-36); the IPA's
   printed annotation from the row tier (DEC-35); `manifest.ledger_count` and the `ledger` block
   for T8 item 6; `ingest_report.json` beside `run.json` (open question 4).
4. **The verdict grep reaches HTML.** Extend the walk to the rendered document: the three status
   tokens only where T8's criteria table prints them, `pass` / `fail` / `verdict` / `meets` /
   `acceptable` / `consistent` / `unbiased` nowhere; the CLI summary line (carried 38) is the
   one place the vocabulary appears outside the block today. The claims checker's forbidden
   tokens list is `VERDICT_WORDS` plus the brief's seven; "certified", "approved", "endorsed",
   "FDA-cleared" belong on it (no claim of certification anywhere).
5. **Numbers come only from the engine.** The renderer inserts `est`, `ci_lo`, `ci_hi`, `n`,
   `method` from `run.json`; no rounding the engine has not made (lens 3's killed mutant
   `compared_value` rounded to six places is the class); a Number with a typed reason prints
   the reason, never a bare estimate (D1 §4.1); `[unverified]` markings survive to the page.
6. **The FDA AI-DSF guidance is a draft (January 2025, not for implementation)** in the anchor
   labels *and* in the structured `guidance_refs` (empty on today's runs: `guidance_refs []`,
   `suppression_log []` — the assembler's keys, filled by E8).
7. If time is left, in this order: carried items 24 (`id` uniqueness, H08), 20 (`scope`
   required), 21–23 (the three declaration gates) — each an H08 / H09 rule with a literal
   test; then items 1–10 (one observer each). Item 40 (the O:E denominator) and 39 (the level
   in `_row`) touch statistical gates and take a DEC-12(i) lens.
8. **E10** rewires `cmd_compare` (cut 2). **DEC-18 (a)** is still without an E slot.

`git diff --name-only a0c9abc HEAD -- src/proofpack/stats/` names six files (measured now) and
the changes in them are the four named routes (`bootstrap.py`'s non-finite sd branch; the ROC
origin threshold; `no_score_column`; `dev_rows`) and the `resample_sd_reason` pass-through, read
line by line by the lens-1 regression (N8, N12); `Number` is untouched (`git diff a0c9abc HEAD --
src/proofpack/stats/number.py` is 0 bytes, measured now); every day-4/5/6 constant reads as the
E6 note lists them (`DEFAULT_B 2000`, `DEFAULT_SEED 20240101`, `DEFAULT_LEVEL
0.95`, `MIN_UNITS_PER_STRATUM 2`, `MAX_FROZEN_VARIANCE_SHARE 0.2`, `MIN_USABLE_FRACTION 0.90`,
`CLIP_EPS 1e-12` — lens 3, read).

## Needs from Josh

1. **`ci_upper_bound` in the `statistic` enum** (decision 1): a third value beside D1 §2's two,
   so a customer can name the conservative end of a `<=` criterion; keep it (D1 §2 amended) or
   drop it (then a `<=` criterion is point-estimate or the literal lower bound only)?
   Recommendation: keep.
2. **After grace = JSON written + exit 4** (decision "After grace"): the most conservative
   reading of D1 §7's "emit JSON only"; the alternative is exit 0 / 2 with the watermark and no
   documents. Recommendation: keep exit 4 (a pipeline must not read an expired run as clean).
3. **Fairness `statistic` / `comparator` beside `bound`**: D1 §2 lists `bound` alone; the
   engine needs to be told which end and which direction. Recommendation: amend D1.
4. **The ledger counts every run with a `criteria` list, in the per-user directory**, so
   `ledger_count` moves between two otherwise identical runs (F17's history key). If the count
   should live beside the pack instead, say so; the file must not be inside it.
5. **The no-licence watermark** (carried 25): with no licence file the manifest says `LICENCE
   EXPIRED - not for submission`. A second string (`NO LICENCE - not for submission`) keeps the
   manifest true and is a D4 footer decision the E8 renderer then prints. Recommendation: add
   it.
6. **One default path for `map --out` and `run`** (carried 31): DEC-26 says the confirmed
   mapping is "written beside the pack directory"; the build reads `<input>.mapping.json`
   beside the input and `map --out` defaults to `./mapping.json`. Name the one default (or let
   `run`'s H07 line name `map --out <input>.mapping.json`).
7. **Two ledger and licence semantics** (carried 18, 27): count a run that wrote no document,
   or record after the write; refuse a valid licence whose `expires + grace + 24 h` leaves
   Python's `datetime` range (a hand-signed file only), or move the grace sum after the `ok`
   test. One line each; neither is a build until decided.

## Open questions (at most five)

1. A two-sided fairness bound (`|gap| <= b`) needs two criteria today (`ci_upper_bound <= b`
   and `ci_lower_bound >= -b`); should `abs` be a declarable form?
2. Should `run` warn (a W-code) when `cryptography` is missing, beside the typed
   `cryptography_unavailable` refusal that already exits 4?
3. `criteria_results` keeps `metric`, `statistic`, `comparator`, `value` echoes beside D1's five
   keys; T8 can read them or the declarations — which does E8 prefer, and does the CLI summary
   line keep its by-status counts (carried 38)?
4. `ingest_report.json` is still written beside `run.json` (lane A's tests read it); drop it
   once E8 renders T8, or keep it as the aggregate-only quick read?
5. The `Unknown/missing` row is excluded from `level: "*"` (named explicitly to include);
   include it and let the customer exclude, or keep as built?

## Sentences refused

Aggregated from the build note, the two repair notes and the six lens notes; each is a sentence
that was not written because its counter-example ran, or because no counter-example was
constructed.

- "criterion passes / fails" anywhere — the three status words only, and only under
  `criteria_results`; `pass`, `fail`, `verdict` occur in no key, reason code or string of the
  document (`test_the_status_words_appear_only_under_criteria_results_and_no_verdict_word_anywhere`
  and its `run` twin walk the documents they are run on).
- "a criterion on a Number without an interval is never `not_met`" — at `ab729d3` it was
  `not_met` on `point_estimate` (B1); the shipped sentence now says what `_row` does (routes on
  `not_estimable_reason` / `method` first) and cites the two test ids; the lenses measured nine
  such rows from `run`, nine rows, not a class.
- "`attainable_at_n: false` is exact under clustering" and "cannot be met by any outcome at that
  `n`, whatever the model does" — deleted; the docstring states the measured cluster-bootstrap
  counter-example (`ci_lo 0.9` above `0.8865` at n = 30) and the rule (`wilson` only).
- "no `met` row can carry `attainable_at_n: false`" — not written as a property: 115 documents
  walked by lens 3 and every Number `proportion` builds at 0.95 hold it; one hand-built Wilson
  Number at `ci_level 0.80` produces it (carried 39). The invariant helper is applied to the four
  documents the tests build and is named as such.
- "on any other method both are `null` and `detail.attainability_not_computed` is
  `method_not_wilson`" as a sentence about every Number — false at n = 0 at `02d00c5`; rewritten
  to the guard the code has, and the `wilson`-with-non-int-`n` case stated as "the guard as
  written", not defended.
- "the annotation is now complete" / "every unfilled attainability field is annotated" — a
  `wilson` Number with a non-int `n` is unannotated by the guard as written (five such `n`
  measured), and the run route was measured on one construction (the S3 all-negative site).
- "no such Number is built by `stats.bootstrap.proportion_ci`" — drafted into the `criteria.py`
  docstring and removed: it generalises over every route that builds a Number.
- "`verify` raises nothing on bad input" / "`verify` never raises" — deleted; five, then six,
  overflow payloads were constructed and refused; lens 2's N6 shows the refusal branches have
  edges nobody has enumerated; the module docstring lists the inputs the tests feed.
- "the sixth payload was new" / "the first five of those six raised at `ab729d3`" as a contrast
  — all six raised there (carried 11).
- "`expires_unparsable` with `detail.field`" over every out-of-range date — split into the two
  `parse_utc` refusals (`detail` empty) and the three sums (`detail.field` names the term
  summed, not the extreme value).
- "three truncations" — two truncations and one same-length replacement, named.
- "`assemble_run` writes nothing" — it writes `<PROOFPACK_HOME>/ledger.json`; the test walks
  recursively now.
- "F17: byte-identical except three keys" / "the manifest keys that differed were `run_id`,
  `started`, `duration_s` and `ledger_count`" / "F17 holds" — replaced by what the test asserts
  (`run_id` and `ledger_count` differ, nothing outside the four, `acceptance_runs` 1 then 2,
  three hashes equal); each lens lists its own pairs as pairs.
- "the ledger counts every acceptance run" — the race (carried 16), the fairness-only run
  (17) and the aborted runs (18).
- "the licence is valid" on the CLI — `status: ok (valid)` prints the typed result; the `ok`
  word never enters the document (the manifest carries `licence_id`, `tier`, `watermark`
  only).
- "the engine verifies production licences" — only ephemeral-key files, the shipped key's
  bytes and id, the site's `verify_licence.py` and its `licence.mjs` under an ephemeral seed
  were measured; no file the Cloudflare function signed exists off the issuer.
- "`verify` accepts exactly what D1 §7 says" — `issued` after `expires`, `models 0` and a
  31-day trial (`issued + 30 d + 24 h` skew) are accepted and D1 does not speak to them.
- "the licence check is unchanged" (repair 1) — three refusal branches were added; DEC-12(i)
  applied and the round-2 lenses attacked them.
- "the suite proves `--offline` opens no socket" — one test makes two `socket` names raise on
  one command, the lenses and this note replace four names on two commands, and a grep finds no
  network import; that is what was inspected.
- "`run` writes nothing outside `--out`" — it writes the ledger file.
- "the engine applies the conservative bound for a `<=` criterion" — refused; the statistic is
  read as named (decision 1).
- "criterion not attainable: increase n to …" — no sample-size advice; the row carries the bound
  and `n`, nothing else.
- "the criteria engine emits the right status on every input" — 95 criteria files and 33
  hand-built Numbers were fed; lens 3's N3 shapes show the routing depends on invariants of the
  Number builders, not of `_row` alone.
- "no non-finite float can reach a run" / "no `inf` can reach the output" — carried 40 is an
  `inf` O:E on a denormal score column, refused by `canonical_json` with exit 5 and no file.
- "the attainability figure is exact" — exact against the lenses' own Wilson for n = 1..500 at
  four levels (0.0); it is the figure at `evaluate`'s level, not the Number's (carried 39).
- "the mutation sweep is adequate" / "shows the tests have teeth" — 25 of 25 declared mutants
  die; 2 of lens 1's 24, 6 of lens 2's 25 and 7 of lens 3's 22 planted mutants survive at the
  tree each attacked (carried items 1–8).
- "every new test pins the repair" — repair 1's RG-N1 and RG-N2 tests and repair 2's F17 and
  `-05:00` params pass on the pre-fix tree by design (sentence repairs); the repair notes and the
  regression lenses say which.
- "the day-7 tests cover every gate the lenses named" — carried items 1–10 and 20–24 are gates
  or routes no test feeds.
- "the shipped mapping default follows DEC-26" — DEC-26 says "beside the pack directory"; the
  build reads beside the input; carried 31 and needs 6.
- "`resample_sd_reason` is pinned by the schema" — it is pinned by `tests/test_bootstrap_carried.py`
  only (carried 33).

In this note: "the 43 items are harmless" was not written — item 40 is an exit 5 on a
constructible table, item 16 an undercount a CI pipeline can hit, items 1–10 behaviours a
regression would ship green; "the lenses found nothing" was not written for round 3 — they
found nine and five items and they are carried above; "E7 is complete" was not written — cut 1
leaves `overall` null on every clustered and y-pred-only run until E8 builds it.
