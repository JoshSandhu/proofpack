# A-P3: written to admit the two inputs the Dockerfile copies, requirements.lock and
# dist/*.whl (tests/test_dockerfile.py reads these lines). GitHub Actions run 36005620750
# (24 September 2026) built the image with this file.
*
!requirements.lock
!dist
dist/*
!dist/*.whl
